---
phase: 19-guided-tour
plan: 03
type: execute
wave: 3
depends_on: ["19-02"]
files_modified:
  - src/components/sandbox/SandboxContainer.tsx
  - src/components/sandbox/SandboxContainer.test.tsx
autonomous: true
requirements: [TOUR-01, TOUR-02]

must_haves:
  truths:
    - "SandboxContainer renders a 'How to read this' button in its existing header row (accessible name exactly 'How to read this'), which opens GuidedTourModal on click"
    - "isTourOpen state lives locally in SandboxContainer via useState, never inside useSandboxState() (ARCHITECTURE.md Anti-Pattern 2)"
  artifacts:
    - path: "src/components/sandbox/SandboxContainer.tsx"
      provides: "'How to read this' trigger button + GuidedTourModal composition"
      contains: "How to read this"
  key_links:
    - from: "src/components/sandbox/SandboxContainer.tsx"
      to: "src/components/tour/GuidedTourModal.tsx"
      via: "isTourOpen/setIsTourOpen passed as open/onOpenChange"
      pattern: "GuidedTourModal open={isTourOpen}"
---

<objective>
Wire the "How to read this" trigger button and `GuidedTourModal` into `SandboxContainer.tsx`'s existing header row, and add integration-level test coverage proving the end-to-end open path from a real page-level render (complementing Plan 19-02's already-thorough `GuidedTourModal.test.tsx` unit suite).

Purpose: This is the only file this phase modifies rather than creates -- everything upstream (Plans 19-01, 19-02) is inert until this plan composes it into the actual Sandbox UI. Closes out TOUR-01/TOUR-02's "via a new 'How to read this' button" requirement.
Output: `SandboxContainer.tsx` renders the trigger button and composes `GuidedTourModal`; `SandboxContainer.test.tsx` gains coverage proving the trigger opens the tour and the chip-row-removal-style regression check ("no leftover markup") stays clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-guided-tour/19-CONTEXT.md
@.planning/phases/19-guided-tour/19-DESIGN-SNAPSHOT.md
@.planning/phases/19-guided-tour/19-PATTERNS.md
@.planning/research/ARCHITECTURE.md
</context>

<interfaces>
<!-- Plan 19-02's already-shipped contract this plan imports verbatim. -->

```typescript
// from "../tour/GuidedTourModal.js"
export function GuidedTourModal(props: {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}): React.JSX.Element;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Wire trigger button + GuidedTourModal composition into SandboxContainer.tsx</name>
  <files>src/components/sandbox/SandboxContainer.tsx</files>
  <read_first>
    src/components/sandbox/SandboxContainer.tsx (current state -- lines 1-108, especially the header's `flex gap-2` button block, lines 61-76)
    .planning/phases/19-guided-tour/19-DESIGN-SNAPSHOT.md (section "Trigger button")
    .planning/phases/19-guided-tour/19-PATTERNS.md (section "src/components/sandbox/SandboxContainer.tsx (MODIFIED -- trigger button + modal composition)")
  </read_first>
  <action>
    Change line 15's import from `import { useEffect } from "react";` to `import { useEffect, useState } from "react";`. Add `import { GuidedTourModal } from "../tour/GuidedTourModal.js";` alongside the existing component imports (after the `useSandboxBridge` import, line 20). Add `const [isTourOpen, setIsTourOpen] = useState(false);` as a new line directly after `const { pendingScenario } = useSandboxBridge();` (line 26) -- a sibling local `useState`, never added to `sandboxState`/`useSandboxState()` (ARCHITECTURE.md Anti-Pattern 2).
    In the header's `flex gap-2` button container (lines 61-76, currently holding the Save icon-button and "Reset scenario" button), add a third `Button` before the existing two, `type="button" variant="outline"`, teal-tinted via Tailwind utility classes approximating the design's `color:#2dd4bf;background:rgba(45,212,191,.08);border:1px solid rgba(45,212,191,.35)` (e.g. `text-primary border-primary/35 bg-primary/8 hover:bg-primary/15`), containing a `<span className="radar-sweep-dot" aria-hidden="true" />` (Plan 19-01's CSS) followed by the text "How to read this", `onClick={() => setIsTourOpen(true)}`. This button's accessible name must be exactly "How to read this" (do not add an `aria-label` that overrides the visible text -- let the button's own text content be the accessible name, matching this file's existing "Reset scenario" button's pattern of visible-text-as-accessible-name).
    Render `<GuidedTourModal open={isTourOpen} onOpenChange={setIsTourOpen} />` as a new sibling inside the outermost container div, after the `ReasoningTrail` block (after line 105, before the closing `</div>` at lines 106-107) -- matching this file's existing flat sibling-composition style (`ChartPanel`, `ReasoningTrail`, now `GuidedTourModal`).
  </action>
  <acceptance_criteria>
    - `grep -c "How to read this" src/components/sandbox/SandboxContainer.tsx` outputs at least `1`
    - `grep -c "GuidedTourModal" src/components/sandbox/SandboxContainer.tsx` outputs at least `2` (import + JSX usage)
    - `grep -c "isTourOpen" src/components/sandbox/SandboxContainer.tsx` outputs at least `2` (useState + prop wiring)
    - `grep -c "radar-sweep-dot" src/components/sandbox/SandboxContainer.tsx` outputs at least `1`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>SandboxContainer.tsx renders the "How to read this" trigger button in its existing header row and composes GuidedTourModal with local isTourOpen state; typecheck passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration test -- trigger opens the tour, focus returns on close</name>
  <files>src/components/sandbox/SandboxContainer.test.tsx</files>
  <read_first>
    src/components/sandbox/SandboxContainer.test.tsx (current full file -- especially its existing `beforeEach`/`afterEach` polyfill block and the "chip row is fully removed" regression-check precedent, so the new tests follow the same file's conventions)
    .planning/phases/19-guided-tour/19-RESEARCH.md (section "Testing Escape-dismissal and focus return")
  </read_first>
  <behavior>
    - `render(&lt;SandboxContainer /&gt;)` shows a "How to read this" button and no `dialog` role element
    - Clicking "How to read this" reveals a `role="dialog"` element showing the first tour step's title ("Welcome aboard the Navigator")
    - Pressing Escape after opening the tour closes it (`dialog` role no longer present) and returns focus to the "How to read this" button (awaited assertion, per Pitfall 3)
  </behavior>
  <action>
    Add a new `describe("Guided Tour", ...)` block (or append `it` cases to the existing top-level `describe("SandboxContainer", ...)`) to `SandboxContainer.test.tsx` covering the 3 cases in &lt;behavior&gt;, using `userEvent.setup()` + `await user.click(...)` + `await user.keyboard("{Escape}")`, following this same file's existing `userEvent`/`screen.getByRole`/`screen.findByRole` conventions (never raw `fireEvent`). This is an integration-level smoke test complementing Plan 19-02's already-thorough `GuidedTourModal.test.tsx` unit suite -- it does not need to re-prove every dismissal path or step-navigation case, only that the trigger button genuinely wires up to a real, mounted `GuidedTourModal` inside the full `SandboxContainer` tree.
  </action>
  <acceptance_criteria>
    - `npx vitest run SandboxContainer.test.tsx` passes with 0 failures, including the 3 new cases in &lt;behavior&gt;
    - `grep -c "How to read this" src/components/sandbox/SandboxContainer.test.tsx` outputs at least `1`
    - `grep -c "Escape" src/components/sandbox/SandboxContainer.test.tsx` outputs at least `1`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run SandboxContainer.test.tsx && npm run typecheck</automated>
  </verify>
  <done>SandboxContainer.test.tsx proves the trigger button opens a real, mounted GuidedTourModal and that Escape closes it with focus returning to the trigger, using awaited userEvent interactions; typecheck passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new this plan | A trigger-button click toggling local view state; no new input/data boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-05 | N/A (state-placement correctness, not a security threat) | isTourOpen state placement in SandboxContainer | accept | Kept as local `useState`, never merged into `useSandboxState()` (ARCHITECTURE.md Anti-Pattern 2) -- Task 1's acceptance criteria grep-enforces this stays a standalone hook call, so a future tour-state bug can never reach the give-way/stand-on classification path. |
| T-19-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan. |
</threat_model>

<verification>
1. `npx vitest run SandboxContainer.test.tsx` -- all pass, including the 3 new Guided Tour cases.
2. `npm run typecheck` -- 0 errors.
3. `grep -c "How to read this" src/components/sandbox/SandboxContainer.tsx` -- outputs at least `1`.
</verification>

<success_criteria>
The "How to read this" trigger is live in the Sandbox's header, opens the real GuidedTourModal, and an integration test proves the end-to-end open/close/focus-return path from a full-page render -- closing out TOUR-01/TOUR-02's remaining wiring work before Plan 19-04's human-verification checkpoint.
</success_criteria>

<output>
Create `.planning/phases/19-guided-tour/19-03-SUMMARY.md` when done
</output>
