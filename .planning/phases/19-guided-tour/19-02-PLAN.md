---
phase: 19-guided-tour
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - src/components/tour/TourStepIllustration.tsx
  - src/components/tour/TourStepIllustration.test.tsx
  - src/components/tour/GuidedTourModal.tsx
  - src/components/tour/GuidedTourModal.test.tsx
autonomous: true
requirements: [TOUR-01, TOUR-02]

must_haves:
  truths:
    - "TourStepIllustration renders a distinct, per-step inline SVG (viewBox 0 0 460 176) for each of the 6 step indices 0-5, as plain JSX (no dangerouslySetInnerHTML)"
    - "GuidedTourModal opens via its open prop, shows TOUR_STEPS[step]'s title/body/points plus the matching illustration, and resets to step 0 whenever it (re)opens"
    - "The forward button reads 'Next' on steps 0-4 and 'Start exploring' on step 5 (D-02); clicking it on step 5 closes the tour instead of advancing past index 5"
    - "Back is absent when step===0 and present otherwise"
    - "Escape, an outside pointerdown, and the Skip button all close the tour via onOpenChange(false), and focus returns to whatever element had focus before the Dialog opened (Radix's default onCloseAutoFocus), proven with an awaited userEvent assertion (Pitfall 3)"
  artifacts:
    - path: "src/components/tour/TourStepIllustration.tsx"
      provides: "Per-step inline SVG illustration switched by step index 0-5"
      contains: "export function TourStepIllustration"
    - path: "src/components/tour/GuidedTourModal.tsx"
      provides: "Controlled Dialog composition: step state, Back/Next/Skip, step dots, D-02 forward-button label"
      contains: "Start exploring"
  key_links:
    - from: "src/components/tour/GuidedTourModal.tsx"
      to: "src/components/ui/dialog.tsx"
      via: "Dialog/DialogContent/DialogTitle/DialogDescription import"
      pattern: "from \"@/components/ui/dialog"
    - from: "src/components/tour/GuidedTourModal.tsx"
      to: "src/components/tour/guided-tour-steps.ts"
      via: "TOUR_STEPS[step] lookup"
      pattern: "TOUR_STEPS\\["
    - from: "src/components/tour/GuidedTourModal.tsx"
      to: "src/components/tour/TourStepIllustration.tsx"
      via: "TourStepIllustration step prop"
      pattern: "<TourStepIllustration"
---

<objective>
Build the tour's two remaining new components -- `TourStepIllustration` (the 6 per-step inline SVGs) and `GuidedTourModal` (the controlled Dialog composition consuming Plan 19-01's `dialog.tsx` and `TOUR_STEPS`) -- each fully self-contained and unit-tested, with no wiring into `SandboxContainer.tsx` yet. Nothing from this plan is imported anywhere else after it lands; Plan 19-03 does the wiring.

Purpose: Delivers the tour's actual visual/behavioral content for TOUR-01/TOUR-02 as an isolated, independently-verifiable unit, proving in an automated test that every one of ROADMAP's Phase 19 success criteria 2-5 (Back/Next nav, Skip/Escape/outside-click dismissal, "Start exploring" closing the tour, focus-return) genuinely holds against this codebase's actual Dialog primitive -- not just asserted from the design source's own (materially gap-riddled) prototype.
Output: `TourStepIllustration.tsx` and `GuidedTourModal.tsx`, each with a passing test file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-guided-tour/19-CONTEXT.md
@.planning/phases/19-guided-tour/19-DESIGN-SNAPSHOT.md
@.planning/phases/19-guided-tour/19-PATTERNS.md
@.planning/phases/19-guided-tour/19-RESEARCH.md
@.planning/research/PITFALLS.md
</context>

<interfaces>
<!-- Plan 19-01's already-shipped contracts these components import, verbatim. -->

```typescript
// from "@/components/ui/dialog"
export function Dialog(props: React.ComponentProps<typeof DialogPrimitive.Root>): React.JSX.Element;
export function DialogContent(props: React.ComponentProps<typeof DialogPrimitive.Content>): React.JSX.Element;
export function DialogTitle(props: React.ComponentProps<typeof DialogPrimitive.Title>): React.JSX.Element;
export function DialogDescription(props: React.ComponentProps<typeof DialogPrimitive.Description>): React.JSX.Element;

// from "./guided-tour-steps.js"
export interface TourStepPoint { tag: string; text: string; }
export interface TourStep { id: number; icon: string; title: string; body: string; points: TourStepPoint[]; }
export const TOUR_STEPS: TourStep[]; // exactly 6 entries, id 0..5
```

<!-- This plan's own new contract -- Plan 19-03's SandboxContainer.tsx wiring imports this verbatim. -->

```typescript
// src/components/tour/GuidedTourModal.tsx
export function GuidedTourModal(props: {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}): React.JSX.Element;

// src/components/tour/TourStepIllustration.tsx
export function TourStepIllustration(props: { step: number }): React.JSX.Element; // step: 0-5
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TourStepIllustration.tsx -- 6 per-step inline SVGs</name>
  <files>src/components/tour/TourStepIllustration.tsx, src/components/tour/TourStepIllustration.test.tsx</files>
  <read_first>
    .planning/phases/19-guided-tour/19-DESIGN-SNAPSHOT.md (section "Per-step illustrations (tourViz, 6 inline SVGs...)", lines 76-137, the full verbatim SVG source for all 6 steps)
    src/components/sandbox/chart/VesselGroup.tsx (plain-JSX SVG composition + per-group WHY-comment convention)
    src/components/hero/HeroPreviewCard.tsx (per-instance sub-component pattern for distinct visual units)
    .planning/phases/19-guided-tour/19-RESEARCH.md (section "Pattern 3: Step-switch presentation, not step-switch data", and "Anti-Patterns to Avoid" re: dangerouslySetInnerHTML)
  </read_first>
  <behavior>
    - `render(&lt;TourStepIllustration step={0} /&gt;)` renders a radar scope: two vessel-wedge `&lt;path&gt;` elements (fill `#EF4444` and `#22C55E`), a dashed relative-bearing `&lt;line&gt;`, and a "RULE 15 · CROSSING" verdict-pill `&lt;text&gt;`
    - `render(&lt;TourStepIllustration step={1} /&gt;)` renders a hull outline with "DRAG HULL" and "ROTATE BOW" `&lt;text&gt;` labels
    - `render(&lt;TourStepIllustration step={2} /&gt;)` renders 4 instrument tiles with `&lt;text&gt;` content "RANGE"/"1.92 NM", "BEARING"/"045°", "CPA"/"0.28 NM", "TCPA"/"04:12"
    - `render(&lt;TourStepIllustration step={3} /&gt;)` renders "CROSSING · RULE 15 · VESSEL A GIVES WAY", "Vessel A"/"Vessel B" labels, and "GIVE WAY"/"STAND ON" badge text
    - `render(&lt;TourStepIllustration step={4} /&gt;)` renders "NAV DECISION CHAIN" plus 4 chained node labels "RULE 7"/"RULE 15"/"RULE 18"/"VERDICT"
    - `render(&lt;TourStepIllustration step={5} /&gt;)` renders 6 scenario-tile labels: "CROSSING", "HEAD-ON", "OVERTAKE", "SAILING", "NUC", "IN DOUBT"
    - Every step's root `&lt;svg&gt;` carries `viewBox="0 0 460 176"`
    - No step's markup is produced via `dangerouslySetInnerHTML` (grep-verified, not just visually reviewed)
  </behavior>
  <action>
    Create one small named sub-component per step (e.g. `WelcomeIllustration`, `MoveVesselsIllustration`, `InstrumentsIllustration`, `VerdictIllustration`, `ReasoningIllustration`, `GalleryIllustration`), each translating one of `19-DESIGN-SNAPSHOT.md`'s 6 raw SVG template literals into plain JSX -- `class` to `className`, self-closing tags, the design's `${...}.map().join('')` interpolations (e.g. the instrument-tile loop, the decision-chain node loop, the scenario-tile grid loop) become real `.map(...)` calls returning JSX elements with a `key` prop, no semantic change to positions/colors/text. Export a single `TourStepIllustration({ step }: { step: number })` that switches on `step` (0-5) and renders the matching sub-component -- do NOT force all 6 into one generic parameterized illustration (they are not near-identical; CLAUDE.md's "no duplicated JSX" rule applies in reverse here, per RESEARCH.md Pattern 3). Every `&lt;svg&gt;` keeps `viewBox="0 0 460 176"` and `font-family="'Geist Mono',monospace"` exactly as the design source. Colors stay the design's exact raw hex literals (`#EF4444`, `#22C55E`, `#2dd4bf`, `#0B0B0E`, etc.) -- these are fixed-at-authoring-time illustration content, matching `hero-preview-geometry.ts`'s own precedent of hardcoded hex for a verdict fixture, NOT the "no raw CSS strings" convention (that convention targets component chrome/animation built as JS template-literal strings, not literal SVG fill/stroke attribute values). Add a short WHY comment above each sub-component noting which design-snapshot step number it ports (per `VesselGroup.tsx`'s existing per-group comment convention).
  </action>
  <acceptance_criteria>
    - `npx vitest run TourStepIllustration.test.tsx` passes with 0 failures covering every case in &lt;behavior&gt;
    - `grep -c "export function TourStepIllustration" src/components/tour/TourStepIllustration.tsx` outputs `1`
    - `grep -c "dangerouslySetInnerHTML" src/components/tour/TourStepIllustration.tsx` outputs `0`
    - `grep -c "viewBox=\"0 0 460 176\"" src/components/tour/TourStepIllustration.tsx` outputs `6`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run TourStepIllustration.test.tsx && npm run typecheck</automated>
  </verify>
  <done>TourStepIllustration renders all 6 distinct step illustrations exactly per &lt;behavior&gt;, as plain JSX with zero dangerouslySetInnerHTML; typecheck passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GuidedTourModal.tsx -- controlled Dialog composition (D-01, D-02, TOUR-01, TOUR-02)</name>
  <files>src/components/tour/GuidedTourModal.tsx, src/components/tour/GuidedTourModal.test.tsx</files>
  <read_first>
    src/components/sandbox/chart/VesselOverlayCard.tsx (floating-card typed-props composition pattern)
    src/components/sandbox/chart/VesselOverlayCard.test.tsx (jsdom pragma + ResizeObserver polyfill + userEvent interaction pattern to mirror)
    src/components/ui/button.tsx (outline/ghost/default variants -- Back=outline, Skip=ghost, forward=default, no new variant needed)
    .planning/phases/19-guided-tour/19-DESIGN-SNAPSHOT.md (sections "Modal structure", "Footer controls")
    .planning/phases/19-guided-tour/19-RESEARCH.md (section "Code Examples: Radix Dialog controlled composition with close-on-last-step", and "Common Pitfalls" Pitfall 3 -- await user-event interactions before asserting document.activeElement)
    .planning/research/ARCHITECTURE.md (Anti-Pattern 2 -- isTourOpen/step state stays local, never in useSandboxState())
  </read_first>
  <behavior>
    - Renders closed (no `dialog` role in the document) when `open` is `false`
    - `open` becomes `true` -&gt; a `role="dialog"` element appears, showing `TOUR_STEPS[0]`'s title/body/points and `TourStepIllustration step={0}`; no "Back" button present
    - Clicking "Next" advances through steps 1, 2, 3, 4 in order, each time showing that step's title/body/points/illustration and a "Back" button
    - On step 5 (the last), the forward button's accessible name is exactly "Start exploring" (not "Next", not "Done") -- clicking it calls `onOpenChange(false)` instead of advancing to a non-existent step 6
    - Clicking "Back" from any step &gt; 0 returns to the previous step's content
    - Clicking "Skip tour" calls `onOpenChange(false)` immediately regardless of current step
    - Pressing Escape while open calls `onOpenChange(false)` (Radix default) and, after the interaction is awaited, focus returns to the element that had focus before the dialog opened
    - A pointerdown outside the dialog content calls `onOpenChange(false)` (Radix default `onPointerDownOutside`)
    - Re-opening the modal after a previous session advanced past step 0 resets back to step 0 (verified by opening, advancing to step 2 via "Next" x2, closing, reopening, and asserting step 0's title is shown again)
    - Exactly 6 step-dot indicator elements are always rendered, regardless of current step
  </behavior>
  <action>
    Create `GuidedTourModal({ open, onOpenChange }: { open: boolean; onOpenChange: (open: boolean) => void })` owning local `const [step, setStep] = useState(0)`, reset via a `useEffect` keyed on `open` (`if (open) setStep(0)`) -- per ARCHITECTURE.md's Anti-Pattern 2, this state is local to this component, never lifted into `useSandboxState()`. Compose `Dialog` (open/onOpenChange passthrough) -&gt; `DialogContent` -&gt; header row (a "GUIDED TOUR · STEP {step + 1}/6" label using the `.radar-sweep-dot` span from Plan 19-01's globals.css, plus a "Skip tour" `Button` with `variant="ghost"` calling `onOpenChange(false)`) -&gt; an icon chip + `DialogTitle` set to `TOUR_STEPS[step].title` (satisfies Radix's built-in accessible-name requirement directly from the step content, no extra visually-hidden title node needed) -&gt; `TourStepIllustration step={step}` -&gt; `DialogDescription` set to `TOUR_STEPS[step].body` -&gt; up to 3 tag/text point rows from `TOUR_STEPS[step].points` -&gt; footer row: "Back" `Button` `variant="outline"` (rendered only when `step > 0`, `onClick={() => setStep((s) => s - 1)}`), exactly 6 step-dot `span`s (active dot wider/teal via `bg-primary`, completed dots via `bg-rule-accent`, upcoming dots via a muted/border token -- express via existing Tailwind color tokens, not raw hex, since the design's literal `#3F3F46` has no exact token match in this app's palette), and the forward `Button` `variant="default"` whose accessible name is `step === TOUR_STEPS.length - 1 ? "Start exploring" : "Next"` (D-02), `onClick` calling a `handleForward()` that either `setStep((s) => s + 1)` or `onOpenChange(false)` when already on the last step. Import `Button` from `@/components/ui/button`, `Dialog`/`DialogContent`/`DialogTitle`/`DialogDescription` from `@/components/ui/dialog`, `TOUR_STEPS` from `./guided-tour-steps.js`, `TourStepIllustration` from `./TourStepIllustration.js`. Add `"use client"` at the top of the file (matches `SandboxContainer.tsx`/`VesselOverlayCard.tsx`'s existing convention for interactive client components).
  </action>
  <acceptance_criteria>
    - `npx vitest run GuidedTourModal.test.tsx` passes with 0 failures covering every case in &lt;behavior&gt;
    - `grep -c "export function GuidedTourModal" src/components/tour/GuidedTourModal.tsx` outputs `1`
    - `grep -c "Start exploring" src/components/tour/GuidedTourModal.tsx` outputs at least `1`
    - `grep -c "useSandboxState" src/components/tour/GuidedTourModal.tsx` outputs `0` (tour state never touches the shared sandbox state hook, per ARCHITECTURE.md Anti-Pattern 2)
    - `grep -c "radar-sweep-dot" src/components/tour/GuidedTourModal.tsx` outputs at least `1`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run GuidedTourModal.test.tsx && npm run typecheck</automated>
  </verify>
  <done>GuidedTourModal implements every behavior in &lt;behavior&gt; against the real Radix Dialog primitive (not a hand-rolled overlay), proven by an automated test that awaits userEvent interactions before asserting focus (Pitfall 3); "Start exploring" (not "Done") is the exact final-step label per D-02; typecheck passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new this plan | Illustration and modal render only the static `TOUR_STEPS` content plus Radix's own internal open/focus state -- no user-supplied input, no new data boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | Tampering (XSS) | TourStepIllustration.tsx | mitigate | Ported as plain JSX, never `dangerouslySetInnerHTML` (Task 1's acceptance criteria grep-enforces zero occurrences) -- eliminates any HTML-injection surface even though this illustration content is developer-authored, not attacker-controlled. |
| T-19-04 | Denial of Use | GuidedTourModal's dismissal paths (Escape / outside-click / Skip / "Start exploring") | mitigate | Delegated entirely to Radix Dialog's native `onOpenChange`/`FocusScope`/`onCloseAutoFocus` defaults rather than hand-rolled keyboard/pointer listeners that could fail to release the trap -- directly closes the gap CONTEXT.md's D-01 identifies in the design source's own prototype (zero Escape handler, zero outside-click handler). |
| T-19-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan. |
</threat_model>

<verification>
1. `npx vitest run TourStepIllustration.test.tsx GuidedTourModal.test.tsx` -- all pass.
2. `npm run typecheck` -- 0 errors.
3. `grep -c "dangerouslySetInnerHTML" src/components/tour/TourStepIllustration.tsx` -- outputs `0`.
4. `grep -c "useSandboxState" src/components/tour/GuidedTourModal.tsx` -- outputs `0`.
</verification>

<success_criteria>
Both new components exist, are independently unit-tested against Plan 19-01's real Dialog primitive and data module, and prove every one of ROADMAP's Phase 19 success criteria 2-5 in an automated test (step nav, final-step label/close, all 3 dismissal paths, focus return). Nothing is wired into SandboxContainer yet -- that happens in Plan 19-03.
</success_criteria>

<output>
Create `.planning/phases/19-guided-tour/19-02-SUMMARY.md` when done
</output>
