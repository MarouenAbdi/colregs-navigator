---
phase: 19-guided-tour
plan: 04
type: execute
wave: 4
depends_on: ["19-03"]
files_modified: []
autonomous: false
requirements: [TOUR-01, TOUR-02]

must_haves:
  truths:
    - "User can click 'How to read this' to open the 6-step tour modal, each step showing an illustration and step-dot progress (ROADMAP criterion 1)"
    - "User can navigate Back/Next; no Back on step 1; 'Start exploring' replaces 'Next' on the final step and closes the tour (ROADMAP criterion 2)"
    - "User can dismiss via Escape, clicking outside, or Skip/Start exploring -- every path closes the tour (ROADMAP criterion 3)"
    - "After the tour closes by any dismissal path, keyboard focus visibly returns to the 'How to read this' trigger button -- human-verified via real Tab-key navigation, not just an automated assertion (ROADMAP criterion 4)"
    - "The tour modal's stacking order does not visually collide with the on-chart VesselOverlayCard, checked in a session where both could plausibly be open (ROADMAP criterion 5)"
  artifacts: []
  key_links: []
---

<objective>
Human-verify, in a real browser, that all 5 ROADMAP Phase 19 success criteria hold end-to-end -- something jsdom-based automated tests (Plans 19-01 through 19-03) structurally cannot observe: real Tab-key focus routing after the Dialog closes, and real CSS stacking order between the Portal-rendered tour modal and the in-flow on-chart `VesselOverlayCard`.

Purpose: Per this project's own documented precedent (STATE.md: "every v1.4 phase's success criteria include an explicit human-browser-verification item... jsdom cannot observe real pointer-capture routing, focus-trap escape, or CSS stacking order") and RESEARCH.md's Environment Availability table (ROADMAP criteria 4 and 5 are explicitly marked "cannot be automated away"), this plan is the mandatory human sign-off gate for Phase 19.
Output: Human confirmation that all 5 ROADMAP Phase 19 success criteria are satisfied in a real browser session, closing out the phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/19-guided-tour/19-CONTEXT.md
@.planning/phases/19-guided-tour/19-DESIGN-SNAPSHOT.md
@.planning/research/PITFALLS.md
@.planning/phases/19-guided-tour/19-01-SUMMARY.md
@.planning/phases/19-guided-tour/19-02-SUMMARY.md
@.planning/phases/19-guided-tour/19-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human-verify the tour's open/nav/content path (TOUR-01, ROADMAP criteria 1-2)</name>
  <files>None -- verification-only task, no files modified</files>
  <read_first>
    .planning/phases/19-guided-tour/19-DESIGN-SNAPSHOT.md (sections "Step content", "Footer controls")
    .planning/phases/19-guided-tour/19-02-SUMMARY.md
  </read_first>
  <action>
    Start the dev server and manually drive the tour open -&gt; step through all 6 steps -&gt; confirm content/illustration/step-dot/button-label fidelity at each step.
  </action>
  <what-built>
    A "How to read this" trigger button in the Sandbox header opens a 6-step `GuidedTourModal` (Radix Dialog-backed), each step rendering its own inline SVG illustration, title, body copy, and up to 3 tag/text points, with a 6-dot step-progress indicator and Back/Next/Skip controls.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open `http://localhost:3000/`. Confirm a "How to read this" button is visible in the Sandbox's header row, with the radar-sweep-dot accent visibly spinning.
    2. Click it -- confirm a modal opens showing step 1 ("Welcome aboard the Navigator"), its radar-scope illustration, and 6 step dots with the first one highlighted; confirm NO "Back" button is visible on step 1.
    3. Click "Next" 4 times -- confirm each step (2 "Move the vessels", 3 "Read the instruments", 4 "Read the verdict", 5 "Follow the reasoning") shows its own distinct illustration, title, body, and points, and that a "Back" button is now visible.
    4. Click "Back" once -- confirm it returns to the previous step's content.
    5. Click "Next" until reaching step 6 ("Start from a classic") -- confirm the forward button's label reads "Start exploring" (not "Next", not "Done") and the step-dot indicator shows all 6 dots with the 6th active.
    6. Click "Start exploring" -- confirm the tour closes.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 checks pass as expected, or describe the specific step/behavior that differs.</resume-signal>
  <acceptance_criteria>
    - Human types "approved" after confirming all 6 steps behave as expected
    - Every one of the 6 steps shows a visually distinct illustration and correct copy (behavior assertion, ROADMAP criterion 1)
    - Back is absent on step 1 and present on steps 2-6; "Start exploring" (not "Next"/"Done") appears only on step 6 and closes the tour (behavior assertion, ROADMAP criterion 2, D-02)
  </acceptance_criteria>
  <verify>
    <automated>MISSING -- this is a human-browser-verification checkpoint; Plan 19-02's GuidedTourModal.test.tsx already automates the underlying step-nav/label logic in jsdom, but full visual/illustration fidelity to the design snapshot requires human review</automated>
  </verify>
  <done>Human has typed "approved", confirming the tour's 6-step content, illustrations, step-dot progress, and Back/Next/"Start exploring" labeling all match the design snapshot.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify all 3 dismissal paths + real Tab-key focus return (TOUR-02, ROADMAP criteria 3-4)</name>
  <files>None -- verification-only task, no files modified</files>
  <read_first>
    .planning/phases/19-guided-tour/19-CONTEXT.md (D-01)
    .planning/phases/19-guided-tour/19-03-SUMMARY.md
  </read_first>
  <action>
    Manually drive all 3 dismissal paths (Escape, outside-click, Skip) from different starting steps, and confirm keyboard focus visibly lands back on the trigger button using real Tab-key navigation each time -- not just automated `document.activeElement` inspection.
  </action>
  <what-built>
    Radix Dialog's native `onEscapeKeyDown`, `onPointerDownOutside`, and `onCloseAutoFocus` defaults provide all 3 dismissal paths plus focus-return with zero hand-rolled event-listener code -- closing the exact gap CONTEXT.md's D-01 identifies in the design source's own prototype (which has neither an Escape handler nor an outside-click handler).
  </what-built>
  <how-to-verify>
    1. Open the tour, press `Escape` -- confirm the modal closes. Immediately press `Tab` once and confirm visible focus (the browser's focus ring) is already ON the "How to read this" button, not merely that the button is somewhere in the tab order.
    2. Reopen the tour, click anywhere outside the modal card (e.g. on the dimmed backdrop or the page behind it) -- confirm the modal closes and focus again visibly returns to the trigger button (repeat the `Tab`-press check from step 1).
    3. Reopen the tour, advance to step 3 or later via "Next", then click "Skip tour" -- confirm the modal closes from a mid-tour step (not just from step 1) and focus again returns to the trigger button.
    4. With the tour open, press `Tab` repeatedly -- confirm focus cycles only among the modal's own interactive elements (Skip, Back if present, step content has no focusable elements, forward button) and never escapes to page content behind the modal (focus trap holds).
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 checks pass as expected, or describe the specific step/behavior that differs.</resume-signal>
  <acceptance_criteria>
    - Human types "approved" after confirming all 4 steps behave as expected
    - All 3 dismissal paths (Escape, outside-click, Skip) independently close the tour from any step, not just step 1 (behavior assertion, ROADMAP criterion 3)
    - Focus visibly returns to the "How to read this" button after every dismissal path, confirmed via a real subsequent Tab press, not an automated assertion alone (behavior assertion, ROADMAP criterion 4)
    - Tab-key focus never escapes the modal to background page content while the tour is open (behavior assertion, focus-trap integrity)
  </acceptance_criteria>
  <verify>
    <automated>MISSING -- this is the project's mandatory human-browser-verification checkpoint for real Tab-key focus routing, which jsdom's synthetic focus model cannot substitute for, per STATE.md's documented precedent and RESEARCH.md's Environment Availability table</automated>
  </verify>
  <done>Human has typed "approved", confirming all 3 dismissal paths close the tour from any step and real Tab-key navigation lands focus back on the trigger button every time, with the focus trap holding while the tour is open.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify no stacking-order collision with the on-chart VesselOverlayCard (ROADMAP criterion 5, D-04)</name>
  <files>None -- verification-only task, no files modified</files>
  <read_first>
    .planning/phases/19-guided-tour/19-CONTEXT.md (D-04)
    .planning/phases/19-guided-tour/19-RESEARCH.md (section "Pitfall 1: Assuming a z-index collision risk exists and inventing a number to fix it")
  </read_first>
  <action>
    Open a vessel's on-chart control overlay, then open the Guided Tour on top of it, and confirm the tour visually and interactively sits above the overlay with no partial-paint or click-through collision.
  </action>
  <what-built>
    Radix Dialog's Portal renders `GuidedTourModal` directly to `document.body` at `z-50` (documented in `dialog.tsx` per D-04); `VesselOverlayCard` declares no explicit z-index at all, so the Portal-rendered tour unconditionally paints above it regardless of relative mount order.
  </what-built>
  <how-to-verify>
    1. Click a vessel on the chart to open its floating `VesselOverlayCard`.
    2. With the overlay still open, click "How to read this" -- confirm the tour modal opens fully on top of the vessel overlay (the overlay is either fully obscured by the tour's backdrop dim, or not visible through any gap), with no visual tearing/flicker/z-fighting between the two surfaces.
    3. Confirm the vessel overlay behind the tour is not interactively reachable while the tour is open (clicking where the overlay's Select/Slider would be, through the tour's backdrop, does nothing -- the tour's own backdrop/focus-trap fully blocks it).
    4. Close the tour via any dismissal path -- confirm the vessel overlay (if not separately closed) reappears fully interactive again, with no lingering visual artifact from the tour.
    5. Open the browser devtools' Elements panel while the tour is open -- confirm the tour's Dialog content element is a direct child of `&lt;body&gt;` (Portal-rendered, not nested inside the Sandbox's chart DOM subtree) and its computed `z-index` is `50`.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 checks pass as expected, or describe the specific step/behavior that differs.</resume-signal>
  <acceptance_criteria>
    - Human types "approved" after confirming all 5 steps behave as expected
    - The tour modal visually and interactively sits above the on-chart vessel overlay with zero collision, in a session where both were plausibly open (behavior assertion, ROADMAP criterion 5)
    - The tour's Dialog content is confirmed Portal-rendered to `document.body` at `z-index: 50` via devtools, not merely assumed from source review (behavior assertion, D-04)
  </acceptance_criteria>
  <verify>
    <automated>MISSING -- this is a human-browser-verification checkpoint for real CSS stacking-context/paint-order behavior across two independently-mounted overlay surfaces, which jsdom cannot observe, per this project's jsdom-measurement-ban precedent</automated>
  </verify>
  <done>Human has typed "approved", confirming the tour modal never visually or interactively collides with the on-chart vessel overlay, with the Portal-to-body/z-50 mechanism confirmed via devtools.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new this plan | This plan performs no code changes -- it is a human-verification checkpoint over Plans 19-01 through 19-03's implementation. No new trust boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | N/A | This plan modifies zero files | accept | No code changes in this plan; the STRIDE registers for the actual code changes live in Plans 19-01/19-02/19-03's own `<threat_model>` blocks. This checkpoint exists purely to human-verify runtime/UX/focus/stacking behavior, not to introduce or mitigate a new threat surface. |
</threat_model>

<verification>
Human confirms all 3 tasks' `<how-to-verify>` steps behave as expected against the live `/` route in a real browser, with particular attention to Task 2's real Tab-key focus-return check and Task 3's stacking-order check with the vessel overlay open.
</verification>

<success_criteria>
All 5 ROADMAP Phase 19 success criteria are satisfied and human-confirmed:
1. "How to read this" opens the 6-step tour with per-step illustrations and step-dot progress.
2. Back/Next navigation works correctly; "Start exploring" replaces "Next" on the final step and closes the tour.
3. Escape, outside-click, and Skip/Start-exploring all dismiss the tour from any step.
4. Focus visibly returns to the trigger button after every dismissal path, verified via real Tab-key navigation.
5. The tour modal never visually collides with the on-chart vessel overlay's stacking order.
</success_criteria>

<output>
Create `.planning/phases/19-guided-tour/19-04-SUMMARY.md` when done
</output>
