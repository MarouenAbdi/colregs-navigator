---
phase: 19-guided-tour
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/dialog.tsx
  - src/components/tour/guided-tour-steps.ts
  - src/components/tour/guided-tour-steps.test.ts
  - app/globals.css
autonomous: true
requirements: [TOUR-01, TOUR-02]

must_haves:
  truths:
    - "dialog.tsx exports a Radix Dialog primitive wrapper matching this repo's data-slot/no-forwardRef convention, with z-50 documented as the shared, sole Portal-content stacking tier (D-04)"
    - "guided-tour-steps.ts exports exactly 6 TOUR_STEPS entries, verbatim from the design source's step content, as a pure JSX-free data module"
    - "app/globals.css defines a real .radar-sweep-dot CSS rule (not a template-literal style string) for the trigger button and modal header's decorative accent"
  artifacts:
    - path: "src/components/ui/dialog.tsx"
      provides: "Radix Dialog primitive wrapper consumed by GuidedTourModal (Plan 19-02)"
      contains: "export function DialogContent"
    - path: "src/components/tour/guided-tour-steps.ts"
      provides: "TOUR_STEPS pure data (6 steps, verbatim design copy) consumed by GuidedTourModal/TourStepIllustration (Plan 19-02)"
      contains: "export const TOUR_STEPS"
    - path: "app/globals.css"
      provides: "radar-sweep-dot keyframes/class consumed by GuidedTourModal's header (Plan 19-02) and SandboxContainer's trigger button (Plan 19-03)"
      contains: ".radar-sweep-dot"
  key_links: []
---

<objective>
Lay the interface-first groundwork for Phase 19: the first Dialog primitive this codebase has ever needed (`src/components/ui/dialog.tsx`), the pure `TOUR_STEPS` data module carrying the tour's verbatim 6-step copy, and the net-new `.radar-sweep-dot` decorative CSS rule. No tour-specific React components (`GuidedTourModal`, `TourStepIllustration`) and no wiring into `SandboxContainer.tsx` yet -- this plan only ships the contracts and static assets Plan 19-02's components are written against.

Purpose: Front-loads the two decisions this phase most needs on paper before any tour-specific JSX exists -- D-01's choice of Radix `Dialog` (closing the design source's own missing Escape/outside-click handling) and D-04's z-index documentation requirement (which, per RESEARCH.md Pitfall 1, needs zero new numeric z-index, only a documented note that `z-50` already unconditionally out-stacks everything else in this app).
Output: `dialog.tsx` (Dialog/DialogTrigger/DialogPortal/DialogOverlay/DialogContent/DialogTitle/DialogDescription/DialogClose), `guided-tour-steps.ts` + its data-integrity test, and `app/globals.css`'s new radar-sweep-dot rule.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/19-guided-tour/19-CONTEXT.md
@.planning/phases/19-guided-tour/19-DESIGN-SNAPSHOT.md
@.planning/phases/19-guided-tour/19-PATTERNS.md
@.planning/phases/19-guided-tour/19-RESEARCH.md
@.planning/research/PITFALLS.md
</context>

<interfaces>
<!-- Exact shapes Plan 19-02's TourStepIllustration.tsx/GuidedTourModal.tsx import verbatim.
     Do not deviate from these names. -->

```typescript
// src/components/ui/dialog.tsx (mirrors select.tsx's data-slot wrapper convention --
// see RESEARCH.md Pattern 2 for the full reference implementation to follow)
export function Dialog(props: React.ComponentProps<typeof DialogPrimitive.Root>): React.JSX.Element;
export function DialogTrigger(props: React.ComponentProps<typeof DialogPrimitive.Trigger>): React.JSX.Element;
export function DialogPortal(props: React.ComponentProps<typeof DialogPrimitive.Portal>): React.JSX.Element;
export function DialogOverlay(props: React.ComponentProps<typeof DialogPrimitive.Overlay>): React.JSX.Element; // z-50
export function DialogContent(props: React.ComponentProps<typeof DialogPrimitive.Content>): React.JSX.Element; // wraps Portal -> Overlay + Content, z-50
export function DialogTitle(props: React.ComponentProps<typeof DialogPrimitive.Title>): React.JSX.Element;
export function DialogDescription(props: React.ComponentProps<typeof DialogPrimitive.Description>): React.JSX.Element;
export function DialogClose(props: React.ComponentProps<typeof DialogPrimitive.Close>): React.JSX.Element;

// src/components/tour/guided-tour-steps.ts
export interface TourStepPoint {
  tag: string;
  text: string;
}

export interface TourStep {
  id: number; // 0-5, matches the design source's 6-step order
  icon: string; // decorative unicode glyph (e.g. "⚓"), NOT concatenated into title
  title: string;
  body: string;
  points: TourStepPoint[]; // 2 or 3 entries, varies per step
}

export const TOUR_STEPS: TourStep[]; // exactly 6 entries, id 0..5
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: dialog.tsx -- Radix Dialog primitive wrapper (D-01, D-04)</name>
  <files>src/components/ui/dialog.tsx</files>
  <read_first>
    src/components/ui/select.tsx
    package.json (confirm "radix-ui": "^1.6.2", "shadcn": "^4.13.1" already installed)
    components.json (confirm "style": "radix-nova", aliases.ui: "@/components/ui")
    .planning/phases/19-guided-tour/19-RESEARCH.md (section "Pattern 2: shadcn-style primitive wrapper" and "Common Pitfalls" Pitfall 1)
    .planning/phases/19-guided-tour/19-PATTERNS.md (section "src/components/ui/dialog.tsx (component, request-response)")
  </read_first>
  <action>
    Attempt `npx shadcn add dialog` first. If it succeeds, verify the generated file matches this repo's existing `ui/` conventions -- single unified `import { Dialog as DialogPrimitive } from "radix-ui"` (not a per-primitive `@radix-ui/react-dialog` import), `cn()` from `@/lib/utils`, `data-slot` attributes on every part, no `React.forwardRef` -- and edit it to conform if the CLI output diverges on any of these points. If the CLI cannot reach the registry (network-restricted sandbox), hand-author the file mirroring `select.tsx`'s exact structure instead.
    Whether CLI-generated or hand-authored, the file must export exactly 8 named functions: `Dialog`, `DialogTrigger`, `DialogPortal`, `DialogOverlay`, `DialogContent`, `DialogTitle`, `DialogDescription`, `DialogClose` -- each a thin `data-slot`-carrying pass-through (`data-slot="dialog"`, `"dialog-trigger"`, `"dialog-portal"`, `"dialog-overlay"`, `"dialog-content"`, `"dialog-title"`, `"dialog-description"`, `"dialog-close"` respectively).
    `DialogOverlay`'s className: `fixed inset-0 z-50 bg-black/70 backdrop-blur-sm` plus the tw-animate-css tokens `data-open:animate-in data-open:fade-in-0 data-closed:animate-out data-closed:fade-out-0` -- the bare-boolean-attribute variant syntax (NOT the bracketed `data-[state=open]:` form), matching `select.tsx`'s `SelectContent` exactly (lines 104-105: `data-open:animate-in data-open:fade-in-0 data-open:zoom-in-95` / `data-closed:animate-out data-closed:fade-out-0 data-closed:zoom-out-95`).
    `DialogContent` wraps `DialogPortal` -> `DialogOverlay` + `DialogPrimitive.Content`, className: `fixed top-1/2 left-1/2 z-50 grid w-full max-w-150 max-h-[820px] -translate-x-1/2 -translate-y-1/2 gap-4 rounded-2xl border border-border bg-card p-0 shadow-lg` plus the same animate-in/out tokens plus `data-open:zoom-in-95` (again the bare-boolean `data-open:`/`data-closed:` variant, not `data-[state=open]:`). `max-w-150` is this project's own 0.25rem-step Tailwind spacing scale expressing exactly 600px -- matches the design source's 600px max-width without a raw px literal; `max-h-[820px]` stays an arbitrary-value bracket since no existing token maps to it (per CONTEXT.md's discretion note on porting the design's dimensions via this repo's conventions, not its raw literals verbatim).
    `DialogTitle`'s className: `text-lg leading-none font-semibold`. `DialogDescription`'s className: `text-sm text-muted-foreground`.
    Add a one-line code comment directly above both `DialogOverlay`'s and `DialogContent`'s `z-50` class declaration: "D-04: Portal-rendered overlays (Select, Dialog) use z-50; nothing else in this app declares an explicit z-index -- do not add one to in-flow Sandbox components without updating this note." This satisfies D-04's documented-shared-scale requirement without inventing a new number -- direct grep confirms zero z-index declarations anywhere in `src/components/sandbox/` today, so Dialog's Portal-to-`document.body` rendering at `z-50` unconditionally out-stacks the on-chart `VesselOverlayCard` regardless.
    Do NOT add a built-in top-right close-icon button inside `DialogContent` -- unlike many shadcn Dialog templates, Plan 19-02's `GuidedTourModal` supplies its own explicit "Skip tour" control per the design source's exact header markup (`19-DESIGN-SNAPSHOT.md`'s "Footer controls" section), so a second close affordance would be chrome the design never has.
  </action>
  <acceptance_criteria>
    - `grep -c "function DialogContent" src/components/ui/dialog.tsx` outputs `1`
    - `grep -c "data-slot=\"dialog" src/components/ui/dialog.tsx` outputs `8`
    - `grep -c "@radix-ui/react-dialog" src/components/ui/dialog.tsx` outputs `0` (must use the unified `radix-ui` package, not a per-primitive import)
    - `grep -c "from \"radix-ui\"" src/components/ui/dialog.tsx` outputs at least `1`
    - `grep -c "forwardRef" src/components/ui/dialog.tsx` outputs `0`
    - `grep -c "z-50" src/components/ui/dialog.tsx` outputs at least `2` (Overlay + Content)
    - `grep -c "data-open:animate-in" src/components/ui/dialog.tsx` outputs at least `2` (Overlay + Content, bare-boolean variant matching select.tsx -- NOT `data-[state=open]:`)
    - `grep -c "data-\[state=" src/components/ui/dialog.tsx` outputs `0` (must not use the bracketed `data-[state=open]:` form -- select.tsx uses bare `data-open:`/`data-closed:`)
    - `grep -c "D-04" src/components/ui/dialog.tsx` outputs at least `1`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>dialog.tsx exports exactly the 8 named Dialog* functions per the &lt;interfaces&gt; contract, mirrors select.tsx's data-slot/no-forwardRef convention, documents z-50 as the shared scale per D-04, and includes no built-in close-X button; typecheck passes. (No dedicated test file -- matches this repo's existing convention that ui/ primitives have zero standalone test files, exercised indirectly via GuidedTourModal.test.tsx in Plan 19-02.)</done>
</task>

<task type="auto">
  <name>Task 2: guided-tour-steps.ts -- verbatim 6-step tour data module</name>
  <files>src/components/tour/guided-tour-steps.ts, src/components/tour/guided-tour-steps.test.ts</files>
  <read_first>
    .planning/phases/19-guided-tour/19-DESIGN-SNAPSHOT.md (section "Step content (verbatim -- ports directly into guided-tour-steps.ts)", lines 38-67)
    src/components/hero/hero-preview-fixture.ts (pure-data-module doc-comment/cite-the-source convention)
    src/server/db/curated-scenarios.ts (interface + typed-array export shape convention)
  </read_first>
  <action>
    Create `guided-tour-steps.ts` as a pure `.ts` module -- zero JSX, zero React import, zero framework import -- following `hero-preview-fixture.ts`'s convention of a top-of-file doc comment citing `19-DESIGN-SNAPSHOT.md`'s "Step content" section as the verbatim source (not paraphrased from memory).
    Export `interface TourStepPoint { tag: string; text: string }`, `interface TourStep { id: number; icon: string; title: string; body: string; points: TourStepPoint[] }`, and `export const TOUR_STEPS: TourStep[]` with exactly 6 entries, `id` 0 through 5 in this exact order and content (icon glyph held separately from title, never concatenated):
    id 0: icon "⚓", title "Welcome aboard the Navigator", body "Drop two vessels on a nautical chart and this engine names the give-way ship — and the exact COLREGS rule behind the verdict. Here is how to read it.", points: [{tag: "RULES 11–18", text: "Every verdict maps to the real Steering & Sailing Rules for vessels in sight of one another."}, {tag: "LIVE", text: "Anything you change recomputes instantly — geometry, verdict, and reasoning all update together."}].
    id 1: icon "✥", title "Move the vessels", body "The chart is fully draggable. Grab a hull to reposition it, or drag its bow handle to swing the heading. Selecting a vessel opens its control card.", points: [{tag: "DRAG HULL", text: "Reposition a ship anywhere on the scope — range and bearing follow."}, {tag: "DRAG BOW", text: "Rotate the small handle ahead of the bow to change heading."}, {tag: "TYPE & SPEED", text: "In the card, set vessel type (power, sailing, fishing, RAM, NUC) and speed — these drive the Rule 18 priority."}].
    id 2: icon "▦", title "Read the instruments", body "The footer strip is your live radar readout. These four numbers tell you whether a real risk of collision is developing.", points: [{tag: "RANGE / BRG", text: "Distance in nautical miles and the compass bearing from A to B."}, {tag: "CPA", text: "Closest Point of Approach — how near the vessels pass on present courses."}, {tag: "TCPA", text: "Time to that closest approach. A positive, shrinking value means they are closing."}].
    id 3: icon "§", title "Read the verdict", body "The header banner states the encounter type, the governing rule, and which vessel must give way. Each ship also carries a GIVE WAY / STAND ON badge and its required action.", points: [{tag: "GIVE WAY", text: "This vessel must take early, substantial action to keep clear."}, {tag: "STAND ON", text: "This vessel holds course and speed — but stays ready to act."}, {tag: "IN DOUBT", text: "Ambiguous geometry is flagged amber — the rules tell you to assume the more dangerous case."}].
    id 4: icon "⟿", title "Follow the reasoning", body "The NAV Decision Chain shows every logical step the engine took — from risk-of-collision through the encounter type to the final Rule 18 verdict. Read it left to right.", points: [{tag: "CONTACT 01", text: "Each node cites its own rule (e.g. Rule 7, 13, 14, 15, 18) and the facts that triggered it."}, {tag: "HIGHLIGHT", text: "The last, glowing node is the decisive step that produced the verdict."}].
    id 5: icon "❖", title "Start from a classic", body "Not sure where to begin? The Gallery below holds textbook COLREGS geometries. Load one into the sandbox with a click, then drag from there.", points: [{tag: "6 SCENARIOS", text: "Crossing, head-on, overtaking, sailing priority, not-under-command, and an in-doubt case."}, {tag: "RESET", text: "The Reset button restores the current scenario to its textbook starting position at any time."}].
    `points` is a plain array (not a fixed 2-3 tuple) since CONTEXT.md leaves the exact TypeScript shape as Claude's discretion and step counts genuinely vary between 2 and 3 entries.
  </action>
  <acceptance_criteria>
    - `npx vitest run guided-tour-steps.test.ts` passes with 0 failures
    - `grep -c "export const TOUR_STEPS" src/components/tour/guided-tour-steps.ts` outputs `1`
    - `grep -c "export interface TourStep" src/components/tour/guided-tour-steps.ts` outputs at least `1`
    - `grep -c "from \"react\"\|from 'react'" src/components/tour/guided-tour-steps.ts` outputs `0` (pure data module, zero React import)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run guided-tour-steps.test.ts && npm run typecheck</automated>
  </verify>
  <done>guided-tour-steps.ts exports TOUR_STEPS with exactly 6 entries (ids 0-5) matching the design source's verbatim copy; guided-tour-steps.test.ts proves entry count, id order, and exact title/point content; zero React import; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 3: radar-sweep-dot decorative CSS (net-new, per Pitfall 2)</name>
  <files>app/globals.css</files>
  <read_first>
    app/globals.css (full file, especially the .section-grid-overlay/.section-grid-overlay--glow block at the bottom)
    .planning/phases/19-guided-tour/19-RESEARCH.md (section "Pitfall 2: The radar-sweep-dot visual... does not actually exist yet in the codebase")
    .planning/phases/19-guided-tour/19-PATTERNS.md (section "app/globals.css (MODIFIED -- radar-sweep-dot keyframes)")
  </read_first>
  <action>
    Add a new CSS block after the existing `.section-grid-overlay--glow` rule (end of the file) defining `@keyframes radar-sweep { to { transform: rotate(360deg); } }` and a `.radar-sweep-dot` class: `position: relative; display: inline-block; width: 8px; height: 8px; border-radius: 9999px; background: conic-gradient(from 0deg, transparent, var(--primary) 60%, transparent); animation: radar-sweep 2s linear infinite;`. Reference `var(--primary)` (this app's existing `#2dd4bf` teal token) for the sweep color -- never a raw hex literal -- so the accent stays theme-consistent and matches this repo's existing `SelectContent`/`Header.tsx` teal palette exactly.
    Precede the rule with a WHY comment citing that a repo-wide grep for `radar`/`sweep`/`conic-gradient`/`animate-spin` returns zero matches prior to this phase (RESEARCH.md Pitfall 2) -- this is genuinely new decorative CSS, not a reuse of an existing accent, and it is a real CSS rule (not a template-literal-built `style` string), per CLAUDE.md's "No raw CSS composed as strings" convention. Consumers (Plan 19-02's `GuidedTourModal` header, Plan 19-03's trigger button) apply only `className="radar-sweep-dot"` -- never construct the `conic-gradient(...)`/`animation` string inline in a `.tsx` file.
  </action>
  <acceptance_criteria>
    - `grep -c "@keyframes radar-sweep" app/globals.css` outputs `1`
    - `grep -c "\.radar-sweep-dot" app/globals.css` outputs `1`
    - `grep -A6 "radar-sweep-dot {" app/globals.css | grep -c "#2dd4bf"` outputs `0` (no raw hex literal -- must reference var(--primary))
    - `grep -A6 "radar-sweep-dot {" app/globals.css | grep -c "var(--primary)"` outputs at least `1`
    - `npm run build` completes without a Tailwind/PostCSS/build error
  </acceptance_criteria>
  <verify>
    <automated>grep -c "@keyframes radar-sweep" app/globals.css && npm run build</automated>
  </verify>
  <done>app/globals.css defines a real @keyframes radar-sweep rule and a .radar-sweep-dot class referencing var(--primary), not a raw hex literal or an inline style string; npm run build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new this plan | A UI primitive wrapper, a static data module, and a decorative CSS rule -- no new input source, no persistence, no network boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | Tampering (supply chain) | dialog.tsx sourced via `npx shadcn add dialog` or hand-authored | accept | No new `package.json` dependency -- `radix-ui` is already installed and vetted; the shadcn CLI only copies a local source file, verified against this repo's own `select.tsx` convention before acceptance (Task 1's acceptance criteria). If the CLI is unreachable, the file is hand-authored directly from the already-reviewed RESEARCH.md Pattern 2 example -- no external code introduced either way. |
| T-19-02 | Information Disclosure | guided-tour-steps.ts's TOUR_STEPS content | accept | Static, developer-authored copy only; no user input is ever rendered by this module, so no injection/XSS surface exists here. |
| T-19-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan -- zero package.json changes. |
</threat_model>

<verification>
1. `npm run typecheck` -- 0 errors.
2. `npx vitest run guided-tour-steps.test.ts` -- all pass.
3. `grep -c "data-slot=\"dialog" src/components/ui/dialog.tsx` -- outputs `8`.
4. `grep -c "\.radar-sweep-dot" app/globals.css` -- outputs `1`.
</verification>

<success_criteria>
This plan delivers the Dialog primitive, the verbatim tour-content data module, and the decorative CSS Plan 19-02's components are written against. No tour-specific React component and no wiring into SandboxContainer yet -- that happens in Plans 19-02 and 19-03.
</success_criteria>

<output>
Create `.planning/phases/19-guided-tour/19-01-SUMMARY.md` when done
</output>
