---
phase: 06-scaffolding
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - components.json
  - app/globals.css
  - tsconfig.json
  - package.json
  - package-lock.json
  - src/lib/utils.ts
  - src/components/ui/button.tsx
  - src/components/layout/Header.tsx
  - src/components/layout/Footer.tsx
  - src/components/shared/README.md
  - app/layout.tsx
autonomous: true
requirements: [SCAF-01, SCAF-02, SCAF-03, SCAF-04, SCAF-05, SCAF-06]

must_haves:
  decisions:
    - "D-01: shadcn install scoped to only Header/Footer primitives (button; no sheet/navigation-menu/card/badge/select/slider/tabs/input front-loaded)"
    - "D-02: no sheet/drawer/overlay primitive installed — mobile nav uses a responsive-hide utility, not a slide-out menu"
    - "D-03: below 640px, Header shows only Logo + Source button; Sandbox/Gallery anchors hidden entirely, no hamburger"
    - "D-04: Header Source link points to https://github.com/MarouenAbdi/colregs-navigator"
    - "D-05: logo mark uses a lucide-react icon (Compass, matched against Main-Design.png) plus wordmark + Rules 11-18 chip"
    - "D-06: scroll-padding-top sized to sticky Header height is set in this phase, not deferred to Hero/Gallery"
  truths:
    - "The app renders in a single dark palette regardless of OS/browser color-scheme preference, with no toggle/next-themes machinery present"
    - "A sticky Header (logo/wordmark/chip, Sandbox+Gallery in-page anchors, external Source link) renders above every route"
    - "The Sandbox/Gallery nav anchors disappear below the 640px breakpoint, leaving only the logo and Source button"
    - "A Footer with the exact locked brand/disclaimer copy renders at the bottom of the page shell"
    - "Geist and Geist Mono fonts are applied app-wide, not a browser-default fallback"
    - "shadcn/ui is installed (Button primitive working) as the component-primitive layer for later phases"
  artifacts:
    - path: "components.json"
      provides: "shadcn CLI config — aliases, base=radix, tailwind css path"
    - path: "app/globals.css"
      provides: "Collapsed single dark-only @theme token palette + scroll-padding-top"
    - path: "tsconfig.json"
      provides: "@/* -> ./src/* path alias, additive to existing config"
    - path: "src/lib/utils.ts"
      provides: "cn() classname-merge helper consumed by every ui/* component"
      exports: ["cn"]
    - path: "src/components/ui/button.tsx"
      provides: "shadcn Button primitive"
      exports: ["Button", "buttonVariants"]
    - path: "src/components/layout/Header.tsx"
      provides: "Sticky Header/Nav Server Component"
      exports: ["Header"]
    - path: "src/components/layout/Footer.tsx"
      provides: "Footer Server Component"
      exports: ["Footer"]
    - path: "src/components/shared/README.md"
      provides: "SCAF-06 shared cross-feature location, structural placeholder"
    - path: "app/layout.tsx"
      provides: "Root layout: hardcoded dark class, Geist fonts, Header/Footer wiring"
  key_links:
    - from: "app/layout.tsx"
      to: "src/components/layout/Header.tsx"
      via: "import { Header } from \"@/components/layout/Header\""
      pattern: "@/components/layout/Header"
    - from: "app/layout.tsx"
      to: "src/components/layout/Footer.tsx"
      via: "import { Footer } from \"@/components/layout/Footer\""
      pattern: "@/components/layout/Footer"
    - from: "src/components/layout/Header.tsx"
      to: "src/components/ui/button.tsx"
      via: "import { Button } from \"@/components/ui/button\""
      pattern: "@/components/ui/button"
    - from: "app/globals.css"
      to: "app/layout.tsx"
      via: "html className=\"dark ...\" consuming --background/--foreground tokens"
      pattern: "className=.*dark"
---

<objective>
Install shadcn/ui as this app's component-primitive layer (Radix base, `nova` preset), collapse its scaffold to a single locked dark-only design-token palette, load Geist/Geist Mono app-wide, and build a Header/Footer page shell wired into `app/layout.tsx` — the foundation every later v1.1 phase (Hero, Sandbox, Gallery) builds on.

Purpose: Phases 7-9 all depend on this phase's tokens, `ui/*` primitives, and page shell existing first (ROADMAP.md Phase 6 "Depends on" chain). Nothing here is consumed directly by end users yet except the chrome (Header/Footer) — the tokens/fonts/alias are pure infrastructure for downstream phases.
Output: `components.json`, collapsed dark theme in `app/globals.css`, `@/*` tsconfig alias, `src/lib/utils.ts`, `src/components/ui/button.tsx`, `src/components/layout/Header.tsx`, `src/components/layout/Footer.tsx`, `src/components/shared/README.md`, updated `app/layout.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-scaffolding/06-CONTEXT.md
@.planning/phases/06-scaffolding/06-RESEARCH.md
@.planning/phases/06-scaffolding/06-PATTERNS.md
@.planning/phases/06-scaffolding/06-UI-SPEC.md
@.planning/design/Main-Design.png
@.planning/design/claude-design-prompt.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run shadcn init and collapse the theme scaffold to one locked dark palette</name>
  <files>components.json, app/globals.css, tsconfig.json, package.json, package-lock.json</files>
  <read_first>
    tsconfig.json
    app/globals.css
    package.json
    next.config.ts
    .planning/phases/06-scaffolding/06-RESEARCH.md (Pattern 1, Pattern 2, Pitfall 2, Pitfall 7, Package Legitimacy Audit)
    .planning/phases/06-scaffolding/06-UI-SPEC.md (Color, Spacing Scale tables)
    .planning/phases/06-scaffolding/06-PATTERNS.md (app/globals.css and tsconfig.json sections)
  </read_first>
  <action>
    Confirm the git working tree is clean (commit or stash any pending changes) before running the CLI, so the `init` diff can be reviewed in isolation (Pitfall 7).

    Run `npx shadcn@latest init --template next --base radix --preset nova -y` from the repo root. Do NOT omit `--base radix` — the CLI's current default silently installs `@base-ui/react` instead of `radix-ui` (RESEARCH.md Critical CLI Finding). If the CLI prompts interactively despite `-y`, answer with this repo's actual layout: components -> `@/components`, ui -> `@/components/ui`, lib -> `@/lib`, utils -> `@/lib/utils`, hooks -> `@/hooks`, global CSS -> `app/globals.css` (repo-root `app/`, not `src/app/` or `src/styles/`).

    After `init` completes, run `git diff components.json app/globals.css tsconfig.json package.json app/layout.tsx` and inspect every changed file before editing anything further (Pitfall 2 - hybrid `app/`-at-root + `src/components`/`src/lib` layout may not auto-detect correctly):
    - In `components.json`, confirm `aliases.components == "@/components"`, `aliases.ui == "@/components/ui"`, `aliases.lib == "@/lib"`, `aliases.utils == "@/lib/utils"`, and the tailwind CSS path resolves to `app/globals.css`. Hand-correct any field that resolved differently.
    - In `tsconfig.json`, confirm `compilerOptions.baseUrl == "."` and `compilerOptions.paths["@/*"] == ["./src/*"]` were added. Do not touch `moduleResolution`, `module`, `jsx`, or any other existing option — those carry load-bearing inline-comment rationale in the current file that must be preserved verbatim.
    - In `package.json`, confirm `radix-ui`, `class-variance-authority`, `clsx`, `tailwind-merge`, `lucide-react` landed as dependencies and `shadcn`, `tw-animate-css` landed as devDependencies, matching RESEARCH.md's Package Legitimacy Audit table (all 7 packages already audited there as "Approved" with zero `[ASSUMED]`/`[SUS]`/`[SLOP]` findings — no additional install checkpoint is required this phase).
    - Do NOT edit `app/layout.tsx` in this task even if `init` already touched it — leave any such diff untouched for Task 3 to reconcile (Pitfall 1: the `nova` preset's `font: "geist"` config may have partially pre-wired Geist).

    Collapse the CLI-generated `:root`/`.dark` dual palette in `app/globals.css` down to one locked dark palette, using UI-SPEC.md's Color table literal hex values (not OKLCH): `--background: #09090B`, `--foreground: #FAFAFA`, `--accent: #2dd4bf`, a zinc-900 `#18181B` secondary-surface token, a zinc-800 `#27272A` border/divider token, plus whatever remaining shadcn default tokens the `nova` preset registered (card, popover, muted, muted-foreground, border, input, ring, destructive, destructive-foreground) set to their zinc-scale/accent equivalents. Keep both the `:root` block and the `.dark` block present with identical values (defense-in-depth against any future `dark:`-prefixed utility in a copy-pasted shadcn snippet) — do not delete the `.dark` block or introduce `next-themes`.

    Add `scroll-padding-top: 64px;` to the `html` (or root scroll-container) rule in `app/globals.css` per CONTEXT.md D-06 — this is project-specific, not part of the standard shadcn scaffold.

    Confirm (or add if the preset didn't) an `@theme inline` mapping of `--font-sans: var(--font-geist-sans);` and `--font-mono: var(--font-geist-mono);` — these variable names must match exactly what Task 3 wires in `app/layout.tsx`.
  </action>
  <acceptance_criteria>
    - `components.json` exists; `aliases.components == "@/components"`, `aliases.ui == "@/components/ui"`, `aliases.lib == "@/lib"`, `aliases.utils == "@/lib/utils"`
    - `tsconfig.json` `compilerOptions` contains `"baseUrl": "."` and `"paths": { "@/*": ["./src/*"] }`
    - `tsconfig.json` `compilerOptions.moduleResolution` is still `"bundler"` and `compilerOptions.jsx` is still `"react-jsx"` (unchanged)
    - `app/globals.css` contains exactly one `:root { ... }` block and one `.dark { ... }` block, both containing `--background: #09090B`, `--foreground: #FAFAFA`, `--accent: #2dd4bf`
    - `app/globals.css` contains the literal string `scroll-padding-top: 64px`
    - `app/globals.css` does not contain the string `next-themes`
    - `package.json` `dependencies` includes `radix-ui`, `class-variance-authority`, `clsx`, `tailwind-merge`, `lucide-react`; `devDependencies` includes `shadcn`, `tw-animate-css`
    - `src/lib/utils.ts` exists and exports a `cn` function
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>shadcn is initialized with the Radix base, the theme is collapsed to a single locked dark palette matching UI-SPEC.md's hex values, the `@/*` alias resolves, and `tsc --noEmit` passes with zero errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add Button primitive and build Header/Footer components</name>
  <files>src/components/ui/button.tsx, src/components/layout/Header.tsx, src/components/layout/Footer.tsx, src/components/shared/README.md</files>
  <read_first>
    src/components/sandbox/SandboxContainer.tsx (header-row layout convention, lines ~144-173)
    src/components/sandbox/CopyLinkButton.tsx (button/icon affordance convention)
    app/gallery/page.tsx (existing async Server Component convention — no "use client")
    .planning/phases/06-scaffolding/06-PATTERNS.md (Header/Footer sections, Pattern 3 code example)
    .planning/phases/06-scaffolding/06-UI-SPEC.md (Copywriting Contract, Color, Spacing Scale, Registry Safety)
    .planning/design/Main-Design.png
  </read_first>
  <action>
    Run `npx shadcn@latest add button` from the repo root to generate `src/components/ui/button.tsx` — do not hand-write this file (RESEARCH.md "Don't Hand-Roll").

    Do NOT run `npx shadcn@latest add sheet`, `navigation-menu`, `separator`, `card`, `badge`, `select`, `slider`, `tabs`, or `input` in this task — CONTEXT.md D-01/D-02 scope this phase to only what Header/Footer need. Default to a plain `border-b`/`border-t` Tailwind utility for the Header/Footer hairline dividers instead of a `Separator` component; only run `npx shadcn@latest add separator` if, while building the markup below, a non-full-width or differently-oriented divider is genuinely required (expected outcome: it is not required).

    Create `src/components/layout/Header.tsx` as a plain Server Component (no `"use client"` — no state or event handlers, matching `app/gallery/page.tsx`'s existing convention): a `<header>` with `sticky top-0 z-50 border-b border-border bg-background`, an inner container (`mx-auto flex h-16 max-w-6xl items-center justify-between px-6`) containing:
    - Left: a `lucide-react` `Compass` icon (`h-5 w-5 text-accent`, `aria-hidden="true"`) + wordmark ("COLREGS" at `font-semibold`, "Navigator" at `font-normal`, both `text-base`) + a "Rules 11-18" chip (`rounded-full border border-border px-2 py-0.5 text-xs`). Use `Compass` per RESEARCH.md's discretion resolution; swap for `Navigation` or `LocateFixed` (both confirmed to exist in the installed `lucide-react`) only if visual comparison against `.planning/design/Main-Design.png`'s header glyph indicates a closer match.
    - Center/right: a `<nav>` with `hidden items-center gap-6 text-sm sm:flex` containing two anchors `<a href="#sandbox">Sandbox</a>` and `<a href="#gallery">Gallery</a>` (Tailwind's built-in `sm:` = 640px breakpoint implements CONTEXT.md D-03's "hide below 640px, no hamburger" exactly).
    - Right: `<Button asChild variant="outline" size="sm">` wrapping `<a href="https://github.com/MarouenAbdi/colregs-navigator" target="_blank" rel="noreferrer">` containing a `lucide-react` `Github` icon (`h-4 w-4`, `aria-hidden="true"`) + "Source" text (CONTEXT.md D-04; `rel="noreferrer"` is mandatory — RESEARCH.md Security Domain reverse-tabnabbing mitigation).

    Create `src/components/layout/Footer.tsx` as a plain Server Component: `<footer>` with `border-t border-border bg-background`, inner container `mx-auto flex flex-col gap-4 px-6 py-4 text-xs text-muted-foreground md:flex-row md:items-center md:justify-between` containing two `<span>` elements with this exact locked copy (verbatim, do not paraphrase, per UI-SPEC.md Copywriting Contract): `"COLREGS Navigator · classification maps to Rules 11-18"` and `"Educational reference only — not a substitute for a qualified watchkeeper or official publications. Not for navigation."`.

    Create `src/components/shared/README.md` documenting SCAF-06's shared location: state that this directory is where cross-feature components/types/design-token re-exports live once Hero/Sandbox/Gallery (Phases 7-9) reveal actual duplication across features, and that it is intentionally empty of components at Phase 6 (RESEARCH.md: "don't pre-abstract").

    Import `Button` from `"@/components/ui/button"` and icons from `"lucide-react"` — these are new shadcn-adjacent imports, so the `@/*` alias is the correct convention here (unlike the existing relative `.js`-suffixed imports elsewhere in the repo, which stay untouched).
  </action>
  <acceptance_criteria>
    - `src/components/ui/button.tsx` exists and exports `Button` and `buttonVariants`
    - `src/components/layout/Header.tsx` exists, contains no `"use client"` directive, contains `href="#sandbox"` and `href="#gallery"`, contains a `<nav>` whose className includes both `hidden` and `sm:flex`, contains `target="_blank"` and `rel="noreferrer"` on the same anchor pointing at `https://github.com/MarouenAbdi/colregs-navigator`, contains the text `Rules 11-18`
    - `src/components/layout/Footer.tsx` exists, contains no `"use client"` directive, contains the exact string `classification maps to Rules 11-18` and the exact string `Not for navigation.`
    - `src/components/shared/README.md` exists
    - No `src/components/ui/sheet.tsx`, `navigation-menu.tsx`, `select.tsx`, `slider.tsx`, `tabs.tsx`, `input.tsx`, or `card.tsx` file exists (front-loading excluded per D-01)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Button is installed via the shadcn CLI, Header and Footer render the exact locked copy/links/icons from UI-SPEC.md as Server Components, no out-of-scope shadcn primitives were pulled in, and `tsc --noEmit` passes.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the dark-only page shell into app/layout.tsx</name>
  <files>app/layout.tsx</files>
  <read_first>
    app/layout.tsx
    .planning/phases/06-scaffolding/06-RESEARCH.md (Code Examples: Geist font loading; Pitfall 1)
    .planning/phases/06-scaffolding/06-PATTERNS.md (app/layout.tsx section)
  </read_first>
  <action>
    Run `git diff app/layout.tsx` against the pre-`init` baseline to check whether Task 1's `shadcn init` already added Geist/Geist Mono font-loading code (Pitfall 1: the `nova` preset declares `font: "geist"`, which may or may not have edited this file). If `Geist`/`Geist_Mono` imports from `next/font/google` and `--font-geist-sans`/`--font-geist-mono` variables are already present and wired into `<html>`'s `className`, treat SCAF-03 as already satisfied and skip re-adding the import — proceed directly to the dark-class/Header/Footer edits below. If absent, add: `import { Geist, Geist_Mono } from "next/font/google";`, `const geistSans = Geist({ variable: "--font-geist-sans", subsets: ["latin"] });`, `const geistMono = Geist_Mono({ variable: "--font-geist-mono", subsets: ["latin"] });`.

    Change `<html lang="en">` to `<html lang="en" className={\`dark ${geistSans.variable} ${geistMono.variable}\`}>` — hardcoded `dark`, no `next-themes`, no toggle UI (CONTEXT.md/RESEARCH.md Pitfall 2).

    Change `<body className="bg-slate-50 min-h-screen">` to `<body className="bg-background text-foreground antialiased min-h-screen">` — replacing the hardcoded light-mode class with the token-driven equivalent defined in Task 1's `app/globals.css`.

    Keep the existing `import { TRPCReactProvider } from "../src/lib/trpc/client.js";` exactly as-is (relative, `.js`-suffixed) — do not convert it to the new `@/*` alias; that alias is scoped to new shadcn-adjacent code only (ARCHITECTURE.md Pattern 1).

    Add `import { Header } from "@/components/layout/Header";` and `import { Footer } from "@/components/layout/Footer";`. Inside `<body>`, render `<Header />` before `<main>` wrapping `<TRPCReactProvider>{children}</TRPCReactProvider>`, and `<Footer />` after `</main>`, in that order.
  </action>
  <acceptance_criteria>
    - `app/layout.tsx`'s `<html>` element `className` attribute contains the substring `dark`
    - `app/layout.tsx`'s `<body>` element `className` attribute contains `bg-background` and `text-foreground`, and no longer contains `bg-slate-50`
    - `app/layout.tsx` contains `import { Header } from "@/components/layout/Header";` and `import { Footer } from "@/components/layout/Footer";`
    - `app/layout.tsx` still contains `import { TRPCReactProvider } from "../src/lib/trpc/client.js";` unchanged
    - `npm run build` exits 0
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>`app/layout.tsx` renders dark-only via a hardcoded class (no toggle machinery), Geist/Geist Mono are wired, Header and Footer wrap every route, and both `typecheck` and `build` pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| npm registry -> local build environment | New CLI-installed dependencies (`shadcn`, `radix-ui`, `class-variance-authority`, `clsx`, `tailwind-merge`, `lucide-react`, `tw-animate-css`) cross from an external package registry into this project's `package.json`/`node_modules` via `npx shadcn` |
| Header "Source" link -> browser | A user-clicked outbound `<a target="_blank">` opens a new browser tab/window to an external origin (`github.com`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-06-01 | Tampering | `src/components/layout/Header.tsx` "Source" link | mitigate | `rel="noreferrer"` set on the `target="_blank"` anchor (Task 2), preventing reverse-tabnabbing access to `window.opener` |
| T-06-02 | Tampering | `package.json`/`package-lock.json` (npm supply chain) | mitigate | All 7 new packages already passed `slopcheck` + Context7 official-docs cross-check in 06-RESEARCH.md's Package Legitimacy Audit — zero `[ASSUMED]`/`[SUS]`/`[SLOP]` findings; Task 1 re-confirms the installed `package.json` diff matches that audited set before proceeding |
| T-06-SC | Tampering | npm installs via `npx shadcn` CLI | mitigate | Same audit as T-06-02, restated per protocol's mandatory `T-{phase}-SC` row; no `[ASSUMED]`/`[SUS]` packages present, so the blocking-human-checkpoint clause does not trigger this phase |
| T-06-03 | Information Disclosure | Header/Footer static markup | accept | No user-supplied or sensitive data is rendered anywhere in this phase's components — all content is hardcoded, locked copy from UI-SPEC.md |
| T-06-04 | Elevation of Privilege | N/A — no auth/session/access-control surface | accept | Phase 6 introduces zero auth/session/permission logic (confirmed in 06-RESEARCH.md Security Domain: ASVS V2/V3/V4 all "No") |
</threat_model>

<verification>
Run, in order, from the repo root:
1. `npx tsc --noEmit` — zero errors
2. `npm run build` (webpack, per `next.config.ts`) — exits 0
3. `grep -rl "next-themes" app src` — returns no matches
4. `grep -c "scroll-padding-top: 64px" app/globals.css` — returns 1 or more
5. Manual verification of dark-render/nav-collapse/font/link behavior is deferred to Plan 06-02's checkpoint (implementation and human verification are intentionally split — see Split Signals).
</verification>

<success_criteria>
- `npx tsc --noEmit` and `npm run build` both exit 0
- `components.json`, `tsconfig.json`, and `package.json` reflect a correctly-aliased, Radix-based shadcn install
- `app/globals.css` defines exactly one locked dark palette (no `.dark`-toggle ambiguity, no `next-themes`)
- `src/components/layout/Header.tsx` and `Footer.tsx` exist, render the exact locked copy, and compose only `Button` + plain markup
- `app/layout.tsx` renders `<html className="dark ...">` and wraps every route in Header/Main/Footer
</success_criteria>

<output>
Create `.planning/phases/06-scaffolding/06-01-SUMMARY.md` when done
</output>
</content>
