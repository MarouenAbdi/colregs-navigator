---
phase: 06-scaffolding
plan: 2
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: []
autonomous: false
requirements: [SCAF-02, SCAF-03, SCAF-04, SCAF-05]

must_haves:
  truths:
    - "A human confirms the app renders dark-only in a real browser even when the OS/browser color-scheme preference is set to light"
    - "A human confirms the Header collapses to Logo + Source only below 640px, with no hamburger/drawer fallback"
    - "A human confirms Geist/Geist Mono are the rendered fonts (not a fallback) via browser devtools"
    - "A human confirms the Footer's locked copy renders correctly at the bottom of the page"
    - "A human confirms the Source link opens the correct external URL in a new tab"
  artifacts: []
  key_links: []
---

<objective>
Manually verify, in a real browser, the dark-only rendering, responsive Header collapse, font application, and Footer/link behavior that Plan 06-01 built — jsdom/Vitest cannot detect a half-wired light/dark toggle, real font-family application, or responsive breakpoint collapse, so this is an unavoidable human checkpoint (per CONTEXT.md/PITFALLS.md Pitfall 2 and STATE.md's Phase 6 blocker note).

Purpose: Catch exactly the class of bug automated tests cannot see: OS/browser color-scheme preference silently overriding a half-wired dark theme, and the D-03 nav-collapse behavior only reasoning about static classNames rather than an actual rendered breakpoint.
Output: Human sign-off (or a list of concrete issues to route back to Plan 06-01's tasks).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-scaffolding/06-CONTEXT.md
@.planning/phases/06-scaffolding/06-UI-SPEC.md
@.planning/design/Main-Design.png
@.planning/phases/06-scaffolding/06-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual browser verification of dark-only shell, nav collapse, fonts, and links</name>
  <read_first>
    app/layout.tsx
    src/components/layout/Header.tsx
    src/components/layout/Footer.tsx
    .planning/phases/06-scaffolding/06-UI-SPEC.md (Color, Typography, Spacing, Copywriting Contract)
  </read_first>
  <files></files>
  <action>
    No code changes in this task — it is a pure human-verification gate over Plan 06-01's already-implemented dark theme, Header, Footer, and font wiring. Perform the 8 browser checks listed in `<how-to-verify>` below and report back "approved" or a concrete list of issues.
  </action>
  <what-built>
    Plan 06-01 installed shadcn/ui (Radix base), collapsed the theme to a single locked dark palette, wired Geist/Geist Mono fonts, and built a sticky Header + Footer wrapping every route in `app/layout.tsx`. All of this is currently only verified by `tsc`/`build`, not a real browser render.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` (webpack dev server) and open `http://localhost:3000`.
    2. Set your OS/browser to a LIGHT color-scheme preference (e.g. macOS System Settings -> Appearance -> Light, or Chrome DevTools -> Rendering tab -> "Emulate CSS media feature prefers-color-scheme: light") and hard-refresh. Confirm the page still renders the dark palette (`#09090B` background, light text) exactly as before — no flash of light-mode content, no visible toggle control anywhere on the page.
    3. Confirm the Header is sticky at the top of the viewport while scrolling, and shows (left to right): the icon + "COLREGS Navigator" wordmark + "Rules 11-18" chip on the left, and "Sandbox" / "Gallery" links + "Source" button on the right.
    4. Resize the browser window (or use DevTools responsive mode) to below 640px width. Confirm the "Sandbox" and "Gallery" links disappear entirely — no hamburger icon, no drawer, no icon-only fallback — while the logo and "Source" button remain visible.
    5. Open DevTools -> Elements -> select a body-text element -> Computed styles -> confirm `font-family` includes "Geist" (not a system/serif fallback).
    6. Click "Source". Confirm it opens `https://github.com/MarouenAbdi/colregs-navigator` in a new browser tab.
    7. Scroll to the bottom of the page. Confirm the Footer renders both locked copy lines exactly: "COLREGS Navigator · classification maps to Rules 11-18" and "Educational reference only — not a substitute for a qualified watchkeeper or official publications. Not for navigation."
    8. Check the browser console for any errors or warnings mentioning `next-themes` or a missing/unresolved component import.
  </how-to-verify>
  <acceptance_criteria>
    - Dark palette renders with OS/browser color-scheme set to light, no toggle UI visible
    - Header sticky, shows correct left/right content per Main-Design.png
    - Sandbox/Gallery links hidden below 640px, no hamburger/drawer present
    - Computed `font-family` includes "Geist" on body text
    - Source link opens `https://github.com/MarouenAbdi/colregs-navigator` in a new tab
    - Footer renders both locked copy lines verbatim
    - No console errors/warnings referencing `next-themes` or missing components
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues (e.g. "nav still visible at 500px width" or "font falls back to system sans").</resume-signal>
  <verify>
    <manual>All 8 steps in how-to-verify completed with no issues found; human types "approved"</manual>
  </verify>
  <done>Human tester confirms dark rendering, nav collapse, font application, Source link behavior, and Footer copy all match spec, with no console errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Header "Source" link -> browser | Same external-link boundary introduced in Plan 06-01 — this plan's checkpoint is the human-verification step that confirms the `rel="noreferrer"` mitigation actually behaves correctly in a real browser (opening in a new tab without granting `window.opener` access) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-06-01 | Tampering | `src/components/layout/Header.tsx` "Source" link | mitigate | Already mitigated in Plan 06-01 (`rel="noreferrer"`); this plan's checkpoint step 6 is the manual confirmation that the mitigation renders/behaves as intended in a real browser, not a new risk |
| T-06-SC | Tampering | npm installs (carried from Plan 06-01) | mitigate | No new installs occur in this plan — restated for traceability; see Plan 06-01's threat model for the full audit |

No new trust boundaries or dependencies are introduced by this plan — it is a verification-only checkpoint of Plan 06-01's already-mitigated surface.
</threat_model>

<verification>
This plan has no automated verification beyond the human checkpoint above — jsdom cannot detect color-scheme-preference overrides, real computed font-family, or responsive breakpoint collapse (the exact reason this is a mandatory human checkpoint per CONTEXT.md/PITFALLS.md Pitfall 2).
</verification>

<success_criteria>
- Human tester types "approved" after completing all 8 verification steps above with no issues found
- If issues are found, they are routed back to Plan 06-01's relevant task (Header, Footer, or layout.tsx) for a fix before Phase 6 is considered complete
</success_criteria>

<output>
Create `.planning/phases/06-scaffolding/06-02-SUMMARY.md` when done
</output>
</content>
