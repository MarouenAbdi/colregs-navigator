---
phase: 20-reasoning-trail-hero-visual-sync
plan: 03
type: execute
wave: 2
depends_on: ["20-01"]
files_modified: [src/components/hero/hero-preview-geometry.ts, src/components/hero/hero-preview-risk.ts, src/components/hero/HeroPreviewCard.tsx]
autonomous: true
requirements: [HERO-05]
must_haves:
  truths:
    - "Hero's card shows an in-card header strip (rule chip + encounter title + risk pill) atop the chart, replacing the removed CardHeader eyebrow line and the below-chart verdict banner"
    - "Hero's footer is a single row of LIVE + RANGE + BEARING A→B + CPA + TCPA, replacing the current 3-column RANGE/BEARING/CPA grid"
    - "Hero's classification output (crossing / vesselA gives way / Rule 15 / RANGE 2.99 NM / CPA 1.18 NM) is byte-for-byte unchanged — this plan touches presentation and geometry constants only, never classifyEncounter()/bearing()/cpa() call sites or the fixture"
  artifacts:
    - path: "src/components/hero/hero-preview-geometry.ts"
      provides: "3-ring radii + compass-bezel constants, extending the existing pure-constant derivation pattern"
      contains: "HERO_RANGE_RING_RADII_PX"
    - path: "src/components/hero/hero-preview-risk.ts"
      provides: "Hero-local copy of chart-header-risk.ts's CPA-threshold risk-tier derivation (D-03: copy, don't import)"
      contains: "deriveHeroPreviewRisk"
    - path: "src/components/hero/HeroPreviewCard.tsx"
      provides: "header strip + footer strip restructuring"
      contains: "TCPA"
  key_links:
    - from: "HeroPreviewCard.tsx's risk pill"
      to: "hero-preview-risk.ts's deriveHeroPreviewRisk()"
      via: "direct function call"
      pattern: "deriveHeroPreviewRisk"
    - from: "HeroPreviewCard.tsx's bezel SVG"
      to: "hero-preview-geometry.ts's HERO_RANGE_RING_RADII_PX/HERO_COMPASS_* constants"
      via: "named import"
      pattern: "HERO_RANGE_RING_RADII_PX"
---

<objective>
Extend `hero-preview-geometry.ts` with the new 3-ring/compass-bezel constants the design requires, add a Hero-local risk-tier derivation module (copying, not importing, `chart-header-risk.ts`'s threshold logic per D-03), and restructure `HeroPreviewCard.tsx`'s header (remove the "Live classification"/"BRG-ring · 12 NM" eyebrow, add an in-card header strip mirroring `ChartHeaderStrip.tsx`'s shape) and footer (remove the 3-tile grid + below-chart verdict banner, add a single-row LIVE+RANGE+BEARING A→B+CPA+TCPA strip mirroring `ChartFooterStrip.tsx`'s `Tile` shape).

Purpose: Deliver HERO-05's header/footer layout-parity requirement (D-03, D-04) while keeping the fixture-driven classification call untouched — this plan is the "contract + strip restructuring" half of the Hero work; the bezel SVG/radar-sweep-overlay visual additions are plan 20-04 (sequenced after this plan since both touch `HeroPreviewCard.tsx`).
Output: `hero-preview-geometry.ts` extended with new constants, new `hero-preview-risk.ts`, `HeroPreviewCard.tsx`'s header/footer restructured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-reasoning-trail-hero-visual-sync/20-CONTEXT.md
@.planning/phases/20-reasoning-trail-hero-visual-sync/20-DESIGN-SNAPSHOT.md
@.planning/phases/20-reasoning-trail-hero-visual-sync/20-UI-SPEC.md
@.planning/milestones/v1.1-phases/07-hero/07-CONTEXT.md
</context>

<interfaces>
<!-- Patterns to mirror (copy, not import, per D-03/Anti-Patterns) -->

From src/components/sandbox/chart/ChartHeaderStrip.tsx (lines 65-104, copy the shape only):
```
<div className="flex items-center gap-3 border-b border-border px-4 py-3">
  <div className="flex min-w-0 items-center gap-2">
    <span className="w-fit shrink-0 rounded-md px-2 py-0.5 font-mono text-[11px] font-semibold text-background {accent}">{ruleBadge}</span>
    <h3 className="truncate text-lg font-bold text-foreground">{title}</h3>
  </div>
  <div className="flex-1" />
  <div className="flex max-w-[46%] items-center gap-[7px] rounded-md border px-[11px] py-[6px] text-[12.5px] {riskToneClassName}">
    <span aria-hidden="true" className="size-2 shrink-0 rounded-full {riskDotClassName}" />
    <span className="truncate">{risk.text}</span>
  </div>
</div>
```

From src/components/sandbox/chart/chart-header-risk.ts (lines 15-51, copy the threshold logic only — do not import):
```
function deriveChartHeaderRisk(cpaNm: number | null, tcpaMinutes: number | null): { tier; text } {
  // null cpaNm -> "none"; tcpaMinutes <= 0 -> "none" (opening);
  // cpaNm < 0.3 -> "high"; cpaNm < 1.0 -> "watch"; else -> "ok"
}
export const CHART_HEADER_RISK_TONE_CLASSNAME: Record<tier, string> // text-{color} bg-{color}/10 border-{color}/35
export const CHART_HEADER_RISK_DOT_CLASSNAME: Record<tier, string>  // bg-{color}
```

From src/components/sandbox/chart/ChartFooterStrip.tsx (lines 37-53 Tile, 119-139 row, copy shape only):
```
function Tile({ label, value }) {
  <div className="rounded-lg border border-border bg-chart-surface px-[11px] py-[10px]">
    <div className="font-mono text-[9.5px] font-semibold tracking-wide text-muted-foreground uppercase">{label}</div>
    <div className="mt-0.75 font-mono text-[15px] font-semibold text-foreground">{value}</div>
  </div>
}
```

From src/components/hero/hero-preview-geometry.ts (existing, lines 34-37 — the derivation-comment style every new constant below must follow):
```ts
export const HERO_OUTER_RING_RADIUS_PX = HERO_CONTAINER_SIZE.width * (115 / 480);
export const HERO_INNER_RING_RADIUS_PX = HERO_CONTAINER_SIZE.width * (60 / 480);
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend hero-preview-geometry.ts with 3-ring + compass-bezel constants</name>
  <files>src/components/hero/hero-preview-geometry.ts</files>
  <read_first>src/components/hero/hero-preview-geometry.ts (full current file — keep HERO_CONTAINER_SIZE, HERO_VIEW_BOX, HERO_CHART_CENTER, VESSEL_A_HULL_COLOR, VESSEL_B_HULL_COLOR, CONNECTOR_STROKE, bearingSectorPath() all present, updating only the ring-radius internals bearingSectorPath() references), .planning/phases/20-reasoning-trail-hero-visual-sync/20-DESIGN-SNAPSHOT.md (Section 2, "Bezel" — verbatim source numbers this task ports)</read_first>
  <action>
    Add a new exported tuple `HERO_RANGE_RING_RADII_PX: readonly [number, number, number]` computed as `[HERO_CONTAINER_SIZE.width * (42 / 480), HERO_CONTAINER_SIZE.width * (84 / 480), HERO_CONTAINER_SIZE.width * (126 / 480)]` (design's 3 range rings at r=42/84/126 on its 480-wide reference canvas → 28px/56px/84px at this codebase's 320px width), with a derivation comment matching the existing style (why `42/480` etc., not eyeballed).

    KEEP the existing `HERO_OUTER_RING_RADIUS_PX` / `HERO_INNER_RING_RADIUS_PX` exports in place, untouched, in this task — do NOT delete them here. `HeroPreviewCard.tsx` still references both directly in 4 places (the `radialGradient`'s `r`, the north-reference line's `y2`, and the two old range-ring `<circle>`s) and is not updated until plan 20-04's Task 1, which migrates those call sites AND removes these two exports in the same wave, once nothing references them anymore. Removing the exports here, before their last consumer is migrated, would leave the project in a non-compiling intermediate state between this plan and 20-04.

    Update every internal reference to `HERO_OUTER_RING_RADIUS_PX` inside `bearingSectorPath()`'s function body — all 5 textual occurrences of the identifier (the `northEdge` y-coordinate, the `bearingEdge` x-coordinate, the `bearingEdge` y-coordinate, and both occurrences inside the arc-radius portion of the returned SVG path string's `A` command, e.g. `A ${HERO_OUTER_RING_RADIUS_PX} ${HERO_OUTER_RING_RADIUS_PX} ...`) — to use `HERO_RANGE_RING_RADII_PX[2]` (the new outermost ring, 84px) instead — this is a change to `bearingSectorPath()`'s own internal implementation only (its exported name/signature is unchanged), so it does not affect any consumer's compile status; it simply changes the bearing-sector wedge's rendered radius to match the new outermost ring. Do not leave any of the 5 occurrences pointing at the old constant — a partial migration would make the wedge's radius internally inconsistent (some edges at the old 115/480 radius, others at the new 126/480 radius).

    Add new exported constants, each with a derivation comment in the existing style:
    - `HERO_COMPASS_RING_RADIUS_PX = HERO_CONTAINER_SIZE.width * (132 / 480)` (design's compass bezel ring at r=132 on 480-canvas → 88px here — deliberately just outside the outermost range ring at 84px).
    - `const HERO_COMPASS_SCALE = HERO_COMPASS_RING_RADIUS_PX / 132;` (local, not exported — the ratio between this card's compass radius and the design's own 480-canvas compass radius, used to proportionally scale the design's literal stroke-width/dasharray numbers below so tick density is preserved).
    - `HERO_COMPASS_MINOR_STROKE_WIDTH_PX = 5 * HERO_COMPASS_SCALE`, `HERO_COMPASS_MINOR_DASHARRAY = \`${1.5 * HERO_COMPASS_SCALE} ${9.42 * HERO_COMPASS_SCALE}\`` (design's minor-tick ring: stroke-width 5, dasharray "1.5 9.42" on its 480-canvas).
    - `HERO_COMPASS_MAJOR_STROKE_WIDTH_PX = 9 * HERO_COMPASS_SCALE`, `HERO_COMPASS_MAJOR_DASHARRAY = \`${3 * HERO_COMPASS_SCALE} ${66.13 * HERO_COMPASS_SCALE}\`` (design's major/cardinal-tick ring: stroke-width 9, dasharray "3 66.13").
    - `HERO_CENTER_HUB_RADIUS_PX = 3` (small ringed dot at dead-center, decorative, not a vessel — a fixed literal since the design snapshot doesn't specify a scalable ratio for this element).
    - `HERO_CARDINAL_LABEL_OFFSET_PX = HERO_COMPASS_RING_RADIUS_PX` (N/S/E/W labels sit at the bezel edge — i.e. the compass ring's own radius, no additional offset).
    - `HERO_RANGE_LABEL_TEXT: readonly string[] = ["0.5 NM", "1.0 NM", "1.5 NM"]` (design's 3 stacked range labels, verbatim copy — these are fixed display strings, not derived from `HERO_RANGE_RING_RADII_PX`'s actual NM-per-pixel scale, matching the design source exactly per this phase's "reproduce literal values" convention).

    Keep every existing export (`HERO_CONTAINER_SIZE`, `HERO_VIEW_BOX`, `HERO_CHART_CENTER`, `HERO_OUTER_RING_RADIUS_PX`, `HERO_INNER_RING_RADIUS_PX`, `VESSEL_A_HULL_COLOR`, `VESSEL_B_HULL_COLOR`, `CONNECTOR_STROKE`, `bearingSectorPath`) with its current name and signature — this task only adds new constants and updates `bearingSectorPath()`'s internal radius reference. `HERO_OUTER_RING_RADIUS_PX`/`HERO_INNER_RING_RADIUS_PX` are removed in plan 20-04's Task 1 (once `HeroPreviewCard.tsx`'s remaining references are migrated), not here.
  </action>
  <acceptance_criteria>
    - `grep -c "HERO_RANGE_RING_RADII_PX" src/components/hero/hero-preview-geometry.ts` returns at least 5 (1 definition line + the 4 distinct usage lines inside `bearingSectorPath()` that together account for all 5 textual occurrences of the migrated identifier — the arc-radius line contains it twice but is still one matching line)
    - `sed -n '/^export function bearingSectorPath/,/^}/p' src/components/hero/hero-preview-geometry.ts | grep -c "HERO_OUTER_RING_RADIUS_PX"` equals 0 (zero remaining references to the OLD constant left inside `bearingSectorPath()`'s function body specifically — proves a full, not partial, migration of all 5 occurrences)
    - `grep -c "HERO_OUTER_RING_RADIUS_PX\|HERO_INNER_RING_RADIUS_PX" src/components/hero/hero-preview-geometry.ts` returns at least 2 (both old exports' DEFINITION lines are STILL present and untouched — removal is deferred to plan 20-04's Task 1, once `HeroPreviewCard.tsx`'s remaining references to them are migrated; this count now reflects only the 2 definition lines, since `bearingSectorPath()`'s 5 internal usages of `HERO_OUTER_RING_RADIUS_PX` are fully migrated away by this task)
    - `grep -c "HERO_COMPASS_RING_RADIUS_PX" src/components/hero/hero-preview-geometry.ts` returns at least 1
    - `grep -c "HERO_RANGE_LABEL_TEXT" src/components/hero/hero-preview-geometry.ts` returns at least 1
    - `npx tsc --noEmit` reports no errors project-wide (not scoped to this file — since every existing export is kept intact, `HeroPreviewCard.tsx`'s unmodified references to `HERO_OUTER_RING_RADIUS_PX`/`HERO_INNER_RING_RADIUS_PX` continue to resolve correctly, and the whole repo compiles cleanly after this task, not just after plan 20-04)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && test "$(sed -n '/^export function bearingSectorPath/,/^}/p' src/components/hero/hero-preview-geometry.ts | grep -c "HERO_OUTER_RING_RADIUS_PX")" = "0"</automated>
  </verify>
  <done>hero-preview-geometry.ts exports HERO_RANGE_RING_RADII_PX (3-tuple) plus compass-bezel/cardinal-label/range-label/center-hub constants, each with a "why this number" derivation comment matching the file's existing style; bearingSectorPath() now sweeps to the new outermost ring radius internally, with all 5 of its internal HERO_OUTER_RING_RADIUS_PX references (not just 2) migrated to HERO_RANGE_RING_RADII_PX[2] — zero left inconsistent within the function body. HERO_OUTER_RING_RADIUS_PX/HERO_INNER_RING_RADIUS_PX remain exported and untouched (still consumed by HeroPreviewCard.tsx) — their removal happens in plan 20-04's Task 1, keeping the repo compiling end-to-end after every task in this plan.</done>
</task>

<task type="auto">
  <name>Task 2: Add hero-preview-risk.ts (copy chart-header-risk.ts's threshold logic)</name>
  <files>src/components/hero/hero-preview-risk.ts</files>
  <read_first>src/components/sandbox/chart/chart-header-risk.ts (full file — the exact threshold logic and Tailwind class strings to copy verbatim, per D-03/Anti-Patterns: "copy the small CPA-threshold risk-tier logic into a Hero-local helper... exactly as ChartHeaderStrip.tsx itself did")</read_first>
  <action>
    Create `src/components/hero/hero-preview-risk.ts` as a new pure, framework-free module (mirrors `hero-preview-geometry.ts`'s pattern: no React/JSX, independently unit-testable), with a header doc comment explaining it is a deliberate copy of `chart-header-risk.ts`'s threshold logic per this phase's D-03 ("no shared code with ChartPanel.tsx" tree), not an import. Export:
    - `type HeroPreviewRiskTier = "none" | "ok" | "watch" | "high"` (same 4-tier shape as `chart-header-risk.ts`, for consistency/future-proofing even though Hero's fixed fixture always resolves to `"ok"`).
    - `deriveHeroPreviewRisk(cpaNm: number | null, tcpaMinutes: number | null): { tier: HeroPreviewRiskTier; text: string }` — reproduce `chart-header-risk.ts`'s exact branching and text templates verbatim: `cpaNm === null` → `{ tier: "none", text: "No defined closest point of approach — courses do not converge." }`; `tcpaMinutes !== null && tcpaMinutes <= 0` → `{ tier: "none", text: "Vessels are opening — CPA already passed. No risk of collision developing on present courses." }`; `cpaNm < 0.3` → `{ tier: "high", text: \`Risk of collision exists — CPA ${cpaNm.toFixed(2)} NM. A substantial, early, readily-apparent action is required.\` }`; `cpaNm < 1.0` → `{ tier: "watch", text: \`Close-quarters developing — CPA ${cpaNm.toFixed(2)} NM. Monitor the compass bearing for appreciable change.\` }`; else → `{ tier: "ok", text: \`Passing clear — CPA ${cpaNm.toFixed(2)} NM on present courses.\` }`.
    - `HERO_PREVIEW_RISK_TONE_CLASSNAME: Record<HeroPreviewRiskTier, string>` and `HERO_PREVIEW_RISK_DOT_CLASSNAME: Record<HeroPreviewRiskTier, string>` — same literal Tailwind class values as `chart-header-risk.ts`'s `CHART_HEADER_RISK_TONE_CLASSNAME`/`CHART_HEADER_RISK_DOT_CLASSNAME` (`none`: `text-muted-foreground bg-muted-foreground/10 border-muted-foreground/35` / `bg-muted-foreground`; `ok`: `text-green-400 bg-green-400/10 border-green-400/35` / `bg-green-400`; `watch`: `text-doubt bg-doubt/10 border-doubt/35` / `bg-doubt`; `high`: `text-red-400 bg-red-400/10 border-red-400/35` / `bg-red-400`).
  </action>
  <acceptance_criteria>
    - `test -f src/components/hero/hero-preview-risk.ts` succeeds
    - `grep -c "export function deriveHeroPreviewRisk" src/components/hero/hero-preview-risk.ts` equals 1
    - `grep -c "import" src/components/sandbox/chart/chart-header-risk.ts` — confirm no reverse import exists (chart-header-risk.ts must not import from hero-preview-risk.ts or vice versa; `grep -c "hero-preview-risk" src/components/sandbox/chart/chart-header-risk.ts` equals 0)
    - `grep -c "chart-header-risk" src/components/hero/hero-preview-risk.ts` equals 0 (confirms copy, not import, per D-03)
    - `npx tsc --noEmit` reports no errors from this file
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && test -f src/components/hero/hero-preview-risk.ts</automated>
  </verify>
  <done>hero-preview-risk.ts exists as an independent, framework-free copy of chart-header-risk.ts's exact threshold logic and class-name maps, with zero import relationship to the Sandbox chart module tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Restructure HeroPreviewCard.tsx's header and footer into strips</name>
  <files>src/components/hero/HeroPreviewCard.tsx</files>
  <read_first>src/components/hero/HeroPreviewCard.tsx (full current file), src/components/hero/hero-preview-risk.ts (as created by Task 2), src/components/sandbox/chart/ChartHeaderStrip.tsx (full file, the shape to mirror for the new header), src/components/sandbox/chart/ChartFooterStrip.tsx (full file, the shape to mirror for the new footer's Tile/LIVE-row)</read_first>
  <behavior>
    - Behavior 1: `HeroPreviewCard` renders exactly one header strip containing the literal text `Rule 15` (hardcoded, matching this fixture's invariant verdict — the existing code already hardcodes this exact string in a `Badge`, so no new derivation is introduced) and the literal text `Crossing` (from the existing `ENCOUNTER_TYPE_TITLE` map keyed by `classification.encounterType`), plus a risk pill whose text is `Passing clear — CPA 1.18 NM on present courses.` (this fixture's fixed `dcpaNm` of 1.18 resolves to `deriveHeroPreviewRisk`'s `"ok"` tier, per Task 2's ported thresholds).
    - Behavior 2: `HeroPreviewCard` renders a footer strip containing `RANGE` `2.99 NM`, `BEARING A→B` `061°`, `CPA` `1.18 NM`, and a new `TCPA` tile whose value is `cpaResult.value.tcpaMinutes.toFixed(1)` + `" min"` (this metric does not exist in the current footer grid at all).
    - Behavior 3: The old `CardHeader`'s "Live classification"/"BRG-ring · 12 NM" eyebrow text and the below-chart verdict-banner `Badge`+sentence block are both fully absent from the rendered output (D-04 — not trimmed/relocated, removed).
  </behavior>
  <action>
    Delete the entire `<CardHeader className="flex flex-row items-center justify-between ...">...</CardHeader>` block (the "Live classification" dot/label + "BRG-ring · 12 NM" span) — per D-04, do not keep a trimmed version. Remove the now-unused `CardHeader` import if `CardContent`/`Card` are the only remaining shadcn imports used from `@/components/ui/card`.

    Inside `CardContent`, before the chart-wrapper `<div>`, add a new header strip mirroring `ChartHeaderStrip.tsx`'s shape (copy the shape, not the import — `ChartHeaderStrip.tsx` lives in `src/components/sandbox/chart/`, Sandbox-scoped): a `flex items-center gap-3 border-b border-border px-4 py-3` row containing a `w-fit shrink-0 rounded-md px-2 py-0.5 font-mono text-[11px] font-semibold text-background bg-rule-accent` chip with the literal text `Rule 15` (keep hardcoded — this mirrors the current code's existing hardcoded `<Badge>Rule 15</Badge>`, since the fixture's verdict never changes), a `truncate text-lg font-bold text-foreground` `<h3>` with `{ENCOUNTER_TYPE_TITLE[classification.encounterType]}`, a `flex-1` spacer, and a risk pill (`flex max-w-[46%] items-center gap-[7px] rounded-md border px-[11px] py-[6px] text-[12.5px] ${HERO_PREVIEW_RISK_TONE_CLASSNAME[risk.tier]}`) with a `size-2 shrink-0 rounded-full ${HERO_PREVIEW_RISK_DOT_CLASSNAME[risk.tier]}` dot and `<span className="truncate">{risk.text}</span>`, where `risk = deriveHeroPreviewRisk(cpaResult.value.dcpaNm, cpaResult.value.tcpaMinutes)` (imported from `./hero-preview-risk.js`).

    Delete the below-chart verdict-banner block (`<div className="mt-3 flex items-center gap-2.5 ...">` containing the `Rule 15` `Badge` + `{verdictText}` span) — its role is now fully covered by the new header strip. Remove the now-unused `giveWayLabel`/`verdictText`/`Badge` import if `Badge` has no remaining usage (verify: none remain after this change).

    Replace the `<div className="mt-2 grid grid-cols-3 gap-2">` 3-tile footer with a single-row strip mirroring `ChartFooterStrip.tsx`'s shape: `flex flex-wrap items-center gap-[22px] border-t border-border px-[18px] py-[13px] font-mono`, containing (a) a LIVE indicator — `flex items-center gap-1.5 text-[11px] font-semibold tracking-wide text-primary uppercase` wrapping a `<span aria-hidden="true" className="hero-live-pulse size-1.5 rounded-full bg-primary" />` (from plan 20-01's CSS — deliberately `bg-primary`/`text-primary`, NOT `bg-rule-accent`/`text-rule-accent`/`animate-pulse`, per D-02's locked distinguishing contract) plus the text `LIVE`; then (b) a local `Tile({ label, value })` subcomponent (copy `ChartFooterStrip.tsx`'s `Tile` shape verbatim into this file, do not import it — `rounded-lg border border-border bg-chart-surface px-[11px] py-[10px]` wrapper, `font-mono text-[9.5px] font-semibold tracking-wide text-muted-foreground uppercase` label, `mt-0.75 font-mono text-[15px] font-semibold text-foreground` value — 15px per this codebase's existing Hero value-role convention, NOT ChartFooterStrip.tsx's 17px, per UI-SPEC's typography contract) rendered 4 times: `RANGE` (`${range.toFixed(2)} NM`), `BEARING A→B` (`` `${Math.round(bearingDegrees).toString().padStart(3, "0")}°` ``), `CPA` (`` `${cpaResult.value.dcpaNm.toFixed(2)} NM` ``), `TCPA` (`` `${cpaResult.value.tcpaMinutes.toFixed(1)} min` `` — net-new metric per D-03).
  </action>
  <verify>
    <automated>npx vitest run src/components/hero/Hero.test.tsx src/components/hero/hero-preview-fixture.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Live classification\|BRG-ring" src/components/hero/HeroPreviewCard.tsx` equals 0
    - `grep -c "grid-cols-3" src/components/hero/HeroPreviewCard.tsx` equals 0 (old 3-tile grid gone)
    - `grep -c "TCPA" src/components/hero/HeroPreviewCard.tsx` returns at least 1
    - `grep -c "deriveHeroPreviewRisk" src/components/hero/HeroPreviewCard.tsx` returns at least 1
    - `grep -c "hero-live-pulse" src/components/hero/HeroPreviewCard.tsx` returns at least 1
    - `grep -c "animate-pulse" src/components/hero/HeroPreviewCard.tsx` equals 0
    - `grep -c "classifyEncounter\|bearing(\|cpa(" src/components/hero/HeroPreviewCard.tsx` unchanged in count from before this task (confirms no domain call sites were touched — compare against the pre-task file read in read_first)
    - `npx vitest run src/components/hero/Hero.test.tsx src/components/hero/hero-preview-fixture.test.ts` — all tests pass
    - `npx tsc --noEmit` reports no errors from this file
  </acceptance_criteria>
  <done>HeroPreviewCard.tsx's header is a strip (rule chip + title + risk pill), the old eyebrow/verdict-banner text is fully removed (not relocated), and the footer is a single LIVE+RANGE+BEARING A→B+CPA+TCPA row — with zero change to any classifyEncounter()/bearing()/cpa() call or the fixture itself.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. `HeroPreviewCard.tsx` is a standalone, fixture-driven component with no props and no external data source (its "input" is the hardcoded `heroPreviewVesselA`/`heroPreviewVesselB` fixture, unchanged by this plan). This plan adds a new local pure-function module (`hero-preview-risk.ts`) that takes only numeric arguments already computed in-process — no new trust boundary is crossed.

## STRIDE Threat Register

No threats apply. Zero new npm packages, zero new data flow, zero new user input, zero change to `classifyEncounter()`/`bearing()`/`cpa()` or the fixture values. `hero-preview-risk.ts` is a pure numeric-threshold function operating on already-validated `Result`-unwrapped numbers from the existing fixture computation — no injection surface, no auth/session boundary, no ASVS Level 1 category applies.
</threat_model>

<verification>
- `npx vitest run src/components/hero/Hero.test.tsx src/components/hero/hero-preview-fixture.test.ts` — both existing test files pass unmodified (this plan does not need to touch either test file: `Hero.test.tsx`'s assertions on `"Rule 15"` text and `"2.99 NM"` occurrence count still hold against the new header-strip chip and footer RANGE tile; `hero-preview-fixture.test.ts` asserts only against the fixture/domain layer, untouched by this plan).
- `npx tsc --noEmit` — no new type errors across the 3 modified/created files.
- Manual diff review: `giveWayLabel`/`verdictText` locals and the `Badge` import are removed if no longer referenced (dead-code check).
</verification>

<success_criteria>
`hero-preview-geometry.ts` exports the new 3-ring/compass-bezel constant set (consumed by plan 20-04); `hero-preview-risk.ts` exists as an independent copy of the Sandbox's risk-threshold logic; `HeroPreviewCard.tsx`'s header/footer are fully restructured into strips per D-03/D-04, with `Hero.test.tsx`/`hero-preview-fixture.test.ts` passing unmodified and zero change to any domain call.
</success_criteria>

<output>
Create `.planning/phases/20-reasoning-trail-hero-visual-sync/20-03-SUMMARY.md` when done
</output>
