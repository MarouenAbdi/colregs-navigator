---
phase: 20-reasoning-trail-hero-visual-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [app/globals.css]
autonomous: true
requirements: [SBOX-09, HERO-05]
must_haves:
  truths:
    - "Every new keyframe animation this phase introduces (conduit, travel, per-token sweep, hero radar sweep, hero LIVE pulse) is a real @keyframes/class rule in app/globals.css, never a template-literal-composed style string in a .tsx file (D-05)"
    - "Every new animation is inert (no `animation` property applied) unless `prefers-reduced-motion: no-preference` matches (D-06)"
  artifacts:
    - path: "app/globals.css"
      provides: "trail-connector/trail-connector-pulse/trail-token/trail-token-sweep-ring/radar-sweep-dot--lg/hero-radar-sweep/hero-radar-sweep-inner/hero-live-pulse classes + conduit/travel/sweep/hero-live-pulse keyframes"
      contains: ".trail-connector"
  key_links:
    - from: "src/components/sandbox/reasoning/ReasoningTrail.tsx (plan 20-02, not yet written)"
      to: "app/globals.css's .trail-connector/.trail-connector-pulse/.trail-token/.trail-token-sweep-ring/.radar-sweep-dot--lg rules"
      via: "shared className strings"
      pattern: "trail-connector|trail-token"
    - from: "src/components/hero/HeroPreviewCard.tsx (plan 20-03/20-04, not yet written)"
      to: "app/globals.css's .hero-radar-sweep/.hero-radar-sweep-inner/.hero-live-pulse rules"
      via: "shared className strings"
      pattern: "hero-radar-sweep|hero-live-pulse"
---

<objective>
Add every new CSS keyframe/class this phase needs (trail's 3 animation layers + Hero's radar-sweep overlay + Hero's distinguished LIVE pulse) to `app/globals.css`, following this file's own established `.radar-sweep-dot` pattern exactly: a static base rule applied unconditionally, with the `animation` property itself gated inside `@media (prefers-reduced-motion: no-preference)`. No component files are touched in this plan — this is the "interface contract" wave that plans 20-02/20-03/20-04 consume by className reference only.

Purpose: Give the trail and Hero components a complete, ready-to-reference set of animation primitives before either is restructured, so those plans only ever apply class names (per CLAUDE.md's "no raw CSS composed as strings in component files" rule) and never author gradient/keyframe definitions inline.
Output: `app/globals.css` extended with 8 new rules/keyframe blocks, zero component changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/20-reasoning-trail-hero-visual-sync/20-DESIGN-SNAPSHOT.md
@.planning/phases/20-reasoning-trail-hero-visual-sync/20-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the reasoning trail's three CSS animation layers</name>
  <files>app/globals.css</files>
  <read_first>app/globals.css (existing `.radar-sweep-dot`/`@keyframes radar-sweep`/`prefers-reduced-motion` block, lines 202-231 — this is the exact structural pattern to replicate for every rule below)</read_first>
  <action>
    Append to `app/globals.css`, immediately after the existing `.radar-sweep-dot` block (do not modify that block — its two current 8px consumers, `SandboxContainer.tsx` and `GuidedTourModal.tsx`, must keep rendering identically):

    1. `.radar-sweep-dot--lg` — a size-only modifier applied alongside the existing `.radar-sweep-dot` class (component applies both class names together): `width: 14px; height: 14px;`. No new animation rule needed — it inherits `.radar-sweep-dot`'s existing `radar-sweep 2s linear infinite` animation and reduced-motion gate by virtue of sharing the base class.

    2. `.trail-connector` (the dashed connector line, `flex:0 0 34px`/`26px` sized `<li>`'s inner div) — base rule (static, unconditional): `height: 3px; width: 100%; border-radius: 2px; background: repeating-linear-gradient(90deg, var(--tone-color) 0 5px, transparent 5px 11px); background-size: 11px 3px;`. Then `@keyframes conduit { to { background-position: 22px 0; } }`, with `animation: conduit 0.55s linear infinite;` applied to `.trail-connector` ONLY inside `@media (prefers-reduced-motion: no-preference)`.

    3. `.trail-connector-pulse` (the small traveling dot inside the same connector `<li>`, needs a `position: relative` ancestor which the connector `<li>` itself provides) — base rule: `position: absolute; top: 50%; left: 50%; width: 6px; height: 6px; margin-top: -3px; border-radius: 9999px; background: var(--tone-color); box-shadow: 0 0 8px 1px var(--tone-color); opacity: 1;`. Then `@keyframes travel { 0% { left: -4px; opacity: 0; } 12% { opacity: 1; } 88% { opacity: 1; } 100% { left: calc(100% + 4px); opacity: 0; } }`, with `animation: travel 1.9s ease-in-out infinite;` applied only inside the same reduced-motion media query. (The base rule's `left: 50%; opacity: 1;` is the static fallback per D-06/UI-SPEC Motion Contract — "fully static equivalent, no animation property applied at all".)

    4. `.trail-token` (the 38px circular step-number token) — base rule (no animation involved): `position: relative; display: flex; align-items: center; justify-content: center; width: 38px; height: 38px; border-radius: 9999px; background: radial-gradient(circle at 32% 28%, var(--tone-color), color-mix(in srgb, var(--tone-color) 45%, black));`.

    5. `.trail-token-sweep-ring` (the spinning ring layered behind the token's number, absolutely positioned inside `.trail-token`) — base rule: `position: absolute; inset: 0; border-radius: 9999px; background: conic-gradient(from 0deg, transparent, var(--tone-color) 55%, transparent); opacity: 0.55;`. Reuses the EXISTING `@keyframes radar-sweep` (do not redefine it) — add `.trail-token-sweep-ring` to the existing reduced-motion media-query block's selector list so it gets `animation: radar-sweep 2s linear infinite;` alongside `.radar-sweep-dot`.

    Every component that sets `--tone-color` does so via inline `style={{ "--tone-color": ... }}` — this plan only defines the CSS that consumes that custom property, per the `SectionGridBackground`/`.section-grid-overlay` `--grid-opacity` precedent already in this file.
  </action>
  <acceptance_criteria>
    - `grep -c "trail-connector\b" app/globals.css` returns at least 1 (class defined)
    - `grep -c "@keyframes conduit" app/globals.css` equals 1
    - `grep -c "@keyframes travel" app/globals.css` equals 1
    - `grep -c "trail-token-sweep-ring" app/globals.css` returns at least 2 (class definition + reduced-motion selector list entry)
    - The existing `.radar-sweep-dot` base rule (8px) and its two current consumers are unmodified — `grep -n "width: 8px" app/globals.css` still matches the original `.radar-sweep-dot` rule
    - Every new `animation:` declaration (`conduit`, `travel`, and `.trail-token-sweep-ring`'s addition to `radar-sweep`) appears only inside a `@media (prefers-reduced-motion: no-preference)` block — `grep -B2 "animation: conduit" app/globals.css` and `grep -B2 "animation: travel" app/globals.css` both show the preceding `@media` line within 2 lines
  </acceptance_criteria>
  <verify>
    <automated>grep -c "trail-connector" app/globals.css && grep -c "@keyframes conduit" app/globals.css && grep -c "@keyframes travel" app/globals.css</automated>
  </verify>
  <done>All 5 new trail-animation rules exist in app/globals.css, structurally matching the `.radar-sweep-dot` static-base-then-gated-animation pattern; the pre-existing `.radar-sweep-dot` rule and its consumers are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Add Hero's radar-sweep overlay and distinguished LIVE-pulse CSS</name>
  <files>app/globals.css</files>
  <read_first>.planning/phases/20-reasoning-trail-hero-visual-sync/20-DESIGN-SNAPSHOT.md (Section 2, "Radar sweep overlay" — verbatim markup/CSS this task ports) and .planning/phases/20-reasoning-trail-hero-visual-sync/20-UI-SPEC.md (Motion Contract table's "Hero LIVE dot color/class contract" — locks the exact color/duration/opacity values below)</read_first>
  <action>
    Append to `app/globals.css`, after Task 1's rules:

    1. `.hero-radar-sweep` (outer positioned wrapper, sibling to Hero's `<svg>` inside the same `position: relative` chart-wrapper div) — base rule: `position: absolute; left: 50%; top: 50%; width: 47.9%; aspect-ratio: 1; transform: translate(-50%, -50%); pointer-events: none;`.

    2. `.hero-radar-sweep-inner` — base rule: `width: 100%; height: 100%; border-radius: 9999px; background: conic-gradient(from 0deg, rgba(45,212,191,.3), rgba(45,212,191,0) 60deg, transparent); mix-blend-mode: screen;`. Then `@keyframes sweep { to { transform: rotate(360deg); } }`, with `animation: sweep 4.5s linear infinite;` applied to `.hero-radar-sweep-inner` only inside `@media (prefers-reduced-motion: no-preference)`.

    3. `.hero-live-pulse` (the Hero footer's distinguished LIVE dot — must NOT reuse Tailwind's `animate-pulse`, which is reserved for `ChartFooterStrip.tsx`'s Sandbox LIVE dot per D-02) — base rule (static fallback, per UI-SPEC: "static dot at a fixed mid-range opacity (.6), no animation"): `opacity: 0.6;`. Then `@keyframes hero-live-pulse { 0%, 100% { opacity: 0.35; } 50% { opacity: 0.85; } }`, with `animation: hero-live-pulse 2.8s ease-in-out infinite;` applied to `.hero-live-pulse` only inside the reduced-motion media query (2.8s is deliberately slower than the real Sandbox's `animate-pulse` 2s default, and the 0.35→0.85 range is dimmer than Tailwind `animate-pulse`'s 0.5→1 — both per D-02/UI-SPEC's locked distinguishing contract).

    Add all three new `animation:` declarations to the SAME `@media (prefers-reduced-motion: no-preference)` block Task 1 used (one shared media-query block for every phase-20 animation is fine — do not create a second, separate media-query block).
  </action>
  <acceptance_criteria>
    - `grep -c "hero-radar-sweep\b" app/globals.css` returns at least 1
    - `grep -c "@keyframes sweep" app/globals.css` equals 1
    - `grep -c "hero-live-pulse" app/globals.css` returns at least 2 (class definition + keyframe name)
    - `grep -c "animate-pulse" app/globals.css` returns 0 (confirms `.hero-live-pulse` is a distinct class, not a reuse/reference of Tailwind's utility)
    - `grep -n "mix-blend-mode: screen" app/globals.css` matches exactly once, inside `.hero-radar-sweep-inner`
    - `npm run build` (or `npx tsc --noEmit` if faster) succeeds with no errors introduced by this CSS-only change
  </acceptance_criteria>
  <verify>
    <automated>grep -c "hero-radar-sweep" app/globals.css && grep -c "@keyframes sweep" app/globals.css && grep -c "hero-live-pulse" app/globals.css && npx tsc --noEmit</automated>
  </verify>
  <done>Hero's radar-sweep overlay and distinguished LIVE-pulse CSS exist in app/globals.css, both gated by the shared prefers-reduced-motion block; no Tailwind `animate-pulse` reference appears anywhere in this new CSS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan modifies only `app/globals.css` — a static stylesheet with zero runtime input, no data flow, no new endpoint, no new dependency. No trust boundary is created or crossed.

## STRIDE Threat Register

No threats apply. This is a CSS-only addition of decorative keyframe animations and class rules to an existing stylesheet; there is no user input handling, no data validation boundary, no injection surface, and no package installs (RESEARCH.md's Package Legitimacy Audit is explicitly skipped for this phase — zero new npm dependencies). ASVS Level 1 has no applicable category here: no auth, no session, no output encoding boundary is touched by adding `@keyframes`/class rules.
</threat_model>

<verification>
- `grep` checks above confirm all 8 new rules/keyframes exist with the correct structural pattern (static base + reduced-motion-gated animation).
- `npm run build` (or equivalent typecheck) passes — confirms no syntax break in `app/globals.css` that would fail the Tailwind/PostCSS build pipeline.
- No `.tsx`/`.ts` file is modified in this plan — `git diff --stat` shows only `app/globals.css`.
</verification>

<success_criteria>
`app/globals.css` contains every animation class name that plans 20-02/20-03/20-04 will reference (`trail-connector`, `trail-connector-pulse`, `trail-token`, `trail-token-sweep-ring`, `radar-sweep-dot--lg`, `hero-radar-sweep`, `hero-radar-sweep-inner`, `hero-live-pulse`), each following the existing `.radar-sweep-dot` static-base/gated-animation structure, with zero component files touched.
</success_criteria>

<output>
Create `.planning/phases/20-reasoning-trail-hero-visual-sync/20-01-SUMMARY.md` when done
</output>
