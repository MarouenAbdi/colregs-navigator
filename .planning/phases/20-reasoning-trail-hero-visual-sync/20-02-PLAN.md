---
phase: 20-reasoning-trail-hero-visual-sync
plan: 02
type: execute
wave: 2
depends_on: ["20-01"]
files_modified: [src/components/sandbox/reasoning/ReasoningTrail.tsx, src/components/sandbox/reasoning/ReasoningTrail.test.tsx]
autonomous: true
requirements: [SBOX-09]
must_haves:
  truths:
    - "The reasoning trail renders as a single horizontal row of step cards joined by connectors at >=900px viewport width, for 3-, 4-, and 5-entry trails (docs/reasoning-trails.json's full length range)"
    - "At <900px viewport width the trail stacks vertically (flex-direction: column) with the connector band shrunk to a fixed height, using this codebase's existing min-[900px]: breakpoint convention (no new raw @media rule)"
    - "The trail header reads 'NAV DECISION CHAIN · radar acquisition' and the count badge reads '{n} contacts' (not '{n} steps')"
    - "No getBoundingClientRect()/getBBox() call exists anywhere in ReasoningTrail.tsx — every connector/sweep effect is driven by CSS classes only"
  artifacts:
    - path: "src/components/sandbox/reasoning/ReasoningTrail.tsx"
      provides: "horizontal two-list-item-per-step trail markup, referencing 20-01's CSS classes by name"
      contains: "trail-connector"
    - path: "src/components/sandbox/reasoning/ReasoningTrail.test.tsx"
      provides: "updated assertions matching the new copy and two-li-per-step DOM shape"
      contains: "contacts"
  key_links:
    - from: "ReasoningTrail.tsx's connector <li> className"
      to: "app/globals.css's .trail-connector/.trail-connector-pulse rules (from plan 20-01)"
      via: "shared className string"
      pattern: "trail-connector"
    - from: "ReasoningTrail.tsx's card <li> token div className"
      to: "app/globals.css's .trail-token/.trail-token-sweep-ring rules (from plan 20-01)"
      via: "shared className string"
      pattern: "trail-token"
---

<objective>
Restructure `ReasoningTrail.tsx`'s body from today's `flex-wrap` grid of cards into the design's true horizontal "NAV DECISION CHAIN" — a single flex row of two list items per step (a `flex:1 1 0` card followed by a `flex:0 0 34px` connector, connector omitted after the last card), stacking to a `flex-direction: column` layout below 900px. Update the header copy per D-07 and the header dot to the 14px radar-sweep variant. Update `ReasoningTrail.test.tsx` to match the new copy and DOM shape.

Purpose: Deliver SBOX-09 — the phase's headline requirement — using only CSS-only animation primitives (already added in plan 20-01) and this codebase's existing `min-[900px]:` breakpoint convention, satisfying Roadmap criteria 1 and 2.
Output: `ReasoningTrail.tsx` restructured in place; `ReasoningTrail.test.tsx` updated to assert the new shape/copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-reasoning-trail-hero-visual-sync/20-CONTEXT.md
@.planning/phases/20-reasoning-trail-hero-visual-sync/20-DESIGN-SNAPSHOT.md
@.planning/phases/20-reasoning-trail-hero-visual-sync/20-UI-SPEC.md
@docs/reasoning-trails.json
</context>

<interfaces>
<!-- CSS classes this plan consumes, defined by plan 20-01. No codebase exploration needed for these names/semantics. -->

From app/globals.css (added by plan 20-01):
- `.radar-sweep-dot--lg` — apply alongside the existing `.radar-sweep-dot` className (both classes on the same element) for the 14px header dot.
- `.trail-connector` — the dashed connector line div; expects a `--tone-color` CSS custom property (hex or `var(--token)` string) set via inline `style`.
- `.trail-connector-pulse` — the traveling pulse dot; same `--tone-color` custom property, must be a child of a `position: relative` ancestor (the connector `<li>` itself).
- `.trail-token` — the 38px circular token background (radial-gradient fill); same `--tone-color` custom property.
- `.trail-token-sweep-ring` — the spinning ring layered inside `.trail-token`; same `--tone-color` custom property, must be `position: absolute` inside a `position: relative` token wrapper, z-index below the step-number text.

From `src/components/sandbox/reasoning/reasoning-trail-tag.ts` (unchanged, already imported):
- `classifyingEntryIndex(trailLength: number): number`
- `ruleNumber(ruleId: string): string`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Header copy/dot change + two-list-item-per-step skeleton</name>
  <files>src/components/sandbox/reasoning/ReasoningTrail.tsx</files>
  <read_first>src/components/sandbox/reasoning/ReasoningTrail.tsx (current full file — keep FACT_LABEL/formatFactValue/FactReadout/TrailTone/TONE_NUMBER_CLASSNAME/TONE_TAG_CLASSNAME/trailStepTag all unchanged), src/components/sandbox/chart/ChartHeaderStrip.tsx (lines 65-84, the `flex-1` spacer-then-pill idiom to mirror for the header row's dot+label-then-badge shape)</read_first>
  <action>
    In the `<CardHeader>` row: replace the plain `Reasoning Trail` label span with a `flex items-center gap-2` wrapper containing (a) a `<span aria-hidden="true" className="radar-sweep-dot radar-sweep-dot--lg" />` (both classes together, 14px per D-05 layer 3/UI-SPEC), then (b) the text `NAV DECISION CHAIN · radar acquisition` (replacing `Reasoning Trail` verbatim, per D-07 — locked, not a paraphrase). Change the `Badge` text from `` `{trail.length} steps` `` to `` `{trail.length} contacts` `` (D-07).

    Replace the `<ol className="flex flex-row flex-wrap gap-3 p-0">{trail.map(...)}</ol>` body with: `<ol className="flex flex-col items-stretch gap-0 p-0 min-[900px]:flex-row">` whose children come from `trail.flatMap((entry, index) => { ... })` — for each entry, return an array containing the card `<li>` and, only when `index < trail.length - 1`, a connector `<li>` immediately after it (last entry returns only its card, no trailing connector). Key the card `${entry.ruleId}-${index}-card` and the connector `${entry.ruleId}-${index}-wire` (avoids key collisions between the two element types).

    Card `<li>`: base classes `relative flex flex-none flex-col gap-2 rounded-[13px] border pt-4 px-[15px] pb-[15px] min-[900px]:flex-1` (Pitfall 2: base = stacked fallback via `flex-none`, `min-[900px]:flex-1` = the equal-width row; Pitfall 4: `flex-col` required for the fact-chip `mt-auto` bottom-pinning added in Task 2). Non-last cards: `border-border bg-chart-surface`. The single last card (index === trail.length - 1): background `bg-[#101016]`, and border/glow driven by its tone (`verdict-mutual` or `verdict-decisive` — the only two tones `trailStepTag` ever assigns to the last index) via inline `style` using `color-mix(in srgb, var(--mutual|--give-way) 53%, transparent)` for the border color and a two-layer `color-mix`-based `boxShadow` (`0 0 0 1px color-mix(in srgb, var(--tone) 19%, transparent), 0 14px 34px -14px color-mix(in srgb, var(--tone) 41%, transparent)`) for the glow — do not hardcode literal hex percentages as a template string composed at multiple call sites; define this once as a small local `TONE_LAST_CARD_STYLE: Record<"verdict-mutual" | "verdict-decisive", CSSProperties>` lookup map (mirrors the existing `TONE_NUMBER_CLASSNAME`/`TONE_TAG_CLASSNAME` lookup-map pattern already in this file) and spread it onto the last card only. Every card `<li>` (including non-last) still sets `style={{ "--tone-color": ... }}` for Task 2's token/corner-bracket/accent-bar elements to consume — add a `TONE_ACCENT_VAR: Record<TrailTone, string>` lookup map (`geometry: "var(--geometry)"`, `rule: "var(--rule-accent)"`, `doubt: "var(--doubt)"`, `"verdict-mutual": "var(--mutual)"`, `"verdict-decisive": "var(--give-way)"`).

    Connector `<li>`: `relative flex flex-none items-center w-full flex-[0_0_26px] min-[900px]:w-auto min-[900px]:flex-[0_0_34px]` (Pitfall 2: 26px fixed band stacked, 34px fixed width in the row), with `style={{ "--tone-color": ... }}` using the SAME entry's tone (the connector after a step takes that step's tone color, matching the design's `dotFor()` per-step coloring). Inside it, two children: `<div className="trail-connector w-full" />` and `<div className="trail-connector-pulse" />`.

    Leave the existing per-card content (number badge, tag chip, ruleId/text row, `FactReadout`) exactly as-is for THIS task — Task 2 replaces the number-badge/token content. Import `type { CSSProperties } from "react"` for the new inline-style typing.
  </action>
  <acceptance_criteria>
    - `grep -c "NAV DECISION CHAIN · radar acquisition" src/components/sandbox/reasoning/ReasoningTrail.tsx` equals 1
    - `grep -c "{trail.length} contacts" src/components/sandbox/reasoning/ReasoningTrail.tsx` equals 1 (as a template literal, so grep for the literal text `contacts` inside the Badge)
    - `grep -c "flex-wrap" src/components/sandbox/reasoning/ReasoningTrail.tsx` equals 0 (the old wrapping grid is gone)
    - `grep -c "trail.flatMap" src/components/sandbox/reasoning/ReasoningTrail.tsx` equals 1
    - `grep -c "min-\[900px\]:flex-row" src/components/sandbox/reasoning/ReasoningTrail.tsx` equals 1
    - `grep -c "getBoundingClientRect\|getBBox" src/components/sandbox/reasoning/ReasoningTrail.tsx` equals 0
    - `npx tsc --noEmit` reports no new errors in this file
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -c "trail.flatMap" src/components/sandbox/reasoning/ReasoningTrail.tsx</automated>
  </verify>
  <done>ReasoningTrail.tsx's header shows the new dot+copy, and its `<ol>` renders two `<li>`s per non-last step (card + connector) via a single `trail.flatMap()` pass, with base classes = stacked/column fallback and `min-[900px]:` classes = the horizontal row, matching Pitfall 2's translation direction exactly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Card content enrichment — token, corner brackets, accent bar, contact label</name>
  <files>src/components/sandbox/reasoning/ReasoningTrail.tsx</files>
  <read_first>src/components/sandbox/reasoning/ReasoningTrail.tsx (as modified by Task 1 — read the current state, not the pre-Task-1 version), .planning/phases/20-reasoning-trail-hero-visual-sync/20-DESIGN-SNAPSHOT.md (Section 1, "Step card (cnode)" — exact corner-bracket/accent-bar/token spec)</read_first>
  <action>
    Inside each card `<li>`, replace the existing 22px `<span className="flex size-[22px] ...">{index + 1}</span>` number badge with a 38px token: a `relative flex size-[38px] shrink-0 items-center justify-center` wrapper containing (a) `<span aria-hidden="true" className="trail-token absolute inset-0" style={{ "--tone-color": ... } as CSSProperties} />`, (b) `<span aria-hidden="true" className="trail-token-sweep-ring absolute inset-0" style={{ "--tone-color": ... } as CSSProperties} />`, (c) `<span className="relative font-mono text-[13px] font-semibold text-white">{index + 1}</span>` (the `relative` keeps the number visually above the absolutely-positioned sweep ring in paint order). Next to (or below) the token, add a `CONTACT 0N` label: `<span className="font-mono text-[8.5px] font-semibold tracking-[0.08em] text-muted-foreground uppercase">{`Contact ${String(index + 1).padStart(2, "0")}`}</span>`.

    Add a 2px accent bar pinned to the card's top inside edge: `<span aria-hidden="true" className="absolute inset-x-0 top-0 h-0.5" style={{ backgroundColor: "var(--tone-color)", opacity: isLast ? 0.9 : 0.55 }} />` (requires the card `<li>` to already have `relative`, added in Task 1).

    Add 4 corner brackets (11x11px, 1.5px border in the tone color, opacity .65 normal / 1 on the last card) as 4 absolutely-positioned `aria-hidden` spans, one per corner: top-left (`absolute top-0 left-0 size-[11px] border-t-[1.5px] border-l-[1.5px]`), top-right (`absolute top-0 right-0 size-[11px] border-t-[1.5px] border-r-[1.5px]`), bottom-left (`absolute bottom-0 left-0 size-[11px] border-b-[1.5px] border-l-[1.5px]`), bottom-right (`absolute bottom-0 right-0 size-[11px] border-b-[1.5px] border-r-[1.5px]`) — each with `style={{ borderColor: "var(--tone-color)", opacity: isLast ? 1 : 0.65 }}`. Extract these 4 into a single small local subcomponent (e.g. `CornerBrackets({ opacity }: { opacity: number })`) per CLAUDE.md's "no duplicated JSX for near-identical instances" convention, rendering all 4 spans from one component call per card.

    Give the existing `<FactReadout facts={entry.facts} />` an `mt-auto` wrapper (or add `mt-auto` directly to `FactReadout`'s own `<dl>` className) so fact chips pin to the card's bottom edge (Pitfall 4 — relies on the card already being `flex flex-col` from Task 1).
  </action>
  <acceptance_criteria>
    - `grep -c "trail-token\b" src/components/sandbox/reasoning/ReasoningTrail.tsx` returns at least 1
    - `grep -c "trail-token-sweep-ring" src/components/sandbox/reasoning/ReasoningTrail.tsx` returns at least 1
    - `grep -c "Contact \${String" src/components/sandbox/reasoning/ReasoningTrail.tsx` returns at least 1 (or equivalent literal check for the `CONTACT 0N` label template)
    - `grep -c "CornerBrackets" src/components/sandbox/reasoning/ReasoningTrail.tsx` returns at least 2 (component definition + at least one call site)
    - `grep -c "size-\[22px\]" src/components/sandbox/reasoning/ReasoningTrail.tsx` equals 0 (old number badge fully replaced)
    - `grep -c "mt-auto" src/components/sandbox/reasoning/ReasoningTrail.tsx` returns at least 1
    - `npx tsc --noEmit` reports no new errors in this file
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -c "trail-token-sweep-ring" src/components/sandbox/reasoning/ReasoningTrail.tsx</automated>
  </verify>
  <done>Each card renders a 38px token (gradient fill + spinning sweep ring + number), a "CONTACT 0N" label, a top accent bar, and 4 corner brackets, all colored via each step's `--tone-color` custom property; fact chips are bottom-pinned via `mt-auto` on a column-flex card.</done>
</task>

<task type="auto">
  <name>Task 3: Update ReasoningTrail.test.tsx for new copy and DOM shape</name>
  <files>src/components/sandbox/reasoning/ReasoningTrail.test.tsx</files>
  <read_first>src/components/sandbox/reasoning/ReasoningTrail.test.tsx (current full file), src/components/sandbox/reasoning/ReasoningTrail.tsx (as modified by Tasks 1-2 — confirm the exact card/connector selector to filter on)</read_first>
  <action>
    Replace every `screen.getByText("3 steps")` / `screen.getByText("5 steps")` assertion with `screen.getByText("3 contacts")` / `screen.getByText("5 contacts")` respectively (D-07 copy change).

    Replace the two `screen.getAllByRole("listitem")` usages that assume 1 `<li>` per trail entry (`toHaveLength(3)` and the `dlCount` computation) with a query scoped to card-only list items. Since `ReasoningTrail.tsx` now renders `2 * trail.length - 1` total `<li>`s (cards + connectors) per row, add a `data-role="trail-card"` attribute to the card `<li>` in `ReasoningTrail.tsx` (a one-line addition alongside its existing className, if not already present from Tasks 1-2) and query it here via `const { container } = render(...)` then `container.querySelectorAll('[data-role="trail-card"]')` — assert `.length` equals `trail.length` (not `2 * trail.length - 1`), and keep the existing per-index text-content assertions (`trailItems[0]`/`[1]`/`[2]` `toHaveTextContent(...)`) working against this card-only NodeList/array instead of the raw `getAllByRole("listitem")` result. The `dlCount` test's `.filter((item) => item.querySelector("dl") !== null).length` logic is unchanged in spirit — apply it to the same card-only collection (connectors never contain a `<dl>`, so this only affects which base collection is filtered, not the filter predicate itself).

    Leave every other assertion (tag/tone text queries: `"GEOMETRY"`, `"RULE 13"`, `"VERDICT"`, `"Rule 7"` count of 2, fact-format strings, both doubt-caveat verbatim strings) exactly as-is — none of that content changed.
  </action>
  <acceptance_criteria>
    - `grep -c "3 contacts\|5 contacts" src/components/sandbox/reasoning/ReasoningTrail.test.tsx` returns at least 2
    - `grep -c "3 steps\|5 steps" src/components/sandbox/reasoning/ReasoningTrail.test.tsx` equals 0
    - `grep -c "trail-card" src/components/sandbox/reasoning/ReasoningTrail.test.tsx` returns at least 1
    - `npx vitest run src/components/sandbox/reasoning/ReasoningTrail.test.tsx` — all 7 existing tests pass
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/sandbox/reasoning/ReasoningTrail.test.tsx</automated>
  </verify>
  <done>All 7 tests in ReasoningTrail.test.tsx pass against the restructured component: copy assertions read "contacts", and card-count/dl-count assertions are scoped to card-only `<li>`s via `data-role="trail-card"`, unaffected by the new interleaved connector `<li>`s.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. `ReasoningTrail.tsx` receives `classification: ClassificationResult` as a prop from its existing parent (unchanged data flow) and renders it — no new user input, no new network call, no new endpoint. This plan only restructures presentation markup and adds `className`/`style` (CSS custom-property) attributes.

## STRIDE Threat Register

No threats apply. Zero new data flow, zero new dependency, zero change to `classifyEncounter()` or any `src/domain/` logic. The only "input" this component ever receives (`classification`) is produced entirely in-process by the existing, unmodified domain layer. No ASVS Level 1 category is implicated by a presentation-only restructure of an already-static-input component.
</threat_model>

<verification>
- `npx tsc --noEmit` — no new type errors across the 2 modified files.
- `npx vitest run src/components/sandbox/reasoning/ReasoningTrail.test.tsx` — all tests pass.
- `grep -c "getBoundingClientRect\|getBBox" src/components/sandbox/reasoning/ReasoningTrail.tsx` — equals 0 (jsdom-measurement-ban precedent honored).
- Manual/human-browser confirmation of the 3/4/5-step horizontal layout and the <900px column fallback happens in plan 20-05 (this plan's automated checks cover structure/copy/DOM shape only, not real-browser visual rendering).
</verification>

<success_criteria>
`ReasoningTrail.tsx` renders a true horizontal "NAV DECISION CHAIN" (two `<li>`s per non-last step, `min-[900px]:flex-row` row / base `flex-col` stack), with the D-07 copy changes and all three CSS-only animation layers wired by className reference to plan 20-01's rules; `ReasoningTrail.test.tsx`'s 7 tests pass unmodified in intent, updated only for the copy/DOM-shape changes this restructure requires.
</success_criteria>

<output>
Create `.planning/phases/20-reasoning-trail-hero-visual-sync/20-02-SUMMARY.md` when done
</output>
