---
phase: 20-reasoning-trail-hero-visual-sync
plan: 05
type: execute
wave: 4
depends_on: ["20-02", "20-04"]
files_modified: []
autonomous: false
requirements: [SBOX-09, HERO-05]

must_haves:
  truths:
    - "The reasoning trail renders as one horizontal row of connected step cards (not a wrapped/vertical list) at >=900px, correctly for 3-, 4-, and 5-step trails (ROADMAP criterion 1)"
    - "The trail's connector treatment is confirmed CSS-only in a real browser and renders correctly across different step counts, with prefers-reduced-motion freezing all trail/Hero animations to a static state (ROADMAP criterion 2)"
    - "The Hero preview card's bezel accents, radar-sweep overlay, and readout styling visually match the updated design file, with the card remaining fully static/fixture-driven (ROADMAP criterion 3)"
    - "No regression to Hero's fixture-driven classification values or to any other Sandbox/Gallery/Tour content occurred during this phase, including no shift to the /#gallery anchor-scroll position (ROADMAP criterion 4)"
  artifacts: []
  key_links: []
---

<objective>
Human-verify, in a real browser, that all 4 ROADMAP Phase 20 success criteria hold end-to-end -- something jsdom-based automated tests (Plans 20-01 through 20-04) structurally cannot observe: real CSS flex-row/column layout behavior across different trail lengths, genuine `prefers-reduced-motion` animation gating, and true visual fidelity of the Hero card's bezel/radar-sweep/readout restructuring against the design file, plus real anchor-scroll position after any Hero card height change.

Purpose: Per this project's own documented precedent (STATE.md: "every v1.4 phase's success criteria include an explicit human-browser-verification item... jsdom cannot observe real pointer-capture routing, focus-trap escape, or CSS stacking order") and this phase's own RESEARCH.md Pitfall 3 (Hero's box-height/`/#gallery` scroll-shift risk), this is the mandatory human sign-off gate for Phase 20.
Output: Human confirmation that all 4 ROADMAP Phase 20 success criteria are satisfied in a real browser session, closing out the phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/20-reasoning-trail-hero-visual-sync/20-CONTEXT.md
@.planning/phases/20-reasoning-trail-hero-visual-sync/20-DESIGN-SNAPSHOT.md
@docs/reasoning-trails.json
@.planning/phases/20-reasoning-trail-hero-visual-sync/20-01-SUMMARY.md
@.planning/phases/20-reasoning-trail-hero-visual-sync/20-02-SUMMARY.md
@.planning/phases/20-reasoning-trail-hero-visual-sync/20-03-SUMMARY.md
@.planning/phases/20-reasoning-trail-hero-visual-sync/20-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human-verify the trail's horizontal layout across 3/4/5-step trails and the <900px stack fallback (SBOX-09, ROADMAP criterion 1)</name>
  <files>None -- verification-only task, no files modified</files>
  <read_first>
    .planning/phases/20-reasoning-trail-hero-visual-sync/20-DESIGN-SNAPSHOT.md (Section 1, "Structure" and "Responsive fallback")
    docs/reasoning-trails.json (trail-length distribution: 3 x3 scenarios, 4 x2, 5 x2)
    .planning/phases/20-reasoning-trail-hero-visual-sync/20-02-SUMMARY.md
  </read_first>
  <action>
    Start the dev server and drive the Sandbox into encounters producing 3-, 4-, and 5-step trails, confirming the row stays a single horizontal line at each length at >=900px, and flips to a vertical stack below 900px.
  </action>
  <what-built>
    ReasoningTrail.tsx now renders a true horizontal "NAV DECISION CHAIN": a single flex row of `flex:1 1 0` step cards joined by `flex:0 0 34px` connector segments (two `<li>`s per non-last step via one `trail.flatMap()` pass), flipping to a `flex-direction: column` stack with a fixed 26px connector band below the codebase's existing 900px breakpoint.
  </how-to-verify>
  <how-to-verify>
    1. Run `npm run dev` and open `http://localhost:3000/`, scrolled to the Sandbox. At a browser width >=900px, drag the vessels into a 3-step trail encounter (e.g. a simple crossing) -- confirm the trail renders as ONE horizontal row (not wrapped to a second line) with 3 comfortably-wide cards.
    2. Drag into a 5-step trail encounter (e.g. a near-boundary doubt-flagged case) -- confirm the row still renders as ONE horizontal line, with all 5 narrower cards fitting without text/content overflow or clipping (check the fact-chip row and rule-id/tag row specifically for wrapping issues).
    3. Confirm a 4-step trail also renders as one clean horizontal row.
    4. Confirm the trail header reads "NAV DECISION CHAIN · radar acquisition" with a 14px spinning dot, and the count badge reads "N contacts" (not "N steps") at every step count.
    5. Resize the browser window below 900px -- confirm the trail flips to a vertical stack (cards full-width, one per row) with the connector band between cards visibly shrunk to a fixed short band, NOT the horizontal row simply squeezed narrower.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 checks pass as expected, or describe the specific step/trail-length/behavior that differs.</resume-signal>
  <acceptance_criteria>
    - Human types "approved" after confirming all 5 steps behave as expected
    - The trail renders as a single horizontal row (no wrapping) at 3, 4, and 5 steps at >=900px (behavior assertion, ROADMAP criterion 1)
    - No text/content overflow or clipping occurs at the narrowest (5-card) end of the range (behavior assertion, RESEARCH.md Pitfall 1)
    - Below 900px the trail stacks vertically with a shrunk fixed-height connector band, not a squeezed horizontal row (behavior assertion, RESEARCH.md Pitfall 2)
  </acceptance_criteria>
  <verify>
    <automated>MISSING -- this is a human-browser-verification checkpoint; no automated test can observe true CSS flex-row rendering/wrapping behavior across real trail lengths in a real viewport, per this project's jsdom-measurement-ban precedent</automated>
  </verify>
  <done>Human has typed "approved", confirming the trail renders as a genuine single horizontal row at 3/4/5 steps with no overflow, and correctly stacks vertically below 900px.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify CSS-only connector/token animations and full prefers-reduced-motion gating (SBOX-09/HERO-05, ROADMAP criterion 2)</name>
  <files>None -- verification-only task, no files modified</files>
  <read_first>
    .planning/phases/20-reasoning-trail-hero-visual-sync/20-CONTEXT.md (D-05, D-06)
    .planning/phases/20-reasoning-trail-hero-visual-sync/20-01-SUMMARY.md
  </read_first>
  <action>
    Confirm the connector's dash-flow and traveling-pulse animate smoothly at every trail length, confirm each step token's sweep ring spins, confirm the Hero radar-sweep overlay and LIVE dot animate, then toggle OS-level "reduce motion" and confirm all five animation layers freeze to a static state with zero animation.
  </action>
  <what-built>
    Three CSS-only trail animation layers (`.trail-connector`'s dash-flow, `.trail-connector-pulse`'s traveling dot, `.trail-token-sweep-ring`'s per-token spin) plus Hero's `.hero-radar-sweep-inner` overlay spin and `.hero-live-pulse` dot, all driven by `@keyframes` in `app/globals.css`, each wrapped in `@media (prefers-reduced-motion: no-preference)` with a fully static fallback.
  </how-to-verify>
  <how-to-verify>
    1. With motion NOT reduced (default OS setting), watch a trail's connectors -- confirm the dashed line shows a continuous flowing motion and a small pulse dot travels along each connector from card to card, with no layout jank or measurement flicker (this is pure `background-position`/`left` animation, no JS).
    2. Confirm each step's 38px token shows a visibly spinning ring behind its number.
    3. Scroll to the Hero card -- confirm a faint rotating radar-sweep overlay is visible over the chart, and the footer's LIVE dot pulses noticeably SLOWER and DIMMER than the real Sandbox's own LIVE dot (compare the two side by side by scrolling between Hero and the Sandbox footer).
    4. Enable your OS's "reduce motion" setting (e.g. macOS: System Settings -> Accessibility -> Display -> Reduce Motion), reload the page -- confirm ALL FIVE animations (trail connector flow, trail traveling pulse, trail token sweep, Hero radar-sweep overlay, Hero LIVE pulse) are now completely static (no movement/flicker at all), not merely slowed down.
    5. Disable "reduce motion" again and confirm all five animations resume.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 checks pass as expected, or describe the specific animation/behavior that differs.</resume-signal>
  <acceptance_criteria>
    - Human types "approved" after confirming all 5 steps behave as expected
    - All 3 trail animation layers and both Hero animation layers are visibly running under default motion settings (behavior assertion, D-05)
    - Hero's LIVE dot is visibly slower/dimmer than the Sandbox's own LIVE dot when compared side by side (behavior assertion, D-02)
    - Every one of the 5 animations goes fully static (zero motion) when the OS's reduce-motion setting is enabled (behavior assertion, D-06, ROADMAP criterion 2)
  </acceptance_criteria>
  <verify>
    <automated>MISSING -- this is a human-browser-verification checkpoint for real prefers-reduced-motion media-query behavior and real CSS keyframe animation, which cannot be observed via jsdom, per this project's jsdom-measurement-ban precedent</automated>
  </verify>
  <done>Human has typed "approved", confirming every phase-20 animation layer runs under default settings and goes fully static under reduce-motion, with Hero's LIVE dot visibly distinguished from the Sandbox's.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify Hero's bezel/header/footer visual fidelity to the design file (HERO-05, ROADMAP criterion 3)</name>
  <files>None -- verification-only task, no files modified</files>
  <read_first>
    .planning/phases/20-reasoning-trail-hero-visual-sync/20-DESIGN-SNAPSHOT.md (Section 2, full)
    .planning/phases/20-reasoning-trail-hero-visual-sync/20-03-SUMMARY.md
    .planning/phases/20-reasoning-trail-hero-visual-sync/20-04-SUMMARY.md
  </read_first>
  <action>
    Visually compare the Hero card's bezel, header strip, and footer strip against the design snapshot's described layout.
  </action>
  <what-built>
    HeroPreviewCard.tsx's chart now shows 3 concentric range rings, a dashed compass-tick ring with N/S/E/W cardinal labels and 3 range labels, a center hub, and a radar-sweep overlay; the header is a strip (rule chip + "Crossing" title + risk pill reading "Passing clear -- CPA 1.18 NM on present courses."); the footer is a single row (LIVE + RANGE + BEARING A->B + CPA + a net-new TCPA tile).
  </how-to-verify>
  <how-to-verify>
    1. Scroll to the Hero section. Confirm the chart's bezel shows 3 distinct concentric range rings (not 2), a dashed ring with visible tick marks around the outside, and N/S/E/W letters positioned at the bezel's edge.
    2. Confirm 3 small range labels ("0.5 NM"/"1.0 NM"/"1.5 NM") are visible stacked above the chart's center, and a small ringed dot sits at dead-center.
    3. Confirm NO "Live classification" text or "BRG-ring · 12 NM" text appears anywhere on the card (D-04 -- fully removed, not relocated).
    4. Confirm the header strip directly above the chart shows a "Rule 15" chip, a "Crossing" title, and a risk pill reading "Passing clear — CPA 1.18 NM on present courses."
    5. Confirm NO separate below-chart verdict banner/sentence exists anymore (its role is now fully covered by the header strip).
    6. Confirm the footer strip below the chart shows, in one row: a LIVE indicator with dot + text, then RANGE (2.99 NM), BEARING A→B (061°), CPA (1.18 NM), and a new TCPA tile with a numeric minutes value -- NOT the old 3-column grid.
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 checks pass as expected, or describe the specific visual element that differs from the design.</resume-signal>
  <acceptance_criteria>
    - Human types "approved" after confirming all 6 steps behave as expected
    - The bezel shows 3 rings + compass ticks + cardinal labels + center hub + radar-sweep overlay (behavior assertion, ROADMAP criterion 3)
    - The old "Live classification"/"BRG-ring · 12 NM" eyebrow and the old below-chart verdict banner are both fully absent, not merely relocated (behavior assertion, D-04)
    - The footer shows a single LIVE+RANGE+BEARING A→B+CPA+TCPA row, not the old 3-column grid (behavior assertion, D-03)
  </acceptance_criteria>
  <verify>
    <automated>MISSING -- this is a human-browser-verification checkpoint; no automated test can observe true visual/CSS fidelity to the design snapshot, per this project's jsdom-measurement-ban precedent</automated>
  </verify>
  <done>Human has typed "approved", confirming the Hero card's bezel, header strip, and footer strip visually match the design file's richer instrument-panel treatment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify zero regression to Hero's classification values, card height, and /#gallery scroll (ROADMAP criterion 4)</name>
  <files>None -- verification-only task, no files modified</files>
  <read_first>
    .planning/phases/20-reasoning-trail-hero-visual-sync/20-RESEARCH.md (Common Pitfalls, Pitfall 3)
    .planning/milestones/v1.1-phases/07-hero/07-CONTEXT.md (D-01's original layout-shift rationale)
  </read_first>
  <action>
    Confirm the Hero card's underlying classification numbers are unchanged, confirm the card's total rendered height has not grown, and confirm the `/#gallery` anchor scroll still lands correctly.
  </action>
  <what-built>
    This phase changed only presentation (header/footer strips, bezel richness, animation layers) -- `classifyEncounter()`, `bearing()`, `cpa()`, and the `heroPreviewVesselA`/`heroPreviewVesselB` fixture were never touched, and every new SVG element was added within the existing unchanged `viewBox="0 0 320 200"`.
  </how-to-verify>
  <how-to-verify>
    1. Confirm the Hero card's displayed values read exactly: encounter "Crossing", RANGE 2.99 NM, BEARING A→B 061°, CPA 1.18 NM (same numbers as before this phase — cross-check against `.planning/milestones/v1.1-phases/07-hero/07-CONTEXT.md`'s reproduced fixture derivation if needed).
    2. Visually compare the Hero card's total height against its pre-phase-20 appearance (or simply confirm nothing on the page below the Hero card jumps/shifts unexpectedly compared to a typical page load) -- the card's `viewBox` and container aspect ratio were not supposed to change.
    3. Click the Hero's "Classic encounters" CTA (the link that anchors to `/#gallery`) -- confirm the page smooth-scrolls and the Gallery section's heading lands correctly under the sticky header (not offset above/below where it used to land), matching this project's existing `scroll-padding-top: 64px` convention.
    4. Spot-check the rest of the page: the Sandbox's chart header/footer strips, vessel overlay, and Guided Tour trigger, and the Gallery section itself -- confirm nothing outside this phase's two target surfaces (ReasoningTrail, HeroPreviewCard) visibly changed or broke.
  </how-to-verify>
  <resume-signal>Type "approved" if all 4 checks pass as expected, or describe the specific regression observed.</resume-signal>
  <acceptance_criteria>
    - Human types "approved" after confirming all 4 steps behave as expected
    - Hero's displayed classification values are unchanged from before this phase (behavior assertion, ROADMAP criterion 4)
    - The Hero card's rendered height has not visibly grown in a way that shifts subsequent page layout (behavior assertion, RESEARCH.md Pitfall 3, D-01's original rationale)
    - The `/#gallery` anchor scroll still lands correctly under the sticky header (behavior assertion, RESEARCH.md Pitfall 3)
    - No other Sandbox/Gallery/Tour surface regressed during this phase (behavior assertion, ROADMAP criterion 4)
  </acceptance_criteria>
  <verify>
    <automated>MISSING -- this is a human-browser-verification checkpoint for real box-height/anchor-scroll behavior, which jsdom cannot observe, per this project's jsdom-measurement-ban precedent and Phase 7's documented D-01 layout-shift rationale</automated>
  </verify>
  <done>Human has typed "approved", confirming Hero's classification values and card height are unchanged, the /#gallery anchor scroll is unaffected, and no other surface regressed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new this plan | This plan performs no code changes -- it is a human-verification checkpoint over Plans 20-01 through 20-04's implementation. No new trust boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | N/A | This plan modifies zero files | accept | No code changes in this plan; the STRIDE registers for the actual code changes live in Plans 20-01/20-02/20-03/20-04's own `<threat_model>` blocks (all of which found zero applicable threats, since this entire phase is presentation-only with zero new dependencies, data flow, or user input). This checkpoint exists purely to human-verify runtime/visual/layout behavior, not to introduce or mitigate a new threat surface. |
</threat_model>

<verification>
Human confirms all 4 tasks' `<how-to-verify>` steps behave as expected against the live `/` route in a real browser, with particular attention to Task 4's Hero card-height/`/#gallery` scroll re-check (this phase's own RESEARCH.md-flagged Pitfall 3).
</verification>

<success_criteria>
All 4 ROADMAP Phase 20 success criteria are satisfied and human-confirmed:
1. The reasoning trail renders as a horizontal sequence of connected step cards at every real trail length (3-5 steps), correctly stacking below 900px.
2. The connector treatment is CSS-only and renders correctly across step counts; every phase-20 animation respects prefers-reduced-motion.
3. The Hero preview card's bezel accents, radar-sweep overlay, and readout styling visually match the updated design file, while remaining fully static/fixture-driven.
4. No regression to Hero's fixture-driven classification values, card height, `/#gallery` scroll behavior, or any other Sandbox/Gallery/Tour content.
</success_criteria>

<output>
Create `.planning/phases/20-reasoning-trail-hero-visual-sync/20-05-SUMMARY.md` when done
</output>
