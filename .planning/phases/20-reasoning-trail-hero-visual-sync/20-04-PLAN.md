---
phase: 20-reasoning-trail-hero-visual-sync
plan: 04
type: execute
wave: 3
depends_on: ["20-03"]
files_modified: [src/components/hero/HeroPreviewCard.tsx, src/components/hero/hero-preview-geometry.ts, src/components/hero/Hero.test.tsx]
autonomous: true
requirements: [HERO-05]
must_haves:
  truths:
    - "Hero's SVG bezel shows 3 range rings + a dashed compass-tick ring (minor+major ticks) + a full crosshair + N/S/E/W cardinal labels + 3 range labels + a center hub, all rendered within the existing viewBox='0 0 320 200' (no viewBox change, no card box-height growth)"
    - "A CSS-only radar-sweep overlay animates over the Hero chart, gated by prefers-reduced-motion, without affecting the card's layout height (position: absolute, not part of document flow)"
    - "Hero's classification output and fixture remain byte-for-byte unchanged — this plan only adds decorative SVG elements and one absolutely-positioned overlay div"
    - "HERO_OUTER_RING_RADIUS_PX/HERO_INNER_RING_RADIUS_PX (kept alive but unused-by-consumer since plan 20-03) have zero remaining references anywhere in the codebase, including in hero-preview-geometry.ts's own exports, after this plan completes"
  artifacts:
    - path: "src/components/hero/HeroPreviewCard.tsx"
      provides: "3-ring bezel + compass-tick ring + crosshair + cardinal/range labels + center hub + radar-sweep overlay"
      contains: "hero-radar-sweep"
    - path: "src/components/hero/hero-preview-geometry.ts"
      provides: "removal of the retired HERO_OUTER_RING_RADIUS_PX/HERO_INNER_RING_RADIUS_PX exports, completing the migration plan 20-03 deferred"
      contains: "HERO_RANGE_RING_RADII_PX"
  key_links:
    - from: "HeroPreviewCard.tsx's bezel SVG elements"
      to: "hero-preview-geometry.ts's HERO_RANGE_RING_RADII_PX/HERO_COMPASS_*/HERO_RANGE_LABEL_TEXT constants (plan 20-03)"
      via: "named import"
      pattern: "HERO_RANGE_RING_RADII_PX"
    - from: "HeroPreviewCard.tsx's overlay div className"
      to: "app/globals.css's .hero-radar-sweep/.hero-radar-sweep-inner rules (plan 20-01)"
      via: "shared className string"
      pattern: "hero-radar-sweep"
---

<objective>
Extend `HeroPreviewCard.tsx`'s existing `<svg viewBox="0 0 320 200">` with the design's richer bezel (3rd range ring, compass-tick dashed ring, full crosshair, N/S/E/W cardinal labels, range labels, center hub) using the constants plan 20-03 added to `hero-preview-geometry.ts`, and add the radar-sweep overlay as an absolutely-positioned sibling div. Verify (and update if needed) `Hero.test.tsx` against the final rendered output.

Purpose: Complete HERO-05's visual-fidelity requirement (bezel richness + radar sweep) while explicitly guarding against Pitfall 3 (Phase 7's D-01 layout-shift concern) — every new element must fit inside the existing 320×200 viewBox with no box-height growth, satisfying Roadmap criterion 4's "no regression to Hero's fixture-driven values or `/#gallery` scroll" requirement. This plan also completes the `HERO_OUTER_RING_RADIUS_PX`/`HERO_INNER_RING_RADIUS_PX` retirement that plan 20-03 intentionally deferred here, so the repo never sits in a non-compiling intermediate state between the two plans.
Output: `HeroPreviewCard.tsx`'s SVG extended in place; the two retired ring-radius constants removed from `hero-preview-geometry.ts`; `Hero.test.tsx` confirmed passing (or updated if the new bezel introduces a text-content collision).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-reasoning-trail-hero-visual-sync/20-CONTEXT.md
@.planning/phases/20-reasoning-trail-hero-visual-sync/20-DESIGN-SNAPSHOT.md
@.planning/phases/20-reasoning-trail-hero-visual-sync/20-UI-SPEC.md
</context>

<interfaces>
<!-- Constants this plan consumes, added by plan 20-03. No re-derivation needed. -->

From src/components/hero/hero-preview-geometry.ts (as extended by plan 20-03 -- note plan 20-03 deliberately KEPT `HERO_OUTER_RING_RADIUS_PX`/`HERO_INNER_RING_RADIUS_PX` exported and untouched so the repo would keep compiling in the interim; this plan's Task 1 is where those two exports and their last remaining consumers in HeroPreviewCard.tsx are finally removed):
- `HERO_RANGE_RING_RADII_PX: readonly [number, number, number]` — the 3 range-ring radii in px (28/56/84 at this codebase's 320px width). `HERO_RANGE_RING_RADII_PX[2]` (84px) is this plan's direct replacement for every remaining `HERO_OUTER_RING_RADIUS_PX` usage.
- `HERO_COMPASS_RING_RADIUS_PX: number` — 88px, the compass-tick ring radius.
- `HERO_COMPASS_MINOR_STROKE_WIDTH_PX`, `HERO_COMPASS_MINOR_DASHARRAY`, `HERO_COMPASS_MAJOR_STROKE_WIDTH_PX`, `HERO_COMPASS_MAJOR_DASHARRAY` — pre-scaled stroke/dasharray values for the two stacked compass-ring circles.
- `HERO_CENTER_HUB_RADIUS_PX: number` — 3px.
- `HERO_CARDINAL_LABEL_OFFSET_PX: number` — radius at which N/S/E/W labels sit (equals `HERO_COMPASS_RING_RADIUS_PX`).
- `HERO_RANGE_LABEL_TEXT: readonly string[]` — `["0.5 NM", "1.0 NM", "1.5 NM"]`.
- `HERO_CHART_CENTER: { screenX: number; screenY: number }` — unchanged, the fixed center every new element is positioned relative to.

From app/globals.css (added by plan 20-01):
- `.hero-radar-sweep` — outer positioned wrapper class (apply directly, no custom property needed).
- `.hero-radar-sweep-inner` — inner conic-gradient/animation class (apply directly, no custom property needed).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend the bezel SVG — 3rd ring, compass ticks, crosshair, cardinal/range labels, center hub</name>
  <files>src/components/hero/HeroPreviewCard.tsx, src/components/hero/hero-preview-geometry.ts</files>
  <read_first>src/components/hero/HeroPreviewCard.tsx (as modified by plan 20-03 — current full file), src/components/hero/hero-preview-geometry.ts (as extended by plan 20-03 — the exact constant names/values this task imports), .planning/phases/20-reasoning-trail-hero-visual-sync/20-DESIGN-SNAPSHOT.md (Section 2 "Bezel" — exact colors/opacities)</read_first>
  <action>
    Update the import from `./hero-preview-geometry.js` to add `HERO_RANGE_RING_RADII_PX`, `HERO_COMPASS_RING_RADIUS_PX`, `HERO_COMPASS_MINOR_STROKE_WIDTH_PX`, `HERO_COMPASS_MINOR_DASHARRAY`, `HERO_COMPASS_MAJOR_STROKE_WIDTH_PX`, `HERO_COMPASS_MAJOR_DASHARRAY`, `HERO_CENTER_HUB_RADIUS_PX`, `HERO_CARDINAL_LABEL_OFFSET_PX`, `HERO_RANGE_LABEL_TEXT`, and REMOVE `HERO_OUTER_RING_RADIUS_PX`/`HERO_INNER_RING_RADIUS_PX` from this import list. Plan 20-03 deliberately kept both exports alive and untouched in `hero-preview-geometry.ts` (rather than deleting them there) specifically so the repo would keep compiling in the interim — this task is where that deferred migration completes, for all 4 of this file's remaining usages.

    Replace the two existing `<circle ... r={HERO_OUTER_RING_RADIUS_PX} .../>` and `<circle ... r={HERO_INNER_RING_RADIUS_PX} .../>` range-ring elements with THREE `<circle>` elements, one per `HERO_RANGE_RING_RADII_PX[0|1|2]`, each `cx={HERO_CHART_CENTER.screenX} cy={HERO_CHART_CENTER.screenY} stroke="rgba(45,212,191,0.16)" fill="none" strokeWidth={1}` (same stroke treatment as the current 2 rings, just 3 radii now).

    Also migrate this file's two other remaining `HERO_OUTER_RING_RADIUS_PX` usages (the full inventory of 4 old-constant references in this file is: the 2 range-ring `<circle>`s just handled above, plus these 2): the `radialGradient`'s `r={HERO_OUTER_RING_RADIUS_PX}` becomes `r={HERO_RANGE_RING_RADII_PX[2]}`, and the existing north-reference `<line>`'s `y2={HERO_CHART_CENTER.screenY - HERO_OUTER_RING_RADIUS_PX}` becomes `y2={HERO_CHART_CENTER.screenY - HERO_RANGE_RING_RADII_PX[2]}` (both preserve the "extend to the outermost ring" role the old constant played, now at the new 84px outer radius — matching `bearingSectorPath()`'s own internal update from plan 20-03). After these 4 migrations, `HeroPreviewCard.tsx` has zero remaining references to `HERO_OUTER_RING_RADIUS_PX`/`HERO_INNER_RING_RADIUS_PX` — confirm with grep before proceeding.

    Immediately after the 3 range rings (and before the existing sector-path `<path d={sectorPath} .../>`, so the crosshair/compass sit visually behind the sector wedge and vessel markers), add:
    - A full crosshair: one horizontal `<line x1={0} y1={HERO_CHART_CENTER.screenY} x2={320} y2={HERO_CHART_CENTER.screenY} stroke="#3F3F46" strokeOpacity={0.7} strokeWidth={1} />` and one vertical `<line x1={HERO_CHART_CENTER.screenX} y1={0} x2={HERO_CHART_CENTER.screenX} y2={200} stroke="#3F3F46" strokeOpacity={0.7} strokeWidth={1} />` (design's "full N-S and E-W lines through center, #3F3F46 @ .7 opacity" — distinct from and in addition to the existing single north-reference `<line>`, which stays as-is since it plays a different role: the bearing-direction indicator, not decorative bezel background).
    - Two stacked compass-tick circles at `r={HERO_COMPASS_RING_RADIUS_PX}`, both `cx`/`cy` at `HERO_CHART_CENTER`, `fill="none"`: the minor-tick circle (`stroke="rgba(45,212,191,0.32)" strokeWidth={HERO_COMPASS_MINOR_STROKE_WIDTH_PX} strokeDasharray={HERO_COMPASS_MINOR_DASHARRAY}`) and the major/cardinal-tick circle (`stroke="rgba(45,212,191,0.5)" strokeWidth={HERO_COMPASS_MAJOR_STROKE_WIDTH_PX} strokeDasharray={HERO_COMPASS_MAJOR_DASHARRAY}`).
    - 4 cardinal `<text>` labels (`N`/`S`/`E`/`W`) at `HERO_CARDINAL_LABEL_OFFSET_PX` from center — N at `(HERO_CHART_CENTER.screenX, HERO_CHART_CENTER.screenY - HERO_CARDINAL_LABEL_OFFSET_PX)`, S at `(..., + HERO_CARDINAL_LABEL_OFFSET_PX)`, E at `(HERO_CHART_CENTER.screenX + HERO_CARDINAL_LABEL_OFFSET_PX, HERO_CHART_CENTER.screenY)`, W at `(... - HERO_CARDINAL_LABEL_OFFSET_PX, ...)`, each `textAnchor="middle" dominantBaseline="middle" fill="#2dd4bf" fontSize={11} fontWeight={700} className="font-mono"`.
    - 3 range `<text>` labels using `HERO_RANGE_LABEL_TEXT`, stacked above center along the vertical crosshair at each of the 3 range-ring radii — position label `i` at `(HERO_CHART_CENTER.screenX + 4, HERO_CHART_CENTER.screenY - HERO_RANGE_RING_RADII_PX[i])`, `textAnchor="start" fill="#52525B" fontSize={9} className="font-mono"`.
    - A center hub: `<circle cx={HERO_CHART_CENTER.screenX} cy={HERO_CHART_CENTER.screenY} r={HERO_CENTER_HUB_RADIUS_PX} fill="#0B0B0E" stroke="#2dd4bf" strokeWidth={1.5} />`.

    Do not change the `<svg viewBox="0 0 320 200" ...>` attribute itself, and do not change `HERO_VIEW_BOX`/`HERO_CONTAINER_SIZE` in `hero-preview-geometry.ts` — every new element above uses coordinates already inside the existing 320×200 box (Pitfall 3 guard).

    Once `HeroPreviewCard.tsx` has zero remaining references to `HERO_OUTER_RING_RADIUS_PX`/`HERO_INNER_RING_RADIUS_PX` (verified above), delete both exports from `src/components/hero/hero-preview-geometry.ts` entirely — they now have zero consumers anywhere in the codebase. This completes, in this single task, the constant-retirement migration plan 20-03 intentionally deferred here to avoid a mid-migration non-compiling state.
  </action>
  <acceptance_criteria>
    - `grep -c "HERO_RANGE_RING_RADII_PX\[0\]\|HERO_RANGE_RING_RADII_PX\[1\]\|HERO_RANGE_RING_RADII_PX\[2\]" src/components/hero/HeroPreviewCard.tsx` returns at least 4 (3 range-ring circles + the radialGradient/north-line's shared use of `[2]`)
    - `grep -c "HERO_COMPASS_RING_RADIUS_PX" src/components/hero/HeroPreviewCard.tsx` returns at least 2 (two stacked compass circles)
    - `grep -c "viewBox=\"0 0 320 200\"" src/components/hero/HeroPreviewCard.tsx` equals 1 (unchanged)
    - `grep -c "HERO_OUTER_RING_RADIUS_PX\|HERO_INNER_RING_RADIUS_PX" src/components/hero/HeroPreviewCard.tsx` equals 0 (no stale reference to the retired constants — all 4 prior usages migrated: 2 range-ring circles, the radialGradient's `r`, the north-reference line's `y2`)
    - `grep -c "HERO_OUTER_RING_RADIUS_PX\|HERO_INNER_RING_RADIUS_PX" src/components/hero/hero-preview-geometry.ts` equals 0 (both exports fully deleted from source, not merely unreferenced by consumers — this is the migration plan 20-03 deferred to this task)
    - `grep -c "getBoundingClientRect\|getBBox" src/components/hero/HeroPreviewCard.tsx` equals 0
    - `npx tsc --noEmit` reports no errors project-wide (this task is where the constant-retirement migration completes, so the whole repo — not just these 2 files — must compile cleanly)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && grep -c "HERO_RANGE_RING_RADII_PX" src/components/hero/HeroPreviewCard.tsx</automated>
  </verify>
  <done>The Hero SVG renders 3 range rings, a dashed 2-layer compass-tick ring, a full crosshair, 4 cardinal labels, 3 range labels, and a center hub — all inside the unchanged 320×200 viewBox, driven entirely by hero-preview-geometry.ts's new constants (no hand-typed magic numbers duplicated in the component). HERO_OUTER_RING_RADIUS_PX/HERO_INNER_RING_RADIUS_PX are fully deleted from hero-preview-geometry.ts with zero remaining references anywhere in the codebase, completing the migration plan 20-03 intentionally deferred to this task; the whole repo compiles cleanly (project-wide `tsc --noEmit`).</done>
</task>

<task type="auto">
  <name>Task 2: Add the radar-sweep overlay div and confirm Hero.test.tsx</name>
  <files>src/components/hero/HeroPreviewCard.tsx, src/components/hero/Hero.test.tsx</files>
  <read_first>src/components/hero/HeroPreviewCard.tsx (as modified by Task 1 above — locate the chart-wrapper div, currently `<div className="overflow-hidden rounded-md border border-border bg-[#0B0B0E]">`), src/components/hero/Hero.test.tsx (full current file — confirm which assertions could collide with new SVG text content)</read_first>
  <action>
    Add `relative` to the chart-wrapper div's className (`overflow-hidden rounded-md border border-border bg-[#0B0B0E] relative`) so the overlay below positions correctly against it. Immediately after the closing `</svg>` tag (as a sibling, NOT nested inside the `<svg>`), add: `<div className="hero-radar-sweep" aria-hidden="true"><div className="hero-radar-sweep-inner" /></div>` (both classes from plan 20-01 — no inline style needed, the CSS classes carry every value verbatim from the design snapshot).

    Run `npx vitest run src/components/hero/Hero.test.tsx` against the fully-restructured card (Tasks 1-2 of this plan plus plan 20-03's header/footer changes). If it fails because the new cardinal-label `N`/`S`/`E`/`W` `<text>` elements or range-label text collide with an existing `screen.getByRole`/`getByText` query (the current test only queries `heading`, two `link`s, `"Rule 15"` text, and `"2.99 NM"` text — none of which match `N`/`S`/`E`/`W`/`"0.5 NM"`/`"1.0 NM"`/`"1.5 NM"`, so no collision is expected), leave the test file untouched. If a genuine collision is found, make the minimal targeted fix (e.g. scope an ambiguous query with an accessible name or role) and note the exact change in this plan's SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -c "hero-radar-sweep\b" src/components/hero/HeroPreviewCard.tsx` returns at least 1
    - `grep -c "hero-radar-sweep-inner" src/components/hero/HeroPreviewCard.tsx` returns at least 1
    - `grep -c "position: relative\|className=\"[^\"]*relative" src/components/hero/HeroPreviewCard.tsx` — the chart-wrapper div's className now includes `relative`
    - `npx vitest run src/components/hero/Hero.test.tsx` — passes (all assertions, updated or not)
    - `npx tsc --noEmit` reports no errors
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/hero/Hero.test.tsx</automated>
  </verify>
  <done>The radar-sweep overlay renders as a `position: absolute` sibling of the SVG inside a `position: relative` chart wrapper (confirmed not to affect layout height), and Hero.test.tsx passes against the fully-restructured card.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None. This plan adds only decorative SVG elements (circles, lines, text) and one `position: absolute`, `pointer-events`-inert overlay div to an already-static, fixture-driven component. No new data flow, no new user input, no new endpoint.

## STRIDE Threat Register

No threats apply. Zero new npm packages, zero change to `classifyEncounter()`/`bearing()`/`cpa()` or the fixture. The radar-sweep overlay is `pointer-events: none` (set in plan 20-01's `.hero-radar-sweep` rule) so it cannot intercept or spoof user interaction — it is purely decorative and non-interactive. No ASVS Level 1 category applies to adding static SVG markup and a CSS-animated overlay.
</threat_model>

<verification>
- `npx vitest run src/components/hero/Hero.test.tsx` — passes.
- `npx tsc --noEmit` — no new type errors, checked project-wide (this plan's Task 1 removes the last two references to the retired ring-radius constants).
- `grep -c "viewBox=\"0 0 320 200\"" src/components/hero/HeroPreviewCard.tsx` — confirms the viewBox is unchanged (Pitfall 3 guard).
- `grep -c "HERO_OUTER_RING_RADIUS_PX\|HERO_INNER_RING_RADIUS_PX" src/components/hero/hero-preview-geometry.ts` — equals 0, confirming the plan 20-03→20-04 constant migration is fully complete with no dangling exports.
- Human-browser confirmation that the card's total rendered height is unchanged and `/#gallery` anchor-scroll behavior is unaffected happens in plan 20-05 (this plan's automated checks cover markup/viewBox/test-pass only, not real-browser layout measurement).
</verification>

<success_criteria>
HeroPreviewCard.tsx's bezel visually matches the design's richer instrument-panel aesthetic (3 rings + compass ticks + crosshair + cardinal/range labels + center hub) entirely within the pre-existing 320×200 viewBox, plus a CSS-only radar-sweep overlay — with Hero.test.tsx passing and zero change to any domain call or fixture value.
</success_criteria>

<output>
Create `.planning/phases/20-reasoning-trail-hero-visual-sync/20-04-SUMMARY.md` when done
</output>
