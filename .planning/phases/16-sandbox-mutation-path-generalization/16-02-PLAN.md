---
phase: 16-sandbox-mutation-path-generalization
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified: []
autonomous: false
requirements: [SBOX-10]

must_haves:
  truths:
    - "Dragging/rotating a vessel on the plain `/` route behaves identically to before the refactor"
    - "Editing a vessel via ControlPanel (speed/type sliders/select) on the plain `/` route behaves identically to before the refactor"
    - "Clicking Reset scenario on the plain `/` route restores the default fixture and clears degenerate/hysteresis state identically to before the refactor"
  artifacts: []
  key_links: []
---

<objective>
Human-verify, in a real browser on the plain `/` route, that every Sandbox mutation source (drag, rotate, ControlPanel field edit, Reset) still behaves identically after Plan 16-01's `loadScenario()` refactor — closing out ROADMAP Phase 16 success criterion 3.

Purpose: This codebase has shipped "tests green, feature broken" bugs before (dev-server bug in Phase 5, hit-testing regressions in Phase 4/8), and jsdom-based automated tests cannot fully substitute for real pointer/drag verification. Per D-08, this check is explicitly scoped to the plain `/` route only — the `/s/[shareId]` saved-scenario route is not required this phase because the refactor doesn't touch `initialScenario` seeding logic, only the mutation choke point downstream of it.
Output: Human confirmation that Plan 16-01's refactor introduced zero behavioral regression, satisfying ROADMAP Phase 16 success criterion 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/16-sandbox-mutation-path-generalization/16-CONTEXT.md
@.planning/phases/16-sandbox-mutation-path-generalization/16-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human-verify Sandbox mutation parity on the plain `/` route</name>
  <files>None — verification-only task, no files modified</files>
  <read_first>
    .planning/phases/16-sandbox-mutation-path-generalization/16-01-SUMMARY.md
    .planning/phases/16-sandbox-mutation-path-generalization/16-CONTEXT.md (D-08)
  </read_first>
  <action>
    Start the dev server and manually drive the 8-step verification sequence below against the plain `/` route to confirm Plan 16-01's loadScenario() refactor introduced zero behavioral regression to drag, rotate, ControlPanel edits, and Reset. This is a human-verification gate, not a code-writing task — no source files are touched.
  </action>
  <what-built>
    Plan 16-01 generalized `useSandboxState()`'s chip-preset mutation path into a single `loadScenario(vesselA, vesselB)` entry point, made `handleReset()` delegate to it, and removed the 6-chip preset row and its backing `chip-scenarios.ts` module. All 4 vessel-mutation sources (drag, rotate, ControlPanel edits, Reset) route through the same `applyVesselUpdate()` choke point as before — only the internal wiring changed, no new UI was added.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open `http://localhost:3000/` (the plain seedless route — do not test `/s/[shareId]`, out of scope per D-08).
    2. Confirm the 6-chip preset row is gone — no button row above the verdict card.
    3. Drag a vessel's hull to a new position — confirm the verdict/reasoning trail updates live, matching pre-refactor behavior.
    4. Drag a vessel's bow/rotation handle to change heading — confirm the verdict updates live.
    5. Open ControlPanel and adjust a vessel's speed slider and type selector — confirm the verdict updates live for each.
    6. Drag a vessel onto the other vessel's exact position (degenerate/coincident case) — confirm "Unable to classify" appears without losing the last-good reasoning trail underneath.
    7. Click "Reset scenario" after step 6 — confirm the default Crossing scenario is restored and the degenerate state clears.
    8. Repeat drag/rotate/edit once more after Reset — confirm the app continues to respond live (no stuck/stale state from the Reset→loadScenario delegation).
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 checks pass as expected, or describe any behavioral difference from before the refactor.</resume-signal>
  <acceptance_criteria>
    - Human types "approved" after confirming all 8 steps in how-to-verify behave identically to pre-refactor Sandbox behavior on the plain `/` route
    - No chip-row button is visible on `/` (behavior assertion, step 2)
    - Drag, rotate, ControlPanel speed/type edits, degenerate-drag, and Reset each still update the verdict/reasoning trail live (behavior assertions, steps 3-8)
  </acceptance_criteria>
  <verify>
    <automated>MISSING — this is a human-browser-verification checkpoint; no automated test can observe real pointer-capture/drag routing per this project's jsdom-measurement-ban precedent (see .planning/research/PITFALLS.md)</automated>
  </verify>
  <done>Human has typed "approved", confirming all 8 verification steps passed with no behavioral regression from the loadScenario() refactor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new this phase | This plan performs no code changes — it is a human-verification checkpoint over Plan 16-01's refactor. No new trust boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | N/A | This plan modifies zero files | accept | No code changes in this plan; the STRIDE register for the actual code change lives in Plan 16-01's `<threat_model>`. This checkpoint exists purely to human-verify runtime behavior, not to introduce or mitigate a new threat surface. |
</threat_model>

<verification>
Human confirms all 8 steps in Task 1's `<how-to-verify>` behave identically to pre-refactor Sandbox behavior on the plain `/` route.
</verification>

<success_criteria>
ROADMAP Phase 16 success criterion 3 is satisfied: "Existing Sandbox interactions (drag/rotate a vessel, edit via ControlPanel, Reset) behave identically to before the refactor — human-verified in a real browser, not just green tests," scoped to the plain `/` route per D-08.
</success_criteria>

<output>
Create `.planning/phases/16-sandbox-mutation-path-generalization/16-02-SUMMARY.md` when done
</output>
