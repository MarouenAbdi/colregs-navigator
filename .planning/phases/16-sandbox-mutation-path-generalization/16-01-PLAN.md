---
phase: 16-sandbox-mutation-path-generalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sandbox/hooks/useSandboxState.ts
  - src/components/sandbox/SandboxContainer.tsx
  - src/components/sandbox/chip-scenarios.ts
  - src/components/sandbox/chip-scenarios.test.ts
  - src/components/sandbox/SandboxContainer.test.tsx
autonomous: true
requirements: [SBOX-10]

must_haves:
  truths:
    - "The Sandbox page (`/` route) no longer displays the 6-chip preset row anywhere"
    - "useSandboxState() exposes a single loadScenario(vesselA, vesselB) entry point (handleChipSelect no longer exists)"
    - "handleReset() delegates its apply step to loadScenario() rather than duplicating the reset-then-apply body"
    - "No orphaned chip code (chip-scenarios.ts, CHIP_SCENARIOS, CHIP_ORDER, ChipId, activeChipId, handleChipSelect) remains anywhere in src/"
  artifacts:
    - path: "src/components/sandbox/hooks/useSandboxState.ts"
      provides: "single generalized loadScenario() mutation entry point"
      contains: "function loadScenario"
    - path: "src/components/sandbox/SandboxContainer.tsx"
      provides: "chip-row-free Sandbox composition"
      min_lines: 100
    - path: "src/components/sandbox/chip-scenarios.ts"
      provides: "DELETED — must not exist"
    - path: "src/components/sandbox/chip-scenarios.test.ts"
      provides: "DELETED — must not exist"
  key_links:
    - from: "handleReset (useSandboxState.ts)"
      to: "loadScenario (useSandboxState.ts)"
      via: "direct function call, delegating the apply step"
      pattern: "loadScenario\\(seedA, seedB\\)"
    - from: "SandboxContainer.test.tsx"
      to: "useSandboxState's loadScenario/handleReset contract"
      via: "RTL render + Reset-button click assertions"
      pattern: "Reset scenario"
---

<objective>
Generalize `useSandboxState()`'s chip-preset mutation path into a single reusable `loadScenario(vesselA, vesselB)` entry point, make `handleReset()` delegate to it, and remove the inline 6-chip preset row and its backing `chip-scenarios.ts` module entirely — the prerequisite Phase 17 (Gallery→Sandbox bridge) and the rest of v1.4 depend on.

Purpose: This is milestone step [1] per `.planning/research/ARCHITECTURE.md` Pattern 1 — every future vessel-mutation source (Gallery load in Phase 17, on-chart overlay edits in Phase 18) must funnel through this one choke point, not a parallel one. No new user-facing feature is added; the chip row's scenario-loading capability is intentionally NOT replaced this phase (Phase 17 replaces it).
Output: `useSandboxState.ts` with `loadScenario`/no `activeChipId`; `SandboxContainer.tsx` with no chip row; `chip-scenarios.ts` and `chip-scenarios.test.ts` deleted; `SandboxContainer.test.tsx` updated to exercise `loadScenario`'s contract instead of removed chip UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-sandbox-mutation-path-generalization/16-CONTEXT.md
@.planning/phases/16-sandbox-mutation-path-generalization/16-PATTERNS.md
</context>

<interfaces>
<!-- Target public return-type shape of useSandboxState() after this plan (from 16-PATTERNS.md, D-01/D-02) -->
<!-- Executor should implement this shape directly — no exploration needed beyond read_first files. -->

```typescript
export function useSandboxState(initialScenario?: { vesselA: Vessel; vesselB: Vessel }): {
  vesselA: Vessel;
  vesselB: Vessel;
  lastGoodClassification: ClassificationResult;
  isDegenerate: boolean;
  saveError: string | null;
  isSaving: boolean;
  onVesselPositionChange: (vessel: VesselLabel, position: Position) => void;
  onVesselHeadingChange: (vessel: VesselLabel, heading: number) => void;
  onVesselSpeedChange: (vessel: VesselLabel, speed: number) => void;
  onVesselTypeChange: (vessel: VesselLabel, type: VesselType) => void;
  handleReset: () => void;
  loadScenario: (nextA: Vessel, nextB: Vessel) => void;
  handleSave: () => void;
};
```

`loadScenario`'s body (identical shape to today's `handleChipSelect`, minus the `ChipId` lookup and `setActiveChipId`):

```typescript
function loadScenario(nextA: Vessel, nextB: Vessel): void {
  previousEncounterTypeRef.current = undefined;
  applyVesselUpdate(nextA, nextB);
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Generalize handleChipSelect into loadScenario in useSandboxState.ts</name>
  <files>src/components/sandbox/hooks/useSandboxState.ts</files>
  <read_first>
    src/components/sandbox/hooks/useSandboxState.ts
    .planning/phases/16-sandbox-mutation-path-generalization/16-PATTERNS.md (section "useSandboxState.ts", lines 21-163 of that doc)
    .planning/phases/16-sandbox-mutation-path-generalization/16-CONTEXT.md (D-01, D-02, D-03)
  </read_first>
  <action>
    Remove the `CHIP_SCENARIOS, type ChipId` import from `../chip-scenarios.js` (current line 14).
    In the exported return-type signature (current lines 30-45): delete the `activeChipId: ChipId | null;` field; rename `handleChipSelect: (chipId: ChipId) => void;` to `loadScenario: (nextA: Vessel, nextB: Vessel) => void;`.
    Delete the `activeChipId` state declaration and its explanatory comment block (current lines 87-97, the `useState<ChipId | null>(...)` call and its 6-line comment).
    Remove the `setActiveChipId(null);` call from the top of each of the 4 handler bodies: `onVesselPositionChange`, `onVesselHeadingChange`, `onVesselSpeedChange`, `onVesselTypeChange` (current lines 134, 142, 150, 158 — one line each, no other change to these 4 functions).
    Add a new function `loadScenario(nextA: Vessel, nextB: Vessel): void` whose body is exactly: set `previousEncounterTypeRef.current = undefined;` then call `applyVesselUpdate(nextA, nextB);` — same 2-line body as today's `handleChipSelect` minus the `CHIP_SCENARIOS[chipId]` lookup and `setActiveChipId(chipId)` call. Give it a WHY comment noting it is the single generalized full-replace + hysteresis-reset entry point that `handleReset` (this phase) and Phase 17's Gallery "Try on Sandbox" bridge will both call.
    Refactor `handleReset()` (current lines 165-175) per D-03: its body becomes a single delegating call `loadScenario(seedA, seedB);` — remove its own `previousEncounterTypeRef.current = undefined;` and `applyVesselUpdate(seedA, seedB);` lines. `handleReset` keeps computing/owning `seedA`/`seedB` (already in scope from the top of the hook, current lines 63-64) — no new plumbing. Update its comment to state it delegates the apply step to `loadScenario` rather than duplicating it (D-03, this repo's "no duplicated near-identical logic" convention).
    Delete the entire `handleChipSelect(chipId: ChipId)` function (current lines 177-184) — its behavior is now `loadScenario`.
    In the return statement (current lines 191-206): remove the `activeChipId,` line; rename `handleChipSelect,` to `loadScenario,`.
    Update the file's top-of-file header comment (current lines 1-10) to drop "AND chip-preset load" — it should describe the choke point as serving drag, form updates, and scenario loads generically, not name the removed chip mechanism specifically.
    Do not modify `applyVesselUpdate`'s body (current lines 111-131) at all.
  </action>
  <acceptance_criteria>
    - `grep -c "CHIP_SCENARIOS\|ChipId\|activeChipId\|handleChipSelect" src/components/sandbox/hooks/useSandboxState.ts` outputs `0`
    - `grep -c "function loadScenario" src/components/sandbox/hooks/useSandboxState.ts` outputs `1`
    - `grep -c "loadScenario(seedA, seedB)" src/components/sandbox/hooks/useSandboxState.ts` outputs `1` (proves handleReset delegates)
    - `npm run typecheck` exits 0 with no errors in `useSandboxState.ts`
    - The exported return object contains a `loadScenario` field and no `activeChipId` field (verify via the grep assertions above plus a manual read of the final return statement)
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && grep -c "function loadScenario" src/components/sandbox/hooks/useSandboxState.ts</automated>
  </verify>
  <done>useSandboxState.ts exports loadScenario(vesselA, vesselB) in place of handleChipSelect; activeChipId is fully removed; handleReset delegates to loadScenario; applyVesselUpdate is untouched; typecheck passes.</done>
</task>

<task type="auto">
  <name>Task 2: Remove chip row UI from SandboxContainer.tsx and delete chip-scenarios.ts + its test</name>
  <files>src/components/sandbox/SandboxContainer.tsx, src/components/sandbox/chip-scenarios.ts, src/components/sandbox/chip-scenarios.test.ts</files>
  <read_first>
    src/components/sandbox/SandboxContainer.tsx
    src/components/sandbox/chip-scenarios.ts
    .planning/phases/16-sandbox-mutation-path-generalization/16-PATTERNS.md (section "SandboxContainer.tsx", and "chip-scenarios.ts (DELETE)")
    .planning/phases/16-sandbox-mutation-path-generalization/16-CONTEXT.md (D-04, D-05)
  </read_first>
  <action>
    Delete the `import { CHIP_ORDER } from "./chip-scenarios.js";` line (current line 21) from `SandboxContainer.tsx`.
    Delete the entire chip row JSX block (current lines 82-108: the leading comment plus the `<div className="mb-8 flex flex-wrap gap-2">...CHIP_ORDER.map(...)...</div>`). No replacement JSX is added — per D-05 the vertical space simply collapses; `<VerdictBanner .../>` (current line 110) becomes the element immediately following `</header>` (current line 80).
    Update the file's top-of-file header comment (current lines 3-13) to drop the "chip-row/reset/save CTAs" phrase — describe this file as JSX composition plus reset/save CTAs only, since there is no longer a chip row.
    Leave the Reset button JSX (current lines 64-67, `onClick={sandboxState.handleReset}`) completely unchanged — its public call site doesn't change, only `handleReset`'s internals changed in Task 1.
    Delete `src/components/sandbox/chip-scenarios.ts` and `src/components/sandbox/chip-scenarios.test.ts` outright (per D-04) — both files' only consumers (`SandboxContainer.tsx`'s chip row, `useSandboxState.ts`'s `CHIP_SCENARIOS`/`ChipId`) are removed by this plan's Task 1 and this task. No migration or fixture preservation is needed: the 3 nontrivial fixtures are already preserved verbatim in `src/domain/colregs/classify-encounter.fixtures.ts` (`crossingSailingPriorityCase`, `crossingNotUnderCommandCase`, `headOnInDoubtCase`); the other 3 have no unique derivation value (D-04).
  </action>
  <acceptance_criteria>
    - `test -f src/components/sandbox/chip-scenarios.ts` exits non-zero (file does not exist)
    - `test -f src/components/sandbox/chip-scenarios.test.ts` exits non-zero (file does not exist)
    - `grep -c "CHIP_ORDER\|chip-scenarios" src/components/sandbox/SandboxContainer.tsx` outputs `0`
    - `grep -c "CHIP_ORDER.map" src/components/sandbox/SandboxContainer.tsx` outputs `0`
    - `npm run typecheck` exits 0 (no dangling imports of the deleted module anywhere in `src/`)
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && test ! -f src/components/sandbox/chip-scenarios.ts && test ! -f src/components/sandbox/chip-scenarios.test.ts</automated>
  </verify>
  <done>SandboxContainer.tsx renders with no chip row and no chip-scenarios import; chip-scenarios.ts and chip-scenarios.test.ts no longer exist in the repo; typecheck passes with no dangling references.</done>
</task>

<task type="auto">
  <name>Task 3: Replace chip-dependent tests in SandboxContainer.test.tsx and close out the orphaned-code check</name>
  <files>src/components/sandbox/SandboxContainer.test.tsx</files>
  <read_first>
    src/components/sandbox/SandboxContainer.test.tsx
    .planning/phases/16-sandbox-mutation-path-generalization/16-PATTERNS.md (section "SandboxContainer.test.tsx" and "Orphaned-code check" under Shared Patterns)
    .planning/phases/16-sandbox-mutation-path-generalization/16-CONTEXT.md (D-06)
  </read_first>
  <action>
    Delete the test `"does not show any chip as active when a saved/shared scenario is loaded..."` (current lines 277-302) entirely — there is no chip UI or activeChipId left to regress.
    Delete the test `"loads the Overtaking chip's fixture scenario when clicked..."` (current lines 358-374) entirely — there is no chip button left to click.
    Strengthen the existing test `"restores the passed-in initialScenario's own vessels (not the default) when Reset scenario is clicked"` (current lines 247-275): update its comment to state explicitly that, since `handleReset` now delegates to `loadScenario` (D-03), this test proves `loadScenario`'s full-replace + hysteresis-reset contract (arbitrary vessel pair replace + `previousEncounterTypeRef` reset landing on the correct verdict), not just "Reset works." No new render pass or assertion logic is required — the existing drag-to-degenerate-then-reset assertions already exercise this contract end-to-end via `handleReset` → `loadScenario`.
    Add one line to the default-mount test `"loads with the default crossing scenario already classified..."` (current lines 148-154) asserting no chip-row button renders: `expect(screen.queryByRole("button", { name: "Classic crossing" })).not.toBeInTheDocument();` — this closes out the "Chip-row removal... verify via a repo-wide usage check" pitfall-checklist item at the component-test level.
    Update the file's top-of-file header comment (current lines 1-14) to drop "and the 6-chip preset row (D-01/D-02)" — there is no chip row left to describe.
    Do not add or remove any imports — `overtakingBothDirectionsCase` and `crossingResidualBasicCase` (already imported, current lines 20-23) remain used by the surviving tests, no new fixture import is needed.
    After all edits in this plan land, run a repo-wide grep across `src/` for the orphaned-code terms and confirm zero hits (the closeout gate from `.planning/research/PITFALLS.md`'s "Looks Done But Isn't" checklist).
  </action>
  <acceptance_criteria>
    - `grep -rn "chip-scenarios\|CHIP_SCENARIOS\|CHIP_ORDER\|ChipId\|activeChipId\|handleChipSelect" src/` outputs zero lines
    - `npm test -- SandboxContainer` (or `npx vitest run SandboxContainer.test.tsx`) passes, 0 failures
    - `npm test` (full suite) passes, 0 failures
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
    - `grep -c "does not show any chip as active" src/components/sandbox/SandboxContainer.test.tsx` outputs `0`
    - `grep -c "loads the Overtaking chip's fixture scenario" src/components/sandbox/SandboxContainer.test.tsx` outputs `0`
  </acceptance_criteria>
  <verify>
    <automated>npm test -- SandboxContainer && npm test && npm run typecheck && npm run lint</automated>
  </verify>
  <done>SandboxContainer.test.tsx has no chip-referencing tests; the Reset/initialScenario test's comment documents it as proving loadScenario's contract; the default-mount test asserts no chip button renders; full test suite, typecheck, and lint are all green; a repo-wide grep confirms zero remaining references to any chip-scenario identifier.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new this phase | Pure internal refactor of already-validated client state (`useSandboxState()`) and removal of dead UI/data. No new input source, no new endpoint, no schema change, no new external dependency. `applyVesselUpdate`'s existing `VesselSchema.safeParse` validation (the actual trust boundary for vessel data) is untouched by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | `loadScenario(nextA, nextB)` new entry point | accept | Identical validation surface to today's `handleChipSelect`/`handleReset` — both already route through `applyVesselUpdate`'s `VesselSchema.safeParse` guard before any state mutation; this plan does not change that guard or bypass it. |
| T-16-02 | Information Disclosure | Deleted `chip-scenarios.ts` fixture data | accept | Fixture vessel geometries are non-sensitive sample data already public in the shipped bundle; deleting the file removes exposure rather than adding any. |
| T-16-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this phase — Package Legitimacy Gate protocol is not applicable (zero `package.json` changes). |
</threat_model>

<verification>
1. `npm run typecheck` — 0 errors.
2. `npm run lint` — 0 errors.
3. `npm test` — full suite green, including the updated `SandboxContainer.test.tsx`.
4. `grep -rn "chip-scenarios\|CHIP_SCENARIOS\|CHIP_ORDER\|ChipId\|activeChipId\|handleChipSelect" src/` — zero hits (proves D-02/D-04 orphaned-code closeout).
5. `test -f src/components/sandbox/chip-scenarios.ts` and `test -f src/components/sandbox/chip-scenarios.test.ts` — both non-zero exit (files deleted).
</verification>

<success_criteria>
- ROADMAP Phase 16 success criteria 1, 2, and 4 are fully met by this plan: no chip row in the UI, `loadScenario(vesselA, vesselB)` is the single generalized entry point replacing `handleChipSelect`, and no orphaned chip code remains anywhere in `src/`.
- Success criterion 3 (human-verified behavioral parity of drag/rotate/ControlPanel-edit/Reset) is verified in the follow-on plan `16-02-PLAN.md`, which depends on this plan.
</success_criteria>

<output>
Create `.planning/phases/16-sandbox-mutation-path-generalization/16-01-SUMMARY.md` when done
</output>
