---
phase: 10-eslint-setup-lint-clean-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - eslint.config.mjs
autonomous: false
requirements: [LINT-01, LINT-02, LINT-05]

must_haves:
  truths:
    - "npm run lint and npm run lint:fix exist as scripts and invoke the ESLint CLI directly -- Next.js 16 removed next lint entirely, so there is no framework fallback"
    - "eslint.config.mjs loads without error and applies eslint-config-next's core-web-vitals + typescript flat-config sub-exports"
    - "typescript-eslint is installed for its tseslint.config() helper without duplicating eslint-config-next/typescript's already-bundled @typescript-eslint/recommended-equivalent rules"
  artifacts:
    - path: "package.json"
      provides: "lint/lint:fix scripts; eslint, eslint-config-next, typescript-eslint devDependencies at pinned versions"
    - path: "eslint.config.mjs"
      provides: "base flat config: eslint-config-next/core-web-vitals + eslint-config-next/typescript, globalIgnores"
  key_links:
    - from: "package.json scripts.lint"
      to: "eslint.config.mjs"
      via: "\"eslint .\" CLI invocation reading the flat config file at repo root"
      pattern: "\"lint\": \"eslint \\.\""
    - from: "eslint.config.mjs"
      to: "eslint-config-next/core-web-vitals + eslint-config-next/typescript"
      via: "import + spread into the defineConfig([...]) array"
      pattern: "eslint-config-next/(core-web-vitals|typescript)"
---

<objective>
Install ESLint 10 for the first time on this codebase (zero prior lint tooling) and wire the minimal working flat config: `eslint-config-next`'s `core-web-vitals` + `typescript` sub-exports, plus `npm run lint`/`npm run lint:fix` scripts. This is the foundation every later plan in this phase extends -- `eslint.config.mjs` and `package.json` are both touched again in Plans 02/03, so this plan must land first (Wave 1).

Purpose: Next.js 16 removed `next lint` entirely (confirmed via official docs, research/STACK.md Question 1) -- there is no fallback CLI, so ESLint must be installed and invoked directly. Shipping two milestones with zero lint tooling is, per research/FEATURES.md, "the single most visible gap an interviewer would find in 10 seconds of opening package.json."
Output: `eslint@10.7.0`, `eslint-config-next@16.2.10`, `typescript-eslint@8.64.0` installed; `eslint.config.mjs` created; `npm run lint`/`npm run lint:fix` scripts wired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@CLAUDE.md
</context>

<interfaces>
<!-- Current state, confirmed by direct read this session -- no prior lint tooling exists. -->

`package.json` currently has NO `lint` key in `scripts` and NO `eslint`/`eslint-config-next`/`typescript-eslint` in `devDependencies`. Existing scripts (do not reorder or modify): `dev`, `build`, `start`, `test`, `test:watch`, `typecheck`. `"type": "module"` is set at the top level -- `eslint.config.mjs` is loaded as ESM.

`next.config.ts` (confirmed by direct read) has NO `eslint` key already -- there is nothing to remove for the "stale eslint key" table-stakes item research/FEATURES.md flags; this has already been verified true for this repo.

Research/STACK.md's exact confirmed flat-config shape (Question 1, official Next.js docs, `lastUpdated: 2025-11-10`, version-pinned to `16.2.10`): import `defineConfig`/`globalIgnores` from the `eslint/config` subpath of the `eslint` package itself (not a separate dependency); import the default export of `eslint-config-next/core-web-vitals` and `eslint-config-next/typescript`; `export default defineConfig([...nextVitals, ...nextTs, globalIgnores([...])])`.
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 0: Confirm package legitimacy before any install this phase</name>
  <files>N/A -- no file changes in this task, human verification only</files>
  <what-built>
    No install has run yet. This phase adds 5 new devDependencies total, pinned in research/STACK.md's own Installation section (verified there via live `npm view` and Context7 this research session): `eslint@10.7.0`, `eslint-config-next@16.2.10`, `typescript-eslint@8.64.0` (this plan) and `eslint-plugin-better-tailwindcss@4.6.1`, `@vitest/eslint-plugin@1.6.23` (Plan 02, which runs after this plan and this same checkpoint in wave order -- no separate checkpoint needed there).
  </what-built>
  <how-to-verify>
    Before any `npm install` runs this phase, visit each package's npm registry page and confirm the maintainer/publish history matches its claimed identity:
    1. https://www.npmjs.com/package/eslint -- ESLint's own org
    2. https://www.npmjs.com/package/eslint-config-next -- Vercel's own package, version should track Next.js's own release line
    3. https://www.npmjs.com/package/typescript-eslint -- the typescript-eslint org (NOT the older split `@typescript-eslint/parser`/`@typescript-eslint/eslint-plugin` packages)
    4. https://www.npmjs.com/package/eslint-plugin-better-tailwindcss -- schoero's actively-maintained package (confirm recent publish activity, not a name-squatted/typo package)
    5. https://www.npmjs.com/package/@vitest/eslint-plugin -- the `vitest-dev` org's own scoped package (NOT the unscoped `eslint-plugin-vitest`, confirmed stale at `0.5.4` per research/STACK.md)
    Flag anything inconsistent with the above (different maintainer, suspiciously low downloads, a recent ownership transfer).
  </how-to-verify>
  <action>No automated action for this task -- it exists solely to pause for human confirmation before any package install runs this phase.</action>
  <acceptance_criteria>
    - Human has explicitly confirmed (via the resume-signal) that all 5 packages' npmjs.com listings match their claimed maintainer/identity
  </acceptance_criteria>
  <verify>
    <automated>N/A -- human confirmation required; no automated command applies to this checkpoint</automated>
  </verify>
  <resume-signal>Type "approved" once all 5 packages are confirmed legitimate on npmjs.com, or list which package(s) look wrong and why.</resume-signal>
  <done>Human has confirmed all 5 packages' legitimacy via npmjs.com; installs may proceed in Task 1 and Plan 02.</done>
</task>

<task type="auto">
  <name>Task 1: Install ESLint core toolchain and wire lint scripts</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    package.json
    .planning/research/STACK.md
  </read_first>
  <action>
    Run `npm install -D eslint@10.7.0 eslint-config-next@16.2.10 typescript-eslint@8.64.0` -- exact versions per research/STACK.md's Installation section (verified live via `npm view` this research session). Then edit `package.json`'s `scripts` object: add `"lint": "eslint ."` and `"lint:fix": "eslint . --fix"` as new keys, positioned alongside the existing `dev`/`build`/`start`/`test`/`test:watch`/`typecheck` keys. Do not reorder, rename, or modify any existing script. Do not add an `eslint` key to `next.config.ts` -- already confirmed absent this session, nothing to remove there.
  </action>
  <acceptance_criteria>
    - `npm pkg get scripts.lint` prints `"eslint ."`
    - `npm pkg get scripts.lint:fix` prints `"eslint . --fix"`
    - `npm pkg get scripts.dev scripts.build scripts.start scripts.test scripts.test:watch scripts.typecheck` prints all 6 existing values byte-unchanged from before this task
    - `npm ls eslint eslint-config-next typescript-eslint --depth=0` exits 0 and lists all three packages installed
  </acceptance_criteria>
  <verify>
    <automated>npm pkg get scripts.lint scripts.lint:fix && npm ls eslint eslint-config-next typescript-eslint --depth=0</automated>
  </verify>
  <done>eslint, eslint-config-next, typescript-eslint installed; lint/lint:fix scripts wired alongside existing scripts, unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Create eslint.config.mjs base flat config</name>
  <files>eslint.config.mjs</files>
  <read_first>
    .planning/research/STACK.md
    next.config.ts
  </read_first>
  <action>
    Create `eslint.config.mjs` at repo root as an ESM module. Import `defineConfig` and `globalIgnores` from the `eslint/config` subpath of the `eslint` package; import the default export of `eslint-config-next/core-web-vitals` as `nextVitals`; import the default export of `eslint-config-next/typescript` as `nextTs`. Export default `defineConfig([...nextVitals, ...nextTs, globalIgnores([".next/**", "out/**", "build/**", "next-env.d.ts"])])` -- exact shape per research/STACK.md Question 1 (official Next.js docs, version-pinned to `16.2.10`).

    Do NOT additionally import the bare `typescript-eslint` package and hand-wire a separate `tseslint.config(tseslint.configs.recommended)` block in this same file -- `eslint-config-next/typescript` already wraps `@typescript-eslint/recommended`-equivalent rules; a second block would register overlapping rule sets for the same concern (research/STACK.md Question 2's explicit guidance). This satisfies LINT-02's "recommended tier" requirement via `eslint-config-next/typescript` alone. The `typescript-eslint` package installed in Task 1 is reserved for Plan 03's `recommended-type-checked` stretch-goal attempt, where it would REPLACE (not stack on top of) this file's TS-rule source.
  </action>
  <acceptance_criteria>
    - `eslint.config.mjs` exists and uses ESM `import`/`export default` syntax (source assertion: `grep -c "^import " eslint.config.mjs` returns at least 3; `grep -c "^export default" eslint.config.mjs` returns 1)
    - `grep -c "eslint-config-next/core-web-vitals" eslint.config.mjs` returns 1; `grep -c "eslint-config-next/typescript" eslint.config.mjs` returns 1
    - `grep -c "from \"typescript-eslint\"" eslint.config.mjs` returns 0 -- confirms the "don't double-register" guidance was followed
    - `npx eslint --print-config app/page.tsx | node -e "JSON.parse(require('fs').readFileSync(0,'utf8'))"` exits 0 (proves the flat config loads and resolves without a syntax/import error)
    - `npm run lint` executes and prints ESLint's own error/warning count summary (not a stack trace mentioning "Cannot find module" or a config-load crash) -- expected to exit non-zero due to pre-existing violations; that baseline is addressed in Plan 03, not here
  </acceptance_criteria>
  <verify>
    <automated>npx eslint --print-config app/page.tsx | node -e "JSON.parse(require('fs').readFileSync(0,'utf8')); console.log('config OK')"</automated>
  </verify>
  <done>eslint.config.mjs exists, applies core-web-vitals + typescript sub-exports, loads without error; npm run lint executes (pre-existing violations expected, resolved in Plan 03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| npm registry -> devDependency tree | New devDependencies (`eslint`, `eslint-config-next`, `typescript-eslint`) execute at install/lint time only -- no runtime/production code path, no user input surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-10-SC | Tampering | npm installs this phase (5 packages total across Plans 01/02) | mitigate | Blocking human-verify checkpoint (Task 0) requiring npmjs.com confirmation of maintainer/identity before any `npm install` runs |
| T-10-01 | Tampering | `eslint.config.mjs` (new file, controls what code ships) | accept | devDependency-only, no runtime effect; reviewed via standard git diff/PR review, same as any other config file in this repo |

This plan installs devDependency tooling only -- zero new runtime code paths, user input handling, or production attack surface.
</threat_model>

<verification>
- `npm pkg get scripts.lint scripts.lint:fix` shows both new scripts
- `npx eslint --print-config app/page.tsx` resolves without error
- `npm run lint` runs to completion (non-zero exit expected/acceptable at this stage)
</verification>

<success_criteria>
- eslint, eslint-config-next, typescript-eslint installed at research-pinned versions
- eslint.config.mjs applies core-web-vitals + typescript flat-config sub-exports
- npm run lint / npm run lint:fix scripts exist and invoke the ESLint CLI directly (no next lint dependency)
</success_criteria>

<output>
Create `.planning/phases/10-eslint-setup-lint-clean-baseline/10-01-SUMMARY.md` when done
</output>
