---
phase: 11-tailwind-deprecated-class-name-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/select.tsx
  - src/components/layout/Header.tsx
  - src/components/gallery/GalleryCard.tsx
  - src/components/sandbox/SandboxContainer.tsx
  - src/components/sandbox/ChartPanel.tsx
autonomous: true
requirements: [TWFX-01, TWFX-02, TWFX-03]

must_haves:
  truths:
    - "outline-none no longer appears as a Tailwind utility in button.tsx, select.tsx, Header.tsx, or GalleryCard.tsx"
    - "D-01: rounded-[0.25rem] no longer appears anywhere in the codebase, renamed to rounded-sm (the canonical Tailwind v4 class), not a workaround"
    - "D-02: the resulting 0.25rem -> 0.375rem radius increase on SandboxContainer.tsx and ChartPanel.tsx is accepted as deliberate, not rolled back"
    - "select.tsx's unrelated rounded-[min(var(--radius-md),10px)] arbitrary-value radius at line 56 is byte-identical to before"
    - "GalleryPreviewChart.tsx's 'rounded to clean numbers' comment and Hero.tsx's 'Grounded in' copy are byte-identical to before (false-positive traps, not Tailwind classes)"
  artifacts:
    - path: "src/components/ui/button.tsx"
      provides: "outline-hidden focus treatment on the shared Button primitive"
      contains: "outline-hidden"
    - path: "src/components/ui/select.tsx"
      provides: "outline-hidden focus treatment on SelectTrigger"
      contains: "outline-hidden"
    - path: "src/components/layout/Header.tsx"
      provides: "outline-hidden focus treatment on nav links"
      contains: "focus-visible:outline-hidden"
    - path: "src/components/gallery/GalleryCard.tsx"
      provides: "outline-hidden focus treatment on the card link wrapper"
      contains: "focus-visible:outline-hidden"
    - path: "src/components/sandbox/SandboxContainer.tsx"
      provides: "rounded-sm radius on the save-status banner"
      contains: "rounded-sm"
    - path: "src/components/sandbox/ChartPanel.tsx"
      provides: "rounded-sm radius on the chart SVG border"
      contains: "rounded-sm"
  key_links:
    - from: "src/components/ui/button.tsx (outline-hidden)"
      to: "focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50 (same className, adjacent)"
      via: "shared className string, no separate wiring"
      pattern: "focus-visible:ring"
    - from: "src/components/layout/Header.tsx (focus-visible:outline-hidden)"
      to: "focus-visible:ring-2 focus-visible:ring-accent/60 focus-visible:ring-offset-2 focus-visible:ring-offset-background (same className, adjacent)"
      via: "shared className string, no separate wiring"
      pattern: "focus-visible:ring"
---

<objective>
Apply the two mechanical Tailwind v3 -> v4 class-name renames from CONTEXT.md decisions D-01/D-02
(radius) across the 6 flagged files, by hand, scoped strictly to real Tailwind `className` usages.

Purpose: TWFX-01 and TWFX-02 require these exact renames; TWFX-03 requires they be applied
file-by-file, not via a blanket find-replace, because this codebase has confirmed
false-positive traps (non-Tailwind text/comments containing similar substrings).
Output: 6 files with `outline-none` -> `outline-hidden` (4 files) and `rounded-[0.25rem]` ->
`rounded-sm` (2 files), verified clean of both the old class names and any accidental edits to
the known false-positive sites.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-tailwind-deprecated-class-name-fixes/11-CONTEXT.md
@.planning/phases/11-tailwind-deprecated-class-name-fixes/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename outline-none to outline-hidden in button.tsx, select.tsx, Header.tsx, GalleryCard.tsx</name>
  <files>src/components/ui/button.tsx, src/components/ui/select.tsx, src/components/layout/Header.tsx, src/components/gallery/GalleryCard.tsx</files>
  <read_first>
    src/components/ui/button.tsx (line 11, buttonVariants cva template literal)
    src/components/ui/select.tsx (lines 40-59, SelectTrigger className, note line 56 out-of-scope arbitrary radius)
    src/components/layout/Header.tsx (lines 67-83, nav link anchor className)
    src/components/gallery/GalleryCard.tsx (lines 46-52, Link wrapper className)
    .planning/phases/11-tailwind-deprecated-class-name-fixes/11-PATTERNS.md (exact current-state excerpts for all 4 sites)
  </read_first>
  <action>
    Per TWFX-01/D (locked requirement, no CONTEXT.md decision override needed — literal canonical rename):
    In button.tsx line 11, replace the bare `outline-none` token with `outline-hidden` inside the
    existing string `whitespace-nowrap transition-all outline-none select-none` -> `whitespace-nowrap
    transition-all outline-hidden select-none`. Leave every other token on that line and the rest of
    the cva template literal unchanged.
    In select.tsx line 48, replace the bare `outline-none` token with `outline-hidden` inside
    `transition-colors outline-none select-none` -> `transition-colors outline-hidden select-none`.
    Do NOT touch line 56 (`data-[size=sm]:rounded-[min(var(--radius-md),10px)]`) — different
    arbitrary-value class, explicitly out of scope.
    In Header.tsx line 78, replace `focus-visible:outline-none` with `focus-visible:outline-hidden`
    (variant-prefixed form, not bare).
    In GalleryCard.tsx line 51, replace `focus-visible:outline-none` with
    `focus-visible:outline-hidden` inside the string `focus-visible:ring-2 focus-visible:ring-ring
    focus-visible:outline-none` -> `focus-visible:ring-2 focus-visible:ring-ring
    focus-visible:outline-hidden`. Leave the ring classes before it unchanged.
    Every site already has an adjacent `focus-visible:ring-*` treatment on the same or a neighboring
    line — do not add, remove, or modify any ring/ring-offset class; this is a single-token rename
    only.
  </action>
  <verify>
    <automated>grep -rn "outline-none" src/components/ui/button.tsx src/components/ui/select.tsx src/components/layout/Header.tsx src/components/gallery/GalleryCard.tsx; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "outline-hidden" src/components/ui/button.tsx` returns exactly 1
    - `grep -c "outline-hidden" src/components/ui/select.tsx` returns exactly 1
    - `grep -c "focus-visible:outline-hidden" src/components/layout/Header.tsx` returns exactly 1
    - `grep -c "focus-visible:outline-hidden" src/components/gallery/GalleryCard.tsx` returns exactly 1
    - `grep -rn "outline-none" src/components/ui/button.tsx src/components/ui/select.tsx src/components/layout/Header.tsx src/components/gallery/GalleryCard.tsx` returns zero matches (exit code 1)
    - `grep -n "rounded-\[min(var(--radius-md),10px)\]" src/components/ui/select.tsx` still matches line 56 unchanged
  </acceptance_criteria>
  <done>All 4 files use outline-hidden in place of outline-none at the exact sites identified; select.tsx line 56's unrelated arbitrary radius class is untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Rename rounded-[0.25rem] to rounded-sm in SandboxContainer.tsx and ChartPanel.tsx</name>
  <files>src/components/sandbox/SandboxContainer.tsx, src/components/sandbox/ChartPanel.tsx</files>
  <read_first>
    src/components/sandbox/SandboxContainer.tsx (lines 227-236, save-banner block)
    src/components/sandbox/ChartPanel.tsx (lines 441-447, chart svg root)
    app/globals.css (lines 42-48 radius scale tokens, line 97 `--radius: 0.625rem`)
    .planning/phases/11-tailwind-deprecated-class-name-fixes/11-CONTEXT.md (D-01, D-02)
  </read_first>
  <action>
    Per D-01: replace `rounded-[0.25rem]` with `rounded-sm` in SandboxContainer.tsx line 230, inside
    the banner div className `rounded-[0.25rem] border border-border bg-card px-3 py-2 text-sm` ->
    `rounded-sm border border-border bg-card px-3 py-2 text-sm`. Replace `rounded-[0.25rem]` with
    `rounded-sm` in ChartPanel.tsx line 446, inside `className="rounded-[0.25rem] border
    border-border bg-chart-surface"` -> `className="rounded-sm border border-border
    bg-chart-surface"`. This is a direct string replacement inside a plain `className="..."`
    literal at both sites — no `cn()`/`cva()` wrapper to navigate. Per D-01/D-02, this accepts the
    resulting 0.25rem -> 0.375rem (`--radius-sm = calc(var(--radius) * 0.6)`, `--radius: 0.625rem`)
    visual radius increase as deliberate; do not add a compensating arbitrary-value override to
    preserve the old 0.25rem value.
  </action>
  <verify>
    <automated>grep -rn "rounded-\[0.25rem\]" src/; test $? -eq 1</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "rounded-\[0.25rem\]" src/` returns zero matches anywhere in the codebase (exit code 1)
    - `grep -n "rounded-sm" src/components/sandbox/SandboxContainer.tsx` matches line 230
    - `grep -n "rounded-sm" src/components/sandbox/ChartPanel.tsx` matches line 446
  </acceptance_criteria>
  <done>Both files use rounded-sm in place of rounded-[0.25rem]; zero rounded-[0.25rem] occurrences remain anywhere in src/.</done>
</task>

<task type="auto">
  <name>Task 3: Verify no false-positive edits and lint stays clean on all 6 files</name>
  <files>src/components/gallery/GalleryPreviewChart.tsx, src/components/hero/Hero.tsx</files>
  <read_first>
    src/components/gallery/GalleryPreviewChart.tsx (line 19, "rounded to clean numbers" comment)
    src/components/hero/Hero.tsx (line 72, "Grounded in " copy)
  </read_first>
  <action>
    Run a read-only verification sweep confirming Task 1 and Task 2 touched only the intended
    6 files and did not alter either confirmed false-positive site: GalleryPreviewChart.tsx:19's
    "rounded to clean numbers" code comment and Hero.tsx:72's "Grounded in " UI copy must remain
    byte-identical to their pre-change state. Then run eslint against the 6 modified files to
    confirm the rename introduced no new lint errors or warnings (CONTEXT.md confirms all 6 files
    passed lint cleanly before this change).
  </action>
  <verify>
    <automated>npx eslint src/components/ui/button.tsx src/components/ui/select.tsx src/components/layout/Header.tsx src/components/gallery/GalleryCard.tsx src/components/sandbox/SandboxContainer.tsx src/components/sandbox/ChartPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `npx eslint src/components/ui/button.tsx src/components/ui/select.tsx src/components/layout/Header.tsx src/components/gallery/GalleryCard.tsx src/components/sandbox/SandboxContainer.tsx src/components/sandbox/ChartPanel.tsx` exits 0 with zero errors/warnings
    - `grep -n "rounded to clean numbers" src/components/gallery/GalleryPreviewChart.tsx` still matches line 19 unchanged
    - `grep -n "Grounded in " src/components/hero/Hero.tsx` still matches line 72 unchanged
    - `git diff --stat` shows only the 6 intended files changed, no other files touched
  </acceptance_criteria>
  <done>Eslint is clean on all 6 renamed files; both false-positive trap sites are confirmed unchanged; git diff touches only the 6 intended files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan is a client-side, presentational Tailwind class-name rename across existing files. No new data flow, input surface, or trust boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-11-01 | Tampering | className strings in button.tsx/select.tsx/Header.tsx/GalleryCard.tsx/SandboxContainer.tsx/ChartPanel.tsx | accept | Presentational-only rename with no user input or data-flow change; scope verified via grep (Task 3) confirming only the intended token changed at each site. |
| T-11-02 | Information Disclosure (a11y regression) | `outline-hidden` removes the browser's default focus outline at 4 sites | mitigate | Each site already carries an adjacent `focus-visible:ring-*` (and Header additionally `ring-offset-*`) treatment confirmed present in Task 1's read_first excerpts, preserving a visible keyboard-focus indicator so no loss of focus visibility occurs. |
</threat_model>

<verification>
- `grep -rn "outline-none" src/components/ui/button.tsx src/components/ui/select.tsx src/components/layout/Header.tsx src/components/gallery/GalleryCard.tsx` returns no matches.
- `grep -rn "rounded-\[0.25rem\]" src/` returns no matches.
- `npx eslint` on the 6 modified files exits 0.
- `git diff --stat` shows exactly 6 files changed.
</verification>

<success_criteria>
- outline-none replaced with outline-hidden at all 4 confirmed sites (TWFX-01).
- rounded-[0.25rem] replaced with rounded-sm at both confirmed sites (TWFX-02).
- Both false-positive trap sites (GalleryPreviewChart.tsx comment, Hero.tsx copy) and the unrelated select.tsx:56 arbitrary radius are unchanged (TWFX-03).
</success_criteria>

<output>
Create `.planning/phases/11-tailwind-deprecated-class-name-fixes/11-01-SUMMARY.md` when done
</output>
