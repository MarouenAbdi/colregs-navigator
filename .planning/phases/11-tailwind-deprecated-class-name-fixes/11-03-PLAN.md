---
phase: 11-tailwind-deprecated-class-name-fixes
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified: []
autonomous: false
requirements: [TWFX-04]

must_haves:
  truths:
    - "A human confirms in a real browser that Hero, Header, Gallery, and Sandbox show no visual or keyboard-focus-outline regression after the outline-hidden and rounded-sm changes"
  artifacts: []
  key_links: []
---

<objective>
Get human confirmation, in a real browser, that the mechanical class-name renames landed in
Plan 01/02 (outline-none -> outline-hidden across 4 sites; rounded-[0.25rem] -> rounded-sm
across 2 sites) produced no visual or keyboard-focus-outline regression.

Purpose: TWFX-04 explicitly requires human confirmation — jsdom/eslint cannot verify visual
paint output or a rendered focus ring. Per D-04, scope is a normal focus-ring check only (no
forced-colors/high-contrast check). Per D-05, go straight to this manual walkthrough — no
automated Claude-in-Chrome pre-check.
Output: Human sign-off (or a described issue) closing out Phase 11.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-tailwind-deprecated-class-name-fixes/11-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual browser verification of focus-ring and radius changes</name>
  <files>N/A -- no file changes in this task, human verification only</files>
  <what-built>
    Two Tailwind v3 -> v4 canonical class-name renames across 6 files:
    (1) outline-none -> outline-hidden in the Button primitive (button.tsx), the Select trigger
    (select.tsx), the Header nav links (Header.tsx), and the Gallery card link wrapper
    (GalleryCard.tsx) — each already pairs this with an existing focus-visible ring treatment.
    (2) rounded-[0.25rem] -> rounded-sm on the Sandbox save-status banner (SandboxContainer.tsx)
    and the chart SVG border (ChartPanel.tsx) — an accepted, deliberate radius increase from
    0.25rem to 0.375rem (per D-01/D-02, this project's --radius-sm theme token, not a bug).
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open the app in a real browser.
    1. Hero: load the home page, confirm the hero section (headline, copy, CTAs, preview card)
       renders with no visual regression. Tab into the CTA buttons and confirm a visible focus
       ring appears on each (Button primitive, outline-hidden change).
    2. Header: tab through the header nav links; confirm each shows a visible focus ring
       (accent-colored ring + ring-offset) when focused via keyboard, with no missing or
       double outline.
    3. Gallery: scroll to the Gallery section; tab to a gallery card link; confirm a visible
       focus ring renders around the card (GalleryCard.tsx change). Also open any Select
       control if present in the flow and tab to confirm its focus ring still renders
       (select.tsx change).
    4. Sandbox: open the Sandbox section. Trigger a save action to surface the save-status
       banner and visually confirm its corner radius looks acceptable (now rounded-sm,
       0.375rem, slightly larger than before — this is expected and accepted per D-02, do not
       flag as a bug unless it looks clearly broken/wrong, not just "slightly bigger"). Confirm
       the chart SVG's border corner radius likewise looks acceptable.
    Per D-04: only check normal focus-ring visibility (tab-key navigation) — do not test
    forced-colors/high-contrast mode, that is out of scope for this checkpoint.
  </how-to-verify>
  <action>No automated action for this task -- it exists solely to pause for human confirmation of the visual/focus-ring state after Plan 01's and Plan 02's changes.</action>
  <acceptance_criteria>
    - Human has explicitly confirmed (via the resume-signal) that all 4 areas (Hero, Header, Gallery, Sandbox) show a visible keyboard focus ring and no visual regression
  </acceptance_criteria>
  <verify>
    <automated>N/A -- human confirmation required; no automated command applies to this checkpoint</automated>
  </verify>
  <resume-signal>Type "approved" if all 4 areas look correct, or describe any specific visual/focus-ring issue observed (which area, what looks wrong).</resume-signal>
  <done>Human has confirmed no visual or keyboard-focus-outline regression across Hero, Header, Gallery, and Sandbox.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Human-verification checkpoint only, no code change in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-11-04 | N/A | N/A | accept | No code change in this plan; risk (if any) was already addressed by Plan 01's mitigation of T-11-02 (focus-visible ring preservation). This checkpoint exists to confirm that mitigation visually, per TWFX-04. |
</threat_model>

<verification>
- Human types "approved" after completing the 4-area browser walkthrough, or reports a specific issue for follow-up.
</verification>

<success_criteria>
- Human confirms no visual or keyboard-focus-outline regression across Hero, Header, Gallery, and Sandbox (TWFX-04), closing Phase 11.
</success_criteria>

<output>
Create `.planning/phases/11-tailwind-deprecated-class-name-fixes/11-03-SUMMARY.md` when done
</output>
