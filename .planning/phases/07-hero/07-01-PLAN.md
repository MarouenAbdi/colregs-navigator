---
phase: 07-hero
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/components/ui/card.tsx
  - src/components/ui/badge.tsx
  - src/components/hero/hero-preview-fixture.ts
  - src/components/hero/hero-preview-fixture.test.ts
  - src/components/hero/Hero.tsx
  - src/components/hero/Hero.test.tsx
  - app/page.tsx
  - app/globals.css
autonomous: true
requirements: [HERO-01, HERO-02, HERO-03, HERO-04]

must_haves:
  truths:
    - "A visitor sees a Hero section above the Sandbox with the exact locked headline/copy/CTAs from Main-Design.png"
    - "A visitor sees an illustrative 'Live classification' preview card (mini chart, Rule 15 badge, RANGE/BEARING/CPA readouts) that is visibly static, not wired to the interactive Sandbox's live state — no pulsing dot, no animated bearing line (D-01), and the mini-chart matches Main-Design.png's full detail rather than a simplified version (D-02)"
    - "Clicking 'Open the sandbox' targets #sandbox, which exists in the server-rendered HTML wrapping SandboxContainer"
    - "Clicking 'Classic encounters' targets #gallery (accepted no-op until Phase 9 per D-06)"
    - "Below 900px the Hero collapses to a single column (text stacked above the preview card, per D-04); below 640px the headline scales down fluidly via clamp()"
    - "The preview card's RANGE/BEARING/CPA/Rule-15/verdict numbers are produced by a real classifyEncounter() call against a fixed fixture, not hand-typed literal strings"
  artifacts:
    - path: "src/components/hero/hero-preview-fixture.ts"
      provides: "Fixed heroPreviewVesselA/heroPreviewVesselB Vessel fixture reproducing the design mock's exact displayed numbers"
      exports: ["heroPreviewVesselA", "heroPreviewVesselB"]
    - path: "src/components/hero/hero-preview-fixture.test.ts"
      provides: "Fixture-drift guard (HERO-02) asserting classifyEncounter()/bearing()/cpa() still reproduce Rule 15/crossing/vesselA-gives-way + 2.99nm/061deg/1.18nm"
    - path: "src/components/hero/Hero.tsx"
      provides: "Plain Server Component: headline/copy/CTAs/trust-note + hand-built static SVG preview card"
      exports: ["Hero"]
    - path: "src/components/hero/Hero.test.tsx"
      provides: "RTL test covering HERO-01 headline render and HERO-03 CTA href values"
    - path: "src/components/ui/card.tsx"
      provides: "shadcn Card primitive (CLI-vendored)"
    - path: "src/components/ui/badge.tsx"
      provides: "shadcn Badge primitive (CLI-vendored)"
    - path: "app/page.tsx"
      provides: "Home page composing <Hero /> above a new <section id=\"sandbox\"> wrapper around the existing SandboxContainer"
    - path: "app/globals.css"
      provides: "scroll-behavior: smooth (reduced-motion guarded, D-05) + --breakpoint-hero: 900px token (Pattern 4)"
  key_links:
    - from: "app/page.tsx"
      to: "src/components/hero/Hero.tsx"
      via: "import { Hero } from \"../src/components/hero/Hero.js\""
      pattern: "components/hero/Hero"
    - from: "src/components/hero/Hero.tsx"
      to: "src/domain/colregs/classify-encounter.ts"
      via: "classifyEncounter(heroPreviewVesselA, heroPreviewVesselB) called inline at render time"
      pattern: "classifyEncounter\\(heroPreviewVesselA"
    - from: "src/components/hero/Hero.tsx"
      to: "src/components/hero/hero-preview-fixture.ts"
      via: "relative import of heroPreviewVesselA/heroPreviewVesselB"
      pattern: "hero-preview-fixture"
    - from: "Hero CTA anchors"
      to: "#sandbox / #gallery"
      via: "plain <a href=\"#...\"> inside Button asChild, native browser fragment-scroll + CSS scroll-behavior: smooth"
      pattern: "href=\"#sandbox\""
---

<objective>
Build the net-new Hero section (Direction A): headline/copy/CTAs, a fully static illustrative "Live classification" preview card driven by a real `classifyEncounter()` call against a fixed fixture, and wire both the anchor-scroll targets and the responsive breakpoint mechanics it depends on.

Purpose: Phase 7 is the one genuinely new UI surface in the v1.1 redesign — it gives visitors an honest, immediate preview of the product's core value (classify an encounter, name the give-way vessel, cite the rule) before they reach the interactive Sandbox tool.
Output: `src/components/hero/Hero.tsx`, `src/components/hero/hero-preview-fixture.ts`, their colocated tests, two new shadcn primitives (`card`, `badge`), and the `app/page.tsx`/`app/globals.css` wiring that makes `#sandbox` a real anchor target and Hero's 900px breakpoint compile to real CSS.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-hero/07-CONTEXT.md
@.planning/phases/07-hero/07-RESEARCH.md
@.planning/phases/07-hero/07-PATTERNS.md
@.planning/phases/07-hero/07-UI-SPEC.md
@.planning/design/Main-Design.png
</context>

<interfaces>
<!-- Extracted directly from the codebase — use these signatures/shapes as-is, no exploration needed. -->

From `src/domain/vessel/vessel.ts`:
```
export const VesselTypeSchema = z.enum(["power-driven","sailing","fishing","not-under-command","restricted-in-ability-to-maneuver"]);
export const PositionSchema = z.object({ x: z.number(), y: z.number() });
export const VesselSchema = z.object({ position: PositionSchema, heading: z.number().gte(0).lt(360), speed: z.number().gte(0), type: VesselTypeSchema });
export type Vessel = z.infer<typeof VesselSchema>;
export type Position = z.infer<typeof PositionSchema>;
```

From `src/domain/shared/result.ts`:
```
export type Result<T> = { ok: true; value: T } | { ok: false; reason: DegenerateCaseReason; details?: Record<string, unknown> };
```

From `src/domain/colregs/classify-encounter.ts`:
```
export function classifyEncounter(vesselA: Vessel, vesselB: Vessel, previous?: EncounterType): Result<ClassificationResult>;
```

From `src/domain/colregs/types.ts`:
```
export type EncounterType = "head-on" | "crossing" | "overtaking";
export type VesselLabel = "vesselA" | "vesselB";
export interface ReasoningTrailEntry { ruleId: string; text: string; facts: Record<string, number | string | boolean>; }
export interface ClassificationResult { encounterType: EncounterType; riskOfCollision: boolean; giveWay: VesselLabel | null; standOn: VesselLabel | null; doubt: boolean; doubtBoundary?: DoubtBoundary; trail: ReasoningTrailEntry[]; }
```

From `src/domain/geometry/bearing.ts`:
```
export function bearing(a: Position, b: Position): Result<number>;
```

From `src/domain/geometry/cpa.ts`:
```
export function cpa(vesselA: Vessel, vesselB: Vessel): Result<{ tcpaMinutes: number; dcpaNm: number }>;
```

From `src/domain/geometry/screen-convert.ts`:
```
export interface ContainerSize { width: number; height: number; }
export interface ChartViewBox { minX: number; minY: number; width: number; height: number; }
export function chartToScreen(position: Position, containerSize: ContainerSize, viewBox: ChartViewBox): { screenX: number; screenY: number };
```
Zero DOM dependency (confirmed in its own header comment) — safe to call from a plain Server Component with no `ResizeObserver`/`useEffect`.

From `src/components/layout/Header.tsx` (CTA + Server Component convention to replicate):
```
import { Compass, Code2 } from "lucide-react";
import { Button } from "@/components/ui/button";
export function Header() { ... <Button asChild variant="outline" size="sm"><a href="..." target="_blank" rel="noreferrer"><Code2 .../>Source</a></Button> ... }
```
No `"use client"` directive. Both existing nav anchors already use plain `<a href="#sandbox">` / `<a href="#gallery">`.

From `components.json`: `"style": "radix-nova"`, `"aliases": { "ui": "@/components/ui" }` — already configured by Phase 6, no re-init needed.

From `package.json` (already installed, Phase 6): `lucide-react@^1.25.0` exports `CircleCheck` and `ArrowRight` (confirmed present in the installed version).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn primitives and build the verified preview-card fixture</name>
  <files>package.json, package-lock.json, src/components/ui/card.tsx, src/components/ui/badge.tsx, src/components/hero/hero-preview-fixture.ts, src/components/hero/hero-preview-fixture.test.ts</files>
  <read_first>
    src/domain/colregs/classify-encounter.fixtures.ts
    src/domain/colregs/classify-encounter.ts
    src/domain/colregs/classify-encounter.test.ts
    src/domain/colregs/types.ts
    src/domain/vessel/vessel.ts
    src/domain/shared/result.ts
    src/domain/geometry/bearing.ts
    src/domain/geometry/cpa.ts
    components.json
    .planning/phases/07-hero/07-RESEARCH.md (Code Examples section — verified fixture values)
    .planning/phases/07-hero/07-PATTERNS.md (hero-preview-fixture.ts and hero-preview-fixture.test.ts sections)
  </read_first>
  <action>
    Run `npx shadcn add card badge` from the repo root against the official registry only (no `--base`/`--registry` flag — `components.json` already pins `"style": "radix-nova"` from Phase 6). This vendors `src/components/ui/card.tsx` (`Card`/`CardHeader`/`CardContent` at minimum) and `src/components/ui/badge.tsx` (`Badge`/`badgeVariants`) as CLI-generated source, not new npm packages beyond their already-installed transitive deps (`radix-ui`, `class-variance-authority`, `clsx`, `tailwind-merge`). Do not hand-write these files. After the CLI completes, read both generated files to confirm the exact exported component names before Task 2 consumes them.

    Create `src/components/hero/hero-preview-fixture.ts` exporting `heroPreviewVesselA` and `heroPreviewVesselB` as `Vessel`-typed constants (relative import `import type { Vessel } from "../../domain/vessel/vessel.js";` — `.js`-suffixed, not the `@/*` alias, since this file is not shadcn-adjacent code). Use these exact values (verified in 07-RESEARCH.md via literal formula replication, not hand-derived — do not alter them): `heroPreviewVesselA = { position: { x: 0, y: 0 }, heading: 0, speed: 12, type: "power-driven" }`; `heroPreviewVesselB = { position: { x: 2.6151, y: 1.4496 }, heading: 280, speed: 8.3, type: "power-driven" }`. Add a doc comment above the exports, matching `classify-encounter.fixtures.ts`'s per-fixture derivation-comment style, stating these values reproduce `classifyEncounter(heroPreviewVesselA, heroPreviewVesselB)` output of `encounterType: "crossing"`, `giveWay: "vesselA"`, `standOn: "vesselB"`, `doubt: false`, and readouts range 2.99 NM (`Math.hypot` of the position delta) / bearing 061° / CPA `dcpaNm` 1.18 NM (per CONTEXT.md D-07).

    Create `src/components/hero/hero-preview-fixture.test.ts` as a plain Vitest file (no `// @vitest-environment jsdom` pragma — matches `classify-encounter.test.ts`'s Node-environment convention, since this test touches zero DOM). Import `classifyEncounter` from `"../../domain/colregs/classify-encounter.js"`, `bearing` from `"../../domain/geometry/bearing.js"`, `cpa` from `"../../domain/geometry/cpa.js"`, and `heroPreviewVesselA`/`heroPreviewVesselB` from the sibling fixture file. Write one `describe`/`it` block that: calls `classifyEncounter(heroPreviewVesselA, heroPreviewVesselB)`, narrows `.ok === true`, and asserts `.value.encounterType === "crossing"`, `.value.giveWay === "vesselA"`, `.value.standOn === "vesselB"`, `.value.doubt === false`. Add a second assertion block computing range via `Math.hypot(heroPreviewVesselB.position.x - heroPreviewVesselA.position.x, heroPreviewVesselB.position.y - heroPreviewVesselA.position.y)` and asserting it is within 0.01 of `2.99`; narrowing `bearing(heroPreviewVesselA.position, heroPreviewVesselB.position)`'s `.ok`/`.value` and asserting it is within 0.5 of `61`; narrowing `cpa(heroPreviewVesselA, heroPreviewVesselB)`'s `.ok`/`.value.dcpaNm` and asserting it is within 0.01 of `1.18`.
  </action>
  <acceptance_criteria>
    - `src/components/ui/card.tsx` and `src/components/ui/badge.tsx` exist and are CLI-generated (not hand-written) — confirmed by their header comments/import shape matching `src/components/ui/button.tsx`'s existing style
    - `src/components/hero/hero-preview-fixture.ts` exports `heroPreviewVesselA` and `heroPreviewVesselB`, both typed `Vessel`, with the exact position/heading/speed values specified above
    - `src/components/hero/hero-preview-fixture.test.ts` exists, has no `// @vitest-environment jsdom` pragma, and asserts `encounterType === "crossing"`, `giveWay === "vesselA"`, `standOn === "vesselB"`, `doubt === false`, plus range/bearing/CPA within tolerance of 2.99/61/1.18
    - `npx vitest run src/components/hero/hero-preview-fixture.test.ts` passes with 0 failures
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/hero/hero-preview-fixture.test.ts</automated>
  </verify>
  <done>`card`/`badge` shadcn primitives are installed, `hero-preview-fixture.ts` exports the exact verified vessel fixture, and its drift-guard test passes, confirming the fixture still reproduces Rule 15/crossing/vesselA-gives-way and the design mock's displayed numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Build Hero.tsx (headline/copy/CTAs + static SVG preview card)</name>
  <files>src/components/hero/Hero.tsx, src/components/hero/Hero.test.tsx</files>
  <read_first>
    src/components/layout/Header.tsx
    src/components/sandbox/ChartPanel.tsx (visual conventions to imitate — polygon hull, named stroke constants, textAnchor="middle" — do NOT import from this file, per D-03)
    src/components/sandbox/ReasoningPanel.tsx (verdict-text derivation pattern to reimplement locally, not import)
    src/components/sandbox/SandboxContainer.tsx (Result-unwrap precedent at a known-good fixture call site; also confirms its own root is `<main>` — do not edit this file this task)
    src/components/hero/hero-preview-fixture.ts (from Task 1)
    src/components/ui/card.tsx (from Task 1 — confirm exact exported subcomponent names)
    src/components/ui/badge.tsx (from Task 1 — confirm exact exported variant prop values)
    src/components/sandbox/environment.smoke.test.tsx
    src/components/sandbox/ControlPanel.test.tsx (jsdom pragma + afterEach(cleanup) boilerplate)
    .planning/phases/07-hero/07-UI-SPEC.md (Copywriting Contract, Typography, Color, Spacing Scale, Executor Notes)
    .planning/phases/07-hero/07-RESEARCH.md (Code Examples: Hero.tsx skeleton, Pattern 1-3)
    .planning/design/Main-Design.png
  </read_first>
  <action>
    Create `src/components/hero/Hero.tsx` as a plain Server Component (no `"use client"` — no state/effects, matching `Header.tsx`'s convention). Import `classifyEncounter` from `"../../domain/colregs/classify-encounter.js"`, `bearing` from `"../../domain/geometry/bearing.js"`, `chartToScreen` from `"../../domain/geometry/screen-convert.js"`, `heroPreviewVesselA`/`heroPreviewVesselB` from `"./hero-preview-fixture.js"`, `Button` from `"@/components/ui/button"`, `Card`/`CardHeader`/`CardContent` from `"@/components/ui/card"`, `Badge` from `"@/components/ui/badge"`, and `ArrowRight`/`CircleCheck` from `"lucide-react"`.

    Call `classifyEncounter(heroPreviewVesselA, heroPreviewVesselB)` inline in the function body; if `!result.ok`, `throw new Error("Hero preview fixture failed to classify -- fixture is broken")` (developer-facing invariant per UI-SPEC's Error-state contract, mirroring `SandboxContainer.tsx`'s seed-fixture reasoning). Compute `screenA`/`screenB` via `chartToScreen(heroPreviewVesselA.position, HERO_CONTAINER_SIZE, HERO_VIEW_BOX)` / same for vesselB, where `HERO_CONTAINER_SIZE = { width: 320, height: 200 }` and `HERO_VIEW_BOX = { minX: -3, minY: -2.5, width: 8, height: 5 }` (8:5 aspect ratio, per UI-SPEC's "Preview Card Dimensions" correction — do NOT use a square viewBox). Also define a fixed `HERO_CHART_CENTER = { screenX: HERO_CONTAINER_SIZE.width / 2, screenY: HERO_CONTAINER_SIZE.height / 2 }` constant (evaluates to `{ screenX: 160, screenY: 100 }`) — pixel measurement of Main-Design.png confirms the two range rings, the bearing-sector apex, and the north reference line are centered on this fixed chart-canvas center, NOT on `screenA` (vessel A's own screen position sits off-center in this viewBox and is a ~40px/12.5%-of-canvas-width discrepancy from the mock's actual ring/sector/north-line center). `screenA`/`screenB` remain the vessel positions used for the heading-vector lines, the A→B connector, the distance-label chip, and the vessel groups — those stay tied to the vessels, unaffected by this constant.

    Derive display text locally (do NOT import `src/components/sandbox/vessel-role.ts` or `ReasoningPanel.tsx` — feature-boundary violation per Pattern 3/ARCHITECTURE.md): define `const ENCOUNTER_TYPE_TITLE: Record<EncounterType, string> = { crossing: "Crossing", "head-on": "Head-on", overtaking: "Overtaking" };` and `const VESSEL_LABEL_TEXT: Record<VesselLabel, string> = { vesselA: "Vessel A", vesselB: "Vessel B" };`, then compute the rule-banner verdict clause as `` `${VESSEL_LABEL_TEXT[result.value.giveWay as VesselLabel]} gives way` `` (fixture guarantees non-null `giveWay` for this crossing case). Derive each vessel's "GW"/"SO" role-pill text from `result.value.giveWay`/`result.value.standOn` (e.g. vesselA's pill text is `"GW"` if `result.value.giveWay === "vesselA"`, else `"SO"` if `result.value.standOn === "vesselA"`) — never hand-type "GW"/"SO" as unconditional literals. Hull/pill fill colors MAY stay hardcoded per UI-SPEC (fixture's verdict is fixed at authoring time): vesselA hull `#EF4444` (give-way), vesselB hull `#22C55E` (stand-on).

    Render markup in this structure, using UI-SPEC's exact locked copy verbatim (do not paraphrase):
    - Outer section: two-column flex layout, `flex-col` by default, `hero:flex-row hero:items-center hero:gap-12` at >=900px (or `min-[900px]:flex-row`/`min-[900px]:gap-12` if the named breakpoint variant is dropped by Task 3's verification step), `gap-8` mobile, `py-8 hero:py-12` (or `min-[900px]:py-12`) vertical section padding, `mx-auto max-w-6xl px-6` container.
    - Left column (`flex flex-col gap-6`): eyebrow `Badge variant="outline"` containing a small solid `bg-accent` dot span plus the text "Collision-avoidance rules engine"; `<h1>` at `text-[clamp(2rem,8vw,3.25rem)] font-extrabold leading-[1.1]` with two `<span>`s — `"Two vessels. One rulebook. "` in `text-foreground`, `"See who gives way — and why."` in `text-accent`; `<p>` body copy at `max-w-[50ch] text-base font-semibold text-muted-foreground` containing exactly: `"Drop two ships on a nautical chart. The engine classifies the encounter under the real International Regulations for Preventing Collisions at Sea, names the give-way vessel, and shows the exact rule and geometry behind the verdict."`; a CTA row (`flex flex-col gap-4 sm:flex-row`) with `<Button asChild size="lg"><a href="#sandbox">Open the sandbox<ArrowRight .../></a></Button>` and `<Button asChild variant="outline" size="lg"><a href="#gallery">Classic encounters</a></Button>`; a trust-note `<p>` at `text-[13px] font-semibold text-muted-foreground` with a leading `CircleCheck` icon (`text-accent`) and text `"Grounded in "` (muted) + `"Rules 11–18"` (in `text-foreground`) + `" of the actual COLREGS — Steering & Sailing Rules, conduct in sight of one another."` (muted).
    - Right column: `Card` (fluid width, no fixed px width) containing `CardHeader` with "Live classification" (a static, non-pulsing `bg-accent` dot + text, per D-01 — no animation classes) on the left and "BRG-ring · 12 NM" on the right, both `font-mono text-[13px] font-semibold text-muted-foreground`; `CardContent` containing (1) a bordered inset `<div>` wrapping an `<svg viewBox="0 0 320 200" width="100%" height="auto">` built per the SVG spec below, (2) a rule banner `<div className="flex items-center gap-2 rounded-md border border-primary/30 bg-primary/10 px-3 py-2">` containing `<Badge className="bg-primary text-primary-foreground">Rule 15</Badge>` and the derived verdict text (`"${ENCOUNTER_TYPE_TITLE[result.value.encounterType]} — ${giveWayClause}"`, e.g. `"Crossing — Vessel A gives way"`), and (3) a 3-column readout grid (`grid grid-cols-3 gap-4`) with tiles for RANGE ("2.99 NM"), BEARING ("061°"), CPA ("1.18 NM"), each tile a bordered `<div>` with a `font-mono text-[13px] font-semibold uppercase tracking-wide text-muted-foreground` label and a `font-mono text-base font-semibold text-foreground` value.

    Build the SVG (zero imports from `ChartPanel.tsx`, per D-03/Pitfall 1 — imitate its visual conventions only) as a layered static illustration matching `Main-Design.png`'s mini-chart in full detail (D-02: two concentric range rings, a shaded bearing sector, both dashed heading-vector lines, a solid distance-labeled connector — not a reduced version). Render these layers back-to-front:
    1. A uniform grid background (replaces the earlier "8 tick marks" idea — the mock's background is a uniform square grid, not discrete ticks): define `<defs><pattern id="heroGrid" width="20" height="20" patternUnits="userSpaceOnUse"><path d="M 20 0 L 0 0 0 20" fill="none" stroke="#1E293B" strokeWidth={1} /></pattern></defs>`, then a full-canvas `<rect x={0} y={0} width={320} height={200} fill="url(#heroGrid)" />`.
    2. Two concentric range rings, both centered at the fixed `HERO_CHART_CENTER` (matching the mock's fixed-canvas-center convention — NOT vessel A's screen position): define `HERO_OUTER_RING_RADIUS_PX = 2.3 * (HERO_CONTAINER_SIZE.width / HERO_VIEW_BOX.width)` (= 92) — corrected down from a single 120px ring, which would self-clip the 200px-tall canvas (a 120px radius centered at `HERO_CHART_CENTER.screenY = 100` spans y ∈ [-20, 220], 20px clipped top and bottom; 92px fits within the 100px vertical half with an 8px margin) — and render an outer `<circle r={HERO_OUTER_RING_RADIUS_PX}>` plus an inner `<circle r={HERO_OUTER_RING_RADIUS_PX * 0.6}>` (= ~55px), both centered at `HERO_CHART_CENTER` (i.e. `cx={HERO_CHART_CENTER.screenX} cy={HERO_CHART_CENTER.screenY}`), `stroke="#334155"` (slate-700), `fill="none"`, `strokeWidth={1}`.
    3. A shaded accent bearing sector (the mock's "range-ring stroke and sector-fill gradient" element, per UI-SPEC Color section): an SVG `<path>` wedge with apex at `HERO_CHART_CENTER` (fixed canvas center — NOT `screenA`, matching the mock's actual pixel geometry), computed from the real bearing (`bearing(heroPreviewVesselA.position, heroPreviewVesselB.position)` — do NOT hardcode 61) at radius `r = HERO_OUTER_RING_RADIUS_PX`. One straight edge runs due north from `HERO_CHART_CENTER` to `{HERO_CHART_CENTER.screenX, HERO_CHART_CENTER.screenY - r}`; the other runs along the true bearing to `{HERO_CHART_CENTER.screenX + r * Math.sin(bearingDeg * Math.PI / 180), HERO_CHART_CENTER.screenY - r * Math.cos(bearingDeg * Math.PI / 180)}`; connect the two endpoints with a clockwise arc (`A {r} {r} 0 0 1 {x2} {y2}`) and close back to `HERO_CHART_CENTER` (`Z`). Fill with a `<radialGradient>` (defined in `<defs>`, centered at `HERO_CHART_CENTER`) from `#2dd4bf` at ~35% opacity near the apex fading to fully transparent at the arc — a gradient fill, not a flat one, per UI-SPEC's explicit "sector-fill gradient" contract.
    4. The north reference line itself: a plain `<line>` from `HERO_CHART_CENTER` to `{HERO_CHART_CENTER.screenX, HERO_CHART_CENTER.screenY - HERO_OUTER_RING_RADIUS_PX}` (reuse the coordinates computed in step 3), `stroke="#334155"`, `strokeWidth={1}`.
    5. The solid A→B connector: `<line x1={screenA.screenX} y1={screenA.screenY} x2={screenB.screenX} y2={screenB.screenY} stroke="#475569" strokeWidth={1.5} />` — SOLID, with no `strokeDasharray` attribute at all, per UI-SPEC's locked Color table ("#475569 | The solid range/connecting line between Vessel A and Vessel B"); dashed rendering is reserved exclusively for the unused amber doubt/ambiguous state, not this line.
    6. A floating distance-label chip on that connector: a `<g transform="translate({(screenA.screenX + screenB.screenX) / 2} {(screenA.screenY + screenB.screenY) / 2})">` containing a small dark rounded `<rect x={-32} y={-11} width={64} height={22} rx={4} fill="#0F172A" />` and a centered `<text textAnchor="middle" dominantBaseline="middle" fill="#F8FAFC" fontFamily="monospace" fontSize={11}>` reading `` `${range.toFixed(2)} NM` `` where `range = Math.hypot(heroPreviewVesselB.position.x - heroPreviewVesselA.position.x, heroPreviewVesselB.position.y - heroPreviewVesselA.position.y)` — computed, not the hand-typed "2.99 NM" literal.
    7. Two dashed heading-vector lines, one per vessel (reproduces the mock's red line trailing Vessel A and green line trailing Vessel B): each a fixed 70px-long `<line strokeDasharray="3 3" strokeWidth={1.5} />` from the vessel's screen position extending in the direction of that vessel's own `heading` (not the bearing to the other vessel) — endpoint `{screen.screenX + 70 * Math.sin(vessel.heading * Math.PI / 180), screen.screenY - 70 * Math.cos(vessel.heading * Math.PI / 180)}`. Vessel A's line is `stroke="#EF4444"` (matches its give-way hull color); Vessel B's is `stroke="#22C55E"` (matches its stand-on hull color).
    8. Two vessel groups, each `<g transform="translate({screen.screenX} {screen.screenY}) rotate({vessel.heading})">` containing a `<polygon points="0,-9 6,7 -6,7">` filled with the vessel's hardcoded hull color, a small circular `<circle r={7} fill="#18181B" stroke="#3F3F46">` + centered `<text>` reading "A"/"B" (white, 10px/600) positioned above the hull (negative-y offset beyond the bow tip), and a colored role-pill (small rounded rect or circle, same hull color, white text) below the hull reading the derived "GW"/"SO" text. No `onPointerDown`/drag handlers anywhere — fully static, non-interactive markup.

    Create `src/components/hero/Hero.test.tsx` with the mandatory `// @vitest-environment jsdom` pragma as the literal first line, `afterEach(cleanup)` boilerplate (matching `ControlPanel.test.tsx`), and a `describe`/`it` block that: `render(<Hero />)`, then asserts `screen.getByRole("heading", { name: /two vessels/i })` is in the document, `screen.getByRole("link", { name: /open the sandbox/i })` has attribute `href="#sandbox"`, `screen.getByRole("link", { name: /classic encounters/i })` has attribute `href="#gallery"`, and `screen.getByText("Rule 15")` and `screen.getByText("2.99 NM")` are both in the document.
  </action>
  <acceptance_criteria>
    - `src/components/hero/Hero.tsx` exports `Hero`, contains no `"use client"` directive
    - Contains the exact string `"Two vessels. One rulebook. "` and the exact string `"See who gives way — and why."`
    - Contains the exact string `"Drop two ships on a nautical chart. The engine classifies the encounter under the real International Regulations for Preventing Collisions at Sea, names the give-way vessel, and shows the exact rule and geometry behind the verdict."`
    - Contains two `<a href="#sandbox">`/`<a href="#gallery">` anchors, each wrapped in `Button asChild`
    - Contains `viewBox="0 0 320 200"` and zero import statements referencing `ChartPanel`, `vessel-role`, or `sandbox/`
    - Contains `classifyEncounter(heroPreviewVesselA, heroPreviewVesselB)` (not a hand-typed literal replacing this call)
    - Contains a `HERO_CHART_CENTER` constant defined as `{ screenX: HERO_CONTAINER_SIZE.width / 2, screenY: HERO_CONTAINER_SIZE.height / 2 }` (evaluating to `{ screenX: 160, screenY: 100 }`)
    - Contains exactly two `<circle>` elements for the range rings (both referencing `HERO_CHART_CENTER`/`HERO_CHART_CENTER.screenX`/`HERO_CHART_CENTER.screenY` — NOT `screenA`), plus a `<radialGradient>` definition centered at `HERO_CHART_CENTER` and a sector `<path>` with its apex at `HERO_CHART_CENTER` containing an `A ` arc command (the shaded bearing wedge) — confirms the two-ring + sector requirement (D-02) instead of the old single-ring/tick-mark version, and confirms the rings/sector are centered at the fixed chart-canvas center rather than vessel A's screen position
    - Contains the north reference `<line>` referencing `HERO_CHART_CENTER` (not `screenA`)
    - Contains two dashed heading-vector `<line>` elements with `strokeDasharray="3 3"` — one `stroke="#EF4444"`, one `stroke="#22C55E"` — and does NOT contain the string `"tick"` or eight repeated 45°-increment radial elements (removed per the corrected mock reading)
    - Contains exactly one A→B connector `<line>` with `stroke="#475569"` that has no `strokeDasharray` attribute (solid, per UI-SPEC's locked Color table — not dashed), referencing `screenA`/`screenB` (vessel positions, unaffected by the `HERO_CHART_CENTER` fix)
    - Contains a distance-label `<text>` whose content is derived from `range.toFixed(2)` (not the hand-typed literal `"2.99 NM"` disconnected from a computed value), positioned inside a `<g>`/`<rect>` chip at the connector's midpoint
    - `src/components/hero/Hero.test.tsx` has `// @vitest-environment jsdom` as its literal first line and calls `cleanup()` in `afterEach`
    - `npx vitest run src/components/hero/Hero.test.tsx` passes with 0 failures
    - `npx tsc --noEmit` passes with 0 errors
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/hero && npx tsc --noEmit</automated>
  </verify>
  <done>Hero.tsx renders the exact locked headline/copy/CTAs/trust-note plus a fully static, independently-built SVG preview card (two range rings, shaded bearing sector, and north reference line centered at the fixed HERO_CHART_CENTER; dashed heading vectors and a solid distance-labeled connector centered on the vessels) whose numbers come from a real classifyEncounter()/bearing() call, and its RTL test passes confirming HERO-01/HERO-02/HERO-03's testable surface.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Hero and the #sandbox anchor into the home page; add smooth-scroll and the 900px breakpoint token</name>
  <files>app/page.tsx, app/globals.css</files>
  <read_first>
    app/page.tsx
    app/globals.css
    src/components/sandbox/SandboxContainer.tsx (confirm its root element is `<main>` — do not touch this file, per Pitfall H1)
    .planning/phases/07-hero/07-RESEARCH.md (Pitfall H1, Pitfall H3, Architecture Patterns diagram)
    .planning/phases/07-hero/07-PATTERNS.md (app/page.tsx and app/globals.css sections)
  </read_first>
  <action>
    Modify `app/page.tsx`: add `import { Hero } from "../src/components/hero/Hero.js";` (relative, `.js`-suffixed, matching the existing `SandboxContainer` import convention — this file is not shadcn-adjacent code). Change the `Home` component to return a fragment rendering `<Hero />` followed by `<section id="sandbox"><SandboxContainer /></section>`. Do NOT edit `src/components/sandbox/SandboxContainer.tsx` in this task — its current root `<main>` element is a pre-existing condition explicitly deferred to Phase 8 (Pitfall H1); adding `id="sandbox"` must land on the new wrapper in `app/page.tsx` only.

    Modify `app/globals.css` in two places:
    1. Inside the existing `@layer base { html { ... } }` block (which already contains `@apply font-sans;` and `scroll-padding-top: 64px;` with a `/* CONTEXT.md D-06: ... */` comment), add a `scroll-behavior: smooth;` declaration wrapped in `@media (prefers-reduced-motion: no-preference) { html { scroll-behavior: smooth; } }`, nested inside the same `@layer base` block, with a new attribution comment citing `CONTEXT.md D-05` (per 07-PATTERNS.md's app/globals.css section).
    2. Inside the existing `@theme inline { ... }` block, add `--breakpoint-hero: 900px;` as a new line, following the existing one-token-per-line style (no extra grouping comment needed).

    After saving both files, run `npm run dev` and inspect the compiled Tailwind CSS output (or the dev server's class-detection warnings) to confirm any `hero:`/`max-hero:` utility class used in Task 2's `Hero.tsx` actually compiles to real CSS (Pitfall H3 — custom-named breakpoint `max-*` variant generation is unverified, MEDIUM confidence). If it does NOT compile, edit `Hero.tsx` to replace every `hero:`/`max-hero:` utility with the equivalent `min-[900px]:`/`max-[899px]:` arbitrary-value utility, and remove the now-unused `--breakpoint-hero: 900px;` line from `app/globals.css`.
  </action>
  <acceptance_criteria>
    - `app/page.tsx` contains `import { Hero } from "../src/components/hero/Hero.js";`
    - `app/page.tsx` renders `<Hero />` immediately followed by `<section id="sandbox">` wrapping `<SandboxContainer />`
    - `git diff -- src/components/sandbox/SandboxContainer.tsx` produces zero output (file untouched)
    - `app/globals.css` contains `scroll-behavior: smooth` nested inside `@media (prefers-reduced-motion: no-preference)`
    - `app/globals.css` still contains `scroll-padding-top: 64px` unchanged
    - Either (`app/globals.css` contains `--breakpoint-hero: 900px;` AND `Hero.tsx`'s `hero:`/`max-hero:` classes are confirmed compiling to real CSS) OR (neither is present and `Hero.tsx` uses only `min-[900px]:`/`max-[899px]:` arbitrary-value classes)
    - `npm run typecheck` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>The home page renders `<Hero />` above a `#sandbox`-anchored `SandboxContainer` with `SandboxContainer.tsx` itself untouched, smooth-scroll respecting `prefers-reduced-motion` is wired, the 900px breakpoint mechanism is verified to compile (or the arbitrary-value fallback is used instead), and both `typecheck`/`build` pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Per 07-RESEARCH.md's Security Domain section: this phase introduces zero new authentication, session, access-control, input-validation, or cryptography surface. Hero is 100% static, non-interactive, server-rendered marketing content with no user input of any kind — there is no untrusted-input trust boundary to model in this phase.

| Boundary | Description |
|----------|--------------|
| npm registry -> local build environment | `npx shadcn add card badge` vendors component source drawing only on already-installed, already-audited dependencies (`radix-ui`, `class-variance-authority`, `clsx`, `tailwind-merge`, `lucide-react`) — no new npm packages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-07-01 | Tampering | `package.json`/`package-lock.json` via `npx shadcn add card badge` | accept | No new npm packages are installed — `card`/`badge` are CLI-vendored source files drawing only on dependencies already audited in Phase 6's Package Legitimacy Audit (zero `[ASSUMED]`/`[SUS]`/`[SLOP]` findings); re-running the gate is not applicable (07-RESEARCH.md Package Legitimacy Audit) |
| T-07-SC | Tampering | npm/CLI installs this phase | accept | Restated per protocol's mandatory row; audit table is intentionally empty (no new packages) — no blocking-human-checkpoint clause triggers |
| T-07-02 | Information Disclosure | Hero.tsx static markup / preview-card fixture | accept | All content is hardcoded, locked copy and a fixed, non-user-derived vessel fixture — no user-supplied or sensitive data is rendered anywhere in this phase |
| T-07-03 | Elevation of Privilege | N/A — no auth/session/access-control surface | accept | Phase 7 introduces zero auth/session/permission logic; confirmed in 07-RESEARCH.md Security Domain |
</threat_model>

<verification>
Run, in order, from the repo root:
1. `npx vitest run src/components/hero` — 0 failures (fixture-drift guard + Hero RTL test)
2. `npx tsc --noEmit` — 0 errors
3. `npm run build` — exits 0
4. `grep -c "classifyEncounter(heroPreviewVesselA" src/components/hero/Hero.tsx` — returns 1 or more (confirms the preview card's numbers are computed, not hand-typed)
5. `git diff -- src/components/sandbox/SandboxContainer.tsx` — returns no output (confirms Pitfall H1's file is untouched)
6. Manual visual/responsive/scroll verification against `Main-Design.png` is deferred to Plan 07-02's checkpoint (implementation and human verification are intentionally split — see Split Signals).
</verification>

<success_criteria>
- `npx vitest run src/components/hero`, `npx tsc --noEmit`, and `npm run build` all exit 0
- `Hero.tsx` renders above a real `#sandbox` anchor target in `app/page.tsx`, with `SandboxContainer.tsx` itself unmodified
- The preview card's RANGE/BEARING/CPA/Rule-15/verdict numbers trace back to a real `classifyEncounter()` call against `hero-preview-fixture.ts`, not hand-typed strings
- Both Hero CTAs are plain `<a href="#sandbox">`/`<a href="#gallery">` anchors wrapped in `Button asChild`, no `next/link`, no JS scroll library
- `app/globals.css` wires `scroll-behavior: smooth` (reduced-motion guarded) and either registers a working `--breakpoint-hero: 900px` token or Hero.tsx uses the arbitrary-value fallback exclusively
</success_criteria>

<output>
Create `.planning/phases/07-hero/07-01-SUMMARY.md` when done
</output>
