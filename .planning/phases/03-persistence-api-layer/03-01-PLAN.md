---
phase: 03-persistence-api-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - prisma/schema.prisma
  - prisma.config.ts
  - docker-compose.yml
  - .env.example
  - .gitignore
  - README.md
  - src/server/db/client.ts
autonomous: false
requirements: [SCEN-02]
user_setup:
  - service: docker
    why: "Local Postgres database for development (D-01) -- Scenario persistence has no in-memory fallback"
    env_vars:
      - name: DATABASE_URL
        source: ".env.example template in this plan -- copy to .env, matches docker-compose.yml service credentials (colregs/colregs/colregs_navigator on localhost:5432)"
    dashboard_config:
      - task: "Install and start Docker Desktop (or an equivalent local Docker daemon) if not already running"
        location: "https://www.docker.com/products/docker-desktop/"

must_haves:
  truths:
    - "The Prisma schema persists only raw scenario inputs (position, heading, speed, type for both vessels) -- no derived-verdict field exists anywhere in the schema"
    - "A local Postgres instance is running and reachable at DATABASE_URL, with the Scenario table created via a committed migration"
    - "Share IDs are non-guessable (Prisma cuid()), never a sequential integer"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Scenario model: flat vesselA*/vesselB* columns (D-03/D-04), cuid() id (D-05), isCurated/displayOrder/rationale gallery fields (D-08/D-09), no verdict column (D-06)"
      contains: "model Scenario"
    - path: "prisma.config.ts"
      provides: "Prisma 7 DATABASE_URL wiring (replaces schema-embedded datasource.url)"
    - path: "docker-compose.yml"
      provides: "Local Postgres 17 service (D-01)"
    - path: "src/server/db/client.ts"
      provides: "PrismaClient singleton constructed with the mandatory @prisma/adapter-pg driver adapter (Prisma 7 requirement)"
      exports: ["prisma"]
    - path: "prisma/migrations"
      provides: "Committed initial migration creating the Scenario table"
  key_links:
    - from: "src/server/db/client.ts"
      to: "@prisma/adapter-pg"
      via: "new PrismaPg({ connectionString })"
      pattern: "new PrismaPg\\("
    - from: "prisma.config.ts"
      to: "process.env.DATABASE_URL"
      via: "dotenv/config + env(\"DATABASE_URL\")"
      pattern: "env\\(.DATABASE_URL.\\)"
---

<objective>
Stand up the persistence foundation for Phase 3: install Prisma 7.8.0/tRPC 11.18.0/Next.js 16.2.10 and their required peers, author the Prisma schema exactly per CONTEXT.md's locked decisions (D-01 through D-09), provision local Docker Postgres, run the initial migration, and create the Prisma client singleton every later plan in this phase depends on.

Purpose: This is the first phase to introduce any framework dependency into the repo (Phases 1-2 are pure domain, zero dependencies). Getting the Prisma 7 driver-adapter architecture and the schema's "no verdict column" constraint right here is what makes SCEN-02's re-derive-on-read guarantee physically enforceable in later plans, not just a convention.
Output: `prisma/schema.prisma`, `prisma.config.ts`, `docker-compose.yml`, a committed initial migration, and `src/server/db/client.ts` exporting a working `prisma` client.

**Open Question 2 resolved (RESEARCH.md):** `@trpc/react-query` and `@tanstack/react-query` are installed as dependencies in this plan (cheap, avoids a second dependency-adding change later), but no `TRPCReactProvider`/`layout.tsx`/client-hook scaffold is built in this phase -- this phase's success criteria only cover routers (server-side), not client consumption, and ROADMAP.md assigns the interactive UI to Phase 4. Phase 4 will add the Provider wiring when it first needs to call these routers from a component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-persistence-api-layer/03-CONTEXT.md
@.planning/phases/03-persistence-api-layer/03-RESEARCH.md
@.planning/phases/03-persistence-api-layer/03-PATTERNS.md
@src/domain/vessel/vessel.ts

<interfaces>
<!-- Domain contract this plan's schema must mirror. No code changes here -- read-only reference. -->
From src/domain/vessel/vessel.ts:
```typescript
export const VesselTypeSchema = z.enum([
  "power-driven", "sailing", "fishing", "not-under-command", "restricted-in-ability-to-maneuver",
]);
export const PositionSchema = z.object({ x: z.number(), y: z.number() });
export const VesselSchema = z.object({
  position: PositionSchema,
  heading: z.number().gte(0).lt(360),
  speed: z.number().gte(0),
  type: VesselTypeSchema,
});
```
Open Question 3 (RESEARCH.md) is resolved here: `vesselAType`/`vesselBType` Prisma columns are plain `String`, NOT a native Postgres enum -- the only write path is Zod-validated tRPC input (Plan 03), so a second enum definition to keep in sync would add no real-world safety.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify dotenv package legitimacy before install</name>
  <files>N/A -- verification-only task, no files created or modified</files>
  <read_first>.planning/phases/03-persistence-api-layer/03-RESEARCH.md (Package Legitimacy Audit table -- dotenv is explicitly NOT in the audited 11-package table, flagged as "[ASSUMED] low-risk, not run through slopcheck")</read_first>
  <action>
    Run these evidence-gathering commands and present the output for human approval before Task 2 installs anything:
    - `npm view dotenv time.created`
    - `npm view dotenv repository.url`
    - `npm view dotenv scripts.postinstall`
    - `npm view dotenv dist-tags.latest`
    - `slopcheck install dotenv` if the `slopcheck` CLI is available in this environment (RESEARCH.md notes this actually performs an install as a side effect -- that is acceptable here since Task 2 installs dotenv properly immediately after; do not revert)
  </action>
  <what-built>Nothing yet -- this is a pre-install supply-chain gate on a package (`dotenv`) that RESEARCH.md's Package Legitimacy Audit did not cover.</what-built>
  <how-to-verify>
    Confirm from the command output: dotenv's repository points to `github.com/motdotla/dotenv`, has no `postinstall` script, and was created many years ago (this is one of the most widely-used npm packages -- tens of millions of weekly downloads). If a `slopcheck` verdict was produced, confirm it reads `[OK]`, not `[SUS]` or `[SLOP]`.
  </how-to-verify>
  <acceptance_criteria>
    - `npm view dotenv repository.url` output contains `motdotla/dotenv`
    - `npm view dotenv scripts.postinstall` returns empty/undefined
    - No `[SLOP]` or `[SUS]` verdict is present in any tool output reviewed
  </acceptance_criteria>
  <verify>
    <automated>npm view dotenv repository.url | grep -c "motdotla/dotenv"</automated>
  </verify>
  <done>Human has reviewed the dotenv legitimacy evidence and typed "approved".</done>
  <resume-signal>Type "approved" to proceed with installing dotenv in Task 2, or describe the concern if the evidence looks wrong.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install dependencies, author Prisma schema/config/Docker Compose, add setup docs</name>
  <files>package.json, package-lock.json, prisma/schema.prisma, prisma.config.ts, docker-compose.yml, .env.example, .gitignore, README.md</files>
  <read_first>
    package.json
    src/domain/vessel/vessel.ts
    .planning/phases/03-persistence-api-layer/03-RESEARCH.md (Standard Stack section, "Code Examples" section: Docker Compose / prisma.config.ts / schema.prisma examples)
    .planning/phases/03-persistence-api-layer/03-CONTEXT.md (D-01 through D-09)
  </read_first>
  <action>
    Run `npm install next@16.2.10 react@19.2.7 react-dom@19.2.7 @trpc/server@11.18.0 @trpc/client@11.18.0 @trpc/react-query@11.18.0 @tanstack/react-query@5.101.2 prisma@7.8.0 @prisma/client@7.8.0 @prisma/adapter-pg@7.8.0 pg@8.22.0 dotenv` then `npm install -D @types/pg`. Do NOT install `@trpc/next` (Pages-Router-only helper, not used with App Router per RESEARCH.md).

    Create `prisma/schema.prisma`:
    - `generator client { provider = "prisma-client"  output = "../generated/prisma" }` (Prisma 7's generator provider name -- NOT the legacy `prisma-client-js`; `output` is mandatory in 7.x)
    - `datasource db { provider = "postgresql" }` -- deliberately NO `url` field (Prisma 7 wires `DATABASE_URL` via `prisma.config.ts`, not the schema)
    - `model Scenario` with exactly these fields (D-03/D-04/D-05/D-06/D-08/D-09, Open Question 3): `id String @id @default(cuid())`, `createdAt DateTime @default(now())`, `vesselAPosX Float`, `vesselAPosY Float`, `vesselAHeading Float`, `vesselASpeed Float`, `vesselAType String`, `vesselBPosX Float`, `vesselBPosY Float`, `vesselBHeading Float`, `vesselBSpeed Float`, `vesselBType String`, `isCurated Boolean @default(false)`, `displayOrder Int?`, `rationale String?`. Add an inline comment directly above/inside the model citing D-06: no verdict/encounterType/giveWay/standOn field exists anywhere in this model, and none should ever be added.

    Create `prisma.config.ts` at the repo root: `import "dotenv/config"` first, then `import { defineConfig, env } from "prisma/config"`, exporting `defineConfig({ schema: "prisma/schema.prisma", migrations: { path: "prisma/migrations" }, datasource: { url: env("DATABASE_URL") } })`.

    Create `docker-compose.yml` at the repo root: one `postgres` service, `image: postgres:17`, `environment: { POSTGRES_USER: colregs, POSTGRES_PASSWORD: colregs, POSTGRES_DB: colregs_navigator }`, `ports: ["5432:5432"]`, a named volume `postgres-data:/var/lib/postgresql/data`, and a `healthcheck` using `pg_isready -U colregs` (interval 5s, timeout 5s, retries 5).

    Create `.env.example` at the repo root containing exactly: `DATABASE_URL="postgresql://colregs:colregs@localhost:5432/colregs_navigator?schema=public"`.

    Update `.gitignore` to add `.env`, `generated/`, and `prisma/generated/` (the last is defensive in case the output path is ever adjusted) as new entries -- do not remove any existing entries (`node_modules/`, `dist/`, `coverage/`, `.DS_Store`, `*.log`).

    Create (or extend, if it does not yet exist) `README.md` with a "## Local Setup" section documenting, in order: prerequisites (Docker Desktop, Node.js 22+), `cp .env.example .env`, `docker compose up -d`, `npm install`, `npx prisma migrate dev`, `npm test`. Explicitly note per D-02: there is no hosted/live deployment for this milestone -- local Docker Postgres only.
  </action>
  <acceptance_criteria>
    - `package.json` `dependencies` include `prisma`, `@prisma/client`, `@prisma/adapter-pg`, `pg`, `next`, `@trpc/server`, `@trpc/client`, `@trpc/react-query`, `@tanstack/react-query`, `dotenv` at the exact versions listed above; `devDependencies` include `@types/pg`
    - `grep -c "provider = \"prisma-client\"" prisma/schema.prisma` returns 1 and `grep -c "prisma-client-js" prisma/schema.prisma` returns 0
    - `grep -c "model Scenario" prisma/schema.prisma` returns 1
    - `grep -v '^//' prisma/schema.prisma | grep -ci 'verdict\|encounterType\|giveWay\|standOn'` returns 0 (no derived-verdict field anywhere, D-06)
    - `grep -c "url" prisma/schema.prisma` returns 0 (no schema-embedded datasource URL -- Prisma 7 convention)
    - `prisma.config.ts` contains `dotenv/config` and `env("DATABASE_URL")`
    - `docker-compose.yml` contains `image: postgres:17` and a `healthcheck` block
    - `.env.example` contains `DATABASE_URL=`
    - `.gitignore` contains a `.env` line
    - `README.md` contains a `docker compose up -d` line and a `prisma migrate dev` line
  </acceptance_criteria>
  <verify>
    <automated>grep -c "model Scenario" prisma/schema.prisma && test "$(grep -v '^//' prisma/schema.prisma | grep -ci 'verdict\|encounterType\|giveWay\|standOn')" = "0"</automated>
  </verify>
  <done>Dependencies installed at locked versions; schema.prisma, prisma.config.ts, docker-compose.yml, .env.example, .gitignore, README.md all exist with the content specified above.</done>
</task>

<task type="auto" gate="blocking">
  <name>Task 3: [BLOCKING] Start Docker Postgres, run initial migration, create Prisma client singleton</name>
  <files>prisma/migrations, generated/prisma, src/server/db/client.ts</files>
  <read_first>
    prisma/schema.prisma (from Task 2)
    prisma.config.ts (from Task 2)
    .planning/phases/03-persistence-api-layer/03-RESEARCH.md (Pattern 1, "Prisma 7 driver-adapter client setup")
  </read_first>
  <action>
    If `.env` does not already exist, run `cp .env.example .env` (never commit `.env`). Confirm the `docker` CLI is present (`docker --version`); if it is not, stop and report this as a blocker to the user rather than fabricating a passing result -- there is no fallback for local Postgres per D-01/D-02.

    Run `docker compose up -d` and poll `docker compose exec -T postgres pg_isready -U colregs` (or `docker compose ps`) until the healthcheck reports ready before proceeding -- do not run the migration against a container that is still starting.

    Run `npx prisma migrate dev --name init` (the `--name` flag avoids the interactive migration-name prompt for this first migration; this environment has no existing migration history so no destructive-change/reset prompt is expected). This creates `prisma/migrations/<timestamp>_init/migration.sql` (containing the `CREATE TABLE "Scenario"` statement) and generates the client at `generated/prisma` per the schema's `output` path. If the command hangs waiting on a TTY prompt that cannot be answered non-interactively, stop and report this as a blocker rather than working around it silently.

    Create `src/server/db/client.ts` per RESEARCH.md Pattern 1 verbatim: import `PrismaClient` from `"../../../generated/prisma/client.js"` and `PrismaPg` from `"@prisma/adapter-pg"`; declare `const globalForPrisma = globalThis as unknown as { prisma?: PrismaClient };`; construct `const adapter = new PrismaPg({ connectionString: process.env.DATABASE_URL });`; export `export const prisma = globalForPrisma.prisma ?? new PrismaClient({ adapter });`; and reassign `globalForPrisma.prisma = prisma;` inside `if (process.env.NODE_ENV !== "production")`. This is the HMR-safe singleton every later plan's repository/service imports.
  </action>
  <acceptance_criteria>
    - `docker compose exec -T postgres pg_isready -U colregs` reports "accepting connections"
    - `prisma/migrations/` contains at least one migration directory whose `migration.sql` contains `CREATE TABLE` and `"Scenario"`
    - `generated/prisma` directory exists and contains generated client files
    - `src/server/db/client.ts` exports `prisma` and imports `PrismaPg` from `"@prisma/adapter-pg"`
    - A one-off connectivity check succeeds, e.g. `npx prisma db execute --stdin <<< "SELECT 1;"` returns no connection error
  </acceptance_criteria>
  <verify>
    <automated>docker compose exec -T postgres pg_isready -U colregs && ls prisma/migrations | wc -l && test -d generated/prisma && echo CLIENT_OK</automated>
  </verify>
  <done>Docker Postgres is running and healthy, the initial migration is applied and committed to `prisma/migrations/`, the Prisma client is generated, and `src/server/db/client.ts` exports a working singleton `prisma` client.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> local install | New third-party packages (Prisma, tRPC, Next.js, pg, dotenv) enter the dependency tree for the first time in this repo |
| Application -> Postgres | `DATABASE_URL` connection string and all query parameters cross this boundary via the Prisma-generated client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-SC | Tampering | npm installs (package.json) | mitigate | RESEARCH.md's Package Legitimacy Audit ran `slopcheck install` against 11 of the 12 new packages, all `[OK]`; the 12th (`dotenv`) is gated by Task 1's blocking human-verify checkpoint before install, per the package-legitimacy-gate protocol for `[ASSUMED]` packages |
| T-3-01 | Information Disclosure | Share ID generation (`prisma/schema.prisma` `id` field) | mitigate | `@default(cuid())` (D-05) provides collision-resistant, non-sequential, non-guessable IDs -- this is the schema-level control that makes share-by-link viable without auth |
| T-3-02 | Tampering | `docker-compose.yml` local Postgres credentials | accept | Hardcoded `colregs`/`colregs` credentials are local-development-only (D-01/D-02: no hosted deployment this milestone); acceptable since the container is bound to `localhost:5432` and not exposed beyond the developer's machine |
</threat_model>

<verification>
Run `npm test` (existing domain suite must still pass unchanged -- this plan adds no domain-layer files). Run `npx tsc --noEmit` once a `tsconfig.json` include path covers the new files (if not yet, this plan does not require it -- Plan 02 introduces the first `src/server/` TypeScript files that need type-checking against the generated Prisma client). Confirm `docker compose ps` shows the `postgres` service `healthy`.
</verification>

<success_criteria>
- `npm install` completes with all locked versions present in `package.json`
- `prisma/schema.prisma` matches D-03/D-04/D-05/D-06/D-08/D-09 and Open Question 3 exactly, with zero verdict-shaped fields
- `docker compose up -d` brings up a healthy Postgres container
- `npx prisma migrate dev --name init` has run successfully and its migration is committed under `prisma/migrations/`
- `src/server/db/client.ts` exports a working `prisma` singleton built with `@prisma/adapter-pg`
</success_criteria>

<output>
Create `.planning/phases/03-persistence-api-layer/03-01-SUMMARY.md` when done
</output>
