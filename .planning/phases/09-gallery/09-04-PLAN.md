---
phase: 09-gallery
plan: 04
type: execute
wave: 3
depends_on: ["09-03"]
files_modified: []
autonomous: false
requirements: [GAL-01, GAL-03, GAL-04]
must_haves:
  truths:
    - "A fresh browser tab navigating to /gallery lands scrolled to the Gallery section, not the top of the page (GAL-03)"
    - "An in-app <Link href=\"#gallery\"> click (Header nav) also lands scrolled to the Gallery section — an independent code path from the fresh-tab redirect (GAL-03)"
    - "The Gallery card grid visually matches Main-Design.png at 3/2/1 column breakpoints (GAL-01)"
  artifacts: []
  key_links: []
---

<objective>
Manually verify, in a real browser (not jsdom), the two independent navigation paths GAL-03 requires -- fresh-tab/bookmark redirect and in-app anchor-link click -- plus a visual fidelity pass on the card grid's responsive breakpoints, since none of these can be confirmed by an automated jsdom test (per `.planning/research/PITFALLS.md` Pitfall 6 and STATE.md's own recorded blocker for this phase).

Purpose: this is the mandatory manual checkpoint the project's Blockers/Concerns section already flags for Phase 9 -- native browser fragment-scroll behavior on redirect cannot be confirmed from docs or automated tests alone.
Output: a human-confirmed pass/fail on GAL-01/GAL-03/GAL-04's actual browser behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-gallery/09-UI-SPEC.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual browser verification of the /gallery redirect, in-app anchor scroll, and card grid fidelity</name>
  <read_first>
    src/components/gallery/GalleryContainer.tsx
    next.config.ts
    app/page.tsx
    .planning/phases/09-gallery/09-UI-SPEC.md (Layout, Mini-Chart Contract, Copywriting Contract sections)
  </read_first>
  <files></files>
  <action>
    No code changes in this task -- it is a pure human-verification gate over Plan 03's already-implemented Gallery section and redirect. Perform the 5 browser checks listed in `<how-to-verify>` below and report back "approved" or a concrete list of issues.
  </action>
  <what-built>
    The Gallery section is now embedded on the home page below the Sandbox (6-card responsive grid, server-rendered), `/gallery` issues a permanent redirect to `/#gallery`, and the old standalone route is deleted.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open the app in a real browser (not just `npm test`/jsdom -- these checks require actual browser navigation/scroll behavior):

    1. **Fresh-tab redirect (GAL-03, path 1 of 2):** Open a brand-new browser tab and navigate directly to `http://localhost:3000/gallery` (type it in the address bar, or open it as if from a bookmark -- not by clicking a link from within the app). Confirm: (a) the URL bar updates to `http://localhost:3000/#gallery`, (b) the page lands scrolled so the "Classic encounters, one click away" heading is visible near the top of the viewport -- NOT scrolled to the very top of the page (Hero should not be visible).
    2. **In-app anchor click (GAL-03, path 2 of 2 -- an independent failure mode from step 1):** From the home page (`http://localhost:3000/`), scroll back to the top, then click the "Gallery" link in the sticky Header nav. Confirm the page smooth-scrolls (or jumps) to the same Gallery section, landing at the same position as step 1.
    3. **Card grid visual fidelity (GAL-01):** With the browser at full desktop width (>=1280px), confirm the Gallery grid shows exactly 3 columns of cards, compare against `.planning/design/Main-Design.png`'s Gallery section for layout/spacing/card content match (mini-chart, Rule-N badge, verdict badge, title, description). Resize the browser to ~700px width and confirm the grid switches to 2 columns. Resize to ~500px width and confirm it switches to 1 column.
    4. **Card click-through (GAL-02, quick regression check):** Click any gallery card and confirm it navigates to `/s/{id}` and renders that scenario's Sandbox pre-seeded with the correct vessels (matching the card's title/badges).
    5. **Server-rendered confirmation (GAL-04):** With JavaScript disabled in dev tools, or via view-source, confirm the Gallery section's HTML (including the `id="gallery"` anchor and all 6 card titles) is present in the initial server-rendered response, not injected after a client-side fetch.
  </how-to-verify>
  <acceptance_criteria>
    - Fresh-tab navigation to `/gallery` updates the URL to `/#gallery` and lands scrolled to the Gallery heading, not the page top
    - Clicking the Header's in-app "Gallery" nav link lands at the same scroll position (independent code path confirmed working)
    - Grid shows 3 columns at >=1280px, 2 columns at ~700px, 1 column at ~500px, matching Main-Design.png
    - Clicking a card navigates to `/s/{id}` and renders the correct pre-seeded scenario
    - The Gallery section's HTML (id="gallery" + all 6 titles) is present with JavaScript disabled / in view-source
  </acceptance_criteria>
  <resume-signal>Type "approved" if all 5 checks pass, or describe which check failed and what you observed.</resume-signal>
  <verify>
    <manual>All 5 steps in how-to-verify completed with no issues found; human types "approved"</manual>
  </verify>
  <done>Human tester confirms both GAL-03 navigation paths land scrolled to the Gallery section, the grid matches the design at all 3 breakpoints, card click-through works, and the section is confirmed server-rendered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Human verifier's browser -> deployed dev server | Manual-only step; no new code changes in this plan, purely a verification gate on Plan 03's already-implemented redirect/section |

## STRIDE Threat Register

No new threat surface is introduced by this plan -- it verifies, but does not modify, Plan 03's redirect and section-rendering behavior. See Plan 03's threat model (T-09-06 through T-09-08) for the underlying implementation's threat register.

No npm/pip/cargo package installs occur in this plan -- the Package Legitimacy Gate is not triggered.
</threat_model>

<verification>
Human-verified per the 5 steps above. No automated command can substitute for this (browser fragment-scroll-on-redirect behavior is not observable in jsdom).
</verification>

<success_criteria>
- Both independent navigation paths (fresh-tab redirect, in-app anchor click) land scrolled to the Gallery section
- The card grid visually matches the design at all 3 breakpoints
- Card click-through still works and the section is confirmed server-rendered
</success_criteria>

<output>
Create `.planning/phases/09-gallery/09-04-SUMMARY.md` when done
</output>
