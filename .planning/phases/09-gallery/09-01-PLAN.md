---
phase: 09-gallery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/colregs/classify-encounter.fixtures.ts
  - src/server/db/curated-scenarios.ts
  - src/server/db/curated-scenarios.test.ts
  - src/server/db/scenario-repository.ts
  - prisma/schema.prisma
  - prisma/seed.ts
autonomous: true
requirements: [GAL-01]
must_haves:
  truths:
    - "Each of the 6 curated gallery rows in the dev DB classifies via classifyEncounter() to the exact encounterType/giveWay the design specifies for that card (D-01)"
    - "The not-under-command and in-doubt fixtures reuse Phase 8's verified chip-scenarios.ts vessel geometry verbatim, exported from classify-encounter.fixtures.ts to respect the server/UI dependency direction (D-02)"
    - "The old stand-on-mirror duplicate entry is dropped, leaving exactly 6 cards with no mirror pair (D-03)"
    - "Each curated row carries a title, a static Rule-N label, and a punchy one-line description matching Main-Design.png verbatim (D-04)"
    - "prisma/seed.ts is updated for the new 6-entry set and the local dev DB is reseeded as part of this phase, idempotently (D-05)"
    - "Re-running the seed script never doubles the curated set (idempotent reseed, Pitfall 4)"
  artifacts:
    - path: "src/domain/colregs/classify-encounter.fixtures.ts"
      provides: "3 new named ClassificationCase exports (crossingSailingPriorityCase, crossingNotUnderCommandCase, headOnInDoubtCase) copied verbatim from chip-scenarios.ts"
    - path: "src/server/db/curated-scenarios.ts"
      provides: "CuratedScenario[] with exactly 6 entries, each carrying title/ruleLabel/rationale/displayOrder, sourced only from classify-encounter.fixtures.ts exports"
    - path: "prisma/schema.prisma"
      provides: "Scenario.title and Scenario.ruleLabel nullable String columns"
  key_links:
    - from: "prisma/seed.ts"
      to: "src/server/db/curated-scenarios.ts"
      via: "curatedScenarios array import, title/ruleLabel passed into prisma.scenario.create"
      pattern: "title: entry.title"
    - from: "src/server/db/scenario-repository.ts"
      to: "prisma/schema.prisma"
      via: "ScenarioRow interface mirrors the Scenario model's columns"
      pattern: "ruleLabel: string \\| null"
---

<objective>
Fix the curated gallery's seed data so its 6 rows match the design's exact 6 labels/rules/give-way vessels (D-01/D-02/D-03), add the `title` and `ruleLabel` fields the cards need (D-04 plus a research-gap correction — see Task 2), migrate the schema, and reseed the local dev DB (D-05) so `gallery.list()` returns exactly the 6 correct rows.

Purpose: this is the one part of Phase 9 that legitimately reaches into `src/server/db/`+`prisma/` (per 09-CONTEXT.md's domain boundary note) — every later Gallery plan (card rendering, mini-chart) depends on this data being correct, so it must land first and independently.
Output: 3 new domain fixtures, a corrected 6-entry `curatedScenarios` array with `title`/`ruleLabel` fields, a migrated schema, an idempotent reseed script, and a rewritten `curated-scenarios.test.ts` that no longer assumes a mirror-pair entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-gallery/09-CONTEXT.md
@.planning/phases/09-gallery/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the 3 missing domain fixtures</name>
  <files>src/domain/colregs/classify-encounter.fixtures.ts</files>
  <read_first>
    src/domain/colregs/classify-encounter.fixtures.ts (read the whole file once — note the un-exported local `interface ClassificationCase` shape at the top, and the existing doc-comment convention above each export, e.g. `crossingResidualBasicCase` at line 220 and `crossingRule18OverrideCase` at line 364)
    src/components/sandbox/chip-scenarios.ts (source of the exact literal values to copy — `sailingHasPriorityVessels`, `notUnderCommandVessels`, `inDoubtVessels`)
  </read_first>
  <action>
    Add 3 new exported consts to classify-encounter.fixtures.ts, appended after the existing `overtakingRule18NoOverrideCase` export (before `stage0CoincidentPropagationCase`), each typed `ClassificationCase` (the file's existing local interface — no new import needed):
    - `crossingSailingPriorityCase`: vesselA `{position:{x:0,y:0}, heading:0, speed:10, type:"power-driven"}`, vesselB `{position:{x:5,y:0}, heading:270, speed:10, type:"sailing"}`, expectedEncounterType `"crossing"`, expectedGiveWay `"vesselA"`, expectedStandOn `"vesselB"`, expectedDoubt `false`. Copied verbatim from chip-scenarios.ts's `sailingHasPriorityVessels`.
    - `crossingNotUnderCommandCase`: vesselA `{position:{x:1.4,y:0}, heading:270, speed:10, type:"power-driven"}`, vesselB `{position:{x:0,y:0}, heading:0, speed:10, type:"not-under-command"}`, expectedEncounterType `"crossing"`, expectedGiveWay `"vesselA"`, expectedStandOn `"vesselB"`, expectedRiskOfCollision `true`, expectedDoubt `false`. Copied verbatim from chip-scenarios.ts's `notUnderCommandVessels`.
    - `headOnInDoubtCase`: vesselA `{position:{x:0,y:0}, heading:45, speed:10, type:"sailing"}`, vesselB `{position:{x:7.66044443118978,y:6.427876096865393}, heading:225, speed:10, type:"sailing"}`, expectedEncounterType `"head-on"`, expectedGiveWay `null`, expectedStandOn `null`, expectedRiskOfCollision `true`, expectedDoubt `true`, expectedDoubtBoundary `"near-head-on-boundary"`. Copied verbatim from chip-scenarios.ts's `inDoubtVessels`.
    Write a one-line doc comment above each matching this file's existing convention (state the source: "copied verbatim from chip-scenarios.ts's X, already proven correct against classifyEncounter() by chip-scenarios.test.ts"). This is pure data — zero change to classify-encounter.ts's dispatch logic.
  </action>
  <acceptance_criteria>
    - `grep -c "export const crossingSailingPriorityCase\|export const crossingNotUnderCommandCase\|export const headOnInDoubtCase" src/domain/colregs/classify-encounter.fixtures.ts` returns 3
    - `npx tsc --noEmit` passes (no type errors from the new exports)
    - `npx vitest run src/domain/colregs/classify-encounter.test.ts` still passes unchanged (existing fixtures untouched)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/domain/colregs/classify-encounter.test.ts</automated>
  </verify>
  <done>3 new named ClassificationCase exports exist, byte-identical in vessel literals to chip-scenarios.ts's corresponding fixtures, with zero change to any existing export.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite curated-scenarios.ts's data and test suite</name>
  <files>src/server/db/curated-scenarios.ts, src/server/db/curated-scenarios.test.ts, src/server/db/scenario-repository.ts</files>
  <read_first>
    src/server/db/curated-scenarios.ts (current 6-entry array to replace entirely)
    src/server/db/curated-scenarios.test.ts (current 5 tests — Test 3 and Test 4 need rewriting)
    src/server/db/scenario-repository.ts (ScenarioRow interface — add the 2 new fields here too)
    .planning/phases/09-gallery/09-CONTEXT.md (D-01 through D-05, and the `<specifics>` verbatim copy table)
  </read_first>
  <action>
    **CuratedScenario interface** (in curated-scenarios.ts): add two new required string fields between `vesselB` and `rationale`: `title: string` and `ruleLabel: string`. Keep `rationale: string` (its VALUE changes to the new punchy one-liner below, replacing the old longer explanatory sentences) and `displayOrder: number` unchanged.

    **Research-gap correction (read this before writing `ruleLabel` values):** 09-RESEARCH.md recommends deriving the card's "Rule N" badge dynamically via `VerdictBanner.tsx`'s `bannerRuleBadge()` pattern (classifyingEntryIndex + doubt-override). Verified directly against `classify-encounter.ts`: (a) doubt is unconditionally `true` for every head-on classification (Stage 4, "Per Assumption A1"), so a doubt-override would render "Rule 7" for BOTH the "Head-on meeting" card and the "In doubt" card, but D-01 requires "Head-on meeting" to show "Rule 14"; (b) `classifyingEntryIndex()` targets the trail's second-to-last entry (the dispatch-stage rule), which for every crossing-type encounter is always "Rule 15" regardless of whether a Rule 18 override actually fired (Stage 6's Rule 18(a)-(c) confirmation is always the trail's FINAL entry, one past what classifyingEntryIndex targets) — so both "Sailing has priority" and "Not under command" would render "Rule 15", not D-01's required "Rule 18". Reusing the dynamic derivation therefore cannot satisfy D-01 for 3 of the 6 cards. Resolution: `ruleLabel` is a static, per-entry field (like `title`), not derived at render time — this is why it is being added to the schema alongside `title` even though CONTEXT.md's D-04 only named `title`.

    **The 6 entries** (displayOrder 0-5, in this exact order), each vesselA/vesselB sourced ONLY from classify-encounter.fixtures.ts imports (never a hand-typed literal, per this file's existing header convention):
    1. displayOrder 0, `title: "Classic crossing"`, `ruleLabel: "Rule 15"`, vesselA/vesselB from `crossingResidualBasicCase` (unchanged), `rationale: "Power-driven vessels on crossing courses; the one with the other to starboard keeps clear."`
    2. displayOrder 1, `title: "Head-on meeting"`, `ruleLabel: "Rule 14"`, vesselA/vesselB from `headOnGenuineCase` (unchanged), `rationale: "Reciprocal courses, each dead ahead of the other — both alter course to starboard."`
    3. displayOrder 2, `title: "Overtaking"`, `ruleLabel: "Rule 13"`, vesselA = `overtakingBothDirectionsCase.vesselB`, vesselB = `overtakingBothDirectionsCase.vesselA` (slots swapped so giveWay resolves to vesselA, per Pitfall 1), `rationale: "A faster vessel comes up from abaft the beam and must keep clear."`
    4. displayOrder 3, `title: "Sailing has priority"`, `ruleLabel: "Rule 18"`, vesselA/vesselB from the new `crossingSailingPriorityCase` (Task 1), `rationale: "A power-driven vessel keeps clear of a sailing vessel regardless of geometry."`
    5. displayOrder 4, `title: "Not under command"`, `ruleLabel: "Rule 18"`, vesselA/vesselB from the new `crossingNotUnderCommandCase` (Task 1), `rationale: "Every other vessel keeps out of the way of a vessel not under command."`
    6. displayOrder 5, `title: "In doubt"`, `ruleLabel: "Rule 7"`, vesselA/vesselB from the new `headOnInDoubtCase` (Task 1), `rationale: "Near-reciprocal but ambiguous — assume a head-on situation exists and act accordingly."`

    Drop the old entry #2 (the stand-on-mirror duplicate, D-03) and the old fishing/`crossingRule18OverrideCase` and `headOnRule18OverrideCase` entries entirely — they are no longer used by any of the 6 new entries.

    **scenario-repository.ts**: add `title: string | null;` and `ruleLabel: string | null;` to the `ScenarioRow` interface, in the same position/style as the existing `rationale: string | null;` field (do not add these to the `create()` function's `data` object here — that happens in Task 3 via `prisma/seed.ts`, this file only defines the read-shape).

    **curated-scenarios.test.ts rewrite**: Test 1 (length 5-8) and Test 2 (every entry classifies) stay unchanged. Rewrite Test 3 (currently asserts a mirror-geometry pair, which no longer exists) to instead assert coverage: encounterType counts of head-on >=1, crossing >=1, overtaking >=1; plus `giveWay === "vesselA"` count >=1 (design's "A GIVES WAY" cards) and `giveWay === null` count >=1 (design's "MUTUAL" cards). Rewrite Test 4 (currently asserts every type-mismatch entry is a genuine Rule 18 override, which is now only true for "Not under command" — "Sailing has priority" is a type-mismatch entry whose baseline already matches, per the research-gap note above) to: keep the type-mismatch-count bound (`>=1 and <=3`), but only assert `genuineOverrideCount >= 1` where `genuineOverrideCount` increments per mismatch entry only when `actual.value.giveWay !== baseline.value.giveWay` (do not assert inequality unconditionally for every mismatch entry — some, like "Sailing has priority", legitimately have `actual.giveWay === baseline.giveWay`, meaning no override fired). Test 5 (unique displayOrder + non-empty rationale): add two more assertions in the same loop — `typeof entry.title === "string"` and `entry.title.length > 0`, and the same for `entry.ruleLabel`.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/server/db/curated-scenarios.test.ts` passes with all 5 tests green
    - `curatedScenarios.length === 6`
    - Every `curatedScenarios[i].vesselA`/`vesselB` is a bare reference to (or field-swap of) a `classify-encounter.fixtures.ts` export — `grep -n "position: {" src/server/db/curated-scenarios.ts` returns 0 matches (no hand-typed vessel literals in this file)
    - `grep -c "title: string" src/server/db/scenario-repository.ts` returns 1 and `grep -c "ruleLabel: string" src/server/db/scenario-repository.ts` returns 1
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/server/db/curated-scenarios.test.ts</automated>
  </verify>
  <done>curated-scenarios.ts has exactly 6 entries matching D-01's labels/rules/give-way vessels, each carrying a static title+ruleLabel+rationale; curated-scenarios.test.ts passes without assuming a mirror pair; scenario-repository.ts's ScenarioRow type includes title/ruleLabel.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate schema, update seed script, reseed local DB</name>
  <files>prisma/schema.prisma, prisma/seed.ts</files>
  <read_first>
    prisma/schema.prisma (the `Scenario` model — `rationale`/`displayOrder`'s existing nullable-field convention to copy)
    prisma/seed.ts (the `main()` function — Pitfall 4's idempotent-reseed guard must be added here)
  </read_first>
  <action>
    In `prisma/schema.prisma`'s `Scenario` model, add two new nullable fields after the existing `rationale String?` line: `title String?` and `ruleLabel String?` (curated-only, populated by prisma/seed.ts, null for non-curated user-shared scenarios — matching `rationale`'s exact convention, comment above them can restate this).

    In `prisma/seed.ts`'s `main()` function: (a) add `await prisma.scenario.deleteMany({ where: { isCurated: true } });` as the first statement inside `main()`, before the `for` loop — this is Pitfall 4's idempotent-reseed guard, preventing the seed from doubling the gallery on a second run while preserving any non-curated rows a developer created locally via the Sandbox's share button; (b) add `title: entry.title,` and `ruleLabel: entry.ruleLabel,` to the existing `prisma.scenario.create({ data: {...} })` call, alongside the existing `rationale: entry.rationale,` line.

    Run the migration and reseed against the local dev DB (Postgres container `colregs-navigator-postgres-1`, already running per `docker ps`): `npx prisma migrate dev --name add_curated_title_and_rule_label`, then `npx prisma db seed` (or rely on `migrate dev`'s own auto-seed invocation per `prisma.config.ts`'s `migrations.seed`, whichever this repo's existing convention triggers — confirm no error, do not skip this step, D-05 requires the local dev DB actually holds the new 6-row set by the end of this task, not just the code that would produce it).
  </action>
  <acceptance_criteria>
    - `grep -c "title *String?" prisma/schema.prisma` returns 1 and `grep -c "ruleLabel *String?" prisma/schema.prisma` returns 1
    - A new file exists under `prisma/migrations/*_add_curated_title_and_rule_label/migration.sql`
    - `grep -c "deleteMany" prisma/seed.ts` returns 1
    - After running the migration+reseed, a direct query confirms exactly 6 curated rows exist with non-null title/ruleLabel: run `npx tsx -e "import {prisma} from './src/server/db/client.js'; prisma.scenario.findMany({where:{isCurated:true}}).then(rows=>{console.log(rows.length, rows.every(r=>r.title && r.ruleLabel))}).finally(()=>prisma.$disconnect())"` and confirm output is `6 true`
  </acceptance_criteria>
  <verify>
    <automated>npx tsx -e "import {prisma} from './src/server/db/client.js'; prisma.scenario.findMany({where:{isCurated:true}}).then(rows=>{console.log(rows.length, rows.every(r=>r.title && r.ruleLabel))}).finally(()=>prisma.$disconnect())"</automated>
  </verify>
  <done>Scenario table has title/ruleLabel columns; prisma/seed.ts is idempotent and populates both new fields; the local dev DB's curated rows are exactly the new 6-entry set with title/ruleLabel populated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Developer CLI -> local Postgres | `prisma migrate dev` / `prisma db seed` run against the local dev container only — no production database exists for this portfolio project |
| tRPC `gallery.list()` (public, read-only) -> Postgres | Unchanged read-only query; the 2 new columns are additive and nullable, no new write path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | `prisma/schema.prisma` migration | accept | Additive, nullable columns only (`title`, `ruleLabel`) — no existing column type/constraint changed, no data-loss risk to the 4 non-curated columns; migration runs only against the local dev Postgres container, never a shared/production instance |
| T-09-02 | Information Disclosure | `title`/`ruleLabel` new columns | accept | Both are curated-only, developer-authored, non-sensitive display strings (card titles/rule citations) — already public via the existing `gallery.list()` query's other fields (`rationale`, vessel positions) |
| T-09-03 | Repudiation | `prisma/seed.ts`'s new `deleteMany({isCurated:true})` | accept | Scoped exclusively to `isCurated: true` rows (the curated demo set); any non-curated (user-shared, `scenario.create`-originated) rows are structurally untouched, matching D-05's explicit "no production/user data exists to lose" acceptance |

No npm/pip/cargo package installs occur in this plan — the Package Legitimacy Gate is not triggered.
</threat_model>

<verification>
Run `npx vitest run src/server/db/curated-scenarios.test.ts src/domain/colregs/classify-encounter.test.ts` — both suites green. Run `npx tsc --noEmit` — no type errors. Confirm the local dev DB holds exactly 6 curated rows with the new fields populated (Task 3's acceptance criteria query).
</verification>

<success_criteria>
- `curatedScenarios` array has exactly 6 entries matching D-01's exact label/rule/give-way pairing, every vessel literal sourced from `classify-encounter.fixtures.ts`
- `Scenario` table has `title`/`ruleLabel` nullable columns, migrated and reseeded locally
- `curated-scenarios.test.ts` passes without assuming a mirror-pair entry that no longer exists
</success_criteria>

<output>
Create `.planning/phases/09-gallery/09-01-SUMMARY.md` when done
</output>
