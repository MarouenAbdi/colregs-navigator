---
phase: 14-pipeline-hooks
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified: []
autonomous: false
requirements: [CI-01, CI-05]

must_haves:
  truths:
    - "A real pull request against this repo triggers all four CI jobs (lint, typecheck, test, build) and their results are visible in the PR checks UI"
    - "The main branch requires the lint, typecheck, test, and build checks to pass before a PR can be merged, with no admin bypass"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Source of the four job names configured as required status checks"
      contains: "jobs:"
  key_links:
    - from: "GitHub branch protection rule on main"
      to: ".github/workflows/ci.yml job names"
      via: "required_status_checks.contexts matching exactly lint/typecheck/test/build"
      pattern: "lint|typecheck|test|build"
---

<objective>
Prove plan 14-01's CI workflow actually runs on a real pull request (not just "the YAML looks
right"), then make all four checks required via branch protection so a red run genuinely
blocks merging into `main` -- closing the loop on CI-01 and CI-05.

Purpose: "CI pipeline passes" and "CI pipeline actually gates merges" are different claims;
this project has direct precedent (Turbopack, tsgo/Program-API) for config that looks correct
but was never exercised end-to-end. A live `gh api` check at planning time surfaced a real,
concrete blocker for this repo specifically that must be resolved by the user, not assumed
away.
Output: a verified real CI run against a live PR; branch protection configured on `main`
(pending the user's decision below).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.github/workflows/ci.yml
</context>

<interfaces>
<!-- Live finding from this planning session, not training data -- verified via `gh api` -->
This repo (`MarouenAbdi/colregs-navigator`) is **private**, and the authenticated account is
on GitHub's Free plan (`gh api user` returns no `plan` field). Both
`gh api repos/MarouenAbdi/colregs-navigator/branches/main/protection` and
`gh api repos/MarouenAbdi/colregs-navigator/rulesets` returned HTTP 403 with the identical
message: `"Upgrade to GitHub Pro or make this repository public to enable this feature."`
This means **neither classic branch protection nor the newer Repository Rulesets feature can
be configured on this repo's `main` branch as it is currently set up** -- this is a real,
current constraint on this exact repo, not a hypothetical. CI-05 cannot be silently skipped
(it is a named phase requirement) and cannot be worked around with a different GitHub API --
it requires a human decision on billing or repo visibility.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Trigger and verify a real CI run against a live PR</name>
  <files>none (scratch branch + PR, no permanent repo files)</files>
  <read_first>.github/workflows/ci.yml</read_first>
  <action>
    Create a scratch branch off `main` (e.g. `ci-verify/14-02`), make a trivial no-op change
    (e.g. a one-line comment addition to a file already tracked, or touch a scratch file
    under a path already gitignored is not committable -- use a real tracked file with a
    genuinely harmless change, such as appending a blank line removed again, or editing this
    plan's own SUMMARY placeholder later), commit, push, and open a PR via `gh pr create`.
    Use `gh pr checks <PR-number> --watch` (or repeated `gh pr checks`) to confirm all four
    jobs (`lint`, `typecheck`, `test`, `build`) actually run and report a result -- capture
    whether each passes. If any job fails for a reason unrelated to this scratch change
    (e.g. a real bug in plan 14-01's workflow), fix `.github/workflows/ci.yml` and re-push
    until all four are green. Once verified, close the PR (do not merge a scratch change) and
    delete the scratch branch (`gh pr close`, `git push origin --delete ci-verify/14-02`).
  </action>
  <verify>
    <automated>gh pr checks &lt;PR-number&gt; --json name,state 2>/dev/null | grep -c '"state":"SUCCESS"'</automated>
  </verify>
  <acceptance_criteria>
    All four job names (`lint`, `typecheck`, `test`, `build`) appear in `gh pr checks` output
    for the scratch PR with state `SUCCESS`. The scratch branch and PR are cleaned up
    afterward (no lingering scratch PR left open).
  </acceptance_criteria>
  <done>A real PR against this repo was observed running all four named CI jobs to a green result.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Decision: how to satisfy CI-05 given this repo is private on GitHub's Free plan</name>
  <files>none (GitHub billing/visibility decision, not a repo file)</files>
  <action>Present the three options below to the user and wait for a selection before Task 2 proceeds -- Claude cannot resolve this via any API call.</action>
  <decision>How to satisfy CI-05 (required status checks on main) given this repo is private on GitHub's Free plan</decision>
  <context>
    Branch protection and Repository Rulesets both return HTTP 403 "Upgrade to GitHub Pro or
    make this repository public" for this exact repo, confirmed live via `gh api` during
    planning (not a hypothetical -- verified against the real `MarouenAbdi/colregs-navigator`
    repo). Claude cannot resolve this via any API call; it requires a billing or
    visibility decision only the account owner can make.

    Consequence of choosing "defer-and-document": if this option is selected, CI-05 is NOT
    satisfied and must NOT be marked validated/complete for this phase. Task 2 below records
    this explicitly in `.planning/STATE.md`'s "Blockers/Concerns" section, and this gap must
    be carried forward as an explicit open item on Phase 15's pre-go-live checklist -- it is
    not a silently-accepted risk, it is a tracked, unresolved requirement that must be
    revisited (via upgrade-to-pro or make-repo-public) before this milestone is considered
    fully shipped.
  </context>
  <options>
    <option id="upgrade-to-pro">
      <name>Upgrade the GitHub account to Pro ($4/mo)</name>
      <pros>Keeps the repo private; unlocks branch protection/rulesets immediately; no change to repo visibility for a portfolio project the user may want to keep gated</pros>
      <cons>Recurring cost for a portfolio project; some interviewers may actually prefer to see the repo publicly without needing an invite</cons>
    </option>
    <option id="make-repo-public">
      <name>Make the repository public</name>
      <pros>Free; unlocks branch protection/rulesets; also makes the README CI badge (plan 14-01) universally visible without requiring repo access -- arguably a better fit for "portfolio project aimed at software engineering interviews" (CLAUDE.md's own stated goal)</pros>
      <cons>Exposes full source/commit history publicly; any secrets accidentally committed historically would also become public (worth a quick `git log -p -- .env` sanity check first, though `.env` was never tracked per PITFALLS.md's own verification)</cons>
    </option>
    <option id="defer-and-document">
      <name>Defer branch protection; document the gap explicitly</name>
      <pros>No cost, no visibility change, unblocks the rest of this phase immediately</pros>
      <cons>CI-05 stays unsatisfied -- a red CI run would NOT actually block a merge to `main` on this repo until one of the other two options is chosen; must be tracked as an explicit open item, not silently dropped</cons>
    </option>
  </options>
  <resume-signal>Select: upgrade-to-pro, make-repo-public, or defer-and-document</resume-signal>
  <verify><human-check>User has explicitly selected one of the three options</human-check></verify>
  <done>A resolved decision (upgrade-to-pro / make-repo-public / defer-and-document) is recorded for Task 2 to act on. If "defer-and-document" was selected, CI-05 is explicitly NOT marked complete for this phase and is recorded as an open item for Phase 15's pre-go-live checklist.</done>
</task>

<task type="auto">
  <name>Task 2: Configure branch protection on main (only reachable after the decision above)</name>
  <files>none (GitHub repo setting, not a tracked file)</files>
  <read_first>.github/workflows/ci.yml (job names), the resolved decision from Task above</read_first>
  <action>
    If the resolved decision was `upgrade-to-pro` or `make-repo-public`: once the
    account/repo state actually changes (user completes the upgrade or flips visibility),
    run `gh api -X PUT repos/MarouenAbdi/colregs-navigator/branches/main/protection` with a
    JSON body setting `required_status_checks.strict: true`,
    `required_status_checks.contexts: ["lint","typecheck","test","build"]`,
    `enforce_admins: true` (per this plan's own threat model -- no silent admin bypass),
    `required_pull_request_reviews: null` (solo-maintained repo, no reviewer to require),
    `restrictions: null`. Re-verify with a GET to the same endpoint that all four contexts are
    present and `enforce_admins.enabled` is `true`.
    If the resolved decision was `defer-and-document`: instead, add a short note to
    `.planning/STATE.md`'s "Blockers/Concerns" section recording that CI-05 is not yet
    enforced pending a GitHub Pro upgrade or making the repo public, so it is not silently
    forgotten before Phase 15's go-live. This note must explicitly state that CI-05 is
    unresolved (not satisfied) for this phase and is carried forward as a required item on
    Phase 15's pre-go-live checklist.
  </action>
  <verify>
    <automated>gh api repos/MarouenAbdi/colregs-navigator/branches/main/protection --jq '.required_status_checks.contexts' 2>&1</automated>
  </verify>
  <acceptance_criteria>
    Either: `gh api .../branches/main/protection` returns `contexts` containing exactly
    `lint`, `typecheck`, `test`, `build` and `enforce_admins.enabled == true`; OR (if deferred)
    `.planning/STATE.md` explicitly records the open CI-05 gap under Blockers/Concerns, and
    that gap is stated as a required item for Phase 15's pre-go-live checklist.
  </acceptance_criteria>
  <done>Branch protection is configured with all four required checks and no admin bypass, or the gap is explicitly and visibly tracked (as an unresolved, not-complete CI-05) if deferred.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| GitHub repo settings -> merge gate on `main` | Controls whether a red CI run can actually block a merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Tampering | Branch protection admin bypass | mitigate | `enforce_admins: true` set explicitly so the repo owner's own admin access cannot silently skip a red required check |
| T-14-06 | Repudiation | No audit trail for who changed branch protection | accept | Solo-maintained repo; GitHub's own native audit log already covers this, no additional mitigation needed |
</threat_model>

<verification>
A real PR was observed running all four named CI jobs to completion. Branch protection is
either fully configured with all four contexts required and no admin bypass, or the gap is
explicitly recorded in STATE.md if deferred by user decision -- in which case CI-05 is
explicitly NOT considered satisfied for this phase and is carried forward as a required item
on Phase 15's pre-go-live checklist.
</verification>

<success_criteria>
CI-01 is proven true by a real, not simulated, PR run. CI-05 is either fully satisfied (branch
protection configured), or -- if deferred -- explicitly marked as unresolved/not-complete for
this phase and tracked as a required item on Phase 15's pre-go-live checklist, per the
already-presented and resolved user decision during this plan's execution.
</success_criteria>

<output>
Create `.planning/phases/14-pipeline-hooks/14-02-SUMMARY.md` when done
</output>
