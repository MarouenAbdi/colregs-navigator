---
phase: 14-pipeline-hooks
plan: 03
type: execute
wave: 2
depends_on: ["14-01"]
files_modified: [package.json, package-lock.json, .husky/pre-commit, lint-staged.config.js, CONTRIBUTING.md]
autonomous: false
requirements: [HOOKS-01, DOCS-CONTRIB-01]

must_haves:
  truths:
    - "Committing a staged file with a lint violation triggers Husky + lint-staged to auto-fix it (eslint --fix) before the commit completes"
    - "Staging a .env-variant file (other than .env.example) and attempting to commit is rejected before the commit is created"
    - "The full typecheck/test suite does NOT run in pre-commit -- only in CI"
    - "A fresh clone followed by npm ci registers the git hook (git config core.hooksPath resolves to .husky)"
    - "CONTRIBUTING.md documents setup, running checks locally, real pre-commit hook behavior, branch/commit conventions, PR expectations, and a rollback note"
  artifacts:
    - path: ".husky/pre-commit"
      provides: "Pre-commit hook: .env* guard + lint-staged"
      contains: "lint-staged"
    - path: "lint-staged.config.js"
      provides: "Staged-file eslint --fix configuration"
      contains: "eslint --fix"
    - path: "CONTRIBUTING.md"
      provides: "Contributor documentation"
      min_lines: 40
  key_links:
    - from: "package.json"
      to: ".husky/pre-commit"
      via: "prepare script (\"prepare\": \"husky\") registering hooks on npm install"
      pattern: "\"prepare\":\\s*\"husky\""
    - from: ".husky/pre-commit"
      to: "lint-staged.config.js"
      via: "npx lint-staged invocation inside the hook script"
      pattern: "lint-staged"
---

<objective>
Add Husky 9 + lint-staged pre-commit enforcement (staged-file ESLint `--fix` only, plus a
`.env*` commit guard) and author `CONTRIBUTING.md` documenting the real, verified hook
behavior -- not an aspirational one.

Purpose: HOOKS-01 and DOCS-CONTRIB-01 are independent of hosting/CD, so this plan proceeds in
parallel with plan 14-02; sequencing `CONTRIBUTING.md` after the hooks are actually built
(same plan, later task) guarantees the doc describes what was actually implemented.
Output: `.husky/pre-commit`, `lint-staged.config.js`, `package.json` (`prepare`,
`devDependencies`, `lint-staged` wiring), `CONTRIBUTING.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@package.json
@eslint.config.mjs
@.gitignore
@README.md
</context>

<interfaces>
<!-- Ground truth extracted at planning time -->
package.json is `"type": "module"` -- `lint-staged.config.js` must use ESM
(`export default { ... }`), matching the rest of the repo's module convention.

CLAUDE.md's "Git workflow" section (locked convention, quote verbatim in CONTRIBUTING.md):
"feature branches, small logical conventional commits; commit messages, branch names, and PR
descriptions are AI-generated but human-reviewed." Existing branch names in this repo's
history follow the pattern `gsd/phase-N-slug` (e.g. `gsd/phase-13-comment-cleanup`) and
`frontend-implementation/phase-N-slug` -- cite these as the real, observed convention rather
than inventing a new one.

No `Package Legitimacy Audit` table exists in any `.planning/research/*.md` file for this
milestone. Per the package-legitimacy gate, `husky` and `lint-staged` are therefore
`[ASSUMED]`, not pre-cleared -- Task 1 below is a blocking human-verify checkpoint before
either package is installed.

Registry check performed at planning time: `npm view husky version` -> `9.1.7`; `npm view
lint-staged version` -> `17.1.0` (newer patch than research/STACK.md's `17.0.8` snapshot --
install whatever the registry resolves to at execution time via an unpinned `npm install -D
husky lint-staged`, consistent with how this repo's other devDependencies use `^` ranges).
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Package legitimacy check: husky and lint-staged (no audit table found, treated as [ASSUMED])</name>
  <files>none (verification only, no repo file changes)</files>
  <action>Do not run `npm install` for husky/lint-staged until this checkpoint is explicitly approved -- no Package Legitimacy Audit table exists in RESEARCH.md for this milestone, so both packages are treated as [ASSUMED] per the package-legitimacy gate.</action>
  <what-built>Nothing yet -- this gate runs before any package is installed, per the package-legitimacy protocol (no audit table exists in RESEARCH.md for this milestone, so both packages are treated as [ASSUMED]).</what-built>
  <how-to-verify>
    Visit https://www.npmjs.com/package/husky and https://www.npmjs.com/package/lint-staged.
    Confirm: (1) `husky` is maintained by `typicode` (the well-known author, matches
    research), current version line 9.x, multi-million weekly downloads; (2) `lint-staged` is
    maintained by `okonet`/the `lint-staged` org, current version line 17.x, multi-million
    weekly downloads. Confirm neither name is a typosquat of a different, unrelated package
    (e.g. not `husky-git` or `lint-staged-cli`).
  </how-to-verify>
  <resume-signal>Type "approved" once both packages are confirmed legitimate on npmjs.com, or describe a concern</resume-signal>
  <verify><human-check>User has typed "approved" after checking both npmjs.com listings</human-check></verify>
  <done>Both husky and lint-staged are confirmed legitimate before Task 1 runs npm install.</done>
</task>

<task type="auto">
  <name>Task 1: Install and wire Husky + lint-staged (staged-file eslint --fix + .env* guard)</name>
  <files>package.json, package-lock.json, .husky/pre-commit, lint-staged.config.js</files>
  <read_first>package.json, eslint.config.mjs, .gitignore</read_first>
  <action>
    Run `npm install -D husky lint-staged` (unpinned, letting npm resolve current registry
    versions per the interfaces note above), then `npx husky init` (creates `.husky/pre-commit`
    and adds a `"prepare": "husky"` script to `package.json`).
    Create `lint-staged.config.js` (ESM default export) mapping the glob
    `"*.{js,jsx,mjs,cjs,ts,tsx}"` to `["eslint --fix"]` only -- deliberately no Prettier step
    (FMT-01 is an explicitly deferred v2 item per PROJECT.md) and deliberately no `tsc`/
    `typecheck` entry (TypeScript's checker needs the whole project graph; scoping it to
    staged files produces false negatives -- ARCHITECTURE.md Anti-Pattern 1 -- so the full
    typecheck stays CI-only, never pre-commit).
    Replace the auto-generated `.husky/pre-commit` content with: first, a `.env*` staged-file
    guard -- list staged file names (`git diff --cached --name-only`), match any that equal
    `.env` or match the pattern `.env.<anything>` while explicitly excluding
    `.env.example`, and if any match, print a clear rejection message naming the offending
    file(s) and exit with a non-zero status *before* lint-staged ever runs (this directly
    closes PITFALLS.md Pitfall 9); second, if no guarded files are staged, invoke
    `npx lint-staged`.
  </action>
  <verify>
    <automated>git config core.hooksPath</automated>
  </verify>
  <acceptance_criteria>
    `git config core.hooksPath` returns `.husky` (or `.husky/_` depending on the installed
    Husky version's exact convention -- confirm via the actual output, not an assumption).
    `.husky/pre-commit` is executable (`ls -l .husky/pre-commit` shows an `x` bit) and its
    contents contain both the `.env*` guard logic and an `npx lint-staged` invocation.
    Manually staging a file with an intentional, fixable lint violation (e.g. a double-quoted
    string where single is the project's convention, if applicable, or any auto-fixable rule
    already enforced by `eslint.config.mjs`) and running `git commit` results in the violation
    being auto-fixed and the commit succeeding. Manually staging a scratch `.env.local` file
    (with placeholder content, never a real credential) and attempting `git commit` is
    rejected with a non-zero exit and a message naming the file, before any commit object is
    created -- then remove the scratch file and unstage it.
  </acceptance_criteria>
  <done>Husky + lint-staged are installed and wired; the pre-commit hook auto-fixes staged lint violations and rejects staged .env* files (except .env.example).</done>
</task>

<task type="auto">
  <name>Task 2: Author CONTRIBUTING.md documenting real setup, hook behavior, conventions, and rollback</name>
  <files>CONTRIBUTING.md</files>
  <read_first>README.md, CLAUDE.md ("Git workflow" section), package.json (final scripts list), .husky/pre-commit (just created)</read_first>
  <action>
    Create `CONTRIBUTING.md` with these sections, each describing real, already-verified
    behavior (not aspirational):
    "Setup" -- mirror README's `cp .env.example .env` / `docker compose up -d` / `npm install`
    / `npx prisma migrate dev` / `npm test` sequence, noting `npm install` also now runs
    `postinstall: prisma generate` automatically (plan 14-01).
    "Running Checks Locally" -- list `npm run lint`, `npm run lint:fix`, `npm run typecheck`,
    `npm test`, `npm run build`, stating these are the exact same commands CI runs (plan
    14-01's four job names).
    "Pre-commit Hooks" -- state precisely what actually runs at commit time: lint-staged's
    `eslint --fix` against staged `*.{js,jsx,mjs,cjs,ts,tsx}` files only, plus the `.env*`
    guard -- and explicitly state that the full `typecheck`/`test` suite deliberately does
    NOT run pre-commit (CI-only), so a commit can still land locally with a type error that
    CI will catch on push/PR.
    "Branch & Commit Conventions" -- quote CLAUDE.md's locked convention (feature branches,
    small logical conventional commits, AI-generated-but-human-reviewed messages/branch
    names/PR descriptions); cite the observed real branch-name patterns
    (`gsd/phase-N-slug`, `frontend-implementation/phase-N-slug`) from this repo's actual git
    history.
    "Pull Request Expectations" -- a PR is expected to pass all four required CI checks
    (`lint`, `typecheck`, `test`, `build` -- plan 14-01/14-02); note the current real state of
    branch protection resulting from plan 14-02's resolved decision (do not assert branch
    protection is enforced unless plan 14-02 actually configured it -- read its SUMMARY if
    available, or phrase this section conditionally if executed out of order).
    "Rollback" -- state honestly that no live hosted deployment exists yet in this phase (that
    is Phase 15's scope); until then, "rollback" means `git revert` of a bad merge commit on
    `main`; note that once Phase 15 ships a live deployment, this section will be superseded
    by the host's one-click "promote a previous deployment" mechanism (HEALTH-04).
  </action>
  <verify>
    <automated>test -f CONTRIBUTING.md && grep -c "^#" CONTRIBUTING.md</automated>
  </verify>
  <acceptance_criteria>
    `CONTRIBUTING.md` exists with at least 6 headings (Setup, Running Checks Locally,
    Pre-commit Hooks, Branch &amp; Commit Conventions, Pull Request Expectations, Rollback).
    Contains the literal strings `eslint --fix`, `lint-staged`, and `git revert`. Does NOT
    claim the pre-commit hook runs `typecheck` or `test` (grep for `pre-commit` context should
    show only lint-related commands, never `npm run typecheck` or `npm test` as pre-commit
    behavior).
  </acceptance_criteria>
  <done>CONTRIBUTING.md exists, accurately reflects the hooks built in Task 1, and covers all six required topics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Developer's local `git commit` -> committed repo history | The last local checkpoint before a credential or secret becomes part of permanent history |
| npm registry -> local devDependency install | New third-party packages entering the toolchain for the first time this milestone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-07 | Information Disclosure | Accidental `.env`/`.env.local`/`.env.production` commit | mitigate | `.husky/pre-commit`'s `.env*` guard rejects any staged env-file variant except `.env.example` before a commit object is ever created |
| T-14-SC | Tampering (supply chain) | `husky`, `lint-staged` npm installs (no Package Legitimacy Audit table found) | mitigate | Blocking `checkpoint:human-verify` gate before install, verified via npmjs.com maintainer/download/typosquat check |
| T-14-08 | Tampering | Husky hooks silently not registering on a fresh clone (PITFALLS.md Pitfall 7) | mitigate | Acceptance criteria explicitly requires confirming `git config core.hooksPath` and the hook file's executable bit, not just "it worked when I set it up" |
</threat_model>

<verification>
`git config core.hooksPath` resolves correctly after install. A staged lint violation is
auto-fixed at commit time. A staged `.env*` file (non-example) is rejected before commit.
`CONTRIBUTING.md` exists with all six required sections and matches the actually-built hook
behavior.
</verification>

<success_criteria>
HOOKS-01: pre-commit runs eslint --fix on staged files only, full typecheck/test stays
CI-only. DOCS-CONTRIB-01: CONTRIBUTING.md documents real setup, checks, hook behavior,
conventions, PR expectations, and a rollback note.
</success_criteria>

<output>
Create `.planning/phases/14-pipeline-hooks/14-03-SUMMARY.md` when done
</output>
