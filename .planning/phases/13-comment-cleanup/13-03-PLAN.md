---
phase: 13-comment-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/server/application/scenario-service.ts
  - src/server/application/scenario-service.test.ts
  - src/server/api/routers/scenario.test.ts
  - src/server/db/scenario-repository.ts
  - src/server/db/scenario-repository.test.ts
  - src/server/db/curated-scenarios.ts
  - src/server/db/curated-scenarios.test.ts
  - src/server/api/trpc.ts
  - src/lib/trpc/banner.ts
autonomous: true
requirements: [CMNT-01]

must_haves:
  truths:
    - "None of the 9 server/tRPC-layer files contain a Phase N / Plan N / bare-doc-filename / 'Task N' comment reference after this plan lands"
  artifacts:
    - path: "src/server/application/scenario-service.ts"
      provides: "Rewritten comments, zero behavior change to getScenario/listGallery/createScenario"
  key_links: []
---

<objective>
Rewrite every stale Phase/Plan/REQ-ID/doc-filename comment reference in the server and tRPC layer (`src/server/`, plus `src/server/api/trpc.ts` and `src/lib/trpc/banner.ts`, folded in here because they are tRPC-adjacent and were missed by the original ESLint-rule-based scoping sweep -- confirmed via a targeted re-grep per CMNT-02) by hand, per CMNT-01. Preserve every comment's WHY content. No change to `classifyEncounter()` invocation timing, Prisma queries, or tRPC router behavior.

Purpose: retroactively enforce the comment convention across the persistence/API layer.
Output: Updated comments in the 9 files listed above; no other diff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite stale comments in scenario-service.ts, scenario-service.test.ts, scenario.test.ts, scenario-repository.ts, scenario-repository.test.ts</name>
  <files>src/server/application/scenario-service.ts, src/server/application/scenario-service.test.ts, src/server/api/routers/scenario.test.ts, src/server/db/scenario-repository.ts, src/server/db/scenario-repository.test.ts</files>
  <read_first>
    src/server/application/scenario-service.ts
    src/server/application/scenario-service.test.ts
    src/server/api/routers/scenario.test.ts
    src/server/db/scenario-repository.ts
    src/server/db/scenario-repository.test.ts
  </read_first>
  <action>
1. `src/server/application/scenario-service.ts` lines 1-17 (opening JSDoc: "the sole place `classifyEncounter()` is invoked in this phase (SCEN-02's core mechanism)... Open Question 1 (RESEARCH.md) resolution: validate at `createScenario` time..."): rewrite "in this phase (SCEN-02's core mechanism)" to "in this codebase (SCEN-02's core mechanism)"; rewrite "Open Question 1 (RESEARCH.md) resolution:" to "Design resolution:". Keep `SCEN-02` (live REQ-ID, verify against `.planning/REQUIREMENTS.md`) and `D-06` (decision ID, out of scope).
2. `src/server/application/scenario-service.ts` line 35 (`* (Open Question 3, RESEARCH.md) -- there is no other write path in this`): rewrite to "* -- there is no other write path in this". This line is a separate comment location from the block above (not covered by the same rewrite) -- verify it via `grep -n 'Open Question 3' src/server/application/scenario-service.ts` before and after.
3. `src/server/application/scenario-service.ts` line 86 (`// SCEN-02: always fresh -- no \`previous\` argument, there is no persisted`): keep `SCEN-02` (live REQ-ID); no stale content on this line beyond the REQ-ID itself, leave as-is.
4. `src/server/application/scenario-service.test.ts` lines 1-10 (opening JSDoc: "SCEN-02's core mechanism... (Open Question 1, resolved in this plan's Task 2)... Runs against the live Docker Postgres instance from Plan 01 (D-01)"): rewrite "(Open Question 1, resolved in this plan's Task 2)" to "(resolved by validating unclassifiable input at `createScenario` time, before persistence)"; rewrite "from Plan 01 (D-01)" to "(D-01)". Keep `SCEN-02` and `D-01`.
5. `src/server/application/scenario-service.test.ts` line 104 (`// 05-02-PLAN.md Task 2 side effect: prisma/seed.ts now populates this`): rewrite to "// prisma/seed.ts side effect: prisma/seed.ts now populates this".
6. `src/server/api/routers/scenario.test.ts` lines 1-7 (opening JSDoc: "This is the test that proves SCEN-02's re-derive-on-read guarantee holds across the FULL router -&gt; service -&gt; repository -&gt; domain chain, not just at the service-unit-test level already covered in Plan 02."): rewrite "already covered in Plan 02" to "already covered at the service-unit-test level". Keep `SCEN-02`.
7. `src/server/api/routers/scenario.test.ts` line 52 (`// 05-02-PLAN.md Task 3: this replaces the now-stale assumption that an`): rewrite to "// prisma/seed.ts follow-up: this replaces the now-stale assumption that an".
8. `src/server/db/scenario-repository.ts` (NOT in the original 54-file ESLint-flagged list -- confirmed via a broader re-grep per CMNT-02 that its header comment references "RESEARCH.md"): read the file's opening JSDoc/header comment and remove any bare `RESEARCH.md` reference, rephrasing to describe the underlying architectural reason directly (e.g. "no generic `Repository&lt;T&gt;` base class -- this project has exactly one repository, so a generic abstraction would add indirection without a second consumer to justify it").
9. `src/server/db/scenario-repository.test.ts` lines 1-5 (opening JSDoc: "Integration tests for scenario-repository.ts against the live Docker Postgres instance from Plan 01 (D-01)..."): rewrite "from Plan 01 (D-01)" to "(D-01)". Keep `D-01`.
  </action>
  <verify>
    <automated>npx vitest run src/server/application/scenario-service.test.ts src/server/db/scenario-repository.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'Plan 0[0-9]|this plan|Open Question|RESEARCH\.md|-PLAN\.md' src/server/application/scenario-service.ts src/server/application/scenario-service.test.ts src/server/api/routers/scenario.test.ts src/server/db/scenario-repository.ts src/server/db/scenario-repository.test.ts` returns no matches
    - `SCEN-02`, `D-01`, `D-06` citations remain present verbatim where noted above (confirm via `grep -n 'SCEN-02' src/server/application/scenario-service.ts`)
    - `npx vitest run src/server/application/scenario-service.test.ts src/server/db/scenario-repository.test.ts` passes with the same pass count as before this task (these tests hit a live Docker Postgres instance -- confirm the dev DB is up via `docker compose ps` first if the run fails to connect)
  </acceptance_criteria>
  <done>All 5 files' listed comment locations rewritten; targeted greps clean; both integration test files pass with unchanged pass counts.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite stale comments in curated-scenarios.ts, curated-scenarios.test.ts, trpc.ts, banner.ts</name>
  <files>src/server/db/curated-scenarios.ts, src/server/db/curated-scenarios.test.ts, src/server/api/trpc.ts, src/lib/trpc/banner.ts</files>
  <read_first>
    src/server/db/curated-scenarios.ts
    src/server/db/curated-scenarios.test.ts
    src/server/api/trpc.ts
    src/lib/trpc/banner.ts
  </read_first>
  <action>
1. `src/server/db/curated-scenarios.ts` lines 1-18 (opening JSDoc: "Curated gallery seed data (GAL-01, D-01 through D-05)... Per 05-RESEARCH.md's Fixture Catalog (Pitfall 3), only fixtures with no `previous` field are safe to reuse here... the same 6 labels Phase 8's Sandbox chip row uses."): rewrite "Per 05-RESEARCH.md's Fixture Catalog (Pitfall 3)" to "Per this project's fixture-catalog design constraint"; rewrite "Phase 8's Sandbox chip row" to "the Sandbox chip row". Keep `GAL-01` (live REQ-ID, verify) and `D-01` through `D-05` (decision IDs, out of scope).
2. `src/server/db/curated-scenarios.ts` line 80 (`// matches Rule 18's outcome -- see Task 2's research-gap note in`): rewrite to "// matches Rule 18's outcome -- see the research-gap note above". Line 81 (`// 09-01-PLAN.md for why \`ruleLabel\` is a static field, not derived.`): fold into the same rewrite as line 80 so the sentence still reads naturally, e.g. "// matches Rule 18's outcome -- see the research-gap note above for why `ruleLabel` is a static field, not derived."
3. `src/server/db/curated-scenarios.test.ts` lines 1-11 (opening JSDoc: "Coverage/validation tests for curated-scenarios.ts (GAL-01, D-01 through D-05)."): keep `GAL-01` and `D-01`-`D-05` (no other stale content in this block); leave unchanged.
4. `src/server/db/curated-scenarios.test.ts` line 19 (`it("Test 1: length is between 5 and 8 inclusive (SCEN-03's range)", () => {`): this is a test-name string literal, not a comment -- rewrite to `it("length is between 5 and 8 inclusive", () => {` (drop both the "Test 1:" numbering and the `SCEN-03` REQ-ID from the test name; the REQ-ID traceability lives in REQUIREMENTS.md, not in the test-runner-visible string).
5. `src/server/api/trpc.ts` (NOT in the original 54-file ESLint-flagged list -- confirmed via a broader re-grep per CMNT-02 that its header comment references "RESEARCH.md"): locate the comment mentioning "from responses when `NODE_ENV === "production"`, per RESEARCH.md's" and rewrite to describe the actual reason directly (e.g. "per this project's own decision to strip stack traces from responses in production while keeping them in development for debuggability").
6. `src/lib/trpc/banner.ts` lines 1-14 (opening JSDoc: "shown by SandboxContainer (05-03's `banner` prop) from a fetched ScenarioRow's..."): rewrite "(05-03's `banner` prop)" to "(the `banner` prop)". Keep `D-02` (decision ID, out of scope).
  </action>
  <verify>
    <automated>npx vitest run src/server/db/curated-scenarios.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'Phase [0-9]|RESEARCH\.md|-PLAN\.md|Task [0-9]|Test 1:|05-03' src/server/db/curated-scenarios.ts src/server/db/curated-scenarios.test.ts src/server/api/trpc.ts src/lib/trpc/banner.ts` returns no matches
    - `GAL-01`, `D-01` through `D-05`, `D-02` citations remain present verbatim where noted above
    - `npx vitest run src/server/db/curated-scenarios.test.ts` passes with the same pass count as before this task
    - `npm run typecheck` passes (confirms the `it(...)` string-literal rename in curated-scenarios.test.ts introduced no syntax error)
  </acceptance_criteria>
  <done>All 4 files' listed comment/test-name locations rewritten; targeted greps clean; curated-scenarios.test.ts passes with unchanged pass count; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | Comment-only edits (plus one test-name string rename) in the server/tRPC layer. No new input crosses any boundary, no dependency added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05 | Tampering | Accidental logic edit to scenario-service.ts's createScenario validation while touching adjacent comments | mitigate | Task 1's acceptance criteria require the integration test suites to pass with the *same* pass count against the live dev Postgres instance. |
| T-13-06 | N/A | npm/pip/cargo installs | accept | No package installs in this phase; not applicable. |
</threat_model>

<verification>
Run `npx vitest run src/server src/lib/trpc` and confirm the full pass count matches the pre-phase baseline (Docker Postgres dev instance must be running for the integration tests). Run `git diff -- src/server src/lib/trpc` and confirm every changed hunk only touches comment/test-name-string content.
</verification>

<success_criteria>
All 9 files' listed stale comment/test-name locations are rewritten with the rotting reference removed and substantive WHY content fully retained; zero test or behavior change.
</success_criteria>

<output>
Create `.planning/phases/13-comment-cleanup/13-03-SUMMARY.md` when done
</output>
