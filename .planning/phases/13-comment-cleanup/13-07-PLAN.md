---
phase: 13-comment-cleanup
plan: 07
type: execute
wave: 2
depends_on: ["13-06"]
files_modified:
  - src/components/hero/Hero.test.tsx
  - src/components/hero/hero-preview-fixture.test.ts
  - src/components/gallery/GalleryCard.test.tsx
  - src/components/gallery/GalleryContainer.test.tsx
  - src/components/gallery/GalleryPreviewChart.test.tsx
  - src/domain/colregs/classify-encounter.test.ts
  - src/domain/colregs/vessel-priority.test.ts
autonomous: true
requirements: [CMNT-01, CMNT-02]

must_haves:
  truths:
    - "No describe()/it() test-name string literal in src/ still embeds a Phase/Plan/REQ-ID-shaped tag (e.g. '(HERO-01, HERO-03)', '(09-03)', 'WR-01 regression:', 'CR-01 regression:') after this plan lands"
    - "Every rewritten test name still describes the same behavior being tested, just without the rotting tag"
  artifacts: []
  key_links: []
---

<objective>
Rewrite every `describe()`/`it()` test-name string literal across the test suite that embeds a Phase/Plan/REQ-ID-shaped tag, per CMNT-02's broader re-grep. These are NOT comments -- they are string-literal arguments to Vitest's `describe`/`it` calls -- so the strict `local/no-stale-id-comments` ESLint rule (which only scans `sourceCode.getAllComments()`) never flagged them. A dedicated broader grep (`grep -rn -E '(describe|it|test)\(\s*"[^"]*\b([A-Z]{2,6}-[0-9]{2,3}|[0-9]{2}(\.[0-9])?-[0-9]{2})\b[^"]*"' src/ app/`) surfaced them; this plan closes that gap.

Test behavior, assertions, and fixture data are completely unchanged -- only the human-readable string passed to `describe()`/`it()` is edited.

Purpose: satisfy CMNT-02's requirement that the milestone's original strict-pattern scoping count (109 occurrences / 54 files, captured via ESLint's own comment-only scan) is not treated as exhaustive; a second, broader sweep against test-name strings closes the gap.
Output: Rewritten `describe()`/`it()` string literals in the 7 files listed above; no other diff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite describe()/it() strings in Hero.test.tsx, hero-preview-fixture.test.ts, GalleryCard.test.tsx, GalleryContainer.test.tsx, GalleryPreviewChart.test.tsx</name>
  <files>src/components/hero/Hero.test.tsx, src/components/hero/hero-preview-fixture.test.ts, src/components/gallery/GalleryCard.test.tsx, src/components/gallery/GalleryContainer.test.tsx, src/components/gallery/GalleryPreviewChart.test.tsx</files>
  <read_first>
    src/components/hero/Hero.test.tsx
    src/components/hero/hero-preview-fixture.test.ts
    src/components/gallery/GalleryCard.test.tsx
    src/components/gallery/GalleryContainer.test.tsx
    src/components/gallery/GalleryPreviewChart.test.tsx
  </read_first>
  <action>
1. `src/components/hero/Hero.test.tsx` line 14: `describe("Hero (HERO-01, HERO-03)", () => {` -&gt; rewrite to `describe("Hero", () => {`. The `HERO-01`/`HERO-03` REQ-ID traceability already lives in `.planning/REQUIREMENTS.md`; dropping it from the runner-visible string loses no WHY content since the individual `it()` names inside already describe the actual behavior under test.
2. `src/components/hero/hero-preview-fixture.test.ts` line 15: `describe("hero-preview-fixture (HERO-02 drift guard)", () => {` -&gt; rewrite to `describe("hero-preview-fixture drift guard", () => {` (drop the bare `HERO-02` tag, keep "drift guard" since it describes what the suite actually verifies).
3. `src/components/gallery/GalleryCard.test.tsx` line 24: `describe("GalleryCard (09-03)", () => {` -&gt; rewrite to `describe("GalleryCard", () => {`.
4. `src/components/gallery/GalleryContainer.test.tsx` line 16: `describe("GalleryContainer (09-03)", () => {` -&gt; rewrite to `describe("GalleryContainer", () => {`.
5. `src/components/gallery/GalleryPreviewChart.test.tsx` line 18: `describe("GalleryPreviewChart (09-02)", () => {` -&gt; rewrite to `describe("GalleryPreviewChart", () => {`.
6. `src/components/gallery/GalleryPreviewChart.test.tsx` line 61: `it("keeps both vessels' dashed heading vectors inside the chart's viewBox on the real 'Overtaking' card geometry (WR-01 regression)", () => {` -&gt; rewrite to `it("keeps both vessels' dashed heading vectors inside the chart's viewBox on the real 'Overtaking' card geometry", () => {` (drop "(WR-01 regression)"; the rest of the name already fully describes the regression scenario being guarded).
  </action>
  <verify>
    <automated>npx vitest run src/components/hero/Hero.test.tsx src/components/hero/hero-preview-fixture.test.ts src/components/gallery/GalleryCard.test.tsx src/components/gallery/GalleryContainer.test.tsx src/components/gallery/GalleryPreviewChart.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '(describe|it|test)\(\s*"[^"]*\b(HERO-0[0-9]|[0-9]{2}-[0-9]{2}|WR-0[0-9])\b' src/components/hero/Hero.test.tsx src/components/hero/hero-preview-fixture.test.ts src/components/gallery/GalleryCard.test.tsx src/components/gallery/GalleryContainer.test.tsx src/components/gallery/GalleryPreviewChart.test.tsx` returns no matches
    - `npx vitest run src/components/hero/Hero.test.tsx src/components/hero/hero-preview-fixture.test.ts src/components/gallery/GalleryCard.test.tsx src/components/gallery/GalleryContainer.test.tsx src/components/gallery/GalleryPreviewChart.test.tsx` passes with the same pass count as before this task
    - `npm run typecheck` passes
  </acceptance_criteria>
  <done>All 6 listed describe()/it() strings rewritten; targeted grep clean; all 5 test files pass with unchanged pass counts; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite describe()/it() strings in classify-encounter.test.ts, vessel-priority.test.ts</name>
  <files>src/domain/colregs/classify-encounter.test.ts, src/domain/colregs/vessel-priority.test.ts</files>
  <read_first>
    src/domain/colregs/classify-encounter.test.ts
    src/domain/colregs/vessel-priority.test.ts
  </read_first>
  <action>
1. `src/domain/colregs/classify-encounter.test.ts` line 79: `it("WR-02 regression: both vessels simultaneously satisfy the abaft-the-beam test (diverging vessels) -- bOvertakesA precedence wins and Rule 7 reports no risk", () => {` -&gt; rewrite to `it("both vessels simultaneously satisfy the abaft-the-beam test (diverging vessels) -- bOvertakesA precedence wins and Rule 7 reports no risk", () => {` (drop "WR-02 regression:").
2. `src/domain/colregs/classify-encounter.test.ts` line 116: `it("WR-01 regression: raises doubt in the sticky path when the current bearing has drifted close to the 112.5 deg boundary", () => {` -&gt; rewrite to `it("raises doubt in the sticky path when the current bearing has drifted close to the 112.5 deg boundary", () => {`.
3. `src/domain/colregs/classify-encounter.test.ts` line 196: `it("WR-03 regression: rbAtoB exactly 0 with a non-reciprocal rbBtoA survives Stage 4's head-on exclusion and pins the Stage 5 tie-break", () => {` -&gt; rewrite to `it("rbAtoB exactly 0 with a non-reciprocal rbBtoA survives Stage 4's head-on exclusion and pins the Stage 5 tie-break", () => {`.
4. `src/domain/colregs/classify-encounter.test.ts` line 269: `describe("classifyEncounter() Rule 18 override (DETM-02)", () => {` -&gt; rewrite to `describe("classifyEncounter() Rule 18 override", () => {`.
5. `src/domain/colregs/classify-encounter.test.ts` line 319: `it("CR-01 regression: does NOT apply Rule 18 to an overtaking encounter -- the overtaking vessel keeps give-way even though it outranks the vessel being overtaken", () => {` -&gt; rewrite to `it("does NOT apply Rule 18 to an overtaking encounter -- the overtaking vessel keeps give-way even though it outranks the vessel being overtaken", () => {` (drop "CR-01 regression:").
6. `src/domain/colregs/vessel-priority.test.ts` line 19: `describe("rule18Overrides() tie-break mechanics (DETM-02)", () => {` -&gt; rewrite to `describe("rule18Overrides() tie-break mechanics", () => {`.
  </action>
  <verify>
    <automated>npx vitest run src/domain/colregs/classify-encounter.test.ts src/domain/colregs/vessel-priority.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '(describe|it|test)\(\s*"[^"]*\b(WR-0[0-9]|CR-0[0-9]|DETM-0[0-9])\b' src/domain/colregs/classify-encounter.test.ts src/domain/colregs/vessel-priority.test.ts` returns no matches
    - `npx vitest run src/domain/colregs/classify-encounter.test.ts src/domain/colregs/vessel-priority.test.ts` passes with the same pass count as before this task (this is the exact regression suite for the geometric edge cases these names document -- a pass-count match confirms no test was accidentally dropped or renamed out of its `describe` block)
    - `npm run typecheck` passes
  </acceptance_criteria>
  <done>All 6 listed describe()/it() strings rewritten; targeted grep clean; both test files pass with unchanged pass counts; typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | Test-name string-literal edits only. No assertions, fixtures, or logic touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-13 | Tampering | Accidental removal or misplacement of a whole `it()`/`describe()` block while editing its name string | mitigate | Every task's acceptance criteria require the exact pre-existing pass count to be reproduced after the edit -- a dropped or misplaced test immediately changes the count. |
| T-13-14 | N/A | npm/pip/cargo installs | accept | No package installs in this phase; not applicable. |
</threat_model>

<verification>
Run `npx vitest run src/components/hero src/components/gallery src/domain/colregs/classify-encounter.test.ts src/domain/colregs/vessel-priority.test.ts` and confirm the combined pass count matches the pre-phase baseline. Run `git diff -- src/components/hero/Hero.test.tsx src/components/hero/hero-preview-fixture.test.ts src/components/gallery/GalleryCard.test.tsx src/components/gallery/GalleryContainer.test.tsx src/components/gallery/GalleryPreviewChart.test.tsx src/domain/colregs/classify-encounter.test.ts src/domain/colregs/vessel-priority.test.ts` and confirm every changed hunk only touches a `describe(...)`/`it(...)` string-literal argument.
</verification>

<success_criteria>
All 12 listed describe()/it() test-name strings are rewritten with the rotting tag removed and the descriptive behavior text fully retained; zero test-count or assertion change.
</success_criteria>

<output>
Create `.planning/phases/13-comment-cleanup/13-07-SUMMARY.md` when done
</output>
