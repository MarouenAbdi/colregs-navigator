---
phase: 13-comment-cleanup
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sandbox/SandboxContainer.test.tsx
  - src/components/sandbox/hooks/useSandboxState.ts
  - src/components/sandbox/types.ts
  - src/components/sandbox/vessel-role.ts
  - src/components/sandbox/environment.smoke.test.tsx
  - src/components/sandbox/hooks/useHullDrag.ts
  - src/components/sandbox/hooks/useHullDrag.test.ts
  - src/components/sandbox/hooks/useRotateHandleDrag.ts
  - src/components/sandbox/hooks/useRotateHandleDrag.test.ts
autonomous: true
requirements: [CMNT-01]

must_haves:
  truths:
    - "None of the 9 files in this Sandbox-cluster-B set contain a Phase N / Plan N / Task N / bare-doc-filename comment reference after this plan lands"
    - "The one genuine Tailwind-color-shade false positive in vessel-role.ts is left untouched, not misidentified as a stale ID"
  artifacts:
    - path: "src/components/sandbox/hooks/useSandboxState.ts"
      provides: "Rewritten comments, zero change to applyVesselUpdate/Rule 13(d) hysteresis behavior"
  key_links: []
---

<objective>
Rewrite every stale Phase/Plan/REQ-ID/Task-N comment reference in `useSandboxState.ts`, `SandboxContainer.test.tsx`, sandbox `types.ts`, `vessel-role.ts`, `environment.smoke.test.tsx`, and the hull/rotate drag hooks (source + test) by hand, per CMNT-01. Preserve every comment's WHY content -- especially the Rule 13(d) hysteresis and Pitfall-5 degenerate-frame-handling rationale in `useSandboxState.ts`, which is the most behaviorally load-bearing comment set in this cluster. No change to drag-gesture math, hysteresis semantics, or state-machine behavior.

This plan folds in `useRotateHandleDrag.test.ts`, which was not in the original 54-file ESLint-flagged list (its `(04-03 Task 3)` header comment doesn't match the strict regex shape) -- confirmed via a broader re-grep per CMNT-02.

Purpose: retroactively enforce the comment convention across SandboxContainer's state machine and drag-gesture hooks.
Output: Updated comments in the 9 files listed above; no other diff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite stale comments in SandboxContainer.test.tsx, useSandboxState.ts, types.ts, vessel-role.ts, environment.smoke.test.tsx</name>
  <files>src/components/sandbox/SandboxContainer.test.tsx, src/components/sandbox/hooks/useSandboxState.ts, src/components/sandbox/types.ts, src/components/sandbox/vessel-role.ts, src/components/sandbox/environment.smoke.test.tsx</files>
  <read_first>
    src/components/sandbox/SandboxContainer.test.tsx
    src/components/sandbox/hooks/useSandboxState.ts
    src/components/sandbox/types.ts
    src/components/sandbox/vessel-role.ts
    src/components/sandbox/environment.smoke.test.tsx
  </read_first>
  <action>
1. `src/components/sandbox/SandboxContainer.test.tsx` lines 2-14 (opening JSDoc: "Exercises default-scenario mount (D-06), live update via ControlPanel (CLAS-05), the degenerate coincident-position state (Pitfall 5), Rule 13(d) hysteresis... and the 6-chip preset row (D-01/D-02)."): keep `CLAS-05` (live REQ-ID, verify), `D-06`, `D-01`, `D-02` (decision IDs, out of scope), and "Pitfall 5" (a domain-behavior label with full inline explanation elsewhere, out of this phase's literal Phase/Plan/REQ-ID scope); no other stale content in this specific block, leave unchanged.
2. `src/components/sandbox/SandboxContainer.test.tsx` line 27 (`// 05-03 Task 2: mock next/navigation's useRouter and the trpc client's`): rewrite to "// Mock next/navigation's useRouter and the trpc client's".
3. `src/components/sandbox/SandboxContainer.test.tsx` line 232 (`// 05-03 Task 1: seed-from-saved-scenario (SCEN-01, D-02, Assumption A3)`): rewrite to "// Seed-from-saved-scenario (SCEN-01, D-02, Assumption A3)". Keep `SCEN-01` (live REQ-ID, verify) and `D-02`.
4. `src/components/sandbox/SandboxContainer.test.tsx` line 277 (`it("does not show any chip as active when a saved/shared scenario is loaded, even though it defaults to Classic crossing on the plain seedless route (code review CR-02)", () => {`): this is a test-name string literal -- rewrite to `it("does not show any chip as active when a saved/shared scenario is loaded, even though it defaults to Classic crossing on the plain seedless route", () => {` (drop "(code review CR-02)"; the reasoning is already fully explained in the comment immediately below this line, so nothing is lost).
5. `src/components/sandbox/SandboxContainer.test.tsx` line 323 (`// 05-03 Task 2: Save button wired to scenario.create + redirect (SCEN-01)`): rewrite to "// Save button wired to scenario.create + redirect (SCEN-01)". Keep `SCEN-01`.
6. `src/components/sandbox/hooks/useSandboxState.ts` lines 47-49 (`// 05-03 Task 2: Save persists the current vesselA/vesselB via` / `// scenario.create (no login step, SCEN-01) and redirects to the` / `// resulting share URL on success.`): rewrite to "// Save persists the current vesselA/vesselB via scenario.create (no login step, SCEN-01) and redirects to the resulting share URL on success." Keep `SCEN-01`.
7. `src/components/sandbox/hooks/useSandboxState.ts` line 59 (`// 05-03: when \`initialScenario\` is provided (saved/shared scenario), seed`): rewrite to "// When `initialScenario` is provided (saved/shared scenario), seed".
8. `src/components/sandbox/hooks/useSandboxState.ts` line 89 (`// scenario -- purely a client-side visual highlight (T-08-07), cleared by`): rewrite to "// scenario -- purely a client-side visual highlight, cleared by" (drop the `T-08-07` Threat-Model-entry pointer; the sentence already states the WHY without it).
9. `src/components/sandbox/hooks/useSandboxState.ts` line 109 (`// vessels with VesselSchema before classifyEncounter() ever sees them` / next line `// (T-04-01), then re-derives the verdict from scratch every time --`): rewrite to remove `(T-04-01)`, e.g. "// vessels with VesselSchema before classifyEncounter() ever sees them, then re-derives the verdict from scratch every time --".
10. `src/components/sandbox/types.ts` lines 1-7 (opening JSDoc: "Shared prop contracts for Phase 4's sandbox UI (04-01). Every later component in this phase -- ChartPanel, ControlPanel, ReasoningPanel, SandboxContainer (04-02 through 04-06) -- imports these types..."): rewrite to "Shared prop contracts for the Sandbox UI. Every component -- ChartPanel, ControlPanel, ReasoningPanel, SandboxContainer -- imports these types rather than inventing its own shape, so the components can be built in parallel against one fixed interface."
11. `src/components/sandbox/types.ts` line 66 (`// default demo fixture (05-03 Task 1, Assumption A3).`): rewrite to "// default demo fixture (Assumption A3)."
12. `src/components/sandbox/vessel-role.ts` lines 1-6 (opening JSDoc: "Shared give-way/stand-on/mutual role derivation (04-01). Reused by ChartPanel (hull color coding, D-02) and ReasoningPanel (role badges)..."): rewrite "(04-01)" out -- e.g. "Shared give-way/stand-on/mutual role derivation. Reused by ChartPanel (hull color coding, D-02) and ReasoningPanel (role badges) so both components agree on the same role for a given vessel/classification pair rather than each re-deriving it independently." Keep `D-02`.
13. `src/components/sandbox/vessel-role.ts` line 26: **DO NOT EDIT.** This line contains Tailwind color-shade names (`red-500`, `green-500`, `slate-400`) that happen to match the ESLint rule's pattern shape case-insensitively -- not a stale ID reference.
14. `src/components/sandbox/vessel-role.ts` line 48 (`// 10%/35% opacity convention established by Phase 7's rule-banner`): rewrite "established by Phase 7's rule-banner precedent" to "established by the Hero rule-banner precedent".
15. `src/components/sandbox/vessel-role.ts` line 50 (`// reinvented, per 08-UI-SPEC.md's Color section measurement of the design`): rewrite "per 08-UI-SPEC.md's Color section measurement of the design mock's own badge tints" to "matching the design mock's own badge-tint measurements".
16. `src/components/sandbox/environment.smoke.test.tsx` lines 2-7 (opening JSDoc: "Pitfall-4 (vitest-dev/vitest#9279) early-warning smoke test (04-01): an open, unresolved GitHub issue..."): rewrite "(04-01)" out -- e.g. "Pitfall-4 (vitest-dev/vitest#9279) early-warning smoke test: an open, unresolved GitHub issue reports jsdom-latest + Vitest-4-latest causing obscure test failures. This trivial render+assert must pass before any real component test is built on top of the RTL/jsdom harness." Keep the GitHub issue link (`vitest-dev/vitest#9279`), which is a stable external reference, not a phase/plan pointer.
  </action>
  <verify>
    <automated>npx vitest run src/components/sandbox/SandboxContainer.test.tsx src/components/sandbox/environment.smoke.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '05-03|Task [0-9]|Phase [0-9]|T-08-07|T-04-01|UI-SPEC\.md' src/components/sandbox/SandboxContainer.test.tsx src/components/sandbox/hooks/useSandboxState.ts src/components/sandbox/types.ts src/components/sandbox/vessel-role.ts src/components/sandbox/environment.smoke.test.tsx` returns no matches
    - `grep -n 'red-500\|green-500\|slate-400' src/components/sandbox/vessel-role.ts` still returns the same match as before this task (confirms the color-name false positive was left untouched)
    - `npx vitest run src/components/sandbox/SandboxContainer.test.tsx src/components/sandbox/environment.smoke.test.tsx` passes with the same pass count as before this task
    - `npm run typecheck` passes (confirms the `it(...)` string-literal rename on line 277 introduced no syntax error)
  </acceptance_criteria>
  <done>All 5 files' listed comment/test-name locations rewritten (except the deliberately-untouched color-name line); targeted greps clean; both test files pass with unchanged pass counts; typecheck clean.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite stale comments in useHullDrag.ts/.test.ts, useRotateHandleDrag.ts/.test.ts</name>
  <files>src/components/sandbox/hooks/useHullDrag.ts, src/components/sandbox/hooks/useHullDrag.test.ts, src/components/sandbox/hooks/useRotateHandleDrag.ts, src/components/sandbox/hooks/useRotateHandleDrag.test.ts</files>
  <read_first>
    src/components/sandbox/hooks/useHullDrag.ts
    src/components/sandbox/hooks/useHullDrag.test.ts
    src/components/sandbox/hooks/useRotateHandleDrag.ts
    src/components/sandbox/hooks/useRotateHandleDrag.test.ts
  </read_first>
  <action>
1. `src/components/sandbox/hooks/useHullDrag.ts` lines 1-9 (opening JSDoc: "position-drag hook (VESL-02/D-01)... this hook holds no state of its own and does not persist/validate the forwarded value itself (04-03-PLAN.md's threat model T-04-09: the actual VesselSchema validation boundary is SandboxContainer.applyVesselUpdate, 04-06)."): rewrite "(04-03-PLAN.md's threat model T-04-09: the actual VesselSchema validation boundary is SandboxContainer.applyVesselUpdate, 04-06)" to "(the actual VesselSchema validation boundary is `SandboxContainer`'s `applyVesselUpdate`, not this hook)". Keep `VESL-02` (live REQ-ID, verify) and `D-01` (decision ID, out of scope).
2. `src/components/sandbox/hooks/useHullDrag.test.ts` lines 2-12 (opening JSDoc: "chart-space Position... Written as a plain `.ts` file per this plan's own file list -- no JSX syntax is used..."): rewrite "per this plan's own file list" to "deliberately -- no JSX syntax is used (React.createElement instead), matching this hook's own plain-function shape".
3. `src/components/sandbox/hooks/useRotateHandleDrag.ts` lines 1-7 (opening JSDoc: "heading-drag hook (VESL-02/D-01). Computes the new heading via bearing()... never a second hand-rolled atan2 call (04-RESEARCH.md Anti-Patterns)."): rewrite "(04-RESEARCH.md Anti-Patterns)" to "-- duplicating this project's own atan2-argument-order convention in a second place would risk the two implementations drifting apart". Keep `VESL-02`/`D-01`.
4. `src/components/sandbox/hooks/useRotateHandleDrag.test.ts` lines 2-9 (opening JSDoc: "useRotateHandleDrag test (04-03 Task 3): drives a real pointerdown+pointermove sequence...") -- NOT in the original ESLint-flagged list, found via broader re-grep per CMNT-02: rewrite "(04-03 Task 3)" out entirely -- e.g. "useRotateHandleDrag test: drives a real pointerdown+pointermove sequence against ChartPanel's rendered rotate-handle hit-circle, asserts onVesselHeadingChange fires with a heading, and regression-tests Pitfall 1 -- the hull hit-rect's own position-change callback must NOT also fire for the same gesture (proves `stopPropagation()` works)."
  </action>
  <verify>
    <automated>npx vitest run src/components/sandbox/hooks/useHullDrag.test.ts src/components/sandbox/hooks/useRotateHandleDrag.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'VESL-02/D-01\)\.|04-03|Task [0-9]|RESEARCH\.md|-PLAN\.md|this plan' src/components/sandbox/hooks/useHullDrag.ts src/components/sandbox/hooks/useHullDrag.test.ts src/components/sandbox/hooks/useRotateHandleDrag.ts src/components/sandbox/hooks/useRotateHandleDrag.test.ts` returns no matches (the `VESL-02/D-01\)\.` sub-pattern only checks that no OLD full parenthetical survives; the bare `VESL-02` REQ-ID and `D-01` decision-ID citations elsewhere are expected to remain)
    - `npx vitest run src/components/sandbox/hooks/useHullDrag.test.ts src/components/sandbox/hooks/useRotateHandleDrag.test.ts` passes with the same pass count as before this task
  </acceptance_criteria>
  <done>All 4 files' listed comment locations rewritten; targeted greps clean; both test files pass with unchanged pass counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | Comment-only edits (plus one test-name string rename) to SandboxContainer's state hook and drag-gesture hooks. No new input crosses any boundary, no dependency added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-09 | Tampering | Accidental logic edit to useSandboxState.ts's applyVesselUpdate/Rule 13(d) hysteresis while touching adjacent comments | mitigate | Task 1's acceptance criteria require SandboxContainer.test.tsx (which directly exercises hysteresis behavior) to pass with the *same* pass count. |
| T-13-10 | N/A | npm/pip/cargo installs | accept | No package installs in this phase; not applicable. |
</threat_model>

<verification>
Run `npx vitest run src/components/sandbox/SandboxContainer.test.tsx src/components/sandbox/environment.smoke.test.tsx src/components/sandbox/hooks` and confirm the combined pass count matches the pre-phase baseline. Run `git diff -- src/components/sandbox/SandboxContainer.test.tsx src/components/sandbox/hooks src/components/sandbox/types.ts src/components/sandbox/vessel-role.ts src/components/sandbox/environment.smoke.test.tsx` and confirm every changed hunk only touches comment/test-name-string content.
</verification>

<success_criteria>
All 9 files' listed stale comment/test-name locations are rewritten with the rotting reference removed and substantive WHY content (including Rule 13(d) hysteresis and Pitfall-5 degenerate-frame rationale) fully retained; the color-name false positive in vessel-role.ts is left untouched; zero test or behavior change.
</success_criteria>

<output>
Create `.planning/phases/13-comment-cleanup/13-05-SUMMARY.md` when done
</output>
