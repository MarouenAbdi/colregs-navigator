---
phase: 13-comment-cleanup
plan: 08
type: execute
wave: 3
depends_on: ["13-01", "13-02", "13-03", "13-04", "13-05", "13-06", "13-07"]
files_modified:
  - eslint-suppressions.json
autonomous: false
requirements: [CMNT-01, CMNT-02]

must_haves:
  truths:
    - "A strict grep (the exact local/no-stale-id-comments regex) across src/ and app/ returns zero genuine stale Phase/Plan/REQ-ID matches, excluding the 4 documented Tailwind-color-shade / COLREGS-rule-range false positives"
    - "A broader re-grep (doc-filenames without phase prefix, describe/it embedded IDs, bare Task N/Wave N mentions, 'this phase'/'this plan') across src/ and app/ returns zero remaining matches"
    - "npm run lint passes with 0 errors, and eslint-suppressions.json's local/no-stale-id-comments entries are pruned down to only the files containing the documented false positives"
    - "The full Vitest suite and npm run typecheck both pass with no change in pass/fail count versus the pre-phase-13 baseline"
    - "A human reviewer confirms via git diff that every changed hunk across all of Phase 13's plans removed only the rotting identifier, never the substantive WHY explanation"
  artifacts:
    - path: "eslint-suppressions.json"
      provides: "Pruned suppression counts for local/no-stale-id-comments, reflecting only the documented false positives"
  key_links: []
---

<objective>
Close out Phase 13 by proving both grep passes return clean, the lint/test/typecheck baseline is unchanged, and a human has reviewed the full comment-cleanup diff for substance loss. This plan performs no new comment rewrites -- it verifies the work done in `13-01` through `13-07` and prunes the now-resolved ESLint suppression entries.

Purpose: satisfy ROADMAP Phase 13's three success criteria directly -- (1) zero remaining stale references from both the strict and broader grep passes, (2) every rewritten comment retains its WHY (verified via diff review), (3) the broader re-grep is documented as having been run, not assumed sufficient by the original count.
Output: Pruned `eslint-suppressions.json`; a documented verification record in this plan's SUMMARY.md covering both grep passes, lint/test/typecheck results, and the diff review outcome.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@eslint-rules/no-stale-id-comments.mjs
@eslint-suppressions.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run strict + broader grep sweeps and prune ESLint suppressions</name>
  <files>eslint-suppressions.json</files>
  <read_first>
    eslint-rules/no-stale-id-comments.mjs
    eslint-suppressions.json
  </read_first>
  <action>
Run the strict pattern sweep first, reproducing the exact regex from `eslint-rules/no-stale-id-comments.mjs` (`/\b(Phase\s+\d+|Plan\s+\d+|REQ-[A-Z]+-\d+|[A-Z]{2,6}-\d{2,3}|\d{2}(\.\d+)?-\d{2}-PLAN\.md|\d{2}(\.\d+)?-[A-Za-z][\w-]*\.md|this\s+(phase|plan))\b|\d{2}(\.\d+)?-\d{2}(?=\s*[),])/i`) via `npx eslint --rule '{"local/no-stale-id-comments":"error"}' -c eslint.config.mjs --format json src/components src/domain src/server app` with `eslint-suppressions.json` temporarily moved aside (so suppressed violations are actually reported, not hidden) -- move it back immediately after capturing the JSON output, whether the command succeeds or fails.

Confirm the JSON output contains ONLY these 4 known-false-positive files/lines, and nothing else:
- `src/components/hero/hero-preview-geometry.ts` lines 42, 43, 44 (Tailwind color-shade names `red-500`/`green-500`/`slate-600`)
- `src/components/sandbox/chart-panel-geometry.ts` lines 17, 34, 93, 94, 95 (Tailwind color-shade names `slate-200`/`zinc-700`/`slate-600`/`amber-500`/`zinc-700`+`slate-300`)
- `src/components/sandbox/vessel-role.ts` line 26 (Tailwind color-shade names `red-500`/`green-500`/`slate-400`)
- `src/domain/colregs/types.ts` lines 11-17 (COLREGS "Rules 12-15" rule-range mention, not a Phase/Plan ID)

If ANY other file/line appears in the JSON output, a genuine stale reference was missed by plans `13-01` through `13-07` -- stop and rewrite it by hand (same discipline: remove the ID, preserve the WHY) before proceeding.

Then run the broader re-grep, combining: (a) bare doc-filename mentions -- `grep -rn -E '\b(CONTEXT|RESEARCH|UI-SPEC|HUMAN-UAT)\.md\b' src/ app/ --include='*.ts' --include='*.tsx'` (expect zero matches; `PROJECT.md`/`README.md`/`CLAUDE.md` are stable top-level docs and are not part of this pattern); (b) describe/it/test embedded IDs -- `grep -rn -E '(describe|it|test)\(\s*"[^"]*\b([A-Z]{2,6}-[0-9]{2,3}|[0-9]{2}(\.[0-9])?-[0-9]{2})\b[^"]*"' src/ app/ --include='*.ts' --include='*.tsx'` (expect zero matches); (c) bare Task/Wave mentions -- `grep -rn -E '\bTask [0-9]\b|\bWave [0-9]\b' src/ app/ --include='*.ts' --include='*.tsx'` (expect zero matches); (d) `this phase`/`this plan` phrasing -- `grep -rn -E 'this phase|this plan' src/ app/ --include='*.ts' --include='*.tsx'` (expect zero matches).

Once both sweeps confirm clean (strict sweep shows only the 4 documented false-positive files, broader sweep shows zero matches across all 4 sub-patterns), run `npx eslint . --prune-suppressions` to shrink `eslint-suppressions.json`'s `local/no-stale-id-comments` entries down to exactly the false-positive files (`hero-preview-geometry.ts`: 3, `chart-panel-geometry.ts`: 5, `vessel-role.ts`: 1) -- `domain/colregs/types.ts` should drop to 0 entries for this rule since its only trigger ("Rules 12-15") was never actually suppressed as a *separate* count from the block's other (now-fixed) content; confirm its exact post-prune count via the diff of `eslint-suppressions.json` and adjust the expectation in your SUMMARY if it differs, explaining why.
  </action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Strict-pattern JSON output contains only the 4 documented false-positive files/lines listed above
    - All 4 broader-regrep sub-pattern commands return zero matches
    - `npx eslint . --prune-suppressions` exits 0 and `git diff -- eslint-suppressions.json` shows only `local/no-stale-id-comments` counts shrinking (no other rule's counts change)
    - `npm run lint` passes with 0 errors after the prune
  </acceptance_criteria>
  <done>Both grep sweeps documented clean (modulo the 4 known false positives); eslint-suppressions.json pruned and committed; npm run lint passes with 0 errors.</done>
</task>

<task type="auto">
  <name>Task 2: Full-suite regression check and self-review of the comment-only diff</name>
  <files>N/A -- verification only, no file changes</files>
  <read_first>
    package.json
  </read_first>
  <action>
Run `npx vitest run` (full suite, no path filter) and `npm run typecheck` and confirm both pass with the exact same pass/fail counts as the last known-clean baseline before Phase 13 started (Phase 12's closing verification recorded 216/216 passing per STATE.md -- confirm the count is still 216/216, or document the new total if legitimate new tests were added elsewhere in the meantime, with a note that no test was deleted or silently skipped).

Then produce a full `git diff` across every file touched by plans `13-01` through `13-07` (`git diff -- src/ app/`) and review it end-to-end for exactly one property: every changed hunk removes/rewrites a Phase/Plan/REQ-ID/doc-filename/Task-N/describe-string identifier and nothing else -- no rule logic, no exported value, no test assertion, no fixture number changed. Flag (and immediately revert) any hunk that touches non-comment/non-string-literal code. Paste the full diff (or a representative sample per cluster, if very long) into this task's SUMMARY.md so the human developer can spot-check it in the next checkpoint task.
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` reports the same total pass count as the pre-Phase-13 baseline (216/216, or the current legitimate total with a documented reason for any change)
    - `npm run typecheck` exits 0
    - Self-review of `git diff -- src/ app/` confirms every hunk is comment-text or test-name-string-only; any exception is reverted before this task is marked done
  </acceptance_criteria>
  <done>Full suite and typecheck pass with an unchanged baseline; diff self-reviewed end-to-end confirming zero WHY-content loss and zero logic drift; diff recorded in SUMMARY.md for the following checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human sign-off on the full comment-cleanup diff</name>
  <files>N/A -- no file changes in this task, human verification only</files>
  <what-built>
Phase 13's complete comment-cleanup diff across ~40 files: every stale Phase/Plan/REQ-ID/Task-N/bare-doc-filename/describe-string reference removed by hand, WHY content rephrased in place, zero logic/behavior change (confirmed by an unchanged full test-suite pass count and a clean typecheck in Task 2 above).
  </what-built>
  <how-to-verify>
1. Run `git log --oneline -20` and `git diff main...HEAD -- src/ app/` (or review the plan-by-plan diffs already committed for 13-01 through 13-07) to see the full comment-cleanup change set.
2. Spot-check at least one file from each cluster (domain/geometry, domain/colregs, server, sandbox, hero/gallery) and confirm the rewritten comment still explains the original WHY as well as the old one did -- not just "ID removed, meaning also gone."
3. Confirm `npm run lint`, `npx vitest run`, and `npm run typecheck` are all green (Task 1 and Task 2 above already ran these; re-run locally if you want an independent check).
  </how-to-verify>
  <action>No automated action for this task -- it exists solely to pause for human confirmation that the comment-cleanup diff retains full WHY content, per ROADMAP Phase 13 success criterion 2.</action>
  <acceptance_criteria>
    - Human has explicitly confirmed (via the resume-signal) that the spot-checked comments retain their original WHY content and that lint/test/typecheck are green
  </acceptance_criteria>
  <verify>
    <automated>N/A -- human confirmation required; no automated command applies to this checkpoint</automated>
  </verify>
  <resume-signal>Type "approved" to close Phase 13, or describe which file/comment needs another pass.</resume-signal>
  <done>Human has confirmed the comment-cleanup diff retains full WHY content across every spot-checked cluster, closing Phase 13.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | Verification-only plan: re-runs existing tooling (ESLint, Vitest, tsc) and reviews a diff. No new code path, no new dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-15 | Tampering | A genuine stale-ID comment slips through undetected because it doesn't match either grep pattern shape | mitigate | Task 1's strict-sweep step requires an EXACT match against only the 4 documented false-positive locations -- any other flagged file/line halts the task and is fixed by hand before proceeding, closing the loop this phase is designed to close. |
| T-13-16 | Repudiation | No record that the broader re-grep was actually run (vs. assumed) | mitigate | Task 1 requires each of the 4 broader-regrep sub-pattern commands to be run and its result recorded in this plan's SUMMARY.md, directly satisfying ROADMAP success criterion 3. |
| T-13-17 | N/A | npm/pip/cargo installs | accept | No package installs in this phase; not applicable. |
</threat_model>

<verification>
`npm run lint && npx vitest run && npm run typecheck` all exit 0. `git diff -- eslint-suppressions.json` shows only expected count reductions. Manual diff review across all of Phase 13's changes confirms comment-only edits throughout.
</verification>

<success_criteria>
ROADMAP Phase 13's three success criteria are all demonstrably met: (1) strict + broader greps return zero remaining genuine stale references, (2) diff review confirms full WHY retention, (3) the broader re-grep is documented as run, not assumed. `npm run lint`, full test suite, and typecheck all remain green.
</success_criteria>

<output>
Create `.planning/phases/13-comment-cleanup/13-08-SUMMARY.md` when done, including the literal grep command outputs and the diff-review confirmation.
</output>
