---
phase: 13-comment-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/geometry/angle-convert.ts
  - src/domain/geometry/bearing.ts
  - src/domain/geometry/bearing.fixtures.ts
  - src/domain/geometry/relative-bearing.ts
  - src/domain/geometry/relative-bearing.fixtures.ts
  - src/domain/geometry/screen-convert.ts
  - src/domain/geometry/screen-convert.fixtures.ts
  - src/domain/shared/result.ts
  - src/domain/geometry/cpa.ts
autonomous: true
requirements: [CMNT-01]

must_haves:
  truths:
    - "None of the 9 files in src/domain/geometry/ or src/domain/shared/ contain a Phase N / Plan N / 'this phase' / 'this plan' comment reference after this plan lands"
    - "Every rewritten comment still explains its original WHY (design rationale, derivation, or constraint) without the archived phase/plan pointer"
  artifacts:
    - path: "src/domain/geometry/angle-convert.ts"
      provides: "Rewritten header/inline comments, zero behavior change"
    - path: "src/domain/geometry/cpa.ts"
      provides: "Rewritten header comments referencing 'RESEARCH.md' bare filename removed"
  key_links: []
---

<objective>
Rewrite every stale Phase/Plan/REQ-ID comment reference in `src/domain/geometry/` and `src/domain/shared/` (plus `cpa.ts`, folded in here because it shares the geometry directory and its own bare `RESEARCH.md` reference was missed by the original ESLint-rule-based scoping sweep) by hand, per CMNT-01. Preserve every comment's substantive WHY content; only the rotting identifier is removed or replaced with a durable description of the underlying reason.

This is a documentation-only change: no runtime logic, exports, signatures, or test assertions are touched. `classifyEncounter()`/`bearing()`/`relativeBearing()`/`cpa()`/`screenToChart()` outputs must stay byte-identical before and after.

Purpose: retroactively enforce this project's own comment convention (CLAUDE.md: "never reference a task/plan ID that will rot once the plan is archived -- reference the reason instead") across files that predate the convention, and against the exact 54-file/109-occurrence baseline that `eslint-suppressions.json`'s `local/no-stale-id-comments` entries currently track for these files.
Output: Updated comments in the 9 files listed above; no other diff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
@eslint-rules/no-stale-id-comments.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite stale comments in angle-convert.ts, bearing.ts, bearing.fixtures.ts, relative-bearing.ts, relative-bearing.fixtures.ts</name>
  <files>src/domain/geometry/angle-convert.ts, src/domain/geometry/bearing.ts, src/domain/geometry/bearing.fixtures.ts, src/domain/geometry/relative-bearing.ts, src/domain/geometry/relative-bearing.fixtures.ts</files>
  <read_first>
    src/domain/geometry/angle-convert.ts
    src/domain/geometry/bearing.ts
    src/domain/geometry/bearing.fixtures.ts
    src/domain/geometry/relative-bearing.ts
    src/domain/geometry/relative-bearing.fixtures.ts
    eslint-rules/no-stale-id-comments.mjs
  </read_first>
  <action>
Rewrite the following exact comment locations (line numbers as of this plan's authoring; re-locate by content if the file has since shifted). In every case, delete the bracketed rotting identifier and keep/rephrase the surrounding sentence so the reasoning still reads naturally.

1. `src/domain/geometry/angle-convert.ts` lines 1-17 (the file's opening JSDoc block, currently opens with "Phase 1 scope: compass&lt;-&gt;math angle conversion only..." and references "this same phase", "this plan's Task 3", and "Phase 4's (Interactive Chart Sandbox)"): rewrite to: "Compass&lt;-&gt;math angle conversion only. The pure screen-pixel &lt;-&gt; chart {x,y} coordinate-conversion functions live in a sibling file (screen-convert.ts) -- kept separate from this file because they convert screen-space pixel coordinates, not compass/math angles. Only the *caller* that measures live containerSize via ResizeObserver and supplies the chart's viewBox is the Interactive Chart Sandbox feature's responsibility, per CLAUDE.md's SVG + Pointer Events architecture note; the conversion functions themselves are not deferred." Keep the remaining sentence about "Unlike bearing/relativeBearing/cpa..." unchanged.
2. `src/domain/geometry/angle-convert.ts` lines 48-53 (the `// D-08: relative bearing normalized...` through `// "justify every abstraction" persona).` line-comment paragraph, currently says "kept file-independent so this plan and Plan 02 can execute in the same wave without a file dependency"): rewrite that clause to "kept file-independent so this file's angle-conversion functions and the sibling screen-space conversion functions can be implemented without a cross-file dependency". Keep the `D-08:` decision-ID prefix and the rest of the paragraph's reasoning (duplication rationale, CLAUDE.md "justify every abstraction" citation) unchanged -- `D-08` is a Decision ID, not a Phase/Plan/REQ-ID, and is explicitly out of this phase's scope (see plan 13-08's audit note).
3. `src/domain/geometry/bearing.ts` lines 1-10 (opening JSDoc, ends with "Result is raw, unrounded degrees in [0, 360) (D-04, D-08)... silently-wrong 0 (D-07, T-01-02)."): remove only `T-01-02` (the rotting Threat-Model-entry pointer into an archived PLAN.md; a bare `T-NN-NN` reference is the same rotting-pointer class as a Phase/Plan-ID even though the strict ESLint pattern doesn't catch its exact shape) -- rewrite the trailing parenthetical from "(D-07, T-01-02)" to "(D-07: degenerate/invalid inputs must be caught explicitly, never silently coerced to 0)". Keep `D-04`/`D-07`/`D-08` decision-ID citations exactly as-is (out of scope, see note above).
4. `src/domain/geometry/bearing.fixtures.ts` line 61 (`// arithmetic runs (T-01-02).`) and line 67 (`// Non-finite coordinate (Infinity) must also be rejected (T-01-02).`): remove the `(T-01-02)` parenthetical from both lines entirely -- the preceding sentence on each line already states the WHY (non-finite coordinates must be rejected before atan2/normalize math runs) with no loss of meaning once the pointer is dropped.
5. `src/domain/geometry/relative-bearing.ts` lines 17-18 (inside `relativeBearing()`, currently "so this guard is unique to this function (T-01-04). Position-level non-finite values are already caught by the composed bearing() call below (T-01-02) -- no duplicate guard needed here."): remove both `(T-01-04)` and `(T-01-02)` parentheticals -- the sentences already state the WHY (this function's own heading guard is unique; position-level guards are already handled by the composed `bearing()` call) without them.
  </action>
  <verify>
    <automated>npx vitest run src/domain/geometry/bearing.test.ts src/domain/geometry/relative-bearing.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '(Phase [0-9]|Plan [0-9]|this phase|this plan)' src/domain/geometry/angle-convert.ts src/domain/geometry/bearing.ts src/domain/geometry/bearing.fixtures.ts src/domain/geometry/relative-bearing.ts` returns no matches
    - `grep -n 'T-01-02\|T-01-04' src/domain/geometry/bearing.ts src/domain/geometry/bearing.fixtures.ts src/domain/geometry/relative-bearing.ts` returns no matches
    - `D-04`, `D-07`, `D-08` decision-ID citations remain present verbatim in bearing.ts and angle-convert.ts (confirm via `grep -n 'D-0[4789]' src/domain/geometry/bearing.ts src/domain/geometry/angle-convert.ts`)
    - `npx vitest run src/domain/geometry/bearing.test.ts src/domain/geometry/relative-bearing.test.ts` passes with the same pass count as before this task (comment-only change, zero test-behavior change)
  </acceptance_criteria>
  <done>All 5 listed comment locations rewritten; no code/logic/export changed; targeted greps above are clean; both test files still pass.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite stale comments in relative-bearing.fixtures.ts, screen-convert.ts, screen-convert.fixtures.ts, result.ts, cpa.ts</name>
  <files>src/domain/geometry/relative-bearing.fixtures.ts, src/domain/geometry/screen-convert.ts, src/domain/geometry/screen-convert.fixtures.ts, src/domain/shared/result.ts, src/domain/geometry/cpa.ts</files>
  <read_first>
    src/domain/geometry/relative-bearing.fixtures.ts
    src/domain/geometry/screen-convert.ts
    src/domain/geometry/screen-convert.fixtures.ts
    src/domain/shared/result.ts
    src/domain/geometry/cpa.ts
  </read_first>
  <action>
1. `src/domain/geometry/relative-bearing.fixtures.ts` line 63 (`// Reciprocal-heading-but-off-axis-bearing (D-16, ROADMAP Phase 1 Success` continuing to `// Criterion #2): own heading 000...`): remove `ROADMAP Phase 1 Success Criterion #2` from the parenthetical, keep `D-16` (decision ID, out of scope) -- rewrite to "(D-16 -- a reciprocal heading alone must not be treated as head-on without checking the actual bearing to the contact):". Line 115 (`// relativeBearing() must guard own.heading itself (T-01-04).`): remove the `(T-01-04)` parenthetical, the sentence stands alone without it.
2. `src/domain/geometry/screen-convert.ts` lines 3-10 (opening JSDoc, "-- zero DOM/browser-observer/graphics-element API dependency (ROADMAP Phase 1 Success Criterion #3). The only Phase-4-owned piece is the *caller*..."): rewrite to "-- zero DOM/browser-observer/graphics-element API dependency by design. The only piece owned by the Interactive Chart Sandbox feature is the *caller* that measures containerSize via a live browser size-tracking API and supplies the chart's viewBox; these functions take both as plain arguments and never touch the DOM."
3. `src/domain/geometry/screen-convert.fixtures.ts` lines 3-11 (opening JSDoc, "(D-13-style, for consistency with the rest of this phase's fixtures even though D-13 itself is trigonometry-specific)"): rewrite "the rest of this phase's fixtures" to "the rest of this project's hand-derived fixture files". Keep `D-13` decision-ID citations as-is.
4. `src/domain/shared/result.ts` line 12 (`// Reserved for Plans 02-03's Number.isFinite input guard (T-01-02/T-01-03).`): rewrite to "// Reserved for a future Number.isFinite input guard on the geometry functions (see bearing.ts/relative-bearing.ts's own guards) --". Line 13 (`// No function in this plan emits this reason yet -- it exists so geometry`): rewrite "in this plan" to "in this file". Lines 33-37 (JSDoc block ending "...once `DegenerateCaseReason` grows past a couple of variants (used by Phase 2+ consumers, not this plan itself)."): rewrite to "(used by the COLREGS rules-engine consumers built on top of this module, not by this file itself)".
5. `src/domain/geometry/cpa.ts` (NOT in the original 54-file ESLint-flagged list -- confirmed via a broader `grep -n 'RESEARCH.md' src/domain/geometry/cpa.ts` re-grep per CMNT-02): line 6 references "RESEARCH.md Open Questions 1 (negative TCPA) and 2 (epsilon threshold)"; line 16 "Resolves RESEARCH.md Open Question 2: a raw"; line 76 "// result (closest approach was in the past) -- resolves RESEARCH.md Open". Rewrite each bare `RESEARCH.md` reference to describe the resolved question directly instead of citing the archived doc, e.g. line 6: "Resolves two open questions this project settled during design: how to handle a negative TCPA (closest approach already happened), and what epsilon threshold distinguishes a genuine parallel/matching course from floating-point noise in the relative-velocity vector,"; line 16: "Resolves the epsilon-threshold question: a raw"; line 76: "// result (closest approach was in the past) -- this resolves the negative-TCPA".
  </action>
  <verify>
    <automated>npx vitest run src/domain/geometry/cpa.test.ts src/domain/geometry/screen-convert.fixtures.ts src/domain/shared/result.test.ts src/domain/geometry/relative-bearing.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'ROADMAP Phase|Phase 2\+|this plan|RESEARCH\.md' src/domain/geometry/relative-bearing.fixtures.ts src/domain/geometry/screen-convert.ts src/domain/geometry/screen-convert.fixtures.ts src/domain/shared/result.ts src/domain/geometry/cpa.ts` returns no matches
    - `D-13`, `D-16` decision-ID citations remain present verbatim (out of scope) -- confirm via `grep -n 'D-1[36]' src/domain/geometry/relative-bearing.fixtures.ts src/domain/geometry/screen-convert.fixtures.ts`
    - `npx vitest run src/domain/geometry/cpa.test.ts src/domain/geometry/screen-convert.fixtures.ts src/domain/shared/result.test.ts src/domain/geometry/relative-bearing.test.ts` passes with the same pass count as before this task
  </acceptance_criteria>
  <done>All 5 files' listed comments rewritten with WHY preserved; targeted greps clean; affected test files still pass unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | This plan only edits comment text inside existing, already-reviewed source files. No new input crosses any boundary, no dependency is added, no runtime code path changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | Accidental logic edit while touching adjacent comments | mitigate | Task acceptance criteria require the listed test files to pass with the *same* pass count after each task; any behavior change would surface as a test failure or a diff outside the comment lines listed in `<action>`. |
| T-13-02 | N/A | npm/pip/cargo installs | accept | No package installs in this phase; not applicable. |
</threat_model>

<verification>
Run `npx vitest run src/domain/geometry src/domain/shared` and confirm the full pass count matches the pre-phase baseline. Run `git diff -- src/domain/geometry src/domain/shared` and confirm every changed hunk only touches comment lines (no code statement changed).
</verification>

<success_criteria>
All 9 files' listed stale comment locations are rewritten with the rotting Phase/Plan/REQ-ID/bare-doc-filename/Threat-ID reference removed, substantive WHY content fully retained, zero test or behavior change.
</success_criteria>

<output>
Create `.planning/phases/13-comment-cleanup/13-01-SUMMARY.md` when done
</output>
