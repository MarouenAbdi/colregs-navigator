---
phase: 13-comment-cleanup
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/hero/Hero.tsx
  - src/components/hero/Hero.test.tsx
  - src/components/hero/HeroPreviewCard.tsx
  - src/components/hero/hero-preview-fixture.ts
  - src/components/hero/hero-preview-fixture.test.ts
  - src/components/hero/hero-preview-geometry.ts
  - app/layout.tsx
  - src/components/gallery/GalleryCard.tsx
  - src/components/gallery/GalleryContainer.tsx
  - src/components/gallery/GalleryPreviewChart.tsx
  - src/components/gallery/gallery-preview-geometry.test.ts
  - src/components/layout/Footer.tsx
  - src/components/shared/SectionGridBackground.tsx
  - src/components/shared/static-chart-geometry.ts
autonomous: true
requirements: [CMNT-01]

must_haves:
  truths:
    - "None of the 14 files in this Hero/Gallery/shared/layout set contain a Phase N / Plan N / Task N / bare-doc-filename comment reference after this plan lands"
    - "The one genuine Tailwind-color-shade false positive in hero-preview-geometry.ts is left untouched, not misidentified as a stale ID"
  artifacts:
    - path: "src/components/hero/hero-preview-fixture.ts"
      provides: "Rewritten comments, byte-identical vessel fixture values"
  key_links: []
---

<objective>
Rewrite every stale Phase/Plan/REQ-ID/Task-N/bare-doc-filename comment reference in the Hero, Gallery, shared, and layout marketing components by hand, per CMNT-01. Preserve every comment's WHY content -- especially `hero-preview-fixture.ts`'s worked-math provenance note and `GalleryPreviewChart.tsx`'s viewBox/scale derivation notes. No change to any rendered value, fixture number, or classification result.

This plan folds in `app/layout.tsx`, which was not in the original 54-file ESLint-flagged list (its `06-01 Task 3`/`Task 1's` comments don't match the strict regex shape) -- confirmed via a broader re-grep per CMNT-02.

Purpose: retroactively enforce the comment convention across the marketing/landing-page layer.
Output: Updated comments in the 14 files listed above; no other diff. `describe()` test-suite-name string rewrites for `Hero.test.tsx`, `GalleryCard.test.tsx`, `GalleryContainer.test.tsx`, `GalleryPreviewChart.test.tsx`, and `hero-preview-fixture.test.ts` are handled separately in plan `13-07` (they are string literals, not comments, and are grouped together as their own edit category).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite stale comments in Hero.tsx, Hero.test.tsx, HeroPreviewCard.tsx, hero-preview-fixture.ts, hero-preview-fixture.test.ts, hero-preview-geometry.ts, app/layout.tsx</name>
  <files>src/components/hero/Hero.tsx, src/components/hero/Hero.test.tsx, src/components/hero/HeroPreviewCard.tsx, src/components/hero/hero-preview-fixture.ts, src/components/hero/hero-preview-fixture.test.ts, src/components/hero/hero-preview-geometry.ts, app/layout.tsx</files>
  <read_first>
    src/components/hero/Hero.tsx
    src/components/hero/Hero.test.tsx
    src/components/hero/HeroPreviewCard.tsx
    src/components/hero/hero-preview-fixture.ts
    src/components/hero/hero-preview-fixture.test.ts
    src/components/hero/hero-preview-geometry.ts
    app/layout.tsx
  </read_first>
  <action>
1. `src/components/hero/Hero.tsx` lines 1-11 (opening JSDoc: "Hero (07-01) -- net-new marketing Hero section (Direction A only, per PROJECT.md's locked v1.1 decision)... Renders the locked headline/copy/CTAs (HERO-01). The "Live classification" preview card (HERO-02) is its own component..."): rewrite "Hero (07-01) -- net-new" to "Net-new". Keep `HERO-01`, `HERO-02` (live REQ-IDs, verify against `.planning/REQUIREMENTS.md`) and `PROJECT.md` (a stable, still-existing top-level doc, not a phase-scoped artifact -- out of scope).
2. `src/components/hero/Hero.test.tsx` line 27 (`// "2.99 NM" appears twice by design (07-UI-SPEC.md): once in the`): rewrite "(07-UI-SPEC.md)" to "(by design)".
3. `src/components/hero/HeroPreviewCard.tsx` lines 1-9 (opening JSDoc: "the illustrative "Live classification" preview card (HERO-02)... Shares zero code with the real interactive `ChartPanel.tsx` (CONTEXT.md D-03) and is never wired to live Sandbox state (CONTEXT.md D-01: no pulsing dot, no animated bearing line)."): rewrite "(CONTEXT.md D-03)" to "(D-03)" and "(CONTEXT.md D-01: no pulsing dot, no animated bearing line)" to "(D-01: no pulsing dot, no animated bearing line)". Keep `HERO-02`, `D-03`, `D-01`.
4. `src/components/hero/HeroPreviewCard.tsx` line 138 (`{/* Static, non-pulsing dot per CONTEXT.md D-01 -- no animation classes. */}`): rewrite "per CONTEXT.md D-01" to "per D-01".
5. `src/components/hero/hero-preview-fixture.ts` lines 1-15 (opening JSDoc: "Hero (07-01) -- fixed vessel fixture... (HERO-02, CONTEXT.md D-07)... Transcribed verbatim from 07-RESEARCH.md's Code Examples section -- do not alter."): rewrite "Hero (07-01) -- fixed" to "Fixed"; rewrite "(HERO-02, CONTEXT.md D-07)" to "(HERO-02, D-07)"; rewrite "Transcribed verbatim from 07-RESEARCH.md's Code Examples section -- do not alter." to "Transcribed verbatim from this project's original worked-math derivation -- do not alter." Keep `HERO-02`, `D-07`.
6. `src/components/hero/hero-preview-fixture.ts` line 48 (`// boolean directly, so this is a non-issue for HERO-02's acceptance`): keep `HERO-02` (live REQ-ID); no other stale content on this line, leave unchanged.
7. `src/components/hero/hero-preview-fixture.test.ts` lines 7-14 (opening JSDoc: "Hero (07-01) -- fixture-drift guard (HERO-02). Asserts..."): rewrite "Hero (07-01) -- fixture-drift" to "Fixture-drift". Keep `HERO-02`.
8. `src/components/hero/hero-preview-geometry.ts` line 10 (`// UI-SPEC.md "Preview Card Dimensions": 8:5 (not square) aspect ratio,`): rewrite "UI-SPEC.md "Preview Card Dimensions":" to "Per the design's Preview Card Dimensions spec:".
9. `src/components/hero/hero-preview-geometry.ts` lines 39-41 (`// Domain-locked colors (UI-SPEC.md Color table): the fixture's verdict is` / `// fixed at authoring time, so hull/pill *colors* may stay hardcoded, but` / `// "GW"/"SO" *text* must still be derived from the classification result.`): rewrite "(UI-SPEC.md Color table)" to "(per the design's fixed color table)".
10. `src/components/hero/hero-preview-geometry.ts` lines 42-44: **DO NOT EDIT.** These lines contain Tailwind/hex color-shade names (`red-500`, `green-500`, `slate-600`) that happen to match the ESLint rule's pattern shape case-insensitively -- not stale ID references.
11. `app/layout.tsx` lines 11-15 (`// 06-01 Task 3 (Pitfall 1): shadcn init's \`nova\` preset had already` / `// partially wired Geist (sans only, variable named "--font-sans"). Renamed` / `// to "--font-geist-sans" and added Geist_Mono, matching the` / `// "--font-geist-sans"/"--font-geist-mono" variable names Task 1's` / `// app/globals.css \`@theme inline\` block maps --font-sans/--font-mono to.`) -- NOT in the original ESLint-flagged list, found via broader re-grep per CMNT-02: rewrite to remove "06-01 Task 3" and "Task 1's" -- e.g. "// Pitfall 1: shadcn init's `nova` preset had already partially wired Geist (sans only, variable named "--font-sans"). Renamed to "--font-geist-sans" and added Geist_Mono, matching the "--font-geist-sans"/"--font-geist-mono" variable names that app/globals.css's own `@theme inline` block maps --font-sans/--font-mono to."
  </action>
  <verify>
    <automated>npx vitest run src/components/hero</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '\(07-01\)|\(06-01\)|CONTEXT\.md|RESEARCH\.md|UI-SPEC\.md|Task [0-9]' src/components/hero/Hero.tsx src/components/hero/Hero.test.tsx src/components/hero/HeroPreviewCard.tsx src/components/hero/hero-preview-fixture.ts src/components/hero/hero-preview-fixture.test.ts src/components/hero/hero-preview-geometry.ts app/layout.tsx` returns no matches
    - `grep -n 'red-500\|green-500\|slate-600' src/components/hero/hero-preview-geometry.ts` still returns the same 3 matches as before this task (confirms the color-name false positives were left untouched)
    - `npx vitest run src/components/hero` passes with the same pass count as before this task
  </acceptance_criteria>
  <done>All 7 files' listed comment locations rewritten (except the deliberately-untouched color-name lines); targeted greps clean; Hero test suite passes with unchanged pass count.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite stale comments in GalleryCard.tsx, GalleryContainer.tsx, GalleryPreviewChart.tsx, gallery-preview-geometry.test.ts, Footer.tsx, SectionGridBackground.tsx, static-chart-geometry.ts</name>
  <files>src/components/gallery/GalleryCard.tsx, src/components/gallery/GalleryContainer.tsx, src/components/gallery/GalleryPreviewChart.tsx, src/components/gallery/gallery-preview-geometry.test.ts, src/components/layout/Footer.tsx, src/components/shared/SectionGridBackground.tsx, src/components/shared/static-chart-geometry.ts</files>
  <read_first>
    src/components/gallery/GalleryCard.tsx
    src/components/gallery/GalleryContainer.tsx
    src/components/gallery/GalleryPreviewChart.tsx
    src/components/gallery/gallery-preview-geometry.test.ts
    src/components/layout/Footer.tsx
    src/components/shared/SectionGridBackground.tsx
    src/components/shared/static-chart-geometry.ts
  </read_first>
  <action>
1. `src/components/gallery/GalleryCard.tsx` lines 1-7 (opening JSDoc: "One Gallery card (GAL-01/GAL-02)... matching the design's "no labeled CTA" affordance (09-UI-SPEC.md Copywriting Contract)."): rewrite "(09-UI-SPEC.md Copywriting Contract)" to "(per the design's Copywriting Contract)". Keep `GAL-01`, `GAL-02` (live REQ-IDs, verify).
2. `src/components/gallery/GalleryCard.tsx` line 27: locate the second `09-UI-SPEC.md` mention (flagged separately by ESLint at line 27) and apply the same rewrite -- replace the bare doc-filename citation with a direct description of what the design specifies at that point.
3. `src/components/gallery/GalleryContainer.tsx` lines 1-7 (opening JSDoc: "Gallery section container (GAL-01/GAL-02/GAL-04)... Reuses SandboxContainer.tsx's exact section-container className convention, per 09-UI-SPEC.md's Layout section."): rewrite "per 09-UI-SPEC.md's Layout section" to "matching the design's Layout section". Keep `GAL-01`, `GAL-02`, `GAL-04`.
4. `src/components/gallery/GalleryPreviewChart.tsx` lines 1-10 (opening JSDoc: "Gallery card's static, illustrative SVG mini-chart (GAL-01, D-06/D-08): a parametrized version of Hero's ... pattern (09-RESEARCH.md Pattern 1)..."): rewrite "(09-RESEARCH.md Pattern 1)" to "(the same static-illustration pattern Hero's preview card established)". Keep `GAL-01`, `D-06`, `D-08`.
5. `src/components/gallery/GalleryPreviewChart.tsx` line 18 (`// UI-SPEC.md "Mini-Chart Contract": fixed 3:2 aspect ratio (358:238`): rewrite "UI-SPEC.md "Mini-Chart Contract":" to "Per the design's Mini-Chart Contract:". Line 24 is the same flagged location if line numbers have shifted -- verify with `grep -n 'UI-SPEC.md' src/components/gallery/GalleryPreviewChart.tsx` and apply this rewrite to every hit at this "Mini-Chart Contract" aspect-ratio comment.
6. `src/components/gallery/GalleryPreviewChart.tsx` line 29 (`// UI-SPEC.md's "Mini-chart per-vessel role pill" correction (Pitfall 3):`): rewrite to "// Per the design's Mini-chart per-vessel role pill correction (Pitfall 3):".
7. `src/components/gallery/GalleryPreviewChart.tsx` line 51 (part of the paragraph starting `// viewBox is recomputed per card by computeCardViewBox() from each curated` / `// scenario's real vessel separation (1.0-10.0 NM across the 6 cards per` / `// 09-RESEARCH.md), so screen-px-per-NM varies by roughly an order of`): rewrite "(1.0-10.0 NM across the 6 cards per 09-RESEARCH.md)" to "(1.0-10.0 NM across the 6 curated cards)".
8. `src/components/gallery/GalleryPreviewChart.tsx` line 77 (`{/* z-order per UI-SPEC.md's Mini-Chart Contract: hull (3), label`): rewrite to "{/* z-order per the design's Mini-Chart Contract: hull (3), label".
9. `src/components/gallery/GalleryPreviewChart.tsx` line 148 (`lower stroke opacity (UI-SPEC.md Mini-Chart Contract item 1:`): rewrite to "lower stroke opacity (per the design's Mini-Chart Contract:".
10. `src/components/gallery/gallery-preview-geometry.test.ts` lines 4-9 (opening JSDoc: "computeCardViewBox() (09-02) -- unit tests against the 1.0-10.0 NM vessel-pair range this phase's actual 6 gallery cards span (per 09-RESEARCH.md's verified per-card range table)..."): rewrite "computeCardViewBox() (09-02) -- unit tests" to "computeCardViewBox() unit tests"; rewrite "this phase's actual 6 gallery cards" to "the actual 6 curated gallery cards"; rewrite "(per 09-RESEARCH.md's verified per-card range table)" to "(verified against the curated scenario set's own per-card range)".
11. `src/components/layout/Footer.tsx` lines 1-5 (opening JSDoc: "Footer (06-01) -- static page-shell chrome... Copy is locked verbatim by 06-UI-SPEC.md's Copywriting Contract -- do not paraphrase."): rewrite "Footer (06-01) -- static" to "Static"; rewrite "locked verbatim by 06-UI-SPEC.md's Copywriting Contract" to "locked verbatim by the design's Copywriting Contract".
12. `src/components/shared/SectionGridBackground.tsx` lines 3-16 (opening JSDoc: "First real occupant of src/components/shared/ (see README.md): Hero (07-01) and the future Gallery section (Phase 9) both use the exact same repeating-grid pattern..."): rewrite "Hero (07-01)" to "Hero"; rewrite "the future Gallery section (Phase 9)" to "the Gallery section". Keep the `README.md` reference (a stable, still-existing top-level doc, not phase-scoped -- out of scope).
13. `src/components/shared/static-chart-geometry.ts` lines 1-10 (opening JSDoc: "extracted from `hero-preview-geometry.ts` once Gallery became a real second consumer of these primitives (09-CONTEXT.md D-09, CLAUDE.md's "extract only on a real second consumer" convention)."): rewrite "(09-CONTEXT.md D-09, CLAUDE.md's ...)" to "(D-09, CLAUDE.md's "extract only on a real second consumer" convention)". Keep `D-09` and the `CLAUDE.md` reference (a stable top-level doc).
  </action>
  <verify>
    <automated>npx vitest run src/components/gallery src/components/layout src/components/shared</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '09-UI-SPEC\.md|UI-SPEC\.md|09-RESEARCH\.md|09-CONTEXT\.md|\(07-01\)|\(06-01\)|Phase 9|this phase' src/components/gallery/GalleryCard.tsx src/components/gallery/GalleryContainer.tsx src/components/gallery/GalleryPreviewChart.tsx src/components/gallery/gallery-preview-geometry.test.ts src/components/layout/Footer.tsx src/components/shared/SectionGridBackground.tsx src/components/shared/static-chart-geometry.ts` returns no matches
    - `GAL-01`, `GAL-02`, `GAL-04`, `D-06`, `D-08`, `D-09` citations remain present verbatim where noted above
    - `npx vitest run src/components/gallery src/components/layout src/components/shared` passes with the same pass count as before this task
  </acceptance_criteria>
  <done>All 7 files' listed comment locations rewritten; targeted greps clean; Gallery/layout/shared test suites pass with unchanged pass counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | Comment-only edits to marketing/landing-page components. No new input crosses any boundary, no dependency added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-11 | Tampering | Accidental value edit to hero-preview-fixture.ts's hand-solved vessel coordinates while touching adjacent comments | mitigate | Task 1's acceptance criteria require the Hero test suite (including hero-preview-fixture.test.ts's drift guard) to pass with the *same* pass count -- any accidental numeric drift is caught immediately. |
| T-13-12 | N/A | npm/pip/cargo installs | accept | No package installs in this phase; not applicable. |
</threat_model>

<verification>
Run `npx vitest run src/components/hero src/components/gallery src/components/layout src/components/shared` and confirm the combined pass count matches the pre-phase baseline. Run `git diff -- src/components/hero app/layout.tsx src/components/gallery src/components/layout/Footer.tsx src/components/shared` and confirm every changed hunk only touches comment lines.
</verification>

<success_criteria>
All 14 files' listed stale comment locations are rewritten with the rotting reference removed and substantive WHY content fully retained; the color-name false positive in hero-preview-geometry.ts is left untouched; zero test or behavior change.
</success_criteria>

<output>
Create `.planning/phases/13-comment-cleanup/13-06-SUMMARY.md` when done
</output>
