---
phase: 13-comment-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/colregs/classify-encounter.ts
  - src/domain/colregs/classify-encounter.fixtures.ts
  - src/domain/colregs/resolve-doubt-geometry.ts
  - src/domain/colregs/resolve-doubt-geometry.fixtures.ts
  - src/domain/colregs/types.ts
  - src/domain/colregs/risk-of-collision.ts
  - src/domain/colregs/risk-of-collision.fixtures.ts
  - src/domain/colregs/risk-of-collision.test.ts
  - src/domain/colregs/vessel-priority.ts
  - src/domain/colregs/vessel-priority.fixtures.ts
autonomous: true
requirements: [CMNT-01]

must_haves:
  truths:
    - "None of the 10 files in src/domain/colregs/ contain a Phase N / Plan N / WR-NN / CR-NN / bare-doc-filename comment reference after this plan lands"
    - "Every rewritten regression-test comment (WR-01/WR-02/WR-03/CR-01) still names the geometric scenario it guards against"
  artifacts:
    - path: "src/domain/colregs/classify-encounter.ts"
      provides: "Rewritten dispatch-stage comments, zero behavior change to classifyEncounter()"
  key_links: []
---

<objective>
Rewrite every stale Phase/Plan/REQ-ID/regression-ticket comment reference in `src/domain/colregs/` by hand, per CMNT-01. Preserve every comment's substantive WHY content -- especially the WR-01/WR-02/WR-03/CR-01 regression-test annotations, which currently double as the only explanation for several deliberately-tricky geometric edge cases (bOvertakesA/aOvertakesB simultaneity, sticky-hysteresis boundary drift, Stage 4/5 dispatch order). `classifyEncounter()` and all COLREGS rule outputs must stay byte-identical before and after.

Purpose: retroactively enforce this project's comment convention across the highest-value test target in the codebase (the COLREGS rules engine) without altering any rule logic.
Output: Updated comments in the 10 files listed above; no other diff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite stale comments in classify-encounter.ts, classify-encounter.fixtures.ts, resolve-doubt-geometry.ts, resolve-doubt-geometry.fixtures.ts, types.ts</name>
  <files>src/domain/colregs/classify-encounter.ts, src/domain/colregs/classify-encounter.fixtures.ts, src/domain/colregs/resolve-doubt-geometry.ts, src/domain/colregs/resolve-doubt-geometry.fixtures.ts, src/domain/colregs/types.ts</files>
  <read_first>
    src/domain/colregs/classify-encounter.ts
    src/domain/colregs/classify-encounter.fixtures.ts
    src/domain/colregs/resolve-doubt-geometry.ts
    src/domain/colregs/resolve-doubt-geometry.fixtures.ts
    src/domain/colregs/types.ts
  </read_first>
  <action>
1. `src/domain/colregs/classify-encounter.ts` lines 1-14 (opening JSDoc: "the Rule 7 -&gt; 13 -&gt; 14 -&gt; 15 -&gt; 18 dispatch (CLAS-01 through CLAS-04, DETM-01, DETM-02, RSON-02)... Composes Phase 1's `relativeBearing()`/`cpa()` and Plan 01's `riskOfCollision`/`rule18Overrides()`/`vesselPriority()` functions exclusively"): drop `Phase 1's` and `Plan 01's`, rewrite the composition sentence to "Composes this codebase's own `relativeBearing()`/`cpa()` geometry functions and `riskOfCollision`/`rule18Overrides()`/`vesselPriority()` domain functions exclusively -- no new trigonometry or threshold logic is introduced here." Keep `CLAS-01` through `CLAS-04`, `DETM-01`, `DETM-02`, `RSON-02` (these are REQ-IDs tracked live in REQUIREMENTS.md's traceability table, not archived plan pointers -- confirm each still resolves to a `- [x]` row in `.planning/REQUIREMENTS.md` before keeping it; if any no longer resolves, drop it too).
2. `src/domain/colregs/classify-encounter.ts` line 117 (`// WR-01: mirror Stage 3's doubt-band check...`): rewrite to "// Sticky-hysteresis doubt-band check: mirror Stage 3's doubt-band check against the same 112.5 deg". Line 138 (`// WR-02: bOvertakesA and aOvertakesB CAN both be true simultaneously`): rewrite to "// bOvertakesA/aOvertakesB simultaneity: bOvertakesA and aOvertakesB CAN both be true simultaneously". Line 201 (`// Stage 5: Rule 15, residual. WR-03: rbAtoB CAN be exactly 0 here --`): rewrite to "// Stage 5: Rule 15, residual. One-sided dead-ahead bearing: rbAtoB CAN be exactly 0 here --".
3. `src/domain/colregs/classify-encounter.fixtures.ts` lines 1-7 (opening JSDoc: "the auditable proof-of-correctness suite CONTEXT.md calls for... transcribed from 02-02-PLAN.md's own worked math, not re-derived here"): rewrite to "the auditable proof-of-correctness suite this project's design process called for. Each fixture documents the worked `relativeBearing()`/`cpa()` derivation behind its expected values inline (hand-transcribed from the original worked math, not re-derived here)."
4. `src/domain/colregs/classify-encounter.fixtures.ts` line 38 (`// higher speed (15kn), same geometry shape as Phase 1's overtakingCase`): rewrite "Phase 1's overtakingCase" to "this file's own overtakingCase" (verify via `grep -n 'export const overtakingCase'` in this same file before picking the wording -- if it instead lives in a sibling fixtures file, name that file directly).
5. `src/domain/colregs/classify-encounter.fixtures.ts` line 119 (`// WR-02 regression: both bOvertakesA and aOvertakesB are simultaneously`): rewrite "WR-02 regression:" to "Regression case:". Line 148 (`// WR-01 regression: sticky-overtaking hysteresis with the current bearing`): rewrite "WR-01 regression:" to "Regression case:". Line 331 (`// WR-03 regression: rbAtoB is exactly 0...`): rewrite "WR-03 regression:" to "Regression case:". Line 441 (`// CR-01 regression: same geometry as overtakingBothDirectionsCase...`): rewrite "CR-01 regression:" to "Regression case (found via manual code review):".
6. `src/domain/colregs/classify-encounter.fixtures.ts` lines 177, 216, 530, 539 (each says "Reuse Phase 1's ...Case vessels directly"): rewrite each "Phase 1's" to "this codebase's" (e.g. "Genuine head-on: reuse this codebase's headOnCase vessels directly").
7. `src/domain/colregs/resolve-doubt-geometry.ts` lines 1-18 (opening JSDoc: "(RSON-03)... Resolves 04-RESEARCH.md's Open Question 2 / Pitfall 3: given a doubt-flagged classification's `DoubtBoundary`, ChartPanel (Wave 2/3) needs to know..."): rewrite "Resolves 04-RESEARCH.md's Open Question 2 / Pitfall 3" to "Resolves an open design question"; rewrite "ChartPanel (Wave 2/3)" to "ChartPanel". Keep `RSON-03` (live REQ-ID, verify against REQUIREMENTS.md) and `D-04` (decision ID, out of scope) unchanged.
8. `src/domain/colregs/resolve-doubt-geometry.ts` line 64 (`// encounter.ts's own documented WR-02 tie-break style.`): rewrite "documented WR-02 tie-break style" to "documented bOvertakesA/aOvertakesB tie-break style".
9. `src/domain/colregs/resolve-doubt-geometry.fixtures.ts` lines 1-8 (opening JSDoc, `(RSON-03)`): keep `RSON-03` (live REQ-ID); no other stale content in this block. Line 110 (`// convention (mirrors classify-encounter.ts's own WR-02-style tie-break`): rewrite "WR-02-style" to "bOvertakesA/aOvertakesB-simultaneity-style".
10. `src/domain/colregs/types.ts`: lines 1-9 (opening JSDoc, "Domain type contracts for Phase 2's COLREGS rules engine (D-11-D-16)... per 02-RESEARCH.md's Standard Stack note... `classify-encounter.ts` (Plan 02) imports all six of these directly."): rewrite to "Domain type contracts for this project's COLREGS rules engine (D-11-D-16)... per this project's Standard Stack decision to keep domain output types free of Zod runtime validation... `classify-encounter.ts` imports all six of these directly." Lines 29-33 (`(D-12) -- richer than a single generic "doubt" boolean, since Phase 4's UI needs to know...`): rewrite "Phase 4's UI" to "the Sandbox UI". Lines 38-43 (`...that were matched/ruled-out are bundled together, so Phase 4 can render...`): rewrite "Phase 4 can render" to "the Sandbox chart can render". Lines 60-72 (`The full output of \`classifyEncounter()\` (Plan 02)...` and later `...\`giveWay\`/\`standOn\` ARE populated (Plan 02's Stage 6 override)`): drop both `(Plan 02)` parentheticals -- rewrite the first to just "The full output of `classifyEncounter()`." and the second to "ARE populated (via `classifyEncounter()`'s own Stage 6 Rule-18 override)."
11. `src/domain/colregs/types.ts` lines 11-17 (`(Rules 12-15). Deliberately three values, not four (D-11)...`): leave this block's text completely unchanged. The only regex-style match here is "12-15", which is the COLREGS rule-number range "Rules 12-15" (legitimate domain content), not a Phase/Plan reference -- this is a documented false positive, not a genuine stale ID (see plan 13-08's audit note). Do not edit this block.
12. `src/domain/colregs/classify-encounter.fixtures.ts` line 358 (`// --- Task 2: Rule 18 interaction matrix + Stage 0 propagation -----------`): this is a bare section-header comment, not a stale ID citation with surrounding WHY prose -- rewrite to `// --- Rule 18 interaction matrix + Stage 0 propagation -----------` (drop "Task 2:", keep the descriptive label).
  </action>
  <verify>
    <automated>npx vitest run src/domain/colregs/classify-encounter.test.ts src/domain/colregs/resolve-doubt-geometry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'Phase [0-9]|Plan 0[0-9]|WR-0[0-9]|CR-0[0-9]|CONTEXT\.md|RESEARCH\.md|Wave [0-9]|Task [0-9]' src/domain/colregs/classify-encounter.ts src/domain/colregs/classify-encounter.fixtures.ts src/domain/colregs/resolve-doubt-geometry.ts src/domain/colregs/resolve-doubt-geometry.fixtures.ts src/domain/colregs/types.ts` returns no matches
    - `grep -n 'Rules 12-15' src/domain/colregs/types.ts` still returns exactly 1 match (confirms the legitimate domain content at lines 11-17 was left untouched)
    - `npx vitest run src/domain/colregs/classify-encounter.test.ts src/domain/colregs/resolve-doubt-geometry.test.ts` passes with the same pass count as before this task
  </acceptance_criteria>
  <done>All 5 files' listed comment locations rewritten (except the deliberately-untouched types.ts lines 11-17), including the bare "Task 2:" section-header comment in classify-encounter.fixtures.ts line 358; targeted greps clean; classify-encounter.test.ts and resolve-doubt-geometry.test.ts still pass with unchanged pass counts.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite stale comments in risk-of-collision.ts, risk-of-collision.fixtures.ts, risk-of-collision.test.ts, vessel-priority.ts, vessel-priority.fixtures.ts</name>
  <files>src/domain/colregs/risk-of-collision.ts, src/domain/colregs/risk-of-collision.fixtures.ts, src/domain/colregs/risk-of-collision.test.ts, src/domain/colregs/vessel-priority.ts, src/domain/colregs/vessel-priority.fixtures.ts</files>
  <read_first>
    src/domain/colregs/risk-of-collision.ts
    src/domain/colregs/risk-of-collision.fixtures.ts
    src/domain/colregs/risk-of-collision.test.ts
    src/domain/colregs/vessel-priority.ts
    src/domain/colregs/vessel-priority.fixtures.ts
  </read_first>
  <action>
1. `src/domain/colregs/risk-of-collision.ts` lines 3-13 (opening JSDoc: "Rule 7 gate (CLAS-03, D-05-D-08)... Consumes the `Result&lt;{ tcpaMinutes, dcpaNm }&gt;` produced by Phase 1's `cpa()`..."): rewrite "produced by Phase 1's `cpa()`" to "produced by this codebase's own `cpa()`". Keep `CLAS-03` (live REQ-ID, verify) and `D-05`-`D-08` (decision IDs, out of scope).
2. `src/domain/colregs/risk-of-collision.fixtures.ts` line 8 (`// Re-export Phase 1's cpa.fixtures.ts cases directly (RESEARCH.md: "Phase 2` continuing to line 9 `// can reuse these as known-correct starting inputs" -- do not re-derive`): rewrite to "// Re-export cpa.fixtures.ts's own cases directly -- this rules-engine layer can reuse these as known-correct starting inputs; do not re-derive". Lines 13-28 (JSDoc block: "`riskOfCollision()`'s own boundary logic (D-05, D-08) is the unit under test here, not `cpa()`'s trigonometry (already fixture-tested in Phase 1 via `toBeCloseTo`)."): rewrite "already fixture-tested in Phase 1 via" to "already fixture-tested elsewhere in this codebase via".
3. `src/domain/colregs/risk-of-collision.test.ts` line 21 (`// (toBeCloseTo, per Phase 1's float-comparison convention) before`): rewrite "per Phase 1's float-comparison convention" to "per this codebase's own float-comparison convention".
4. `src/domain/colregs/vessel-priority.ts` lines 3-14 (opening JSDoc: "Rule 18 vessel-type priority hierarchy (DETM-02)... NUC and RIATM are each named as vessels that others must keep clear of, with no rule text ranking one above the other (Phase 1's D-11: these are legally distinct, co-equal-priority statuses, not aliases)."): rewrite "(Phase 1's D-11:" to "(D-11:". Keep `DETM-02` (live REQ-ID, verify) and `D-11` (decision ID, out of scope).
5. `src/domain/colregs/vessel-priority.ts` line 16 (`// quotes) intentionally match this plan's exact acceptance-criteria grep`): rewrite "this plan's exact acceptance-criteria grep pattern" to "this module's own co-equal-priority test-fixture grep pattern" (keep the reasoning that single-quoted literals are intentional and grep-matched, drop the plan-artifact framing).
6. `src/domain/colregs/vessel-priority.fixtures.ts` lines 3-8 (opening JSDoc: "Rule 18 priority-tier and tie-break fixtures (DETM-02)... covers each `rule18Overrides()` behavior case from CONTEXT.md's "Claude's Discretion" note on tie-break mechanics."): rewrite "from CONTEXT.md's "Claude's Discretion" note on tie-break mechanics" to "left to engineering discretion during this module's design: how to break a tie when two different vessel types are equally prioritized". Keep `DETM-02` (live REQ-ID, verify).
  </action>
  <verify>
    <automated>npx vitest run src/domain/colregs/risk-of-collision.test.ts src/domain/colregs/vessel-priority.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'Phase [0-9]|this plan|RESEARCH\.md|CONTEXT\.md' src/domain/colregs/risk-of-collision.ts src/domain/colregs/risk-of-collision.fixtures.ts src/domain/colregs/risk-of-collision.test.ts src/domain/colregs/vessel-priority.ts src/domain/colregs/vessel-priority.fixtures.ts` returns no matches
    - `npx vitest run src/domain/colregs/risk-of-collision.test.ts src/domain/colregs/vessel-priority.test.ts` passes with the same pass count as before this task
  </acceptance_criteria>
  <done>All 5 files' listed comment locations rewritten; targeted greps clean; both test files still pass with unchanged pass counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | Comment-only edits inside the domain rules-engine layer. No new input crosses any boundary, no dependency added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Tampering | Accidental logic edit to classify-encounter.ts's Stage 3/4/5 dispatch while touching adjacent WR-01/02/03 comments | mitigate | Task 1's acceptance criteria require classify-encounter.test.ts to pass with the *same* pass count; this suite includes the exact WR-01/WR-02/WR-03/CR-01 regression cases these comments document, so any accidental logic drift is caught immediately. |
| T-13-04 | N/A | npm/pip/cargo installs | accept | No package installs in this phase; not applicable. |
</threat_model>

<verification>
Run `npx vitest run src/domain/colregs` and confirm the full pass count matches the pre-phase baseline. Run `git diff -- src/domain/colregs` and confirm every changed hunk only touches comment lines.
</verification>

<success_criteria>
All 10 files' listed stale comment locations are rewritten with the rotting reference removed and substantive WHY content (including every regression-test rationale) fully retained; zero test or behavior change; the one documented false-positive block in types.ts is left untouched.
</success_criteria>

<output>
Create `.planning/phases/13-comment-cleanup/13-02-SUMMARY.md` when done
</output>
