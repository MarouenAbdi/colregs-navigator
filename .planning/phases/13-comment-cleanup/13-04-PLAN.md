---
phase: 13-comment-cleanup
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sandbox/ChartPanel.tsx
  - src/components/sandbox/ChartPanel.test.tsx
  - src/components/sandbox/chart-panel-geometry.ts
  - src/components/sandbox/ControlPanel.tsx
  - src/components/sandbox/ControlPanel.test.tsx
  - src/components/sandbox/CopyLinkButton.tsx
  - src/components/sandbox/InstrumentReadouts.tsx
  - src/components/sandbox/InstrumentReadouts.test.tsx
  - src/components/sandbox/ReasoningTrail.tsx
  - src/components/sandbox/ReasoningTrail.test.tsx
  - src/components/sandbox/VerdictBanner.tsx
  - src/components/sandbox/VerdictBanner.test.tsx
autonomous: true
requirements: [CMNT-01]

must_haves:
  truths:
    - "None of the 12 files in this Sandbox-cluster-A set contain a Phase N / Plan N / Task N / bare-doc-filename comment reference after this plan lands"
    - "The one genuine Tailwind-color-shade false positive in chart-panel-geometry.ts is left untouched, not misidentified as a stale ID"
  artifacts:
    - path: "src/components/sandbox/ChartPanel.tsx"
      provides: "Rewritten header comment, zero change to hull-drag/rotate-drag wiring"
  key_links: []
---

<objective>
Rewrite every stale Phase/Plan/REQ-ID/Task-N comment reference in the ChartPanel/ControlPanel/InstrumentReadouts/ReasoningTrail/VerdictBanner cluster (both source and test files) by hand, per CMNT-01. Preserve every comment's WHY content. No change to rendering, drag/rotate gesture wiring, or classification-derived UI state.

This plan folds in `InstrumentReadouts.test.tsx`, `ReasoningTrail.test.tsx`, and `VerdictBanner.test.tsx` -- none of these 3 test files were in the original 54-file ESLint-flagged list, because their stale `(08-04 Task N)` header comments don't match the strict `local/no-stale-id-comments` regex shape (a bare `NN-NN` pair followed by a word, not `)`/`,`). Confirmed via a broader re-grep per CMNT-02.

Purpose: retroactively enforce the comment convention across the Sandbox's reasoning-display components.
Output: Updated comments in the 12 files listed above; no other diff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite stale comments in ChartPanel.tsx, ChartPanel.test.tsx, chart-panel-geometry.ts, ControlPanel.tsx, ControlPanel.test.tsx, CopyLinkButton.tsx</name>
  <files>src/components/sandbox/ChartPanel.tsx, src/components/sandbox/ChartPanel.test.tsx, src/components/sandbox/chart-panel-geometry.ts, src/components/sandbox/ControlPanel.tsx, src/components/sandbox/ControlPanel.test.tsx, src/components/sandbox/CopyLinkButton.tsx</files>
  <read_first>
    src/components/sandbox/ChartPanel.tsx
    src/components/sandbox/ChartPanel.test.tsx
    src/components/sandbox/chart-panel-geometry.ts
    src/components/sandbox/ControlPanel.tsx
    src/components/sandbox/ControlPanel.test.tsx
    src/components/sandbox/CopyLinkButton.tsx
  </read_first>
  <action>
1. `src/components/sandbox/ChartPanel.tsx` lines 3-16 (opening JSDoc: "the SVG chart surface (CHRT-01/CHRT-02) rendering both vessels (hull color/badge per D-02)... Also renders the relative-bearing line and the overtaking-boundary reference cones (RSON-03), and wires the hull-drag (position) and rotate-handle-drag (heading) gestures (VESL-02/D-01) onto their SVG hit-targets."): keep `CHRT-01`, `CHRT-02`, `RSON-03`, `VESL-02` (live REQ-IDs, verify each against `.planning/REQUIREMENTS.md`) and `D-02`/`D-01` (decision IDs, out of scope); no stale content in this block beyond REQ/Decision IDs that are explicitly out of scope -- leave unchanged. (Confirm via the acceptance criteria below that no OTHER pattern -- Phase/Plan/this-phase/doc-filename -- appears here.)
2. `src/components/sandbox/ChartPanel.test.tsx` lines 2-12 (opening JSDoc: "ChartPanel render + doubt-overlay tests (04-03 Task 3)... jsdom's missing ResizeObserver/Pointer-Capture APIs are polyfilled..."; also references `04-RESEARCH.md`): rewrite "(04-03 Task 3)" to remove the task tag entirely -- e.g. "ChartPanel render + doubt-overlay tests." Rewrite any `04-RESEARCH.md` mention in this block to describe the reason directly (e.g. "per this project's own jsdom-polyfill pattern for ResizeObserver/Pointer-Capture, used consistently across every Sandbox component test").
3. `src/components/sandbox/ChartPanel.test.tsx` line 85 (`// Regression test for a real hit-testing dead zone (code review CR-01):`): rewrite to "// Regression test for a real hit-testing dead zone found via manual code review:".
4. `src/components/sandbox/chart-panel-geometry.ts` line 11 (`// Fixed chart-space viewBox for this phase: 20nm x 20nm centered on the`): rewrite "for this phase" to "for the Sandbox chart".
5. `src/components/sandbox/chart-panel-geometry.ts` lines 37, 56 (`(04-HUMAN-UAT.md Gap 1 follow-up: ...)` and `(04-HUMAN-UAT.md Gap 1)`): rewrite both to describe the underlying finding directly instead of citing the archived UAT doc, e.g. "(a real usability finding: separating two invisible padded hit-shapes by a numeric margin still left drag/rotate feeling imprecise...)" for line 37, and "(the same usability finding that motivated the bigger, visible-shape-only hit-targets above)" for line 56.
6. `src/components/sandbox/chart-panel-geometry.ts` line 105 (`// (04-RESEARCH.md Pattern 3). \`startBearingDeg\`/\`endBearingDeg\` are TRUE`): rewrite "(04-RESEARCH.md Pattern 3)" to "(the overtaking-boundary reference-cone pattern used throughout this component)".
7. `src/components/sandbox/chart-panel-geometry.ts` lines 17, 34, 93, 94, 95: **DO NOT EDIT.** These lines contain Tailwind/hex color-shade names (`slate-200`, `zinc-700`, `slate-600`, `amber-500`, `slate-300`) that happen to match the ESLint rule's `[A-Z]{2,6}-\d{2,3}` shape case-insensitively -- they are not stale Phase/Plan/REQ-ID references. Confirm each is a color name, not a rewrite target, before moving on.
8. `src/components/sandbox/ControlPanel.tsx` lines 102-105 (`{/* Only element in this restyled section allowed to use --primary, per 08-UI-SPEC.md's Color section -- every other`): rewrite "per 08-UI-SPEC.md's Color section" to "per this component's own color-token convention".
9. `src/components/sandbox/ControlPanel.tsx` line 12 (`// D-12: display labels are UI-only presentation strings...`): keep `D-12` (decision ID, out of scope); no other stale content, leave unchanged.
10. `src/components/sandbox/ControlPanel.tsx` line 71 (`// getVesselRole() (08-01 widened the prop contract).`) -- NOT in the original ESLint-flagged list (misses the strict pattern's lookahead shape), found via broader re-grep per CMNT-02: rewrite to "// getVesselRole() (this component's prop contract was widened to accept the full classification, not just the two vessels)."
11. `src/components/sandbox/ControlPanel.test.tsx` lines 2-8 (opening JSDoc: "rewritten for the Radix Select/Slider primitive swap (SBOX-04)... per 08-RESEARCH.md's "Test rewrite pattern" Code Example."): rewrite "per 08-RESEARCH.md's "Test rewrite pattern" Code Example" to "using the click+findByRole / focus+keyboard interaction pattern Radix's own Select/Slider primitives require". Keep `SBOX-04` (live REQ-ID, verify).
12. `src/components/sandbox/CopyLinkButton.tsx` lines 3-8 (opening JSDoc: "Restyled to an icon-only outline button matching the rest of Phase 8's dark palette..."): rewrite "Phase 8's dark palette" to "the Sandbox's dark palette".
  </action>
  <verify>
    <automated>npx vitest run src/components/sandbox/ChartPanel.test.tsx src/components/sandbox/ControlPanel.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'Phase [0-9]|Task [0-9]|RESEARCH\.md|UI-SPEC\.md|HUMAN-UAT\.md|code review CR-0[0-9]' src/components/sandbox/ChartPanel.tsx src/components/sandbox/ChartPanel.test.tsx src/components/sandbox/chart-panel-geometry.ts src/components/sandbox/ControlPanel.tsx src/components/sandbox/ControlPanel.test.tsx src/components/sandbox/CopyLinkButton.tsx` returns no matches
    - `grep -n 'slate-200\|zinc-700\|slate-600\|amber-500\|slate-300' src/components/sandbox/chart-panel-geometry.ts` still returns the same 5 matches as before this task (confirms the color-name false positives were left untouched)
    - `npx vitest run src/components/sandbox/ChartPanel.test.tsx src/components/sandbox/ControlPanel.test.tsx` passes with the same pass count as before this task
  </acceptance_criteria>
  <done>All 6 files' listed comment locations rewritten (except the deliberately-untouched color-name lines); targeted greps clean; both test files pass with unchanged pass counts.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite stale comments in InstrumentReadouts.tsx/.test.tsx, ReasoningTrail.tsx/.test.tsx, VerdictBanner.tsx/.test.tsx</name>
  <files>src/components/sandbox/InstrumentReadouts.tsx, src/components/sandbox/InstrumentReadouts.test.tsx, src/components/sandbox/ReasoningTrail.tsx, src/components/sandbox/ReasoningTrail.test.tsx, src/components/sandbox/VerdictBanner.tsx, src/components/sandbox/VerdictBanner.test.tsx</files>
  <read_first>
    src/components/sandbox/InstrumentReadouts.tsx
    src/components/sandbox/InstrumentReadouts.test.tsx
    src/components/sandbox/ReasoningTrail.tsx
    src/components/sandbox/ReasoningTrail.test.tsx
    src/components/sandbox/VerdictBanner.tsx
    src/components/sandbox/VerdictBanner.test.tsx
  </read_first>
  <action>
1. `src/components/sandbox/InstrumentReadouts.tsx` lines 1-8 (opening JSDoc: "the status pill, both inside one `Card` (08-UI-SPEC.md: "the status pill is NOT a separate card")..."): rewrite "(08-UI-SPEC.md: "the status pill is NOT a separate card")" to "(the status pill is deliberately NOT a separate card, per the design)". Keep `D-05` (decision ID, out of scope).
2. `src/components/sandbox/InstrumentReadouts.tsx` line 33 (`// stand-on badge shade, per 08-UI-SPEC.md's Status Pill color table (plain`): rewrite "per 08-UI-SPEC.md's Status Pill color table" to "per this component's own status-pill color-token convention".
3. `src/components/sandbox/InstrumentReadouts.test.tsx` lines 2-8 (opening JSDoc: "InstrumentReadouts tests (08-04 Task 2). Verifies the 4-tile...") -- NOT in the original ESLint-flagged list, found via broader re-grep per CMNT-02: rewrite "(08-04 Task 2)" out entirely -- e.g. "InstrumentReadouts tests. Verifies the 4-tile Range/Bearing/CPA/TCPA grid + status pill...".
4. `src/components/sandbox/InstrumentReadouts.test.tsx` line 30 (`// Coincident position AND matching heading/speed -- per 08-01's own`) -- NOT in the original ESLint-flagged list, found via broader re-grep: rewrite "per 08-01's own degenerate-fixture correction" to "per this codebase's own degenerate-fixture correction".
5. `src/components/sandbox/ReasoningTrail.tsx` lines 1-9 (opening JSDoc: "the numbered reasoning-trail card (SBOX-03): renders..."): keep `SBOX-03` (live REQ-ID, verify); no other stale content, leave unchanged.
6. `src/components/sandbox/ReasoningTrail.test.tsx` lines 2-9 (opening JSDoc: "ReasoningTrail tests (08-04 Task 3). Verifies dynamic step count/order...") -- NOT in the original ESLint-flagged list, found via broader re-grep: rewrite "(08-04 Task 3)" out entirely -- e.g. "ReasoningTrail tests. Verifies dynamic step count/order, tag+tone derivation for all 3 tone buckets...".
7. `src/components/sandbox/VerdictBanner.tsx` lines 1-7 (opening JSDoc: "the full-width verdict card (SBOX-02)... so the design's exact card boundary can be expressed directly (08-UI-SPEC.md Layout: "verdict banner ... is not nested inside the reasoning column")."): rewrite "(08-UI-SPEC.md Layout: "verdict banner ... is not nested inside the reasoning column")" to "(the design places the verdict banner as its own top-level card, not nested inside the reasoning column)". Keep `SBOX-02` (live REQ-ID, verify).
8. `src/components/sandbox/VerdictBanner.test.tsx` lines 2-10 (opening JSDoc: "VerdictBanner tests (08-04 Task 1). Verifies the 3 behavior cases...") -- NOT in the original ESLint-flagged list, found via broader re-grep: rewrite "(08-04 Task 1)" out entirely -- e.g. "VerdictBanner tests. Verifies the 3 behavior cases split out of the former single combined reasoning aside...".
  </action>
  <verify>
    <automated>npx vitest run src/components/sandbox/InstrumentReadouts.test.tsx src/components/sandbox/ReasoningTrail.test.tsx src/components/sandbox/VerdictBanner.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'Task [0-9]|UI-SPEC\.md' src/components/sandbox/InstrumentReadouts.tsx src/components/sandbox/InstrumentReadouts.test.tsx src/components/sandbox/ReasoningTrail.tsx src/components/sandbox/ReasoningTrail.test.tsx src/components/sandbox/VerdictBanner.tsx src/components/sandbox/VerdictBanner.test.tsx` returns no matches
    - `SBOX-02`, `SBOX-03`, `D-05` citations remain present verbatim where noted above
    - `npx vitest run src/components/sandbox/InstrumentReadouts.test.tsx src/components/sandbox/ReasoningTrail.test.tsx src/components/sandbox/VerdictBanner.test.tsx` passes with the same pass count as before this task
  </acceptance_criteria>
  <done>All 6 files' listed comment locations rewritten; targeted greps clean; all 3 test files pass with unchanged pass counts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None | Comment-only edits to Sandbox reasoning-display components. No new input crosses any boundary, no dependency added. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Tampering | Accidental logic edit to ChartPanel.tsx's drag-gesture wiring while touching adjacent comments | mitigate | Task acceptance criteria require the relevant `.test.tsx` suites to pass with the *same* pass count; ChartPanel.test.tsx includes the hull/rotate hit-testing regression check. |
| T-13-08 | N/A | npm/pip/cargo installs | accept | No package installs in this phase; not applicable. |
</threat_model>

<verification>
Run `npx vitest run src/components/sandbox/ChartPanel.test.tsx src/components/sandbox/ControlPanel.test.tsx src/components/sandbox/InstrumentReadouts.test.tsx src/components/sandbox/ReasoningTrail.test.tsx src/components/sandbox/VerdictBanner.test.tsx` and confirm the combined pass count matches the pre-phase baseline. Run `git diff -- src/components/sandbox/ChartPanel.tsx src/components/sandbox/ChartPanel.test.tsx src/components/sandbox/chart-panel-geometry.ts src/components/sandbox/ControlPanel.tsx src/components/sandbox/ControlPanel.test.tsx src/components/sandbox/CopyLinkButton.tsx src/components/sandbox/InstrumentReadouts.tsx src/components/sandbox/InstrumentReadouts.test.tsx src/components/sandbox/ReasoningTrail.tsx src/components/sandbox/ReasoningTrail.test.tsx src/components/sandbox/VerdictBanner.tsx src/components/sandbox/VerdictBanner.test.tsx` and confirm every changed hunk only touches comment lines.
</verification>

<success_criteria>
All 12 files' listed stale comment locations are rewritten with the rotting reference removed and substantive WHY content fully retained; the color-name false positives in chart-panel-geometry.ts are left untouched; zero test or behavior change.
</success_criteria>

<output>
Create `.planning/phases/13-comment-cleanup/13-04-SUMMARY.md` when done
</output>
