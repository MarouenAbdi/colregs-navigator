---
phase: 15-deploy-verify
plan: 04
type: execute
wave: 3
depends_on: ["15-03"]
files_modified: [README.md]
autonomous: true
requirements: [HEALTH-04]

must_haves:
  truths:
    - "A documented rollback procedure exists in README.md describing the exact vercel rollback / vercel promote CLI commands"
    - "A live rollback from the newest production deployment to the prior production deployment was actually exercised via the Vercel CLI and confirmed to reassign the production alias with no rebuild"
    - "The production alias was restored forward to the newest deployment afterward, leaving the live site in its intended final state"
  artifacts:
    - path: "README.md"
      provides: "Rollback Procedure subsection under Deployment"
      contains: "vercel rollback"
  key_links:
    - from: "README.md"
      to: "Vercel CLI rollback/promote commands"
      via: "documented runbook"
      pattern: "vercel rollback"
---

<objective>
Live-exercise HEALTH-04's rollback path: push a trivial, docs-only commit to create a second
production deployment, roll back to the prior one via `vercel rollback`, verify the
alias-only (no-rebuild) semantics hold, then restore forward state via `vercel promote`.
Document the exact runbook in README.md.

Purpose: CONTEXT.md left "actually exercise vs. just document" as an open discretion point;
RESEARCH.md resolves it -- rolling back to an already-production deployment is Vercel's
Instant Rollback (alias reassignment only, never a rebuild, never a migration re-run), which
makes a live exercise cheap and safe rather than something to fake or skip.

Output: An updated `README.md` `### Rollback Procedure` subsection, plus a live-verified
rollback -> promote cycle against the real Vercel project.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-deploy-verify/15-CONTEXT.md
@.planning/phases/15-deploy-verify/15-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document the rollback runbook and create a second production deployment</name>
  <files>README.md</files>
  <read_first>README.md (Deployment section as updated by Plan 15-03), .planning/phases/15-deploy-verify/15-RESEARCH.md (Pitfall 5, Pitfall 6, Code Examples "Rollback verification cycle")</read_first>
  <action>
    Record the current production deployment's ID/URL (from Plan 15-03 Task 1's `vercel ls`
    output) as deployment A -- the baseline to roll back to. Add a new
    `### Rollback Procedure` subsection under README.md's `## Deployment` section,
    documenting: rolling back to a deployment that has already served production traffic is
    Vercel's "Instant Rollback" -- an alias-only reassignment with no rebuild and no
    migration re-run (RESEARCH.md Pitfall 5/6); the exact commands
    (`npx vercel rollback DEPLOYMENT_ID --token "$VERCEL_TOKEN"` to roll back,
    `npx vercel promote DEPLOYMENT_ID --token "$VERCEL_TOKEN"` to restore forward); the
    caveat that a rollback restores that deployment's build-time env var snapshot, so any env
    vars changed since should be reviewed after a real incident rollback (Pitfall 6). Commit
    this README change with a conventional commit message (e.g.
    `docs(15): document verified rollback procedure`) and push to `main` -- this trivial,
    docs-only commit is what creates deployment B (the second production deployment needed
    to exercise a real rollback). Wait for Vercel's native Git integration to build and
    deploy this commit, then run `npx vercel ls --token "$VERCEL_TOKEN"` and confirm a new
    deployment reaches `READY` and is now the production alias -- record its ID as
    deployment B.
  </action>
  <verify>
    <automated>git push origin main && npx vercel ls --token "$VERCEL_TOKEN" | grep -c READY</automated>
  </verify>
  <acceptance_criteria>
    README.md contains a new `### Rollback Procedure` heading documenting the `vercel
    rollback`/`vercel promote` commands.
    The docs commit is pushed to `main` and a new deployment (deployment B) reaches `READY`
    per `vercel ls`.
    Deployment B's ID differs from deployment A's ID.
  </acceptance_criteria>
  <done>A real second production deployment (B) exists, and the rollback procedure is documented in README.md.</done>
</task>

<task type="auto">
  <name>Task 2: Execute the live rollback to deployment A and verify no rebuild occurred</name>
  <files>N/A -- CLI verification only, no file changes in this task</files>
  <read_first>.planning/phases/15-deploy-verify/15-RESEARCH.md (Pitfall 5: rollback must target a deployment that already served production traffic)</read_first>
  <action>
    Run `npx vercel rollback DEPLOYMENT_A_ID --token "$VERCEL_TOKEN"` -- deployment A already
    served production traffic before deployment B replaced it (per Task 1), so this is
    unambiguously the no-rebuild Instant Rollback path, not a first-time preview promotion.
    Immediately run `npx vercel inspect DEPLOYMENT_A_ID --token "$VERCEL_TOKEN"` and confirm
    its state is `READY` with no new "Building" timestamp (proving no rebuild occurred). Run
    `npx vercel ls --token "$VERCEL_TOKEN"` and confirm the production alias now points to
    deployment A's ID, not B's. Run
    `curl -s https://PRODUCTION_URL/api/health` and confirm it still returns `200
    {"status":"ok"}` (the health check behaves identically on the rolled-back deployment,
    since the route handler's code is unchanged between A and B).
  </action>
  <verify>
    <automated>npx vercel rollback "$DEPLOYMENT_A_ID" --token "$VERCEL_TOKEN" --yes && npx vercel ls --token "$VERCEL_TOKEN" | grep -c "$DEPLOYMENT_A_ID"</automated>
  </verify>
  <acceptance_criteria>
    `vercel inspect DEPLOYMENT_A_ID` shows state `READY` with no rebuild triggered.
    `vercel ls`'s production alias now resolves to deployment A's ID.
    `curl /api/health` against the production URL still returns 200 `{"status":"ok"}`
    immediately after the rollback.
  </acceptance_criteria>
  <done>The production alias was successfully rolled back to the prior deployment via a single CLI command, with no rebuild and no migration re-run, confirming HEALTH-04's rollback path works live.</done>
</task>

<task type="auto">
  <name>Task 3: Restore forward state to deployment B</name>
  <files>N/A -- CLI verification only, no file changes in this task</files>
  <read_first>N/A -- context established by Task 1/Task 2 of this plan</read_first>
  <action>
    Run `npx vercel promote DEPLOYMENT_B_ID --token "$VERCEL_TOKEN"` to reassign the
    production alias back to deployment B (the newest deployment, containing the rollback
    documentation) -- deployment B also already served production traffic momentarily
    before the rollback in Task 2, so this promote is also an Instant Rollback (alias-only,
    no rebuild), per the same Pitfall 5 reasoning. Run
    `npx vercel ls --token "$VERCEL_TOKEN"` and confirm the production alias now resolves to
    deployment B's ID again, restoring the intended final live state.
  </action>
  <verify>
    <automated>npx vercel promote "$DEPLOYMENT_B_ID" --token "$VERCEL_TOKEN" --yes && npx vercel ls --token "$VERCEL_TOKEN" | grep -c "$DEPLOYMENT_B_ID"</automated>
  </verify>
  <acceptance_criteria>
    `vercel ls`'s production alias resolves to deployment B's ID after the promote.
    `vercel inspect DEPLOYMENT_B_ID` shows no new "Building" state (confirms no rebuild
    during the restore-forward step either).
  </acceptance_criteria>
  <done>The live site is left in its intended final state (deployment B, containing the documented rollback runbook), with the full rollback cycle demonstrated end-to-end via CLI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude's execution shell (`VERCEL_TOKEN`) -> Vercel's deployment-alias-mutating CLI surface | The only plan in this phase that runs a destructive/state-changing action (rollback, promote) against the live production project |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-09 | Elevation of Privilege / Tampering | `vercel rollback`/`vercel promote` invoked against the wrong deployment ID or wrong project | mitigate | Every rollback/promote command in this plan targets a deployment ID explicitly recorded and cross-checked via `vercel ls`/`vercel inspect` output in the immediately preceding task (never a bare "latest"/interactive default), against the same project confirmed in Plan 15-03 Task 1 |
| T-15-10 | Tampering | Confusing a first-time Preview-to-Production promotion (full rebuild) with an Instant Rollback (no rebuild) | mitigate | Both deployments A and B are explicitly confirmed to have already served production traffic (via `vercel ls`) before any rollback/promote command targets them, per RESEARCH.md Pitfall 5 -- acceptance criteria check for the absence of a new "Building" state after each action |
| T-15-11 | Information Disclosure | Env var snapshot restored by rollback silently differing from current config | accept | Documented in the README runbook (Pitfall 6) as a known behavior to review during a real future incident rollback; not a practical risk in this verification cycle since no env vars change between deployment A and B |
</threat_model>

<verification>
A live deploy -> rollback -> promote cycle was exercised via the Vercel CLI against two real
production deployments, with each step's alias-only (no-rebuild) semantics explicitly
confirmed, and the live site restored to its intended final state.
</verification>

<success_criteria>
HEALTH-04 is confirmed with a real, live-exercised rollback -> restore cycle (not just a
documented mechanism), and README.md's Rollback Procedure subsection accurately describes
the exact commands used.
</success_criteria>

<output>
Create `.planning/phases/15-deploy-verify/15-04-SUMMARY.md` when done
</output>
