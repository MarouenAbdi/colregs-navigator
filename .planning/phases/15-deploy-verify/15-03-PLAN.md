---
phase: 15-deploy-verify
plan: 03
type: execute
wave: 2
depends_on: ["15-01", "15-02"]
files_modified: [README.md]
autonomous: true
requirements: [DEPLOY-01, DEPLOY-02, DEPLOY-03, HEALTH-01, HEALTH-02]

must_haves:
  truths:
    - "npx vercel ls confirms a production deployment in READY state aliased to a real public Vercel URL"
    - "The production build log shows prisma generate, the gated migrate-if-production.mjs step running prisma migrate deploy successfully (no PgBouncer prepared-statement error), and a completed next build --webpack"
    - "npx vercel env ls confirms DATABASE_URL is configured in the Production environment (name only, value never printed)"
    - "A live curl against the production URL's root page returns 200, and against /api/health returns 200 {\"status\":\"ok\"} with a Cache-Control: no-store header"
    - "README.md's Deployment section states the real production URL and accurate, non-aspirational live status"
  artifacts:
    - path: "README.md"
      provides: "Accurate, live Deployment section"
      contains: "https://"
  key_links:
    - from: "README.md"
      to: "live Vercel production URL"
      via: "prose link in the Deployment section"
      pattern: "https://"
---

<objective>
Verify, entirely via the Vercel CLI (`npx vercel`, authenticated with the `VERCEL_TOKEN`
handed off in Plan 15-02), that the real production deployment created in Plan 15-02 is
correctly wired -- reachable, migrated, fully configured -- and that a real external request
against it succeeds end-to-end. Then update README.md to state the real, live status.

Purpose: DEPLOY-01/02/03 all require live confirmation, not an assumption that the manual
dashboard steps in Plan 15-02 worked. This is also the first time RESEARCH.md's two highest
first-deploy risks (a PgBouncer/pooled-connection-string migration failure, and a
Turbopack/webpack build-command mismatch) get checked against a real build log rather than
a local stand-in.

Output: Confirmed-live DEPLOY-01/02/03 and HEALTH-01/02, plus an updated `README.md`
Deployment section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-deploy-verify/15-CONTEXT.md
@.planning/phases/15-deploy-verify/15-RESEARCH.md
@package.json
@scripts/migrate-if-production.mjs
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Link the Vercel project and confirm deployment/build/env-var state</name>
  <files>N/A -- CLI verification only, no file changes in this task</files>
  <read_first>.planning/phases/15-deploy-verify/15-RESEARCH.md (Code Examples: CLI auth/linking; Common Pitfalls 1/3/4/5; Security Domain table), package.json (vercel-build script), scripts/migrate-if-production.mjs</read_first>
  <action>
    Confirm `$VERCEL_TOKEN` is set in the shell (handed off in Plan 15-02), then run
    `npx vercel whoami --token "$VERCEL_TOKEN"` to confirm authentication succeeds. Run
    `npx vercel link --yes --token "$VERCEL_TOKEN"` from the repo root to write
    `.vercel/project.json`; open that file and confirm its `projectId`/`orgId` correspond to
    the project the user created in Plan 15-02 -- cross-check the project name shown in
    `npx vercel ls --token "$VERCEL_TOKEN"` output against the project the user reported.
    This confirms every subsequent command targets the correct project (this plan's threat
    model). Run `npx vercel ls --token "$VERCEL_TOKEN"` and confirm exactly one deployment
    shows state `READY` and is aliased to the production domain the user reported. Capture
    that production domain into a real exported shell variable so downstream commands (in
    this task and Task 2) are directly runnable rather than requiring manual substitution:
    `export PROD_URL=$(npx vercel ls --token "$VERCEL_TOKEN" | awk '/READY/ && /Production/ {print $2; exit}')`
    (adjust the `awk` column selection to match this Vercel CLI version's actual `vercel ls`
    table layout, confirmed by first running the bare command and inspecting its columns --
    `PROD_URL` must end up holding the bare hostname, e.g. `colregs-navigator.vercel.app`,
    with no `https://` prefix, so it composes cleanly into `https://$PROD_URL/...` in Task 2).
    Echo `$PROD_URL` to confirm it is non-empty before proceeding. Run
    `npx vercel inspect $PROD_URL --token "$VERCEL_TOKEN" --logs` and confirm the build log
    contains, in order: a `prisma generate` invocation, the `migrate-if-production.mjs`
    script running `prisma migrate deploy` successfully -- either applying a migration or
    reporting the schema already up to date -- with no error text containing
    `prepared statement` or `PgBouncer` (closes RESEARCH.md Pitfall 1), and a successful
    `next build --webpack` completion (webpack, not Turbopack -- Pitfall 3 / STATE.md's
    flagged first-deploy risk). Run `npx vercel env ls --token "$VERCEL_TOKEN"` and confirm
    `DATABASE_URL` is listed under the Production scope -- never print, log, or otherwise
    surface the variable's value at any point (the command itself never does).
  </action>
  <verify>
    <automated>npx vercel whoami --token "$VERCEL_TOKEN" && npx vercel ls --token "$VERCEL_TOKEN" | grep -c READY && export PROD_URL=$(npx vercel ls --token "$VERCEL_TOKEN" | awk '/READY/ && /Production/ {print $2; exit}') && test -n "$PROD_URL" && echo "PROD_URL=$PROD_URL"</automated>
  </verify>
  <acceptance_criteria>
    `npx vercel whoami --token "$VERCEL_TOKEN"` exits 0 and prints an account identity.
    `.vercel/project.json`'s `projectId`/`orgId` match the project name the user reported in
    Plan 15-02.
    `npx vercel ls` output contains exactly one deployment in `READY` state aliased to the
    reported production domain, and that domain is captured into a real exported `PROD_URL`
    shell variable (non-empty, no `https://` prefix) usable verbatim by Task 2.
    `npx vercel inspect <deployment> --logs` output contains the substrings `prisma
    generate`, `migrate deploy`, and a webpack "Compiled" success line, and contains zero
    occurrences of `prepared statement` or `PgBouncer`.
    `npx vercel env ls` output contains `DATABASE_URL` under `Production`, and the captured
    output for this task contains zero occurrences of `postgresql://`.
  </acceptance_criteria>
  <done>DEPLOY-01/02/03 are confirmed live via Vercel CLI, targeting the verified correct project, with no secret value ever surfaced in command output, and `PROD_URL` exported for Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Real end-to-end live request against the production deployment</name>
  <files>N/A -- CLI/curl verification only, no file changes in this task</files>
  <read_first>app/api/health/route.ts (the deployed code), .planning/phases/15-deploy-verify/15-RESEARCH.md (System Architecture Diagram)</read_first>
  <action>
    Using `$PROD_URL` exported by Task 1 (re-export it in this task's shell if Task 1 ran in
    a separate session: `export PROD_URL=$(npx vercel ls --token "$VERCEL_TOKEN" | awk '/READY/ && /Production/ {print $2; exit}')`),
    run `curl -s -o /dev/null -w "%{http_code}" "https://$PROD_URL/"` and confirm it prints
    `200` (the real, deployed front-end loads -- HEALTH-02's "not just a green build log"
    requirement). Run `curl -s -i "https://$PROD_URL/api/health"` and confirm the response
    status line is `200`, the body is exactly `{"status":"ok"}`, and the response headers
    include `cache-control: no-store`. Run the same `/api/health` curl a second time
    immediately after and confirm neither response shows an `x-vercel-cache: HIT` (or
    equivalent stale-cache indicator) header -- proving D-08's no-store guarantee holds
    against Vercel's real edge network, not just locally (Plan 15-01).
  </action>
  <verify>
    <automated>test -n "$PROD_URL" || export PROD_URL=$(npx vercel ls --token "$VERCEL_TOKEN" | awk '/READY/ && /Production/ {print $2; exit}'); curl -s -o /dev/null -w "%{http_code}" "https://$PROD_URL/api/health"</automated>
  </verify>
  <acceptance_criteria>
    Root page curl returns HTTP 200.
    `/api/health` curl returns HTTP 200, body exactly `{"status":"ok"}`, header
    `cache-control: no-store`.
    Neither of two consecutive `/api/health` requests shows an `x-vercel-cache: HIT` header.
  </acceptance_criteria>
  <done>A real, external HTTP request against the live production deployment succeeds end-to-end, confirming both the app and the DB-backed health check work outside any local/CI environment.</done>
</task>

<task type="auto">
  <name>Task 3: Update README.md's Deployment section to reflect real, live status</name>
  <files>README.md</files>
  <read_first>README.md (current `## Deployment` section, lines 37-53)</read_first>
  <action>
    Replace README.md's `## Deployment` section (currently stating "No live hosted
    deployment exists yet") with real, confirmed facts: the production URL confirmed in
    Task 1/2 (`https://$PROD_URL`); a bullet confirming the `vercel-build` three-step
    sequence (`prisma generate` -&gt; gated `migrate deploy` -&gt; `next build --webpack`)
    ran for real on this deployment, not just locally (updating Phase 14's "locally
    verified" framing); a new bullet describing `/api/health` (GET, DB-connectivity check
    via the existing Prisma singleton, `200 {status:"ok"}` / `503 {status:"error"}`, never
    cached); keep the existing bullet about CD-01's native-Git-integration mechanism and the
    `.nvmrc` Node version note, updating only the "not yet" framing to reflect this is now
    live. Follow the section's existing tone (short prose paragraph + bullet list, `--` as
    an em-dash substitute, matching the rest of this file).
  </action>
  <verify>
    <automated>grep -c "No live hosted deployment exists yet" README.md</automated>
  </verify>
  <acceptance_criteria>
    README.md's Deployment section no longer contains the string "No live hosted deployment
    exists yet" (the grep above returns 0).
    README.md's Deployment section contains the real production URL (an `https://` link)
    and mentions `/api/health`.
  </acceptance_criteria>
  <done>README accurately documents the live deployment, matching CONTEXT.md's canonical-refs instruction to update this section "once live."</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude's execution shell (holding `VERCEL_TOKEN`) -> Vercel's production API/CLI surface | Read-only verification against a real, live production project for the first time this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | Elevation of Privilege | `vercel inspect`/`env ls`/`ls` commands run with a full-account token | mitigate | Task 1 cross-checks `.vercel/project.json`'s `projectId`/`orgId` against the user-reported project name before running any command that reads project state; every command in this plan is read-only (no promote/rollback -- that is isolated to Plan 15-04) |
| T-15-07 | Information Disclosure | `VERCEL_TOKEN` or `DATABASE_URL` value leaking into command output/README | mitigate | Acceptance criteria explicitly require zero occurrences of `postgresql://` in captured `vercel env ls` output; README updates (Task 3) describe the health check's behavior, never its connection string |
| T-15-08 | Tampering (false confidence via stale cache) | Live `/api/health` served from Vercel's edge cache | mitigate | Task 2 explicitly checks for the absence of an `x-vercel-cache: HIT` header across two consecutive requests, verifying D-08 holds in the real production environment, not just locally (Plan 15-01) |
</threat_model>

<verification>
`vercel ls`/`vercel inspect`/`vercel env ls` confirm a Ready, correctly-migrated, correctly
env-configured production deployment. Live curl requests against the root page and
`/api/health` both succeed. README.md's Deployment section is updated to match.
</verification>

<success_criteria>
DEPLOY-01, DEPLOY-02, DEPLOY-03, HEALTH-01 (live), and HEALTH-02 are all confirmed against
the real production deployment via CLI and a real HTTP request, with README.md updated to
reflect accurate, non-aspirational status.
</success_criteria>

<output>
Create `.planning/phases/15-deploy-verify/15-03-SUMMARY.md` when done
</output>
