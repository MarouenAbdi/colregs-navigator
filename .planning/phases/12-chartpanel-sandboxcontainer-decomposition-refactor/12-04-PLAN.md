---
phase: 12-chartpanel-sandboxcontainer-decomposition-refactor
plan: 04
type: execute
wave: 3
depends_on: ["12-02", "12-03"]
files_modified:
  - src/components/sandbox/VesselGroup.tsx
  - src/components/sandbox/ChartPanel.tsx
  - src/components/sandbox/ChartPanel.test.tsx
autonomous: false
requirements: [RFCT-05, RFCT-06, RFCT-07, RFCT-08]

must_haves:
  truths:
    - "VesselGroup.tsx (hull polygon, rotate-handle circle, pointerEvents=none badge overlay) is extracted as one atomic, byte-for-byte unit (RFCT-05)"
    - "The rotating <g transform=rotate(...)> group precedes the non-rotating <g pointerEvents=none> badge group in document order, verified by an automated assertion"
    - "The existing pointerEvents=none regression test in ChartPanel.test.tsx passes unmodified"
    - "A human confirms in a real browser that dragging and rotating both vessels at heading 0 shows no hit-testing regression (RFCT-06)"
    - "Both ChartPanel.tsx and SandboxContainer.tsx land under ~150-200 lines (RFCT-07, final gate)"
    - "The full existing Vitest/RTL suite passes with only import-path updates, no behavior-driven test changes (RFCT-08)"
  artifacts:
    - path: "src/components/sandbox/VesselGroup.tsx"
      provides: "hull/rotate-handle/badge-overlay presentation + hit-testing wiring"
      exports: ["VesselGroup", "VesselGroupProps"]
  key_links:
    - from: "src/components/sandbox/ChartPanel.tsx"
      to: "src/components/sandbox/VesselGroup.tsx"
      via: "<VesselGroup label=... vessel=... screen=... role=... hullDrag=... rotateDrag=... />"
      pattern: "<VesselGroup"
    - from: "src/components/sandbox/VesselGroup.tsx"
      to: "src/components/sandbox/hooks/useHullDrag.ts"
      via: "hullDrag: DragHandlers prop, onPointerDown/Move/Up wired directly to the hull polygon"
      pattern: "hullDrag\\.onPointer"
---

<objective>
Extract `VesselGroup` (hull polygon, rotate-handle circle, letter/badge overlay) from
ChartPanel.tsx into its own `VesselGroup.tsx` file as one atomic, byte-for-byte cut-paste, add a
DOM-order regression assertion, and get human confirmation in a real browser that drag/rotate
hit-testing is unregressed.

Purpose: RFCT-05/RFCT-06 require this extraction to be the last, most carefully handled step --
this exact code has caused two prior hit-testing regressions in this project (Phase 4 commits
`3bf6f24`/`f559e98`; Phase 8 commit `fb0ec8a`). Per D-03, this MUST be its own final,
separately-reviewable plan/commit, after all other extractions (12-01, 12-02, 12-03) have landed,
within the same PR/branch (`gsd/phase-12-chartpanel-sandboxcontainer-decomposition-refactor`).
Completing this plan also lands both ChartPanel.tsx and SandboxContainer.tsx under the project's
~150-200 line convention (RFCT-07), closing out the phase.
Output: `VesselGroup.tsx`, a ChartPanel.tsx that consumes it, a new DOM-order regression test, and
human sign-off on real-browser drag/rotate behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-chartpanel-sandboxcontainer-decomposition-refactor/12-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/12-chartpanel-sandboxcontainer-decomposition-refactor/12-PATTERNS.md
@.planning/phases/12-chartpanel-sandboxcontainer-decomposition-refactor/12-03-SUMMARY.md
</context>

<interfaces>
<!-- Locked signature from ARCHITECTURE.md -- unchanged, move only. -->

```typescript
// VesselGroup.tsx -- moved verbatim, signature unchanged
export interface VesselGroupProps {
  label: VesselLabel;
  vessel: Vessel;
  screen: { screenX: number; screenY: number };
  role: VesselRole;
  hullDrag: DragHandlers;   // from ./hooks/useHullDrag.js, unchanged
  rotateDrag: DragHandlers; // from ./hooks/useRotateHandleDrag.js, unchanged
}
export function VesselGroup(props: VesselGroupProps): JSX.Element;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create VesselGroup.tsx (byte-for-byte move)</name>
  <files>src/components/sandbox/VesselGroup.tsx</files>
  <read_first>
    src/components/sandbox/ChartPanel.tsx (current, post-12-01/12-03 -- locate the VesselGroupProps interface + VesselGroup function by content; neither prior plan touched this region)
    .planning/PROJECT.md (Key Decisions table -- both documented hit-testing regressions: Phase 4 commits 3bf6f24/f559e98, Phase 8 commit fb0ec8a)
    .planning/research/ARCHITECTURE.md ("Critical constraint" section and Anti-Patterns 1 and 2)
    src/components/sandbox/ChartPanel.test.tsx (lines 84-97: existing pointerEvents=none regression test -- this file must keep passing unmodified after the move)
    src/components/sandbox/chart-panel-geometry.ts (constants VesselGroup consumes: HULL_BOW_Y, ROTATE_STALK_Y2, HULL_POINTS, HULL_STROKE, HULL_STROKE_WIDTH, ROTATE_HANDLE_CY, ROTATE_HANDLE_VISIBLE_R, LETTER_OFFSET_X, LETTER_OFFSET_Y, LETTER_CIRCLE_R, BADGE_OFFSET_X, BADGE_OFFSET_Y, BADGE_RECT_WIDTH, BADGE_RECT_HEIGHT)
  </read_first>
  <action>
    Create `src/components/sandbox/VesselGroup.tsx`. Move the `VesselGroupProps` interface and
    `VesselGroup` function BYTE-FOR-BYTE from `ChartPanel.tsx` -- identical JSX structure, identical
    sibling order (the rotating `<g transform={`rotate(${vessel.heading})`}>` group, containing the
    stalk `<line>`, hull `<polygon data-testid={`hull-hit-${label}`}>`, and rotate-handle `<circle
    data-testid={`rotate-hit-${label}`}>`, MUST remain the first child of the outer `<g
    transform={`translate(...)`}>`), identical `pointerEvents="none"` attribute on the badge
    overlay `<g>` (which MUST remain the second/last child), identical direct attachment of
    `onPointerDown`/`onPointerMove`/`onPointerUp` to the hull polygon and rotate-handle circle.
    Preserve every existing comment verbatim, including every comment documenting the Phase 4/Phase
    8 regression fixes (the stalk's "no pointer handlers" comment, the hull's "visible painted
    area IS the hit target" comment, the rotate handle's matching comment, and the badge overlay
    group's "pointerEvents: none on both groups is load-bearing, not decoration" comment) -- do not
    summarize, compress, paraphrase, or reformat any of them. Add `export` to both
    `VesselGroupProps` and `VesselGroup` (currently module-private). No signature or behavior
    change -- this is a pure cut-and-paste per ARCHITECTURE.md's Extraction 5 row and Anti-Pattern
    2 ("no reformatting, no reordering, no dropping pointerEvents='none'").
    Imports: `type DragHandlers` from `"./hooks/useHullDrag.js"`; `type Vessel` from
    `"../../domain/vessel/vessel.js"`; `type VesselLabel` from `"../../domain/colregs/types.js"`;
    `ROLE_HULL_FILL_CLASS`, `ROLE_BADGE_TEXT`, `type VesselRole` from `"./vessel-role.js"`;
    `HULL_BOW_Y`, `ROTATE_STALK_Y2`, `HULL_POINTS`, `HULL_STROKE`, `HULL_STROKE_WIDTH`,
    `ROTATE_HANDLE_CY`, `ROTATE_HANDLE_VISIBLE_R`, `LETTER_OFFSET_X`, `LETTER_OFFSET_Y`,
    `LETTER_CIRCLE_R`, `BADGE_OFFSET_X`, `BADGE_OFFSET_Y`, `BADGE_RECT_WIDTH`, `BADGE_RECT_HEIGHT`
    from `"./chart-panel-geometry.js"`.
  </action>
  <verify>
    <automated>grep -n "export function VesselGroup" src/components/sandbox/VesselGroup.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function VesselGroup" src/components/sandbox/VesselGroup.tsx` matches
    - `grep -n "export interface VesselGroupProps" src/components/sandbox/VesselGroup.tsx` matches
    - `grep -n 'data-testid={\`hull-hit-\${label}\`}' src/components/sandbox/VesselGroup.tsx` matches
    - `grep -n 'data-testid={\`rotate-hit-\${label}\`}' src/components/sandbox/VesselGroup.tsx` matches
    - `grep -c 'pointerEvents="none"' src/components/sandbox/VesselGroup.tsx` returns exactly 1
    - The line number of `grep -n 'pointerEvents="none"' src/components/sandbox/VesselGroup.tsx` is greater than the line number of `grep -n 'transform={\`rotate(' src/components/sandbox/VesselGroup.tsx` (confirms the rotating group precedes the non-rotating badge group in source order)
  </acceptance_criteria>
  <done>VesselGroup.tsx exists with VesselGroupProps + VesselGroup exported, byte-for-byte identical to the source region, sibling order and pointerEvents="none" preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Wire VesselGroup into ChartPanel.tsx, add DOM-order regression test, run full suite</name>
  <files>src/components/sandbox/ChartPanel.tsx, src/components/sandbox/ChartPanel.test.tsx</files>
  <read_first>
    src/components/sandbox/ChartPanel.tsx (current, post-12-01/12-03)
    src/components/sandbox/ChartPanel.test.tsx (lines 84-97: existing pointerEvents=none test to preserve unmodified, and to place the new test adjacent to)
    src/components/sandbox/VesselGroup.tsx (just created)
  </read_first>
  <action>
    Modify `ChartPanel.tsx`: remove the inline `VesselGroupProps` interface and `VesselGroup`
    function, replace with `import { VesselGroup } from "./VesselGroup.js";`. The two
    `<VesselGroup ... />` JSX usages (passing `label`/`vessel`/`screen`/`role`/`hullDrag`/`rotateDrag`)
    stay completely unchanged -- only the JSX consuming `DragHandlers`' output moves, per CONTEXT.md's
    invariant; `useHullDrag`/`useRotateHandleDrag` themselves are NOT touched. Remove now-dead
    imports from `ChartPanel.tsx` if `VesselGroup.tsx` now owns their only remaining consumer in
    this file (e.g. `ROLE_HULL_FILL_CLASS`/`ROLE_BADGE_TEXT`/`ROTATE_HANDLE_CY`/etc. from
    `"./chart-panel-geometry.js"`/`"./vessel-role.js"` -- verify each before removing by confirming
    it has zero remaining references in ChartPanel.tsx's own body). Keep `type DragHandlers` import
    for the `useHullDrag`/`useRotateHandleDrag` call sites, which remain in ChartPanel.tsx.

    Add to `ChartPanel.test.tsx`, immediately after the existing "gives the decorative
    letter/role-badge overlay pointer-events:none..." test (lines 84-97): a new `it(...)` block
    asserting DOM order, per CONTEXT.md's Claude's Discretion note and ARCHITECTURE.md's Extraction
    5 row. Render `ChartPanel` via the existing `renderChartPanel()` helper, locate the badge
    overlay group the same way the existing test does
    (`within(container).getByText("GW").closest("g[pointer-events]")`), locate the rotating group
    via `container.querySelector('g[transform^="rotate("]')`, and assert
    `overlayGroup.compareDocumentPosition(rotatingGroup) & Node.DOCUMENT_POSITION_PRECEDING` is
    truthy -- i.e. the rotating `<g transform="rotate(...)">` group precedes the non-rotating `<g
    pointer-events="none">` sibling in document order. Follow this test file's existing
    `container.querySelector`/`.closest` style; do not introduce a new querying convention or test
    helper.
  </action>
  <verify>
    <automated>npx vitest run src/components/sandbox/ChartPanel.test.tsx && npx vitest run && npx tsc --noEmit && npx eslint src/components/sandbox/</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/sandbox/ChartPanel.test.tsx` passes, including both the pre-existing pointerEvents test and the new DOM-order test
    - `npx vitest run` (full suite) exits 0
    - `npx tsc --noEmit` exits 0
    - `npx eslint src/components/sandbox/` exits 0
    - `wc -l < src/components/sandbox/ChartPanel.tsx` prints a value between 100 and 200 (RFCT-07 final gate)
    - `wc -l < src/components/sandbox/SandboxContainer.tsx` prints a value between 100 and 200 (RFCT-07 final gate, confirms Plan 12-02's result still holds)
    - `grep -n "DOCUMENT_POSITION_PRECEDING" src/components/sandbox/ChartPanel.test.tsx` matches
    - The pre-existing "gives the decorative letter/role-badge overlay pointer-events:none..." test body (originally at lines 84-97) is byte-identical to before this change -- only a new test block was appended, no existing assertion edited
  </acceptance_criteria>
  <done>ChartPanel.tsx imports VesselGroup from ./VesselGroup.js instead of defining it inline; ChartPanel.test.tsx has a new DOM-order regression assertion alongside the unmodified pointerEvents test; full suite, typecheck, and lint all pass; both target files land under the ~150-200 line convention.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Real-browser drag/rotate verification (RFCT-06)</name>
  <files>N/A -- human verification only, no file changes in this task</files>
  <what-built>
    VesselGroup.tsx extracted as a byte-for-byte move from ChartPanel.tsx. The automated
    point-in-polygon regression test (pointerEvents:none badge overlay) passes unmodified, and a
    new automated DOM-order assertion (rotating group precedes the non-rotating badge group) also
    passes. Per CONTEXT.md's Claude's Discretion note, no automated Claude-in-Chrome pre-check is
    run -- this is the direct human real-browser pass for RFCT-06, mirroring Phase 11's D-05
    precedent.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open the Sandbox in a real browser. The default scenario seeds both
    vessels at heading 0 (`crossingResidualBasicCase`) -- this is the exact heading where the
    Phase 8 badge/hull overlap regression previously occurred.
    For EACH vessel (A and B):
    1. Drag the hull (the solid colored kite/arrow shape) to reposition it. Confirm it tracks the
       cursor smoothly and the verdict/reasoning trail updates live.
    2. Grab the rotate handle (the small white-filled circle beyond the bow, connected by a dashed
       stalk) and drag it around the vessel. Confirm the heading updates smoothly and the hull
       rotates to match.
    3. Specifically try starting a drag from the region where the letter chip (upper-left of the
       hull) and the role badge chip (lower-right of the hull) visually sit near/over the hull's
       own painted area at heading 0. Confirm clicking/dragging in that region still starts a hull
       drag (the badge overlay must never swallow the pointerdown).
    Repeat all 3 checks for both vessels.
  </how-to-verify>
  <action>No automated action for this task -- it exists solely to pause for human confirmation of real-browser hit-testing behavior after the VesselGroup.tsx extraction.</action>
  <acceptance_criteria>
    - Human has explicitly confirmed (via the resume-signal) that both vessels' drag-to-reposition and drag-to-rotate gestures work with no dead zones or hit-testing regressions
  </acceptance_criteria>
  <verify>
    <automated>N/A -- human confirmation required; no automated command applies to this checkpoint</automated>
  </verify>
  <resume-signal>Type "approved" if drag/rotate works correctly for both vessels with no dead zones, or describe the specific vessel/gesture/region that shows a regression.</resume-signal>
  <done>Human confirms no hit-testing regression for either vessel's drag-to-reposition or drag-to-rotate gesture at heading 0, closing RFCT-06 and the phase.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Byte-for-byte relocation of existing, already-hardened SVG hit-testing JSX. No new user input, endpoint, or schema -- but this is the single highest-risk step in the phase given two prior real regressions in this exact code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-12-04a | Tampering | VesselGroup.tsx hull/rotate-handle hit-targets | mitigate | Byte-for-byte move only, no reformatting (Task 1's acceptance criteria grep-verify `data-testid` attributes and handler attachment survive intact) -- Anti-Pattern 1 (padded invisible hit-shape) avoided by not modifying pointer-handler attachment; handlers stay on the visible painted polygon/circle exactly as today. |
| T-12-04b | Tampering | VesselGroup.tsx pointerEvents="none" badge overlay | mitigate | Anti-Pattern 2 (losing pointerEvents="none" during a "cleanup" pass) avoided via: (1) the pre-existing regression test running unmodified, (2) a new automated DOM-order assertion, (3) a mandatory human real-browser drag/rotate pass (Task 3) before this plan -- and the phase -- is considered complete. |
| T-12-04-SC | Tampering (supply chain) | N/A -- no new npm/pip/cargo package installs in this plan | accept | This plan only relocates existing first-party code between files; no dependency changes. |
</threat_model>

<verification>
- `npx vitest run` exits 0 for the full suite, including the pre-existing pointerEvents test (unmodified) and the new DOM-order test.
- `npx tsc --noEmit` and `npx eslint src/components/sandbox/` both exit 0.
- `wc -l` confirms both ChartPanel.tsx and SandboxContainer.tsx land between 100 and 200 lines.
- Human types "approved" after the real-browser drag/rotate walkthrough for both vessels.
</verification>

<success_criteria>
- VesselGroup.tsx is extracted as one atomic, byte-for-byte unit (RFCT-05).
- Zero hit-testing regression, confirmed by both the automated regression/DOM-order tests and a human real-browser pass (RFCT-06).
- Both ChartPanel.tsx and SandboxContainer.tsx land under the ~150-200 line convention (RFCT-07).
- The full existing suite passes with only import-path updates, no behavior-driven test changes (RFCT-08), closing Phase 12.
</success_criteria>

<output>
Create `.planning/phases/12-chartpanel-sandboxcontainer-decomposition-refactor/12-04-SUMMARY.md` when done
</output>
