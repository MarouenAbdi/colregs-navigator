---
phase: 12-chartpanel-sandboxcontainer-decomposition-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sandbox/chart-panel-geometry.ts
  - src/components/sandbox/chart-panel-geometry.test.ts
  - src/components/sandbox/chart-panel-derivation.ts
  - src/components/sandbox/chart-panel-derivation.test.ts
  - src/components/sandbox/ChartPanel.tsx
autonomous: true
requirements: [RFCT-01, RFCT-02, RFCT-08]

must_haves:
  truths:
    - "ChartPanel.tsx's pure geometry constants, wedgePath(), and buildGridLineSegments() live in chart-panel-geometry.ts, not ChartPanel.tsx (RFCT-01)"
    - "ChartPanel.tsx's per-render derivation logic lives in deriveChartOverlayState() in chart-panel-derivation.ts, not buried in the component body (RFCT-02)"
    - "D-01: deriveChartOverlayState()'s rangeNm comes from deriveInstrumentReadouts(vesselA, vesselB).rangeNm, not a second independent Math.hypot(...) computation"
    - "chart-panel-geometry.ts and chart-panel-derivation.ts both have zero React import and return plain data, never JSX (Anti-Pattern 3 avoided)"
    - "ChartPanel.test.tsx's existing 4 tests pass unmodified after the wiring change into ChartPanel.tsx"
  artifacts:
    - path: "src/components/sandbox/chart-panel-geometry.ts"
      provides: "pure geometry constants + wedgePath() + buildGridLineSegments()"
      exports: ["wedgePath", "buildGridLineSegments", "CHART_VIEW_BOX"]
    - path: "src/components/sandbox/chart-panel-geometry.test.ts"
      provides: "numeric-fixture unit tests for wedgePath()/buildGridLineSegments()"
    - path: "src/components/sandbox/chart-panel-derivation.ts"
      provides: "deriveChartOverlayState() per-render derivation"
      exports: ["deriveChartOverlayState", "ChartOverlayState"]
    - path: "src/components/sandbox/chart-panel-derivation.test.ts"
      provides: "fixture-based tests reusing crossingResidualBasicCase/headOnBoundaryInclusiveCase/doubtBandNearOvertakingBoundaryCase"
  key_links:
    - from: "src/components/sandbox/ChartPanel.tsx"
      to: "src/components/sandbox/chart-panel-geometry.ts"
      via: "import { ... } from \"./chart-panel-geometry.js\""
      pattern: "from \"\\./chart-panel-geometry\\.js\""
    - from: "src/components/sandbox/ChartPanel.tsx"
      to: "src/components/sandbox/chart-panel-derivation.ts"
      via: "deriveChartOverlayState(vesselA, vesselB, classification, containerSize, CHART_VIEW_BOX) call"
      pattern: "deriveChartOverlayState\\("
    - from: "src/components/sandbox/chart-panel-derivation.ts"
      to: "src/components/sandbox/instrument-readouts.ts"
      via: "deriveInstrumentReadouts(vesselA, vesselB).rangeNm (D-01 dedup)"
      pattern: "deriveInstrumentReadouts\\("
---

<objective>
Extract ChartPanel.tsx's pure geometry (constants, `wedgePath()`, `buildGridLineSegments()`) into
`chart-panel-geometry.ts`, and its per-render derivation logic into a new
`deriveChartOverlayState()` function in `chart-panel-derivation.ts`, applying the D-01
`rangeNm` dedup along the way. Wire both back into a thinner `ChartPanel.tsx`.

Purpose: RFCT-01 and RFCT-02 require these two pure-module extractions, following this
project's "split computation from presentation" convention (CLAUDE.md) and the two prior
successful applications of it (`hero-preview-geometry.ts`, `static-chart-geometry.ts`). This is
step 1+2 of ARCHITECTURE.md's risk-ordered extraction table — both rated "Zero" risk, safe to do
first and combine into one plan/commit per D-03 (extractions 1-4b have no ordering dependency).
Output: `chart-panel-geometry.ts`, `chart-panel-geometry.test.ts`, `chart-panel-derivation.ts`,
`chart-panel-derivation.test.ts`, and a `ChartPanel.tsx` that imports and calls both instead of
defining this logic inline.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-chartpanel-sandboxcontainer-decomposition-refactor/12-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/12-chartpanel-sandboxcontainer-decomposition-refactor/12-PATTERNS.md
</context>

<interfaces>
<!-- Locked signatures from ARCHITECTURE.md -- use these exactly, no deviation. -->

From `.planning/research/ARCHITECTURE.md` (Integration Points section):
```typescript
// chart-panel-geometry.ts -- pure, zero React import
export function wedgePath(
  center: { screenX: number; screenY: number },
  radiusPx: number,
  startBearingDeg: number,
  endBearingDeg: number,
): string; // unchanged signature, move only

export function buildGridLineSegments(
  containerSize: ContainerSize,
  viewBox: ChartViewBox,
): Array<{ key: string; x1: number; y1: number; x2: number; y2: number }>;
// RENAMED from buildGridLines(); return type changes from React.ReactNode[]
// to plain data.

// chart-panel-derivation.ts -- pure, calls domain fns only (no DOM/SVG)
export interface ChartOverlayState {
  screenA: { screenX: number; screenY: number };
  screenB: { screenX: number; screenY: number };
  chartCenter: { screenX: number; screenY: number };
  innerRingRadiusPx: number;
  outerRingRadiusPx: number;
  roleA: VesselRole;
  roleB: VesselRole;
  bearingStroke: string;
  bearingDashArray: string | undefined;
  rangeNm: number;
  bearingMidpoint: { screenX: number; screenY: number };
  doubtVessel: VesselLabel | null;
  conePathA: string;
  conePathB: string;
}
export function deriveChartOverlayState(
  vesselA: Vessel,
  vesselB: Vessel,
  classification: ClassificationResult,
  containerSize: ContainerSize,
  viewBox: ChartViewBox,
): ChartOverlayState;
```

From `src/components/sandbox/instrument-readouts.ts` (D-01 dedup target, already exists):
```typescript
export interface InstrumentReadouts {
  rangeNm: number;
  bearingAtoBDegrees: number | null;
  cpaNm: number | null;
  tcpaMinutes: number | null;
}
export function deriveInstrumentReadouts(vesselA: Vessel, vesselB: Vessel): InstrumentReadouts;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create chart-panel-geometry.ts + chart-panel-geometry.test.ts</name>
  <files>src/components/sandbox/chart-panel-geometry.ts, src/components/sandbox/chart-panel-geometry.test.ts</files>
  <read_first>
    src/components/sandbox/ChartPanel.tsx (lines 32-194: all constants, wedgePath(), buildGridLines())
    src/components/hero/hero-preview-geometry.ts (whole file -- doc-comment + zero-React-import convention)
    src/components/shared/static-chart-geometry.ts (whole file -- "return plain data, not JSX" precedent)
    src/components/sandbox/instrument-readouts.test.ts (whole file -- numeric-fixture test style analog)
  </read_first>
  <action>
    Create `src/components/sandbox/chart-panel-geometry.ts`. Add a top-of-file doc comment
    following `hero-preview-geometry.ts`'s convention ("Pure, framework-free geometry for
    ChartPanel's SVG chart (RFCT-01)..."). Move, with `export` added and every existing comment
    preserved verbatim, all constants currently in `ChartPanel.tsx` lines 32-123:
    `CHART_VIEW_BOX`, `GRID_STEP_CHART_UNITS`, `GRID_STROKE`, `FINE_GRID_CELL_PX`,
    `FINE_GRID_STROKE`, `RANGE_RING_STROKE`, `RANGE_RING_INNER_RADIUS_RATIO`,
    `RANGE_RING_OUTER_RADIUS_RATIO`, `CROSSHAIR_STROKE`, `HULL_BOW_Y`, `HULL_STERN_Y`,
    `HULL_NOTCH_Y`, `HULL_HALF_WIDTH`, `HULL_POINTS`, `HULL_STROKE`, `HULL_STROKE_WIDTH`,
    `BADGE_RECT_WIDTH`, `BADGE_RECT_HEIGHT`, `LETTER_OFFSET_X`, `LETTER_OFFSET_Y`,
    `LETTER_CIRCLE_R`, `BADGE_OFFSET_X`, `BADGE_OFFSET_Y`, `ROTATE_HANDLE_CY`,
    `ROTATE_HANDLE_VISIBLE_R`, `ROTATE_STALK_Y2`, `BEARING_LINE_DEFAULT_STROKE`, `DOUBT_STROKE`,
    `CONE_DEFAULT_STROKE`, `CONE_RADIUS_PX`, `CONE_START_RELATIVE_BEARING_DEGREES`,
    `CONE_END_RELATIVE_BEARING_DEGREES`. Move `wedgePath()` (lines 129-154) verbatim, exported,
    unchanged signature.
    Move `buildGridLines()` (lines 156-194) RENAMED to `buildGridLineSegments()` per the locked
    signature above: it must accept `(containerSize: ContainerSize, viewBox: ChartViewBox)` as two
    explicit parameters (viewBox is no longer read implicitly from the module-level
    `CHART_VIEW_BOX` -- pass it explicitly so the function is independently testable), and its
    return type changes from `React.ReactNode[]` to
    `Array<{ key: string; x1: number; y1: number; x2: number; y2: number }>`. Replace both
    `lines.push(<line key=... x1=... y1=... x2=... y2=... stroke={GRID_STROKE} strokeWidth={1}/>)`
    calls with `lines.push({ key: ..., x1: ..., y1: ..., x2: ..., y2: ... })` -- drop
    `stroke`/`strokeWidth` from the pushed object entirely, that becomes the JSX caller's concern.
    Only import: `import { chartToScreen, type ChartViewBox, type ContainerSize } from
    "../../domain/geometry/screen-convert.js";`. Do NOT import anything from `"react"` (no
    `React.ReactNode`, no JSX) -- this is the exact bar Anti-Pattern 3 in ARCHITECTURE.md exists
    to enforce.

    Create `src/components/sandbox/chart-panel-geometry.test.ts` following
    `instrument-readouts.test.ts`'s plain `describe`/`it` style (no `@vitest-environment jsdom`
    pragma, no React import, no component render): test `wedgePath()` with a fixed center (e.g.
    `{screenX: 100, screenY: 100}`), `radiusPx: 60`, `startBearingDeg: 0`, `endBearingDeg: 90`,
    asserting the returned path string starts with `"M 100 100"` and contains `"L "` and `"A "`
    segments with the expected numeric endpoints (compute expected x/y via the same
    bearing-to-screen-angle formula documented in `wedgePath()`'s own comment). Test
    `buildGridLineSegments()` with a fixed `containerSize` (e.g. `{width: 400, height: 400}`) and
    a `viewBox` matching `CHART_VIEW_BOX` (`{minX: -10, minY: -10, width: 20, height: 20}`),
    asserting the returned array's length (one segment per `GRID_STEP_CHART_UNITS` step across
    both axes) and at least one specific segment's `x1`/`y1`/`x2`/`y2` values.
  </action>
  <verify>
    <automated>npx vitest run src/components/sandbox/chart-panel-geometry.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "from \"react\"" src/components/sandbox/chart-panel-geometry.ts` returns no matches
    - `grep -n "React.ReactNode" src/components/sandbox/chart-panel-geometry.ts` returns no matches
    - `grep -n "export function buildGridLineSegments" src/components/sandbox/chart-panel-geometry.ts` matches
    - `grep -n "export function wedgePath" src/components/sandbox/chart-panel-geometry.ts` matches
    - `npx vitest run src/components/sandbox/chart-panel-geometry.test.ts` exits 0
  </acceptance_criteria>
  <done>chart-panel-geometry.ts exists with all ~35 constants, wedgePath(), and buildGridLineSegments() exported, zero React import; chart-panel-geometry.test.ts passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create chart-panel-derivation.ts + chart-panel-derivation.test.ts (D-01 dedup)</name>
  <files>src/components/sandbox/chart-panel-derivation.ts, src/components/sandbox/chart-panel-derivation.test.ts</files>
  <read_first>
    src/components/sandbox/ChartPanel.tsx (lines 368-439: the per-render derivation block)
    src/components/sandbox/instrument-readouts.ts (whole file -- direct structural analog + D-01 dedup target)
    src/components/sandbox/vessel-role.ts (lines 18-26 -- duplication-avoidance precedent comment to cite)
    src/domain/colregs/resolve-doubt-geometry.ts (resolveDoubtGeometry signature)
    src/components/sandbox/chart-panel-geometry.ts (just created in Task 1 -- wedgePath + constants to import)
    src/components/sandbox/ChartPanel.test.tsx (lines 1-75: fixture imports + classifyEncounter guard pattern to mirror in the new test file)
  </read_first>
  <action>
    Create `src/components/sandbox/chart-panel-derivation.ts` following `instrument-readouts.ts`'s
    exact shape (doc comment explaining why this derivation is its own module, then named domain
    imports, then an exported interface + a single exported derive function). Export interface
    `ChartOverlayState` and function `deriveChartOverlayState(vesselA, vesselB, classification,
    containerSize, viewBox)` per the locked signature in this plan's `<interfaces>` block. Move
    verbatim from `ChartPanel.tsx` lines 381-439: `screenA`/`screenB` (via `chartToScreen`),
    `chartCenter`, `innerRingRadiusPx`/`outerRingRadiusPx` (via `containerSize.width *
    RANGE_RING_INNER_RADIUS_RATIO`/`RANGE_RING_OUTER_RADIUS_RATIO`), `roleA`/`roleB` (via
    `getVesselRole`), the `bearingDoubt`/`bearingStroke`/`bearingDashArray` block, `rangeNm`,
    `bearingMidpoint`, the `doubtVessel` resolution block (including its exact "If
    resolveDoubtGeometry returns !ok..." comment, unchanged), and `conePathA`/`conePathB` (via
    `wedgePath`). Apply D-01: replace the inline `Math.hypot(vesselB.position.x -
    vesselA.position.x, vesselB.position.y - vesselA.position.y)` rangeNm computation with
    `const { rangeNm } = deriveInstrumentReadouts(vesselA, vesselB);`, importing
    `deriveInstrumentReadouts` from `"./instrument-readouts.js"`. Add a one-line comment above this
    call citing `vessel-role.ts`'s "Single-sourced role -> style maps" comment as precedent (same
    duplication class, same silent-bug risk) -- do not restate what the code does.
    Imports: `chartToScreen`, `type ChartViewBox`, `type ContainerSize` from
    `"../../domain/geometry/screen-convert.js"`; `getVesselRole`, `type VesselRole` from
    `"./vessel-role.js"`; `resolveDoubtGeometry` from
    `"../../domain/colregs/resolve-doubt-geometry.js"`; `wedgePath`,
    `RANGE_RING_INNER_RADIUS_RATIO`, `RANGE_RING_OUTER_RADIUS_RATIO`, `DOUBT_STROKE`,
    `BEARING_LINE_DEFAULT_STROKE`, `CONE_RADIUS_PX`, `CONE_START_RELATIVE_BEARING_DEGREES`,
    `CONE_END_RELATIVE_BEARING_DEGREES` from `"./chart-panel-geometry.js"`; `deriveInstrumentReadouts`
    from `"./instrument-readouts.js"`; `type Vessel` from `"../../domain/vessel/vessel.js"`;
    `type ClassificationResult`, `type VesselLabel` from `"../../domain/colregs/types.js"`. No
    `"react"` import, no JSX anywhere in this file.

    Create `src/components/sandbox/chart-panel-derivation.test.ts` per D-02: import
    `crossingResidualBasicCase`, `headOnBoundaryInclusiveCase`,
    `doubtBandNearOvertakingBoundaryCase` from `"../../domain/colregs/classify-encounter.fixtures.js"`,
    `classifyEncounter` from `"../../domain/colregs/classify-encounter.js"`,
    `deriveInstrumentReadouts` from `"./instrument-readouts.js"`, and `deriveChartOverlayState`
    from `"./chart-panel-derivation.js"`. For each of the 3 fixtures, call `classifyEncounter(vesselA,
    vesselB)`, guard with `if (!result.ok) throw new Error(...)` (mirroring
    `ChartPanel.test.tsx`'s exact guard), call `deriveChartOverlayState(vesselA, vesselB,
    result.value, { width: 400, height: 400 }, { minX: -10, minY: -10, width: 20, height: 20 })`,
    and assert: for `headOnBoundaryInclusiveCase`, `bearingStroke === "#F59E0B"` and
    `bearingDashArray === "4 3"` (mirrors `ChartPanel.test.tsx`'s own doubt assertion); for
    `doubtBandNearOvertakingBoundaryCase`, `doubtVessel` is not `null` and is `"vesselA"` or
    `"vesselB"`; for `crossingResidualBasicCase` (baseline, no doubt), `bearingDashArray` is
    `undefined`, `doubtVessel` is `null`, and `rangeNm` equals
    `deriveInstrumentReadouts(vesselA, vesselB).rangeNm` exactly (proves the D-01 dedup wires
    through correctly).
  </action>
  <verify>
    <automated>npx vitest run src/components/sandbox/chart-panel-derivation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "deriveInstrumentReadouts" src/components/sandbox/chart-panel-derivation.ts` matches
    - `grep -n "Math.hypot" src/components/sandbox/chart-panel-derivation.ts` returns no matches
    - `grep -n "from \"react\"" src/components/sandbox/chart-panel-derivation.ts` returns no matches
    - `npx vitest run src/components/sandbox/chart-panel-derivation.test.ts` exits 0
  </acceptance_criteria>
  <done>chart-panel-derivation.ts exports ChartOverlayState + deriveChartOverlayState(), applies the D-01 dedup, has zero React import; chart-panel-derivation.test.ts passes.</done>
</task>

<task type="auto">
  <name>Task 3: Wire chart-panel-geometry.ts + chart-panel-derivation.ts into ChartPanel.tsx</name>
  <files>src/components/sandbox/ChartPanel.tsx</files>
  <read_first>
    src/components/sandbox/ChartPanel.tsx (current full file -- to know exactly what to remove/replace)
    src/components/sandbox/chart-panel-geometry.ts (just created)
    src/components/sandbox/chart-panel-derivation.ts (just created)
    src/components/sandbox/ChartPanel.test.tsx (regression gate -- all 4 tests must keep passing)
  </read_first>
  <action>
    Modify `ChartPanel.tsx`: remove the ~35 now-duplicated constants (lines 32-123), `wedgePath()`
    (129-154), and `buildGridLines()` (156-194). Replace with an import from
    `"./chart-panel-geometry.js"` covering only the constants ChartPanel.tsx's remaining body
    (still-inline `VesselGroup`, plus the still-inline decorative `<svg>` JSX -- neither is
    extracted in this plan) directly references: `CHART_VIEW_BOX`, `GRID_STROKE`,
    `FINE_GRID_CELL_PX`, `FINE_GRID_STROKE`, `RANGE_RING_STROKE`, `CROSSHAIR_STROKE`,
    `HULL_BOW_Y`, `HULL_STERN_Y`, `HULL_NOTCH_Y`, `HULL_HALF_WIDTH`, `HULL_POINTS`, `HULL_STROKE`,
    `HULL_STROKE_WIDTH`, `BADGE_RECT_WIDTH`, `BADGE_RECT_HEIGHT`, `LETTER_OFFSET_X`,
    `LETTER_OFFSET_Y`, `LETTER_CIRCLE_R`, `BADGE_OFFSET_X`, `BADGE_OFFSET_Y`, `ROTATE_HANDLE_CY`,
    `ROTATE_HANDLE_VISIBLE_R`, `ROTATE_STALK_Y2`, plus `buildGridLineSegments`.
    Replace the JSX `<g>{buildGridLines(containerSize)}</g>` with:
    `<g>{buildGridLineSegments(containerSize, CHART_VIEW_BOX).map((seg) => (<line key={seg.key}
    x1={seg.x1} y1={seg.y1} x2={seg.x2} y2={seg.y2} stroke={GRID_STROKE} strokeWidth={1} />))}</g>`
    -- this 4-line `.map()` is the JSX-layer responsibility ARCHITECTURE.md's Integration Points
    section calls for.
    Remove the derivation block (lines 381-439). Replace with:
    `const overlay = deriveChartOverlayState(vesselA, vesselB, classification, containerSize,
    CHART_VIEW_BOX);` then reference `overlay.screenA`, `overlay.screenB`, `overlay.chartCenter`,
    `overlay.innerRingRadiusPx`, `overlay.outerRingRadiusPx`, `overlay.roleA`, `overlay.roleB`,
    `overlay.bearingStroke`, `overlay.bearingDashArray`, `overlay.rangeNm`,
    `overlay.bearingMidpoint`, `overlay.doubtVessel`, `overlay.conePathA`, `overlay.conePathB` at
    each of their existing JSX use sites (chart center circles/crosshair, `VesselGroup`
    role props, bearing `<line>` stroke/dasharray, range tooltip text/position, both cone
    `<path>` elements' doubt-styling and `d` attribute).
    Import `deriveChartOverlayState` from `"./chart-panel-derivation.js"`.
    Remove `getVesselRole` and `resolveDoubtGeometry` from ChartPanel.tsx's own top-level imports
    -- both are now only referenced inside `chart-panel-derivation.ts`. Keep `ROLE_BADGE_TEXT`/
    `ROLE_HULL_FILL_CLASS` imports from `vessel-role.js` (still used inline by the still-inline
    `VesselGroup`). Do NOT touch `VesselGroup`, the `useHullDrag`/`useRotateHandleDrag` calls, the
    `containerRef`/`containerSize` state + `ResizeObserver` effect, or any JSX outside the pieces
    listed above -- those move in later plans (12-03, 12-04).
  </action>
  <verify>
    <automated>npx vitest run src/components/sandbox/ChartPanel.test.tsx && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/sandbox/ChartPanel.test.tsx` passes, all 4 existing tests green
    - `npx vitest run` (full suite) exits 0
    - `npx tsc --noEmit` exits 0
    - `grep -n "Math.hypot" src/components/sandbox/ChartPanel.tsx` returns no matches
    - `grep -n "function wedgePath" src/components/sandbox/ChartPanel.tsx` returns no matches
    - `grep -n "function buildGridLines" src/components/sandbox/ChartPanel.tsx` returns no matches (renamed and moved)
    - `grep -n "deriveChartOverlayState" src/components/sandbox/ChartPanel.tsx` matches
  </acceptance_criteria>
  <done>ChartPanel.tsx imports and calls chart-panel-geometry.ts and chart-panel-derivation.ts instead of defining this logic inline; full test suite and typecheck pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Pure internal module split of existing, already-validated presentation-layer code. No new user input, endpoint, or schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-12-01 | Tampering | chart-panel-geometry.ts / chart-panel-derivation.ts (pure modules) | accept | Zero new input surface -- pure functions operating on already-validated Vessel/ClassificationResult data supplied by the caller; behavior verified byte-identical via ChartPanel.test.tsx passing unmodified plus new fixture-based unit tests. |
| T-12-01-SC | Tampering (supply chain) | N/A -- no new npm/pip/cargo package installs in this plan | accept | This plan only relocates existing first-party code between files; no dependency changes. |
</threat_model>

<verification>
- `npx vitest run` exits 0 for the full suite, including the two new test files and the unmodified ChartPanel.test.tsx.
- `npx tsc --noEmit` exits 0.
- `grep -n "Math.hypot"` in chart-panel-derivation.ts and ChartPanel.tsx both return no matches (D-01 dedup landed in exactly one place: instrument-readouts.ts).
</verification>

<success_criteria>
- ChartPanel.tsx's pure geometry and per-render derivation logic live in two new zero-JSX modules (RFCT-01, RFCT-02).
- The D-01 rangeNm dedup is wired through deriveInstrumentReadouts().
- ChartPanel.tsx's existing test suite passes with only the wiring change, no assertion edits (RFCT-08 partial).
</success_criteria>

<output>
Create `.planning/phases/12-chartpanel-sandboxcontainer-decomposition-refactor/12-01-SUMMARY.md` when done
</output>
