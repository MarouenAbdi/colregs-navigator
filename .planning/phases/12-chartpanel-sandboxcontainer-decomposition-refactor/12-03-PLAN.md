---
phase: 12-chartpanel-sandboxcontainer-decomposition-refactor
plan: 03
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - src/components/sandbox/hooks/useContainerSize.ts
  - src/components/sandbox/ChartBackdrop.tsx
  - src/components/sandbox/ChartPanel.tsx
autonomous: true
requirements: [RFCT-03, RFCT-08]

must_haves:
  truths:
    - "Decorative chart chrome (defs/fine-grid/background/gridlines/range-rings/crosshair/N-label) lives in ChartBackdrop.tsx, not inline in ChartPanel.tsx (RFCT-03)"
    - "Resize observation lives in useContainerSize(), not inline in ChartPanel.tsx (RFCT-03)"
    - "ChartBackdrop.tsx has zero pointer-event handlers and zero internal state -- purely presentational"
    - "ChartPanel.test.tsx's MockResizeObserver polyfill keeps working unmodified after the hook extraction"
  artifacts:
    - path: "src/components/sandbox/hooks/useContainerSize.ts"
      provides: "containerRef/containerSize tracked via ResizeObserver"
      exports: ["useContainerSize"]
    - path: "src/components/sandbox/ChartBackdrop.tsx"
      provides: "decorative chart chrome JSX (fine-grid, gridlines, range rings, crosshair, N label)"
      exports: ["ChartBackdrop", "ChartBackdropProps"]
  key_links:
    - from: "src/components/sandbox/ChartPanel.tsx"
      to: "src/components/sandbox/hooks/useContainerSize.ts"
      via: "const { containerRef, containerSize } = useContainerSize();"
      pattern: "useContainerSize\\("
    - from: "src/components/sandbox/ChartPanel.tsx"
      to: "src/components/sandbox/ChartBackdrop.tsx"
      via: "<ChartBackdrop containerSize=... chartCenter=... innerRingRadiusPx=... outerRingRadiusPx=... />"
      pattern: "<ChartBackdrop"
---

<objective>
Extract ChartPanel.tsx's resize-observation state into a `useContainerSize()` hook, and its
decorative chart chrome (fine-grid pattern, gridlines, range rings, crosshair, "N" label) into a
new `ChartBackdrop.tsx` presentational component. Wire both back into ChartPanel.tsx.

Purpose: RFCT-03 requires both extractions. This is ARCHITECTURE.md's Extraction 3+4 -- rated
"Zero"/"Low" risk (no pointer handlers in either region). Depends on Plan 12-01 because both
plans modify ChartPanel.tsx (shared-file dependency forces sequential waves, not because of any
logical ordering requirement -- ARCHITECTURE.md confirms extractions 1-4b have no dependency on
each other).
Output: `hooks/useContainerSize.ts`, `ChartBackdrop.tsx`, and a further-thinned `ChartPanel.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-chartpanel-sandboxcontainer-decomposition-refactor/12-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/12-chartpanel-sandboxcontainer-decomposition-refactor/12-PATTERNS.md
@.planning/phases/12-chartpanel-sandboxcontainer-decomposition-refactor/12-01-SUMMARY.md
</context>

<interfaces>
<!-- Locked signatures from ARCHITECTURE.md -- use exactly, no deviation. -->

```typescript
// ChartBackdrop.tsx -- new, presentational only, no pointer handlers
export interface ChartBackdropProps {
  containerSize: ContainerSize;
  chartCenter: { screenX: number; screenY: number };
  innerRingRadiusPx: number;
  outerRingRadiusPx: number;
}
export function ChartBackdrop(props: ChartBackdropProps): JSX.Element;

// hooks/useContainerSize.ts -- new
export function useContainerSize(): {
  containerRef: React.RefObject<HTMLDivElement>;
  containerSize: ContainerSize | null;
};
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create hooks/useContainerSize.ts</name>
  <files>src/components/sandbox/hooks/useContainerSize.ts</files>
  <read_first>
    src/components/sandbox/ChartPanel.tsx (current, post-12-01 state -- the containerRef/containerSize useState + ResizeObserver useEffect block)
    src/components/sandbox/hooks/useHullDrag.ts (doc-comment convention for this folder's hooks)
    src/components/sandbox/ChartPanel.test.tsx (lines 26-59: MockResizeObserver polyfill -- this hook must keep working with it unmodified)
  </read_first>
  <action>
    Create `src/components/sandbox/hooks/useContainerSize.ts`. Export function
    `useContainerSize()` returning `{ containerRef, containerSize }` per the locked signature in
    this plan's `<interfaces>` block. Move verbatim from `ChartPanel.tsx`: `const containerRef =
    useRef<HTMLDivElement>(null);`, `const [containerSize, setContainerSize] =
    useState<ContainerSize | null>(null);`, and the `useEffect` `ResizeObserver` block (constructs
    a `ResizeObserver`, calls `observer.observe(element)`, returns `() =>
    observer.disconnect()`). Wrap all three inside the hook body and `return { containerRef,
    containerSize };` at the end. Imports: `useEffect`, `useRef`, `useState` from `"react"`; `type
    ContainerSize` from `"../../../domain/geometry/screen-convert.js"`. Add a top-of-file doc
    comment following `useHullDrag.ts`'s convention: state this hook's single responsibility
    ("tracks the chart container's live pixel size via ResizeObserver, used to drive
    chartToScreen/screenToChart"), reference RFCT-03. This hook still calls the same global
    `ResizeObserver` constructor `ChartPanel.test.tsx`'s `MockResizeObserver` polyfill stubs --
    no test-file change is required for this extraction.
  </action>
  <verify>
    <automated>grep -n "export function useContainerSize" src/components/sandbox/hooks/useContainerSize.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function useContainerSize" src/components/sandbox/hooks/useContainerSize.ts` matches
    - `grep -n "new ResizeObserver" src/components/sandbox/hooks/useContainerSize.ts` matches
    - `grep -n "return { containerRef, containerSize }" src/components/sandbox/hooks/useContainerSize.ts` matches
  </acceptance_criteria>
  <done>useContainerSize.ts exists, exports the locked { containerRef, containerSize } shape, and moves the ResizeObserver effect verbatim.</done>
</task>

<task type="auto">
  <name>Task 2: Create ChartBackdrop.tsx</name>
  <files>src/components/sandbox/ChartBackdrop.tsx</files>
  <read_first>
    src/components/sandbox/ChartPanel.tsx (current, post-12-01 -- locate the `<defs>`...`<text>N</text>` decorative JSX block inside the `<svg>` by content, not a hardcoded line number, since 12-01 already edited this file)
    src/components/shared/SectionGridBackground.tsx (whole file -- presentational-only, props-in/JSX-out pattern analog)
    src/components/sandbox/chart-panel-geometry.ts (constants + buildGridLineSegments this component consumes)
  </read_first>
  <action>
    Create `src/components/sandbox/ChartBackdrop.tsx` as a presentational component. Export
    interface `ChartBackdropProps` and `function ChartBackdrop(props: ChartBackdropProps)` per the
    locked signature in this plan's `<interfaces>` block. Move verbatim, as this component's single
    returned JSX fragment, the decorative chrome currently inline in `ChartPanel.tsx`'s `<svg>`:
    the `<defs>` fine-grid `<pattern id="chart-fine-grid">`, the background `<rect fill="url(#chart-fine-grid)")`,
    the grid-lines `<g>` (using `buildGridLineSegments(props.containerSize, CHART_VIEW_BOX).map((seg)
    => (<line key={seg.key} x1={seg.x1} y1={seg.y1} x2={seg.x2} y2={seg.y2} stroke={GRID_STROKE}
    strokeWidth={1} />))` -- import `CHART_VIEW_BOX` here since it is not part of the locked
    `ChartBackdropProps` shape), the range-rings `<g stroke={RANGE_RING_STROKE} fill="none">` (two
    `<circle>` elements using `props.chartCenter`/`props.innerRingRadiusPx`/`props.outerRingRadiusPx`),
    the crosshair `<g stroke={CROSSHAIR_STROKE} strokeWidth={1}>` (two `<line>` elements, same
    props), and the "N" label `<text>`. Confirm zero pointer handlers move with this block (none
    exist in the source region). Imports: `FINE_GRID_CELL_PX`, `FINE_GRID_STROKE`, `GRID_STROKE`,
    `RANGE_RING_STROKE`, `CROSSHAIR_STROKE`, `CHART_VIEW_BOX`, `buildGridLineSegments` from
    `"./chart-panel-geometry.js"`; `type ContainerSize` from
    `"../../domain/geometry/screen-convert.js"`. No `useState`/`useEffect`, no pointer-handler
    props, no default `"react"` import needed.
  </action>
  <verify>
    <automated>grep -n "export function ChartBackdrop" src/components/sandbox/ChartBackdrop.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "export function ChartBackdrop" src/components/sandbox/ChartBackdrop.tsx` matches
    - `grep -n "export interface ChartBackdropProps" src/components/sandbox/ChartBackdrop.tsx` matches
    - `grep -n "onPointer" src/components/sandbox/ChartBackdrop.tsx` returns no matches
    - `grep -n "useState\|useEffect" src/components/sandbox/ChartBackdrop.tsx` returns no matches
  </acceptance_criteria>
  <done>ChartBackdrop.tsx exists, exports ChartBackdropProps + ChartBackdrop(), contains the fine-grid/gridlines/range-rings/crosshair/N-label JSX moved verbatim, zero pointer handlers or internal state.</done>
</task>

<task type="auto">
  <name>Task 3: Wire useContainerSize() and ChartBackdrop into ChartPanel.tsx, run full regression suite</name>
  <files>src/components/sandbox/ChartPanel.tsx</files>
  <read_first>
    src/components/sandbox/ChartPanel.tsx (current, post-12-01)
    src/components/sandbox/hooks/useContainerSize.ts (just created)
    src/components/sandbox/ChartBackdrop.tsx (just created)
    src/components/sandbox/ChartPanel.test.tsx (regression gate -- all 4 tests must keep passing)
  </read_first>
  <action>
    Modify `ChartPanel.tsx`: replace the inline `containerRef`/`containerSize` `useState` +
    `ResizeObserver` `useEffect` with `const { containerRef, containerSize } =
    useContainerSize();`, importing `useContainerSize` from `"./hooks/useContainerSize.js"`.
    Remove `useEffect`/`useRef`/`useState` from ChartPanel.tsx's own `"react"` import if nothing
    else in the remaining component body needs them directly. Replace the `<defs>`...`<text>N</text>`
    decorative block (including the grid-line `<g>` wired in Plan 12-01's Task 3) with:
    `<ChartBackdrop containerSize={containerSize} chartCenter={overlay.chartCenter}
    innerRingRadiusPx={overlay.innerRingRadiusPx} outerRingRadiusPx={overlay.outerRingRadiusPx}
    />`, importing `ChartBackdrop` from `"./ChartBackdrop.js"`. Remove now-unused imports from
    ChartPanel.tsx's `"./chart-panel-geometry.js"` import line: `FINE_GRID_CELL_PX`,
    `FINE_GRID_STROKE`, `RANGE_RING_STROKE`, `CROSSHAIR_STROKE`, `GRID_STROKE`,
    `buildGridLineSegments` -- all now only consumed inside `ChartBackdrop.tsx`. Keep every
    constant still directly referenced by the still-inline `VesselGroup`/cone/bearing-line/tooltip
    JSX in `ChartPanel.tsx` (verify by grepping each constant name against the file's remaining
    body before removing its import). Do not touch `VesselGroup`, the drag-hook calls, or any JSX
    outside the pieces listed above -- `VesselGroup` moves in Plan 12-04.
  </action>
  <verify>
    <automated>npx vitest run src/components/sandbox/ChartPanel.test.tsx && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/sandbox/ChartPanel.test.tsx` passes, all 4 existing tests green
    - `npx vitest run` (full suite) exits 0
    - `npx tsc --noEmit` exits 0
    - `grep -n "new ResizeObserver" src/components/sandbox/ChartPanel.tsx` returns no matches (moved out)
    - `grep -n "<ChartBackdrop" src/components/sandbox/ChartPanel.tsx` matches
    - `wc -l < src/components/sandbox/ChartPanel.tsx` prints a value under 260 (VesselGroup is still inline at this point -- the final ~150-200 line RFCT-07 gate is checked in Plan 12-04 after VesselGroup's extraction)
  </acceptance_criteria>
  <done>ChartPanel.tsx calls useContainerSize() and renders <ChartBackdrop> instead of owning resize-observation state and decorative chrome inline; full test suite and typecheck pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | Pure internal extraction of decorative/observational presentation code. No new user input, endpoint, or schema. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-12-03 | Tampering | ChartBackdrop.tsx / useContainerSize.ts | accept | Purely decorative/observational code with zero pointer handlers and zero user input -- confirmed via `grep -n "onPointer" ChartBackdrop.tsx` returning no matches (Task 2 acceptance criteria). |
| T-12-03-SC | Tampering (supply chain) | N/A -- no new npm/pip/cargo package installs in this plan | accept | This plan only relocates existing first-party code between files; no dependency changes. |
</threat_model>

<verification>
- `npx vitest run` exits 0 for the full suite, including the unmodified ChartPanel.test.tsx.
- `npx tsc --noEmit` exits 0.
- `grep -n "onPointer" src/components/sandbox/ChartBackdrop.tsx` returns no matches.
</verification>

<success_criteria>
- Decorative chart chrome and resize observation live in ChartBackdrop.tsx and useContainerSize.ts (RFCT-03).
- ChartPanel.test.tsx's MockResizeObserver polyfill keeps working unmodified.
- The existing test suite passes with only the wiring change, no assertion edits (RFCT-08 partial).
</success_criteria>

<output>
Create `.planning/phases/12-chartpanel-sandboxcontainer-decomposition-refactor/12-03-SUMMARY.md` when done
</output>
