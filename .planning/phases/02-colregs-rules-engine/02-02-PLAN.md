---
phase: 02-colregs-rules-engine
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/domain/colregs/classify-encounter.ts
  - src/domain/colregs/classify-encounter.fixtures.ts
  - src/domain/colregs/classify-encounter.test.ts
autonomous: true
requirements: [CLAS-01, CLAS-02, CLAS-03, CLAS-04, DETM-01, DETM-02, RSON-02]

must_haves:
  truths:
    - "classifyEncounter() classifies head-on, crossing, and overtaking per Rules 12-15 using relative bearing computed in BOTH directions (never a single-direction check), against a textbook fixture suite (CLAS-01)"
    - "Overtaking is evaluated before head-on/crossing per Rule 13's precedence, and an established overtaking classification stays 'overtaking' when previous='overtaking' is supplied and Rule 7's gate still passes -- but fully releases (not softens) to fresh geometric classification the instant the gate fails (CLAS-02, D-01, D-02, D-03, D-04)"
    - "Diverging/parallel vessels (Rule 7 gate fails: no-closure or negative TCPA or DCPA over 1.0nm) never receive riskOfCollision=true, independent of what encounterType geometry alone would produce (CLAS-03)"
    - "Near-boundary cases at the 112.5 degree overtaking/crossing edge and the 0 degree head-on edge return one definite EncounterType plus an explicit doubt flag identifying which boundary -- never a 4th ambiguous type, never a hard unflagged cutoff (CLAS-04, D-09, D-10, D-11, D-12)"
    - "Give-way/stand-on determination applies Rule 18's vessel-type hierarchy as an override on the geometric baseline for crossing/overtaking, and additionally assigns give-way/stand-on for head-on encounters ONLY when the two vessels' types differ in priority tier -- same-tier (including the NUC/RIATM co-equal tie) head-on encounters keep giveWay/standOn null, the mutual-obligation case (DETM-01, DETM-02)"
    - "Every ClassificationResult carries an ordered reasoning trail (ruleId + text + facts) built inline at each dispatch stage in Rule7->13->14->15->18 order, never a separate re-derived explanation function (RSON-02, D-13, D-14, D-15, D-16)"
  artifacts:
    - path: "src/domain/colregs/classify-encounter.ts"
      provides: "classifyEncounter(vesselA: Vessel, vesselB: Vessel, previous?: EncounterType): Result<ClassificationResult>"
      contains: "export function classifyEncounter"
    - path: "src/domain/colregs/classify-encounter.fixtures.ts"
      provides: "hand-derived fixtures covering both-direction overtaking/head-on, hysteresis hold+release, doubt-band boundaries, Rule 18 override matrix, and Stage 0 propagation"
    - path: "src/domain/colregs/classify-encounter.test.ts"
      provides: "full dispatch test suite, one describe block per decision cluster"
  key_links:
    - from: "src/domain/colregs/classify-encounter.ts"
      to: "src/domain/colregs/risk-of-collision.ts"
      via: "classifyEncounter() calls riskOfCollision(cpaResult) in Stage 1 to gate confident give-way verdicts and determine the sticky-overtaking release condition"
      pattern: "riskOfCollision\\("
    - from: "src/domain/colregs/classify-encounter.ts"
      to: "src/domain/colregs/vessel-priority.ts"
      via: "classifyEncounter() calls vesselPriority()/rule18Overrides() in Stage 6 to apply the Rule 18 hierarchy override"
      pattern: "rule18Overrides\\(|vesselPriority\\("
    - from: "src/domain/colregs/classify-encounter.ts"
      to: "src/domain/geometry/relative-bearing.ts"
      via: "classifyEncounter() calls relativeBearing() in both directions (A,B) and (B,A) as its sole geometric input for Rule 13/14 classification"
      pattern: "relativeBearing\\("
    - from: "src/domain/colregs/classify-encounter.ts"
      to: "src/domain/geometry/cpa.ts"
      via: "classifyEncounter() calls cpa(A,B) once and feeds its Result into riskOfCollision()"
      pattern: "cpa\\("
---

<objective>
Implement `classifyEncounter(vesselA, vesselB, previous?)` — the sequential Rule 7 -> 13 -> 14 -> 15 -> 18 dispatch function that is the entire portfolio-value proposition of this project — plus a fixture suite deep enough to serve as the auditable proof of correctness CONTEXT.md calls for.

Purpose: Every prior file in Phase 2 (Plan 01) and Phase 1 exists to be composed here. This function is the highest-risk-of-subtle-bugs work in the project (per 02-RESEARCH.md's directional-convention pitfalls) and the sole reason an interviewer would read this codebase.
Output: `classify-encounter.ts` with the full 6-stage dispatch, `classify-encounter.fixtures.ts` with hand-derived boundary/hysteresis/override fixtures, and a passing Vitest suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-colregs-rules-engine/02-CONTEXT.md
@.planning/phases/02-colregs-rules-engine/02-RESEARCH.md
@.planning/phases/02-colregs-rules-engine/02-PATTERNS.md

<interfaces>
<!-- Contracts created by Plan 01 (02-01-PLAN.md) -- Plan 01 must execute first. Use these directly, no exploration needed. -->

From src/domain/colregs/types.ts (created by Plan 01, Task 1):
```typescript
export type EncounterType = 'head-on' | 'crossing' | 'overtaking';
export type VesselLabel = 'vesselA' | 'vesselB';
export type DoubtBoundary = 'near-overtaking-crossing-boundary' | 'near-head-on-boundary';
export interface ReasoningTrailEntry { ruleId: string; text: string; facts: Record<string, number | string | boolean>; }
export interface GiveWayResult { giveWay: VesselLabel | null; standOn: VesselLabel | null; }
export interface ClassificationResult {
  encounterType: EncounterType;
  riskOfCollision: boolean;
  giveWay: VesselLabel | null;
  standOn: VesselLabel | null;
  doubt: boolean;
  doubtBoundary?: DoubtBoundary;
  trail: ReasoningTrailEntry[];
}
```

From src/domain/colregs/risk-of-collision.ts (created by Plan 01, Task 2):
```typescript
export const RISK_OF_COLLISION_DCPA_THRESHOLD_NM = 1.0;
export function riskOfCollision(cpaResult: Result<{ tcpaMinutes: number; dcpaNm: number }>): boolean;
```

From src/domain/colregs/vessel-priority.ts (created by Plan 01, Task 3):
```typescript
export function vesselPriority(type: VesselType): number; // 1=highest (NUC/RIATM) ... 4=lowest (power-driven)
export function rule18Overrides(geometricGiveWayType: VesselType, geometricStandOnType: VesselType): boolean;
```

From src/domain/geometry/relative-bearing.ts and cpa.ts (Phase 1, unchanged):
```typescript
export function relativeBearing(own: Vessel, contact: Vessel): Result<number>; // (-180, 180]
export function cpa(vesselA: Vessel, vesselB: Vessel): Result<{ tcpaMinutes: number; dcpaNm: number }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: classifyEncounter() dispatch (Stages 0-6) + core fixture set</name>
  <files>src/domain/colregs/classify-encounter.ts, src/domain/colregs/classify-encounter.fixtures.ts, src/domain/colregs/classify-encounter.test.ts</files>
  <read_first>
    .planning/phases/02-colregs-rules-engine/02-RESEARCH.md "Architecture Patterns" full diagram (Stages 0-6), "Pattern 1: Sequential Rule Dispatch with Trail Accumulation", "Common Pitfalls" 1-4, "Code Examples" (verbatim rule text + fixture pattern)
    .planning/phases/02-colregs-rules-engine/02-CONTEXT.md decisions D-01 through D-16 (all of them -- this task implements the full decision set)
    .planning/phases/02-colregs-rules-engine/02-PATTERNS.md "src/domain/colregs/classify-encounter.ts" and ".fixtures.ts/.test.ts" sections
    src/domain/colregs/types.ts, src/domain/colregs/risk-of-collision.ts, src/domain/colregs/vessel-priority.ts (all three from Plan 01 -- read the actual files, not just the interfaces block above, to confirm exact exported names)
    src/domain/geometry/relative-bearing.ts, src/domain/geometry/cpa.ts (Phase 1 -- composition targets)
    src/domain/geometry/relative-bearing.fixtures.ts (reuse `headOnCase`, `crossingCase` vessel pairs directly per D-16 cross-phase reuse)
  </read_first>
  <behavior>
    Constants: `OVERTAKING_BOUNDARY_DEGREES = 112.5` (Rule 13(b)'s 22.5 abaft-the-beam, verbatim from rule text). `DOUBT_BAND_DEGREES = 5` (D-09/D-10, one shared constant for both boundaries). The head-on sector itself is defined as `|relativeBearing| <= DOUBT_BAND_DEGREES` on BOTH sides (Assumption A1 from 02-RESEARCH.md: since Rule 14 gives no numeric sector boundary, the ±5 doubt band IS the entire head-on sector -- every head-on classification therefore always carries `doubt: true, doubtBoundary: 'near-head-on-boundary'`, there is no separate wider "confident head-on, no doubt" core).

    Stage 0 (geometry inputs): compute `relativeBearing(vesselA, vesselB)`, `relativeBearing(vesselB, vesselA)`, `cpa(vesselA, vesselB)`. If either `relativeBearing` call fails (`!ok`), return that failure unchanged (both its possible reasons, `'invalid-input'` and `'coincident-position'`, are genuine Stage-0 errors). If `cpa()` fails with reason `'invalid-input'`, return that failure unchanged too. If `cpa()` fails with reason `'no-closure'`, do NOT propagate as an error -- keep the whole `Result` (still `!ok`) to hand to `riskOfCollision()` in Stage 1 (D-07: no-closure is a valid gate input, not a Stage-0 failure).

    Stage 1 (Rule 7 gate): `const risk = riskOfCollision(cpaResult);` push a trail entry `{ ruleId: 'Rule 7', text: ..., facts: { riskOfCollision: risk, ...(cpaResult.ok ? { tcpaMinutes: cpaResult.value.tcpaMinutes, dcpaNm: cpaResult.value.dcpaNm } : {}) } }`. Compute `const effectivePrevious = risk ? previous : undefined;` -- when the gate fails, `effectivePrevious` becomes `undefined` OUTRIGHT (D-02/D-03), not a softened/demoted hint (Pitfall 4).

    Stage 2 (sticky overtaking, D-01/D-02): if `effectivePrevious === 'overtaking'`, force `encounterType = 'overtaking'`, push a trail entry `{ ruleId: 'Rule 13(d)', text: 'Overtaking situation persists (once overtaking, always overtaking until finally past and clear) -- previous classification was overtaking and Rule 7 risk of collision still holds.', facts: {} }`, and determine direction WITHOUT the 112.5 threshold (bearing may have drifted below it -- that's the whole point of hysteresis): if `Math.abs(relBearingAtoB) >= Math.abs(relBearingBtoA)`, vesselB is overtaking vesselA (`giveWay: 'vesselB', standOn: 'vesselA'`); otherwise vesselA is overtaking vesselB (`giveWay: 'vesselA', standOn: 'vesselB'`). Set `doubt = false` (no boundary check ran). SKIP Stages 3, 4, 5 entirely (no ruled-out trail entries for Rule 13/14/15 in the sticky path -- jump straight to Stage 6).

    Stage 3 (Rule 13, both directions, only when not sticky): `const bOvertakesA = Math.abs(relBearingAtoB) > OVERTAKING_BOUNDARY_DEGREES;` (bearing from A to B is >22.5 abaft A's beam -> B is overtaking A, A is being overtaken -- Pitfall 1). `const aOvertakesB = Math.abs(relBearingBtoA) > OVERTAKING_BOUNDARY_DEGREES;`. If either matches: `encounterType = 'overtaking'`, `giveWay`/`standOn` set to the overtaking vessel / overtaken vessel respectively, push a matched trail entry citing Rule 13(a)-(b) with the triggering `relativeBearing` fact, compute doubt: `Math.abs(Math.abs(triggeringBearing) - OVERTAKING_BOUNDARY_DEGREES) <= DOUBT_BAND_DEGREES` -> if true, `doubt = true, doubtBoundary = 'near-overtaking-crossing-boundary'`. If neither matches, push a ruled-out entry ("not overtaking in either direction") and proceed to Stage 4.

    Stage 4 (Rule 14, both directions, only when Stage 3 ruled out): head-on requires `Math.abs(relBearingAtoB) <= DOUBT_BAND_DEGREES && Math.abs(relBearingBtoA) <= DOUBT_BAND_DEGREES` (Pitfall 2: BOTH directions, not one). If matched: `encounterType = 'head-on'`, `giveWay = null, standOn = null` (baseline -- Stage 6 may still populate these if vessel types differ), `doubt = true, doubtBoundary = 'near-head-on-boundary'` (always, per the Assumption A1 note above), push a matched trail entry citing Rule 14(a)-(b) with both relative bearings as facts. If not matched, push a ruled-out entry and proceed to Stage 5.

    Stage 5 (Rule 15, residual, only when Stage 4 ruled out): `encounterType = 'crossing'`. Give-way is whichever vessel has the other on her starboard side: if `relBearingAtoB > 0`, `giveWay = 'vesselA', standOn = 'vesselB'`; else `giveWay = 'vesselB', standOn = 'vesselA'` (per dispatch order, `relBearingAtoB` is never exactly 0 or the sign-flip boundary at this stage since Stages 3-4 already excluded the overtaking/head-on sectors -- confirmed by 02-RESEARCH.md Open Question 2). `doubt = false` unless within `DOUBT_BAND_DEGREES` of `OVERTAKING_BOUNDARY_DEGREES` in EITHER direction (same doubt check as Stage 3, reused since a crossing classification can still be near the overtaking edge from the non-triggering side). Push a trail entry citing Rule 15.

    Stage 6 (Rule 18 override, always runs): if `encounterType` is `'crossing'` or `'overtaking'` (giveWay/standOn are non-null vessel labels): look up each vessel's `.type`, call `rule18Overrides(giveWayVesselType, standOnVesselType)`; if `true`, swap `giveWay`/`standOn`. If `encounterType === 'head-on'` (baseline giveWay/standOn are null): compare `vesselPriority(vesselA.type)` vs `vesselPriority(vesselB.type)`; if they differ, the vessel with the HIGHER priority number (lower real-world priority) becomes `giveWay`, the other becomes `standOn`; if equal (including the NUC/RIATM tie), leave `giveWay`/`standOn` as `null`/`null` (mutual obligation, DETM-02). Push a trail entry either way (override applied, no-op-because-baseline-already-correct, or no-op-because-types-equal) citing Rule 18(a)-(c).

    Return `ok({ encounterType, riskOfCollision: risk, giveWay, standOn, doubt, doubtBoundary, trail })`.

    Core fixture set (exact vessel values -- use these, do not re-derive):
    - `overtakingBothDirectionsCase`: `vesselA={position:{x:0,y:0},heading:0,speed:8,type:'power-driven'}`, `vesselB={position:{x:0.5,y:-0.8660254},heading:0,speed:15,type:'power-driven'}`. `relativeBearing(A,B)=150` (same angle as Phase 1's `overtakingCase`, scaled to 1/10 magnitude so DCPA falls under threshold). `cpa` yields `tcpaMinutes~7.42, dcpaNm~0.5`. Expected: `encounterType='overtaking', riskOfCollision=true, giveWay='vesselB', standOn='vesselA', doubt=false`. ALSO assert calling `classifyEncounter(vesselB, vesselA)` (swapped argument order) yields `encounterType='overtaking', giveWay='vesselA'` -- the same physical overtaking vessel, now in the first argument position, is still correctly identified (Pitfall 1 symmetry check).
    - `overtakingHysteresisHoldsCase`: `vesselA={position:{x:0,y:0},heading:0,speed:0,type:'power-driven'}`, `vesselB={position:{x:5,y:0},heading:270,speed:15,type:'power-driven'}`, `previous='overtaking'`. `relativeBearing(A,B)=90` (reads as crossing geometrically), `relativeBearing(B,A)=0`. `cpa` yields `tcpaMinutes=20, dcpaNm=0` (direct collision course -- gate holds). Expected: `encounterType='overtaking'` (sticky, D-02), `riskOfCollision=true`, `giveWay='vesselB', standOn='vesselA'` (via the magnitude tie-break: `|90| >= |0|`).
    - `overtakingHysteresisReleasesCase`: `vesselA={position:{x:0,y:0},heading:0,speed:8,type:'power-driven'}`, `vesselB={position:{x:5,y:0},heading:270,speed:15,type:'power-driven'}`, `previous='overtaking'`. `relativeBearing(A,B)=90`. `cpa` yields `tcpaMinutes~15.57, dcpaNm~2.353` (over the 1.0nm threshold -- gate FAILS). Expected: `encounterType='crossing'` (sticky RELEASES, not softened -- Pitfall 4 regression), `riskOfCollision=false`. ALSO assert this result is deep-equal (encounterType/giveWay/standOn) to calling `classifyEncounter(vesselA, vesselB)` with NO `previous` argument at all on the identical vessels -- proving full release, not partial bias.
    - `headOnGenuineCase`: reuse Phase 1's `headOnCase` vessels directly (`own`->`vesselA`, `contact`->`vesselB`, both `type:'power-driven'`). Both `relativeBearing` directions `=0`. Expected: `encounterType='head-on', doubt=true, doubtBoundary='near-head-on-boundary', giveWay=null, standOn=null` (same-type tie).
    - `headOnPitfall2OneSidedCase`: `vesselA={position:{x:0,y:0},heading:0,speed:10,type:'power-driven'}`, `vesselB={position:{x:0.5234,y:9.9863},heading:100,speed:10,type:'power-driven'}`. `relativeBearing(A,B)=3` (within the ±5 head-on sector from A's side alone), `relativeBearing(B,A)=83` (NOT within ±5 from B's side -- non-reciprocal heading). Expected: `encounterType='crossing'` (head-on check correctly requires BOTH directions -- Pitfall 2), `giveWay='vesselA', standOn='vesselB'` (relativeBearing(A,B)=3>0), `doubt=false`.
    - `crossingResidualBasicCase`: reuse Phase 1's `crossingCase` vessels directly, both `type:'power-driven'`. `relativeBearing(A,B)=90, relativeBearing(B,A)=0`. Expected: `encounterType='crossing', giveWay='vesselA', standOn='vesselB', doubt=false` (no Rule 18 override, same type).
    - `doubtBandNearOvertakingBoundaryCase` (110 degrees, still crossing): `vesselA={position:{x:0,y:0},heading:0,speed:10,type:'power-driven'}`, `vesselB={position:{x:9.397,y:-3.420},heading:290,speed:10,type:'power-driven'}`. `relativeBearing(A,B)=110, relativeBearing(B,A)=0`. Expected: `encounterType='crossing'` (110 <= 112.5), `doubt=true, doubtBoundary='near-overtaking-crossing-boundary'` (within ±5 of 112.5), `giveWay='vesselA', standOn='vesselB'`.
    - `doubtBandJustOverOvertakingBoundaryCase` (115 degrees, now overtaking): `vesselA={position:{x:0,y:0},heading:0,speed:10,type:'power-driven'}`, `vesselB={position:{x:9.063,y:-4.226},heading:295,speed:10,type:'power-driven'}`. `relativeBearing(A,B)=115, relativeBearing(B,A)=0`. Expected: `encounterType='overtaking'` (115 > 112.5), `doubt=true, doubtBoundary='near-overtaking-crossing-boundary'`, `giveWay='vesselB', standOn='vesselA'`.
    - `headOnBoundaryInclusiveCase` (exactly 5 degrees both directions): `vesselA={position:{x:0,y:0},heading:0,speed:10,type:'power-driven'}`, `vesselB={position:{x:0.8716,y:9.9619},heading:180,speed:10,type:'power-driven'}`. `relativeBearing(A,B)=5, relativeBearing(B,A)=5`. Expected: `encounterType='head-on'` (inclusive boundary), `doubt=true, doubtBoundary='near-head-on-boundary'`.
    - `justOutsideHeadOnSectorCase` (5.5 degrees both directions): `vesselA={position:{x:0,y:0},heading:0,speed:10,type:'power-driven'}`, `vesselB={position:{x:0.9585,y:9.9539},heading:180,speed:10,type:'power-driven'}`. `relativeBearing(A,B)=5.5, relativeBearing(B,A)=5.5`. Expected: `encounterType='crossing'` (outside the head-on sector -- fails the `<=5` test), `doubt=false` (no doubt band extends beyond the sector itself per Assumption A1), `giveWay='vesselA', standOn='vesselB'`.

    Write each fixture as a named exported const `{ vesselA, vesselB, previous?, expected... }` in classify-encounter.fixtures.ts with an inline comment showing the worked relative-bearing/cpa derivation (values already computed above -- transcribe them, do not re-derive from scratch). Write classify-encounter.test.ts with `describe` blocks grouped per 02-PATTERNS.md's convention: `"classifyEncounter() overtaking direction (Rule 13)"`, `"classifyEncounter() hysteresis (D-01-D-04)"`, `"classifyEncounter() head-on (Rule 14, Pitfall 2)"`, `"classifyEncounter() crossing residual (Rule 15)"`, `"classifyEncounter() doubt band (D-09-D-12)"`.
  </behavior>
  <action>
    Implement `src/domain/colregs/classify-encounter.ts` exactly per the Stage 0-6 dispatch described in `<behavior>` above, importing `relativeBearing`/`cpa` from `../geometry/relative-bearing.js`/`../geometry/cpa.js`, `riskOfCollision` from `./risk-of-collision.js`, `rule18Overrides`/`vesselPriority` from `./vessel-priority.js`, and `EncounterType`/`ClassificationResult`/`ReasoningTrailEntry`/`VesselLabel` from `./types.js`. Export `export function classifyEncounter(vesselA: Vessel, vesselB: Vessel, previous?: EncounterType): Result<ClassificationResult>`. Build the trail as a local `const trail: ReasoningTrailEntry[] = []` array, pushing at each stage that actually executes (never re-derive the trail after computing the verdict -- accumulate inline, per RSON-02 and the anti-pattern warning in 02-RESEARCH.md). Write classify-encounter.fixtures.ts and classify-encounter.test.ts with the exact fixture values given in `<behavior>`.
  </action>
  <verify>
    <automated>npx vitest run src/domain/colregs/classify-encounter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `classify-encounter.test.ts` passes with all 10 fixtures from `<behavior>` present and asserted (including both the swapped-argument-order assertion and the no-previous-vs-released-previous deep-equal assertion).
    - `grep -c "relativeBearing(vesselA, vesselB)" src/domain/colregs/classify-encounter.ts` is at least 1 AND `grep -c "relativeBearing(vesselB, vesselA)" src/domain/colregs/classify-encounter.ts` is at least 1 (confirms both-direction geometry, not a single-direction shortcut -- Pitfall 1/2).
    - `grep -c "effectivePrevious" src/domain/colregs/classify-encounter.ts` is at least 2 (confirms the gate-failure release variable is actually used, not just computed and ignored).
    - `grep -c "riskOfCollision(" src/domain/colregs/classify-encounter.ts` equals 1 (single Stage-1 call site, per the architecture diagram).
    - A test exists asserting `overtakingHysteresisReleasesCase`'s result is deep-equal to the same vessels classified with no `previous` argument.
  </acceptance_criteria>
  <done>classifyEncounter() implements the full Rule 7->13->14->15->18 dispatch with both-direction bearing checks, sticky-overtaking hysteresis with correct release semantics, and inclusive doubt-band boundaries; the core 10-fixture suite passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rule 18 interaction matrix + Stage 0 propagation fixtures</name>
  <files>src/domain/colregs/classify-encounter.fixtures.ts, src/domain/colregs/classify-encounter.test.ts</files>
  <read_first>
    src/domain/colregs/classify-encounter.ts (implemented in Task 1 of this plan -- confirm the exact Stage 6 head-on-null-override branch and Stage 0 propagation logic before adding fixtures against it)
    src/domain/colregs/classify-encounter.fixtures.ts (Task 1's core fixtures -- reuse `crossingResidualBasicCase` and `headOnGenuineCase`'s vessel positions, only changing `.type`)
    .planning/phases/02-colregs-rules-engine/02-CONTEXT.md "Claude's Discretion" note on Rule 18 tie-break mechanics for head-on
    src/domain/geometry/relative-bearing.fixtures.ts `coincidentPropagationCase` (Phase 1 -- reuse directly for the Stage 0 propagation fixture)
    src/domain/geometry/cpa.fixtures.ts `parallelNoClosureCase` (reuse directly for the no-closure-does-not-propagate fixture)
  </read_first>
  <behavior>
    - `crossingRule18OverrideCase`: same positions as `crossingResidualBasicCase` (`vesselA={position:{x:0,y:0},heading:0,speed:10}`, `vesselB={position:{x:5,y:0},heading:270,speed:10}`), but `vesselA.type='fishing'`, `vesselB.type='power-driven'`. Geometric baseline: `giveWay='vesselA'(fishing), standOn='vesselB'(power-driven)`. `rule18Overrides('fishing','power-driven')=true` -> OVERRIDE. Expected final: `encounterType='crossing', giveWay='vesselB', standOn='vesselA'` (flipped -- power-driven must give way to fishing per Rule 18(a)(iii)).
    - `crossingRule18NonOverrideCase`: same positions, `vesselA.type='power-driven'`, `vesselB.type='fishing'`. Geometric baseline: `giveWay='vesselA'(power-driven), standOn='vesselB'(fishing)`. `rule18Overrides('power-driven','fishing')=false` -> no override (baseline already correct). Expected: `giveWay='vesselA', standOn='vesselB'` (unchanged).
    - `headOnRule18OverrideCase`: same positions as `headOnGenuineCase`, but `vesselA.type='sailing'`, `vesselB.type='power-driven'`. Baseline (head-on) is `giveWay=null, standOn=null`. Priorities differ (`sailing`=3, `power-driven`=4) -> Stage 6 assigns `giveWay='vesselB'` (power-driven, higher number/lower rank), `standOn='vesselA'` (sailing). Expected: `encounterType='head-on', doubt=true, giveWay='vesselB', standOn='vesselA'`.
    - `headOnNucRiatmTieCase`: same positions as `headOnGenuineCase`, but `vesselA.type='not-under-command'`, `vesselB.type='restricted-in-ability-to-maneuver'`. Priorities equal (both 1) -> no override. Expected: `giveWay=null, standOn=null` (mutual obligation stands even between two special-status vessels -- Rule 18 gives no ranking between them, DETM-02).
    - `stage0CoincidentPropagationCase`: reuse Phase 1's `coincidentPropagationCase` vessels directly (`own`->`vesselA`, `contact`->`vesselB`, identical positions). Expected: `classifyEncounter(vesselA, vesselB)` returns `{ ok: false, reason: 'coincident-position' }`, propagated unchanged from `relativeBearing()`'s own failure -- no re-wrapping.
    - `noClosureDoesNotPropagateCase`: reuse Phase 1's `parallelNoClosureCase` vessels directly (both `type:'power-driven'`). `relativeBearing(A,B)=90, relativeBearing(B,A)=270`(raw)/`-90`(normalized). `cpa()` returns `err('no-closure', ...)`. Expected: `classifyEncounter(vesselA, vesselB)` returns `ok(...)` (NOT propagated as an error, per D-07), with `encounterType='crossing', riskOfCollision=false, giveWay='vesselA', standOn='vesselB'`.
  </behavior>
  <action>
    Add the six fixtures above to classify-encounter.fixtures.ts as named exported consts (each `{ vesselA, vesselB, expected... }`, or for the propagation cases `{ vesselA, vesselB, expectedOk: false, expectedReason: 'coincident-position' }`). Add corresponding `it` cases to classify-encounter.test.ts under two new `describe` blocks: `"classifyEncounter() Rule 18 override (DETM-02)"` (the four override/tie fixtures) and `"classifyEncounter() Stage 0 geometry propagation"` (the two propagation fixtures). No new implementation code is expected in classify-encounter.ts for this task -- if any of these six fixtures fails, the Stage 6 head-on-null-override branch or the Stage 0 no-closure-passthrough logic from Task 1 has a bug; fix `classify-encounter.ts` directly rather than adjusting the fixture's expected value to match incorrect behavior.
  </action>
  <verify>
    <automated>npx vitest run src/domain/colregs/classify-encounter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `classify-encounter.test.ts` passes with all 16 total fixtures (10 from Task 1 + 6 from this task).
    - A test exists asserting `rule18Overrides` fires for the fishing/power-driven crossing case (`giveWay` flips from `'vesselA'` to `'vesselB'`) AND a separate test asserting the reverse assignment does NOT flip.
    - A test exists asserting `headOnNucRiatmTieCase` keeps `giveWay`/`standOn` both `null` (not defaulted to either vessel).
    - A test exists asserting `stage0CoincidentPropagationCase` returns `result.reason === 'coincident-position'` with `result.ok === false`.
    - `grep -c "no-closure" src/domain/colregs/classify-encounter.ts` equals 0 (confirms `'no-closure'` is never referenced as a literal string comparison in the dispatch function itself -- it is handled generically by `riskOfCollision()` consuming the whole `Result`, per Plan 01's design, not by `classify-encounter.ts` special-casing the reason string).
  </acceptance_criteria>
  <done>The full Rule 18 override matrix (crossing override, crossing non-override, head-on override, head-on tie) and both Stage 0 Result-propagation paths (genuine failure vs. no-closure passthrough) are fixture-verified against the Task 1 implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Direct object-literal construction -> `classifyEncounter()` | A caller can construct `Vessel`-shaped `vesselA`/`vesselB` arguments bypassing `VesselSchema.parse()`; `classifyEncounter()` must not assume its inputs are already Zod-validated (same trust boundary Phase 1 established for `bearing()`/`relativeBearing()`/`cpa()`). |
| `previous` parameter -> sticky-overtaking logic | `previous` is an explicit, untrusted-in-principle caller-supplied value (not internal state) per D-01; a caller could pass an unexpected string. TypeScript's `EncounterType` union constrains this at compile time; no additional runtime validation is added since this is an internal domain function, not an external API boundary (that validation, if ever needed, belongs at Phase 3's tRPC input boundary). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | `src/domain/colregs/classify-encounter.ts` (Stage 0) | mitigate | Stage 0 explicitly checks `.ok` on both `relativeBearing()` calls and on `cpa()` (excluding the `'no-closure'` reason) and propagates any genuine failure unchanged before any arithmetic in Stages 1-6 runs -- non-finite/coincident input can never reach the classification logic and silently produce a wrong verdict; it is rejected at the boundary, matching Phase 1's `Number.isFinite`/`Result<T>` convention continued at this layer (per the threat_model_requirement's V5 Input Validation note). |
| T-02-04 | Tampering | `src/domain/colregs/classify-encounter.ts` (`previous` parameter) | accept | `previous` is typed as `EncounterType \| undefined` at the TypeScript boundary; a caller bypassing this (e.g. via `any`) supplying an unrecognized string would simply fail the `=== 'overtaking'` sticky check and fall through to fresh geometric classification (D-03's own "no bias, no hidden default" fresh-evaluation path) -- a safe default, not a crash or silent misclassification. No external/untrusted input boundary exists at this phase (Phase 3's tRPC layer, not yet built, is where a malicious `previous` value could first originate). |

No `high`-severity threats apply to this plan. This phase remains a pure, framework-free domain function with no I/O, network, auth, or persistence surface -- confirmed via 02-RESEARCH.md's Security Domain section, which found only V5 (Input Validation) applicable, addressed by T-02-03 above.

</threat_model>

<verification>
Run `npx vitest run src/domain/colregs/classify-encounter.test.ts` -- all 16 fixtures pass with zero failures. Run `npx vitest run` (full suite) -- all Phase 1 and Phase 2 tests pass together with zero regressions. Run `npx tsc --noEmit` -- no type errors anywhere in `src/domain/`. Confirm `classify-encounter.ts` imports from `./risk-of-collision.js`, `./vessel-priority.js`, `./types.js`, `../geometry/relative-bearing.js`, and `../geometry/cpa.js` (full composition, zero reimplemented trigonometry or threshold logic).
</verification>

<success_criteria>
- `classifyEncounter()` correctly classifies head-on/crossing/overtaking using both-direction relative bearing, with overtaking evaluated before head-on/crossing per Rule 13's precedence (CLAS-01, CLAS-02).
- Sticky overtaking holds while Rule 7's gate passes and fully releases (not softens) the instant it fails (CLAS-02, CLAS-03).
- Near-boundary cases at both the 112.5-degree and 0-degree edges return a definite classification plus an explicit, boundary-identifying doubt flag (CLAS-04).
- Rule 18's vessel-type hierarchy correctly overrides the geometric baseline for crossing/overtaking and correctly assigns give-way/stand-on for head-on encounters only when vessel-type priority differs, leaving the NUC/RIATM tie and same-type cases as a genuine mutual `null`/`null` (DETM-01, DETM-02).
- Every result's `trail` is built inline during dispatch in Rule7->13->14->15->18 order, never reverse-engineered after the verdict (RSON-02).
</success_criteria>

<output>
Create `.planning/phases/02-colregs-rules-engine/02-02-SUMMARY.md` when done
</output>
