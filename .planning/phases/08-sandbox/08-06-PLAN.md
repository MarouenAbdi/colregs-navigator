---
phase: 08-sandbox
plan: 06
type: execute
wave: 4
depends_on: ["08-05"]
files_modified: []
autonomous: false
requirements: [SBOX-01, SBOX-05]

must_haves:
  truths:
    - "A human has confirmed, in a real browser tab, that drag-to-reposition and drag-to-rotate both still work correctly against the restyled dark-theme hull/rotate-handle shapes"
    - "A human has confirmed the responsive breakpoints (900px stack order, 640px vessel-control-card stack) behave correctly, which jsdom/Vitest cannot verify"
  artifacts: []
  key_links: []
---

<objective>
Perform the mandatory manual browser UAT pass this phase's own goal statement and `.planning/STATE.md`'s Blockers/Concerns section both require before this phase's PR can merge: jsdom cannot detect a hit-testing regression on the hull/rotate-handle shapes (PITFALLS.md Pitfall 1), and cannot verify real responsive breakpoint behavior at all.

Purpose: every prior plan in this phase (08-02 through 08-05) restyled paint values and layout classes around the chart/controls/reasoning surfaces while explicitly preserving hit-testing logic and passing the existing Vitest/RTL suite unmodified where possible -- but a green `npm test` run is not sufficient proof for this specific regression class, per this project's own locked precedent.
Output: a human-confirmed PASS (or a documented list of issues to fix before merge) covering drag/rotate interaction, visual fidelity, and responsive layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-sandbox/08-CONTEXT.md
@.planning/phases/08-sandbox/08-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual browser verification of drag/rotate hit-testing, visual fidelity, chip row, Save/Reset, and responsive breakpoints</name>
  <read_first>
    src/components/sandbox/ChartPanel.tsx
    src/components/sandbox/SandboxContainer.tsx
    .planning/design/Main-Design.png
    .planning/phases/08-sandbox/08-UI-SPEC.md
  </read_first>
  <files></files>
  <action>
    No code changes in this task -- it is a pure human-verification gate over Plans 08-02 through 08-05's already-implemented restyle. Perform the 6 browser checks listed in `<how-to-verify>` below and report back "approved" or a concrete list of issues.
  </action>
  <what-built>
    The Sandbox section (chart, controls, reasoning trail, 6-chip preset row) has been fully restyled to the dark theme per the design mock, with a new 2-column responsive grid layout, a chip-driven scenario loader, and a split verdict-banner/instrument-readouts/reasoning-trail card structure. Chart hull/rotate-handle drag logic was explicitly NOT touched -- only paint values (fill/stroke classes) were changed.
  </what-built>
  <how-to-verify>
    Run `npm run dev` and open the app in a real browser tab (not just `npm test`) at the Sandbox section (`/#sandbox` on the home page, and separately `/s/[shareId]` for an existing saved scenario if one exists).

    1. **Drag-to-reposition (Pitfall 1, the highest-risk regression this phase carries):** click-and-drag Vessel A's hull to a new position on the chart. Confirm the vessel visually follows the pointer smoothly and the verdict banner/instrument readouts/reasoning trail all update live, with no visual snapping/offset glitch. Repeat for Vessel B.
    2. **Drag-to-rotate:** click-and-drag Vessel A's rotate handle (the small ringed circle beyond the bow) to change its heading. Confirm the hull visually rotates to track the pointer and the verdict re-derives live. Repeat for Vessel B.
    3. **Visual fidelity against the design:** compare the rendered Sandbox section against `.planning/design/Main-Design.png`'s Sandbox band -- verdict banner (rule badge color, title, description, role badges), chart colors (grid, cones, bearing line, hull colors, "1 NM" scale legend), instrument-readout tiles + status pill, numbered reasoning trail (colored dots/tags/connecting line), vessel-control cards (letter chip, role badge, Select, Slider, read-only Heading), and the 6-chip preset row (active chip's teal-accent fill vs. inactive outline pills).
    4. **Chip row:** click each of the 6 chips in turn ("Classic crossing", "Head-on meeting", "Overtaking", "Sailing has priority", "Not under command", "In doubt"). Confirm each one loads a visibly different vessel arrangement with no page reload, the verdict/role badges update correctly for each (in particular: "In doubt" should show mutual role badges and a "Rule 7" badge; "Not under command"/"Sailing has priority" should both show Vessel A given a give-way verdict after the Rule 18 override), and the active chip's highlight follows your clicks. Then manually drag a vessel after clicking a chip and confirm the chip highlight clears.
    5. **Save/Reset:** click the icon-only Save/Share button and confirm it redirects to a new `/s/{shareId}` URL; click "Reset scenario" and confirm the sandbox returns to its default scenario.
    6. **Responsive breakpoints (SBOX-05, cannot be verified by jsdom):** resize the browser window (or use devtools' responsive mode) across 900px -- confirm the layout collapses from the 2-column chart/reasoning grid to a single stacked column in the order Chart -> Reasoning (Instrument Readouts + Reasoning Trail) -> Controls. Continue narrowing across 640px -- confirm the two vessel-control cards stack to a single column.
  </how-to-verify>
  <acceptance_criteria>
    - Drag-to-reposition and drag-to-rotate both confirmed smooth and accurate for both vessels, with live verdict/readout/trail updates
    - Rendered Sandbox section visually matches `Main-Design.png`'s Sandbox band across all listed elements
    - All 6 chips load visibly distinct scenarios with correct verdicts/role badges/doubt treatment, and the active-chip highlight clears after a manual drag
    - Save redirects to a new `/s/{shareId}` URL; Reset restores the default scenario
    - Layout collapses to single-column (Chart -> Reasoning -> Controls) below 900px, and vessel-control cards stack below 640px
    - Human tester's resume-signal is "approved", or a concrete, itemized list of issues is recorded for follow-up
  </acceptance_criteria>
  <resume-signal>Type "approved" if all 6 checks pass, or describe exactly which check failed and what you observed (e.g. "drag-to-rotate on Vessel B doesn't track the pointer past 90 degrees" or "the 640px vessel-card stack doesn't trigger until 600px").</resume-signal>
  <verify>
    <manual>All 6 steps in how-to-verify completed with no issues found; human types "approved"</manual>
  </verify>
  <done>Human tester confirms drag/rotate hit-testing, visual fidelity, chip-row correctness, Save/Reset functionality, and both responsive breakpoints all behave correctly in a real browser.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

None -- this plan performs zero code changes. It is a verification gate over the code changes made in 08-02 through 08-05.

## STRIDE Threat Register

Not applicable -- no code is modified by this plan. Refer to 08-02 through 08-05's threat models for the actual implementation's threat register.
</threat_model>

<verification>
Human-confirmed "approved" resume-signal is the only verification for this plan -- no automated command applies to a manual browser interaction/visual check.
</verification>

<success_criteria>
- Drag-to-reposition and drag-to-rotate both confirmed working in a real browser for both vessels
- Visual fidelity confirmed against the design mock
- All 6 chips confirmed loading correct, distinct scenarios with correct verdicts/badges
- Save/Reset confirmed still functional
- Both responsive breakpoints (900px, 640px) confirmed behaving correctly
- Any issues found are documented for a follow-up gap-closure plan before this phase's PR merges
</success_criteria>

<output>
Create `.planning/phases/08-sandbox/08-06-SUMMARY.md` when done, recording the human's verdict and any follow-up issues.
</output>
