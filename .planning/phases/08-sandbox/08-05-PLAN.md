---
phase: 08-sandbox
plan: 05
type: execute
wave: 3
depends_on: ["08-01", "08-02", "08-03", "08-04"]
files_modified:
  - src/components/sandbox/SandboxContainer.tsx
  - src/components/sandbox/SandboxContainer.test.tsx
  - src/components/sandbox/CopyLinkButton.tsx
  - app/s/[shareId]/page.tsx
autonomous: true
requirements: [SBOX-02, SBOX-04, SBOX-05]

must_haves:
  truths:
    - "Clicking any of the 6 preset chips loads that scenario's exact vessel data into the live sandbox in place, with a full hysteresis reset, and no page navigation (D-01: in-place chip wiring; D-02: full-replace + hysteresis reset, mechanically identical to handleReset())"
    - "The verdict banner spans full width above a 2-column (chart | reasoning) grid at >=900px, collapsing to a single Chart -> Reasoning -> Controls stack below 900px, with the 2 vessel-control cards further stacking below 640px (D-06)"
    - "Save (relocated to an icon button) and Reset Scenario both still work exactly as before -- no regression to SCEN-01's create-and-redirect flow (D-07: Save/Share relocated to a header icon button, not dropped)"
    - "SandboxContainer no longer renders a nested <main> landmark (the app shell in app/layout.tsx already owns one)"
  artifacts:
    - path: "src/components/sandbox/SandboxContainer.tsx"
      provides: "Restyled container: new header copy, chip row, 2-col/full-width grid composition, handleChipSelect wired through the existing applyVesselUpdate choke point"
  key_links:
    - from: "src/components/sandbox/SandboxContainer.tsx"
      to: "src/components/sandbox/chip-scenarios.ts"
      via: "CHIP_SCENARIOS/CHIP_ORDER lookup in handleChipSelect"
      pattern: "CHIP_SCENARIOS"
    - from: "src/components/sandbox/SandboxContainer.tsx"
      to: "src/components/sandbox/VerdictBanner.tsx / InstrumentReadouts.tsx / ReasoningTrail.tsx"
      via: "direct composition in the new 2-col grid layout"
      pattern: "VerdictBanner|InstrumentReadouts|ReasoningTrail"
---

<objective>
Restyle `SandboxContainer.tsx`'s top-level composition -- new header copy/layout, the 6-chip preset row wired through the existing `applyVesselUpdate` choke point, and the design's 2-column (chart | reasoning) grid collapsing to a responsive single-column stack -- then rewrite `SandboxContainer.test.tsx` for the new markup, and restyle `CopyLinkButton.tsx` to an icon button.

Purpose: this is the plan that actually wires together every artifact the previous 4 plans built (tokens, Select/Slider, chip fixtures, the 3 split reasoning cards) into the one interactive surface a user sees; it is deliberately last so every dependency it needs already exists.
Output: restyled `SandboxContainer.tsx` with working chip row + responsive layout; rewritten `SandboxContainer.test.tsx`; restyled `CopyLinkButton.tsx`; a small spacing fix in `app/s/[shareId]/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-sandbox/08-CONTEXT.md
@.planning/phases/08-sandbox/08-RESEARCH.md
@.planning/phases/08-sandbox/08-UI-SPEC.md
</context>

<interfaces>
<!-- chip-scenarios.ts's exports this plan consumes (08-01 output) -->
```typescript
export type ChipId = "classic-crossing" | "head-on-meeting" | "overtaking" | "sailing-has-priority" | "not-under-command" | "in-doubt";
export const CHIP_SCENARIOS: Record<ChipId, { vesselA: Vessel; vesselB: Vessel }>;
export const CHIP_ORDER: readonly { id: ChipId; label: string }[]; // 6 entries, design order
```

<!-- New components this plan composes (08-04 output) -->
```typescript
<VerdictBanner classification={lastGoodClassification} isDegenerate={isDegenerate} />
<InstrumentReadouts vesselA={vesselA} vesselB={vesselB} classification={lastGoodClassification} isDegenerate={isDegenerate} />
<ReasoningTrail classification={lastGoodClassification} />
```

<!-- ControlPanelProps now requires classification (08-01/08-03) -->
```typescript
<ControlPanel vesselA={vesselA} vesselB={vesselB} classification={lastGoodClassification} onVesselSpeedChange={...} onVesselTypeChange={...} />
```

<!-- Current SandboxContainer.tsx's locked choke-point logic (read the full current file before starting -- this shape must NOT change) -->
```typescript
function applyVesselUpdate(nextA: Vessel, nextB: Vessel): void { /* VesselSchema.safeParse both, then classifyEncounter, unchanged */ }
function handleReset(): void {
  previousEncounterTypeRef.current = undefined;
  applyVesselUpdate(seedA, seedB);
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Restyle the header, retire the nested &lt;main&gt;, and rebuild the layout grid</name>
  <files>src/components/sandbox/SandboxContainer.tsx</files>
  <read_first>
    src/components/sandbox/SandboxContainer.tsx
    src/components/sandbox/types.ts
    src/components/hero/Hero.tsx
    src/components/ui/button.tsx
    app/layout.tsx
    app/page.tsx
  </read_first>
  <action>
    Change the component's root element from `<main className="flex flex-col gap-8 p-16">` to a plain `<div className="mx-auto max-w-300 px-5 py-12 min-[900px]:px-6 min-[900px]:py-16">` (or an equivalent section-scale wrapper matching Hero.tsx's own `max-w-300`/responsive-padding convention) -- `app/layout.tsx` already renders a single page-level `<main>` around every route's children, so a second nested `<main>` here is an invalid duplicate landmark, not a locked requirement; `app/page.tsx`'s existing `<section id="sandbox">` wrapper already supplies the anchor-scroll target, unaffected by this change.

    Replace the `<h1>COLREGS Navigator</h1>` + inline Save/Reset `<button>`s header block with the design's own section header (08-UI-SPEC.md Copywriting Contract, verbatim): an eyebrow line reading `"Interactive sandbox · night-display mode"` (Label role, 13px/600, Geist Mono, `text-muted-foreground`, uppercase tracking, plain text -- no accent dot, unlike Hero's eyebrow), a heading reading `"Drag a vessel — watch the verdict update"` (Heading role, 24px/600/1.25, `text-foreground`), and a subhead reading `"Drag a hull to reposition it, grab the bow handle to change heading, and adjust speed & type below. Classification recomputes live."` (Body role, 16px/600, `text-muted-foreground`), with the Save/Reset button row right-aligned on the same row as the heading (flex row, `justify-between`, wrapping to stack below 640px if needed).

    Rebuild the Save/Reset button row: Reset becomes `<Button variant="outline" onClick={handleReset}>` with a leading `RotateCcw` icon (from `lucide-react` -- verify the exact export name exists in the installed `lucide-react` version before using it) and text `"Reset scenario"` (lowercase "s", per the Copywriting Contract -- note this CHANGES the current button's exact accessible name from `"Reset Scenario"`, which `SandboxContainer.test.tsx` currently queries by name; Task 3 updates that query). Save becomes an icon-only `<Button variant="outline" size="icon-sm" aria-label="Save and share this scenario" onClick={() => createScenario.mutate({ vesselA, vesselB })} disabled={createScenario.isPending}>` containing a `Link2` icon (from `lucide-react`), placed immediately to the LEFT of the Reset button (D-07/Pitfall S3 -- preserves SCEN-01's validated create-and-redirect flow, now presented as a small icon affordance since the design mock shows no visible Save/Share button).

    Keep the existing `banner` rendering block (the saved/shared-scenario notice) exactly as-is, just re-themed to use `bg-card`/`border-border`/`text-muted-foreground` instead of the current `bg-slate-100`/`text-slate-700` light-theme classes -- no structural change.

    Replace the current `<div className="flex flex-row gap-8 items-start">` wrapping `ChartPanel`/`ControlPanel`/`ReasoningPanel` with: (1) `<VerdictBanner classification={lastGoodClassification} isDegenerate={isDegenerate} />` full-width, standing alone (NOT nested inside the grid below -- confirmed via direct image inspection in 08-UI-SPEC.md that the verdict banner spans both grid columns); (2) directly below it, `<div className="grid grid-cols-1 gap-4 min-[900px]:grid-cols-[minmax(0,3fr)_minmax(0,2fr)]">` containing `<ChartPanel .../>` (unchanged props) as the first grid child and `<div className="flex flex-col gap-4"><InstrumentReadouts .../><ReasoningTrail .../></div>` as the second grid child (D-06: below 900px this single grid collapses to one column, and since Chart is listed first / the reasoning `<div>` second in DOM order, the natural single-column stack already reads Chart -> Instrument-Readouts -> Reasoning-Trail top-to-bottom with zero extra reordering CSS needed); (3) below the grid, `<div className="mt-8 grid grid-cols-1 gap-4 sm:grid-cols-2">` containing the two `<ControlPanel>`-composed vessel cards (SBOX-05: Tailwind's default `sm:` breakpoint is 640px, matching the design's required vessel-control-card stacking threshold exactly, no new breakpoint token needed).

    Pass `classification={lastGoodClassification}` to `<ControlPanel>` (its prop contract now requires it, per 08-01/08-03).
  </action>
  <acceptance_criteria>
    - `grep -c "<main" src/components/sandbox/SandboxContainer.tsx` returns 0
    - `grep -c "Interactive sandbox · night-display mode"` returns 1; `grep -c "Drag a vessel — watch the verdict update"` returns 1; `grep -c "Reset scenario"` returns 1 (lowercase "s"); `grep -c "Reset Scenario"` (capital S, the OLD copy) returns 0
    - `grep -c "aria-label=\"Save and share this scenario\""` returns 1
    - `grep -c "VerdictBanner\|InstrumentReadouts\|ReasoningTrail"` returns at least 3 (one usage each)
    - `grep -c "min-\[900px\]:grid-cols-" src/components/sandbox/SandboxContainer.tsx` returns 1; `grep -c "sm:grid-cols-2"` returns 1
    - `grep -A3 "<ControlPanel" src/components/sandbox/SandboxContainer.tsx | grep -c classification` returns 1
    - `npm run typecheck` reports zero errors for `SandboxContainer.tsx`
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>SandboxContainer's root/header/layout fully restyled per the design, VerdictBanner/InstrumentReadouts/ReasoningTrail composed in the new grid, no nested landmark.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the 6-chip preset row through applyVesselUpdate/handleReset's exact pattern</name>
  <files>src/components/sandbox/SandboxContainer.tsx</files>
  <read_first>
    src/components/sandbox/SandboxContainer.tsx
    src/components/sandbox/chip-scenarios.ts
  </read_first>
  <behavior>
    - Clicking the "Overtaking" chip calls `applyVesselUpdate` with exactly `CHIP_SCENARIOS["overtaking"].vesselA`/`.vesselB`, clears `previousEncounterTypeRef.current` first (full hysteresis reset, D-02 -- mechanically identical to `handleReset()`), and sets the active-chip highlight to `"overtaking"`.
    - After a chip click, subsequently dragging a vessel's hull (a manual position change via `onVesselPositionChange`) clears the active-chip highlight back to `null` -- the highlight never lingers on a stale chip once the loaded scenario has been manually altered.
    - Clicking "Classic crossing" (the default-active chip) is a no-op in substance (its fixture matches `SandboxContainer`'s own default seed) but still re-runs the full reset path, matching how Reset Scenario already behaves when clicked while already at the default.
  </behavior>
  <action>
    Import `CHIP_SCENARIOS`, `CHIP_ORDER`, and `ChipId` from `./chip-scenarios.js`. Add `const [activeChipId, setActiveChipId] = useState<ChipId | null>(null);` (the default seed matches `classicCrossingCase`/`"classic-crossing"`'s own fixture values per 08-RESEARCH.md, but leave the initial highlight state as a discretionary call -- either `null` or `"classic-crossing"` is acceptable since the design shows that chip pre-selected; if choosing `"classic-crossing"` as the initial value, confirm the fixture is byte-identical to `crossingResidualBasicCase` before relying on that assumption).

    Add `function handleChipSelect(chipId: ChipId): void` mirroring `handleReset()`'s exact shape: `previousEncounterTypeRef.current = undefined; applyVesselUpdate(CHIP_SCENARIOS[chipId].vesselA, CHIP_SCENARIOS[chipId].vesselB); setActiveChipId(chipId);` -- do NOT introduce a parallel state-update path; this must call the same `applyVesselUpdate` every other update site uses.

    Clear `activeChipId` (set to `null`) inside `onVesselPositionChange`, `onVesselHeadingChange`, `onVesselSpeedChange`, and `onVesselTypeChange` (the 4 existing handlers) -- any manual interaction that doesn't originate from a chip click invalidates the highlight. Do NOT clear it inside `handleReset()` (Reset restores the seed scenario, which may or may not correspond to a chip -- leaving this as `null` after Reset, since Reset's own fixture is not necessarily one of the 6 chip fixtures, is the simpler and correct default).

    Render the chip row below the header, above the verdict banner: a `<div className="flex flex-wrap gap-2">` of 6 pill buttons built from `CHIP_ORDER`, each `<button type="button" onClick={() => handleChipSelect(id)} aria-pressed={activeChipId === id} className={...}>` -- active state (`activeChipId === id`) styled `bg-rule-accent border-rule-accent text-white`, inactive styled `bg-card border-border text-muted-foreground hover:text-foreground` (per 08-UI-SPEC.md's Color section: active chip fill uses `--color-rule-accent`, never `--primary`). This is a plain button row, NOT a Radix `Tabs`/`ToggleGroup` (Pattern 2 -- a one-shot data-load action, not persistent tabbed content).
  </action>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>6-chip row renders in design order, clicking any chip performs a full reset-shaped update through applyVesselUpdate, active-chip highlight clears on any manual edit.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle CopyLinkButton, fix its page-level spacing, and rewrite SandboxContainer.test.tsx</name>
  <files>src/components/sandbox/CopyLinkButton.tsx, app/s/[shareId]/page.tsx, src/components/sandbox/SandboxContainer.test.tsx</files>
  <read_first>
    src/components/sandbox/CopyLinkButton.tsx
    app/s/[shareId]/page.tsx
    src/components/sandbox/SandboxContainer.test.tsx
    src/components/layout/Header.tsx
  </read_first>
  <action>
    Restyle `CopyLinkButton.tsx` (this is the separate "copy the current share URL" affordance rendered on `/s/[shareId]` -- distinct from Task 1's relocated Save button, which creates a NEW scenario): keep the `useState`/`navigator.clipboard.writeText`/2-second-reset `setTimeout` logic byte-for-byte unchanged, only swap the rendered markup from a raw `<button>` to `<Button variant="outline" size="icon-sm" onClick={handleClick} aria-label={copied ? "Link copied" : "Copy link"}>` (`@/components/ui/button`) containing a `Check` icon when `copied` is true, a `Link2` icon otherwise (both from `lucide-react`, verify exact export names exist before using them).

    In `app/s/[shareId]/page.tsx`, wrap the existing `<CopyLinkButton />` sibling in a small spacing container so it doesn't float flush against the page edge now that `SandboxContainer`'s own root padding changed (Task 1): e.g. `<div className="mx-auto max-w-300 px-5 pb-8 min-[900px]:px-6"><CopyLinkButton /></div>` -- matching the same responsive horizontal padding `SandboxContainer`'s own root wrapper now uses, so the two don't visually misalign. Do not change the data-fetching logic in this file (`getCaller().scenario.get`, `notFound()` handling, `rowToVessels`/`buildScenarioBanner`) -- markup-only.

    Rewrite `SandboxContainer.test.tsx`'s assertions that depend on now-changed markup/copy (do not touch the mocking setup for `next/navigation`/`trpc` client, the `MockResizeObserver`, or the `dragHullTo` helper -- all unchanged):
    - `DEFAULT_VERDICT_TEXT`/`OVERTAKING_VERDICT_TEXT` constants currently hold the OLD combined `"Crossing — Vessel A gives way"` string -- `VerdictBanner` now renders title ("Crossing") and description ("Vessel A gives way. Give-way vessel takes early...") as two separate text nodes. Update every assertion referencing these constants to instead assert the title text alone (e.g. `screen.getByText("Crossing")`) AND separately assert the description text is present (e.g. `screen.getByText(/Vessel A gives way\. Give-way vessel takes early/)`), or use a combined `expect(container.textContent).toContain(...)` check spanning both nodes where the original test cared about the FULL verdict semantics rather than a single exact string match.
    - Replace `screen.getByText(DEFAULT_VERDICT_TEXT).closest("aside")` (the reasoning-panel-textContent-diff pattern in the live-update test) with a query against the new `ReasoningTrail`/`InstrumentReadouts` card instead -- e.g. locate the "REASONING TRAIL" heading's closest ancestor card, or simply assert on the specific fact-readout values that are expected to change (TCPA/DCPA text) rather than a whole-subtree textContent diff.
    - `screen.getByRole("button", { name: "Reset Scenario" })` -> `screen.getByRole("button", { name: "Reset scenario" })` (lowercase "s", Task 1's copy change).
    - `screen.getByRole("button", { name: "Save" })` -> `screen.getByRole("button", { name: "Save and share this scenario" })` (the button is now icon-only with an `aria-label`, Task 1's change).
    - `screen.getAllByLabelText("Speed (kn)")[0]` (native input, used to drive speed changes in 3 different tests) -> the Radix `Slider` equivalent: locate via `screen.getAllByRole("slider")[0]`/`[1]` and drive with `await user.click(...)` + `await user.keyboard("{ArrowRight}")` repeated the needed number of times to reach the target speed (e.g. 15 presses to go from 10kn to 25kn), matching Plan 08-03's `ControlPanel.test.tsx` rewrite pattern exactly -- do not reintroduce `user.clear`/`user.type` against a text input that no longer exists.
    - Add one new test asserting a chip click loads the expected scenario: click a chip button by its accessible name (e.g. `screen.getByRole("button", { name: "Overtaking" })`), then assert the verdict title text changes to `"Overtaking"` and the give-way role badge lands on the fixture's documented vessel.
  </action>
  <acceptance_criteria>
    - `grep -c "user.selectOptions\|getAllByLabelText(\"Speed (kn)\")" src/components/sandbox/SandboxContainer.test.tsx` returns 0
    - `grep -c "Reset Scenario\"" src/components/sandbox/SandboxContainer.test.tsx` (old capitalization) returns 0; `grep -c "Reset scenario\"" src/components/sandbox/SandboxContainer.test.tsx` returns at least 1
    - `grep -c "name: \"Save\"" src/components/sandbox/SandboxContainer.test.tsx` returns 0; `grep -c "Save and share this scenario"` returns at least 1
    - `grep -c "getAllByRole(\"slider\")" src/components/sandbox/SandboxContainer.test.tsx` returns at least 1
    - `npx vitest run src/components/sandbox/SandboxContainer.test.tsx` passes, 100% of tests green, including the new chip-click test
    - `npm run typecheck` reports zero errors across the whole `src/components/sandbox/` directory and `app/`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/sandbox/SandboxContainer.test.tsx</automated>
  </verify>
  <done>CopyLinkButton restyled to an icon button with unchanged clipboard logic; page-level spacing fixed; SandboxContainer.test.tsx fully rewritten and green, including new chip-click coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Chip click -> `applyVesselUpdate` | Hardcoded developer fixture data, same validation path as every other update |
| `navigator.clipboard.writeText(window.location.href)` (CopyLinkButton, unchanged) | Reads the browser's own current URL, writes to the clipboard -- no new surface, logic untouched this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-08-06 | Tampering | `handleChipSelect` bypassing validation | accept | Routes through the exact same `applyVesselUpdate` -> `VesselSchema.safeParse()` gate as every drag/form update (D-01/D-02); no parallel/unvalidated state-update path is introduced |
| T-08-07 | Spoofing (UI) | Active-chip highlight (`activeChipId`) | accept | Purely a client-side visual-only state, not used in any authorization/validation decision; a stale/incorrect highlight has no security consequence, only a cosmetic one |

No new network call, external dependency, or trust boundary is introduced -- Save (`createScenario.mutate`) and the chip data path both reuse existing, already-validated code paths.
</threat_model>

<verification>
- `npx vitest run src/components/sandbox/SandboxContainer.test.tsx` passes
- `npm run typecheck` clean repo-wide
- `npx vitest run` (full suite) passes, confirming no cross-file regression
</verification>

<success_criteria>
- SandboxContainer fully restyled: header, chip row, 2-col/full-width responsive grid, no nested &lt;main&gt;
- Chip row functionally wired through the exact applyVesselUpdate/handleReset pattern, with correct active-highlight invalidation
- Save (icon button) and Reset (renamed copy) both still work, zero regression to SCEN-01
- CopyLinkButton restyled, page-level spacing preserved
- SandboxContainer.test.tsx fully rewritten and green for every markup/copy change this phase introduced
</success_criteria>

<output>
Create `.planning/phases/08-sandbox/08-05-SUMMARY.md` when done
</output>
