---
phase: 08-sandbox
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - src/components/ui/select.tsx
  - src/components/ui/slider.tsx
  - src/components/ui/label.tsx
  - src/components/sandbox/vessel-role.ts
  - src/components/sandbox/vessel-role.test.ts
  - src/components/sandbox/types.ts
  - src/components/sandbox/chip-scenarios.ts
  - src/components/sandbox/chip-scenarios.test.ts
  - src/components/sandbox/instrument-readouts.ts
  - src/components/sandbox/instrument-readouts.test.ts
  - src/components/sandbox/status-pill.ts
  - src/components/sandbox/status-pill.test.ts
  - src/components/sandbox/reasoning-trail-tag.ts
  - src/components/sandbox/reasoning-trail-tag.test.ts
autonomous: true
requirements: [SBOX-01, SBOX-02]

must_haves:
  truths:
    - "Every new semantic color the restyle needs (give-way/stand-on/mutual/rule-accent/doubt/geometry/chart-surface) resolves to a real generated Tailwind utility class, not an unstyled/transparent element"
    - "shadcn Select/Slider/Label primitives exist locally and can be imported by ControlPanel in the next wave"
    - "The 6 preset-chip fixtures classify correctly against the real classifyEncounter() with the exact verdicts documented in 08-RESEARCH.md, so no downstream plan wires broken/mismatched scenario data"
    - "Vessel-role, instrument-readout, status-pill, and reasoning-trail-tag derivations are pure, framework-free, and independently unit-tested before any component imports them (D-04: status pill copy; D-05: CPA/TCPA/range/bearing derived at the UI layer, not added to ClassificationResult)"
  artifacts:
    - path: "app/globals.css"
      provides: "7 new @theme tokens (give-way, stand-on, mutual, rule-accent, doubt, geometry, chart-surface) registered in @theme inline + :root + .dark"
    - path: "src/components/ui/select.tsx"
      provides: "shadcn Select primitive"
    - path: "src/components/ui/slider.tsx"
      provides: "shadcn Slider primitive"
    - path: "src/components/sandbox/chip-scenarios.ts"
      provides: "6 ChipScenario fixtures + CHIP_ORDER lookup, sandbox-local (no src/domain or src/server import)"
    - path: "src/components/sandbox/instrument-readouts.ts"
      provides: "deriveInstrumentReadouts(vesselA, vesselB) pure function"
    - path: "src/components/sandbox/status-pill.ts"
      provides: "statusPillCopy(riskOfCollision, cpaNm) pure function"
    - path: "src/components/sandbox/reasoning-trail-tag.ts"
      provides: "classifyingEntryIndex/ruleNumber pure helpers shared by VerdictBanner and ReasoningTrail (wave 2)"
  key_links:
    - from: "src/components/sandbox/vessel-role.ts"
      to: "app/globals.css"
      via: "ROLE_HULL_FILL_CLASS/ROLE_BADGE_CLASSNAME referencing fill-give-way/bg-give-way/... Tailwind utilities"
      pattern: "give-way|stand-on|mutual"
    - from: "src/components/sandbox/chip-scenarios.test.ts"
      to: "src/domain/colregs/classify-encounter.ts"
      via: "classifyEncounter() call against each of the 6 fixtures"
      pattern: "classifyEncounter"
---

<objective>
Establish every shared building block Phase 8's restyle and new chip-row feature depend on, before any presentational component is touched: the 7 new semantic design tokens (Pitfall S2 — these do not exist yet), the 3 new shadcn primitives (Select/Slider/Label), the consolidated `vessel-role.ts` style maps, the widened `types.ts` prop contracts, and 4 new pure/framework-free derivation modules (`chip-scenarios.ts`, `instrument-readouts.ts`, `status-pill.ts`, `reasoning-trail-tag.ts`).

Purpose: every later Wave-2/3 plan (ChartPanel, ControlPanel, the ReasoningPanel split, SandboxContainer) imports directly from the artifacts this plan creates — building them first (Interface-First Task Ordering) means no downstream plan has to invent a contract or guess at a token name.
Output: registered `@theme` tokens; `select.tsx`/`slider.tsx`/`label.tsx` under `src/components/ui/`; extended `vessel-role.ts`/`types.ts`; 4 new pure `.ts` modules with passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-sandbox/08-CONTEXT.md
@.planning/phases/08-sandbox/08-RESEARCH.md
@.planning/phases/08-sandbox/08-UI-SPEC.md
@components.json
</context>

<interfaces>
<!-- Current state of every file this plan extends -- read in full before editing, do not assume shape from this excerpt. -->

From `src/components/sandbox/vessel-role.ts` (full file, 16 lines):
```typescript
import type { ClassificationResult, VesselLabel } from "../../domain/colregs/types.js";
export type VesselRole = "give-way" | "stand-on" | "mutual";
export function getVesselRole(vessel: VesselLabel, classification: ClassificationResult): VesselRole { ... }
```

From `src/components/sandbox/types.ts` (current, full file):
```typescript
export interface VesselUpdateHandlers { onVesselPositionChange, onVesselHeadingChange, onVesselSpeedChange, onVesselTypeChange }
export interface ChartPanelProps { vesselA; vesselB; classification; isDegenerate; onVesselPositionChange; onVesselHeadingChange }
export interface ControlPanelProps { vesselA; vesselB; onVesselSpeedChange; onVesselTypeChange }
export interface ReasoningPanelProps { classification; isDegenerate }
export interface SandboxContainerProps { initialScenario?; banner? }
```

From `src/domain/vessel/vessel.ts`: `VesselSchema`/`Vessel`/`Position`/`VesselType` (5 `VesselType` enum members: `power-driven`, `sailing`, `fishing`, `not-under-command`, `restricted-in-ability-to-maneuver`).

From `src/domain/colregs/types.ts`: `ClassificationResult { encounterType; riskOfCollision; giveWay; standOn; doubt; doubtBoundary?; trail: ReasoningTrailEntry[] }`, `VesselLabel = "vesselA" | "vesselB"`.

From `src/domain/geometry/relative-bearing.ts`: `relativeBearing(own: Vessel, contact: Vessel): Result<number>` (raw degrees, `(-180, 180]`).
From `src/domain/geometry/cpa.ts`: `cpa(vesselA: Vessel, vesselB: Vessel): Result<{ tcpaMinutes: number; dcpaNm: number }>`.

From `app/globals.css` (current `@theme inline` + `:root` + `.dark` triad, lines 7-127): every existing token follows the `--color-X: var(--X)` (in `@theme inline`) + `--X: <value>` (identical in both `:root` and `.dark`) pairing -- e.g. `--color-destructive: var(--destructive)` / `:root { --destructive: oklch(...) }` / `.dark { --destructive: oklch(...) }` (same value in both blocks, defense-in-depth per the file's own header comment).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn Select/Slider/Label primitives and register the 7 missing semantic design tokens</name>
  <files>src/components/ui/select.tsx, src/components/ui/slider.tsx, src/components/ui/label.tsx, app/globals.css</files>
  <read_first>
    app/globals.css
    components.json
  </read_first>
  <action>
    Run `npx shadcn@latest add select slider label` from the repo root -- shadcn official registry only, no `--registry` flag (Registry Safety: not applicable, per 08-UI-SPEC.md). This vendors `select.tsx`/`slider.tsx`/`label.tsx` into `src/components/ui/` using the project's existing `components.json` config (`style: "radix-nova"`, `baseColor: "neutral"`, `@/*` aliases) -- do not hand-write these files.

    Then edit `app/globals.css`: add 7 new tokens, following the file's own existing `--color-X: var(--X)` pairing pattern exactly (Pitfall S2 -- confirmed absent from the file today, contra 08-CONTEXT.md's incorrect "already registered in Phase 6" claim).

    In the `@theme inline` block, add: `--color-give-way: var(--give-way);`, `--color-stand-on: var(--stand-on);`, `--color-mutual: var(--mutual);`, `--color-rule-accent: var(--rule-accent);`, `--color-doubt: var(--doubt);`, `--color-geometry: var(--geometry);`, `--color-chart-surface: var(--chart-surface);`.

    In BOTH the `:root` block AND the `.dark` block (identical values in both, matching the file's existing defense-in-depth duplication -- this app is dark-only, both blocks must carry the same value), add: `--give-way: #EF4444;`, `--stand-on: #22C55E;`, `--mutual: #94A3B8;`, `--rule-accent: #0D9488;`, `--doubt: #F59E0B;`, `--geometry: #475569;`, `--chart-surface: #0B0B0E;`.

    `--rule-accent` (#0D9488, teal-600) is deliberately DISTINCT from the existing `--primary`/`--accent` (#2dd4bf) -- confirmed via direct pixel sampling in 08-UI-SPEC.md's Color section: `--rule-accent` is reserved for the active chip fill, the classifying-rule reasoning-trail tag/number-circle, and the existing rotate-handle circle stroke (wave-2 ChartPanel plan will reference it); `--primary` stays reserved for the Speed slider only. `--chart-surface` (#0B0B0E) replaces the raw `bg-[#0B0B0E]` arbitrary-value literal currently duplicated in both `ChartPanel.tsx` (wave 2) and the already-shipped `HeroPreviewCard.tsx` (Phase 7, out of this phase's scope -- do not edit that file) -- this plan only registers the token; wave 2's ChartPanel plan consumes it.
  </action>
  <acceptance_criteria>
    - `src/components/ui/select.tsx`, `src/components/ui/slider.tsx`, `src/components/ui/label.tsx` exist and export `Select`/`SelectTrigger`/`SelectContent`/`SelectItem`/`SelectValue`, `Slider`, and `Label` respectively (source assertion: `grep -n "^export" src/components/ui/select.tsx src/components/ui/slider.tsx src/components/ui/label.tsx` each returns at least one match)
    - `app/globals.css`'s `@theme inline` block contains all 7 new `--color-*` lines (source assertion: `grep -c "color-give-way\|color-stand-on\|color-mutual\|color-rule-accent\|color-doubt\|color-geometry\|color-chart-surface" app/globals.css` returns exactly 3, one per block: `@theme inline`, `:root`, `.dark`, for each of the 7 tokens -- i.e. `grep -c -- "--give-way:" app/globals.css` returns 2 (once in `:root`, once in `.dark`), and `grep -c -- "--color-give-way:" app/globals.css` returns 1)
    - `:root` and `.dark` blocks contain byte-identical values for all 7 new raw tokens (behavior assertion: extract both blocks' 7 new lines and diff them -- zero differences)
    - `npm run typecheck` passes with no new errors
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>select/slider/label primitives generated; 7 new tokens registered in all 3 required blocks with matching dark-only values.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate role→style lookups in vessel-role.ts and widen types.ts prop contracts</name>
  <files>src/components/sandbox/vessel-role.ts, src/components/sandbox/vessel-role.test.ts, src/components/sandbox/types.ts</files>
  <read_first>
    src/components/sandbox/vessel-role.ts
    src/components/sandbox/vessel-role.test.ts
    src/components/sandbox/types.ts
    src/components/sandbox/ChartPanel.tsx
    src/components/sandbox/ReasoningPanel.tsx
  </read_first>
  <action>
    Extend `vessel-role.ts` (keep `getVesselRole` and the `VesselRole` type exactly as-is) by adding 3 new exported `Record<VesselRole, string>` lookups that consolidate the role→style maps currently duplicated across `ChartPanel.tsx` (`HULL_FILL_CLASS`, `ROLE_BADGE_TEXT`) and `ReasoningPanel.tsx` (`ROLE_BADGE`, `VESSEL_LABEL_TEXT`): `ROLE_HULL_FILL_CLASS` (`"give-way": "fill-give-way"`, `"stand-on": "fill-stand-on"`, `mutual: "fill-mutual"` -- referencing the new tokens from Task 1, replacing the old `fill-red-500`/`fill-green-500`/`fill-slate-400` Tailwind-default-palette classes), `ROLE_BADGE_TEXT` (`"give-way": "GW"`, `"stand-on": "SO"`, `mutual: "MUTUAL"` -- identical values to both files' current maps, now single-sourced), and `ROLE_BADGE_CLASSNAME` (a full badge className per role using the 10%/35% opacity convention already established by Phase 7's `bg-primary/10`/`border-primary/30` rule-banner precedent: `"give-way": "bg-give-way/10 border-give-way/35 text-give-way"`, `"stand-on": "bg-stand-on/10 border-stand-on/35 text-stand-on"`, `mutual: "bg-mutual/10 border-mutual/35 text-mutual"`). Do NOT modify `ChartPanel.tsx`/`ReasoningPanel.tsx` in this task -- wave 2 plans consume these new exports and delete the old duplicated maps from those files.

    Widen `types.ts`: (1) add `classification: ClassificationResult;` to `ControlPanelProps` -- the wave-2 ControlPanel plan's vessel-control card needs to render a role badge in its header row per the design, which `getVesselRole()` requires; (2) rename `ReasoningPanelProps` to `VerdictBannerProps` (same two fields, `classification`/`isDegenerate`, unchanged shape -- only the name changes, since `ReasoningPanel.tsx` itself is retired in wave 2's split); (3) add a new `InstrumentReadoutsProps` interface with `vesselA: Vessel; vesselB: Vessel; classification: ClassificationResult; isDegenerate: boolean;` (D-05's instrument readouts need live vessel state, which `ClassificationResult` alone does not carry); (4) add a new `ReasoningTrailProps` interface with just `classification: ClassificationResult;` (the trail renders `classification.trail`/`classification.doubt`/`classification.doubtBoundary` only, no vessel state needed). None of these changes touch `src/domain/colregs/types.ts` -- `ClassificationResult` itself stays byte-for-byte unchanged (the milestone's locked "zero domain change" boundary).

    Extend `vessel-role.test.ts` with 3 new `describe`/`it` blocks asserting `ROLE_HULL_FILL_CLASS`, `ROLE_BADGE_TEXT`, and `ROLE_BADGE_CLASSNAME` each have exactly the 3 `VesselRole` keys (`give-way`/`stand-on`/`mutual`) with the exact string values specified above.
  </action>
  <acceptance_criteria>
    - `vessel-role.ts` exports `ROLE_HULL_FILL_CLASS`, `ROLE_BADGE_TEXT`, `ROLE_BADGE_CLASSNAME` in addition to the existing `getVesselRole`/`VesselRole` (source assertion: `grep -c "^export const ROLE_HULL_FILL_CLASS\|^export const ROLE_BADGE_TEXT\|^export const ROLE_BADGE_CLASSNAME" src/components/sandbox/vessel-role.ts` returns 3)
    - `types.ts` no longer exports `ReasoningPanelProps`; exports `VerdictBannerProps`, `InstrumentReadoutsProps`, `ReasoningTrailProps` instead (source assertion: `grep -c "ReasoningPanelProps" src/components/sandbox/types.ts` returns 0; `grep -c "VerdictBannerProps\|InstrumentReadoutsProps\|ReasoningTrailProps" src/components/sandbox/types.ts` returns 3)
    - `ControlPanelProps` includes a `classification: ClassificationResult;` field (source assertion: `grep -A5 "interface ControlPanelProps" src/components/sandbox/types.ts | grep -c classification` returns 1)
    - `npx vitest run src/components/sandbox/vessel-role.test.ts` passes (all existing + 3 new assertions)
    - `npm run typecheck` fails at this point ONLY for `ChartPanel.tsx`/`ReasoningPanel.tsx`/`ControlPanel.tsx` (still referencing the now-renamed `ReasoningPanelProps` or missing the new `classification` prop) -- this is expected and resolved by wave-2 plans, not a regression to fix here
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/sandbox/vessel-role.test.ts</automated>
  </verify>
  <done>vessel-role.ts exports 3 new consolidated maps referencing the new tokens; types.ts widened/renamed per the 4 changes above; vessel-role.test.ts covers the new exports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build the 4 new pure derivation modules -- chip-scenarios, instrument-readouts, status-pill, reasoning-trail-tag</name>
  <files>src/components/sandbox/chip-scenarios.ts, src/components/sandbox/chip-scenarios.test.ts, src/components/sandbox/instrument-readouts.ts, src/components/sandbox/instrument-readouts.test.ts, src/components/sandbox/status-pill.ts, src/components/sandbox/status-pill.test.ts, src/components/sandbox/reasoning-trail-tag.ts, src/components/sandbox/reasoning-trail-tag.test.ts</files>
  <read_first>
    src/domain/colregs/classify-encounter.fixtures.ts
    src/components/hero/hero-preview-fixture.ts
    src/components/hero/hero-preview-fixture.test.ts
    src/components/hero/hero-preview-geometry.ts
    src/domain/geometry/relative-bearing.ts
    src/domain/geometry/cpa.ts
    src/domain/colregs/types.ts
  </read_first>
  <behavior>
    - `chip-scenarios.ts`/`chip-scenarios.test.ts`: `CHIP_SCENARIOS` (a `Record<ChipId, { vesselA: Vessel; vesselB: Vessel }>`) and `CHIP_ORDER` (a `readonly { id: ChipId; label: string }[]` in the exact design order) exist for all 6 `ChipId` values (`classic-crossing`, `head-on-meeting`, `overtaking`, `sailing-has-priority`, `not-under-command`, `in-doubt`). Test: for each of the 6, call the real `classifyEncounter(vesselA, vesselB)` and assert it returns `ok: true` with the exact `encounterType`/`giveWay`/`standOn`/`doubt` documented in 08-RESEARCH.md's "Code Examples" section (Classic crossing: crossing/vesselA/vesselB/false; Head-on meeting: head-on/null/null/true; Overtaking: overtaking/vesselA/vesselB/false; Sailing has priority: crossing/vesselA/vesselB/false; Not under command: crossing/vesselA/vesselB/false, `riskOfCollision: true`; In doubt: head-on/null/null/true, `riskOfCollision: true`) -- this is the "exercise the two new fixtures through the real classifyEncounter()" safety net 08-RESEARCH.md explicitly calls for, extended to cover all 6.
    - `instrument-readouts.ts`/`instrument-readouts.test.ts`: `deriveInstrumentReadouts(vesselA, vesselB): { rangeNm: number; bearingAtoBDegrees: number | null; cpaNm: number | null; tcpaMinutes: number | null }`. Test with `classify-encounter.fixtures.ts`'s `crossingResidualBasicCase` vessels: `rangeNm` closeTo 5 (0dp tolerance not needed, use `toBeCloseTo`), `bearingAtoBDegrees` non-null, `cpaNm`/`tcpaMinutes` non-null and matching `cpa()`'s own direct output for the same vessels. Also test the degenerate/coincident-position case (both vessels at the same `Position`): `bearingAtoBDegrees`/`cpaNm`/`tcpaMinutes` are all `null` (relativeBearing/cpa both fail on coincident input), while `rangeNm` is still `0` (Math.hypot of a zero vector, always computable).
    - `status-pill.ts`/`status-pill.test.ts`: `statusPillCopy(riskOfCollision: boolean, cpaNm: number | null): { text: string; tone: "clear" | "risk" }`. Test: `riskOfCollision: false, cpaNm: 1.81` returns `{ text: "Passing clear — CPA 1.81 NM on present courses.", tone: "clear" }` (verbatim from 08-UI-SPEC.md's Copywriting Contract); `riskOfCollision: true, cpaNm: 0.87` returns `{ text: "Risk of collision — CPA 0.87 NM on present courses.", tone: "risk" }`; `cpaNm: null` renders `"—"` in place of the numeric segment for both tones.
    - `reasoning-trail-tag.ts`/`reasoning-trail-tag.test.ts`: `classifyingEntryIndex(trailLength: number): number` returns `trailLength - 2` (the always-second-to-last entry is the actual dispatch-stage rule -- Rule 13/14/15 -- since the trail's first entry is always Rule 7 and its last entry is always the Rule 18/13(a) confirmation, verified against `classify-encounter.ts`'s own push sequence). `ruleNumber(ruleId: string): string` extracts just the leading rule number from a `ruleId` string via a `Rule (\d+)` match (e.g. `"Rule 13(a)-(b)"` → `"13"`, `"Rule 15"` → `"15"`); falls back to the raw `ruleId` unchanged if no match. Test both functions directly, plus an integration-style test that runs `classifyEncounter()` against `classify-encounter.fixtures.ts`'s `crossingResidualBasicCase` (5-entry trail) and `overtakingBothDirectionsCase` (3-entry trail) and asserts `classifyingEntryIndex(trail.length)` points at the entry whose `ruleId` is `"Rule 15"` / `"Rule 13(a)-(b)"` respectively (i.e. the actual encounter-defining stage, not Rule 7 or the Rule 18 confirmation).
  </behavior>
  <action>
    Write all 4 modules as pure, framework-free TypeScript (zero React/JSX imports, matching `hero-preview-geometry.ts`'s established shape) with a WHY-only derivation comment at each module's top, not at every call site (CLAUDE.md convention).

    `chip-scenarios.ts`: define `ChipId` as the union of the 6 kebab-case string literals above; define `ChipScenario { id: ChipId; label: string; vesselA: Vessel; vesselB: Vessel }`. Copy the 6 fixture `Vessel` literals VERBATIM from 08-RESEARCH.md's "Code Examples" section ("The two genuinely new chip fixtures" + "Remaining chips") as new local literals -- do NOT import from `classify-encounter.fixtures.ts` (D-03: sandbox-local standalone copies only, zero `src/domain`/`src/server` coupling beyond the read-only `classifyEncounter()` call in the test file). Preserve each fixture's inline worked-derivation comment (relativeBearing/cpa values) from the research doc, mirroring `classify-encounter.fixtures.ts`'s own comment convention. Chip labels/order (verbatim, per 08-UI-SPEC.md Copywriting Contract): "Classic crossing", "Head-on meeting", "Overtaking", "Sailing has priority", "Not under command", "In doubt".

    `instrument-readouts.ts`: implement exactly as specified in 08-RESEARCH.md's Code Examples ("Instrument-readout derivation") -- `rangeNm` via `Math.hypot` of the raw position delta (never scanned from `classification.trail[].facts`, per the Anti-Pattern in 08-RESEARCH.md: the sticky-overtaking entry has empty `facts`, no-closure `cpa()` results never attach `tcpaMinutes`/`dcpaNm` to any trail entry, and Range has no source in `facts` under any path), `bearingAtoBDegrees`/`cpaNm`/`tcpaMinutes` via direct `relativeBearing()`/`cpa()` calls, `null` on either function's `!ok` result. Import path is `../../domain/geometry/relative-bearing.js` / `../../domain/geometry/cpa.js` / `../../domain/vessel/vessel.js` (two levels up, matching every other sandbox file's existing import depth).

    `status-pill.ts` (implements CONTEXT.md D-04's status pill): implement exactly as specified in 08-RESEARCH.md's Code Examples ("Status pill copy derivation"), using the exact locked "clear" copy and the UI-SPEC-approved "risk" copy (see behavior block above for the verbatim strings, note the trailing period on both).

    `reasoning-trail-tag.ts`: implement `classifyingEntryIndex`/`ruleNumber` as specified in the behavior block. These two functions are deliberately generic (no `doubt`/`ClassificationResult` parameter) -- wave 2's VerdictBanner/ReasoningTrail components compose the doubt-substitution ("Rule 7" override) and casing ("Rule 15" vs "RULE 15") themselves, since those are per-consumer presentation choices, not shared derivation logic.
  </action>
  <acceptance_criteria>
    - All 4 new `.ts` modules contain zero `import` from `"react"` or any `.tsx` file (source assertion: `grep -l "from \"react\"\|from 'react'" src/components/sandbox/chip-scenarios.ts src/components/sandbox/instrument-readouts.ts src/components/sandbox/status-pill.ts src/components/sandbox/reasoning-trail-tag.ts` returns no matches)
    - `chip-scenarios.ts` contains zero imports from `classify-encounter.fixtures.ts` or any `src/server/` path (source assertion: `grep -c "classify-encounter.fixtures\|src/server" src/components/sandbox/chip-scenarios.ts` returns 0)
    - `npx vitest run src/components/sandbox/chip-scenarios.test.ts src/components/sandbox/instrument-readouts.test.ts src/components/sandbox/status-pill.test.ts src/components/sandbox/reasoning-trail-tag.test.ts` passes, all behaviors above included
    - `npm run typecheck` still fails ONLY for `ChartPanel.tsx`/`ReasoningPanel.tsx`/`ControlPanel.tsx` (unchanged from Task 2 -- resolved in wave 2)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/sandbox/chip-scenarios.test.ts src/components/sandbox/instrument-readouts.test.ts src/components/sandbox/status-pill.test.ts src/components/sandbox/reasoning-trail-tag.test.ts</automated>
  </verify>
  <done>4 new pure modules exist, each independently unit-tested; chip-scenarios' 6 fixtures verified against the real classifyEncounter() with the exact documented verdicts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Developer-authored fixture literals -> `classifyEncounter()` | `chip-scenarios.ts`'s 6 `Vessel` literals are hardcoded constants, never user input, but still flow through the same domain entry point real drag/form updates use downstream (wave 3's `applyVesselUpdate`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-08-01 | Tampering | `chip-scenarios.ts` fixture literals | accept | Not user-reachable input; a malformed literal (e.g. `heading: 400`) is still caught by `VesselSchema.safeParse()` in wave 3's `applyVesselUpdate()`, the same validation gate every other update path already uses -- no new bypass is introduced |
| T-08-02 | Tampering | `app/globals.css` new `@theme` tokens | accept | Static, build-time CSS custom properties with no user-controlled interpolation; standard Tailwind v4 token registration, same pattern as every existing token in the file |

This plan introduces zero new user input surfaces, zero new network/tRPC calls, and zero new external dependencies (shadcn CLI only vendors additional source files from the already-audited `radix-ui@1.6.2` package). No ASVS category beyond the already-accepted V5 (input validation, unchanged) applies.
</threat_model>

<verification>
- `npm run typecheck` (expected to still report ChartPanel/ReasoningPanel/ControlPanel errors, resolved in wave 2 -- confirm no OTHER file reports new errors)
- `npx vitest run src/components/sandbox/vessel-role.test.ts src/components/sandbox/chip-scenarios.test.ts src/components/sandbox/instrument-readouts.test.ts src/components/sandbox/status-pill.test.ts src/components/sandbox/reasoning-trail-tag.test.ts` all pass
- `grep -n "give-way\|stand-on\|mutual\|rule-accent\|chart-surface" app/globals.css` shows all 7 tokens present in all 3 required blocks
</verification>

<success_criteria>
- shadcn Select/Slider/Label primitives installed and importable
- 7 new semantic tokens registered (Pitfall S2 closed)
- vessel-role.ts is the single source of truth for role→style mapping (3 new consolidated exports)
- types.ts widened/renamed to support the wave-2 component split and the wave-3 ControlPanel role badge
- 4 new pure modules exist, fully unit-tested, with the 6 chip fixtures verified against the real domain classifier
</success_criteria>

<output>
Create `.planning/phases/08-sandbox/08-01-SUMMARY.md` when done
</output>
