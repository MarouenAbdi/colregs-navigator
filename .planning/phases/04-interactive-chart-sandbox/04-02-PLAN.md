---
phase: 04-interactive-chart-sandbox
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/domain/colregs/resolve-doubt-geometry.ts
  - src/domain/colregs/resolve-doubt-geometry.fixtures.ts
  - src/domain/colregs/resolve-doubt-geometry.test.ts
autonomous: true
requirements: [RSON-03]

must_haves:
  truths:
    - "Given a doubt-flagged classification, the UI can always determine exactly which vessel and which relative-bearing value triggered the doubt, without guessing or reading inconsistent per-stage trail facts."
  artifacts:
    - path: "src/domain/colregs/resolve-doubt-geometry.ts"
      provides: "resolveDoubtGeometry(vesselA, vesselB, doubtBoundary) -> Result<{ vessel, relativeBearingDegrees }>"
      exports: ["resolveDoubtGeometry"]
  key_links:
    - from: "src/domain/colregs/resolve-doubt-geometry.ts"
      to: "src/domain/geometry/relative-bearing.ts"
      via: "relativeBearing() calls, not a re-derived atan2"
      pattern: "relativeBearing\\("
    - from: "src/domain/colregs/resolve-doubt-geometry.ts"
      to: "src/domain/colregs/classify-encounter.ts"
      via: "OVERTAKING_BOUNDARY_DEGREES import, not a re-declared 112.5 constant"
      pattern: "OVERTAKING_BOUNDARY_DEGREES"
---

<objective>
Resolve 04-RESEARCH.md's Open Question 2 / Pitfall 3: build the small, pure, independently-tested `resolveDoubtGeometry()` helper that later Wave-2 ChartPanel work (04-03) uses to know exactly which vessel's relative-bearing line/overtaking-boundary wedge to render as the dashed-amber doubt overlay (D-04), without inspecting the reasoning trail's inconsistent per-stage `facts` shape (the sticky-hysteresis branch's trail entry has `facts: {}` and cannot answer this question at all).

Purpose: keeps the doubt-geometry-resolution logic in the framework-free `src/domain/` layer (Clean Architecture boundary: components import FROM domain, never the reverse), gets it the same fixture-driven unit-test rigor as the rest of the rules engine, and gives ChartPanel a single deterministic call instead of ad-hoc trail parsing.
Output: `src/domain/colregs/resolve-doubt-geometry.ts` + fixtures + tests, all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-interactive-chart-sandbox/04-RESEARCH.md
@.planning/phases/04-interactive-chart-sandbox/04-PATTERNS.md

<interfaces>
From src/domain/colregs/classify-encounter.ts (existing, read-only):
```typescript
export const OVERTAKING_BOUNDARY_DEGREES = 112.5;
export const DOUBT_BAND_DEGREES = 5;
```

From src/domain/geometry/relative-bearing.ts (existing, read-only):
```typescript
export function relativeBearing(own: Vessel, contact: Vessel): Result<number>;
```

From src/domain/colregs/types.ts (existing, read-only):
```typescript
export type VesselLabel = "vesselA" | "vesselB";
export type DoubtBoundary = "near-overtaking-crossing-boundary" | "near-head-on-boundary";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: resolveDoubtGeometry() -- pure doubt-triggering-vessel/bearing resolution</name>
  <files>src/domain/colregs/resolve-doubt-geometry.ts, src/domain/colregs/resolve-doubt-geometry.fixtures.ts, src/domain/colregs/resolve-doubt-geometry.test.ts</files>
  <read_first>
    - src/domain/colregs/classify-encounter.ts (Stage 2/3/5 logic that produces each `doubtBoundary` value -- the sticky-overtaking branch at "Stage 2" pushes `facts: {}`, which is exactly why this function must recompute geometry independently rather than read the trail)
    - src/domain/geometry/relative-bearing.ts (signature + `Result<number>` propagation convention to mirror)
    - src/domain/colregs/classify-encounter.fixtures.ts lines 220-320 (`crossingResidualBasicCase`, `doubtBandNearOvertakingBoundaryCase`, `doubtBandJustOverOvertakingBoundaryCase`, `headOnBoundaryInclusiveCase`, `overtakingHysteresisNearBoundaryCase` -- reuse the `vesselA`/`vesselB` shapes from these as inspiration for this file's own fixtures, do not import them directly since this file's fixtures need a `doubtBoundary` field the classification fixtures don't carry)
    - src/domain/geometry/relative-bearing.fixtures.ts (`coincidentPropagationCase` shape, for the degenerate-propagation test case)
    - src/domain/shared/result.ts (`Result<T>`, `ok`, `err` -- this function returns `Result<{ vessel: VesselLabel; relativeBearingDegrees: number }>`)
  </read_first>
  <behavior>
    - Signature: `resolveDoubtGeometry(vesselA: Vessel, vesselB: Vessel, doubtBoundary: DoubtBoundary): Result<{ vessel: VesselLabel; relativeBearingDegrees: number }>`.
    - Test 1 (near-overtaking-crossing-boundary, A-side): vesselA/vesselB positioned so `relativeBearing(vesselA, vesselB)` is within `DOUBT_BAND_DEGREES` of `OVERTAKING_BOUNDARY_DEGREES` (e.g. ~110 deg, matching the shape of `doubtBandNearOvertakingBoundaryCase`) while `relativeBearing(vesselB, vesselA)` is far from the boundary (e.g. ~0 deg) -> returns `ok({ vessel: "vesselA", relativeBearingDegrees: ~110 })`.
    - Test 2 (near-overtaking-crossing-boundary, B-side): construct a fixture where `relativeBearing(vesselB, vesselA)`'s magnitude is the one closer to 112.5 deg (not A's) -> returns `ok({ vessel: "vesselB", relativeBearingDegrees: <that value> })`. This is the case Pitfall 3 warns a naive `facts.relativeBearingAtoB`-only reader would get wrong.
    - Test 3 (sticky-hysteresis case): use vessel shapes matching `overtakingHysteresisNearBoundaryCase` (previous='overtaking', bearing drifted near the boundary) -- resolveDoubtGeometry does NOT take a `previous` argument and does NOT read any trail; it must derive the correct vessel/bearing purely from `vesselA`/`vesselB` geometry, proving the trail's empty `facts: {}` for the sticky branch is a non-issue for this function.
    - Test 4 (near-head-on-boundary): use vessel shapes matching `headOnBoundaryInclusiveCase` (both `relativeBearing` values within `DOUBT_BAND_DEGREES` of 0) -> returns `ok({ vessel: "vesselA", relativeBearingDegrees: <rbAtoB> })`. Document in a comment why vesselA is the fixed convention for this branch: the head-on doubt band is symmetric (both vessels see each other within +/-5 deg of dead ahead), so either vessel's bearing is geometrically equivalent for rendering purposes -- vesselA is picked as an arbitrary, pinned, always-A convention (mirrors classify-encounter.ts's own "WR" comment style for arbitrary-but-pinned tie-breaks).
    - Test 5 (degenerate propagation): vesselA and vesselB at the identical position -> `relativeBearing()` returns `err("coincident-position", ...)` -> resolveDoubtGeometry propagates that exact `Result` failure unchanged (no re-wrapping), mirroring `classify-encounter.ts`'s Stage-0 "propagate unchanged" idiom.
  </behavior>
  <action>
    Implement `resolveDoubtGeometry` in `src/domain/colregs/resolve-doubt-geometry.ts`: import `relativeBearing` from `"../geometry/relative-bearing.js"`, `OVERTAKING_BOUNDARY_DEGREES`/`DOUBT_BAND_DEGREES` from `"./classify-encounter.js"`, `type { Result }` from `"../shared/result.js"`, `ok` from the same, `type { DoubtBoundary, VesselLabel }` from `"./types.js"`, `type { Vessel }` from `"../vessel/vessel.js"`.
    Compute `rbAtoBResult = relativeBearing(vesselA, vesselB)`; if `!rbAtoBResult.ok` return it unchanged. Compute `rbBtoAResult = relativeBearing(vesselB, vesselA)`; if `!rbBtoAResult.ok` return it unchanged.
    If `doubtBoundary === "near-head-on-boundary"`: return `ok({ vessel: "vesselA", relativeBearingDegrees: rbAtoBResult.value })`.
    Else (`doubtBoundary === "near-overtaking-crossing-boundary"`): compute `distA = Math.abs(Math.abs(rbAtoBResult.value) - OVERTAKING_BOUNDARY_DEGREES)` and `distB = Math.abs(Math.abs(rbBtoAResult.value) - OVERTAKING_BOUNDARY_DEGREES)`. If `distA <= distB` return `ok({ vessel: "vesselA", relativeBearingDegrees: rbAtoBResult.value })`, else return `ok({ vessel: "vesselB", relativeBearingDegrees: rbBtoAResult.value })`. The `<=` tie-break favors vesselA, consistent with `classify-encounter.ts`'s own documented WR-02 tie-break style.
    Follow the RED -> GREEN -> REFACTOR cycle: write `resolve-doubt-geometry.fixtures.ts` (five named `RegExpDoubtGeometryCase`-style const fixtures, one per Test above -- name them `overtakingBoundaryTriggeredByACase`, `overtakingBoundaryTriggeredByBCase`, `stickyHysteresisNearBoundaryCase`, `headOnBoundaryCase`, `coincidentPositionCase`) and `resolve-doubt-geometry.test.ts` (import fixtures, assert against a not-yet-implemented `resolveDoubtGeometry` so the suite fails), commit as `test(04-02): add failing tests for resolveDoubtGeometry`, then implement `resolve-doubt-geometry.ts` until green, commit as `feat(04-02): implement resolveDoubtGeometry`.
  </action>
  <verify>
    <automated>npx vitest run src/domain/colregs/resolve-doubt-geometry.test.ts</automated>
  </verify>
  <done>All five behavior cases pass; `resolveDoubtGeometry` exported as a named export; no new `atan2` call anywhere in this file (only `relativeBearing()` calls); `npx vitest run` (full suite) still exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SandboxContainer-validated `Vessel` objects -> resolveDoubtGeometry() | This function only ever runs inside the UI flow (Wave 2/3) after `SandboxContainer`'s `applyVesselUpdate` choke point has already validated the vessels via `VesselSchema` (see 04-06's threat model for the actual mitigation) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering | `resolveDoubtGeometry()` non-finite numeric input | accept | defensively safe regardless: the composed `relativeBearing()`/`bearing()` calls already return a tagged `Result` failure for non-finite `Position`/`heading` values (existing Phase 1 guard, T-01-02/T-01-04) rather than propagating `NaN`; this function adds no new unguarded arithmetic beyond that composition |
</threat_model>

<verification>
- `npx vitest run src/domain/colregs/resolve-doubt-geometry.test.ts` exits 0
- `npx vitest run` (full suite) exits 0
</verification>

<success_criteria>
- `resolveDoubtGeometry(vesselA, vesselB, doubtBoundary)` deterministically identifies the triggering vessel and its relative bearing for both `DoubtBoundary` values, without reading the reasoning trail
- Zero duplicated `atan2`/boundary-constant logic (reuses `relativeBearing()` and `OVERTAKING_BOUNDARY_DEGREES` directly)
</success_criteria>

<output>
Create `.planning/phases/04-interactive-chart-sandbox/04-02-SUMMARY.md` when done
</output>
