---
phase: 04-interactive-chart-sandbox
plan: 05
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/sandbox/ReasoningPanel.tsx
  - src/components/sandbox/ReasoningPanel.test.tsx
autonomous: true
requirements: [RSON-01]

must_haves:
  truths:
    - "A persistent side panel always shows the full Rule 7 -> 13 -> 14 -> 15 -> 18 reasoning trail, including rules that were checked and ruled out, not just the final verdict."
    - "The trail text and rule citations are rendered verbatim from classifyEncounter()'s own output -- the panel invents no new rule-explanation copy."
    - "The panel renders each trail entry's underlying geometric facts (relative bearings, TCPA, DCPA, risk-of-collision, vessel types) as visible font-mono key/value readouts beneath that entry's rule text -- RSON-01's 'geometric logic (relative bearing, closing angle) that produced the verdict' is not satisfied by rule citations and prose alone."
    - "A doubt caveat line naming the specific boundary appears when classification.doubt is true."
    - "When the classification is degenerate (coincident-position mid-drag), the panel shows the 'Unable to classify' inline note while still showing the last-good trail underneath."
  artifacts:
    - path: "src/components/sandbox/ReasoningPanel.tsx"
      provides: "Full always-visible reasoning trail panel with verdict banner, per-entry geometric fact readouts, and doubt caveat"
      exports: ["ReasoningPanel"]
  key_links:
    - from: "src/components/sandbox/ReasoningPanel.tsx"
      to: "src/domain/colregs/types.ts"
      via: "classification.trail.map(...) renders ReasoningTrailEntry[] (including its facts object) with zero transformation"
      pattern: "classification\\.trail\\.map"
---

<objective>
Build `ReasoningPanel`: the persistent, always-visible, full reasoning-trail side panel (D-03) with the verdict banner, per-rule trail entries (rule citation, prose, and the underlying geometric facts that produced them), and the doubt caveat line (D-04) and degenerate "Unable to classify" inline note.

Purpose: RSON-01's entire rendering surface, and the most literal expression of the project's Core Value ("clearly explain — not just assert"). RSON-01 explicitly requires showing "the geometric logic (relative bearing, closing angle) that produced the verdict," not just the rule citation -- this plan renders both.
Output: a presentational component consuming exactly the `ReasoningPanelProps` contract from 04-01, ready to be mounted by `SandboxContainer` in 04-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-interactive-chart-sandbox/04-UI-SPEC.md
@.planning/phases/04-interactive-chart-sandbox/04-PATTERNS.md
@.planning/phases/04-interactive-chart-sandbox/04-01-PLAN.md

<interfaces>
From src/components/sandbox/types.ts (created by 04-01, read-only -- import, do not redefine):
```typescript
export interface ReasoningPanelProps {
  classification: ClassificationResult; // always the LAST-GOOD result
  isDegenerate: boolean;
}
```

From src/components/sandbox/vessel-role.ts (created by 04-01, read-only):
```typescript
export type VesselRole = "give-way" | "stand-on" | "mutual";
export function getVesselRole(vessel: VesselLabel, classification: ClassificationResult): VesselRole;
```

From src/domain/colregs/types.ts (existing, read-only):
```typescript
export interface ReasoningTrailEntry { ruleId: string; text: string; facts: Record<string, number | string | boolean>; }
export interface ClassificationResult {
  encounterType: EncounterType; riskOfCollision: boolean;
  giveWay: VesselLabel | null; standOn: VesselLabel | null;
  doubt: boolean; doubtBoundary?: DoubtBoundary; trail: ReasoningTrailEntry[];
}
```

From src/domain/colregs/classify-encounter.ts (existing, read-only -- confirms the exact `facts` keys each trail stage can produce; do not invent additional keys or assume a normalized shape):
- Rule 7 entry: `facts: { riskOfCollision: boolean, tcpaMinutes?: number, dcpaNm?: number }` (`tcpaMinutes`/`dcpaNm` present only when the underlying `cpa()` call succeeded).
- Rule 13(d) (sticky overtaking) entry: `facts: {}` (always empty -- render no fact-readout line for this entry).
- Rule 13(a)-(b) overtaking-direction entries: `facts: { relativeBearingAtoB: number }` OR `facts: { relativeBearingBtoA: number }` (exactly one key, whichever vessel is overtaking).
- Rule 13(a)-(b) not-overtaking / Rule 14(a)-(b) (both branches) / entries: `facts: { relativeBearingAtoB: number, relativeBearingBtoA: number }`.
- Rule 15 entry: `facts: { relativeBearingAtoB: number }`.
- Rule 18(a)-(c) entries (all three branches) and the Rule 13(a) overtaking-skip entry: `facts: { vesselAType: string, vesselBType: string }`.
- The only fact keys that ever appear anywhere in `trail[].facts` are: `riskOfCollision`, `tcpaMinutes`, `dcpaNm`, `relativeBearingAtoB`, `relativeBearingBtoA`, `vesselAType`, `vesselBType`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ReasoningPanel -- verdict banner, full trail list with geometric fact readouts, doubt caveat, degenerate note</name>
  <files>src/components/sandbox/ReasoningPanel.tsx</files>
  <read_first>
    - src/components/sandbox/types.ts (ReasoningPanelProps contract -- do not add/rename props)
    - src/components/sandbox/vessel-role.ts (getVesselRole, reused here so give-way/stand-on/mutual badge styling matches ChartPanel exactly, no independent reimplementation)
    - src/domain/colregs/classify-encounter.ts (confirm the exact `facts` key/value shapes listed in this plan's `<interfaces>` block directly against the source before implementing the fact-readout renderer)
    - .planning/phases/04-interactive-chart-sandbox/04-UI-SPEC.md (Copywriting Contract's Error state copy and doubt-caveat copy verbatim, Typography's Display/Heading/Label/Body roles and the "Numeric readouts... use font-mono" rule, Color's Domain Semantic Colors for badge coloring)
  </read_first>
  <action>
    No `"use client";` needed -- this component has no hooks/event handlers, purely renders props (it will still run client-side because it is imported into the client-boundary `SandboxContainer` in 04-06, but does not itself require the directive).
    Panel title `<h2 className="text-xl font-semibold">Reasoning Trail</h2>` (Heading, 20px/600).
    Verdict banner (Display, 28px/600, `className="text-[28px] font-semibold"`): if `isDegenerate`, render the UI-SPEC Error state verbatim -- heading `"Unable to classify"`, body `"Vessel A and Vessel B are at the same position — drag one apart to resume live classification."` (Body, 16px/400) -- and continue rendering the full trail below it (from the still-current `classification` prop, which is the last-good value, per this component's contract). If not degenerate: compose `"{EncounterTypeTitle} — {verdictClause}"` where `EncounterTypeTitle` maps `"head-on"->"Head-on"`, `"crossing"->"Crossing"`, `"overtaking"->"Overtaking"`, and `verdictClause` is `"mutual obligation"` when `classification.giveWay === null && classification.standOn === null`, else `"{Vessel Label} gives way"` where `{Vessel Label}` is `"Vessel A"`/`"Vessel B"` for `"vesselA"`/`"vesselB"` (e.g. `"Crossing — Vessel A gives way"`, matching 04-UI-SPEC.md's own example verbatim). This banner composes only already-computed structured fields (`encounterType`, `giveWay`, `standOn`) into a heading -- it introduces no new rule-explanation text, so it does not violate RSON-02's "byproduct of rule evaluation, not reverse-engineered" guarantee (that guarantee governs the trail `text` strings, which are rendered unmodified below).
    Vessel role badges: two small badges near the banner, one per vessel, using `getVesselRole(label, classification)` -- `"give-way"` -> text `"GW"`, `className` includes `text-red-500`; `"stand-on"` -> text `"SO"`, `text-green-500`; `"mutual"` -> text `"MUTUAL"`, `text-slate-400` -- identical color mapping to `ChartPanel`'s hull fills (both read from the same `getVesselRole` helper, so they cannot drift).
    Trail list: `<ol>` mapping `classification.trail` (in array order -- this IS the evaluation order, do not sort/filter/reorder) to `<li>` items, each rendering the `ruleId` as a `<span className="text-sm font-semibold">` badge (Label, 14px/600, e.g. `"Rule 15"`) followed by `text` verbatim as Body (16px/400) -- do not paraphrase, truncate, or re-author any `text` value, including the Rule 18(a)-(c) "mutual obligation stands" string.
    Fact readouts (RSON-01's "geometric logic that produced the verdict"): beneath each trail entry's `text`, when `Object.keys(entry.facts).length > 0`, render one additional `<dl className="flex flex-wrap gap-x-4 gap-y-0.5 font-mono text-sm text-slate-600">` mapping each `[key, value]` pair from `Object.entries(entry.facts)` (in object insertion order -- do not sort/reorder keys) into a `<div className="flex gap-1"><dt>{label}:</dt><dd>{formattedValue}</dd></div>` pair. Use this exact label/format mapping, keyed by fact key -- these are the only seven keys `classify-encounter.ts` ever produces (per this plan's `<interfaces>` block): `riskOfCollision` -> label `"Risk of collision"`, value `value ? "Yes" : "No"`; `tcpaMinutes` -> label `"TCPA"`, value `` `${(value as number).toFixed(1)} min` ``; `dcpaNm` -> label `"DCPA"`, value `` `${(value as number).toFixed(2)} nm` ``; `relativeBearingAtoB` -> label `"Rel. bearing A→B"`, value `` `${(value as number).toFixed(1)}°` ``; `relativeBearingBtoA` -> label `"Rel. bearing B→A"`, value `` `${(value as number).toFixed(1)}°` ``; `vesselAType` -> label `"Vessel A type"`, value `String(value)`; `vesselBType` -> label `"Vessel B type"`, value `String(value)`. Do not skip this for any entry whose `facts` object is non-empty. Rule 13(d)'s sticky-overtaking entry has an empty `facts: {}` and correctly renders no readout line for that entry -- this is the one intentional exception. This block applies the UI-SPEC's own font-mono numeric-readout rule to the trail, and is what makes RSON-01's "geometric logic (relative bearing, closing angle)" requirement actually visible rather than only asserted via rule citation and prose.
    Doubt caveat: when `classification.doubt` is true, render one additional `<p className="text-sm text-amber-600">` line beneath the trail, chosen by `classification.doubtBoundary` verbatim from 04-UI-SPEC.md's Copywriting Contract: `"near-overtaking-crossing-boundary"` -> `"Near the overtaking/crossing boundary (112.5° abaft the beam) — this verdict may flip with a small heading change."`; `"near-head-on-boundary"` -> `"Near the head-on boundary (reciprocal heading) — this verdict may flip with a small heading change."`.
    Layout: `<aside className="p-6 flex flex-col gap-4">` (`lg`=24px section padding, `md`=16px inter-element gap per Spacing Scale).
  </action>
  <acceptance_criteria>
    - Contains the exact Error-state strings `"Unable to classify"` and `"Vessel A and Vessel B are at the same position — drag one apart to resume live classification."`
    - Contains both exact doubt-caveat strings verbatim
    - Renders `classification.trail.map(` with no `.sort(`/`.filter(` applied to the trail array
    - For any trail entry with a non-empty `facts` object, renders a `font-mono` key/value readout line beneath its `text` -- file contains `font-mono`, `Object.entries(`, and `toFixed(` (used for the bearing/TCPA/DCPA numeric formatting)
    - Contains all seven fact-key labels: `"Risk of collision"`, `"TCPA"`, `"DCPA"`, `"Rel. bearing A`, `"Rel. bearing B`, `"Vessel A type"`, `"Vessel B type"`
    - Imports and calls `getVesselRole` rather than reimplementing the give-way/stand-on/mutual check
    - Verdict banner composes `encounterType`/`giveWay`/`standOn` only -- contains no new hardcoded rule-explanation sentence beyond the banner template, the fact-readout labels, and the two doubt-caveat strings
  </acceptance_criteria>
  <verify>
    <automated>MISSING -- Task 2 of this plan creates ReasoningPanel.test.tsx; until then, verify via `npx tsc --noEmit` (type-check only)</automated>
  </verify>
  <done>ReasoningPanel renders the verdict banner, full ordered trail with per-entry geometric fact readouts, and doubt caveat exactly per 04-UI-SPEC.md's Copywriting Contract; type-checks clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReasoningPanel tests</name>
  <files>src/components/sandbox/ReasoningPanel.test.tsx</files>
  <read_first>
    - src/components/sandbox/environment.smoke.test.tsx (jsdom+RTL harness pattern, `// @vitest-environment jsdom` docblock)
    - src/domain/colregs/classify-encounter.fixtures.ts (crossingResidualBasicCase, headOnNucRiatmTieCase or headOnGenuineCase for the mutual-obligation case, doubtBandNearOvertakingBoundaryCase for the doubt-caveat case -- reuse these vessel/expected shapes to build ClassificationResult-shaped test props)
    - ReasoningPanel.tsx (as implemented by Task 1 of this plan)
  </read_first>
  <behavior>
    - Given a `crossingResidualBasicCase`-shaped classification (`giveWay: "vesselA"`), the banner text is exactly `"Crossing — Vessel A gives way"`.
    - Given a mutual-obligation classification (`giveWay: null, standOn: null, encounterType: "head-on"`), the banner text is exactly `"Head-on — mutual obligation"` and the `"MUTUAL"` badge is shown, not `"GW"`/`"SO"`.
    - Given `classification.trail` with 3 entries, all 3 `ruleId`+`text` pairs are present in the rendered output, in the same order as the array.
    - Given a trail entry shaped `{ ruleId: "Rule 15", text: "...", facts: { relativeBearingAtoB: 127.34 } }`, the rendered output contains the formatted fact readout `"127.3°"` beneath that entry. Given a Rule 7 entry shaped `{ ruleId: "Rule 7", text: "...", facts: { riskOfCollision: true, tcpaMinutes: 4.2, dcpaNm: 0.83 } }`, the rendered output contains `"4.2 min"` and `"0.83 nm"`. Given a Rule 13(d) entry shaped `{ ruleId: "Rule 13(d)", text: "...", facts: {} }`, no fact-readout `<dl>` is rendered for that entry (assert its sibling entries' `<dl>` count matches only the non-empty-`facts` entries).
    - Given `doubt: true, doubtBoundary: "near-overtaking-crossing-boundary"`, the exact overtaking-boundary caveat string is shown; given `"near-head-on-boundary"`, the exact head-on caveat string is shown instead.
    - Given `isDegenerate: true`, `"Unable to classify"` is shown AND the trail entries from the (still-current, last-good) `classification` prop are still present in the output.
  </behavior>
  <action>
    Write `ReasoningPanel.test.tsx` against the Task 1 implementation using `render`/`screen` from `@testing-library/react`. `// @vitest-environment jsdom` docblock as the first line. Build minimal inline `ClassificationResult` fixture objects per case (or import/adapt vessel shapes from `classify-encounter.fixtures.ts` where noted in read_first) -- this file's fixtures are small enough not to need a separate `.fixtures.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/components/sandbox/ReasoningPanel.test.tsx</automated>
  </verify>
  <done>All six behavior cases pass; `npx vitest run` (full suite) exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| classification.trail text/facts -> JSX rendering | `ReasoningTrailEntry.text`/`.ruleId`/`.facts` values are static, developer-authored strings/numbers baked into `classify-encounter.ts` at Phase 2 -- not user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Tampering / Information Disclosure | Reasoning trail text/facts rendering | accept | `text`/`ruleId`/`facts` are static strings/numbers, not user input; React's default JSX text-node escaping applies regardless; this plan introduces no `dangerouslySetInnerHTML` anywhere |
</threat_model>

<verification>
- `npx vitest run src/components/sandbox/ReasoningPanel.test.tsx` exits 0
- `npx vitest run` (full suite) exits 0
</verification>

<success_criteria>
- `ReasoningPanel` renders the full, ordered, unmodified reasoning trail -- rule citation, prose, AND the underlying geometric facts -- alongside a verdict banner and doubt caveat
- The degenerate "Unable to classify" state renders without discarding the last-good trail
</success_criteria>

<output>
Create `.planning/phases/04-interactive-chart-sandbox/04-05-SUMMARY.md` when done
</output>
