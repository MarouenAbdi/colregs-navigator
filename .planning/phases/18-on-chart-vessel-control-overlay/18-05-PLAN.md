---
phase: 18-on-chart-vessel-control-overlay
plan: 05
type: execute
wave: 5
depends_on: ["18-04"]
files_modified: []
autonomous: false
requirements: [SBOX-06, SBOX-07, SBOX-08]

must_haves:
  truths:
    - "A single merged header strip (rule badge + encounter title + risk badge) sits atop the chart -- the separate VerdictBanner card is gone (ROADMAP criterion 1)"
    - "A single merged footer strip (LIVE/RANGE/BEARING/CPA/TCPA + each vessel's required-action text) sits below the chart -- the separate InstrumentReadouts card is gone (ROADMAP criterion 2)"
    - "User can click a vessel to open a floating control card (type, speed, heading); clicking the other vessel moves the overlay to it; re-clicking the same vessel closes it; the side ControlPanel is fully removed (ROADMAP criterion 3)"
    - "With the overlay open, dragging and rotating either vessel still works correctly with no dead zones -- human-verified, not assumed (ROADMAP criterion 4, this codebase's 3rd occurrence of the hit-testing regression class)"
  artifacts: []
  key_links: []
---

<objective>
Human-verify, in a real browser, that all 4 ROADMAP Phase 18 success criteria hold end-to-end -- something jsdom-based automated tests (Plans 18-01 through 18-04) structurally cannot observe: real CSS stacking/positioning of the floating overlay card, real pointer-capture routing across two painted surfaces (SVG hull/rotate-handle vs. an HTML overlay card), and genuine visual fidelity to the design snapshot.

Purpose: Per this project's own documented incident history (Phase 4 and Phase 8 hit-testing regressions, both requiring live human UAT before being caught/fixed) and `.planning/research/PITFALLS.md`'s Pitfall 5 ("passing automated tests is not this project's actual acceptance bar"), ROADMAP Phase 18 success criterion 4 explicitly requires human verification of the drag/rotate-with-overlay-open interaction, not just green CI. This is the 3rd occurrence of this exact regression class in this codebase.
Output: Human confirmation that all 4 ROADMAP Phase 18 success criteria are satisfied in a real browser session, closing out the phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-on-chart-vessel-control-overlay/18-CONTEXT.md
@.planning/phases/18-on-chart-vessel-control-overlay/18-DESIGN-SNAPSHOT.md
@.planning/research/PITFALLS.md
@.planning/phases/18-on-chart-vessel-control-overlay/18-01-SUMMARY.md
@.planning/phases/18-on-chart-vessel-control-overlay/18-02-SUMMARY.md
@.planning/phases/18-on-chart-vessel-control-overlay/18-03-SUMMARY.md
@.planning/phases/18-on-chart-vessel-control-overlay/18-04-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human-verify header/footer strip visual fidelity (SBOX-06/SBOX-07, ROADMAP criteria 1-2)</name>
  <files>None -- verification-only task, no files modified</files>
  <read_first>
    .planning/phases/18-on-chart-vessel-control-overlay/18-DESIGN-SNAPSHOT.md (sections "Header command strip", "Footer instrument bar")
    .planning/phases/18-on-chart-vessel-control-overlay/18-02-SUMMARY.md
  </read_first>
  <action>
    Start the dev server and visually confirm the header/footer strips match the design snapshot's layout and content, and that no trace of the retired standalone cards remains.
  </action>
  <what-built>
    ChartHeaderStrip (rule badge + title + 4-tier CPA-distance-driven risk pill, D-02) and ChartFooterStrip (LIVE indicator + RANGE/BEARING/CPA/TCPA tiles + per-vessel role badge + D-03's exact required-action copy) now render as sub-regions of one merged ChartPanel card, replacing the standalone VerdictBanner and InstrumentReadouts cards (deleted in Plan 18-04).
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open `http://localhost:3000/`. Confirm the header strip (rule badge + "Crossing"-style title + risk pill) sits directly atop the chart as ONE bordered unit -- no separate standalone verdict card anywhere on the page.
    2. Confirm the footer strip (LIVE pulsing dot + RANGE/BEARING A→B/CPA/TCPA tiles on the left, a divider, then each vessel's role badge + required-action sentence on the right) sits directly below the chart as part of the same bordered unit -- no separate standalone instrument-readouts card.
    3. Drag a vessel to a new position/speed such that its CPA crosses each of the 4 risk thresholds in turn (very close CPA &lt; 0.3nm, then 0.3-1.0nm, then &gt; 1.0nm, then a course where the vessels are opening/CPA already passed) -- confirm the risk pill's tone/text visibly changes across all 4 tiers (high/watch/ok/none) and never crashes or shows a blank pill.
    4. Drag both vessels onto the exact same position -- confirm the header shows "Unable to classify" with NO rule badge, the risk pill shows the degenerate amber framing, and the footer's action-text cells for both vessels show "—" while the RANGE tile shows "0.00 NM".
    5. Resize the browser window to a narrow (mobile-width, &lt;900px) viewport -- confirm the footer strip's per-vessel action panel switches from a left divider to a top divider (wraps to full width) without visually breaking.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 checks pass as expected, or describe the specific step/behavior that differs.</resume-signal>
  <acceptance_criteria>
    - Human types "approved" after confirming all 5 steps behave as expected
    - No standalone VerdictBanner or InstrumentReadouts card is visible anywhere on the page (behavior assertion, ROADMAP criteria 1-2)
    - All 4 risk-pill tiers are reachable and visually distinct (behavior assertion, D-02)
    - The degenerate state shows no rule badge, "Unable to classify" framing, and "—" action-text placeholders (behavior assertion, D-02/D-03)
  </acceptance_criteria>
  <verify>
    <automated>MISSING -- this is a human-browser-verification checkpoint; no automated test can observe true visual/CSS fidelity to the design snapshot, per this project's jsdom-measurement-ban precedent (see .planning/research/PITFALLS.md Pitfall 5)</automated>
  </verify>
  <done>Human has typed "approved", confirming the header/footer strips visually and behaviorally match the design snapshot across all risk tiers and the degenerate state.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify the overlay's open/close/switch paths and live type/speed editing (SBOX-08, ROADMAP criterion 3)</name>
  <files>None -- verification-only task, no files modified</files>
  <read_first>
    .planning/phases/18-on-chart-vessel-control-overlay/18-CONTEXT.md (D-01, D-04)
    .planning/phases/18-on-chart-vessel-control-overlay/18-03-SUMMARY.md
  </read_first>
  <action>
    Manually drive every one of D-01's 3 close paths plus the toggle and switch behaviors, and confirm the overlay's TYPE/SPEED fields genuinely re-derive the live classification.
  </action>
  <what-built>
    ChartPanel's selectedVessel state (Plan 18-03) opens a vessel's VesselOverlayCard (Plan 18-02) on hull/rotate-handle pointerdown (D-04), toggles closed on a second press of the same vessel (D-01), switches to the other vessel on its press, and closes on either the card's × button or a bare-chart-background click. The overlay's TYPE/SPEED fields route through the exact same useSandboxState() callbacks ControlPanel used to call (removed in Plan 18-04).
  </what-built>
  <how-to-verify>
    1. Confirm no always-visible side ControlPanel column exists on the page.
    2. Click Vessel A's hull -- confirm a floating card opens near it showing TYPE/SPEED/HEADING for Vessel A, anchored diagonally opposite Vessel A's own on-chart quadrant (not covering the vessel it describes).
    3. Click Vessel A's hull again -- confirm the card closes (D-01 toggle).
    4. Click Vessel A's hull to reopen it, then click Vessel B's hull -- confirm the overlay switches directly to Vessel B (not closing first, not showing both).
    5. With Vessel B's overlay open, click its × close button -- confirm it closes.
    6. Reopen an overlay, then click a bare area of the chart background (not on either vessel) -- confirm the overlay closes.
    7. With an overlay open, change its TYPE via the Select and its SPEED via the Slider -- confirm the header/footer strips' verdict and risk pill update live, with no separate submit step, exactly as ControlPanel used to behave.
    8. Confirm the HEADING field in the overlay is read-only (no control), matching the existing drag-only-heading convention.
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 checks pass as expected, or describe the specific step/behavior that differs.</resume-signal>
  <acceptance_criteria>
    - Human types "approved" after confirming all 8 steps behave as expected
    - All 3 close paths (re-click-toggle, × button, empty-chart click) independently close the overlay (behavior assertion, D-01)
    - Switching between vessels never closes the overlay first (behavior assertion, the regression this plan's Plan 18-03 automated test also covers, now confirmed live)
    - TYPE/SPEED edits via the overlay live-update the header/footer strips with no submit step (behavior assertion, SBOX-08)
  </acceptance_criteria>
  <verify>
    <automated>MISSING -- this is a human-browser-verification checkpoint; jsdom's synthetic PointerEvent dispatch cannot substitute for real pointer-capture/CSS-stacking behavior across the SVG chart and the HTML overlay card, per this project's jsdom-measurement-ban precedent</automated>
  </verify>
  <done>Human has typed "approved", confirming all 3 close paths, the toggle, the vessel-switch behavior, and live TYPE/SPEED editing all work as designed in a real browser.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: CRITICAL -- human-verify drag/rotate hit-testing with the overlay open (ROADMAP criterion 4, 3rd occurrence of this regression class)</name>
  <files>None -- verification-only task, no files modified</files>
  <read_first>
    .planning/research/PITFALLS.md (Pitfall 1, full section)
    .planning/PROJECT.md (Phase 4 and Phase 8 hit-testing regression precedents in the Key Decisions table)
  </read_first>
  <action>
    This is the single highest-priority verification item in the phase, per ROADMAP's own explicit callout. Manually drive drag and rotate gestures against both vessels while an overlay is open, including the specific configuration where the overlay card visually overlaps the other vessel.
  </action>
  <what-built>
    Plan 18-03's automated tests already proved (via synthetic jsdom PointerEvent dispatch) that dragging vesselB still fires onVesselPositionChange while vesselA's overlay is open and visually near it. This task re-verifies the same scenario with a real browser's actual pointer-capture and CSS-paint-order behavior, which jsdom cannot faithfully emulate -- this codebase has twice previously shipped a passing test suite alongside a real hit-testing regression (Phase 4, Phase 8).
  </what-built>
  <how-to-verify>
    1. Load the default scenario (Vessel A and Vessel B roughly 5nm apart). Click Vessel A to open its overlay. Confirm the overlay card's on-screen position and check whether it visually overlaps any part of Vessel B's hull or rotate-handle.
    2. With Vessel A's overlay open, drag Vessel A's own hull to a new position -- confirm the drag works smoothly with no dead zones or stutters, and the overlay follows/updates or closes/reopens sensibly (does not get "stuck" showing stale data).
    3. With Vessel A's overlay open, drag Vessel B's hull to a new position -- confirm the drag starts immediately on pointer-down (no missed first click, no requiring a second attempt), even in the area where the overlay visually overlaps Vessel B.
    4. With Vessel A's overlay open, grab Vessel B's rotate handle and rotate it through a full circle -- confirm the rotation tracks smoothly with no dead zones, including at headings where the hull polygon's shape changes its screen footprint.
    5. Drag the two vessels close enough together that Vessel A's overlay, once opened, visually covers a significant portion of Vessel B -- repeat steps 3-4 in this deliberately-adversarial configuration.
    6. Open the browser devtools' Elements panel and confirm the overlay card's root element (or its wrapping div) shows `pointer-events: none` in its computed styles, and that only its interactive children (close button, Select trigger, Slider) show `pointer-events: auto`.
    7. Close the overlay via any path and confirm dragging/rotating both vessels continues to work normally afterward (no lingering dead zone from an unmounted-but-still-hit-testable element).
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 checks pass with zero dead zones or missed gestures, or describe the specific step/behavior that fails.</resume-signal>
  <acceptance_criteria>
    - Human types "approved" after confirming all 7 steps show zero dead zones or missed pointer gestures
    - Dragging/rotating either vessel works correctly whether or not an overlay is open, and regardless of which vessel's overlay is open (behavior assertion, ROADMAP criterion 4)
    - The overlay card's computed `pointer-events: none`/`auto` split is confirmed via devtools, not just assumed from source review (behavior assertion, Pitfall 1)
    - No dead zone persists after an overlay is closed (behavior assertion, regression-class-specific: a delayed unmount was the exact failure mode in a prior incident class)
  </acceptance_criteria>
  <verify>
    <automated>MISSING -- this is the project's mandatory human-browser-verification checkpoint for its 3rd occurrence of the painted-element-swallows-pointer-event hit-testing regression class (Phase 4, Phase 8 precedent); jsdom cannot observe real pointer-capture routing or real CSS stacking/pointer-events computed values, per .planning/research/PITFALLS.md Pitfall 5</automated>
  </verify>
  <done>Human has typed "approved", confirming zero drag/rotate dead zones exist with any vessel's overlay open, in any relative vessel configuration, verified against real computed pointer-events styles in devtools.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new this plan | This plan performs no code changes -- it is a human-verification checkpoint over Plans 18-01 through 18-04's implementation. No new trust boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07 | N/A | This plan modifies zero files | accept | No code changes in this plan; the STRIDE registers for the actual code changes live in Plans 18-01/18-02/18-03/18-04's own `<threat_model>` blocks. This checkpoint exists purely to human-verify runtime/UX/visual behavior, not to introduce or mitigate a new threat surface. |
</threat_model>

<verification>
Human confirms all 3 tasks' `<how-to-verify>` steps behave as expected against the live `/` route in a real browser, with particular attention to Task 3's hit-testing regression re-check.
</verification>

<success_criteria>
All 4 ROADMAP Phase 18 success criteria are satisfied and human-confirmed:
1. The merged header strip replaces VerdictBanner, with correct 4-tier risk-pill behavior.
2. The merged footer strip replaces InstrumentReadouts, with correct per-vessel action copy.
3. Click-to-open overlay replaces ControlPanel, with all 3 close paths + toggle + switch working.
4. Drag/rotate hit-testing has zero dead zones with any overlay open, in any vessel configuration -- human-verified, not assumed.
</success_criteria>

<output>
Create `.planning/phases/18-on-chart-vessel-control-overlay/18-05-SUMMARY.md` when done
</output>
