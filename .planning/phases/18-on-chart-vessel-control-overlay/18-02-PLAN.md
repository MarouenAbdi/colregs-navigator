---
phase: 18-on-chart-vessel-control-overlay
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - src/components/sandbox/chart/ChartHeaderStrip.tsx
  - src/components/sandbox/chart/ChartHeaderStrip.test.tsx
  - src/components/sandbox/chart/ChartFooterStrip.tsx
  - src/components/sandbox/chart/ChartFooterStrip.test.tsx
  - src/components/sandbox/chart/VesselOverlayCard.tsx
  - src/components/sandbox/chart/VesselOverlayCard.test.tsx
autonomous: true
requirements: [SBOX-06, SBOX-07, SBOX-08]

must_haves:
  truths:
    - "ChartHeaderStrip renders the same rule-badge/title/degenerate logic VerdictBanner.tsx currently renders, plus a new 4-tier risk pill on the right (D-02), truncating instead of wrapping when content overflows"
    - "ChartFooterStrip renders the same RANGE/BEARING/CPA/TCPA tiles InstrumentReadouts.tsx currently renders, plus each vessel's role badge and D-03's exact required-action copy, with both vessels' action text degrading to '—' when isDegenerate"
    - "VesselOverlayCard renders the same TYPE(editable)/SPEED(editable)/HEADING(read-only) fields ControlPanel.tsx's VesselFormSection currently renders, plus a role badge and an explicit close control, with the card's own non-interactive chrome set to pointer-events-none and only its real interactive controls re-enabled to pointer-events-auto (Pitfall 1's explicit overlay mitigation)"
  artifacts:
    - path: "src/components/sandbox/chart/ChartHeaderStrip.tsx"
      provides: "Merged rule-badge + title + risk-pill header strip (SBOX-06)"
      contains: "export function ChartHeaderStrip"
    - path: "src/components/sandbox/chart/ChartFooterStrip.tsx"
      provides: "Merged RANGE/BEARING/CPA/TCPA + per-vessel role/action footer strip (SBOX-07)"
      contains: "export function ChartFooterStrip"
    - path: "src/components/sandbox/chart/VesselOverlayCard.tsx"
      provides: "Floating per-vessel TYPE/SPEED/HEADING control card (SBOX-08)"
      contains: "export function VesselOverlayCard"
  key_links:
    - from: "VesselOverlayCard.tsx's Select/Slider onChange handlers"
      to: "onVesselSpeedChange/onVesselTypeChange props (the same useSandboxState() callbacks ControlPanel.tsx used)"
      via: "direct prop call, no new mutation path (Phase 16 single-choke-point invariant)"
      pattern: "onVesselSpeedChange\\(|onVesselTypeChange\\("
---

<objective>
Build the three new presentational components this phase introduces -- `ChartHeaderStrip`, `ChartFooterStrip`, `VesselOverlayCard` -- each fully self-contained and unit-tested against Plan 18-01's contracts/utils, with no wiring into `ChartPanel.tsx`/`SandboxContainer.tsx` yet. None of these components is imported anywhere yet after this plan; Plan 18-03 does the wiring.

Purpose: Delivers the actual visual/behavioral content for SBOX-06/07/08 as isolated, independently-verifiable units, mining `VerdictBanner.tsx`/`InstrumentReadouts.tsx`/`ControlPanel.tsx`'s existing logic (all three are retired in Plan 18-04, so this plan copies their relevant logic rather than importing from them) while applying D-02's 4-tier risk pill, D-03's action copy, and Pitfall 1's pointer-events-none-by-default overlay mitigation.
Output: `ChartHeaderStrip.tsx`, `ChartFooterStrip.tsx`, `VesselOverlayCard.tsx`, each with a passing test file.

Note on scope: CONTEXT.md's "Claude's Discretion" section lists the design's decorative corner bezel accents (small teal corner brackets on the chart chrome) as optional -- not called out in any ROADMAP success criterion. This plan deliberately omits them to stay within budget; this is a discretionary choice, not a gap, and can be picked up later if desired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-on-chart-vessel-control-overlay/18-CONTEXT.md
@.planning/phases/18-on-chart-vessel-control-overlay/18-DESIGN-SNAPSHOT.md
@.planning/phases/18-on-chart-vessel-control-overlay/18-PATTERNS.md
@.planning/research/PITFALLS.md
</context>

<interfaces>
<!-- Plan 18-01's already-shipped contracts these components import, verbatim. -->

```typescript
// from "../types.js"
export interface ChartHeaderStripProps { vesselA: Vessel; vesselB: Vessel; classification: ClassificationResult; isDegenerate: boolean; }
export interface ChartFooterStripProps { vesselA: Vessel; vesselB: Vessel; classification: ClassificationResult; isDegenerate: boolean; }
export interface VesselOverlayCardProps {
  label: VesselLabel; letter: "A" | "B"; vessel: Vessel; classification: ClassificationResult;
  onVesselSpeedChange: VesselUpdateHandlers["onVesselSpeedChange"];
  onVesselTypeChange: VesselUpdateHandlers["onVesselTypeChange"];
  onClose: () => void;
}

// from "./chart-header-risk.js"
export function deriveChartHeaderRisk(cpaNm: number | null, tcpaMinutes: number | null): { tier: ChartHeaderRiskTier; text: string };
export const CHART_HEADER_RISK_TONE_CLASSNAME: Record<ChartHeaderRiskTier, string>;
export const CHART_HEADER_RISK_DOT_CLASSNAME: Record<ChartHeaderRiskTier, string>;

// from "./footer-action-copy.js"
export const ROLE_ACTION_TEXT: Record<VesselRole, string>;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChartHeaderStrip.tsx -- merged rule badge + title + 4-tier risk pill (SBOX-06)</name>
  <files>src/components/sandbox/chart/ChartHeaderStrip.tsx, src/components/sandbox/chart/ChartHeaderStrip.test.tsx</files>
  <read_first>
    src/components/sandbox/reasoning/VerdictBanner.tsx (full file -- mine bannerRuleBadge/ENCOUNTER_TYPE_TITLE/DEGENERATE_TITLE logic; this file is deleted in Plan 18-04, so copy, do not import)
    src/components/sandbox/reasoning/reasoning-trail-tag.ts (classifyingEntryIndex/ruleNumber -- import from here directly, this file is NOT retired)
    src/components/sandbox/chart/chart-header-risk.ts (Plan 18-01 output -- import, do not re-derive)
    src/components/sandbox/instruments/instrument-readouts.ts (deriveInstrumentReadouts -- source of cpaNm/tcpaMinutes for the risk pill)
    .planning/phases/18-on-chart-vessel-control-overlay/18-DESIGN-SNAPSHOT.md (section "Header command strip")
    .planning/phases/18-on-chart-vessel-control-overlay/18-CONTEXT.md (D-02)
  </read_first>
  <behavior>
    - Non-degenerate crossing classification -&gt; renders a rule badge (e.g. "Rule 15"), an `<h3>`-or-similar title (e.g. "Crossing"), and a risk pill on the right whose text comes from `deriveChartHeaderRisk(cpaNm, tcpaMinutes)` (values from `deriveInstrumentReadouts(vesselA, vesselB)`)
    - `classification.doubt === true` -&gt; rule badge reads "Rule 7" regardless of `classification.trail`'s actual classifying entry (mirrors VerdictBanner's existing doubt-always-wins rule)
    - `isDegenerate === true` -&gt; NO rule badge renders at all (mirrors VerdictBanner's exact current behavior: the badge span is omitted, not shown as a placeholder), title reads "Unable to classify", and the risk pill uses `CHART_HEADER_RISK_TONE_CLASSNAME.watch`'s tone class with "Unable to classify" framing in its text -- ignoring `deriveChartHeaderRisk()`'s own output entirely while degenerate (per D-02: "the degenerate/error state reuses the amber `watch` palette with the existing 'Unable to classify' framing")
    - Mutual encounter (`classification.giveWay === null && classification.standOn === null`) -&gt; rule-badge/divider accent uses the mutual token, not the default rule-accent teal (mirrors VerdictBanner's `bannerAccentClassName`)
  </behavior>
  <action>
    Create a flex-row component: left group = rule-badge span (background from the same accent-class logic as VerdictBanner's retired `bannerAccentClassName`, using `bg-doubt`/`bg-mutual`/`bg-rule-accent`) + title, with `overflow-hidden text-ellipsis whitespace-nowrap` on the title so it truncates rather than wraps (per the design snapshot's header markup); a `flex-1` spacer; then the risk pill on the right with `max-w-[46%]` and its own `overflow-hidden text-ellipsis whitespace-nowrap` on the text span, a small rounded dot (`CHART_HEADER_RISK_DOT_CLASSNAME[tier]`) + the risk text, wrapped in a rounded-md bordered chip using `CHART_HEADER_RISK_TONE_CLASSNAME[tier]`. Copy (do not import) `ENCOUNTER_TYPE_TITLE`, `DEGENERATE_TITLE`, and the `bannerRuleBadge`/`bannerAccentClassName`-equivalent logic from `VerdictBanner.tsx` verbatim into this file (identical values, since `VerdictBanner.tsx` is retired in Plan 18-04). Call `deriveInstrumentReadouts(vesselA, vesselB)` once for `cpaNm`/`tcpaMinutes`, then `isDegenerate ? { tier: "watch", text: "Unable to classify — vessels are coincident." } : deriveChartHeaderRisk(cpaNm, tcpaMinutes)` to decide the pill's actual tier/text (exact wording of the degenerate text is discretionary per CONTEXT.md -- meaning must match "Unable to classify"). Root container gets `flex items-center gap-3 px-4 py-3 border-b border-border` (matches the design's `hdrbar` border-bottom, and the outer merged-card frame Plan 18-03 wraps this in) -- do not use the shadcn `Card`/`CardHeader` primitives here (this is a sub-region of one merged chart card, not a standalone card, unlike the retired `VerdictBanner`).
  </action>
  <acceptance_criteria>
    - `npx vitest run ChartHeaderStrip.test.tsx` passes with 0 failures covering every case in &lt;behavior&gt;
    - `grep -c "export function ChartHeaderStrip" src/components/sandbox/chart/ChartHeaderStrip.tsx` outputs `1`
    - `grep -c "deriveChartHeaderRisk" src/components/sandbox/chart/ChartHeaderStrip.tsx` outputs at least `1`
    - `grep -c "from \"../reasoning/VerdictBanner" src/components/sandbox/chart/ChartHeaderStrip.tsx` outputs `0` (logic is copied, not imported, since VerdictBanner.tsx is retired next plan)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run ChartHeaderStrip.test.tsx && npm run typecheck</automated>
  </verify>
  <done>ChartHeaderStrip renders rule badge + title + 4-tier risk pill exactly per &lt;behavior&gt;; no import from VerdictBanner.tsx; typecheck passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ChartFooterStrip.tsx -- merged instrument readouts + per-vessel role/action panel (SBOX-07)</name>
  <files>src/components/sandbox/chart/ChartFooterStrip.tsx, src/components/sandbox/chart/ChartFooterStrip.test.tsx</files>
  <read_first>
    src/components/sandbox/instruments/InstrumentReadouts.tsx (full file -- mine the Tile component + formatBearing/formatCpa/formatTcpa; this file is deleted in Plan 18-04, so copy, do not import)
    src/components/sandbox/vessel-role.ts (getVesselRole, ROLE_BADGE_TEXT, ROLE_BADGE_CLASSNAME -- import, do not duplicate)
    src/components/sandbox/chart/footer-action-copy.ts (Plan 18-01 output -- import ROLE_ACTION_TEXT, do not re-derive)
    .planning/phases/18-on-chart-vessel-control-overlay/18-DESIGN-SNAPSHOT.md (section "Footer instrument bar")
    .planning/phases/18-on-chart-vessel-control-overlay/18-CONTEXT.md (D-03)
  </read_first>
  <behavior>
    - Real (non-degenerate) crossing classification -&gt; renders 4 tiles (RANGE/BEARING A→B/CPA/TCPA) with the exact same formatted values `InstrumentReadouts.test.tsx`'s existing fixture already proves (e.g. "5.00 NM"/"090°"/"3.54 NM"/"15.0 min" for `crossingResidualBasicCase`), plus a "LIVE" label with a pulsing dot, plus both vessels' role badge (GW/SO) and `ROLE_ACTION_TEXT[role]`'s exact string
    - Coincident vessels (bearing/cpa/tcpa all null) with `isDegenerate: false` -&gt; the 3 dependent tiles render "—" placeholders and RANGE renders "0.00 NM" (identical to `InstrumentReadouts.test.tsx`'s existing degenerate-fixture case) -- but the action-copy panel still shows the last-good role's real action text, since `isDegenerate` is what actually gates the action text, not the tile values
    - `isDegenerate: true` -&gt; both vessels' action-text cell renders `"—"` (D-03's exact placeholder convention), while the role badges (GW/SO/MUTUAL) still render normally from the retained last-good `classification`
  </behavior>
  <action>
    Create a flex-row component with two zones separated by a `flex-1` spacer, following the design's `ftrbar` layout: left zone = "LIVE" label (a small dot using the Tailwind `animate-pulse` utility class -- do NOT add a new `@keyframes` to `app/globals.css`, `animate-pulse` already ships in Tailwind v4 core and is sufficient) + 4 `Tile` sub-components (copy `Tile`/`formatBearing`/`formatCpa`/`formatTcpa`/the local `PLACEHOLDER = "—"` constant verbatim from `InstrumentReadouts.tsx`, calling `deriveInstrumentReadouts(vesselA, vesselB)` the same way); right zone = per-vessel action panel, one cell per vessel (letter chip + `getVesselRole(label, classification)` badge via `ROLE_BADGE_TEXT`/`ROLE_BADGE_CLASSNAME` + action text), separated by a divider that is `border-t` on mobile and `border-l` at `min-[900px]:` (matching `SandboxContainer.tsx`'s existing custom-breakpoint convention, and the design's own mobile `border-top` swap). Action text per vessel: `isDegenerate ? "—" : ROLE_ACTION_TEXT[getVesselRole(label, classification)]`. Root container gets `flex flex-wrap items-stretch border-t border-border` (matches the design's `ftrbar` border-top and the outer merged-card frame Plan 18-03 wraps this in) -- do not use shadcn `Card`/`CardHeader`/`CardContent` here, same reasoning as Task 1.
  </action>
  <acceptance_criteria>
    - `npx vitest run ChartFooterStrip.test.tsx` passes with 0 failures covering every case in &lt;behavior&gt;
    - `grep -c "export function ChartFooterStrip" src/components/sandbox/chart/ChartFooterStrip.tsx` outputs `1`
    - `grep -c "ROLE_ACTION_TEXT" src/components/sandbox/chart/ChartFooterStrip.tsx` outputs at least `1`
    - `grep -c "@keyframes" src/components/sandbox/chart/ChartFooterStrip.tsx app/globals.css` totals `0` (no new custom CSS added for the LIVE dot)
    - `grep -c "from \"../instruments/InstrumentReadouts" src/components/sandbox/chart/ChartFooterStrip.tsx` outputs `0` (logic copied, not imported -- that file is retired next plan)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run ChartFooterStrip.test.tsx && npm run typecheck</automated>
  </verify>
  <done>ChartFooterStrip renders the 4 readout tiles + LIVE indicator + per-vessel role/action panel exactly per &lt;behavior&gt;; uses animate-pulse (no new CSS); typecheck passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: VesselOverlayCard.tsx -- floating TYPE/SPEED/HEADING control card, pointer-events-safe (SBOX-08)</name>
  <files>src/components/sandbox/chart/VesselOverlayCard.tsx, src/components/sandbox/chart/VesselOverlayCard.test.tsx</files>
  <read_first>
    src/components/sandbox/control-panel/ControlPanel.tsx (full file -- mine VesselFormSection's Select/Slider wiring, VESSEL_TYPE_LABELS, formatHeading; this file is deleted in Plan 18-04, so copy, do not import)
    src/components/sandbox/vessel-role.ts (getVesselRole, ROLE_BADGE_TEXT, ROLE_BADGE_CLASSNAME -- import, do not duplicate)
    src/components/sandbox/control-panel/ControlPanel.test.tsx (Radix Select/Slider interaction-test pattern to mirror: click+findByRole for Select, focus+keyboard for Slider, plus the ResizeObserver/PointerCapture/scrollIntoView jsdom polyfills)
    .planning/phases/18-on-chart-vessel-control-overlay/18-DESIGN-SNAPSHOT.md (section "Vessel-control overlay")
    .planning/research/PITFALLS.md (Pitfall 1 -- "pointer-events: none on its own root container by default, re-enable pointer-events: auto only on the specific interactive controls inside it")
  </read_first>
  <behavior>
    - Renders letter chip ("A"/"B"), role badge (GW/SO/MUTUAL via `getVesselRole`), and a close button
    - TYPE: a `Select` listing exactly the 5 `VesselTypeSchema.options` with `VESSEL_TYPE_LABELS`' display strings (identical list `ControlPanel.test.tsx` already proves) -- calls `onVesselTypeChange(label, type)` on change
    - SPEED: a `Slider` (min 0, max 40, step 1 -- this codebase's real domain range, NOT the design snapshot's own `max=24` literal, which does not match `VesselSchema`) showing the live `${vessel.speed} kn` value -- calls `onVesselSpeedChange(label, speed)` on change
    - HEADING: a read-only `${formatHeading(vessel.heading)}` span, e.g. "035°" -- no control, no onChange handler at all (matches `ControlPanel.tsx`'s existing "heading is drag-only" convention)
    - Close button click calls `onClose()`
    - The card's root/chrome wrapper carries `pointer-events-none`; the close button, the Select's trigger, and the Slider each individually carry `pointer-events-auto` so they remain clickable/draggable despite the ancestor's `pointer-events-none` (Pitfall 1's explicit mitigation -- verified via a DOM assertion, not just visual inspection)
  </behavior>
  <action>
    Create the component reading `label`/`letter`/`vessel`/`classification`/`onVesselSpeedChange`/`onVesselTypeChange`/`onClose` from `VesselOverlayCardProps`. Copy (do not import) `VESSEL_TYPE_LABELS` and `formatHeading()` verbatim from `ControlPanel.tsx` into this file. Root container: a plain `div` (not shadcn `Card`) with `pointer-events-none w-full rounded-xl border border-rule-accent/45 bg-card/95 p-[13px] shadow-lg backdrop-blur-sm` (semi-transparent floating-chip look per the design's `posCard`, expressed via this codebase's existing tokens/utilities, not the design's raw hex/rgba literals -- positioning itself, e.g. `absolute`/`left`/`top`, is applied by the wrapping div `ChartPanel.tsx` renders in Plan 18-03, not by this component). Header row: letter chip + `getVesselRole(label, classification)` badge (`ROLE_BADGE_CLASSNAME`/`ROLE_BADGE_TEXT`) + a close `<button type="button" aria-label={`Close ${letter === "A" ? "Vessel A" : "Vessel B"} control card`} className="pointer-events-auto ..." onClick={onClose}>` using an `X` icon from `lucide-react` (already a project dependency; matches `SandboxContainer.tsx`'s existing `RotateCcw`/`Link2` icon-import precedent). TYPE: `<Select value={vessel.type} onValueChange={(type) => onVesselTypeChange(label, type as VesselType)}>` with `SelectTrigger className="pointer-events-auto w-full"` (`SelectContent`/`SelectItem` render via Radix's own portal to `document.body`, escaping the `pointer-events-none` ancestor automatically -- no extra class needed there). SPEED: `<Slider aria-label={...} value={[vessel.speed]} min={0} max={40} step={1} onValueChange={([speed]) => onVesselSpeedChange(label, speed)} className="pointer-events-auto" />`. HEADING: a plain read-only `<span>{formatHeading(vessel.heading)}</span>`, no `pointer-events-auto` needed (not interactive).
  </action>
  <acceptance_criteria>
    - `npx vitest run VesselOverlayCard.test.tsx` passes with 0 failures covering every case in &lt;behavior&gt;, including a DOM assertion that the root container has class `pointer-events-none` AND the close button/Select-trigger/Slider each have class `pointer-events-auto`
    - `grep -c "export function VesselOverlayCard" src/components/sandbox/chart/VesselOverlayCard.tsx` outputs `1`
    - `grep -c "pointer-events-none" src/components/sandbox/chart/VesselOverlayCard.tsx` outputs at least `1`
    - `grep -c "pointer-events-auto" src/components/sandbox/chart/VesselOverlayCard.tsx` outputs at least `3` (close button, Select trigger, Slider)
    - `grep -c "max={40}" src/components/sandbox/chart/VesselOverlayCard.tsx` outputs `1` (real domain range, not the design's max=24)
    - `grep -c "from \"../control-panel/ControlPanel" src/components/sandbox/chart/VesselOverlayCard.tsx` outputs `0` (logic copied, not imported -- that file is retired next plan)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run VesselOverlayCard.test.tsx && npm run typecheck</automated>
  </verify>
  <done>VesselOverlayCard renders TYPE(editable)/SPEED(editable)/HEADING(read-only) + role badge + close button exactly per &lt;behavior&gt;, with the pointer-events-none/auto split proven via a DOM assertion, not just visual review; typecheck passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new this plan | Three presentational components rendering already-classified, already-validated in-memory data via already-existing callback props (`onVesselSpeedChange`/`onVesselTypeChange`), unwired to anything yet. No new input source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-02 | Tampering | VesselOverlayCard's TYPE/SPEED fields | accept | Identical downstream validation surface to `ControlPanel.tsx`'s retired fields -- `onVesselSpeedChange`/`onVesselTypeChange` still route through `useSandboxState()`'s existing `VesselSchema.safeParse` guard once wired in Plan 18-03; this plan adds no new mutation path. |
| T-18-03 | Denial of Service (UI) | VesselOverlayCard's pointer-events-none/auto split | mitigate | Explicit DOM-assertion test (Task 3) proves the close button/Select/Slider remain clickable despite the ancestor's pointer-events-none, directly closing Pitfall 1's regression class rather than relying on visual review alone. |
| T-18-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan -- `lucide-react` is an existing dependency; zero package.json changes. |
</threat_model>

<verification>
1. `npx vitest run ChartHeaderStrip.test.tsx ChartFooterStrip.test.tsx VesselOverlayCard.test.tsx` -- all pass.
2. `npm run typecheck` -- 0 errors.
3. `grep -rc "from \"../reasoning/VerdictBanner\|from \"../instruments/InstrumentReadouts\|from \"../control-panel/ControlPanel" src/components/sandbox/chart/ChartHeaderStrip.tsx src/components/sandbox/chart/ChartFooterStrip.tsx src/components/sandbox/chart/VesselOverlayCard.tsx` -- all `0` (no cross-imports of soon-to-be-retired files).
</verification>

<success_criteria>
All 3 new components exist, are independently unit-tested, and correctly mine (not import) the logic of the 3 components they will replace. Nothing is wired into ChartPanel/SandboxContainer yet -- that happens in Plan 18-03.
</success_criteria>

<output>
Create `.planning/phases/18-on-chart-vessel-control-overlay/18-02-SUMMARY.md` when done
</output>
