---
phase: 18-on-chart-vessel-control-overlay
plan: 04
type: execute
wave: 4
depends_on: ["18-03"]
files_modified:
  - src/components/sandbox/SandboxContainer.tsx
  - src/components/sandbox/SandboxContainer.test.tsx
  - src/components/sandbox/types.ts
  - src/components/sandbox/reasoning/VerdictBanner.tsx
  - src/components/sandbox/reasoning/VerdictBanner.test.tsx
  - src/components/sandbox/instruments/InstrumentReadouts.tsx
  - src/components/sandbox/instruments/InstrumentReadouts.test.tsx
  - src/components/sandbox/instruments/status-pill.ts
  - src/components/sandbox/instruments/status-pill.test.ts
  - src/components/sandbox/control-panel/ControlPanel.tsx
  - src/components/sandbox/control-panel/ControlPanel.test.tsx
autonomous: true
requirements: [SBOX-06, SBOX-07, SBOX-08]

must_haves:
  truths:
    - "The Sandbox page shows no standalone VerdictBanner card, no standalone InstrumentReadouts card, and no always-visible side ControlPanel column -- only ChartPanel (header strip + chart + footer strip + click-to-open overlay) and ReasoningTrail remain"
    - "Editing a vessel's type/speed via its on-chart overlay still re-derives the classification live through the exact same useSandboxState() callbacks ControlPanel used to call, with no submit step"
    - "No orphaned code remains: VerdictBanner.tsx, InstrumentReadouts.tsx, status-pill.ts, ControlPanel.tsx and their test files are deleted, not merely unreferenced"
  artifacts:
    - path: "src/components/sandbox/SandboxContainer.tsx"
      provides: "Composition with VerdictBanner/InstrumentReadouts/ControlPanel fully removed -- ChartPanel + ReasoningTrail only"
  key_links:
    - from: "SandboxContainer.tsx"
      to: "ChartPanel (header/footer/overlay already wired in Plan 18-03)"
      via: "sole chart-area element, no side column"
      pattern: "grid-cols-\\[minmax\\(0,3fr\\)_minmax\\(0,2fr\\)\\]"
---

<objective>
Complete the phase's replacement: remove `VerdictBanner`/`InstrumentReadouts`/`ControlPanel` from `SandboxContainer.tsx`'s composition, delete the 3 retired components (plus the now-dead `status-pill.ts`) and their test files, remove their now-unused prop-contract interfaces from `types.ts`, and rewrite `SandboxContainer.test.tsx`'s two slider-dependent tests for the new click-to-open-overlay interaction model.

Purpose: `ChartPanel.tsx` (Plan 18-03) already renders everything these 3 components used to provide -- this plan removes the now-duplicate old UI and its dead code, which is the last step that makes ROADMAP Phase 18 success criteria 1-3 literally true ("the separate VerdictBanner card is gone" / "the separate InstrumentReadouts card is gone" / "the side ControlPanel is fully removed").
Output: `SandboxContainer.tsx` composing only `ChartPanel` + `ReasoningTrail`; 4 files deleted (+ their 4 test files); `types.ts` with 3 fewer interfaces; `SandboxContainer.test.tsx` fully green against the new interaction model.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-on-chart-vessel-control-overlay/18-CONTEXT.md
@.planning/phases/18-on-chart-vessel-control-overlay/18-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove VerdictBanner/InstrumentReadouts/ControlPanel from SandboxContainer.tsx's composition</name>
  <files>src/components/sandbox/SandboxContainer.tsx</files>
  <read_first>
    src/components/sandbox/SandboxContainer.tsx
  </read_first>
  <action>
    Remove the 3 imports (`VerdictBanner`, `InstrumentReadouts`, `ControlPanel`) and their 3 JSX usages: the `<VerdictBanner .../>` block above the chart grid, and the entire `<div className="flex flex-col gap-4">...</div>` side-column wrapper containing `<InstrumentReadouts .../>` and `<ControlPanel .../>`. `ChartPanel` (already fully wired to render the header strip, footer strip, and click-to-open overlay per Plan 18-03) becomes the sole content of the `grid` row that previously held `ChartPanel` + the side column -- collapse the grid's 2-column template (`grid-cols-1 min-[900px]:grid-cols-[minmax(0,3fr)_minmax(0,2fr)]`) to a single column (`grid-cols-1`) since there is no longer a second column to size, or replace the `<div className="mt-4 grid ...">` wrapper entirely with a plain `<div className="mt-4">` directly containing `<ChartPanel .../>` -- either is acceptable as long as no empty/leftover grid-column div remains. `ChartPanel`'s own props are unchanged from Plan 18-03 (already receives `isDegenerate`/`onVesselSpeedChange`/`onVesselTypeChange`). Update the header description copy from "Drag a hull to reposition it, grab the bow handle to change heading, and adjust speed &amp; type below." to "Drag a hull to reposition it, grab the bow handle to change heading, and click a vessel to adjust its speed &amp; type." -- the old copy is now factually wrong (there is no "below" section anymore). Update this file's own top-of-file doc comment, which currently says it "wires ChartPanel, ControlPanel, and the 3 split reasoning cards... together" -- correct it to reflect the new composition (ChartPanel + ReasoningTrail).
  </action>
  <acceptance_criteria>
    - `grep -c "VerdictBanner\|InstrumentReadouts\|ControlPanel" src/components/sandbox/SandboxContainer.tsx` outputs `0`
    - `grep -c "adjust its speed" src/components/sandbox/SandboxContainer.tsx` outputs `1`
    - `grep -c "<ChartPanel" src/components/sandbox/SandboxContainer.tsx` outputs `1`
    - `grep -c "<ReasoningTrail" src/components/sandbox/SandboxContainer.tsx` outputs `1`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>SandboxContainer.tsx composes only ChartPanel and ReasoningTrail below the header; no reference to the 3 retired components remains; typecheck passes (will fail vitest until Task 2 deletes the now-orphaned component files' own imports of types.ts's soon-to-be-removed interfaces -- expected, resolved by Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Delete the 3 retired components (+ dead status-pill.ts) and their now-unused type contracts</name>
  <files>src/components/sandbox/reasoning/VerdictBanner.tsx, src/components/sandbox/reasoning/VerdictBanner.test.tsx, src/components/sandbox/instruments/InstrumentReadouts.tsx, src/components/sandbox/instruments/InstrumentReadouts.test.tsx, src/components/sandbox/instruments/status-pill.ts, src/components/sandbox/instruments/status-pill.test.ts, src/components/sandbox/control-panel/ControlPanel.tsx, src/components/sandbox/control-panel/ControlPanel.test.tsx, src/components/sandbox/types.ts</files>
  <read_first>
    src/components/sandbox/types.ts
    src/components/sandbox/instruments/status-pill.ts
  </read_first>
  <action>
    First, confirm zero remaining references before deleting anything: `grep -rln "VerdictBanner\|InstrumentReadouts\b\|ControlPanel\b\|statusPillCopy" src app --include="*.ts*"` should list only the 8 files being deleted in this task plus `types.ts` (which Task 1 already cleared of JSX usage; this task removes its remaining type-only references). Do NOT delete `src/components/sandbox/control-panel/CopyLinkButton.tsx` -- it is an unrelated component that happens to live in the same `control-panel/` directory (consumed by `app/s/[shareId]/page.tsx`), not part of `ControlPanel.tsx`/`VesselFormSection`.
    Delete these 8 files: `src/components/sandbox/reasoning/VerdictBanner.tsx`, `src/components/sandbox/reasoning/VerdictBanner.test.tsx`, `src/components/sandbox/instruments/InstrumentReadouts.tsx`, `src/components/sandbox/instruments/InstrumentReadouts.test.tsx`, `src/components/sandbox/instruments/status-pill.ts` (dead after `InstrumentReadouts.tsx`'s removal -- its only consumer), `src/components/sandbox/instruments/status-pill.test.ts`, `src/components/sandbox/control-panel/ControlPanel.tsx`, `src/components/sandbox/control-panel/ControlPanel.test.tsx`.
    In `types.ts`: remove the now-unused `ControlPanelProps`, `VerdictBannerProps`, and `InstrumentReadoutsProps` interfaces (their only consumers are the files just deleted).
  </action>
  <acceptance_criteria>
    - `test -e src/components/sandbox/reasoning/VerdictBanner.tsx` exits nonzero (file does not exist)
    - `test -e src/components/sandbox/instruments/InstrumentReadouts.tsx` exits nonzero
    - `test -e src/components/sandbox/instruments/status-pill.ts` exits nonzero
    - `test -e src/components/sandbox/control-panel/ControlPanel.tsx` exits nonzero
    - `test -e src/components/sandbox/control-panel/CopyLinkButton.tsx` exits `0` (unrelated file, NOT deleted)
    - `grep -c "export interface ControlPanelProps\|export interface VerdictBannerProps\|export interface InstrumentReadoutsProps" src/components/sandbox/types.ts` outputs `0`
    - `npm run typecheck` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm run build</automated>
  </verify>
  <done>All 4 retired source files + their 4 test files are deleted; CopyLinkButton.tsx is untouched; types.ts has 3 fewer interfaces; typecheck and production build both succeed with zero orphaned references.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite SandboxContainer.test.tsx's slider-dependent tests for the click-to-open-overlay model</name>
  <files>src/components/sandbox/SandboxContainer.test.tsx</files>
  <read_first>
    src/components/sandbox/SandboxContainer.test.tsx
    src/components/sandbox/chart/ChartPanel.test.tsx (as updated in Plan 18-03 -- overlay open/close dispatch pattern to mirror)
  </read_first>
  <action>
    Add a new helper function `openOverlay(container: HTMLElement, vessel: VesselLabel): void` mirroring the existing `dragHullTo` helper's shape: query `[data-testid="hull-hit-${vessel}"]`, assert it is not null, then `act(() => { hitRect?.dispatchEvent(new PointerEvent("pointerdown", { pointerId: 1, clientX: 0, clientY: 0, bubbles: true })); });` (opening the overlay per D-04 needs only the `pointerdown`, matching `ChartPanel.test.tsx`'s own new regression tests -- no `pointermove` needed since this is a click-to-open action, not a drag).
    Update exactly 2 existing tests (leave all others unchanged -- they do not touch any slider):
    (a) `"re-derives the classification live when a ControlPanel speed slider changes, with no submit step"` -- rename to reference the overlay instead of ControlPanel; before the existing 15x `{ArrowRight}` sequence, call `openOverlay(container, "vesselA")` (requires switching this test to destructure `container` from `render(...)`, matching the pattern other tests in this file already use) and replace `screen.getAllByRole("slider")[0]` with `screen.getByRole("slider")` (exactly one slider exists now, since only one vessel's overlay can be open at a time).
    (b) `"threads previousEncounterTypeRef through a fresh-overtaking-then-sticky update sequence (Rule 13(d) hysteresis wiring)"` -- before the first `pressArrow` sequence, call `openOverlay(container, "vesselA")` and replace `screen.getAllByRole("slider")[0]` with `screen.getByRole("slider")`; before the second `pressArrow` sequence (currently `screen.getAllByRole("slider")[1]`, vesselB), call `openOverlay(container, "vesselB")` (this switches the overlay from A to B per D-01, since a different vessel is pressed while A's is open) and replace `screen.getAllByRole("slider")[1]` with `screen.getByRole("slider")`.
    Update this file's own top-of-file doc comment (currently references "ChartPanel + ControlPanel + the 3 split reasoning cards") to describe the new composition and the click-to-open-overlay interaction the 2 updated tests now exercise.
  </action>
  <acceptance_criteria>
    - `npx vitest run SandboxContainer.test.tsx` passes with 0 failures, all 13 existing test cases still present and green
    - `grep -c "function openOverlay" src/components/sandbox/SandboxContainer.test.tsx` outputs `1`
    - `grep -c "getAllByRole(\"slider\")" src/components/sandbox/SandboxContainer.test.tsx` outputs `0` (both call sites now use singular `getByRole`)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run SandboxContainer.test.tsx && npm run typecheck</automated>
  </verify>
  <done>SandboxContainer.test.tsx's full suite passes against the new click-to-open-overlay composition, with only the 2 slider-dependent tests altered and all other 11 tests untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new this plan | Deletion + composition cleanup of already-reviewed presentational code; no new input source, no new mutation path -- `onVesselSpeedChange`/`onVesselTypeChange` continue routing through `useSandboxState()`'s existing `VesselSchema.safeParse` guard, now called from `VesselOverlayCard` instead of `ControlPanel`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-06 | Tampering | Deletion of ControlPanel.tsx's mutation-triggering fields | accept | `VesselOverlayCard` (Plan 18-02) already threads the identical `onVesselSpeedChange`/`onVesselTypeChange` callbacks through the identical `useSandboxState()` choke point -- this plan only removes the now-duplicate old UI surface, not any validation. |
| T-18-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan -- zero package.json changes. |
</threat_model>

<verification>
1. `npm run typecheck` -- 0 errors.
2. `npm run build` -- succeeds.
3. `npx vitest run` -- full suite green, no leftover references to the 4 deleted files anywhere in the test run.
4. `grep -rln "VerdictBanner\|InstrumentReadouts\b\|ControlPanel\b" src app` -- empty (zero hits).
</verification>

<success_criteria>
ROADMAP Phase 18 success criteria 1-3 are literally true in the codebase: the separate VerdictBanner card, the separate InstrumentReadouts card, and the side ControlPanel are all gone, replaced by ChartPanel's header strip / footer strip / click-to-open overlay (Plans 18-01 through 18-03). Only success criterion 4 (human-verified hit-testing regression re-check) remains, addressed in Plan 18-05.
</success_criteria>

<output>
Create `.planning/phases/18-on-chart-vessel-control-overlay/18-04-SUMMARY.md` when done
</output>
