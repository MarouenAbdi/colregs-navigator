---
phase: 18-on-chart-vessel-control-overlay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sandbox/types.ts
  - src/components/sandbox/chart/chart-header-risk.ts
  - src/components/sandbox/chart/chart-header-risk.test.ts
  - src/components/sandbox/chart/footer-action-copy.ts
  - src/components/sandbox/chart/footer-action-copy.test.ts
  - src/components/sandbox/chart/vessel-overlay-position.ts
  - src/components/sandbox/chart/vessel-overlay-position.test.ts
autonomous: true
requirements: [SBOX-06, SBOX-07, SBOX-08]

must_haves:
  truths:
    - "deriveChartHeaderRisk() derives exactly 4 tiers (none/ok/watch/high) purely from cpaNm/tcpaMinutes, matching D-02's verbatim adoption of the design's thresholds (high: cpa<0.3, watch: cpa<1.0, ok: else, none: opening/no-closure)"
    - "ROLE_ACTION_TEXT supplies the 3 exact D-03 required-action strings keyed by this codebase's existing VesselRole type, not a new role vocabulary"
    - "deriveOverlayAnchor() computes a quadrant-opposite anchor (right/below booleans + pixel offsets) purely from a vessel's screen position and containerSize, with zero DOM/React dependency, so it is unit-testable independent of pointer-event wiring (per PITFALLS.md Pitfall 1's explicit recommendation)"
  artifacts:
    - path: "src/components/sandbox/chart/chart-header-risk.ts"
      provides: "4-tier risk-pill derivation (D-02), deliberately CPA-distance-driven, not classification.riskOfCollision-driven"
      contains: "export function deriveChartHeaderRisk"
    - path: "src/components/sandbox/chart/footer-action-copy.ts"
      provides: "per-role required-action copy (D-03)"
      contains: "export const ROLE_ACTION_TEXT"
    - path: "src/components/sandbox/chart/vessel-overlay-position.ts"
      provides: "pure quadrant-anchor derivation consumed by ChartPanel.tsx in Plan 18-03 to position the floating overlay card"
      contains: "export function deriveOverlayAnchor"
    - path: "src/components/sandbox/types.ts"
      provides: "3 new prop-contract interfaces backing Plan 18-02's header/footer/overlay components -- ChartPanelProps itself is extended later, in Plan 18-03, alongside its actual consumption"
      contains: "export interface VesselOverlayCardProps"
  key_links:
    - from: "chart-header-risk.ts's deriveChartHeaderRisk(cpaNm, tcpaMinutes)"
      to: "instruments/instrument-readouts.ts's deriveInstrumentReadouts() cpaNm/tcpaMinutes output"
      via: "same CPA/TCPA values, never recomputed independently (D-02's explicit instruction)"
      pattern: "cpaNm.*tcpaMinutes"
---

<objective>
Lay the interface-first groundwork for Phase 18: three new prop-contract interfaces in `types.ts` (consumed by Plan 18-02's components) and three pure, framework-free derivation modules -- the 4-tier risk-pill logic (D-02), the per-role required-action copy (D-03), and the floating-overlay quadrant-anchor math (Pitfall 1's explicit "extract a small pure function, unit-test it independent of pointer-event wiring" recommendation). No React components and no wiring into `ChartPanel`/`SandboxContainer` yet -- this plan only builds and unit-tests the contracts and math later plans consume, so nothing downstream needs to guess at shapes or re-derive values.

Purpose: Front-loads the two decisions this phase most needs to get right on paper before any JSX exists -- D-02's knowing divergence from `status-pill.ts`'s prior CPA-heuristic rejection, and Pitfall 1's "positioning math must be independently testable" mandate (this codebase's 3rd occurrence of the painted-element-hit-testing regression class, per Phase 4/Phase 8 precedent).
Output: `chart-header-risk.ts`, `footer-action-copy.ts`, `vessel-overlay-position.ts` (each with a passing unit test file), and `types.ts` extended with `ChartHeaderStripProps`/`ChartFooterStripProps`/`VesselOverlayCardProps`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-on-chart-vessel-control-overlay/18-CONTEXT.md
@.planning/phases/18-on-chart-vessel-control-overlay/18-DESIGN-SNAPSHOT.md
@.planning/phases/18-on-chart-vessel-control-overlay/18-PATTERNS.md
@.planning/research/PITFALLS.md
</context>

<interfaces>
<!-- Exact shapes Plan 18-02's components (Wave 2) will import. Do not deviate
     from these names -- Plan 18-02 is written against them verbatim. -->

```typescript
// src/components/sandbox/chart/chart-header-risk.ts
export type ChartHeaderRiskTier = "none" | "ok" | "watch" | "high";

export function deriveChartHeaderRisk(
  cpaNm: number | null,
  tcpaMinutes: number | null,
): { tier: ChartHeaderRiskTier; text: string };

export const CHART_HEADER_RISK_TONE_CLASSNAME: Record<ChartHeaderRiskTier, string>;
export const CHART_HEADER_RISK_DOT_CLASSNAME: Record<ChartHeaderRiskTier, string>;

// src/components/sandbox/chart/footer-action-copy.ts
export const ROLE_ACTION_TEXT: Record<VesselRole, string>; // VesselRole from "../vessel-role.js"

// src/components/sandbox/chart/vessel-overlay-position.ts
export interface OverlayAnchor {
  right: boolean;  // true: vessel is in the left half -> anchor the card to its right
  below: boolean;  // true: vessel is in the top half -> anchor the card below it
  offsetXPx: number; // distance from the anchored horizontal edge (left or right) to the card
  offsetYPx: number; // distance from the anchored vertical edge (top or bottom) to the card
}

export function deriveOverlayAnchor(
  screen: { screenX: number; screenY: number },
  containerSize: ContainerSize, // from "../../../domain/geometry/screen-convert/screen-convert.js"
): OverlayAnchor;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add ChartHeaderStripProps/ChartFooterStripProps/VesselOverlayCardProps to types.ts</name>
  <files>src/components/sandbox/types.ts</files>
  <read_first>
    src/components/sandbox/types.ts
    .planning/phases/18-on-chart-vessel-control-overlay/18-PATTERNS.md (section "src/components/sandbox/types.ts (modified — extend ChartPanelProps)")
  </read_first>
  <action>
    Add three new exported interfaces to `types.ts`, following the file's existing one-interface-per-component convention (mirrors `ControlPanelProps`/`VerdictBannerProps`/`InstrumentReadoutsProps`'s existing shape). `ChartHeaderStripProps` and `ChartFooterStripProps` are identical in shape: `vesselA: Vessel; vesselB: Vessel; classification: ClassificationResult; isDegenerate: boolean` (the header needs `vesselA`/`vesselB` to call `deriveInstrumentReadouts()` for the risk pill's `cpaNm`/`tcpaMinutes`, per D-02; the footer needs them for its own RANGE/BEARING/CPA/TCPA tiles). `VesselOverlayCardProps` is `label: VesselLabel; letter: "A" | "B"; vessel: Vessel; classification: ClassificationResult; onVesselSpeedChange: VesselUpdateHandlers["onVesselSpeedChange"]; onVesselTypeChange: VesselUpdateHandlers["onVesselTypeChange"]; onClose: () => void` -- deliberately excludes any positioning field (anchor/offset), since positioning is `ChartPanel`'s concern (Plan 18-03), not the card's own content concern, per this codebase's "split computation from presentation" convention.
    Do NOT touch `ChartPanelProps`, `ControlPanelProps`, `VerdictBannerProps`, or `InstrumentReadoutsProps` in this task -- `ChartPanelProps`'s extension (adding `isDegenerate`/`onVesselSpeedChange`/`onVesselTypeChange`) is deferred to Plan 18-03, where it happens in the same task that updates `ChartPanel.tsx`'s own destructuring and `SandboxContainer.tsx`'s call site together (adding required fields to `ChartPanelProps` now, with no consumer supplying them yet, would break `npm run typecheck` immediately). The 3 soon-to-be-retired interfaces (`ControlPanelProps`/`VerdictBannerProps`/`InstrumentReadoutsProps`) stay untouched until Plan 18-04 deletes them alongside their consuming components.
    Add a one-line WHY comment above the 3 new interfaces noting they back Phase 18's on-chart header/footer/overlay components (SBOX-06/07/08).
  </action>
  <acceptance_criteria>
    - `grep -c "export interface ChartHeaderStripProps" src/components/sandbox/types.ts` outputs `1`
    - `grep -c "export interface ChartFooterStripProps" src/components/sandbox/types.ts` outputs `1`
    - `grep -c "export interface VesselOverlayCardProps" src/components/sandbox/types.ts` outputs `1`
    - `grep -c "export interface ControlPanelProps" src/components/sandbox/types.ts` outputs `1` (unchanged, not yet removed)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>types.ts exports the 3 new interfaces exactly per the &lt;interfaces&gt; contract above; no existing interface or consumer is touched; typecheck passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: chart-header-risk.ts -- 4-tier risk-pill derivation (D-02)</name>
  <files>src/components/sandbox/chart/chart-header-risk.ts, src/components/sandbox/chart/chart-header-risk.test.ts</files>
  <read_first>
    src/components/sandbox/instruments/status-pill.ts (analog shape + the rationale this module explicitly diverges from)
    src/components/sandbox/instruments/status-pill.test.ts (test-shape analog)
    src/components/sandbox/instruments/instrument-readouts.ts (cpaNm/tcpaMinutes source this module's inputs come from)
    .planning/phases/18-on-chart-vessel-control-overlay/18-DESIGN-SNAPSHOT.md (section "Risk pill 4-tier derivation")
    .planning/phases/18-on-chart-vessel-control-overlay/18-CONTEXT.md (D-02)
  </read_first>
  <behavior>
    - `deriveChartHeaderRisk(1.81, 4.2)` -&gt; `{ tier: "ok", text: "Passing clear — CPA 1.81 NM on present courses." }`
    - `deriveChartHeaderRisk(0.87, 3.5)` -&gt; tier `"watch"` (0.3 &lt;= 0.87 &lt; 1.0), text mentions "Close-quarters developing" and "0.87 NM"
    - `deriveChartHeaderRisk(0.2, 1.0)` -&gt; tier `"high"` (&lt; 0.3), text mentions "Risk of collision" and "0.20 NM"
    - `deriveChartHeaderRisk(1.81, -2.5)` -&gt; tier `"none"` (tcpaMinutes &lt;= 0, closest approach already passed), text mentions "opening"
    - `deriveChartHeaderRisk(null, null)` -&gt; tier `"none"` (no-closure/parallel courses -- no defined CPA at all), text does not throw or read `.toFixed` on null
    - Boundary: `deriveChartHeaderRisk(0.3, 5)` -&gt; tier `"watch"` (0.3 is NOT `&lt; 0.3`, falls to watch's `&lt; 1.0` branch); `deriveChartHeaderRisk(1.0, 5)` -&gt; tier `"ok"` (1.0 is NOT `&lt; 1.0`)
    - `CHART_HEADER_RISK_TONE_CLASSNAME`/`CHART_HEADER_RISK_DOT_CLASSNAME` each have exactly the 4 keys `none`/`ok`/`watch`/`high`, and reuse this codebase's existing tokens, not new hex literals: `watch` reuses the `doubt` token (`text-doubt`/`bg-doubt/…`/`border-doubt/…`) since it is the same amber `#F59E0B` the design's `watch` tier and this app's existing `--doubt` token both already use; `ok`/`high` reuse the same `green-400`/`red-400` utility classes `InstrumentReadouts.tsx`'s retired `STATUS_PILL_TONE_CLASSNAME` used; `none` reuses `muted-foreground`, matching that same file's retired `"opening"` tone.
  </behavior>
  <action>
    Create `deriveChartHeaderRisk(cpaNm: number | null, tcpaMinutes: number | null): { tier: ChartHeaderRiskTier; text: string }` with branch order: (1) `cpaNm === null` -&gt; tier `"none"`, text along the lines of "No defined closest point of approach — courses do not converge."; (2) `tcpaMinutes !== null && tcpaMinutes <= 0` -&gt; tier `"none"`, text "Vessels are opening — CPA already passed. No risk of collision developing on present courses." (verbatim from the design snapshot); (3) `cpaNm < 0.3` -&gt; tier `"high"`, text `` `Risk of collision exists — CPA ${cpaNm.toFixed(2)} NM. A substantial, early, readily-apparent action is required.` `` (verbatim); (4) `cpaNm < 1.0` -&gt; tier `"watch"`, text `` `Close-quarters developing — CPA ${cpaNm.toFixed(2)} NM. Monitor the compass bearing for appreciable change.` `` (verbatim); (5) else -&gt; tier `"ok"`, text `` `Passing clear — CPA ${cpaNm.toFixed(2)} NM on present courses.` `` (verbatim, matches the retired `statusPillCopy`'s own "clear" wording).
    Export `CHART_HEADER_RISK_TONE_CLASSNAME: Record<ChartHeaderRiskTier, string>` mapping `none` -&gt; `"text-muted-foreground bg-muted-foreground/10 border-muted-foreground/35"`, `ok` -&gt; `"text-green-400 bg-green-400/10 border-green-400/35"`, `watch` -&gt; `"text-doubt bg-doubt/10 border-doubt/35"`, `high` -&gt; `"text-red-400 bg-red-400/10 border-red-400/35"`. Export `CHART_HEADER_RISK_DOT_CLASSNAME: Record<ChartHeaderRiskTier, string>` with the bg-only equivalents (`bg-muted-foreground`/`bg-green-400`/`bg-doubt`/`bg-red-400`), mirroring `InstrumentReadouts.tsx`'s retired `STATUS_PILL_DOT_CLASSNAME` shape.
    Add a doc comment at the top explaining this is a **deliberate, explicit divergence** from `status-pill.ts`'s own documented rationale (per D-02) -- CPA-distance thresholds here are correct on purpose for this one header risk-pill, never fed back into `classification.riskOfCollision` or any `src/domain/` logic.
  </action>
  <acceptance_criteria>
    - `npx vitest run chart-header-risk.test.ts` passes with 0 failures, covering every case in &lt;behavior&gt; above including both boundary cases
    - `grep -c "export function deriveChartHeaderRisk" src/components/sandbox/chart/chart-header-risk.ts` outputs `1`
    - `grep -c "riskOfCollision" src/components/sandbox/chart/chart-header-risk.ts` outputs `0` (this module never reads the domain risk signal, by design)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run chart-header-risk.test.ts && npm run typecheck</automated>
  </verify>
  <done>chart-header-risk.ts exports deriveChartHeaderRisk + both classname maps exactly per &lt;interfaces&gt;; all behavior cases pass as real tests, not just typechecked shapes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: footer-action-copy.ts (D-03) + vessel-overlay-position.ts (Pitfall 1's testable-anchor mandate)</name>
  <files>src/components/sandbox/chart/footer-action-copy.ts, src/components/sandbox/chart/footer-action-copy.test.ts, src/components/sandbox/chart/vessel-overlay-position.ts, src/components/sandbox/chart/vessel-overlay-position.test.ts</files>
  <read_first>
    src/components/sandbox/vessel-role.ts (Record&lt;VesselRole, string&gt; map pattern to mirror)
    src/components/sandbox/chart/chart-panel-derivation.ts (pure-derivation doc-comment/file shape to mirror for vessel-overlay-position.ts)
    src/domain/geometry/screen-convert/screen-convert.ts (ContainerSize type)
    .planning/phases/18-on-chart-vessel-control-overlay/18-DESIGN-SNAPSHOT.md (sections "Footer instrument bar" per-vessel action copy, and "Positioning" posCard)
    .planning/research/PITFALLS.md (Pitfall 1, "Extract the overlay's open/anchor logic into a small pure function... unit-tested independent of pointer-event wiring")
  </read_first>
  <behavior>
    footer-action-copy.ts:
    - `ROLE_ACTION_TEXT["give-way"]` === `"Alter course early & substantially — pass well clear astern."`
    - `ROLE_ACTION_TEXT["stand-on"]` === `"Hold course & speed; stand ready to act if she does not."`
    - `ROLE_ACTION_TEXT["mutual"]` === `"No privilege — both take early, decisive avoiding action."`

    vessel-overlay-position.ts:
    - vessel in the left half of a 400x400 container (e.g. `screenX: 100, screenY: 100`, top-left quadrant) -&gt; `{ right: true, below: true }`, with `offsetXPx` derived from `screenX + 26` and `offsetYPx` derived from `screenY - 6` (never negative -- clamp at 0)
    - vessel in the right half, bottom half (e.g. `screenX: 300, screenY: 300`) -&gt; `{ right: false, below: false }`, with `offsetXPx` derived from `containerSize.width - screenX + 26` and `offsetYPx` derived from `containerSize.height - screenY - 6`
    - exact halfway point (`screenX: containerSize.width / 2`) resolves deterministically to one side (document which, via the `<` comparison direction) -- no ambiguous/NaN output
  </behavior>
  <action>
    `footer-action-copy.ts`: export `ROLE_ACTION_TEXT: Record<VesselRole, string>` (import `type VesselRole` from `"../vessel-role.js"`) with the 3 exact strings above, verbatim per D-03. No `PLACEHOLDER` export here -- the consuming `ChartFooterStrip.tsx` (Plan 18-02) declares its own local `"—"` placeholder constant, matching `InstrumentReadouts.tsx`'s existing per-file convention rather than centralizing it.
    `vessel-overlay-position.ts`: export `interface OverlayAnchor { right: boolean; below: boolean; offsetXPx: number; offsetYPx: number }` and `function deriveOverlayAnchor(screen: { screenX: number; screenY: number }, containerSize: ContainerSize): OverlayAnchor`. Declare two named constants, `HORIZONTAL_GAP_PX = 26` and `VERTICAL_NUDGE_PX = 6` (matching the design snapshot's own `+26`/`-6` literals, ported as behavior not raw CSS). `right = screen.screenX < containerSize.width / 2`; `below = screen.screenY < containerSize.height / 2`. `offsetXPx = right ? screen.screenX + HORIZONTAL_GAP_PX : containerSize.width - screen.screenX + HORIZONTAL_GAP_PX`. `offsetYPx = Math.max(below ? screen.screenY - VERTICAL_NUDGE_PX : containerSize.height - screen.screenY - VERTICAL_NUDGE_PX, 0)`. Do NOT port the design's own `v.x / 960 * 100` percentage normalization (that assumes the design's fixed 960x640 canvas) -- this function takes real pixel-space `screen`/`containerSize` values, matching `chart-panel-derivation.ts`'s existing `chartToScreen()`-based precedent. Add a doc comment mirroring `chart-panel-derivation.ts`'s own ("pure, zero-React derivation, recomputed fresh on every call") and cite Pitfall 1 as the reason this math is a standalone, independently-tested function rather than inlined in `ChartPanel.tsx`.
  </action>
  <acceptance_criteria>
    - `npx vitest run footer-action-copy.test.ts vessel-overlay-position.test.ts` passes with 0 failures, covering every case in &lt;behavior&gt; above
    - `grep -c "export const ROLE_ACTION_TEXT" src/components/sandbox/chart/footer-action-copy.ts` outputs `1`
    - `grep -c "export function deriveOverlayAnchor" src/components/sandbox/chart/vessel-overlay-position.ts` outputs `1`
    - `grep -c "React\|useState\|useEffect" src/components/sandbox/chart/vessel-overlay-position.ts` outputs `0` (zero React dependency, per Pitfall 1's mandate)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run footer-action-copy.test.ts vessel-overlay-position.test.ts && npm run typecheck</automated>
  </verify>
  <done>Both modules exist with the exact exports in &lt;interfaces&gt;, both fully unit-tested with zero DOM/React dependency, ready for Plan 18-02's components to import unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new this plan | Pure derivation functions over already-classified, already-validated in-memory vessel/classification data. No new input source, no persistence, no network boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | chart-header-risk.ts's 4-tier divergence from classification.riskOfCollision | accept | Scoped explicitly to this one UI element per D-02; never feeds back into the give-way/stand-on verdict or any src/domain/ logic -- documented in the module's own header comment so the divergence can't be silently "fixed" later by someone unaware it's intentional. |
| T-18-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan -- zero package.json changes; Package Legitimacy Gate protocol not applicable. |
</threat_model>

<verification>
1. `npx vitest run chart-header-risk.test.ts footer-action-copy.test.ts vessel-overlay-position.test.ts` -- all pass.
2. `npm run typecheck` -- 0 errors.
3. `grep -c "export interface VesselOverlayCardProps" src/components/sandbox/types.ts` -- outputs `1`.
</verification>

<success_criteria>
This plan delivers the pure-logic groundwork (D-02's 4-tier risk derivation, D-03's action copy, Pitfall 1's independently-tested anchor math) and the 3 new type contracts Plan 18-02's components are written against verbatim. No user-observable behavior changes yet -- ChartPanel/SandboxContainer are untouched until Plans 18-03/18-04.
</success_criteria>

<output>
Create `.planning/phases/18-on-chart-vessel-control-overlay/18-01-SUMMARY.md` when done
</output>
