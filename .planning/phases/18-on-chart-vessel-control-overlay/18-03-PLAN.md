---
phase: 18-on-chart-vessel-control-overlay
plan: 03
type: execute
wave: 3
depends_on: ["18-02"]
files_modified:
  - src/components/sandbox/types.ts
  - src/components/sandbox/hooks/useHullDrag.ts
  - src/components/sandbox/hooks/useHullDrag.test.ts
  - src/components/sandbox/hooks/useRotateHandleDrag.ts
  - src/components/sandbox/hooks/useRotateHandleDrag.test.ts
  - src/components/sandbox/chart/ChartPanel.tsx
  - src/components/sandbox/chart/ChartPanel.test.tsx
  - src/components/sandbox/SandboxContainer.tsx
autonomous: true
requirements: [SBOX-06, SBOX-07, SBOX-08]

must_haves:
  truths:
    - "Pressing a vessel's hull or rotate-handle opens that vessel's floating overlay; pressing the already-open vessel's own hull/rotate-handle again closes it (D-01 toggle); pressing the OTHER vessel switches the overlay to it without closing anything; pressing bare chart background (the svg itself, not a vessel) closes an open overlay"
    - "ChartPanel visually composes ChartHeaderStrip above and ChartFooterStrip below the same chart surface as one merged unit, with the floating VesselOverlayCard positioned via vessel-overlay-position.ts's deriveOverlayAnchor()"
    - "Opening vessel A's overlay never blocks a drag/rotate pointerdown+pointermove gesture aimed at vessel B's hull or rotate-handle, even when they are close enough on-chart for the overlay card to visually overlap vessel B (Pitfall 1 regression, this codebase's 3rd occurrence of this hit-testing class)"
  artifacts:
    - path: "src/components/sandbox/chart/ChartPanel.tsx"
      provides: "selectedVessel state, selectVessel() toggle handler, svg-background-click close handler, and full header/chart/overlay/footer composition"
      contains: "function selectVessel"
    - path: "src/components/sandbox/hooks/useHullDrag.ts"
      provides: "onSelect callback fired on pointerdown, with stopPropagation preventing the svg's empty-click-close handler from clobbering a vessel-switch"
      contains: "onSelect"
    - path: "src/components/sandbox/hooks/useRotateHandleDrag.ts"
      provides: "same onSelect wiring as useHullDrag.ts"
      contains: "onSelect"
  key_links:
    - from: "VesselGroup's hull/rotate-handle onPointerDown (via useHullDrag/useRotateHandleDrag)"
      to: "ChartPanel's selectVessel()"
      via: "onSelect(vessel) callback param, called in the same breath as setPointerCapture (D-04)"
      pattern: "onSelect\\("
    - from: "ChartPanel's <svg> onPointerDown"
      to: "setSelectedVessel(null)"
      via: "empty-chart-space close path (D-01), only reached when stopPropagation did NOT already fire from a vessel press"
      pattern: "setSelectedVessel\\(null\\)"
---

<objective>
Wire the selection state machine into `ChartPanel.tsx`: add `onSelect` to `useHullDrag`/`useRotateHandleDrag` (D-04, opens the overlay on the same `pointerdown` that starts a drag), add `ChartPanel`'s own `selectedVessel` state with a toggle handler (D-01's "re-click closes" requirement layered on top of the design source's own non-toggling behavior), close-on-empty-chart-click, and compose `ChartHeaderStrip`/`ChartFooterStrip`/`VesselOverlayCard` (Plan 18-02) around the existing chart surface. This is the highest-risk plan in the phase -- it directly touches the pointer-event wiring this codebase has already regressed on twice (Phase 4, Phase 8).

Purpose: Delivers the actual interactive behavior locked by D-01/D-04 and ROADMAP success criterion 3, while proactively closing Pitfall 1's exact failure mode (a new painted overlay silently swallowing a drag gesture aimed at the other vessel) via automated regression tests, not just manual review.
Output: `ChartPanel.tsx` fully wired and composed; `useHullDrag.ts`/`useRotateHandleDrag.ts` extended with `onSelect`; `ChartPanelProps` extended in `types.ts`; `SandboxContainer.tsx`'s `<ChartPanel>` call site updated with the 2 new required callback props (its `VerdictBanner`/`InstrumentReadouts`/`ControlPanel` usage is untouched -- that removal is Plan 18-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-on-chart-vessel-control-overlay/18-CONTEXT.md
@.planning/phases/18-on-chart-vessel-control-overlay/18-DESIGN-SNAPSHOT.md
@.planning/phases/18-on-chart-vessel-control-overlay/18-PATTERNS.md
@.planning/research/PITFALLS.md
</context>

<interfaces>
<!-- Plan 18-02's already-shipped components, consumed here verbatim. -->

```typescript
// from "./ChartHeaderStrip.js" / "./ChartFooterStrip.js"
function ChartHeaderStrip(props: ChartHeaderStripProps): JSX.Element;
function ChartFooterStrip(props: ChartFooterStripProps): JSX.Element;
// from "./VesselOverlayCard.js"
function VesselOverlayCard(props: VesselOverlayCardProps): JSX.Element;
// from "./vessel-overlay-position.js"
function deriveOverlayAnchor(screen: { screenX: number; screenY: number }, containerSize: ContainerSize): OverlayAnchor;
```

Target `useHullDrag`/`useRotateHandleDrag` signatures after this plan (new final parameter, both hooks):

```typescript
export function useHullDrag(
  vessel: VesselLabel,
  onVesselPositionChange: ChartPanelProps["onVesselPositionChange"],
  containerSize: ContainerSize | null,
  viewBox: ChartViewBox,
  onSelect: (vessel: VesselLabel) => void, // NEW
): DragHandlers;

export function useRotateHandleDrag(
  vessel: VesselLabel,
  vesselPosition: Position,
  onVesselHeadingChange: ChartPanelProps["onVesselHeadingChange"],
  containerSize: ContainerSize | null,
  viewBox: ChartViewBox,
  onSelect: (vessel: VesselLabel) => void, // NEW
): DragHandlers;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extend ChartPanelProps, add onSelect to both drag hooks, and wire ChartPanel.tsx's full composition</name>
  <files>src/components/sandbox/types.ts, src/components/sandbox/hooks/useHullDrag.ts, src/components/sandbox/hooks/useRotateHandleDrag.ts, src/components/sandbox/chart/ChartPanel.tsx, src/components/sandbox/SandboxContainer.tsx</files>
  <read_first>
    src/components/sandbox/types.ts
    src/components/sandbox/chart/ChartPanel.tsx
    src/components/sandbox/hooks/useHullDrag.ts
    src/components/sandbox/hooks/useRotateHandleDrag.ts
    src/components/sandbox/SandboxContainer.tsx
    src/components/sandbox/chart/vessel-overlay-position.ts (Plan 18-01 output)
    .planning/phases/18-on-chart-vessel-control-overlay/18-CONTEXT.md (D-01, D-04)
    .planning/research/PITFALLS.md (Pitfall 1)
  </read_first>
  <action>
    In `types.ts`: add `isDegenerate: boolean`, `onVesselSpeedChange: VesselUpdateHandlers["onVesselSpeedChange"]`, `onVesselTypeChange: VesselUpdateHandlers["onVesselTypeChange"]` to the existing `ChartPanelProps` interface (do not remove any existing field).
    In `useHullDrag.ts`: add a new final parameter `onSelect: (vessel: VesselLabel) => void`. Inside the existing `onPointerDown`, add `event.stopPropagation();` as the FIRST line (before `setPointerCapture`), then call `onSelect(vessel);` right after `setPointerCapture` -- in the same breath as starting the drag, per D-04. Add a WHY comment: the `stopPropagation()` is not (only) about sibling overlap like `useRotateHandleDrag`'s existing one -- it specifically prevents this pointerdown from bubbling up to `ChartPanel`'s new `<svg>`-level "close on empty chart click" handler, which would otherwise also fire in the same event dispatch and call `setSelectedVessel(null)` AFTER this hook's own `onSelect` call already queued a different value, silently overwriting a vessel-to-vessel switch back to "closed" (trace this exact bug in the comment: pressing vessel B while vessel A's overlay is open must switch to B, not close everything).
    In `useRotateHandleDrag.ts`: add the identical new final parameter `onSelect: (vessel: VesselLabel) => void` and call `onSelect(vessel);` right after the existing `setPointerCapture` call (the existing `event.stopPropagation()` already covers the same svg-bubbling concern, plus its original hull-sibling-overlap reason -- extend its comment to note both reasons now apply).
    In `ChartPanel.tsx`: add `import { useState } from "react";`. Destructure the 3 new props (`isDegenerate`, `onVesselSpeedChange`, `onVesselTypeChange`) from `ChartPanelProps` alongside the existing 5. Add `const [selectedVessel, setSelectedVessel] = useState<VesselLabel | null>(null);` and a `function selectVessel(vessel: VesselLabel): void { setSelectedVessel((current) => (current === vessel ? null : vessel)); }` (D-01's toggle). Pass `selectVessel` as the new final argument to all 4 existing `useHullDrag`/`useRotateHandleDrag` calls. Add `onPointerDown={() => { if (selectedVessel) setSelectedVessel(null); }}` to the existing `<svg>` element (D-01's "click empty chart space closes" path). Import `ChartHeaderStrip`, `ChartFooterStrip`, `VesselOverlayCard` from `./ChartHeaderStrip.js`/`./ChartFooterStrip.js`/`./VesselOverlayCard.js`, and `deriveOverlayAnchor` from `./vessel-overlay-position.js`. Restructure the component's return so the existing zero-size early-return div and the full-render branch both nest inside one outer wrapper: `<div className="flex flex-col overflow-hidden rounded-sm border border-border bg-chart-surface"><ChartHeaderStrip vesselA={vesselA} vesselB={vesselB} classification={classification} isDegenerate={isDegenerate} /><div ref={containerRef} className="relative aspect-square w-full">{/* existing zero-size-guard + svg + legend, unchanged except the svg's border/rounded classes move to the new outer wrapper */}</div><ChartFooterStrip vesselA={vesselA} vesselB={vesselB} classification={classification} isDegenerate={isDegenerate} /></div>` -- so the header/footer strips render even before the first `ResizeObserver` callback fires (matching the design's "always-framed" chart card), while the svg/legend/overlay-card still only render once `containerSize` is non-zero. Inside the sized branch, after the existing "1 NM" legend div, conditionally render the overlay: when `selectedVessel` is non-null, compute `const overlayScreen = selectedVessel === "vesselA" ? screenA : screenB;` and `const anchor = deriveOverlayAnchor(overlayScreen, containerSize);`, then render an absolutely-positioned wrapper div using Tailwind arbitrary-value classes referencing 2 CSS custom properties (per this codebase's "expose numeric values as a CSS custom property, don't compose the whole rule in JS" convention) -- e.g. `className={`absolute w-56 max-w-[46%] ${anchor.right ? "left-[var(--ov-x)]" : "right-[var(--ov-x)]"} ${anchor.below ? "top-[var(--ov-y)]" : "bottom-[var(--ov-y)]"}`}` with `style={{ "--ov-x": `${anchor.offsetXPx}px`, "--ov-y": `${anchor.offsetYPx}px` } as CSSProperties}`, wrapping a `<VesselOverlayCard label={selectedVessel} letter={selectedVessel === "vesselA" ? "A" : "B"} vessel={selectedVessel === "vesselA" ? vesselA : vesselB} classification={classification} onVesselSpeedChange={onVesselSpeedChange} onVesselTypeChange={onVesselTypeChange} onClose={() => setSelectedVessel(null)} />`.
    In `SandboxContainer.tsx`: add `isDegenerate={sandboxState.isDegenerate}`, `onVesselSpeedChange={sandboxState.onVesselSpeedChange}`, `onVesselTypeChange={sandboxState.onVesselTypeChange}` to the existing `<ChartPanel .../>` call (these values already exist on `sandboxState`, currently only threaded to `ControlPanel`). Do NOT remove or modify the existing `VerdictBanner`/`InstrumentReadouts`/`ControlPanel` usage in this file -- that removal is Plan 18-04's job; both the old cards and the new ChartPanel-hosted strips/overlay will render simultaneously until then, which is expected mid-phase, not a bug.
  </action>
  <acceptance_criteria>
    - `grep -c "onSelect" src/components/sandbox/hooks/useHullDrag.ts` outputs at least `2` (parameter + call)
    - `grep -c "onSelect" src/components/sandbox/hooks/useRotateHandleDrag.ts` outputs at least `2`
    - `grep -c "event.stopPropagation()" src/components/sandbox/hooks/useHullDrag.ts` outputs `1` (newly added)
    - `grep -c "function selectVessel" src/components/sandbox/chart/ChartPanel.tsx` outputs `1`
    - `grep -c "current === vessel ? null : vessel" src/components/sandbox/chart/ChartPanel.tsx` outputs `1` (D-01 toggle)
    - `grep -c "ChartHeaderStrip\|ChartFooterStrip\|VesselOverlayCard" src/components/sandbox/chart/ChartPanel.tsx` outputs at least `3`
    - `grep -c "isDegenerate" src/components/sandbox/types.ts` outputs at least `2` (ChartPanelProps + at least one of the Plan 18-01 strip interfaces)
    - `grep -c "onVesselSpeedChange={sandboxState.onVesselSpeedChange}" src/components/sandbox/SandboxContainer.tsx` outputs `1`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>ChartPanel.tsx renders the merged header/chart/footer composition with a working selectVessel toggle and svg-background close path; both drag hooks fire onSelect; SandboxContainer passes the 2 new required props; typecheck passes with zero errors (the temporary dual-rendering of old cards + new ChartPanel content is expected, not an error).</done>
</task>

<task type="auto">
  <name>Task 2: Update existing test files' required-prop shape (mechanical)</name>
  <files>src/components/sandbox/chart/ChartPanel.test.tsx, src/components/sandbox/hooks/useHullDrag.test.ts, src/components/sandbox/hooks/useRotateHandleDrag.test.ts</files>
  <read_first>
    src/components/sandbox/chart/ChartPanel.test.tsx
    src/components/sandbox/hooks/useHullDrag.test.ts
    src/components/sandbox/hooks/useRotateHandleDrag.test.ts
  </read_first>
  <action>
    In `ChartPanel.test.tsx`'s `renderChartPanel()` helper: add `isDegenerate: false`, `onVesselSpeedChange: vi.fn()`, `onVesselTypeChange: vi.fn()` to the default `props` object (before the `...overrides` spread, so individual tests can still override them).
    In `useHullDrag.test.ts` and `useRotateHandleDrag.test.ts`: add the same 3 fields (`isDegenerate: false`, `onVesselSpeedChange: vi.fn()`, `onVesselTypeChange: vi.fn()`) to each file's `createElement(ChartPanel, {...})` props object. No other change to either file -- these tests exercise the underlying drag-position/heading behavior, which is unaffected by this plan's `onSelect` addition (both hooks still call `onVesselPositionChange`/`onVesselHeadingChange` exactly as before).
  </action>
  <acceptance_criteria>
    - `npx vitest run ChartPanel.test.tsx useHullDrag.test.ts useRotateHandleDrag.test.ts` passes with 0 failures (all pre-existing assertions still hold, unmodified)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run ChartPanel.test.tsx useHullDrag.test.ts useRotateHandleDrag.test.ts && npm run typecheck</automated>
  </verify>
  <done>All 3 existing test files compile and pass unmodified in behavior, now supplying the 3 newly-required ChartPanelProps fields.</done>
</task>

<task type="auto">
  <name>Task 3: New ChartPanel.test.tsx regression coverage for D-01's close-paths and Pitfall 1's overlap case</name>
  <files>src/components/sandbox/chart/ChartPanel.test.tsx</files>
  <read_first>
    src/components/sandbox/chart/ChartPanel.test.tsx (as updated by Task 2)
    src/components/sandbox/hooks/useHullDrag.test.ts (real pointerdown/pointermove dispatch pattern to mirror)
    src/domain/colregs/classify-encounter.fixtures.js (crossingResidualBasicCase -- vesselA at (0,0), vesselB at (5,0), close enough on a 20nm-wide chart that vesselA's overlay card plausibly visually overlaps vesselB's hull)
    .planning/phases/18-on-chart-vessel-control-overlay/18-CONTEXT.md (D-01)
    .planning/research/PITFALLS.md (Pitfall 1)
  </read_first>
  <action>
    Add these test cases to the existing `describe("ChartPanel", ...)` block, using `renderChartPanel()`'s default `crossingResidualBasicCase` fixture and dispatching real `PointerEvent("pointerdown", { pointerId: 1, bubbles: true })` events against `[data-testid="hull-hit-vesselA"]`/`[data-testid="hull-hit-vesselB"]` (matching `useHullDrag.test.ts`'s existing dispatch pattern) -- assert overlay presence/absence via a stable query such as `screen.queryByLabelText(/Close Vessel A control card/i)` (the aria-label Plan 18-02's `VesselOverlayCard` close button carries) or `container.querySelector('[data-testid="hull-hit-vesselA"]')`'s absence of a role="slider" descendant when closed:
    (1) "opens vesselA's overlay on hull pointerdown" -- dispatch pointerdown on vesselA's hull-hit rect, assert a slider (role) is now present in the document.
    (2) "closes vesselA's overlay when its own hull is pressed again (D-01 toggle)" -- dispatch pointerdown on vesselA twice, assert no slider is present after the second press.
    (3) "switches the overlay to vesselB when vesselB's hull is pressed while vesselA's overlay is open, not closing it" -- dispatch pointerdown on vesselA (opens A), then on vesselB (should switch, not close) -- assert exactly one overlay's close button is present and it is vesselB's (`Close Vessel B control card`), proving the `stopPropagation()`-guarded svg bubbling fix from Task 1 works (this is the exact regression this plan's Task 1 comment traces).
    (4) "closes an open overlay when the bare svg background is pressed" -- open vesselA's overlay, then dispatch `pointerdown` directly on the `container.querySelector("svg")` element itself (not a hull/rotate hit-target), assert no slider/close-button is present afterward.
    (5) "still fires onVesselPositionChange for a drag on vesselB while vesselA's overlay is open and visually near it" (Pitfall 1 regression) -- open vesselA's overlay (pointerdown on vesselA's hull), then dispatch a real pointerdown+pointermove sequence on `[data-testid="hull-hit-vesselB"]` with a fresh `onVesselPositionChange` spy, and assert it was called with `"vesselB"` and a `Position` -- proving the new overlay card does not silently swallow the pointer events meant for the other vessel's drag, even though `crossingResidualBasicCase`'s 5nm vessel separation puts them close enough on-chart for this to be a genuine risk, not a contrived one.
  </action>
  <acceptance_criteria>
    - `npx vitest run ChartPanel.test.tsx` passes with 0 failures, including all 5 new cases above
    - Test file contains all 5 new `it(...)` blocks (grep-verifiable by their distinguishing substrings, e.g. `grep -c "D-01 toggle" src/components/sandbox/chart/ChartPanel.test.tsx` outputs at least `1`)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run ChartPanel.test.tsx && npm run typecheck</automated>
  </verify>
  <done>ChartPanel.test.tsx automated-regression-proves all 3 close paths + the toggle + the switch-while-open edge case + the Pitfall-1 overlap-does-not-block-drag case, before any human-verification checkpoint runs in Plan 18-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chart pointer-event surface | `ChartPanel`'s `<svg>` now has its own top-level `onPointerDown`, in addition to the existing per-vessel hull/rotate handlers -- a new listener on an already-sensitive interaction surface (this codebase's 3rd occurrence of this regression class). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Denial of Service (UI) | svg-background-click-closes-overlay handler clobbering a vessel-to-vessel switch | mitigate | `stopPropagation()` added to both drag hooks' `onPointerDown` (Task 1), with the exact bug traced in a code comment; Task 3's regression test (3) asserts the switch-not-close outcome directly, not just typecheck. |
| T-18-05 | Denial of Service (UI) | Floating overlay card silently blocking a drag on the other vessel (Pitfall 1) | mitigate | Task 3's regression test (5) drives a real pointerdown+pointermove against the other vessel's hull while an overlay is open and visually close, asserting the position-change callback still fires. |
| T-18-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan -- zero package.json changes. |
</threat_model>

<verification>
1. `npx vitest run ChartPanel.test.tsx useHullDrag.test.ts useRotateHandleDrag.test.ts` -- all pass, including the 5 new regression cases.
2. `npm run typecheck` -- 0 errors.
3. `npm run build` -- succeeds (confirms no Server/Client boundary or prop-shape break from the ChartPanelProps extension).
</verification>

<success_criteria>
ChartPanel fully hosts the header/footer strips and the toggle-aware floating overlay, with automated regression coverage for D-01's 3 close paths and Pitfall 1's cross-vessel drag-safety concern. SandboxContainer's old cards (VerdictBanner/InstrumentReadouts/ControlPanel) still render alongside the new content until Plan 18-04 removes them.
</success_criteria>

<output>
Create `.planning/phases/18-on-chart-vessel-control-overlay/18-03-SUMMARY.md` when done
</output>
