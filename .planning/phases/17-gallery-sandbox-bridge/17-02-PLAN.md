---
phase: 17-gallery-sandbox-bridge
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - src/components/gallery/card/TryOnSandboxButton.tsx
  - src/components/gallery/card/TryOnSandboxButton.test.tsx
  - src/components/gallery/card/GalleryCard.tsx
  - src/components/gallery/card/GalleryCard.test.tsx
  - src/components/gallery/GalleryContainer.test.tsx
autonomous: true
requirements: [GAL-05, GAL-06]

must_haves:
  truths:
    - "Clicking 'Try on Sandbox' on a gallery card calls requestLoad(vesselA, vesselB) with that card's exact vessel pair, and no <Link>/anchor exists anywhere on the card"
    - "The button overlays the card's mini-chart preview, centered, with the chart dimmed via a semi-opaque scrim for contrast (D-01), labeled with an icon + the literal text 'Try on Sandbox' (D-02), styled as a solid accent-colored Button variant=\"default\" -- never variant=\"outline\" (D-03)"
    - "The 'Try on Sandbox' button (and its dimming chart scrim) reveal on both :hover and :focus-within, not hover-only, so keyboard Tab reaches it (D-06, GAL-06); on touch/coarse-pointer devices the button and scrim sit at low, non-zero opacity by default rather than fully hidden (D-05)"
    - "The whole-card hover/focus border+shadow polish (today's group-hover) is preserved after removing the <Link> wrapper, now firing on group-focus-within too (D-04)"
    - "The click handler calls scrollIntoView on #sandbox with no explicit `behavior` override (D-07 -- relies on the app's global prefers-reduced-motion-gated smooth scroll, never hardcodes behavior: 'smooth'), fired synchronously alongside requestLoad in the same handler (D-08)"
  artifacts:
    - path: "src/components/gallery/card/TryOnSandboxButton.tsx"
      provides: "the Gallery's sole interactive CTA, a \"use client\" leaf calling useSandboxBridge().requestLoad + scrollIntoView"
      contains: "useSandboxBridge"
    - path: "src/components/gallery/card/GalleryCard.tsx"
      provides: "Link-free card composing TryOnSandboxButton as its sole interactive child"
      contains: "TryOnSandboxButton"
  key_links:
    - from: "TryOnSandboxButton.tsx"
      to: "SandboxBridgeProvider (Plan 17-01)"
      via: "useSandboxBridge().requestLoad(vesselA, vesselB) call inside the button's onClick"
      pattern: "requestLoad\\(vesselA, vesselB\\)"
    - from: "GalleryCard.tsx"
      to: "TryOnSandboxButton.tsx"
      via: "JSX composition inside the relative chart-preview wrapper div"
      pattern: "<TryOnSandboxButton"
---

<objective>
Give the Gallery its side of the bridge: a new `TryOnSandboxButton` client leaf that calls `useSandboxBridge().requestLoad()` + scrolls to `#sandbox`, and a restructured `GalleryCard` that drops its whole-card `<Link>` in favor of this button as the sole interactive affordance &mdash; per D-01 through D-09's exact hover/focus/touch/scroll contract.

Purpose: This is milestone build-order step [2]'s Gallery-side half, and the first real consumer of Plan 17-01's `SandboxBridgeProvider`. Closes out `.planning/research/PITFALLS.md` Pitfall 2's specific failure mode ("Try on Sandbox" secretly falling back to `router.push`) by construction &mdash; the button never navigates, it only calls `requestLoad` + `scrollIntoView`.
Output: `TryOnSandboxButton.tsx` (new, `"use client"`) with its own test; `GalleryCard.tsx` with the `<Link>` removed and `group-focus-within:` parity added (D-04/D-06); `GalleryCard.test.tsx` and `GalleryContainer.test.tsx` updated to assert the new Link-free, button-based affordance instead of the removed `<Link>`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-gallery-sandbox-bridge/17-CONTEXT.md
@.planning/phases/17-gallery-sandbox-bridge/17-PATTERNS.md
@.planning/phases/17-gallery-sandbox-bridge/17-UI-SPEC.md
@.planning/phases/17-gallery-sandbox-bridge/17-01-SUMMARY.md
</context>

<interfaces>
<!-- SandboxBridgeProvider's public contract, shipped by Plan 17-01 -- this plan
     only consumes it, does not redefine it. -->

```typescript
export function useSandboxBridge(): {
  pendingScenario: { vesselA: Vessel; vesselB: Vessel; requestId: number } | null;
  requestLoad: (vesselA: Vessel, vesselB: Vessel) => void;
};
```

<!-- TryOnSandboxButton's own props contract, defined in Task 1, consumed by GalleryCard in Task 2. -->

```typescript
export function TryOnSandboxButton({
  vesselA,
  vesselB,
  title,
}: {
  vesselA: Vessel;
  vesselB: Vessel;
  title: string;
}): JSX.Element;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create TryOnSandboxButton.tsx and its test</name>
  <files>src/components/gallery/card/TryOnSandboxButton.tsx, src/components/gallery/card/TryOnSandboxButton.test.tsx</files>
  <read_first>
    src/components/sandbox/control-panel/CopyLinkButton.tsx (structural analog: "use client" leaf Button with a single onClick, no other state machinery)
    src/components/hero/Hero.tsx (lines ~52-59: the app's only existing solid-accent Button usage, styling reference for D-03)
    src/components/ui/button.tsx (buttonVariants: "default" vs "outline" variant classes)
    .planning/phases/17-gallery-sandbox-bridge/17-PATTERNS.md (section "TryOnSandboxButton.tsx", full analog + bridge-consumption pattern)
    .planning/phases/17-gallery-sandbox-bridge/17-UI-SPEC.md (Interaction Contract table, Color section)
    .planning/phases/17-gallery-sandbox-bridge/17-CONTEXT.md (D-01 through D-08)
  </read_first>
  <action>
    Create a `"use client"` leaf component exporting `TryOnSandboxButton({ vesselA, vesselB, title }: { vesselA: Vessel; vesselB: Vessel; title: string })`. Import `Vessel` type from `"../../../domain/vessel/vessel.js"`; import `useSandboxBridge` from `"../../sandbox/bridge/SandboxBridgeProvider.js"` (Plan 17-01's contract); import `Button` from `"@/components/ui/button"`; import the `PlayCircle` icon from `"lucide-react"` (D-02's discretion pick &mdash; conveys "load/play/go", avoids collision with `RotateCcw`/Reset and `Link2`/Save already used elsewhere on this page).
    Destructure `const { requestLoad } = useSandboxBridge();`. Define `function handleClick(): void` whose body: calls `requestLoad(vesselA, vesselB);` then, immediately after (synchronously, same function body, no `await`/`setTimeout`/effect delay), `document.getElementById("sandbox")?.scrollIntoView({ block: "start" });` &mdash; per D-08, the scroll fires in parallel with the Context state update in the same click handler, not gated behind Sandbox's re-render completing (the `#sandbox` anchor's position doesn't depend on which scenario is loaded, so there is no "scrolled but chart hasn't changed yet" flash to guard against). Use NO `behavior` key in that options object (D-07: relies on `app/globals.css`'s existing `prefers-reduced-motion`-gated global `scroll-behavior: smooth`; never add `behavior: "smooth"` literally here, per CONTEXT.md's explicit override of the architecture research's sample code on this one point).
    Render a wrapping `<div className="absolute inset-0 flex items-center justify-center rounded-md">` (D-01: centered overlay over the chart) containing two children: (1) a decorative scrim `<div aria-hidden="true" className="absolute inset-0 rounded-md bg-background/70 opacity-0 transition-opacity duration-200 group-hover:opacity-100 group-focus-within:opacity-100">`, with an added arbitrary Tailwind media variant equivalent to `@media (hover: none)` gating a low, non-zero default opacity for coarse/touch pointers (D-05 &mdash; e.g. `[@media(hover:none)]:opacity-40`, adjust exact escaping/value to this project's Tailwind v4 setup as needed); and (2) the `Button` itself: `type="button"`, `variant="default"` (D-03 &mdash; explicitly NOT `variant="outline"`, that is Reset/Save/CopyLink's look; `default` is `buttonVariants`'s solid `bg-primary text-primary-foreground` per `src/components/ui/button.tsx`), `onClick={handleClick}`, `aria-label={\`Try ${title} on Sandbox\`}` (exact copy per the UI-SPEC's Copywriting Contract), with the same hover/focus-within/touch-opacity classes as the scrim plus `relative z-1` so it visually sits above it, containing a `<PlayCircle aria-hidden="true" />` icon followed by the literal text "Try on Sandbox" (D-02's exact wording, matches GAL-05).
    Every `group-hover:` utility added must have a matching `group-focus-within:` utility (D-04/D-06/GAL-06 &mdash; this is a mechanical Tailwind extension of the codebase's existing `group-hover:` convention, applied for the first time to `group-focus-within:`; there is no in-repo analog to copy for this specific half, per 17-PATTERNS.md's "No Analog Found" note).
  </action>
  <acceptance_criteria>
    - `grep -c '"use client"' src/components/gallery/card/TryOnSandboxButton.tsx` outputs `1`
    - `grep -c "group-focus-within" src/components/gallery/card/TryOnSandboxButton.tsx` outputs at least `2`
    - `grep -c 'behavior:' src/components/gallery/card/TryOnSandboxButton.tsx` outputs `0` (D-07: no hardcoded scroll behavior override anywhere in this file)
    - `grep -c 'variant="default"' src/components/gallery/card/TryOnSandboxButton.tsx` outputs `1` (D-03)
    - `grep -c 'variant="outline"' src/components/gallery/card/TryOnSandboxButton.tsx` outputs `0` (D-03 negative check)
    - `npx vitest run TryOnSandboxButton.test.tsx` passes, asserting: (a) a button with accessible name `Try {title} on Sandbox` renders; (b) clicking it calls a mocked `requestLoad` with exactly `(vesselA, vesselB)`; (c) clicking it calls `scrollIntoView` on a sibling `#sandbox` element with a call-argument object that has no `behavior: "smooth"` key set
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run TryOnSandboxButton.test.tsx && npm run typecheck</automated>
  </verify>
  <done>TryOnSandboxButton.tsx renders a single interactive Button revealed via group-hover/group-focus-within (with a distinct low-opacity touch-default state), calling requestLoad + a behavior-override-free scrollIntoView synchronously on click, styled variant="default" per D-03; its own test file proves both the click-wiring and the D-07 no-hardcoded-behavior constraint.</done>
</task>

<task type="auto">
  <name>Task 2: Remove GalleryCard's whole-card Link, compose TryOnSandboxButton, and fix the two existing test files it breaks</name>
  <files>src/components/gallery/card/GalleryCard.tsx, src/components/gallery/card/GalleryCard.test.tsx, src/components/gallery/GalleryContainer.test.tsx</files>
  <read_first>
    src/components/gallery/card/GalleryCard.tsx (full current file, 80 lines)
    src/components/gallery/card/GalleryCard.test.tsx (full current file)
    src/components/gallery/GalleryContainer.test.tsx (full current file)
    .planning/phases/17-gallery-sandbox-bridge/17-PATTERNS.md (section "GalleryCard.tsx", target shape + D-04's exact wrapper change)
    .planning/phases/17-gallery-sandbox-bridge/17-CONTEXT.md (D-04, and PROJECT.md's Out of Scope ruling against a dual Link+button card)
  </read_first>
  <action>
    In `GalleryCard.tsx`: remove `import Link from "next/link";` and add `import { TryOnSandboxButton } from "./TryOnSandboxButton.js";`. Replace the outer `<Link href={\`/s/${id}\`} aria-label={...} className="group block rounded-xl focus-visible:ring-2 focus-visible:ring-ring focus-visible:outline-hidden">` wrapper (current lines 46-53) with a plain `<div className="group relative block rounded-xl">` &mdash; per D-04, drop `href`/`aria-label`/the `focus-visible:ring-*` classes entirely; those affordances move onto `TryOnSandboxButton`'s own `Button`, which already gets a focus ring for free from `buttonVariants`'s built-in `focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50`.
    On the `Card`'s className (current lines 55-58), add `group-focus-within:border-primary/40 group-focus-within:shadow-sm` immediately after the existing `group-hover:border-primary/40 group-hover:shadow-sm` (D-04: Tab-focusing the new button must trigger the same whole-card hover polish, not just mouse hover).
    Change the mini-chart wrapping `<div className="mb-4">` (current line 59) to `<div className="relative mb-4">` and add `<TryOnSandboxButton vesselA={vesselA} vesselB={vesselB} title={title} />` as a sibling immediately after `<GalleryPreviewChart .../>` inside that div &mdash; the exact target shape shown in 17-PATTERNS.md's "GalleryCard.tsx" section. Leave the badge row / `<h3>` / `<p>` JSX (current lines 62-76) completely unchanged.
    In `GalleryCard.test.tsx`: delete the test `"wraps the whole card in a single Link to /s/{id} with the exact aria-label"` (current lines 66-84) entirely. Add a replacement test asserting (a) `screen.queryAllByRole("link").length` is `0` (no anchor/Link exists anywhere on the card) and (b) `screen.getByRole("button", { name: "Try Classic crossing on Sandbox" })` is in the document and is not `disabled` &mdash; this replaces the removed Link-based coverage with proof of the new button-based affordance's presence/reachability, matching D-04's Out of Scope ruling against a dual Link+button card. This test file renders `GalleryCard` standalone, and `GalleryCard` now transitively renders `TryOnSandboxButton`, which calls the real `useSandboxBridge()` hook &mdash; add a top-of-file `vi.mock("../../sandbox/bridge/SandboxBridgeProvider.js", () => ({ useSandboxBridge: () => ({ pendingScenario: null, requestLoad: vi.fn() }) }));` (mirroring `TryOnSandboxButton.test.tsx`'s own mock) or every existing render call in this file will throw the "must be used within SandboxBridgeProvider" guard error.
    In `GalleryContainer.test.tsx`: this file also renders the real `GalleryContainer` (which renders 6 `GalleryCard`s, each transitively rendering `TryOnSandboxButton`) with no `SandboxBridgeProvider` ancestor &mdash; add the same `vi.mock("../sandbox/bridge/SandboxBridgeProvider.js", () => ({ useSandboxBridge: () => ({ pendingScenario: null, requestLoad: vi.fn() }) }));` at the top of this file (note the shallower `../sandbox/...` relative path from `src/components/gallery/`, one level up from `card/`). Then update the test currently named `"renders the section heading, eyebrow, and exactly 6 links to /s/*"`: since `GalleryCard` no longer renders any `<Link>`, replace its body's link assertions with `expect(screen.queryAllByRole("link").length).toBe(0);` and `expect(screen.getAllByRole("button", { name: /try .* on sandbox/i }).length).toBe(6);`, and rename the test to describe the new behavior (e.g. `"renders the section heading, eyebrow, and exactly 6 'Try on Sandbox' buttons, with no <Link> anywhere"`). The second test (`"includes the real curated scenario titles..."`) needs no change &mdash; it only asserts on text content, unaffected by the Link removal.
  </action>
  <acceptance_criteria>
    - `grep -c "next/link" src/components/gallery/card/GalleryCard.tsx` outputs `0`
    - `grep -c "<Link" src/components/gallery/card/GalleryCard.tsx` outputs `0`
    - `grep -c "TryOnSandboxButton" src/components/gallery/card/GalleryCard.tsx` outputs at least `2` (import + JSX usage)
    - `grep -c "group-focus-within:border-primary/40" src/components/gallery/card/GalleryCard.tsx` outputs `1`
    - `grep -c "vi.mock" src/components/gallery/card/GalleryCard.test.tsx` outputs at least `1`
    - `grep -c "vi.mock" src/components/gallery/GalleryContainer.test.tsx` outputs at least `1`
    - `npx vitest run GalleryCard.test.tsx` passes, 0 failures
    - `npx vitest run GalleryContainer.test.tsx` passes, 0 failures
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run GalleryCard.test.tsx GalleryContainer.test.tsx && npm run typecheck && npm run lint</automated>
  </verify>
  <done>GalleryCard.tsx has no Link, wraps its content in a group div with group-focus-within polish, and renders TryOnSandboxButton as the sole interactive child; both GalleryCard.test.tsx and GalleryContainer.test.tsx are updated to assert zero Links and the new button-based affordance (6 "Try on Sandbox" buttons at the container level); full test suite, typecheck, and lint are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new this phase | `TryOnSandboxButton` passes already-server-validated `Vessel` data (sourced from `GalleryContainer`'s existing `rowToVessels()` conversion, unchanged) across a Server-to-Client prop boundary and into the same Context signaling channel Plan 17-01 built. No new user input, no new endpoint, no new persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03 | Tampering | `TryOnSandboxButton`'s `vesselA`/`vesselB` props | accept | These are server-fetched, already-persisted curated-scenario vessels (via `GalleryContainer`'s existing `gallery.list()` tRPC call + `rowToVessels()`), passed through unchanged as React props &mdash; not user-editable input at the point this button touches them. Downstream, `loadScenario()` still routes through `applyVesselUpdate()`'s `VesselSchema.safeParse` guard exactly like every other mutation source. |
| T-17-04 | Spoofing | `aria-label`/accessible-name mismatch between visual button text and screen-reader announcement | accept | Intentional and spec'd (UI-SPEC Copywriting Contract): `aria-label` deliberately includes the per-card `title` for screen-reader disambiguation across a grid of otherwise-identical "Try on Sandbox" buttons &mdash; not a spoofing risk, a documented accessibility choice. |
| T-17-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan &mdash; `lucide-react` and shadcn `Button` are both already installed dependencies; zero `package.json` changes. |
</threat_model>

<verification>
1. `npm run typecheck` &mdash; 0 errors.
2. `npm run lint` &mdash; 0 errors.
3. `npx vitest run TryOnSandboxButton.test.tsx GalleryCard.test.tsx GalleryContainer.test.tsx` &mdash; all green.
4. `grep -c "<Link" src/components/gallery/card/GalleryCard.tsx` &mdash; `0` (Link fully removed, matching the locked "no dual Link+button card" decision).
</verification>

<success_criteria>
This plan delivers the Gallery-side implementation of ROADMAP Phase 17 success criteria 1 (load-in-place, no navigation), 2 (scroll fires on click), and 3 (CTA discoverable via hover/keyboard-focus/touch, per D-05/D-06/GAL-06) at the component level. Full end-to-end human verification of all 4 criteria in a real browser happens in Plan 17-04.
</success_criteria>

<output>
Create `.planning/phases/17-gallery-sandbox-bridge/17-02-SUMMARY.md` when done
</output>
