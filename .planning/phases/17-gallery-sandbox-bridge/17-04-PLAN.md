---
phase: 17-gallery-sandbox-bridge
plan: 04
type: execute
wave: 3
depends_on: ["17-02"]
files_modified: []
autonomous: false
requirements: [GAL-05, GAL-06]

must_haves:
  truths:
    - "User can click 'Try on Sandbox' on any gallery card and see the Sandbox chart/readouts immediately update to that scenario's two vessels, with no URL change and no full page navigation"
    - "After clicking, the page smooth-scrolls to the Sandbox section automatically, landing on a chart that already shows the new scenario (not one that changes visibly after landing)"
    - "The 'Try on Sandbox' CTA is discoverable and operable via mouse hover, keyboard Tab focus, and touch/coarse-pointer -- not hover-only"
    - "Loading a second gallery card after a first correctly replaces the previously loaded scenario, not stale from the first load"
  artifacts: []
  key_links: []
---

<objective>
Human-verify, in a real browser, that the full Gallery&rarr;Sandbox bridge (Plans 17-01 + 17-02) satisfies all 4 of ROADMAP Phase 17's success criteria end-to-end &mdash; something jsdom-based automated tests structurally cannot observe (real navigation/URL behavior, real scroll timing, real focus-reveal via Tab, real touch-emulation discoverability).

Purpose: Per `.planning/research/PITFALLS.md` Pitfall 5, a green CI run does not substitute for this project's actual acceptance bar. Pitfall 2's specific failure mode ("Try on Sandbox" secretly falling back to `router.push`) and the "Looks Done But Isn't" checklist's "Gallery 'Try on Sandbox'" item both explicitly call out that URL/navigation-absence and stale-state-replacement must be human-confirmed, not inferred from passing unit tests.
Output: Human confirmation that all 4 ROADMAP Phase 17 success criteria hold in a real browser session, closing out the phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/17-gallery-sandbox-bridge/17-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/phases/17-gallery-sandbox-bridge/17-01-SUMMARY.md
@.planning/phases/17-gallery-sandbox-bridge/17-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human-verify the Gallery&rarr;Sandbox bridge end-to-end against all 4 Roadmap success criteria</name>
  <files>None -- verification-only task, no files modified</files>
  <read_first>
    .planning/phases/17-gallery-sandbox-bridge/17-01-SUMMARY.md
    .planning/phases/17-gallery-sandbox-bridge/17-02-SUMMARY.md
    .planning/research/PITFALLS.md (Pitfall 2, Pitfall 5, "Looks Done But Isn't" checklist's "Gallery 'Try on Sandbox'" item)
  </read_first>
  <action>
    Start the dev server and manually drive the 8-step verification sequence below against the homepage `/` route, confirming Plans 17-01/17-02's bridge wiring genuinely updates live Sandbox state with no navigation, scrolls correctly, is reachable via all 3 input modes, and correctly replaces state on a second load. This is a human-verification gate, not a code-writing task -- no source files are touched.
  </action>
  <what-built>
    Plan 17-01 built `SandboxBridgeProvider`/`useSandboxBridge()` (a page-scoped React Context wrapping `app/page.tsx` and `app/s/[shareId]/page.tsx`) and wired `SandboxContainer` to call `loadScenario()` whenever a pending scenario's `requestId` changes. Plan 17-02 built `TryOnSandboxButton` (replacing `GalleryCard`'s whole-card `<Link>`) which calls `requestLoad(vesselA, vesselB)` then `scrollIntoView` on `#sandbox`, with hover/focus-within/touch-visible reveal states.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open `http://localhost:3000/`. Note the Sandbox chart's current verdict/vessels at the top of the page.
    2. Hover a gallery card with a mouse -- confirm the "Try on Sandbox" button and its dimming chart scrim fade in over the mini-chart preview.
    3. Click "Try on Sandbox" -- confirm the URL bar does NOT change (still exactly `/`, no `/s/{id}` navigation, no full page reload) and the Sandbox chart above visibly updates to that card's two vessels/verdict.
    4. Confirm the page auto-scrolls to `#sandbox`, landing on a chart that already shows the new scenario (not a stale one that changes visibly after the scroll lands).
    5. Reload `/` fresh. Tab through the page with the keyboard only (no mouse) until a gallery card's "Try on Sandbox" button receives visible focus -- confirm it is visibly revealed while focused (not hidden/invisible), then press Enter -- confirm the same load+scroll behavior as steps 3-4.
    6. Using browser devtools' mobile/touch emulation (or a real touch device), confirm the button is visibly discoverable on a coarse-pointer viewport without requiring a first "reveal" tap, and tapping it triggers the same load+scroll behavior.
    7. From the now-loaded scenario, click a DIFFERENT gallery card's "Try on Sandbox" button -- confirm the Sandbox chart replaces the first loaded scenario with the second card's vessels (not stale/unchanged from step 3/5).
    8. Confirm no console errors appear in devtools during any of the above steps, and separately confirm `/s/[shareId]` (any existing saved-scenario share link) still loads normally with no `SandboxBridgeProvider`-guard runtime error.
  </how-to-verify>
  <resume-signal>Type "approved" if all 8 checks pass as expected, or describe the specific step/behavior that differs.</resume-signal>
  <acceptance_criteria>
    - Human types "approved" after confirming all 8 steps in how-to-verify behave as expected
    - The URL bar shows no navigation change in steps 3, 5, 6, and 7 (behavior assertion, ROADMAP criterion 1)
    - The Sandbox chart visibly reflects each loaded scenario's vessels immediately, with the scroll landing on already-updated content (behavior assertion, ROADMAP criterion 2)
    - Mouse hover, keyboard Tab, and touch/coarse-pointer emulation each independently reveal and trigger the button (behavior assertion, ROADMAP criterion 3 / GAL-06)
    - The second gallery card's load genuinely replaces the first, not stale (behavior assertion, ROADMAP criterion 4)
    - `/s/[shareId]` still renders with no runtime error from the bridge's Provider guard (regression check for Plan 17-01 Task 2's fix)
  </acceptance_criteria>
  <verify>
    <automated>MISSING -- this is a human-browser-verification checkpoint; no automated test can observe real navigation/URL state, real scroll-completion timing, or real Tab-focus/touch-emulation reveal, per this project's jsdom-measurement-ban precedent (see .planning/research/PITFALLS.md Pitfall 5)</automated>
  </verify>
  <done>Human has typed "approved", confirming all 8 verification steps passed and all 4 ROADMAP Phase 17 success criteria hold in a real browser session.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new this phase | This plan performs no code changes -- it is a human-verification checkpoint over Plans 17-01/17-02's implementation. No new trust boundary is introduced. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | N/A | This plan modifies zero files | accept | No code changes in this plan; the STRIDE registers for the actual code changes live in Plans 17-01/17-02/17-03's own `<threat_model>` blocks. This checkpoint exists purely to human-verify runtime/UX behavior, not to introduce or mitigate a new threat surface. |
</threat_model>

<verification>
Human confirms all 8 steps in Task 1's `<how-to-verify>` behave as expected against the live `/` route (and a `/s/[shareId]` regression spot-check) in a real browser.
</verification>

<success_criteria>
All 4 ROADMAP Phase 17 success criteria are satisfied and human-confirmed:
1. "Try on Sandbox" loads a scenario into live Sandbox state with no URL change and no page navigation.
2. The page smooth-scrolls to Sandbox automatically, sequenced after the scenario state has already updated.
3. The CTA is discoverable/operable via mouse hover, keyboard Tab focus, and touch/coarse-pointer.
4. A second gallery-card load correctly replaces the first, not stale.
</success_criteria>

<output>
Create `.planning/phases/17-gallery-sandbox-bridge/17-04-SUMMARY.md` when done
</output>
