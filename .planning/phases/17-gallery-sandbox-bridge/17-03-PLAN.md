---
phase: 17-gallery-sandbox-bridge
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sandbox/hooks/useSandboxState.ts
  - src/domain/geometry/bearing/bearing.ts
  - src/domain/geometry/bearing/bearing.fixtures.ts
  - src/domain/geometry/bearing/bearing.test.ts
autonomous: true
requirements: [GAL-05]

must_haves:
  truths:
    - "useSandboxState()'s lazy initializer never unwraps a classifyEncounter() Result via an unchecked type cast; it falls back to a known-good fixture, and only throws if that fallback itself fails"
    - "bearing() classifies vessel pairs separated by less than 1e-6 chart-space units as coincident-position, not only exact (0,0) separation"
  artifacts:
    - path: "src/components/sandbox/hooks/useSandboxState.ts"
      provides: "safe .ok-checked lazy initializer with a documented two-level fallback chain"
      contains: "initialResult.ok"
    - path: "src/domain/geometry/bearing/bearing.ts"
      provides: "distance-threshold coincident-position guard (widened from exact equality)"
      contains: "COINCIDENT_DISTANCE_THRESHOLD"
  key_links:
    - from: "useSandboxState.ts's lazy initializer"
      to: "crossingResidualBasicCase fallback fixture"
      via: "a second classifyEncounter() call only reached if the seed vessels somehow fail to classify"
      pattern: "classifyEncounter\\(crossingResidualBasicCase.vesselA, crossingResidualBasicCase.vesselB\\)"
    - from: "bearing.ts"
      to: "bearing.fixtures.ts's nearCoincidentPositionCase"
      via: "bearing.test.ts's new degenerate-case assertion"
      pattern: "nearCoincidentPositionCase"
---

<objective>
Land the two folded-todo hardening fixes CONTEXT.md scoped into this phase: replace `useSandboxState.ts`'s unchecked `Result` cast with a real `.ok` check + safe fallback (D-10), and widen `bearing()`'s exact-equality coincident-position guard to a small distance threshold (D-11).

Purpose: Both fixes were pending todos folded into Phase 17's scope by CONTEXT.md (originally CR-01 from Phase 16's code review, and a drag-to-near-coincidence gap report) because they touch files this milestone is already working in, and they harden the exact mutation/degenerate-detection path the Gallery bridge's `loadScenario()` now has a second real-world call site for. Per `.planning/research/ARCHITECTURE.md`'s Suggested Build Order, this work is independent of the Gallery&harr;Sandbox wiring (Plans 17-01/17-02) and can proceed in parallel &mdash; it shares no files with either.
Output: `useSandboxState.ts`'s lazy initializer with a documented `.ok`-checked fallback chain instead of an unchecked cast; `bearing.ts`'s coincident-position guard widened to a documented `1e-6`-unit distance threshold, with a new fixture + test proving it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-gallery-sandbox-bridge/17-CONTEXT.md
@.planning/phases/17-gallery-sandbox-bridge/17-PATTERNS.md
@.planning/todos/pending/2026-07-25-unsafe-result-cast-in-usesandboxstate-lazy-initializer.md
@.planning/todos/pending/2026-07-25-drag-to-coincident-position-rarely-triggers-unable-to-classify.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the unsafe Result cast in useSandboxState.ts's lazy initializer (D-10)</name>
  <files>src/components/sandbox/hooks/useSandboxState.ts</files>
  <read_first>
    src/components/sandbox/hooks/useSandboxState.ts (full file, current lazy initializer at lines 74-82, and the existing `.ok`-narrowing idiom in applyVesselUpdate at lines 105-116)
    .planning/phases/17-gallery-sandbox-bridge/17-PATTERNS.md (section "useSandboxState.ts -- D-10 fix only", exact required fix shape)
    .planning/phases/17-gallery-sandbox-bridge/17-CONTEXT.md (D-10, and the Claude's Discretion note on the fallback fixture choice)
    .planning/todos/pending/2026-07-25-unsafe-result-cast-in-usesandboxstate-lazy-initializer.md
  </read_first>
  <action>
    Replace the lazy initializer's unsafe cast (current lines 74-82: `const initialResult = classifyEncounter(seedA, seedB) as { ok: true; value: ClassificationResult }; return initialResult.value;`) with a real `.ok` narrowing check per D-10: call `const initialResult = classifyEncounter(seedA, seedB);`, then `if (initialResult.ok) return initialResult.value;`. If `initialResult.ok` is `false` (the seed-vessels-always-classify invariant is somehow violated), fall back to the app's own known-good default fixture: call `classifyEncounter(crossingResidualBasicCase.vesselA, crossingResidualBasicCase.vesselB)` (already imported at line 15) and, if that also fails, `throw new Error("crossingResidualBasicCase fixture failed to classify — invariant broken");` rather than silently returning a wrong/degenerate value.
    Follow the exact `Result<T>`/`.ok` discriminated-union narrowing idiom this same file already uses in `applyVesselUpdate` (current lines 105-116) &mdash; do not introduce a new pattern.
    Update the surrounding comment (current lines 67-73) to describe the real two-level fallback chain (seed classification &rarr; `crossingResidualBasicCase` fallback &rarr; throw) instead of the stale "the ONE place... without a preceding `.ok` check" description, which becomes inaccurate once this fix lands.
  </action>
  <acceptance_criteria>
    - `grep -c "as { ok: true" src/components/sandbox/hooks/useSandboxState.ts` outputs `0` (unsafe cast fully removed)
    - `grep -c "initialResult.ok" src/components/sandbox/hooks/useSandboxState.ts` outputs at least `1`
    - `grep -c "classifyEncounter(crossingResidualBasicCase.vesselA, crossingResidualBasicCase.vesselB)" src/components/sandbox/hooks/useSandboxState.ts` outputs `1` (the new fallback call, distinct from the existing `seedA ?? crossingResidualBasicCase.vesselA` nullish-coalescing default at the top of the file)
    - `npm run typecheck` exits 0
    - `npx vitest run SandboxContainer.test.tsx` passes (the existing default-mount test already exercises this lazy initializer on every render)
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npx vitest run SandboxContainer.test.tsx</automated>
  </verify>
  <done>The lazy initializer performs a real .ok check with a documented two-level fallback chain (seed encounter -&gt; crossingResidualBasicCase -&gt; throw); no unchecked cast remains anywhere in the file; typecheck and the existing SandboxContainer default-mount test both pass.</done>
</task>

<task type="auto">
  <name>Task 2: Widen bearing()'s coincident-position check from exact equality to a distance threshold (D-11)</name>
  <files>src/domain/geometry/bearing/bearing.ts, src/domain/geometry/bearing/bearing.fixtures.ts, src/domain/geometry/bearing/bearing.test.ts</files>
  <read_first>
    src/domain/geometry/bearing/bearing.ts (full file, 39 lines)
    src/domain/geometry/bearing/bearing.fixtures.ts (full file, existing coincidentPositionCase)
    src/domain/geometry/bearing/bearing.test.ts (full file, existing degenerate-cases describe block)
    .planning/phases/17-gallery-sandbox-bridge/17-PATTERNS.md (section "bearing.ts -- D-11 fix only", exact required fix shape and test/fixture pattern)
    .planning/phases/17-gallery-sandbox-bridge/17-CONTEXT.md (D-11, and the Claude's Discretion note on the exact threshold value)
    .planning/todos/pending/2026-07-25-drag-to-coincident-position-rarely-triggers-unable-to-classify.md
  </read_first>
  <action>
    In `bearing.ts`: replace the exact-equality coincident check (current lines 31-33: `if (dx === 0 && dy === 0) { return err("coincident-position", { a, b }); }`) with a small-distance-threshold check. Declare a module-level `const COINCIDENT_DISTANCE_THRESHOLD = 1e-6;` with an inline comment documenting why (a drag gesture can land two vessels a sub-pixel fraction apart in chart-space units without ever producing exact floating-point equality, which let near-coincident drags slip past the old exact-equality guard and reach `atan2` with a near-zero-but-nonzero argument, producing a wildly unstable bearing instead of the intended degenerate signal; `1e-6` chart-space units is far below any humanly-perceptible vessel separation on this chart's viewBox scale, so it only catches true coincident/near-coincident drags). Then replace the check with `if (Math.hypot(dx, dy) < COINCIDENT_DISTANCE_THRESHOLD) { return err("coincident-position", { a, b }); }`.
    In `bearing.fixtures.ts`: add a new fixture `export const nearCoincidentPositionCase: ErrCase = { a: { x: 2, y: 3 }, b: { x: 2 + 1e-7, y: 3 } };` with a comment documenting the exact sub-threshold separation (1e-7 &lt; `COINCIDENT_DISTANCE_THRESHOLD` of 1e-6) and why it must still classify as coincident.
    In `bearing.test.ts`: import `nearCoincidentPositionCase` alongside the existing `coincidentPositionCase` import, and add one new `it(...)` block inside the existing `describe("bearing() degenerate cases", ...)` block, mirroring the exact-equality test's shape (current lines 61-70: `expect(result.ok).toBe(false)` then `if (!result.ok) expect(result.reason).toBe("coincident-position")`) but exercising `nearCoincidentPositionCase` instead of `coincidentPositionCase`.
  </action>
  <acceptance_criteria>
    - `grep -c "COINCIDENT_DISTANCE_THRESHOLD" src/domain/geometry/bearing/bearing.ts` outputs `2` (declaration + usage)
    - `grep -c "dx === 0 && dy === 0" src/domain/geometry/bearing/bearing.ts` outputs `0` (old exact-equality check fully removed)
    - `grep -c "nearCoincidentPositionCase" src/domain/geometry/bearing/bearing.fixtures.ts` outputs `1`
    - `grep -c "nearCoincidentPositionCase" src/domain/geometry/bearing/bearing.test.ts` outputs at least `2` (import + usage in the new test)
    - `npx vitest run bearing.test.ts` passes with 9 total tests (the existing 8 plus 1 new), 0 failures
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run bearing.test.ts && npm run typecheck</automated>
  </verify>
  <done>bearing()'s coincident-position guard uses a documented small-distance threshold (1e-6) instead of exact equality; a new near-coincident fixture and test prove the widened guard catches sub-threshold-but-nonzero separations; all existing cardinal/boundary/degenerate tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new this phase | Both fixes harden existing internal domain/state logic against an already-validated input shape (`Vessel`/`Position`, both Zod-typed) &mdash; no new input source, no new endpoint, no new external dependency. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-05 | Denial of Service | Unhandled throw if `crossingResidualBasicCase` itself fails to classify (D-10's final fallback) | accept | This fixture is a hardcoded, already-proven-correct constant covered by existing domain tests; the throw path is an unreachable-in-practice invariant guard, not a user-triggerable failure &mdash; equivalent risk profile to the pre-existing unchecked cast it replaces, but now fails loudly instead of silently misbehaving. |
| T-17-06 | Tampering | Widened `COINCIDENT_DISTANCE_THRESHOLD` changing degenerate-classification boundary behavior | accept | `1e-6` is far below any real vessel separation this chart's viewBox scale can represent meaningfully; the change only affects the already-existing "Unable to classify" degenerate-state path (`isDegenerate`), which has no security implication &mdash; it is a display-state derivation, not a data-integrity boundary. |
| T-17-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan &mdash; zero `package.json` changes. |
</threat_model>

<verification>
1. `npm run typecheck` &mdash; 0 errors.
2. `npx vitest run SandboxContainer.test.tsx bearing.test.ts` &mdash; both files green.
3. `npm test` (full suite) &mdash; green, confirming neither fix regresses any other existing coverage.
</verification>

<success_criteria>
Both folded todos (D-10, D-11) from CONTEXT.md are closed: `useSandboxState.ts`'s lazy initializer has zero unchecked casts, and `bearing.ts`'s coincident-position guard is a documented distance threshold rather than exact equality, each proven by an automated test. This work is independent hardening tangential to the Gallery bridge's file surface but supports GAL-05's overall reliability, per CONTEXT.md's explicit folding of both todos into this phase's scope.
</success_criteria>

<output>
Create `.planning/phases/17-gallery-sandbox-bridge/17-03-SUMMARY.md` when done
</output>
