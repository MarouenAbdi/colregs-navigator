---
phase: 05-save-share-gallery
plan: 05
type: execute
wave: 3
depends_on: ["05-01", "05-03", "05-04"]
files_modified: [src/lib/trpc/banner.ts, src/lib/trpc/banner.test.ts, "app/s/[shareId]/page.tsx", src/components/sandbox/CopyLinkButton.tsx]
autonomous: false
requirements: [SCEN-01]

must_haves:
  truths:
    - "Opening /s/[validShareId] loads the exact saved vessels into the fully editable sandbox with a freshly recomputed verdict (SCEN-01, SCEN-02 reuse)."
    - "Opening /s/[shareId] for a curated gallery row shows its rationale text in the banner; for a plain saved scenario, shows the generic 'Viewing saved scenario' label (D-02)."
    - "Opening /s/[nonexistentShareId] renders Next's real 404 page, not a crash or blank screen."
    - "Navigating client-side between two different /s/[shareId] pages shows the NEW scenario's vessels, not the previous page's stale state (Pitfall 1)."
  artifacts:
    - path: "app/s/[shareId]/page.tsx"
      provides: "server component: scenario.get -> notFound() | SandboxContainer(initialScenario, banner)"
    - path: src/lib/trpc/banner.ts
      provides: "buildScenarioBanner(row) pure helper"
  key_links:
    - from: "app/s/[shareId]/page.tsx"
      to: src/lib/trpc/server.ts
      via: "getCaller().scenario.get({ shareId })"
      pattern: "scenario\\.get"
    - from: "app/s/[shareId]/page.tsx"
      to: src/components/sandbox/SandboxContainer.tsx
      via: "<SandboxContainer key={shareId} initialScenario={...} banner={...} />"
      pattern: "key=\\{shareId\\}"
---

<objective>
Build the `/s/[shareId]` detail route — the single page that serves BOTH plain shared links (Plan 05-03's Save button redirects here) and gallery entries (Plan 05-04's cards link here), differentiated only by whether `isCurated`/`rationale` is present on the fetched row. Add a small Copy Link affordance. Close the phase with an end-to-end human verification of the full save/share/gallery flow.

Purpose: this is the page that makes SCEN-01's "receive a shareable link" and SCEN-02's "always re-derive on read" observable in the UI for the first time.
Output: app/s/[shareId]/page.tsx, src/lib/trpc/banner.ts, src/components/sandbox/CopyLinkButton.tsx, and a human-verified full flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-save-share-gallery/05-CONTEXT.md
@.planning/phases/05-save-share-gallery/05-RESEARCH.md
@.planning/phases/05-save-share-gallery/05-PATTERNS.md
</context>

<interfaces>
From src/lib/trpc/server.ts (Plan 05-01): `export function getCaller(): ReturnType<typeof appRouter.createCaller>;`

From src/components/sandbox/types.ts (Plan 05-03): `export interface SandboxContainerProps { initialScenario?: { vesselA: Vessel; vesselB: Vessel }; banner?: { label: string; rationale?: string } }`

From src/server/api/routers/scenario.ts (existing, lines 23-32):
```typescript
get: publicProcedure.input(z.object({ shareId: z.string() })).query(async ({ ctx, input }) => {
  const scenario = await ctx.scenarioService.getScenario(input.shareId);
  if (!scenario) throw new TRPCError({ code: "NOT_FOUND" });
  return scenario;
}),
```

From src/server/application/scenario-service.ts (existing, lines 38-56): `export function rowToVessels(row: ScenarioRow): { vesselA: Vessel; vesselB: Vessel };` — reuse directly, do not re-derive the field mapping.

ScenarioRow (src/server/db/scenario-repository.ts, lines 17-33) includes `isCurated: boolean`, `rationale: string | null`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the buildScenarioBanner contract</name>
  <files>src/lib/trpc/banner.ts, src/lib/trpc/banner.test.ts</files>
  <read_first>
    src/server/db/scenario-repository.ts (ScenarioRow interface, lines 17-33 — isCurated/rationale fields)
    .planning/phases/05-save-share-gallery/05-CONTEXT.md (D-02 — banner label wording)
  </read_first>
  <behavior>
    - Test: `buildScenarioBanner({ isCurated: true, rationale: "x" })` returns `{ label: "Gallery example", rationale: "x" }`.
    - Test: `buildScenarioBanner({ isCurated: true, rationale: null })` returns `{ label: "Viewing saved scenario — drag to explore" }` (no `rationale` key) — curated but missing rationale still falls back to the generic label.
    - Test: `buildScenarioBanner({ isCurated: false, rationale: null })` returns `{ label: "Viewing saved scenario — drag to explore" }`.
  </behavior>
  <action>
    Create src/lib/trpc/banner.ts exporting `export function buildScenarioBanner(row: { isCurated: boolean; rationale: string | null }): { label: string; rationale?: string }` implementing exactly the three cases in the behavior block (curated-with-rationale returns the rationale; every other combination returns the generic label with no `rationale` key). Create src/lib/trpc/banner.test.ts covering the three cases with plain vitest `describe`/`it`/`expect` (pure function, no jsdom pragma needed).
  </action>
  <acceptance_criteria>
    - buildScenarioBanner is a pure function with no imports from react/next/prisma
    - `npm test -- src/lib/trpc/banner.test.ts` exits 0 covering all 3 cases
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/lib/trpc/banner.test.ts</automated>
  </verify>
  <done>banner.ts exports buildScenarioBanner matching the 3 documented cases; all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Build the share/gallery-detail page and Copy Link button</name>
  <files>app/s/[shareId]/page.tsx, src/components/sandbox/CopyLinkButton.tsx</files>
  <read_first>
    src/server/api/routers/scenario.ts (lines 23-32, NOT_FOUND TRPCError contract)
    src/server/application/scenario-service.ts (lines 38-56, rowToVessels — reuse, do not re-derive)
    src/lib/trpc/server.ts (Plan 05-01 output, getCaller())
    src/lib/trpc/banner.ts (Task 1 output)
    src/components/sandbox/SandboxContainer.tsx (Plan 05-03 output — initialScenario/banner prop shape)
    app/page.tsx (route-file minimal shape)
  </read_first>
  <action>
    Create app/s/[shareId]/page.tsx as `async function SharedScenarioPage({ params }: { params: Promise<{ shareId: string }> })` (Next 16's `params` is a Promise — must `await params`). Import `notFound` from "next/navigation", `TRPCError` from "@trpc/server", `getCaller` from "../../../src/lib/trpc/server.js", `rowToVessels` from "../../../src/server/application/scenario-service.js", `buildScenarioBanner` from "../../../src/lib/trpc/banner.js", `SandboxContainer` from "../../../src/components/sandbox/SandboxContainer.js", and `CopyLinkButton` from "../../../src/components/sandbox/CopyLinkButton.js".

    `await params` to get `shareId`; call `getCaller().scenario.get({ shareId })` inside a try/catch — on `err instanceof TRPCError && err.code === "NOT_FOUND"`, call `notFound()`; otherwise rethrow. Build `initialScenario` via `rowToVessels(scenario)`. Build `banner` via `buildScenarioBanner(scenario)`. Render `<SandboxContainer key={shareId} initialScenario={initialScenario} banner={banner} />` (the `key={shareId}` is mandatory — Pitfall 1, forces remount across client-side navigation between two different shareIds) plus `<CopyLinkButton />` positioned near the banner.

    Create src/components/sandbox/CopyLinkButton.tsx ("use client" as the first line): a button styled with the existing `rounded border border-slate-200 px-2 py-1` input-adjacent convention (secondary styling, not the primary teal-600 used by Save/Reset) that calls `navigator.clipboard.writeText(window.location.href)` on click and shows "Copied!" text for ~2 seconds via a local `useState` toggle.
  </action>
  <acceptance_criteria>
    - page.tsx awaits `params` before destructuring `shareId`
    - page.tsx calls `notFound()` only inside the `TRPCError`+`NOT_FOUND` branch, and rethrows any other error
    - `<SandboxContainer>` receives `key={shareId}`
    - page.tsx reuses `rowToVessels` from scenario-service.ts rather than re-deriving the vesselA/vesselB field mapping inline
    - CopyLinkButton.tsx starts with `"use client";` and calls `navigator.clipboard.writeText`
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The route file compiles; a request for a valid shareId renders the sandbox pre-seeded with its vessels; a request for a bogus shareId triggers notFound().</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end verification of the save/share/gallery flow</name>
  <files>N/A -- verification-only task, no files created or modified</files>
  <action>
    Start the local stack and walk through the full save/share/gallery loop yourself, then report results: `docker compose up -d`, `npx prisma db seed` (once, against a fresh database), `npm run dev`, then perform the 8 steps in how-to-verify below in a browser.
  </action>
  <what-built>Save button (Plan 05-03) -> /s/[shareId] detail page (this plan) -> /gallery listing (Plan 05-04), backed by the Plan 05-02 seed script and Plan 05-01's tRPC wiring.</what-built>
  <how-to-verify>
    1. Ensure Postgres is running (`docker compose up -d`) and the seed has been applied (`npx prisma db seed` — safe to run once against a fresh database; re-running creates duplicate curated rows, which is fine for this manual check but not for a production deploy).
    2. Run `npm run dev` and open `http://localhost:3000/`.
    3. Drag a vessel to change the encounter, click "Save". Confirm the browser navigates to a `/s/<id>` URL and the same encounter (post-drag) is displayed with a "Viewing saved scenario — drag to explore" banner.
    4. Click "Copy Link", confirm a "Copied!" acknowledgement appears and the clipboard contains the current URL (paste it somewhere to confirm).
    5. Open `http://localhost:3000/gallery`. Confirm 6 cards render, each with an encounter-type badge and rationale text matching Plan 05-02's authored copy.
    6. Click a gallery card. Confirm it navigates to `/s/<that row's id>` and shows a "Gallery example" banner with the card's rationale text, and that the head-on card's doubt-caveat banner still renders (Pitfall 2 — expected domain behavior, not a bug).
    7. Visit `http://localhost:3000/s/this-id-does-not-exist`. Confirm Next's 404 page renders (not a crash, not a blank page).
    8. Click between two different gallery cards in the same browser tab (client-side navigation). Confirm the chart updates to the NEW scenario's vessels each time (Pitfall 1 regression check).
  </how-to-verify>
  <verify>
    <automated>MISSING -- this task exercises full-stack Next.js SSR routes (dynamic params, notFound(), Clipboard API) with no e2e test runner (e.g. Playwright) installed in this project; human-verify is the automation-first fallback per GSD's checkpoint guidance until an e2e framework is a deliberate future addition</automated>
  </verify>
  <resume-signal>Type "approved" once all 8 steps behave as described, or describe which step failed.</resume-signal>
  <done>All 8 steps in how-to-verify behave as described, confirmed by the human reviewer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Anonymous browser -> /s/[shareId] | Any valid id publicly viewable by design (V4 Access Control accepted — SCEN-01's explicit "no login required") |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | Malformed/garbage `shareId` path segment reaching `prisma.findUnique` | mitigate | Already handled by Prisma's parameterized query generation (returns `null` -> `notFound()`, never raw SQL, T-3-03 carried forward from Phase 3) |
| T-05-09 | Information Disclosure | Verbose Prisma/tRPC error internals leaking through a rethrown non-NOT_FOUND error | mitigate | The existing `errorFormatter` in src/server/api/trpc.ts already strips Prisma details in production (T-3-07, unchanged); this page's rethrow flows through the same tRPC caller |
</threat_model>

<verification>
`npm test -- src/lib/trpc/banner.test.ts` passes; `npx tsc --noEmit` passes; the human-verify checkpoint confirms all 8 steps of the full save/share/gallery flow.
</verification>

<success_criteria>
- /s/[shareId] loads a valid scenario into the editable sandbox with a correct banner.
- /s/[bogus-id] renders Next's 404.
- Client-side navigation between two share pages remounts the sandbox (Pitfall 1).
- The full save -> share -> gallery -> detail loop works end-to-end, human-confirmed.
</success_criteria>

<output>
Create `.planning/phases/05-save-share-gallery/05-05-SUMMARY.md` when done
</output>
