---
phase: 05-save-share-gallery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/trpc/client.tsx, src/lib/trpc/server.ts, app/layout.tsx]
autonomous: true
requirements: [SCEN-01, SCEN-03]

must_haves:
  truths:
    - "A client-side tRPC provider is mounted once at the app root, making trpc.*.useMutation()/useQuery() callable from any client component."
    - "A server-side tRPC caller is available for Server Components to call scenario.get/gallery.list with no HTTP round-trip."
  artifacts:
    - path: src/lib/trpc/client.tsx
      provides: "createTRPCReact<AppRouter>() instance (`trpc`) + TRPCReactProvider component"
      exports: [trpc, TRPCReactProvider]
    - path: src/lib/trpc/server.ts
      provides: "getCaller() server-side tRPC caller factory wrapper"
      exports: [getCaller]
  key_links:
    - from: app/layout.tsx
      to: src/lib/trpc/client.tsx
      via: "<TRPCReactProvider> wrapping {children}"
      pattern: "TRPCReactProvider"
---

<objective>
Stand up the first-ever client-side tRPC/React Query wiring and a reusable server-side tRPC caller. This is pure plumbing consumed by later plans (SandboxContainer's Save mutation, the /s/[shareId] and /gallery server components) — no user-visible behavior changes yet.

Purpose: Every downstream plan in this phase (05-03 Save flow, 05-04 gallery page, 05-05 share page) needs one of these two entry points; building them first, as a fixed contract, avoids each downstream plan reinventing its own tRPC access pattern.
Output: src/lib/trpc/client.tsx, src/lib/trpc/server.ts, and app/layout.tsx wired to mount the client provider.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-save-share-gallery/05-CONTEXT.md
@.planning/phases/05-save-share-gallery/05-RESEARCH.md
</context>

<interfaces>
From src/server/api/routers/_app.ts (existing, unchanged):
```typescript
export const appRouter = createTRPCRouter({ scenario: scenarioRouter, gallery: galleryRouter });
export type AppRouter = typeof appRouter;
```

From src/server/api/trpc.ts (existing, unchanged):
```typescript
export function createTRPCContext() {
  return { scenarioService: { createScenario, getScenario, listGallery } };
}
export const createTRPCRouter = t.router;
export const publicProcedure = t.procedure;
export const createCallerFactory = t.createCallerFactory;
```

Proven server-side caller shape (src/server/api/routers/scenario.test.ts, lines 10-15):
```typescript
import { createCallerFactory, createTRPCContext } from "../trpc.js";
import { appRouter } from "./_app.js";
const createCaller = createCallerFactory(appRouter);
const caller = createCaller(createTRPCContext());
```

Installed dependency versions (package.json — do NOT introduce anything not on this list):
`@trpc/react-query@11.18.0`, `@trpc/client@11.18.0`, `@trpc/server@11.18.0`, `@tanstack/react-query@5.101.2`. `@trpc/tanstack-react-query` is NOT installed — never import it.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Define tRPC client provider and server caller contracts</name>
  <files>src/lib/trpc/client.tsx, src/lib/trpc/server.ts</files>
  <read_first>
    src/server/api/routers/_app.ts (AppRouter type export, line 16)
    src/server/api/trpc.ts (createTRPCRouter/publicProcedure/createCallerFactory/createTRPCContext exports, lines 19-40)
    src/server/api/routers/scenario.test.ts (lines 10-15, proven createCallerFactory(appRouter)(createTRPCContext()) call shape)
    package.json (confirm @trpc/react-query@11.18.0, @trpc/client@11.18.0, @tanstack/react-query@5.101.2 — do NOT import @trpc/tanstack-react-query, not installed)
  </read_first>
  <action>
    Create src/lib/trpc/client.tsx (new file, "use client" as the literal first line). Call `createTRPCReact<AppRouter>()` (imported from "@trpc/react-query") and export the result as `trpc`, importing the `AppRouter` type from "../../server/api/routers/_app.js". Export `TRPCReactProvider({ children }: { children: React.ReactNode })`: build a `makeQueryClient()` helper using a real `import { QueryClient } from "@tanstack/react-query"` (not a runtime `require`); memoize the client-side instance in a module-level `let clientQueryClientSingleton: QueryClient | undefined` guarded by `typeof window === "undefined"` (server always gets a fresh instance, browser reuses one across renders); inside the component, get the query client via that helper and build the tRPC client with `trpc.createClient({ links: [httpBatchLink({ url: getUrl() })] })` (imported from "@trpc/client") inside a `useState` lazy initializer; render `<trpc.Provider client={trpcClient} queryClient={queryClient}><QueryClientProvider client={queryClient}>{children}</QueryClientProvider></trpc.Provider>`. `getUrl()` returns `"/api/trpc"` when `typeof window !== "undefined"`, else `"http://localhost:3000/api/trpc"`.

    Create src/lib/trpc/server.ts (new file, plain .ts, no "use client"). Export `getCaller()` returning `createCallerFactory(appRouter)(createTRPCContext())`, importing `appRouter` from "../../server/api/routers/_app.js" and `createTRPCContext`/`createCallerFactory` from "../../server/api/trpc.js". Do NOT import the "server-only" package — it is not an installed dependency (absent from package.json/package-lock.json) and this file is exclusively imported by Server Components in this codebase already, so the "never bundled to the client" guarantee holds without adding an unaudited dependency.
  </action>
  <acceptance_criteria>
    - src/lib/trpc/client.tsx starts with the exact string `"use client";` on line 1
    - src/lib/trpc/client.tsx exports `trpc` (created via `createTRPCReact<AppRouter>()`) and `TRPCReactProvider`
    - src/lib/trpc/client.tsx contains no import of `@trpc/tanstack-react-query`
    - src/lib/trpc/server.ts exports a function `getCaller` that calls `createCallerFactory(appRouter)`
    - src/lib/trpc/server.ts contains no import of the string `server-only`
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Both files exist, export the contracts above, and `npx tsc --noEmit` exits 0 with no new errors.</done>
</task>

<task type="auto">
  <name>Task 2: Mount TRPCReactProvider at the app root</name>
  <files>app/layout.tsx</files>
  <read_first>
    app/layout.tsx (current 18-line file)
    src/lib/trpc/client.tsx (Task 1 output, this task's dependency)
  </read_first>
  <action>
    In app/layout.tsx, import `TRPCReactProvider` from "../src/lib/trpc/client.js" (matches app/page.tsx's existing "../src/..." relative-import convention) and wrap the existing `{children}` inside `<body className="bg-slate-50 min-h-screen">` with `<TRPCReactProvider>{children}</TRPCReactProvider>`. Do not modify the `metadata` export, the `<html lang="en">` element, or the `body` className.
  </action>
  <acceptance_criteria>
    - app/layout.tsx imports TRPCReactProvider from "../src/lib/trpc/client.js"
    - app/layout.tsx renders `<TRPCReactProvider>` as the sole child of `<body>`, wrapping `{children}`
    - the `metadata` export and `<html lang="en">` element are unchanged from the current file
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npm test</automated>
  </verify>
  <done>`npm run dev` boots without a runtime error (manually confirmed by the executor loading `/` once) and typecheck/tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> /api/trpc | Existing Phase 3 endpoint; this plan adds its first client-side consumer (httpBatchLink) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | httpBatchLink request body | mitigate | Already handled by tRPC + Zod input schemas at the existing router boundary (Phase 3) — no new input surface added by this plan |
| T-05-02 | Information Disclosure | Client-side React Query cache | accept | In-memory only, no persistence to disk/localStorage; scenario data is not sensitive by design (SCEN-01 is deliberately no-login/public-by-link) |
</threat_model>

<verification>
`npx tsc --noEmit` passes with both new files present; `npm test` (full suite) stays green; `npm run dev` boots and `/` still renders (manual smoke check, no route behavior changed yet).
</verification>

<success_criteria>
- src/lib/trpc/client.tsx and src/lib/trpc/server.ts exist and export the documented contracts.
- app/layout.tsx mounts TRPCReactProvider at the root with no other changes.
- No existing test regresses.
</success_criteria>

<output>
Create `.planning/phases/05-save-share-gallery/05-01-SUMMARY.md` when done
</output>
