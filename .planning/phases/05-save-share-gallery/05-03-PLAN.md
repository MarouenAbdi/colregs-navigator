---
phase: 05-save-share-gallery
plan: 03
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [src/components/sandbox/SandboxContainer.tsx, src/components/sandbox/SandboxContainer.test.tsx, src/components/sandbox/types.ts]
autonomous: true
requirements: [SCEN-01]

must_haves:
  truths:
    - "SandboxContainer can be seeded from a saved scenario's vessels instead of the hardcoded default fixture, and a visible banner communicates that a saved/shared scenario is loaded (D-02)."
    - "Resetting a saved/shared scenario returns to that scenario's own original vessels, not the app's unrelated default demo scenario (Assumption A3)."
    - "Clicking Save persists the current vesselA/vesselB via scenario.create and navigates to the resulting share URL, with no login step (SCEN-01)."
  artifacts:
    - path: src/components/sandbox/SandboxContainer.tsx
      provides: "initialScenario?/banner? props, Save button wired to trpc.scenario.create.useMutation()"
  key_links:
    - from: src/components/sandbox/SandboxContainer.tsx
      to: "/api/trpc (scenario.create)"
      via: "trpc.scenario.create.useMutation()"
      pattern: "scenario\\.create\\.useMutation"
    - from: src/components/sandbox/SandboxContainer.tsx
      to: "/s/[shareId]"
      via: "router.push on mutation onSuccess"
      pattern: "router\\.push"
---

<objective>
Extend Phase 4's `SandboxContainer` — without building a second read-only rendering mode (D-02 explicitly rejects that) — with an optional seed-from-saved-scenario prop and a Save button that persists the current encounter via `scenario.create` and redirects to its share URL.

Purpose: this is the ONE component both the plain "/" route and the new "/s/[shareId]" route (Plan 05-05) will render; extending it in place is the literal reuse the phase goal calls for.
Output: SandboxContainer accepts `initialScenario`/`banner` props, resets to the loaded scenario (not the global default) when one was provided, and exposes a working Save -> redirect flow.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-save-share-gallery/05-CONTEXT.md
@.planning/phases/05-save-share-gallery/05-RESEARCH.md
@.planning/phases/05-save-share-gallery/05-PATTERNS.md
</context>

<interfaces>
From src/lib/trpc/client.tsx (Plan 05-01 output — this plan's dependency):
```typescript
export const trpc: ReturnType<typeof createTRPCReact<AppRouter>>;
export function TRPCReactProvider({ children }: { children: React.ReactNode }): JSX.Element;
```

From src/server/api/routers/scenario.ts (existing, unchanged):
```typescript
const CreateScenarioInput = z.object({ vesselA: VesselSchema, vesselB: VesselSchema });
create: publicProcedure.input(CreateScenarioInput).mutation(...) // returns { shareId: string }
```

Current SandboxContainer.tsx state shape (exact lines to change):
- Lines 33-34: `useState<Vessel>(crossingResidualBasicCase.vesselA)` / `.vesselB`
- Lines 41-49: `lastGoodClassification` lazy initializer, calls `classifyEncounter(crossingResidualBasicCase.vesselA, crossingResidualBasicCase.vesselB)`
- Lines 114-122: `handleReset()`, resets to `crossingResidualBasicCase.vesselA`/`.vesselB`
- Lines 124-135: `<header>` containing the existing "Reset Scenario" button (`bg-teal-600 text-white px-4 py-2 rounded`)
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add initialScenario/banner props and seed-aware reset</name>
  <files>src/components/sandbox/SandboxContainer.tsx, src/components/sandbox/SandboxContainer.test.tsx, src/components/sandbox/types.ts</files>
  <read_first>
    src/components/sandbox/SandboxContainer.tsx (current 160-line file)
    src/components/sandbox/types.ts (existing *Props naming convention)
    src/components/sandbox/SandboxContainer.test.tsx (current test file, jsdom polyfill pattern lines 33-62, DEFAULT_VERDICT_TEXT constant)
    src/domain/vessel/vessel.ts (Vessel type)
    src/domain/colregs/classify-encounter.fixtures.ts (overtakingBothDirectionsCase — used as a distinct-from-default fixture in the new test)
  </read_first>
  <behavior>
    - Test: `render(<SandboxContainer initialScenario={{ vesselA: overtakingBothDirectionsCase.vesselA, vesselB: overtakingBothDirectionsCase.vesselB }} />)` shows an "Overtaking" verdict banner text derived from the PASSED-IN vessels, not the default crossing scenario — proving the seed source changed, not just the fixture.
    - Test: clicking "Reset Scenario" when `initialScenario` was provided restores THAT scenario's vessels (verdict returns to the overtaking verdict after a drag away from it), not `crossingResidualBasicCase`'s crossing verdict.
    - Test: the existing "loads with the default crossing scenario" and "restores the default scenario... when Reset Scenario is clicked" tests (current file, no props passed) continue to pass unchanged — regression coverage that the `initialScenario`-absent path still falls back to `crossingResidualBasicCase`.
    - Test: `render(<SandboxContainer banner={{ label: "Viewing saved scenario — drag to explore" }} />)` renders that exact label text somewhere in the document; when `banner.rationale` is also provided, that rationale text renders too.
  </behavior>
  <action>
    Add `export interface SandboxContainerProps { initialScenario?: { vesselA: Vessel; vesselB: Vessel }; banner?: { label: string; rationale?: string } }` to src/components/sandbox/types.ts (matching the existing `*Props` naming convention already used by `ChartPanelProps`/`ControlPanelProps`/`ReasoningPanelProps`). Change SandboxContainer's signature to `SandboxContainer({ initialScenario, banner }: SandboxContainerProps)`.

    Introduce two local consts immediately inside the component body, before the existing `useState` calls (current lines 33-34): `const seedA = initialScenario?.vesselA ?? crossingResidualBasicCase.vesselA;` and `const seedB = initialScenario?.vesselB ?? crossingResidualBasicCase.vesselB;`. Replace the two `useState` initializers with `seedA`/`seedB`, and replace BOTH hardcoded `crossingResidualBasicCase.vesselA`/`.vesselB` references inside the `lastGoodClassification` lazy initializer (current lines 41-49) with `seedA`/`seedB`.

    In `handleReset` (current lines 114-122), replace both `crossingResidualBasicCase.vesselA`/`.vesselB` references with `seedA`/`seedB` (Assumption A3 — Reset returns to the scenario THIS instance loaded with; when no `initialScenario` was passed, `seedA`/`seedB` already equal `crossingResidualBasicCase.vesselA`/`.vesselB`, so the plain "/" route's behavior is unchanged).

    In the `<header>` (current lines 124-135), render a banner element below the existing `<h1>`/button row when `banner` is truthy: a `<div>` showing `banner.label`, and `banner.rationale` immediately below it when present, styled `bg-slate-100 text-slate-700 rounded px-3 py-2 text-sm` (the existing neutral-slate palette — do not reuse `teal-*`, that color is reserved for primary actions in this file's convention).
  </action>
  <acceptance_criteria>
    - SandboxContainer accepts `initialScenario`/`banner` optional props without changing the signature of any existing prop
    - `useState<Vessel>(crossingResidualBasicCase.vesselA)` no longer appears verbatim in SandboxContainer.tsx — replaced by a `seedA`/`seedB`-derived initializer
    - `handleReset` no longer references `crossingResidualBasicCase` directly — it uses `seedA`/`seedB`
    - `npm test -- src/components/sandbox/SandboxContainer.test.tsx` exits 0, including the 4 new/regression cases above
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/components/sandbox/SandboxContainer.test.tsx</automated>
  </verify>
  <done>All SandboxContainer.test.tsx tests pass, covering both the initialScenario-provided path and the no-props regression path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the Save button to scenario.create and redirect to the share URL</name>
  <files>src/components/sandbox/SandboxContainer.tsx, src/components/sandbox/SandboxContainer.test.tsx</files>
  <read_first>
    src/components/sandbox/SandboxContainer.tsx (Task 1's output — the header block and vesselA/vesselB state this task reads from)
    src/lib/trpc/client.tsx (Plan 05-01 output — the `trpc` export this task imports)
    src/server/api/routers/scenario.ts (lines 12-22, CreateScenarioInput shape and the `{ shareId: string }` mutation return)
  </read_first>
  <behavior>
    - Test: clicking the "Save" button calls a mutation with `{ vesselA, vesselB }` matching current component state (mock `trpc.scenario.create.useMutation` to capture the call args).
    - Test: on mutation success (`onSuccess` receiving `{ shareId }`), the mocked `next/navigation` `useRouter().push` is called with `/s/${shareId}`.
    - Test: while the mutation is pending (mocked `isPending: true`), the Save button is disabled (prevents duplicate submissions).
  </behavior>
  <action>
    Add a "Save" `<button>` in the `<header>` next to "Reset Scenario", reusing the exact `bg-teal-600 text-white px-4 py-2 rounded` class already used by that button. Import `trpc` from "../../lib/trpc/client.js" and `useRouter` from "next/navigation". At the top of the component body: `const router = useRouter();` and `const createScenario = trpc.scenario.create.useMutation({ onSuccess: ({ shareId }) => router.push(\`/s/${shareId}\`) });`. The Save button's `onClick` calls `createScenario.mutate({ vesselA, vesselB })`; set `disabled={createScenario.isPending}` on the button.

    In the test file, mock "../../lib/trpc/client.js"'s `trpc.scenario.create.useMutation` (via `vi.mock`) to return a controllable stub object (`{ mutate: vi.fn(), isPending: false }`), and mock `next/navigation`'s `useRouter` to return `{ push: vi.fn() }`. Assert on both mocks per the behavior block above.
  </action>
  <acceptance_criteria>
    - SandboxContainer.tsx imports `trpc` from "../../lib/trpc/client.js" and `useRouter` from "next/navigation"
    - the Save button's onClick calls `createScenario.mutate({ vesselA, vesselB })`
    - `onSuccess` calls `router.push` with a string matching `/s/${shareId}`
    - `npm test -- src/components/sandbox/SandboxContainer.test.tsx` exits 0
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/components/sandbox/SandboxContainer.test.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Save button exists, wired to scenario.create, and mocked tests prove both the mutation call args and the post-success redirect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser -> scenario.create (via the new Save button) | Existing Phase 3 endpoint; this plan adds its first UI trigger reachable by ordinary users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Denial of Service | Save button makes the pre-existing unauthenticated `scenario.create` endpoint reachable by ordinary users, not just API testers | accept | Documented, existing Phase 3 no-auth exposure per RESEARCH.md Security Domain; rate limiting is explicitly out of this phase's scope |
| T-05-06 | Repudiation / Tampering | No ownership tracking on created scenarios (D-01) | accept | By design — no-auth constraint, PROJECT.md Out of Scope (no user accounts) |
</threat_model>

<verification>
`npm test -- src/components/sandbox/SandboxContainer.test.tsx` passes all cases (existing + 7 new/regression); `npx tsc --noEmit` passes.
</verification>

<success_criteria>
- SandboxContainer renders correctly with and without `initialScenario`/`banner` props.
- Reset returns to the loaded scenario, not an unrelated default, when one was provided.
- Save calls scenario.create and redirects to `/s/${shareId}` on success.
</success_criteria>

<output>
Create `.planning/phases/05-save-share-gallery/05-03-SUMMARY.md` when done
</output>
