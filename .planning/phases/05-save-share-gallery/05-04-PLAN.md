---
phase: 05-save-share-gallery
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified: [app/gallery/page.tsx]
autonomous: true
requirements: [SCEN-03]

must_haves:
  truths:
    - "A user visiting /gallery sees a card per curated scenario with its encounter type and rationale text (SCEN-03)."
    - "Each gallery card links to the same /s/[shareId] detail route a plain shared link uses (D-02/D-03 — no duplicate detail page)."
  artifacts:
    - path: app/gallery/page.tsx
      provides: "server component rendering gallery.list() as a card grid"
  key_links:
    - from: app/gallery/page.tsx
      to: src/lib/trpc/server.ts
      via: "getCaller().gallery.list()"
      pattern: "gallery\\.list"
    - from: app/gallery/page.tsx
      to: "/s/[shareId]"
      via: "<Link href={`/s/${row.id}`}>"
      pattern: "s/\\$\\{row"
---

<objective>
Build the `/gallery` route: a Server Component that lists all curated scenarios via `gallery.list()` and links each card to the same `/s/[shareId]` detail route a plain shared link uses — no separate gallery-detail page, per D-02/D-03's literal reuse requirement.

Purpose: this is the browsing entry point for SCEN-03's curated gallery, consuming Plan 05-02's seeded data and Plan 05-01's server-side tRPC caller.
Output: app/gallery/page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-save-share-gallery/05-CONTEXT.md
@.planning/phases/05-save-share-gallery/05-RESEARCH.md
</context>

<interfaces>
From src/lib/trpc/server.ts (Plan 05-01 output):
```typescript
export function getCaller(): ReturnType<typeof appRouter.createCaller>;
```

From src/server/api/routers/gallery.ts (existing, unchanged):
```typescript
export const galleryRouter = createTRPCRouter({
  list: publicProcedure.query(async ({ ctx }) => ctx.scenarioService.listGallery()),
});
```

`listGallery()` return shape (src/server/application/scenario-service.ts, lines 102-117): `Array<ScenarioRow & { verdict: ClassificationResult }>`, ordered by `displayOrder` ascending (src/server/db/scenario-repository.ts, lines 63-68, `findCurated()`). `ScenarioRow` (src/server/db/scenario-repository.ts, lines 17-33) includes `id`, `isCurated`, `rationale`, `displayOrder`. `ClassificationResult.encounterType` (src/domain/colregs/types.ts, line 74) is `"head-on" | "crossing" | "overtaking"`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Build the gallery listing page</name>
  <files>app/gallery/page.tsx</files>
  <read_first>
    app/page.tsx (only existing route, minimal file shape to match)
    src/server/api/routers/gallery.ts (full 13-line file — `list` query contract, never throws, D-07)
    src/server/application/scenario-service.ts (lines 102-117, listGallery()'s return shape)
    src/server/db/scenario-repository.ts (lines 63-68, findCurated()'s displayOrder ordering)
  </read_first>
  <action>
    Create app/gallery/page.tsx as an `async function GalleryPage()` server component (no "use client" directive). Import `getCaller` from "../../src/lib/trpc/server.js" and `Link` from "next/link". Call `const scenarios = await getCaller().gallery.list();` (no try/catch needed — this query never throws, D-07).

    Wrap the page in `<main className="flex flex-col gap-8 p-16">` (matching SandboxContainer's top-level layout convention) with an `<h1 className="text-2xl font-semibold">Curated Encounters</h1>` heading. Render a responsive card grid `<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">` mapping over `scenarios`; each card is `<Link href={`/s/${row.id}`} key={row.id}>` wrapping `<div className="rounded border border-slate-200 p-4 flex flex-col gap-2 hover:border-teal-600">` containing an encounter-type badge (`<span className="text-xs font-semibold uppercase text-teal-700">{row.verdict.encounterType}</span>`) and the rationale text (`<p className="text-sm text-slate-700">{row.rationale}</p>`). If `scenarios.length === 0`, render `<p>No curated scenarios yet — run the seed script.</p>` instead of an empty grid.
  </action>
  <acceptance_criteria>
    - app/gallery/page.tsx has no "use client" directive (stays a Server Component)
    - app/gallery/page.tsx calls `getCaller().gallery.list()` exactly once
    - each rendered card wraps a `<Link href={`/s/${row.id}`}>`
    - a zero-scenario fallback message renders when `scenarios.length === 0`
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>The page file compiles, renders one card per curated row (after Plan 05-02's seed has run), and each card links to /s/[shareId].</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Anonymous browser -> /gallery | Public, no auth, by design (V4 Access Control accepted per RESEARCH.md Security Domain) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Information Disclosure | gallery.list() exposes all curated rows' full vessel geometry to any visitor | accept | Literal SCEN-03 product requirement (a public curated gallery) — no sensitive data involved |
</threat_model>

<verification>
`npx tsc --noEmit` passes; after Plan 05-02's seed has run against the local Postgres, `npm run dev` + visiting `/gallery` shows 6 cards, each linking to `/s/[shareId]`.
</verification>

<success_criteria>
- /gallery renders a card per curated scenario with encounter-type badge and rationale.
- Every card links to /s/[shareId].
- Empty-gallery fallback renders correctly when no curated rows exist.
</success_criteria>

<output>
Create `.planning/phases/05-save-share-gallery/05-04-SUMMARY.md` when done
</output>
