---
phase: 05-save-share-gallery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [src/server/db/curated-scenarios.ts, src/server/db/curated-scenarios.test.ts, prisma/seed.ts, prisma.config.ts, src/server/api/routers/scenario.test.ts]
autonomous: true
requirements: [SCEN-03]

must_haves:
  truths:
    - "Running the seed script populates the Scenario table with 6 curated rows covering head-on, give-way crossing, stand-on crossing, overtaking, and Rule 18 vessel-type-priority encounter types (D-04)."
    - "gallery.list() returns curated rows with a freshly recomputed verdict and a non-empty rationale string for each (SCEN-03, SCEN-02)."
  artifacts:
    - path: src/server/db/curated-scenarios.ts
      provides: "exported `curatedScenarios` array (vesselA/vesselB/rationale/displayOrder per entry)"
    - path: prisma/seed.ts
      provides: "one-time script writing curatedScenarios into the Scenario table via Prisma, bypassing scenario.create (D-03)"
  key_links:
    - from: prisma/seed.ts
      to: src/server/db/client.ts
      via: "reuses the existing `prisma` singleton (Pitfall 4)"
      pattern: "prisma/client"
    - from: prisma/seed.ts
      to: src/domain/colregs/classify-encounter.ts
      via: "dry-run classifyEncounter() validation before persisting each entry"
      pattern: "classifyEncounter"
---

<objective>
Author the curated gallery's seed data (D-04's shape: one clean case per core rule, plus 1-2 Rule 18 examples) as a validated, tested data module, then write the Prisma seed script that writes it directly to the database — deliberately bypassing the public `scenario.create` mutation (D-03).

Purpose: `gallery.list()` currently returns `[]` because no curated rows exist (Phase 3 added the schema fields but deferred seeding to this phase). This plan is the first code to populate them.
Output: src/server/db/curated-scenarios.ts (+ test), prisma/seed.ts, prisma.config.ts updated with the seed command, and a fixed pre-existing test that assumed an empty gallery.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-save-share-gallery/05-CONTEXT.md
@.planning/phases/05-save-share-gallery/05-RESEARCH.md
</context>

<interfaces>
Fixtures this plan reuses verbatim, from src/domain/colregs/classify-encounter.fixtures.ts (none of these sets a `previous` field — all safe to seed as fresh reads; Pitfall 3: fixtures like `overtakingHysteresisHoldsCase` are NOT safe and must not be used here):
- `headOnGenuineCase` — head-on, expectedGiveWay/standOn: null (mutual obligation), expectedDoubt: true (Assumption A1 — every head-on case in this domain always carries doubt; this is by design, not a defect)
- `crossingResidualBasicCase` — crossing, both vessels power-driven (no Rule 18 involvement), expectedGiveWay: "vesselA", expectedStandOn: "vesselB", expectedDoubt: false. This plan uses it TWICE to build the D-04 give-way/stand-on mirror pair: once passed as `classifyEncounter(vesselA, vesselB)` in normal order (give-way crossing), and once with the SAME two vessel objects passed in SWAPPED order as `classifyEncounter(vesselB, vesselA)` (stand-on crossing) — verified directly in this session that swapping argument order flips the returned `giveWay`/`standOn` labels (`vesselA`<->`vesselB`) while the underlying physical geometry (positions/headings) is unchanged, giving a genuine mirror-view pair of the identical encounter rather than two unrelated fixtures.
- `overtakingBothDirectionsCase` — overtaking, expectedGiveWay: "vesselB", expectedStandOn: "vesselA", expectedDoubt: false
- `crossingRule18OverrideCase` — crossing, expectedGiveWay: "vesselB", expectedStandOn: "vesselA" (vesselA fishing, vesselB power-driven — Rule 18 flips the geometric baseline). Same underlying positions as `crossingResidualBasicCase` (fixtures file's own comment, line 360) but used here ONLY for the Rule 18 vessel-type-priority slot, not the plain crossing mirror pair, to avoid seeding the same geometry three times.
- `headOnRule18OverrideCase` — head-on, expectedGiveWay: "vesselB", expectedStandOn: "vesselA" (vesselA sailing, vesselB power-driven — Rule 18 breaks the mutual-obligation tie), expectedDoubt: true

ScenarioRow shape (src/server/db/scenario-repository.ts, lines 17-33) — the three curated-only fields `isCurated`/`displayOrder`/`rationale` that only this seed script may set.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author curated scenario data + coverage/validation test</name>
  <files>src/server/db/curated-scenarios.ts, src/server/db/curated-scenarios.test.ts</files>
  <read_first>
    src/domain/colregs/classify-encounter.fixtures.ts (full file, especially headOnGenuineCase lines 181-190, crossingResidualBasicCase lines 220-227, overtakingBothDirectionsCase lines 42-60, crossingRule18OverrideCase lines 364-376, headOnRule18OverrideCase lines 402-415)
    src/domain/colregs/classify-encounter.ts (Assumption A1 — head-on always doubt:true, Pitfall 2)
    src/domain/vessel/vessel.ts (Vessel/VesselType shape)
    .planning/phases/05-save-share-gallery/05-RESEARCH.md (Fixture Catalog table — safe vs. unsafe fixtures, Pitfall 3)
  </read_first>
  <behavior>
    - Test 1: `curatedScenarios.length` is >= 5 and <= 8 (SCEN-03's "5-8" range).
    - Test 2: every entry's `classifyEncounter(entry.vesselA, entry.vesselB)` returns `{ ok: true }` — proves no hysteresis-dependent fixture was reused (Pitfall 3).
    - Test 3: the resulting `encounterType` set (computed via classifyEncounter, not hand-copied) includes at least one "head-on", at least two "crossing" entries where one has `giveWay: "vesselA"` and another has `giveWay: "vesselB"` (give-way + mirror-view stand-on coverage per D-04), and at least one "overtaking". ADDITIONALLY, for those two crossing entries specifically, assert the genuine mirror-geometry property (not just that give-way differs): the give-way-"vesselA" entry's `vesselA.position`/`vesselA.heading` must deep-equal the give-way-"vesselB" entry's `vesselB.position`/`vesselB.heading`, AND the give-way-"vesselA" entry's `vesselB.position`/`vesselB.heading` must deep-equal the give-way-"vesselB" entry's `vesselA.position`/`vesselA.heading` — i.e., the two entries are the same physical encounter with vessel-slot labels swapped, not two different encounters that happen to both be crossings.
    - Test 4: at least 1 and at most 2 entries have `vesselA.type !== vesselB.type` AND that mismatch changes the give-way assignment vs. what a same-type baseline would produce (a genuine Rule 18 override), demonstrating D-04's "1-2 Rule 18 vessel-type-priority examples".
    - Test 5: every entry's `displayOrder` is a unique integer across the array, and every `rationale` is a non-empty string.
  </behavior>
  <action>
    Create src/server/db/curated-scenarios.ts exporting `interface CuratedScenario { vesselA: Vessel; vesselB: Vessel; rationale: string; displayOrder: number }` and `export const curatedScenarios: CuratedScenario[]` with exactly these 6 entries (displayOrder 0-5, in this order), each importing its vessel objects from "../../domain/colregs/classify-encounter.fixtures.js" (never re-typing vessel literals, so this file can never drift from already-tested fixture geometry):

    0. `headOnGenuineCase.vesselA`/`.vesselB` — rationale: "Vessel B is nearly dead ahead of Vessel A and closing — a clear head-on situation requiring both vessels to alter course to starboard."
    1. `crossingRule18OverrideCase.vesselA`/`.vesselB` — rationale: "The geometry alone would put Vessel A in the give-way role, but Vessel A is fishing and Vessel B is power-driven — Rule 18's vessel-type hierarchy overrides the baseline, so the power-driven Vessel B must give way to the fishing vessel."

    Wait — reorder for clarity below; the give-way/stand-on mirror pair must sit at slots 1 and 2 per D-04's ordering intent. Use this exact assignment instead:

    0. `headOnGenuineCase.vesselA`/`.vesselB` — rationale: "Vessel B is nearly dead ahead of Vessel A and closing — a clear head-on situation requiring both vessels to alter course to starboard."
    1. vesselA: `crossingResidualBasicCase.vesselA`, vesselB: `crossingResidualBasicCase.vesselB` (normal order — both power-driven, no Rule 18 involved) — rationale: "Vessel A has Vessel B fine on her own starboard side — a textbook Rule 15 crossing situation where Vessel A, as the give-way vessel, must take early and substantial action to keep well clear."
    2. vesselA: `crossingResidualBasicCase.vesselB`, vesselB: `crossingResidualBasicCase.vesselA` (SAME two vessel objects as entry 1, slots swapped — this is the genuine mirror view of that identical encounter, not a different fixture) — rationale: "This is the same crossing encounter viewed from the other vessel's perspective: Vessel A now holds the stand-on role and must hold course and speed, while Vessel B — the vessel crossing on Vessel A's own starboard side — is the one that must keep clear."
    3. `overtakingBothDirectionsCase.vesselA`/`.vesselB` — rationale: "Vessel B is closing on Vessel A from well abaft the beam at a higher speed — an overtaking situation where Vessel B, as the overtaking vessel, must keep clear until finally past and clear."
    4. `crossingRule18OverrideCase.vesselA`/`.vesselB` — rationale: "The geometry alone would put Vessel A in the give-way role, but Vessel A is fishing and Vessel B is power-driven — Rule 18's vessel-type hierarchy overrides the baseline, so the power-driven Vessel B must give way to the fishing vessel."
    5. `headOnRule18OverrideCase.vesselA`/`.vesselB` — rationale: "A head-on encounter would normally impose a mutual obligation on both vessels, but Vessel A is sailing and Vessel B is power-driven — Rule 18 breaks the tie, requiring the power-driven Vessel B to give way to the sailing vessel."

    (Ignore the stray duplicate numbering above the "Wait — reorder" line; it is not part of the final 6-entry list. The authoritative list is the second one: 0 head-on, 1 give-way crossing, 2 stand-on crossing (mirror of 1), 3 overtaking, 4 Rule 18 crossing override, 5 Rule 18 head-on override.)

    Do not confuse entry 2's construction with a new vessel literal: it reuses `crossingResidualBasicCase.vesselA` and `crossingResidualBasicCase.vesselB` verbatim, only assigning them to the opposite `CuratedScenario.vesselA`/`.vesselB` struct fields versus entry 1.

    Then write src/server/db/curated-scenarios.test.ts implementing the 5 behavior-block tests above, importing `classifyEncounter` from "../../domain/colregs/classify-encounter.js".
  </action>
  <acceptance_criteria>
    - `npm test -- src/server/db/curated-scenarios.test.ts` exits 0
    - src/server/db/curated-scenarios.ts imports all vessel objects from classify-encounter.fixtures.js rather than declaring new literal vessel objects
    - curatedScenarios.length is between 5 and 8 inclusive
    - Entries 1 and 2 use `crossingResidualBasicCase.vesselA`/`.vesselB` with slots swapped between them (not two different fixtures), and no rationale text claims a "mirror image" relationship that the underlying geometry does not actually have
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/server/db/curated-scenarios.test.ts</automated>
  </verify>
  <done>curated-scenarios.test.ts passes all 5 assertions (including the mirror-geometry check in Test 3); the data file exports exactly 6 entries with unique displayOrder 0-5.</done>
</task>

<task type="auto">
  <name>Task 2: Write the Prisma seed script and register it in prisma.config.ts</name>
  <files>prisma/seed.ts, prisma.config.ts</files>
  <read_first>
    src/server/db/client.ts (prisma singleton, Pitfall 4 — must reuse, never `new PrismaClient()` directly)
    src/server/db/scenario-repository.ts (lines 35-53, flat-column create() shape to mirror, plus the 3 curated-only fields it deliberately omits)
    src/server/application/scenario-service.ts (lines 62-71, createScenario's dry-run classifyEncounter guard pattern to mirror)
    prisma.config.ts (current 8-line file)
    src/server/db/curated-scenarios.ts (Task 1 output — the data this script writes)
  </read_first>
  <action>
    In prisma.config.ts, add `seed: "node prisma/seed.ts"` as a new key inside the existing `migrations: { path: "prisma/migrations" }` object, so it becomes `migrations: { path: "prisma/migrations", seed: "node prisma/seed.ts" }` — do not restructure any other key.

    Create prisma/seed.ts (new, ESM, matches package.json's `"type": "module"`): first line `import "dotenv/config";` (Pitfall 5 — do not rely on process-env inheritance from the parent `prisma` CLI subprocess). Import `prisma` from "../src/server/db/client.js" (the existing singleton — never construct a second `new PrismaClient()`, Pitfall 4). Import `curatedScenarios` from "../src/server/db/curated-scenarios.js". Import `classifyEncounter` from "../src/domain/colregs/classify-encounter.js". In an async `main()` function, loop over `curatedScenarios`; for each entry call `classifyEncounter(entry.vesselA, entry.vesselB)` as a dry-run guard (mirrors `createScenario`'s pattern) and `throw new Error(...)` including the entry's `displayOrder` and `result.reason` if `!result.ok` (never persist `result.value` — no verdict column exists, D-06). For each entry call `prisma.scenario.create({ data: {...} })` with the same flat-column mapping `scenario-repository.ts`'s `create()` uses (`vesselAPosX`/`vesselAPosY`/`vesselAHeading`/`vesselASpeed`/`vesselAType`, same for B) PLUS `isCurated: true`, `rationale: entry.rationale`, `displayOrder: entry.displayOrder` (the three curated-only fields the public repository function never sets). Call `main().then(() => prisma.$disconnect())` on success and `.catch(async (err) => { console.error(err); await prisma.$disconnect(); process.exit(1); })` on failure.
  </action>
  <acceptance_criteria>
    - prisma.config.ts's `migrations` object contains both `path: "prisma/migrations"` and `seed: "node prisma/seed.ts"`
    - prisma/seed.ts imports `prisma` from "../src/server/db/client.js" and contains no `new PrismaClient(` call
    - prisma/seed.ts calls `classifyEncounter` once per curated entry before that entry's `prisma.scenario.create` call
    - Running `npx prisma db seed` against the local Docker Postgres (`docker compose up -d` first) exits 0 and inserts exactly 6 rows with `isCurated: true`
  </acceptance_criteria>
  <verify>
    <automated>docker compose up -d && npx prisma db seed</automated>
  </verify>
  <done>`npx prisma db seed` exits 0; a follow-up `npm test -- src/server/api/routers/scenario.test.ts` (after Task 3 updates its stale assertion) still passes with the curated rows present in the shared dev database.</done>
</task>

<task type="auto">
  <name>Task 3: Fix the now-stale "gallery.list resolves to []" assumption</name>
  <files>src/server/api/routers/scenario.test.ts</files>
  <read_first>
    src/server/api/routers/scenario.test.ts (current 54-line file, especially the "gallery.list resolves to [] when no curated scenarios exist" test at lines 50-53)
    src/server/db/scenario-repository.test.ts (lines 68-85, findCurated()'s test — creating + `prisma.scenario.update`-ing a row to isCurated:true directly, then cleaning up in afterAll)
  </read_first>
  <action>
    Replace the "gallery.list resolves to [] when no curated scenarios exist" test — this assumption no longer holds once prisma/seed.ts has populated the shared dev database (Task 2) — with a test that: creates a scenario via `caller.scenario.create({ vesselA: headOnGenuineCase.vesselA, vesselB: headOnGenuineCase.vesselB })`, promotes it to curated via a direct `prisma.scenario.update({ where: { id: shareId }, data: { isCurated: true, displayOrder: 999, rationale: "test rationale" } })` call (mirroring scenario-repository.test.ts's findCurated test pattern, importing `prisma` from "../../db/client.js"), calls `caller.gallery.list()`, and asserts the result array contains an entry with that `shareId`, `isCurated: true`, `rationale: "test rationale"`, and a `verdict` object with a defined `encounterType`. Add an `afterAll` that deletes the created row via `prisma.scenario.deleteMany({ where: { id: shareId } })`. The test must tolerate OTHER curated rows already present (from a prior seed run) — assert containment (`.find(...)`/`.some(...)`), never exact array equality.
  </action>
  <acceptance_criteria>
    - scenario.test.ts no longer contains the string "resolves to []"
    - the new test creates its own curated row via `prisma.scenario.update` and cleans it up in `afterAll`
    - `npm test -- src/server/api/routers/scenario.test.ts` exits 0 regardless of whether prisma/seed.ts has already been run against the shared dev database
  </acceptance_criteria>
  <verify>
    <automated>npm test -- src/server/api/routers/scenario.test.ts</automated>
  </verify>
  <done>The updated test passes both before and after `npx prisma db seed` has populated additional curated rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Developer CLI -> Postgres (prisma/seed.ts) | Not internet-reachable; runs only via `npx prisma db seed` on a developer/deploy machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | prisma/seed.ts writes directly via Prisma Client, bypassing all tRPC/Zod input validation | accept | Deliberately privileged, developer-run, non-networked script (D-03) — never exposed as an HTTP endpoint; validated by its own dry-run `classifyEncounter()` guard before every write |
| T-05-04 | Denial of Service | Curated rows are schema-indistinguishable from user-created rows once written | accept | No new exposure — same accepted risk as Phase 3's unauthenticated `scenario.create` |
</threat_model>

<verification>
`npm test -- src/server/db/curated-scenarios.test.ts` and `npm test -- src/server/api/routers/scenario.test.ts` both pass; `npx prisma db seed` (against `docker compose up -d`'s Postgres) exits 0 and inserts 6 curated rows.
</verification>

<success_criteria>
- curated-scenarios.ts exports 6 validated entries covering D-04's required shape, with the give-way/stand-on crossing pair being a genuine mirror of the same underlying encounter.
- prisma/seed.ts writes them via the reused Prisma singleton, never touching the public mutation.
- The pre-existing "empty gallery" test assumption is fixed to tolerate seeded data.
</success_criteria>

<output>
Create `.planning/phases/05-save-share-gallery/05-02-SUMMARY.md` when done
</output>
