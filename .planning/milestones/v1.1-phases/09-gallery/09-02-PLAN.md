---
phase: 09-gallery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/shared/static-chart-geometry.ts
  - src/components/hero/hero-preview-geometry.ts
  - src/components/hero/HeroPreviewCard.tsx
  - src/components/sandbox/vessel-role.ts
  - src/components/gallery/gallery-preview-geometry.ts
  - src/components/gallery/gallery-preview-geometry.test.ts
  - src/components/gallery/GalleryPreviewChart.tsx
  - src/components/gallery/GalleryPreviewChart.test.tsx
autonomous: true
requirements: [GAL-01]
must_haves:
  truths:
    - "A parametrized static SVG mini-chart renders correctly for vessel pairs ranging from 1.0 NM to 10.0 NM apart, without distortion or clipping (D-06/D-07/D-08)"
    - "Hero's existing preview card is visually unchanged after the shared-geometry extraction (zero regression)"
    - "Hull/pill/heading-vector colors derive from the vessel's actual give-way/stand-on/mutual role, never a hardcoded per-slot color (Pattern 3 — required for the mutual-role cards Hero never needed)"
  artifacts:
    - path: "src/components/shared/static-chart-geometry.ts"
      provides: "headingVectorEndpoint(), midpoint(), HULL_PATH, HULL_STROKE, HULL_STROKE_WIDTH — extracted, second-consumer-justified per CLAUDE.md's shared-primitive convention"
    - path: "src/components/gallery/gallery-preview-geometry.ts"
      provides: "computeCardViewBox(a, b, aspectRatio, paddingFraction): ChartViewBox — per-card dynamic bounding-box sizing"
    - path: "src/components/gallery/GalleryPreviewChart.tsx"
      provides: "Parametrized static mini-chart component accepting vesselA/vesselB Vessel props"
  key_links:
    - from: "src/components/gallery/GalleryPreviewChart.tsx"
      to: "src/components/shared/static-chart-geometry.ts"
      via: "import of HULL_PATH/headingVectorEndpoint/midpoint"
      pattern: "from \"../shared/static-chart-geometry.js\""
    - from: "src/components/gallery/GalleryPreviewChart.tsx"
      to: "src/components/sandbox/vessel-role.ts"
      via: "getVesselRole()-derived hull/pill/stroke color classes"
      pattern: "getVesselRole\\("
---

<objective>
Extract the chart-drawing primitives Hero's preview card and Gallery's new mini-chart both need into `src/components/shared/`, satisfying CLAUDE.md's "extract only on a real second consumer" rule (D-09) now that Gallery is that second consumer — then build Gallery's parametrized, per-card-sized mini-chart component on top of the extracted primitives.

Purpose: this is genuinely new, reusable presentation-layer work with zero dependency on Phase 9's data-correctness fix (Plan 01) — it can proceed in the same wave, independently, and Plan 03 (the actual gallery cards) depends on this plan's `GalleryPreviewChart` component.
Output: `static-chart-geometry.ts` (shared), `gallery-preview-geometry.ts` (Gallery-local dynamic viewBox math), `GalleryPreviewChart.tsx` (the mini-chart), with Hero's existing card re-wired to the extracted shared file and otherwise visually unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/09-gallery/09-CONTEXT.md
@.planning/phases/09-gallery/09-RESEARCH.md
@.planning/phases/09-gallery/09-UI-SPEC.md
</context>

<interfaces>
From src/domain/geometry/screen-convert.ts (unchanged, both Hero and Gallery already depend on this):
```
export interface ContainerSize { width: number; height: number; }
export interface ChartViewBox { minX: number; minY: number; width: number; height: number; }
export function chartToScreen(position: Position, containerSize: ContainerSize, viewBox: ChartViewBox): { screenX: number; screenY: number };
```

From src/components/sandbox/vessel-role.ts (existing, to be extended in Task 1 — do not duplicate elsewhere):
```
export type VesselRole = "give-way" | "stand-on" | "mutual";
export function getVesselRole(vessel: VesselLabel, classification: ClassificationResult): VesselRole;
export const ROLE_HULL_FILL_CLASS: Record<VesselRole, string>; // "fill-give-way" | "fill-stand-on" | "fill-mutual"
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Extract static-chart-geometry.ts and add ROLE_STROKE_CLASS</name>
  <files>src/components/shared/static-chart-geometry.ts, src/components/hero/hero-preview-geometry.ts, src/components/hero/HeroPreviewCard.tsx, src/components/sandbox/vessel-role.ts</files>
  <read_first>
    src/components/hero/hero-preview-geometry.ts (whole file — 5 of its exports move, the rest stay)
    src/components/hero/HeroPreviewCard.tsx (its import block, lines 18-33, needs splitting across two source files)
    src/components/sandbox/vessel-role.ts (whole file — the existing `ROLE_HULL_FILL_CLASS` map is the pattern to mirror for the new `ROLE_STROKE_CLASS`)
  </read_first>
  <action>
    Create `src/components/shared/static-chart-geometry.ts`. Move these 5 exports OUT of `hero-preview-geometry.ts` and INTO this new file, verbatim, unchanged: `HULL_PATH`, `HULL_STROKE`, `HULL_STROKE_WIDTH`, `headingVectorEndpoint()`, `midpoint()`. Add a file-header doc comment stating these are shared between Hero's and Gallery's independent static-illustration components (the second-consumer justification for this extraction, per CLAUDE.md's "extract only on a real second consumer" rule and 09-CONTEXT.md D-09).

    In `hero-preview-geometry.ts`: remove the 5 moved exports. Keep everything else unchanged (`HERO_CONTAINER_SIZE`, `HERO_VIEW_BOX`, `HERO_CHART_CENTER`, `HERO_OUTER_RING_RADIUS_PX`, `HERO_INNER_RING_RADIUS_PX`, `VESSEL_A_HULL_COLOR`, `VESSEL_B_HULL_COLOR`, `CONNECTOR_STROKE`, `bearingSectorPath()`) — Gallery does not use rings/gradient/fixed-viewbox/slot-based colors, so none of these are candidates for extraction.

    In `HeroPreviewCard.tsx`: update its import block (currently lines 18-33) to import `headingVectorEndpoint`, `midpoint`, `HULL_PATH`, `HULL_STROKE`, `HULL_STROKE_WIDTH` from `../shared/static-chart-geometry.js`, and the remaining names (`HERO_CONTAINER_SIZE`, `HERO_VIEW_BOX`, `HERO_CHART_CENTER`, `HERO_OUTER_RING_RADIUS_PX`, `HERO_INNER_RING_RADIUS_PX`, `VESSEL_A_HULL_COLOR`, `VESSEL_B_HULL_COLOR`, `CONNECTOR_STROKE`, `bearingSectorPath`) from `./hero-preview-geometry.js` unchanged. Do not change any JSX or rendering logic in this file — this is an import-source change only, output must be pixel-identical to before.

    In `src/components/sandbox/vessel-role.ts`: add a new exported const `ROLE_STROKE_CLASS: Record<VesselRole, string>` immediately after the existing `ROLE_HULL_FILL_CLASS` export, mapping `"give-way"` -> `"stroke-give-way"`, `"stand-on"` -> `"stroke-stand-on"`, `"mutual"` -> `"stroke-mutual"` (same `--color-give-way`/`--color-stand-on`/`--color-mutual` tokens already registered in `app/globals.css`, just the `stroke-*` Tailwind utility instead of `fill-*` — needed by Gallery's dashed heading-vector lines in Task 3, which must be role-colored per Pattern 3, not a static hex literal). One-line comment: this mirrors `ROLE_HULL_FILL_CLASS`'s existing pattern for the one new consumer (Gallery) needing a stroke instead of a fill.
  </action>
  <acceptance_criteria>
    - `grep -c "export const HULL_PATH\|export function headingVectorEndpoint\|export function midpoint" src/components/shared/static-chart-geometry.ts` returns 3
    - `grep -c "HULL_PATH\|headingVectorEndpoint\|midpoint" src/components/hero/hero-preview-geometry.ts` returns 0 (fully moved out)
    - `grep -c "ROLE_STROKE_CLASS" src/components/sandbox/vessel-role.ts` returns 1
    - `npx vitest run src/components/hero/Hero.test.tsx src/components/hero/hero-preview-fixture.test.ts` passes unchanged (regression check — HeroPreviewCard's rendered output must not have changed)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/components/hero/Hero.test.tsx src/components/hero/hero-preview-fixture.test.ts</automated>
  </verify>
  <done>static-chart-geometry.ts exists with the 5 extracted exports; hero-preview-geometry.ts no longer defines them; HeroPreviewCard.tsx imports from both files correctly and renders identically to before; vessel-role.ts has a new ROLE_STROKE_CLASS export.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gallery-preview-geometry.ts (per-card dynamic viewBox)</name>
  <files>src/components/gallery/gallery-preview-geometry.ts, src/components/gallery/gallery-preview-geometry.test.ts</files>
  <read_first>
    src/domain/geometry/screen-convert.ts (the `ChartViewBox`/`Position` shapes this function returns/consumes)
    .planning/phases/09-gallery/09-RESEARCH.md (Architecture Patterns, Pattern 1 — the exact verified function body and its rationale)
  </read_first>
  <behavior>
    - Test 1: two vessels 5 NM apart on the x-axis (e.g. a=(0,0), b=(5,0)), aspectRatio 1.5, paddingFraction 0.35 -> returned viewBox has `width/height` exactly equal to 1.5 (aspect ratio enforced) and both vessel positions fall strictly inside `[minX, minX+width]`/`[minY, minY+height]`
    - Test 2: two vessels 1.0 NM apart (e.g. a=(0,0), b=(1,0)) and two vessels 10.0 NM apart (e.g. a=(0,0), b=(10,0)) both produce valid (non-degenerate, finite, width>0 height>0) viewBoxes with the same aspect ratio — proving the function scales correctly across Phase 9's actual 1.0-10.0 NM card range (09-RESEARCH.md's verified per-card range table)
    - Test 3: two vessels at the same x but different y (a=(2,0), b=(2,5)) — a vertical-only pair — still produces a valid viewBox (guards the `Math.max(rawWidth, 0.001)` coincident-axis edge case, since `rawWidth` would otherwise be 0)
    - Test 4: the returned viewBox is centered on the midpoint of the two vessels (centerX/centerY equal the average of the two positions' x/y, accounting for the aspect-ratio-forcing step only adjusting whichever dimension is smaller, not re-centering)
  </behavior>
  <action>
    Create `gallery-preview-geometry.ts` with a doc comment explaining this is Gallery-local (not `shared/`) because it is genuinely new bounding-box-from-vessel-pair logic neither Hero's single hand-solved viewBox nor `ChartPanel.tsx`'s fixed-20nm box needs, per CLAUDE.md's "extract only on a real second consumer" rule (there is currently no second consumer of this specific function). Implement `computeCardViewBox(a: Position, b: Position, aspectRatio: number, paddingFraction: number): ChartViewBox` exactly as specified in 09-RESEARCH.md's Pattern 1 Code Example: compute the raw bounding box of both positions (with a `Math.max(..., 0.001)` floor on width/height to guard coincident axes), expand by `paddingFraction` on each side, then force the box onto `aspectRatio` by growing whichever of width/height is proportionally smaller (never shrinking, so neither vessel ever clips outside the final box), centered on the original bounding box's center. Import `ChartViewBox` from `../../domain/geometry/screen-convert.js` and `Position` from `../../domain/vessel/vessel.js`.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/gallery/gallery-preview-geometry.test.ts` — all 4 behaviors pass
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/gallery/gallery-preview-geometry.test.ts</automated>
  </verify>
  <done>computeCardViewBox() is implemented, unit-tested against the 1.0-10.0 NM range this phase's actual 6 cards span, and handles the coincident-axis edge case.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GalleryPreviewChart.tsx (parametrized mini-chart)</name>
  <files>src/components/gallery/GalleryPreviewChart.tsx, src/components/gallery/GalleryPreviewChart.test.tsx</files>
  <read_first>
    src/components/hero/HeroPreviewCard.tsx (the `VesselMarker` sub-component and its exact SVG element set — the direct analog to mirror, at reduced detail)
    .planning/phases/09-gallery/09-UI-SPEC.md (the "Mini-Chart Contract" section — the authoritative, pixel-verified element set/z-order/dimensions/colors for this component; this supersedes 09-RESEARCH.md's Pattern 2 on the "grid pattern present" correction)
    src/components/sandbox/vessel-role.ts (getVesselRole, ROLE_HULL_FILL_CLASS, ROLE_STROKE_CLASS from Task 1)
  </read_first>
  <behavior>
    - Test 1: given a give-way-type vessel pair (e.g. `crossingResidualBasicCase`'s vesselA/vesselB), the rendered `<svg>` has `role="img"`, contains exactly one text node "A" and one "B" (the vessel circle labels), and contains a "GW" pill and an "SO" pill (not "MUTUAL")
    - Test 2: given a mutual-type vessel pair (e.g. `headOnGenuineCase`'s vesselA/vesselB, giveWay/standOn both null), both pills render as "MU" (not "MUTUAL", not "GW"/"SO") and both hulls use the mutual role's fill class
    - Test 3: the component throws a descriptive Error if `classifyEncounter(vesselA, vesselB)` returns `!ok` (fixture-invariant guard, matching HeroPreviewCard.tsx's own pattern) — verify by passing two coincident-position vessels
  </behavior>
  <action>
    Create `GalleryPreviewChart.tsx` accepting props `{ vesselA: Vessel; vesselB: Vessel }`. Define a local `GALLERY_CONTAINER_SIZE: ContainerSize = { width: 360, height: 240 }` (3:2 aspect ratio, per UI-SPEC's Mini-Chart Contract measured 358:238 dimensions, rounded to clean numbers) and a `paddingFraction` of `0.35` (engineering default per 09-RESEARCH.md's confidence note — visually compare against `.planning/design/Main-Design.png`'s 6 gallery cards during implementation and adjust if any card's vessels look clipped or too small/centered).

    Call `classifyEncounter(vesselA, vesselB)`; throw `new Error(...)` if `!result.ok` (fixture-invariant, mirroring HeroPreviewCard.tsx's pattern — do NOT copy its `giveWay === null` throw, since Gallery's mutual-role cards are a valid, expected state here, not an error).

    Compute `viewBox = computeCardViewBox(vesselA.position, vesselB.position, 360/240, 0.35)` (Task 2's function), then `screenA`/`screenB` via `chartToScreen(position, GALLERY_CONTAINER_SIZE, viewBox)`.

    Derive `roleA = getVesselRole("vesselA", classification)`, `roleB = getVesselRole("vesselB", classification)`. Build the SVG per UI-SPEC's Mini-Chart Contract element set, in this exact z-order: (1) a faint grid `<pattern>` background, reusing Hero's `heroGrid` `<pattern>` technique verbatim but at `rgba(120,120,130,0.08)` stroke opacity (not Hero's 0.14) and a new pattern `id` (e.g. `galleryGrid`, since SVG pattern IDs must be unique per document and both components can render on the same page); (2) a solid connector line between `screenA`/`screenB` using Tailwind's `stroke-geometry` class (the `--color-geometry` token, not a hex literal); (3) two hull `<path d={HULL_PATH}>` groups (rotate-with-heading, per `VesselMarker`'s existing translate+rotate wrapper pattern), each `className={ROLE_HULL_FILL_CLASS[role]}` (never a hardcoded per-slot hex, per Pattern 3) with `stroke={HULL_STROKE}`/`strokeWidth={HULL_STROKE_WIDTH}` from the shared file; (4) a non-rotated dark circle + white "A"/"B" text label per hull, copying HeroPreviewCard's exact `VesselMarker` circle/text convention (`r={7}`, `fill="#18181B"`, `fontSize={10}`, `fontWeight={600}`, white text) unchanged; (5) a solid role-colored 2-letter pill per hull using a NEW local map `{ "give-way": "GW", "stand-on": "SO", mutual: "MU" }` (do not reuse `vessel-role.ts`'s `ROLE_BADGE_TEXT.mutual`, which is the 6-character "MUTUAL" — see Pitfall 3), rendered as `<rect className={ROLE_HULL_FILL_CLASS[role]}>` + white text, same `width={20} height={12} rx={3}` dimensions as Hero's pill; (6) a dashed heading-vector line per hull from `headingVectorEndpoint(screen, vessel.heading)` (shared file, Task 1), `className={ROLE_STROKE_CLASS[role]}` (role-colored, not Hero's fixed per-slot color), `strokeDasharray="3 3"`; (7) a floating range-label chip at `midpoint(screenA, screenB)` showing `${range.toFixed(2)} NM`, copying Hero's exact chip markup (`#18181B` fill / `#27272A` border / `#D4D4D8` text / `fontSize={12}`) unchanged. Wrap the whole `<svg viewBox="0 0 360 240" width="100%" height="auto">` in a `<div className="overflow-hidden rounded-md border border-border bg-chart-surface">` (per UI-SPEC's correction: use the registered `bg-chart-surface` token, not Hero's separate inline `#0B0B0E` hex literal). Do NOT include range rings, radial gradient bearing sector, or a north reference line — UI-SPEC confirms these are absent from every gallery card.
  </action>
  <acceptance_criteria>
    - `npx vitest run src/components/gallery/GalleryPreviewChart.test.tsx` — all 3 behaviors pass
    - `grep -c "heroSectorGradient\|HERO_OUTER_RING_RADIUS_PX\|HERO_INNER_RING_RADIUS_PX" src/components/gallery/GalleryPreviewChart.tsx` returns 0 (no rings/gradient copied in)
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/gallery/GalleryPreviewChart.test.tsx</automated>
  </verify>
  <done>GalleryPreviewChart renders a role-colored, dynamically-sized mini-chart for any vessel pair, correctly distinguishing give-way/stand-on/mutual roles in hull color, pill text, and heading-vector color, with no rings/gradient/grid-at-Hero's-opacity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None new | This plan is pure presentation-layer refactoring/component code — no new data ingress, no user input, no persistence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | `hero-preview-geometry.ts` extraction | accept | Refactor-only change (moving existing, already-reviewed constants/functions between files) — no behavior change, regression-checked by Hero's existing test suite in Task 1's acceptance criteria |
| T-09-05 | Denial of Service | `computeCardViewBox()` | accept | Pure, bounded arithmetic on two finite `Position` values already validated upstream by `classifyEncounter()`'s own Stage 0 finite-number checks — no unbounded loop or recursion, no externally-triggerable resource exhaustion |

No npm/pip/cargo package installs occur in this plan — the Package Legitimacy Gate is not triggered.
</threat_model>

<verification>
`npx vitest run src/components/gallery/gallery-preview-geometry.test.ts src/components/gallery/GalleryPreviewChart.test.tsx src/components/hero/Hero.test.tsx src/components/hero/hero-preview-fixture.test.ts` — all green. `npx tsc --noEmit` — no errors.
</verification>

<success_criteria>
- Hero's preview card renders identically after the extraction (zero visual regression)
- Gallery's mini-chart component correctly renders any vessel pair across the 1.0-10.0 NM range with role-correct (not slot-correct) coloring
</success_criteria>

<output>
Create `.planning/phases/09-gallery/09-02-SUMMARY.md` when done
</output>
