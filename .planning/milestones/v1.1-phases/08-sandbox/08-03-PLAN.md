---
phase: 08-sandbox
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/sandbox/ControlPanel.tsx
  - src/components/sandbox/ControlPanel.test.tsx
autonomous: true
requirements: [SBOX-02, SBOX-04]

must_haves:
  truths:
    - "Vessel type is chosen via shadcn Select and speed via shadcn Slider, both still calling the exact same onVesselTypeChange/onVesselSpeedChange callback contract as before"
    - "Each vessel control card shows a letter-chip + heading + role badge in its header row, and a read-only HEADING readout (no new editable heading input)"
    - "Every existing ControlPanel interaction test still proves the same behavior, rewritten for the Radix Select/Slider interaction pattern"
  artifacts:
    - path: "src/components/sandbox/ControlPanel.tsx"
      provides: "Restyled vessel-control cards using Select/Slider/Card/Badge"
  key_links:
    - from: "src/components/sandbox/ControlPanel.tsx"
      to: "src/components/sandbox/vessel-role.ts"
      via: "getVesselRole + ROLE_BADGE_TEXT/ROLE_BADGE_CLASSNAME for the card-header role badge"
      pattern: "getVesselRole"
    - from: "src/components/sandbox/ControlPanel.tsx"
      to: "src/components/ui/select.tsx"
      via: "Select/SelectTrigger/SelectContent/SelectItem/SelectValue"
      pattern: "from \"@/components/ui/select\""
---

<objective>
Swap `ControlPanel.tsx`'s native `<input type="number">`/`<select>` for shadcn `Slider`/`Select`, wrap each vessel's form section in a `Card` matching the design's letter-chip + heading + role-badge header row, add the read-only HEADING display, and rewrite `ControlPanel.test.tsx`'s interaction assertions for the new Radix-primitive interaction pattern (SBOX-04).

Purpose: this is the other half of SBOX-02 (verdict banner is wave 2's other parallel plan) -- proving the exact same `onVesselSpeedChange`/`onVesselTypeChange` contract survives a full primitive swap with zero classification-behavior change.
Output: restyled `ControlPanel.tsx`; `ControlPanel.test.tsx` fully rewritten (not incrementally patched) for the Select/Slider interaction pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-sandbox/08-CONTEXT.md
@.planning/phases/08-sandbox/08-RESEARCH.md
@.planning/phases/08-sandbox/08-UI-SPEC.md
</context>

<interfaces>
<!-- Widened ControlPanelProps from 08-01 -- this plan's component must accept and use `classification` -->
```typescript
// src/components/sandbox/types.ts (08-01 output)
export interface ControlPanelProps {
  vesselA: Vessel;
  vesselB: Vessel;
  classification: ClassificationResult; // NEW -- drives the card-header role badge
  onVesselSpeedChange: VesselUpdateHandlers["onVesselSpeedChange"];
  onVesselTypeChange: VesselUpdateHandlers["onVesselTypeChange"];
}
```

<!-- vessel-role.ts exports this plan consumes (08-01 output) -->
```typescript
export function getVesselRole(vessel: VesselLabel, classification: ClassificationResult): VesselRole;
export const ROLE_BADGE_TEXT: Record<VesselRole, string>;
export const ROLE_BADGE_CLASSNAME: Record<VesselRole, string>;
```

<!-- shadcn Select/Slider controlled-value API (verified via Context7 /shadcn-ui/ui, from 08-RESEARCH.md Code Examples) -->
```tsx
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import { Slider } from "@/components/ui/slider";
import { Label } from "@/components/ui/label";
// Select: value/onValueChange are single values (string).
// Slider: value/onValueChange are always ARRAYS, even single-thumb -- destructure [speed] both directions.
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Swap native inputs for Select/Slider, add the Card header row and HEADING readout</name>
  <files>src/components/sandbox/ControlPanel.tsx</files>
  <read_first>
    src/components/sandbox/ControlPanel.tsx
    src/components/sandbox/types.ts
    src/components/sandbox/vessel-role.ts
    src/components/ui/card.tsx
    src/components/ui/badge.tsx
  </read_first>
  <behavior>
    - `ControlPanel` still renders both "Vessel A" and "Vessel B" sections (component/DOM-level behavior, verified in Task 2's test rewrite).
    - Changing Vessel A's Select to "Fishing" calls `onVesselTypeChange("vesselA", "fishing")` -- exact same call contract as the pre-restyle native `<select>`.
    - Moving Vessel B's Slider thumb right by one step calls `onVesselSpeedChange("vesselB", vessel.speed + 1)`.
    - The card header shows the vessel's current give-way/stand-on/mutual role badge, derived via `getVesselRole(label, classification)` -- never a locally re-derived role.
    - A read-only "HEADING" field shows `vessel.heading` formatted as a 3-digit zero-padded degree string (e.g. `035°`), with NO input element -- heading stays drag-only on the chart (D-06/specifics, no regression to the existing chart-only heading control).
  </behavior>
  <action>
    Keep `VESSEL_TYPE_LABELS` and the "never hand-author a parallel option list" comment unchanged -- `VesselTypeSchema.options` stays the single source of truth for `Select`'s option values.

    Replace the native `<input type="number">` speed control with `Slider` (`value={[vessel.speed]}`, `min={0}`, `max={40}`, `step={1}`, `onValueChange={([speed]) => onVesselSpeedChange(label, speed)}`) plus a live numeric readout `<span>` next to/above it showing `${vessel.speed} kn` styled per 08-UI-SPEC.md's Typography section (Body role, 16px/600, Geist Mono, `text-primary` -- the ONLY element in this whole restyled section allowed to use `--primary`; every other accent need in this file must use the new `--color-rule-accent`/role tokens instead, never `--primary`).

    Replace the native `<select>` with `Select`/`SelectTrigger`/`SelectContent`/`SelectItem`/`SelectValue` exactly per the Code Example in `<interfaces>` above -- `value={vessel.type}`, `onValueChange={(type) => onVesselTypeChange(label, type as VesselType)}`, `SelectTrigger id={`${label}-type`}`, mapping `VesselTypeSchema.options` to `SelectItem`s labeled via `VESSEL_TYPE_LABELS`.

    Reorder the fields to match the design: TYPE first, then SPEED, then the new read-only HEADING readout last (current file order is Speed-then-Type -- flip it). Use shadcn `Label` (from `@/components/ui/label`, installed in 08-01) for each field's uppercase "TYPE"/"SPEED"/"HEADING" label text (Label role, 13px/600, `text-muted-foreground`, uppercase), with `htmlFor` pointing at each control's `id` (`${label}-type` for the Select trigger; the Slider's root does not natively support `htmlFor` association the same way -- associate via `aria-label={`${heading} speed`}` on the `Slider` itself instead, or wrap in a `<div>` with the `Label` visually preceding it without a strict `htmlFor` link, since Radix `Slider`'s root is a non-form-control div).

    Wrap each `VesselFormSection` in a `Card`/`CardHeader`/`CardContent` (`@/components/ui/card`, already installed). `CardHeader` renders, left-to-right: a letter-chip (`<span className="flex h-5 w-5 items-center justify-center rounded bg-foreground text-[13px] font-semibold text-background">` containing `"A"`/`"B"`), the heading text ("Vessel A"/"Vessel B", Body role 16px/600 `text-foreground`), and a role `Badge` on the right using `ROLE_BADGE_CLASSNAME[role]` (from `vessel-role.ts`) with text `ROLE_BADGE_TEXT[role]`, where `role = getVesselRole(label, classification)`.

    Update `VesselFormSectionProps` to accept `classification: ControlPanelProps["classification"]` (passed down from `ControlPanel`'s own now-required prop) instead of deriving role some other way.
  </action>
  <verify>
    <automated>npx vitest run src/components/sandbox/ControlPanel.test.tsx</automated>
  </verify>
  <done>ControlPanel.tsx renders Select/Slider/Card/Badge per the design, TYPE/SPEED/HEADING field order, unchanged onVesselSpeedChange/onVesselTypeChange contract.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite ControlPanel.test.tsx for the Radix Select/Slider interaction pattern</name>
  <files>src/components/sandbox/ControlPanel.test.tsx</files>
  <read_first>
    src/components/sandbox/ControlPanel.test.tsx
    src/components/sandbox/ControlPanel.tsx
  </read_first>
  <action>
    This is a full interaction-pattern rewrite, not an incremental patch (per 08-RESEARCH.md/08-PATTERNS.md's explicit guidance) -- `user.selectOptions(vesselBSelect, "fishing")` no longer resolves against a Radix `Select` (it is not a native `<select>` element).

    Keep the file's existing `afterEach(() => cleanup())` boilerplate (required because `vitest.config.ts` sets `globals: false`) and the `ControlledHarness` component pattern (a controlled-re-render harness owning `vesselA`/`vesselB` state) -- both stay valid. Add a `classificationFixture: ClassificationResult` constant (any valid crossing-shape fixture, e.g. `giveWay: "vesselA", standOn: "vesselB"`) and thread it through every `render(<ControlPanel .../>)` call and the `ControlledHarness`'s own render, since `ControlPanelProps` now requires it.

    Rewrite the "lists exactly five vessel-type options" test: instead of reading `HTMLSelectElement.options`, open the first `Select` via `await user.click(screen.getAllByRole("combobox")[0])`, then assert the 5 expected option labels via `await screen.findAllByRole("option")` (async -- `SelectContent` only mounts into its portal once opened) mapped to `.textContent`, in the exact same order as `VESSEL_TYPE_LABELS`.

    Rewrite the speed-change test: locate Vessel A's slider thumb via `screen.getAllByRole("slider")[0]`, focus it (`await user.click(...)` or `.focus()`), then drive one increment via `await user.keyboard("{ArrowRight}")` (Radix `Slider` responds to arrow keys natively, stepping by the configured `step={1}`) -- assert `onVesselSpeedChange` was last called with `("vesselA", vesselAFixture.speed + 1)`. Do NOT use `user.type`/`user.clear` (Slider is not a text input).

    Rewrite the type-change test: `await user.click(screen.getAllByRole("combobox")[1])` (Vessel B's trigger), then `await user.click(await screen.findByRole("option", { name: "Fishing" }))` -- assert `onVesselTypeChange` was called with `("vesselB", "fishing")`, exact same assertion as before, only the interaction mechanics changed.

    Add/keep any test-only polyfills Radix `Select`/`Slider` need under jsdom (per 08-RESEARCH.md Pitfall reference: watch for `target.hasPointerCapture is not a function` / `scrollIntoView is not a function`) scoped locally to this file's own `beforeEach`, matching `ChartPanel.test.tsx`'s existing local-polyfill convention -- do not add anything to a global `vitest.setup.ts`.

    Add one new test asserting the card-header role badge renders correctly: with `classificationFixture.giveWay === "vesselA"`, assert `screen.getAllByText("GW")` contains at least one match inside a card ancestor for Vessel A, and `screen.getAllByText("SO")` for Vessel B.
  </action>
  <acceptance_criteria>
    - `grep -c "user.selectOptions" src/components/sandbox/ControlPanel.test.tsx` returns 0 (old native-select interaction fully removed)
    - `grep -c "getAllByRole(\"combobox\")\|findByRole(\"option\"" src/components/sandbox/ControlPanel.test.tsx` returns at least 2
    - `grep -c "getAllByRole(\"slider\")" src/components/sandbox/ControlPanel.test.tsx` returns at least 1
    - `npx vitest run src/components/sandbox/ControlPanel.test.tsx` passes, 100% of tests green
    - `npm run typecheck` reports zero errors for `ControlPanel.tsx`/`ControlPanel.test.tsx`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/sandbox/ControlPanel.test.tsx</automated>
  </verify>
  <done>ControlPanel.test.tsx fully rewritten for Select/Slider interaction, zero references to the old native-control API, all tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| User-selected vessel type/speed -> `applyVesselUpdate` | Form input already flows through `VesselSchema.safeParse()` downstream (unchanged this plan) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-08-04 | Tampering | Radix `Select`/`Slider` emitted values | accept | `Select`'s options are exhaustively enumerated from `VesselTypeSchema.options` (cannot emit an out-of-enum value); `Slider`'s `min`/`max`/`step` clamp the emitted number client-side, and `VesselSchema.safeParse()` in `SandboxContainer.applyVesselUpdate()` (unchanged) is the authoritative server-independent validation gate regardless |

No new trust boundary or external dependency -- this plan replaces one set of already-validated form controls with another, using the project's already-audited `radix-ui@1.6.2` package (per `research/STACK.md`'s Package Legitimacy Audit, applicable milestone-wide).
</threat_model>

<verification>
- `npx vitest run src/components/sandbox/ControlPanel.test.tsx` passes
- `npm run typecheck` clean for both files
</verification>

<success_criteria>
- ControlPanel restyled with Select/Slider/Card/Badge, TYPE/SPEED/HEADING order, card-header role badge
- Test suite fully rewritten for the Radix interaction pattern, zero references to the retired native-control API
- Identical onVesselSpeedChange/onVesselTypeChange call contract preserved
</success_criteria>

<output>
Create `.planning/phases/08-sandbox/08-03-SUMMARY.md` when done
</output>
