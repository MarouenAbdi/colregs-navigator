---
phase: 08-sandbox
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/sandbox/ChartPanel.tsx
autonomous: true
requirements: [SBOX-01]

must_haves:
  truths:
    - "The chart's visible surface, grid, cones, bearing line, and hull/rotate-handle colors all read correctly against the dark theme, with zero raw unregistered hex literal left for a value that has a real semantic token"
    - "Every hull/rotate-handle shape's fill/stroke stays a real paint (never \"none\"), so pointer-events: visiblePainted hit-testing is provably unchanged"
    - "A '1 NM' scale-bar legend renders at the correct proportional pixel length for the current container size"
  artifacts:
    - path: "src/components/sandbox/ChartPanel.tsx"
      provides: "Re-themed SVG chart panel consuming vessel-role.ts's ROLE_HULL_FILL_CLASS/ROLE_BADGE_TEXT and app/globals.css's new tokens"
  key_links:
    - from: "src/components/sandbox/ChartPanel.tsx"
      to: "src/components/sandbox/vessel-role.ts"
      via: "ROLE_HULL_FILL_CLASS[role] on the hull <polygon> className"
      pattern: "ROLE_HULL_FILL_CLASS"
    - from: "src/components/sandbox/ChartPanel.tsx"
      to: "app/globals.css"
      via: "bg-chart-surface / stroke-rule-accent / stroke-mutual Tailwind classes"
      pattern: "chart-surface|rule-accent"
---

<objective>
Re-theme `ChartPanel.tsx`'s remaining unregistered hex-literal colors against the dark palette, consolidate its hull-fill/role-badge maps into `vessel-role.ts` (this phase's Wave 1 output), and add the design's "1 NM" scale-bar legend -- with an explicit, zero-tolerance constraint that hull/rotate-handle hit-testing (SBOX-01's single highest regression risk) is never touched beyond a paint-value swap.

Purpose: this is the one file in the phase where a restyle mistake can silently break the product's core drag/rotate interaction loop -- confining this plan to paint-only changes on the existing shapes, with an explicit "do not touch" list, is the mitigation.
Output: `ChartPanel.tsx` fully re-themed, scale-bar legend added, existing `ChartPanel.test.tsx` still green with zero edits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-sandbox/08-CONTEXT.md
@.planning/phases/08-sandbox/08-RESEARCH.md
@.planning/phases/08-sandbox/08-UI-SPEC.md
</context>

<interfaces>
<!-- vessel-role.ts's 3 new exports this plan consumes (created in 08-01, do not redefine locally) -->
```typescript
// src/components/sandbox/vessel-role.ts
export const ROLE_HULL_FILL_CLASS: Record<VesselRole, string>; // "give-way": "fill-give-way", "stand-on": "fill-stand-on", mutual: "fill-mutual"
export const ROLE_BADGE_TEXT: Record<VesselRole, string>; // "give-way": "GW", "stand-on": "SO", mutual: "MUTUAL"
```

<!-- New tokens available from app/globals.css (08-01) as Tailwind utility classes:
     bg-chart-surface, fill-give-way/stand-on/mutual, stroke-rule-accent, stroke-mutual -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Re-theme remaining hex literals and consolidate hull/badge maps into vessel-role.ts</name>
  <files>src/components/sandbox/ChartPanel.tsx</files>
  <read_first>
    src/components/sandbox/ChartPanel.tsx
    src/components/sandbox/ChartPanel.test.tsx
    src/components/sandbox/vessel-role.ts
  </read_first>
  <action>
    Re-run `grep -n '#[0-9A-Fa-f]\{3,6\}' src/components/sandbox/ChartPanel.tsx` fresh before editing -- do not trust `PITFALLS.md`'s stale example list (it cites `#E2E8F0`/`#CBD5E1`, which are ALREADY fixed in the current file with "was slate-X" comments; treat 08-RESEARCH.md's Pitfall S1 table as the authoritative current-state list instead).

    Leave `GRID_STROKE` (`#27272A`) and `CONE_DEFAULT_STROKE` (`#3F3F46`) untouched -- both already carry a "was slate-X" re-theme comment and are correct as-is.

    Change the `<svg>` element's `className` from `"bg-[#0B0B0E] border border-border rounded"` to `"bg-chart-surface border border-border rounded"` -- replaces the raw arbitrary-hex literal with the new semantic token from 08-01 (same visual value, now named/reusable).

    Delete the local `HULL_FILL_CLASS` and `ROLE_BADGE_TEXT` `const` declarations entirely. Import `ROLE_HULL_FILL_CLASS` and `ROLE_BADGE_TEXT` from `./vessel-role.js` instead (alongside the existing `getVesselRole` import already there). Update the `<polygon className={HULL_FILL_CLASS[role]}>` reference to `ROLE_HULL_FILL_CLASS[role]` and the `<text>{ROLE_BADGE_TEXT[role]}</text>` reference stays the same name (now imported, not locally declared).

    Convert the rotate-handle `<circle>`'s hardcoded `stroke="#0D9488"` attribute to a Tailwind class instead: remove the `stroke` attribute and add `className="stroke-rule-accent"` -- this is the SAME color value (#0D9488), now sourced from the new `--color-rule-accent` token per 08-UI-SPEC.md's explicit "keep distinct, do not align to `--primary`" decision (this is the original working-tool accent teal, deliberately different from the app's brand `--primary` #2dd4bf).

    Convert the rotate-handle stalk `<line>`'s hardcoded `stroke="#94A3B8"` attribute the same way: remove the attribute, add `className="stroke-mutual"` -- `#94A3B8` (slate-400) is the EXACT same hex value as the `--mutual` token registered in 08-01, so this consolidates a second occurrence of the same color onto the shared token rather than leaving a second, differently-named hex literal for an identical color (per 08-UI-SPEC.md's explicit warning against exactly that).

    Leave `BEARING_LINE_DEFAULT_STROKE` (`#475569`) and `DOUBT_STROKE` (`#F59E0B`) as raw hex `const` string literals, UNCHANGED -- these values are passed as JSX `stroke={...}`/`fill={...}` attribute props on `<line>`/`<path>` elements read directly by `ChartPanel.test.tsx`'s existing assertions (`bearingLine?.getAttribute("stroke")).toBe("#F59E0B")`), and Tailwind's `stroke-*` utility classes cannot be asserted the same way via `getAttribute("stroke")` -- converting these two would require rewriting passing tests for zero visual benefit (the values already exactly match the new `--doubt`/`--geometry` tokens registered in 08-01; this is a deliberate two-representation choice, not a gap -- the CSS tokens serve non-SVG-attribute Tailwind-class contexts like the wave-4 ReasoningTrail tag/dot coloring, the raw consts serve this file's `stroke`/`fill` attribute props).

    Add the "1 NM" scale-bar legend: inside the existing `<div ref={containerRef} className="aspect-square w-full relative">` wrapper, as a sibling AFTER the `<svg>` element (plain HTML, not inside the SVG -- avoids needing SVG coordinate math), add a `<div className="absolute bottom-2 left-2 flex items-center gap-1 font-mono text-[13px] font-semibold text-muted-foreground">` containing a short horizontal tick (`<span className="h-px bg-border" style={{ width: containerSize.width / CHART_VIEW_BOX.width }} />` -- this computes the true on-screen pixel length of exactly 1 nautical mile given the current `containerSize`/fixed 20nm `CHART_VIEW_BOX.width`, so the legend stays accurate across different screen/container sizes, not a hardcoded decorative width) followed by the text `"1 NM"`. Only render this legend in the branch where `containerSize` is non-null (the function already early-returns a bare empty div when `containerSize` is null/zero-area -- the legend must live in the same branch as the real `<svg>`, after the null-guard).
  </action>
  <acceptance_criteria>
    - `grep -n '#[0-9A-Fa-f]\{3,6\}' src/components/sandbox/ChartPanel.tsx` returns exactly 4 matches: `GRID_STROKE`, `CONE_DEFAULT_STROKE`, `BEARING_LINE_DEFAULT_STROKE`, `DOUBT_STROKE` const declarations only -- zero hex literals remain as inline JSX `stroke`/`fill`/`className` attribute values on the rotate-handle circle, stalk line, or `<svg>` element
    - `grep -c "HULL_FILL_CLASS\s*=\|ROLE_BADGE_TEXT\s*=" src/components/sandbox/ChartPanel.tsx` (local `const` declarations) returns 0 -- both are now imports
    - `grep -c "from \"./vessel-role.js\"" src/components/sandbox/ChartPanel.tsx` returns 1, and that import line includes `ROLE_HULL_FILL_CLASS`, `ROLE_BADGE_TEXT`, and `getVesselRole`
    - `grep -c "bg-chart-surface" src/components/sandbox/ChartPanel.tsx` returns 1; `grep -c 'bg-\[#0B0B0E\]'` returns 0
    - `grep -c "stroke-rule-accent" src/components/sandbox/ChartPanel.tsx` returns 1; `grep -c "stroke-mutual" src/components/sandbox/ChartPanel.tsx` returns 1
    - `grep -c '"1 NM"'` returns 1
    - No change to `HULL_POINTS`, `HULL_BOW_Y`, `HULL_STERN_Y`, `HULL_HALF_WIDTH`, `ROTATE_HANDLE_CY`, `ROTATE_HANDLE_VISIBLE_R`, any `onPointerDown`/`onPointerMove`/`onPointerUp` handler wiring, or any `fill="none"`/`stroke="none"` introduction (behavior assertion: diff the file against its pre-task state and confirm the only changed lines are the ones listed above)
    - `npx vitest run src/components/sandbox/ChartPanel.test.tsx` passes with ZERO test-file edits (proves the restyle preserved every externally-observable behavior the existing suite checks)
    - `npm run typecheck` reports zero errors for `ChartPanel.tsx`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/sandbox/ChartPanel.test.tsx</automated>
  </verify>
  <done>ChartPanel.tsx fully re-themed per Pitfall S1's authoritative table, hull/badge maps consolidated into vessel-role.ts, scale-bar legend added, zero hit-testing-relevant code touched, existing test suite green unmodified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Pointer/drag input -> hull/rotate-handle hit-targets | Untrusted in the sense that any user gesture can trigger it, but this plan changes zero handler wiring -- purely a paint-value restyle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-08-03 | Denial of Service (regression) | Hull `<polygon>` / rotate-handle `<circle>` `fill`/`stroke` values | mitigate | Acceptance criteria explicitly forbid introducing `fill="none"`/`stroke="none"` on either shape and forbid touching `HULL_POINTS`/pointer handler wiring; `ChartPanel.test.tsx` must pass with zero test edits as proof; the phase's mandatory manual browser drag+rotate UAT (08-06) is the final backstop jsdom cannot provide |

No new trust boundary, user input surface, or dependency is introduced by this plan -- it is a pure presentation-layer restyle of an existing, already-tested SVG surface.
</threat_model>

<verification>
- `npx vitest run src/components/sandbox/ChartPanel.test.tsx` passes unmodified
- `grep -n '#[0-9A-Fa-f]\{3,6\}' src/components/sandbox/ChartPanel.tsx` shows only the 4 expected const declarations
- Manual visual smoke check deferred to 08-06 (mandatory browser UAT) -- not required to pass before this plan's SUMMARY, but flagged as outstanding until then
</verification>

<success_criteria>
- Chart re-themed against the dark palette with no remaining unregistered-token hex literal
- Hull-fill/role-badge maps single-sourced from vessel-role.ts
- "1 NM" scale-bar legend present and proportionally accurate
- Zero change to hit-testing-relevant code, proven by an unmodified passing test suite
</success_criteria>

<output>
Create `.planning/phases/08-sandbox/08-02-SUMMARY.md` when done
</output>
