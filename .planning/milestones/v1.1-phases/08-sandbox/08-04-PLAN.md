---
phase: 08-sandbox
plan: 04
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/components/sandbox/VerdictBanner.tsx
  - src/components/sandbox/VerdictBanner.test.tsx
  - src/components/sandbox/InstrumentReadouts.tsx
  - src/components/sandbox/InstrumentReadouts.test.tsx
  - src/components/sandbox/ReasoningTrail.tsx
  - src/components/sandbox/ReasoningTrail.test.tsx
  - src/components/sandbox/ReasoningPanel.tsx
  - src/components/sandbox/ReasoningPanel.test.tsx
autonomous: true
requirements: [SBOX-02, SBOX-03, SBOX-04]

must_haves:
  truths:
    - "The verdict banner, instrument-readout grid + status pill, and numbered reasoning trail are three visually distinct cards, matching the design's 1:1 layout instead of one combined aside"
    - "The reasoning trail renders classification.trail's actual length every time (3 to 5 entries depending on encounter type), never a hardcoded 3 steps"
    - "The doubt/Rule 7 badge treatment is generic (driven by classification.doubt), not specific to any one preset chip's fixture (D-08)"
    - "Every behavior the retired ReasoningPanel.test.tsx covered (verdict text, MUTUAL badge, trail order/content, fact readouts, doubt caveats, degenerate state) is still covered by the 3 new test files, combined"
  artifacts:
    - path: "src/components/sandbox/VerdictBanner.tsx"
      provides: "Full-width verdict card: rule badge, title, description, 2 role badges, degenerate handling"
    - path: "src/components/sandbox/InstrumentReadouts.tsx"
      provides: "2x2 Range/Bearing/CPA/TCPA grid + status pill in one card"
    - path: "src/components/sandbox/ReasoningTrail.tsx"
      provides: "Numbered-step reasoning trail card with dynamic tag/tone-per-position mapping"
  key_links:
    - from: "src/components/sandbox/VerdictBanner.tsx"
      to: "src/components/sandbox/reasoning-trail-tag.ts"
      via: "classifyingEntryIndex/ruleNumber deriving the dynamic Rule {N} badge"
      pattern: "classifyingEntryIndex"
    - from: "src/components/sandbox/InstrumentReadouts.tsx"
      to: "src/components/sandbox/instrument-readouts.ts"
      via: "deriveInstrumentReadouts(vesselA, vesselB)"
      pattern: "deriveInstrumentReadouts"
    - from: "src/components/sandbox/ReasoningTrail.tsx"
      to: "src/components/sandbox/reasoning-trail-tag.ts"
      via: "classifyingEntryIndex/ruleNumber deriving each middle entry's tag"
      pattern: "classifyingEntryIndex"
---

<objective>
Split the current single `ReasoningPanel.tsx` aside into 3 dedicated cards matching the design 1:1 -- `VerdictBanner.tsx`, `InstrumentReadouts.tsx`, `ReasoningTrail.tsx` -- each independently tested, then retire `ReasoningPanel.tsx`/`ReasoningPanel.test.tsx`.

Purpose: SBOX-02 (verdict banner) and SBOX-03 (reasoning trail) both require the design's exact card boundaries, which the current single-aside structure cannot express without becoming an unreviewable mega-component; splitting also isolates the one genuinely new derived-data need (D-05's live instrument readouts) from the two purely-restyled pieces.
Output: 3 new presentational components + 3 new test files; `ReasoningPanel.tsx`/`ReasoningPanel.test.tsx` deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-sandbox/08-CONTEXT.md
@.planning/phases/08-sandbox/08-RESEARCH.md
@.planning/phases/08-sandbox/08-UI-SPEC.md
</context>

<interfaces>
<!-- Prop contracts from 08-01's types.ts (read that file directly before starting -- do not assume this excerpt is exhaustive) -->
```typescript
export interface VerdictBannerProps { classification: ClassificationResult; isDegenerate: boolean; }
export interface InstrumentReadoutsProps { vesselA: Vessel; vesselB: Vessel; classification: ClassificationResult; isDegenerate: boolean; }
export interface ReasoningTrailProps { classification: ClassificationResult; }
```

<!-- Pure modules from 08-01 this plan consumes, do not reimplement -->
```typescript
// src/components/sandbox/instrument-readouts.ts
export function deriveInstrumentReadouts(vesselA: Vessel, vesselB: Vessel): { rangeNm: number; bearingAtoBDegrees: number | null; cpaNm: number | null; tcpaMinutes: number | null };
// src/components/sandbox/status-pill.ts
export function statusPillCopy(riskOfCollision: boolean, cpaNm: number | null): { text: string; tone: "clear" | "risk" };
// src/components/sandbox/reasoning-trail-tag.ts
export function classifyingEntryIndex(trailLength: number): number; // trailLength - 2
export function ruleNumber(ruleId: string): string; // "Rule 13(a)-(b)" -> "13"
// src/components/sandbox/vessel-role.ts
export function getVesselRole(vessel: VesselLabel, classification: ClassificationResult): VesselRole;
export const ROLE_BADGE_TEXT: Record<VesselRole, string>;
export const ROLE_BADGE_CLASSNAME: Record<VesselRole, string>;
```

<!-- Current ReasoningPanel.tsx logic that MUST be preserved verbatim across the split (read the full current file before starting) -->
```typescript
const ENCOUNTER_TYPE_TITLE: Record<string, string> = { "head-on": "Head-on", crossing: "Crossing", overtaking: "Overtaking" };
const DOUBT_CAVEAT_TEXT: Record<string, string> = {
  "near-overtaking-crossing-boundary": "Near the overtaking/crossing boundary (112.5° abaft the beam) — this verdict may flip with a small heading change.",
  "near-head-on-boundary": "Near the head-on boundary (reciprocal heading) — this verdict may flip with a small heading change.",
};
const FACT_LABEL: Record<string, string> = { riskOfCollision, tcpaMinutes, dcpaNm, relativeBearingAtoB, relativeBearingBtoA, vesselAType, vesselBType }; // full map, see file
function formatFactValue(key, value): string; // riskOfCollision -> Yes/No, tcpaMinutes -> "N.N min", dcpaNm -> "N.NN nm", bearings -> "N.N°"
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: VerdictBanner.tsx -- rule badge, title, new description copy, role badges, degenerate handling</name>
  <files>src/components/sandbox/VerdictBanner.tsx, src/components/sandbox/VerdictBanner.test.tsx</files>
  <read_first>
    src/components/sandbox/ReasoningPanel.tsx
    src/components/sandbox/ReasoningPanel.test.tsx
    src/components/sandbox/types.ts
    src/components/sandbox/vessel-role.ts
    src/components/sandbox/reasoning-trail-tag.ts
    src/components/ui/card.tsx
    src/components/ui/badge.tsx
  </read_first>
  <behavior>
    - Give-way case (e.g. `crossingClassification` fixture from the current `ReasoningPanel.test.tsx`, `giveWay: "vesselA"`): renders `"Crossing"` as the title (a standalone element, NOT concatenated with the verdict clause -- this differs from the current file's single-string `verdictBannerText()`, see action below), a description reading `"Vessel A gives way. Give-way vessel takes early, substantial action; stand-on vessel holds course and speed."`, a `Rule 15` badge, and role badges `"GW"`/`"SO"` on Vessel A/B respectively.
    - Mutual case (`giveWay: null, standOn: null`, e.g. a head-on/doubt fixture): renders `"Head-on"` title, `"MUTUAL"` badges on BOTH vessels (never `"GW"`/`"SO"`), a description reading `"Mutual obligation. Both vessels must alter course to starboard early and substantially to avoid a close-quarters situation."` (new copy, `[ASSUMED]`, matching the give-way description's tone/length), and a `Rule 7` badge (head-on/doubt cases are always `doubt: true` per the domain's own Rule 14 dispatch, so the badge is always the doubt-substituted "Rule 7", never "Rule 14" -- D-08's generic-not-chip-specific behavior falls out of this naturally).
    - Degenerate case (`isDegenerate: true`): renders `"Unable to classify"` / `"Vessel A and Vessel B are at the same position — drag one apart to resume live classification."` in place of the title/description (verbatim, unchanged copy) and suppresses the rule badge entirely, but STILL renders both role badges from the last-good `classification` (matches the exact pre-restyle precedent: role badges never disappear during a degenerate frame).
  </behavior>
  <action>
    Move `ENCOUNTER_TYPE_TITLE` and the degenerate-state copy verbatim from `ReasoningPanel.tsx` into this new file (single remaining consumer). Define a local `VESSEL_LABEL_TEXT: Record<VesselLabel, string> = { vesselA: "Vessel A", vesselB: "Vessel B" }` (also moved verbatim -- single consumer now).

    Derive the rule badge text via a new small local function, e.g. `bannerRuleBadge(classification): string` -- `classification.doubt ? "Rule 7" : `Rule ${ruleNumber(classification.trail[classifyingEntryIndex(classification.trail.length)].ruleId)}`` (using `classifyingEntryIndex`/`ruleNumber` imported from `./reasoning-trail-tag.js`, per 08-01). Suppress this badge entirely (render nothing in its place) when `isDegenerate` is true.

    Derive the title via `ENCOUNTER_TYPE_TITLE[classification.encounterType]` alone (NOT the current file's combined `${encounterTitle} — ${verdictClause}` string -- the design shows title and description as two visually separate elements, per 08-UI-SPEC.md's Typography section: "Verdict banner title ('Crossing'): reuses the Heading role exactly... Verdict banner description ('Vessel A gives way...'): Body role"). Style the title with the Heading role (`text-2xl font-semibold leading-[1.25]`, `text-foreground`) -- this replaces the old file's `text-[28px] font-semibold` (28px -> 24px is a deliberate, mandatory change per UI-SPEC's "3-4 sizes ceiling has no semantic-justification escape valve" rule, not a discretionary tweak).

    Derive the description via a new local function `verdictBannerDescription(classification): string`: mutual case (`giveWay === null && standOn === null`) returns the fixed mutual-obligation sentence in the behavior block above; give-way case returns `` `${VESSEL_LABEL_TEXT[classification.giveWay]} gives way. Give-way vessel takes early, substantial action; stand-on vessel holds course and speed.` `` (verbatim template from 08-UI-SPEC.md's Typography section). Style with Body role (16px/600, `text-muted-foreground`).

    When `isDegenerate` is true, render the exact verbatim degenerate title/description IN PLACE of the title/description above (same conditional structure as the current file's `{isDegenerate ? ... : ...}` ternary) -- everything else (rule badge suppression aside) stays.

    Render 2 role badges below the title/description, one per vessel. Per CLAUDE.md's "No duplicated JSX for near-identical instances" convention (a prior rotation bug fixed in one vessel's block but not the other motivated this rule), do NOT hand-write two copy-pasted badge blocks -- derive from `(["vesselA", "vesselB"] as const).map(vesselLabel => ...)` (or an extracted small `RoleBadge` subcomponent taking `vesselLabel`/`classification` as props), each showing `ROLE_BADGE_TEXT[role]` styled with `ROLE_BADGE_CLASSNAME[role]` (both from `vessel-role.ts`) where `role = getVesselRole(vesselLabel, classification)` -- unconditional, regardless of `isDegenerate` (exact pre-restyle precedent). Each badge additionally shows the vessel label ("Vessel A"/"Vessel B") above/beside the role text as a 2-line stack per 08-UI-SPEC.md's Typography section ("Vessel A"/"Vessel B" at Label role 13px/400-weight muted, "GIVE WAY"/"STAND ON"/"MUTUAL" at Label role 13px/600 mono uppercase in the role color).

    Compose the whole thing as a full-width `Card`/`CardHeader`/`CardContent` (`@/components/ui/card`) with the rule `Badge` (`@/components/ui/badge`) using a className built from `bg-rule-accent`/`text-white` (per 08-UI-SPEC.md's Color section -- NOT `--primary`, this is the one badge in this file allowed to use `--color-rule-accent`).
  </action>
  <verify>
    <automated>npx vitest run src/components/sandbox/VerdictBanner.test.tsx</automated>
  </verify>
  <done>VerdictBanner.tsx renders title/description/rule-badge/role-badges per the 3 behavior cases above, all covered by VerdictBanner.test.tsx.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: InstrumentReadouts.tsx -- 2x2 Range/Bearing/CPA/TCPA grid + status pill in one card</name>
  <files>src/components/sandbox/InstrumentReadouts.tsx, src/components/sandbox/InstrumentReadouts.test.tsx</files>
  <read_first>
    src/components/sandbox/types.ts
    src/components/sandbox/instrument-readouts.ts
    src/components/sandbox/status-pill.ts
    src/components/hero/HeroPreviewCard.tsx
    src/components/ui/card.tsx
  </read_first>
  <behavior>
    - Given `crossingResidualBasicCase`'s vessels (from `classify-encounter.fixtures.ts`) and its real `ClassificationResult`, renders 4 tiles labeled `RANGE`/`BEARING A→B`/`CPA`/`TCPA` with values formatted `"5.00 NM"` / `"090°"` (3-digit zero-padded) / a real `.toFixed(2)` NM value / a real `.toFixed(1)` min value, plus a status pill below the grid reading `"Passing clear — CPA {X.XX} NM on present courses."` or `"Risk of collision — CPA {X.XX} NM on present courses."` depending on `classification.riskOfCollision`.
    - Degenerate case (`isDegenerate: true`, coincident-position vessels): `BEARING`/`CPA`/`TCPA` tiles render `"—"` placeholders (their `deriveInstrumentReadouts()` values are `null`), `RANGE` still renders `"0.00 NM"` (always computable via `Math.hypot`), and the status pill renders `"—"` in place of its numeric CPA segment.
    - The status pill is the LAST child inside this same `Card` -- never rendered as a separate sibling card (08-UI-SPEC.md: "the status pill is NOT a separate card").
  </behavior>
  <action>
    Call `deriveInstrumentReadouts(vesselA, vesselB)` (from `./instrument-readouts.js`) once at the top of the component to get `{ rangeNm, bearingAtoBDegrees, cpaNm, tcpaMinutes }`. Call `statusPillCopy(classification.riskOfCollision, cpaNm)` (from `./status-pill.js`, implementing CONTEXT.md D-04) for the pill's `{ text, tone }`.

    Render a 2x2 CSS grid (`grid grid-cols-2 gap-2`, matching `HeroPreviewCard.tsx`'s existing 3-tile grid pattern extended to 4 tiles) of tiles: each tile is a `rounded-md border border-border px-2.5 py-2.25` div containing a Label-role uppercase tile-label (`RANGE`/`BEARING A→B`/`CPA`/`TCPA`, `font-mono text-[9.5px] font-semibold tracking-wide text-muted-foreground uppercase`) and a Body-role mono value (`font-mono text-[15px] font-semibold text-foreground`) -- reuse `HeroPreviewCard.tsx`'s exact tile markup shape as the analog, do not invent a new tile style.

    Format each value: `rangeNm.toFixed(2)` + `" NM"` (always present, never null); `bearingAtoBDegrees === null ? "—" : `${Math.round(bearingAtoBDegrees).toString().padStart(3, "0")}°`` (matching `HeroPreviewCard.tsx`'s exact bearing-format precedent); `cpaNm === null ? "—" : `${cpaNm.toFixed(2)} NM`` ; `tcpaMinutes === null ? "—" : `${tcpaMinutes.toFixed(1)} min`` (matching `ReasoningPanel.tsx`'s existing `formatFactValue` precedent for `tcpaMinutes`, one decimal + " min").

    Render the status pill below the grid: a small pill (`inline-flex items-center gap-1 rounded-full px-2 py-1`) with a leading 4px solid dot and the `statusPillCopy().text`, colored per `tone` -- `"clear"` uses Tailwind `green-400` at the badge-shade-lighter convention (`text-green-400`, `bg-green-400/10`, `border-green-400/35`), `"risk"` uses `red-400` (`text-red-400`, `bg-red-400/10`, `border-red-400/35`) -- per 08-UI-SPEC.md's Status Pill color table (these are plain Tailwind default-palette utilities, NOT new `@theme` tokens -- the design's own one-step-lighter-than-the-badge convention, distinct from the give-way/stand-on badge shades).

    Compose the whole thing inside one `Card`/`CardContent` (`@/components/ui/card`) -- grid first, status pill last child, same card.
  </action>
  <verify>
    <automated>npx vitest run src/components/sandbox/InstrumentReadouts.test.tsx</automated>
  </verify>
  <done>InstrumentReadouts.tsx renders the 4-tile grid + status pill in one card, correct formatting and degenerate placeholders, covered by InstrumentReadouts.test.tsx.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ReasoningTrail.tsx -- dynamic numbered steps, tag/tone-per-position, doubt caveat; retire ReasoningPanel</name>
  <files>src/components/sandbox/ReasoningTrail.tsx, src/components/sandbox/ReasoningTrail.test.tsx, src/components/sandbox/ReasoningPanel.tsx, src/components/sandbox/ReasoningPanel.test.tsx</files>
  <read_first>
    src/components/sandbox/ReasoningPanel.tsx
    src/components/sandbox/ReasoningPanel.test.tsx
    src/components/sandbox/reasoning-trail-tag.ts
    src/components/ui/card.tsx
    src/components/ui/badge.tsx
  </read_first>
  <behavior>
    - Given `crossingClassification`'s 3-entry trail (`Rule 7`, `Rule 13(a)-(b)`, `Rule 15`, in that exact order -- the current `ReasoningPanel.test.tsx`'s fixture), renders exactly 3 `<li>`s, in the same order, each still showing the original `ruleId`/`text`/fact-readout content (SBOX-03: no content/order change), tagged `GEOMETRY` (index 0), `RULE 13` (index 1 -- NOT the classifying entry here since `classifyingEntryIndex(3) === 1`... verify: for THIS 3-entry array `classifyingEntryIndex(3) = 1`, so index 1 IS the classifying entry -- tag becomes `RULE 13` since `doubt: false`), `VERDICT` (index 2, last). Header shows `"3 steps"`.
    - Given a 5-entry crossing trail with `doubt: true` (construct via `withDoubt(...)` harness, mirroring the current test file's pattern), the classifying entry (index `length - 2 === 3`) is tagged `Rule 7` instead of its own `ruleId`-derived tag, while every OTHER middle entry (index 1, 2) still shows its own `RULE N` tag.
    - A trail entry with empty `facts: {}` (the `Rule 13(d)` sticky-overtaking case) renders no `<dl>` fact-readout block for that entry; entries with non-empty facts still render one.
    - Doubt caveat: when `classification.doubt && classification.doubtBoundary`, renders the exact verbatim caveat sentence for that boundary below the numbered list (unchanged copy/position from the current file).
    - Degenerate state: this component receives `classification` only (no `isDegenerate` prop) -- it always renders the last-good trail regardless of any parent-level degenerate flag, matching the pre-restyle precedent exactly ("shows the degenerate note while still rendering the last-good trail" -- the note itself now lives in `VerdictBanner`, not here).
  </behavior>
  <action>
    Move `FACT_LABEL`, `formatFactValue`, `FactReadout`, and `DOUBT_CAVEAT_TEXT` verbatim from `ReasoningPanel.tsx` into this new file (unchanged logic/copy, single remaining consumer).

    Compute, once per render: `const classifyingIndex = classifyingEntryIndex(classification.trail.length);` (from `./reasoning-trail-tag.js`). For each `entry` at `index` in `classification.trail`, derive its tag/tone via a local function, e.g. `trailStepTag(entry, index, trailLength, doubt): { tag: string; tone: "geometry" | "rule" | "verdict" }`:
    - `index === 0` -> `{ tag: "GEOMETRY", tone: "geometry" }` (tone maps to `--color-geometry`)
    - `index === trailLength - 1` (last) -> `{ tag: "VERDICT", tone: "verdict" }` (tone maps to `--color-give-way`, per 08-UI-SPEC.md's explicit "always this color regardless of which vessel actually gives way")
    - `index === classifyingIndex && doubt` -> `{ tag: "Rule 7", tone: "rule" }` (tone maps to `--color-rule-accent`)
    - otherwise (any other middle entry, including the classifying entry when `doubt` is false) -> `{ tag: `RULE ${ruleNumber(entry.ruleId)}`, tone: "rule" }`

    Render each step as a numbered circle (Label role, 13px/600, white text on the step's tone color -- `bg-geometry`/`bg-rule-accent`/`bg-give-way` per tone) + the tag pill (Label role, 13px/600, uppercase, colored per tone) + the entry's own `ruleId`/`text` (unchanged, Body role) + `FactReadout` (unchanged) -- connected by a `border-l border-border` vertical line down the left edge between steps (a simple `<ol>` with each `<li>` positioned relative to a shared connecting line, or a per-`<li>` `border-l-2` on a wrapping column -- component structure is this task's discretion, dictated only by "matches the design 1:1").

    Render a card header reading `"REASONING TRAIL"` + an outline `Badge` pill reading `` `${classification.trail.length} steps` `` (dynamic count, per 08-UI-SPEC.md's Copywriting Contract -- never hardcode `"3 steps"`).

    Render the doubt caveat (unchanged copy/logic) as the last element inside the same card, below the numbered list.

    Delete `ReasoningPanel.tsx` and `ReasoningPanel.test.tsx` entirely once `VerdictBanner`/`InstrumentReadouts`/`ReasoningTrail` (this plan, all 3 tasks) collectively cover every behavior the old file's test suite asserted -- cross-check against the deleted test file's assertions one more time before removing it (verdict banner text, MUTUAL badge exclusivity, trail order/content, fact-readout presence/absence, doubt caveats x2, degenerate note + persisted trail) to confirm no behavior was dropped, only relocated.
  </action>
  <acceptance_criteria>
    - `src/components/sandbox/ReasoningPanel.tsx` and `src/components/sandbox/ReasoningPanel.test.tsx` no longer exist (source assertion: `test -f src/components/sandbox/ReasoningPanel.tsx` exits non-zero)
    - `grep -rn "ReasoningPanel" src/components/sandbox/*.tsx src/components/sandbox/*.ts` (excluding the deleted files themselves) returns 0 matches -- no dangling import anywhere
    - `npx vitest run src/components/sandbox/ReasoningTrail.test.tsx` passes, covering: dynamic step count (3 and 5+ entry cases), tag derivation for all 3 tone buckets, the doubt-substitution override, empty-facts suppression, both doubt-caveat strings verbatim
    - `npm run typecheck` reports zero errors across the whole `src/components/sandbox/` directory
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run src/components/sandbox/ReasoningTrail.test.tsx</automated>
  </verify>
  <done>ReasoningTrail.tsx renders the dynamic-length numbered trail with correct tag/tone derivation; ReasoningPanel.tsx/test.tsx retired with zero behavior loss.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `classification.trail[].facts` -> rendered text | Domain-derived data, not user input, but still passes through `formatFactValue`'s `switch` before rendering |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-08-05 | Information Disclosure / Tampering | `FactReadout`/`formatFactValue` rendering `classification.trail[].facts` | accept | Facts originate exclusively from `classifyEncounter()`'s own typed `ReasoningTrailEntry.facts: Record<string, number \| string \| boolean>` -- never raw user text, no HTML/script injection surface (React's default JSX text-node escaping applies to every value rendered here, unchanged from the pre-split component) |

No new trust boundary, external dependency, or user input surface -- this plan is a pure decomposition/restyle of already-rendered, already-typed domain output.
</threat_model>

<verification>
- `npx vitest run src/components/sandbox/VerdictBanner.test.tsx src/components/sandbox/InstrumentReadouts.test.tsx src/components/sandbox/ReasoningTrail.test.tsx` all pass
- `grep -rn "ReasoningPanel" src/` returns 0 matches
- `npm run typecheck` clean
</verification>

<success_criteria>
- 3 new cards replace the single ReasoningPanel aside, matching the design 1:1
- Reasoning trail step count/tags are always derived from real data, never hardcoded
- Doubt/Rule 7 badge treatment is generic across both the banner and the trail
- Zero behavior regression versus the retired ReasoningPanel.test.tsx's coverage
</success_criteria>

<output>
Create `.planning/phases/08-sandbox/08-04-SUMMARY.md` when done
</output>
