---
phase: 07-hero
plan: 2
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: []
autonomous: false
requirements: [HERO-01, HERO-02, HERO-03, HERO-04]

must_haves:
  truths:
    - "A human confirms the Hero section visually matches Main-Design.png (headline two-tone color split, eyebrow badge, CTAs, trust note, preview card) in a real browser"
    - "A human confirms the preview card is fully static — no pulsing dot, no animated bearing line, no motion of any kind"
    - "A human confirms the Hero collapses to a single column (text stacked above the preview card) below 900px, and the headline visibly scales down below 640px"
    - "A human confirms clicking each CTA smooth-scrolls to #sandbox / attempts #gallery with no console errors"
  artifacts: []
  key_links: []
---

<objective>
Manually verify, in a real browser, the visual fidelity, static (non-animated) preview card, responsive breakpoint collapse, and CTA anchor-scroll behavior that Plan 07-01 built — jsdom cannot render real layout/media-query behavior or confirm pixel-level visual fidelity against `Main-Design.png` (per 07-RESEARCH.md's Validation Architecture, which marks HERO-04's responsive behavior and the SVG's visual fidelity as explicitly "manual-only").

Purpose: Catch exactly the class of bug automated tests cannot see: a Hero that type-checks and passes RTL assertions but doesn't actually match the design mock's proportions/colors, doesn't actually collapse at the right breakpoint, or has an accidental animation/motion on the "illustrative" preview card that CONTEXT.md D-01 explicitly locks against.
Output: Human sign-off (or a list of concrete issues to route back to Plan 07-01's tasks).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-hero/07-CONTEXT.md
@.planning/phases/07-hero/07-UI-SPEC.md
@.planning/design/Main-Design.png
@.planning/phases/07-hero/07-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Manual browser verification of Hero visual fidelity, static preview card, responsive breakpoints, and CTA scroll behavior</name>
  <read_first>
    src/components/hero/Hero.tsx
    app/page.tsx
    app/globals.css
    .planning/phases/07-hero/07-UI-SPEC.md (Visual Focal Point, Typography, Color, Spacing Scale, Executor Notes)
  </read_first>
  <files></files>
  <action>
    No code changes in this task — it is a pure human-verification gate over Plan 07-01's already-implemented Hero. Perform the 7 browser checks listed in `<how-to-verify>` below and report back "approved" or a concrete list of issues.
  </action>
  <what-built>
    Plan 07-01 built `Hero.tsx` (headline/copy/CTAs, static SVG "Live classification" preview card driven by a real `classifyEncounter()` call), wired `app/page.tsx` to render it above a new `#sandbox`-anchored `SandboxContainer` wrapper, and added `scroll-behavior: smooth` (reduced-motion guarded) plus a `--breakpoint-hero: 900px` token (or its arbitrary-value fallback) to `app/globals.css`. None of this has been visually confirmed against `Main-Design.png` in a real browser yet.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` (webpack dev server) and open `http://localhost:3000`. Compare the Hero section side-by-side against `.planning/design/Main-Design.png`'s top section: headline two-tone color split ("Two vessels. One rulebook." white, "See who gives way — and why." teal), eyebrow badge, body copy, both CTA buttons (filled + outline), trust note with checkmark icon, and the right-hand "Live classification" preview card — specifically confirm: two concentric range rings and a shaded teal bearing sector centered on the chart's fixed canvas center point (not on Vessel A's own position), a north reference line from that same fixed center, a red dashed heading-vector line trailing Vessel A and a green dashed heading-vector line trailing Vessel B, a solid (not dashed) gray connector line between the two vessels with a floating "2.99 NM" distance-label chip on it, a uniform grid background (not discrete tick marks), and both vessel triangles with their "A"/"B" circular labels and "GW"/"SO" role badges.
    2. Watch the preview card for at least 5 seconds. Confirm nothing animates or pulses — no moving dot, no animated bearing line, no transition on load (CONTEXT.md D-01).
    3. Resize the browser window (or DevTools responsive mode) to below 900px width. Confirm the Hero collapses to a single column with the headline/copy/CTAs/trust-note stacked ABOVE the preview card (text-first order, per CONTEXT.md D-04) — not the preview card first.
    4. Continue resizing below 640px width. Confirm the headline visibly scales down (smaller font size) while staying legible and keeping its two-tone color split.
    5. At full desktop width, click "Open the sandbox". Confirm the page smooth-scrolls down to the Sandbox section (the interactive chart tool), landing with the sticky Header's ~64px offset clear of the top (not overlapping the Sandbox content).
    6. Click "Classic encounters". Confirm the browser attempts to scroll to `#gallery` with no JavaScript console error (an inert no-op is expected and correct — Phase 9 has not shipped a real `#gallery` section yet, per CONTEXT.md D-06).
    7. Open the browser DevTools console. Confirm there are no errors or warnings referencing `Hero`, a missing `lucide-react` icon export, or an uncompiled Tailwind class.
  </how-to-verify>
  <acceptance_criteria>
    - Hero's visual layout, colors, typography, and copy match `Main-Design.png` closely (headline two-tone split, eyebrow badge, CTAs, trust note, preview card detail: two range rings, shaded bearing sector, dashed heading vectors, solid distance-labeled connector, grid background)
    - Preview card shows zero animation/motion over a 5+ second observation window
    - Single-column collapse (text stacked above preview card) occurs below 900px width
    - Headline visibly scales down below 640px width, staying legible
    - "Open the sandbox" smooth-scrolls to the Sandbox section, clearing the sticky Header
    - "Classic encounters" attempts `#gallery` with no console error
    - No console errors/warnings referencing Hero, missing icons, or uncompiled Tailwind classes
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues (e.g. "preview card still first below 900px" or "headline doesn't shrink below 640px").</resume-signal>
  <verify>
    <manual>All 7 steps in how-to-verify completed with no issues found; human types "approved"</manual>
  </verify>
  <done>Human tester confirms visual fidelity against Main-Design.png, zero preview-card animation, correct responsive collapse at 900px/640px, correct CTA scroll behavior, and no console errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries or dependencies are introduced by this plan — it is a verification-only checkpoint of Plan 07-01's already-mitigated, zero-authentication-surface Hero implementation (per 07-RESEARCH.md's Security Domain conclusion: "Not applicable... zero new authentication, session, access-control, input-validation, or cryptography surface").

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-------------------|
| T-07-SC | Tampering | npm installs (carried from Plan 07-01) | accept | No new installs occur in this plan — restated for traceability; see Plan 07-01's threat model for the full (empty) Package Legitimacy Audit |
| T-07-04 | N/A | Hero (whole surface) | accept | This phase has no in-scope STRIDE category beyond the supply-chain row already carried from Plan 07-01 — Hero is static, non-interactive, server-rendered marketing content with zero authentication/session/access-control/input-validation/cryptography surface |
</threat_model>

<verification>
This plan has no automated verification beyond the human checkpoint above — jsdom cannot render real CSS layout/media-query behavior, confirm pixel-level visual fidelity against a design mock, or observe motion/animation over time (the exact reason this is a mandatory human checkpoint per 07-RESEARCH.md's Validation Architecture, which marks HERO-04's responsive behavior as "manual-only").
</verification>

<success_criteria>
- Human tester types "approved" after completing all 7 verification steps above with no issues found
- If issues are found, they are routed back to Plan 07-01's relevant task (Task 2 for markup/SVG/copy issues, Task 3 for wiring/breakpoint/scroll issues) for a fix before Phase 7 is considered complete
</success_criteria>

<output>
Create `.planning/phases/07-hero/07-02-SUMMARY.md` when done
</output>
</content>
</output>
