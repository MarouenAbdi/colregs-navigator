---
quick_id: 260720-kg5
type: quick
files_modified: [eslint.config.mjs, eslint-rules/no-stale-id-comments.mjs, eslint-suppressions.json]
autonomous: true
must_haves:
  truths:
    - "eslint.config.mjs no longer imports or registers the local/no-stale-id-comments rule"
    - "eslint-rules/no-stale-id-comments.mjs no longer exists on disk"
    - "eslint-suppressions.json contains zero local/no-stale-id-comments keys, and no file entry is left as a dangling empty object as a result"
    - "npm run lint exits 0 with no error referencing local/no-stale-id-comments, and no new errors were introduced as a side effect of the edit"
  artifacts:
    - path: "eslint.config.mjs"
      provides: "Flat ESLint config with the local/no-stale-id-comments plugin block and its import removed; all other rule blocks (Next.js, better-tailwindcss, vitest, domain-boundary no-restricted-imports, max-lines, no-restricted-syntax) untouched"
    - path: "eslint-suppressions.json"
      provides: "Suppressions file pruned of every local/no-stale-id-comments entry, still valid JSON, no empty top-level file objects"
  key_links:
    - from: "eslint.config.mjs"
      to: "eslint-rules/no-stale-id-comments.mjs"
      via: "import statement (now removed)"
      pattern: "no-stale-id-comments"
---

<objective>
Delete the custom `local/no-stale-id-comments` ESLint rule entirely -- the
rule, its registration in `eslint.config.mjs`, and every suppression entry
for it in `eslint-suppressions.json`. This is a removal, not a fix: none of
the comments the rule was flagging are touched.

Purpose: the rule fires on every rotting Phase/Plan/REQ-ID-style comment
reference across the codebase and the user has decided to stop enforcing
this convention via lint rather than rewrite the flagged comments.

Output: `eslint.config.mjs` and `eslint-suppressions.json` updated,
`eslint-rules/no-stale-id-comments.mjs` deleted, `npm run lint` clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@eslint.config.mjs
@eslint-rules/no-stale-id-comments.mjs
@eslint-suppressions.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the rule's registration from eslint.config.mjs and delete the rule file</name>
  <files>eslint.config.mjs, eslint-rules/no-stale-id-comments.mjs</files>
  <action>
In `eslint.config.mjs`:
1. Delete line 6, `import noStaleIdComments from "./eslint-rules/no-stale-id-comments.mjs";`.
2. Delete the entire flat-config object block that registers the rule (currently lines 104-113 in the file as read) -- this is the block whose leading comment reads "Local repo rule (ESLint's own flat-config replacement for the deprecated --rulesdir flag) operationalizing CLAUDE.md's..." and whose body is `{ plugins: { local: { rules: { "no-stale-id-comments": noStaleIdComments } } }, rules: { "local/no-stale-id-comments": "error" } }`. Remove the comment, the object, and its trailing comma -- do not leave an empty `{}` entry in the `defineConfig([...])` array.
3. Leave every other block in the array untouched (Next.js core-web-vitals, babel parser block, better-tailwindcss block, vitest block, domain-boundary no-restricted-imports/max-lines block, no-restricted-syntax block, globalIgnores).

Delete `eslint-rules/no-stale-id-comments.mjs`. There is no co-located test
file for this rule (confirmed: `eslint-rules/` contains only this one file)
-- nothing else to delete there. Do not delete the now-empty `eslint-rules/`
directory itself; leaving it is harmless and out of scope.
  </action>
  <verify>
    <automated>grep -c "no-stale-id-comments\|noStaleIdComments" eslint.config.mjs | grep -qx 0 && echo OK; test ! -f eslint-rules/no-stale-id-comments.mjs && echo FILE_GONE</automated>
  </verify>
  <done>
`eslint.config.mjs` contains zero references to `no-stale-id-comments` or
`noStaleIdComments` (import and registration block both gone), the file
still exports a valid `defineConfig([...])` array with every other block
intact, and `eslint-rules/no-stale-id-comments.mjs` no longer exists.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prune eslint-suppressions.json and verify lint is clean</name>
  <files>eslint-suppressions.json</files>
  <action>
Remove every `"local/no-stale-id-comments"` key from `eslint-suppressions.json`.
Per the current file contents, this means:

Remove the entire top-level file entry (key + object) for these files, since
`local/no-stale-id-comments` is their only rule key and removing it would
otherwise leave a dangling empty `{}`:
`eslint-rules/no-stale-id-comments.mjs`, `next.config.ts`,
`src/components/gallery/GalleryContainer.tsx`,
`src/components/gallery/card/GalleryCard.tsx`,
`src/components/gallery/chart/GalleryPreviewChart.tsx`,
`src/components/hero/Hero.tsx`, `src/components/hero/HeroPreviewCard.tsx`,
`src/components/hero/hero-preview-fixture.ts`,
`src/components/hero/hero-preview-geometry.ts`,
`src/components/sandbox/SandboxContainer.test.tsx`,
`src/components/sandbox/chart/ChartPanel.tsx`,
`src/components/sandbox/chart/chart-panel-geometry.ts`,
`src/components/sandbox/control-panel/ControlPanel.test.tsx`,
`src/components/sandbox/hooks/useHullDrag.ts`,
`src/components/sandbox/hooks/useRotateHandleDrag.ts`,
`src/components/sandbox/hooks/useSandboxState.ts`,
`src/components/sandbox/reasoning/ReasoningTrail.tsx`,
`src/components/sandbox/reasoning/VerdictBanner.tsx`,
`src/components/sandbox/vessel-role.ts`,
`src/domain/colregs/classify-encounter.fixtures.ts`,
`src/domain/colregs/risk-of-collision/risk-of-collision.ts`,
`src/domain/colregs/types.ts`,
`src/domain/colregs/vessel-priority/vessel-priority.fixtures.ts`,
`src/domain/colregs/vessel-priority/vessel-priority.ts`,
`src/server/api/routers/scenario.test.ts`,
`src/server/application/scenario-service.test.ts`,
`src/server/application/scenario-service.ts`,
`src/server/db/curated-scenarios.test.ts`,
`src/server/db/curated-scenarios.ts`, `vitest.config.ts`, `vitest.setup.ts`.

Remove only the `"local/no-stale-id-comments"` key (keep the file entry and
its other rule keys) for: `src/components/hero/hero-preview-fixture.test.ts`
(keeps its `vitest/no-conditional-expect` key).

Leave completely untouched (no `local/no-stale-id-comments` key present):
`app/layout.tsx`, `src/components/shared/SectionGridBackground.tsx`,
`src/components/sandbox/chip-scenarios.test.ts`,
`src/domain/colregs/classify-encounter.test.ts`,
`src/domain/colregs/doubt-geometry/resolve-doubt-geometry.test.ts`,
`src/domain/colregs/risk-of-collision/risk-of-collision.test.ts`,
`src/domain/geometry/bearing/bearing.test.ts`,
`src/domain/geometry/cpa/cpa.test.ts`,
`src/domain/geometry/relative-bearing/relative-bearing.test.ts`,
`src/domain/shared/result.test.ts`,
`src/components/sandbox/instruments/instrument-readouts.test.ts`.

Keep the JSON valid (no trailing commas, no dangling empty objects) -- if in
doubt, re-derive the final shape by reading the current file, applying the
removals above, and writing back a well-formed JSON object.

Then run `npm run lint` from the repo root and confirm it exits 0 with no
`local/no-stale-id-comments` error anywhere in the output, and no other new
error was introduced as a side effect of these edits (compare error count/
content to what's expected given the untouched suppressions above).
  </action>
  <verify>
    <automated>node -e "const s=require('./eslint-suppressions.json'); const bad=Object.entries(s).filter(([f,r])=>Object.keys(r).includes('local/no-stale-id-comments')); if(bad.length){console.error('still present:',bad.map(b=>b[0]));process.exit(1)} console.log('suppressions clean')" &amp;&amp; npm run lint</automated>
  </verify>
  <done>
`eslint-suppressions.json` is valid JSON, contains zero
`local/no-stale-id-comments` keys anywhere, has no dangling empty
top-level file objects, and `npm run lint` exits 0.
  </done>
</task>

</tasks>

<verification>
`grep -c "no-stale-id-comments" eslint.config.mjs` returns 0. `test -f
eslint-rules/no-stale-id-comments.mjs` fails (file absent). `node -e
"JSON.parse(require('fs').readFileSync('eslint-suppressions.json','utf8'))"`
exits 0 and contains no `local/no-stale-id-comments` substring. `npm run
lint` exits 0.
</verification>

<success_criteria>
- The `local/no-stale-id-comments` rule is fully removed: no import, no
  registration block, no rule source file, no suppression entries.
- `eslint.config.mjs`'s other rule blocks (Next.js, better-tailwindcss,
  vitest, domain-boundary imports, max-lines, no-restricted-syntax) are
  byte-identical to before this change.
- `eslint-suppressions.json` stays valid JSON with no dangling empty file
  objects.
- `npm run lint` passes with 0 errors.
- No comment text anywhere in the codebase was edited -- this plan touches
  only `eslint.config.mjs`, `eslint-rules/no-stale-id-comments.mjs` (deleted),
  and `eslint-suppressions.json`.
</success_criteria>

<output>
Create `.planning/quick/260720-kg5-remove-local-no-stale-id-comments-eslint/260720-kg5-SUMMARY.md` when done.
</output>
